OpenAI Codex v0.116.0 (research preview)
--------
workdir: /Users/kwak/Projects/paperclip/paperclip-addon
model: gpt-5.3-codex
provider: openai
approval: never
sandbox: workspace-write [workdir, /tmp, $TMPDIR, /Users/kwak/.codex/memories]
reasoning effort: high
reasoning summaries: none
session id: 019d1e02-c31b-79f2-8502-48eef279a07d
--------
user
아래 Paperclip Workflow Engine Plugin의 코드 리뷰 결과를 반영하여 수정하세요.

프로젝트 경로: /Users/kwak/Projects/paperclip/paperclip-addon/plugins/workflow-engine/

## Critical 수정 3건

### 1. activateBacklogStep()에 이슈 생성 추가
파일: src/worker.ts의 activateBacklogStep() 함수

현재: step을 todo로 전환하고 agent를 invoke만 함
필요: ctx.issues.create()로 Paperclip 이슈를 생성하고, 생성된 issueId를 stepRun에 저장

```typescript
async function activateBacklogStep(...) {
  // 1. Paperclip 이슈 생성
  const issue = await ctx.issues.create(companyId, {
    title: `[${workflowName}] ${stepDef.title}`,
    description: getStepDescription(stepDef) ?? `Workflow step: ${stepDef.id}`,
    assigneeAgentId: resolvedAgent.agentId,
    status: "todo",
  });
  
  // 2. stepRun에 issueId 저장
  await updateStepRun(ctx, stepRunRecord.id, {
    status: STEP_STATUSES.todo,
    issueId: issue.id,
    startedAt: new Date().toISOString(),
  });
  
  // 3. agent invoke (이슈 할당으로 자동 wakeup되므로 선택적)
}
```

### 2. startWorkflow() 함수 추가
파일: src/worker.ts에 새 함수 추가 + data handler로 등록

workflow를 시작하는 진입점:
- WorkflowRun entity 생성 (status: "running")
- 모든 step에 대해 WorkflowStepRun entity 생성 (status: "backlog")
- agent name → agentId resolve (ctx.agents.list + name 매칭)
- dependsOn이 빈 step들 → activateBacklogStep() 호출 (이슈 생성 + todo + wakeup)
- parent issue 생성 (optional, workflow 전체를 묶는 이슈)

data handler로 등록하여 UI나 webhook에서 호출 가능하게:
```typescript
ctx.data.handle("start-workflow", async (params) => {
  // params: { workflowId, companyId }
  return startWorkflow(ctx, params.workflowId, params.companyId);
});
```

### 3. ctx.entities.get(id) 대신 안전한 조회 패턴
파일: src/workflow-store.ts

getEntityByType() 함수에서 ctx.entities.get(id) 후 entityType 체크하는 방식 대신,
가능하면 ctx.entities.list({ entityType, filters }) 패턴 사용.
ctx.entities.list가 externalId 필터를 지원하지 않으면 현재 방식 유지하되 주석으로 한계 명시.

## Important 수정 4건

### 4. 중복 헬퍼 → shared utils 추출
새 파일: src/workflow-utils.ts 생성

worker.ts와 reconciler.ts에 중복된 함수들을 추출:
- toWorkflowRunRecord, toWorkflowStepRunRecord, toWorkflowDefinitionRecord
- findStepDefinition
- getStepAgentName / getStepAgentNameHint
- TERMINAL_STEP_STATUSES

양쪽 파일에서 import로 교체.

### 5. parentId filler 구현
파일: src/worker.ts의 setup()에 issue.created 이벤트 핸들러 추가

```typescript
ctx.events.on("issue.created", async (event) => {
  // 이슈에 parentId가 없고 assigneeAgentId가 있으면
  // agent metadata에서 defaultParentIssueId 읽기
  // ctx.issues.update(issueId, { parentId }) 호출
});
```

agent metadata 접근: ctx.agents.get(agentId, companyId) → metadata.defaultParentIssueId

### 6. Agent Sessions 연동
파일: src/worker.ts의 invokeAgentForStep() 수정

step definition에 sessionMode 필드 확인:
- "fresh" (기본): ctx.agents.invoke (현재 동작)
- "reuse": stepRun에 이전 sessionId가 있으면 ctx.agents.sessions.sendMessage, 없으면 sessions.create

### 7. 타입 캐스팅 정리
파일: src/workflow-store.ts

toTypedRecord<T>() 함수에서 data 필드를 런타임 검증하는 guard 추가:
```typescript
function toTypedRecord<T>(record: PluginEntityRecord, entityType: string): TypedEntityRecord<T> {
  if (record.entityType !== entityType) {
    throw new Error(`Expected entity type "${entityType}", got "${record.entityType}"`);
  }
  return record as TypedEntityRecord<T>;
}
```

## 규칙
- Company ID, Agent ID 등 하드코딩 절대 금지. name 기반 resolve.
- ctx.entities 사용 (ctx.state 아님)
- 이벤트 핸들러는 멱등하게 (idempotency key 체크)
- 기존 코드 구조와 스타일 유지
mcp: pencil starting
mcp: stitch starting
mcp: pencil ready
mcp: stitch ready
mcp startup: ready: pencil, stitch
codex
요청하신 7개 수정사항을 기존 구조를 유지하며 반영하겠습니다. 먼저 `workflow-engine` 플러그인 현재 구현을 빠르게 스캔해서 함수 시그니처와 엔티티 스키마를 확인한 뒤, 순서대로 `worker.ts`/`workflow-store.ts`/신규 `workflow-utils.ts`를 패치하겠습니다.
exec
/bin/zsh -lc 'rg --files plugins/workflow-engine' in /Users/kwak/Projects/paperclip/paperclip-addonexec
/bin/zsh -lc 'ls -la' in /Users/kwak/Projects/paperclip/paperclip-addon succeeded in 0ms:
plugins/workflow-engine/src/worker.ts
plugins/workflow-engine/src/reconciler.ts
plugins/workflow-engine/src/index.ts
plugins/workflow-engine/src/manifest.ts
plugins/workflow-engine/src/constants.ts
plugins/workflow-engine/src/dag-engine.ts
plugins/workflow-engine/src/workflow-store.ts
plugins/workflow-engine/src/ui/index.tsx
plugins/workflow-engine/tsconfig.json
plugins/workflow-engine/scripts/build-ui.mjs
plugins/workflow-engine/package.json
 succeeded in 0ms:
total 2240
drwxr-xr-x  18 kwak  staff     576 Mar 24 11:57 .
drwxr-xr-x@ 15 kwak  staff     480 Mar 24 09:13 ..
-rw-r--r--@  1 kwak  staff    6148 Mar 24 09:10 .DS_Store
-rw-r--r--@  1 kwak  staff   10445 Mar 24 11:57 TASKS.md
drwxr-xr-x@  4 kwak  staff     128 Mar 24 10:29 agents
drwxr-xr-x@  5 kwak  staff     160 Mar 23 16:21 docs
drwxr-xr-x@ 14 kwak  staff     448 Mar 24 08:58 logs
-rw-r--r--@  1 kwak  staff  171314 Mar 19 15:48 paperclip-korean-final.png
-rw-r--r--@  1 kwak  staff  180985 Mar 19 15:47 paperclip-korean-ui.png
-rw-r--r--@  1 kwak  staff  172120 Mar 19 15:54 paperclip-light-confirmed.png
-rw-r--r--@  1 kwak  staff  154444 Mar 19 15:49 paperclip-org-chart.png
-rw-r--r--@  1 kwak  staff  350168 Mar 19 16:17 paperclip-running.png
drwxr-xr-x@  5 kwak  staff     160 Mar 24 12:00 plugins
drwxr-xr-x@  3 kwak  staff      96 Mar 23 00:23 qa-artifacts
drwxr-xr-x@  8 kwak  staff     256 Mar 24 08:55 reports
drwxr-xr-x@ 14 kwak  staff     448 Mar 24 10:25 scripts
drwxr-xr-x@  9 kwak  staff     288 Mar 23 13:27 tmp
-rw-r--r--@  1 kwak  staff   83784 Mar 20 16:11 tmp-issues.json
exec
/bin/zsh -lc 'rg -n "activateBacklogStep|startWorkflow|invokeAgentForStep|issue.created|toTypedRecord|getEntityByType|toWorkflowRunRecord|toWorkflowStepRunRecord|toWorkflowDefinitionRecord|findStepDefinition|getStepAgentName|getStepAgentNameHint|STEP_STATUSES|sessions" plugins/workflow-engine/src' in /Users/kwak/Projects/paperclip/paperclip-addon succeeded in 0ms:
plugins/workflow-engine/src/manifest.ts:29:    "agent.sessions.create",
plugins/workflow-engine/src/manifest.ts:30:    "agent.sessions.send",
plugins/workflow-engine/src/manifest.ts:31:    "agent.sessions.close",
plugins/workflow-engine/src/workflow-store.ts:47:function toTypedRecord<T>(record: PluginEntityRecord): TypedEntityRecord<T> {
plugins/workflow-engine/src/workflow-store.ts:65:async function getEntityByType<T>(
plugins/workflow-engine/src/workflow-store.ts:76:  return toTypedRecord<T>(record);
plugins/workflow-engine/src/workflow-store.ts:85:  const record = await getEntityByType<T>(ctx, id, entityType);
plugins/workflow-engine/src/workflow-store.ts:112:  return await getEntityByType<WorkflowDefinition>(
plugins/workflow-engine/src/workflow-store.ts:168:  return await getEntityByType<WorkflowRun>(ctx, id, ENTITY_TYPES.workflowRun);
plugins/workflow-engine/src/workflow-store.ts:224:  return await getEntityByType<WorkflowStepRun>(
plugins/workflow-engine/src/reconciler.ts:3:import { ENTITY_TYPES, RUN_STATUSES, STEP_STATUSES } from "./constants.js";
plugins/workflow-engine/src/reconciler.ts:27:const TERMINAL_STEP_STATUSES = new Set<string>([
plugins/workflow-engine/src/reconciler.ts:28:  STEP_STATUSES.done,
plugins/workflow-engine/src/reconciler.ts:29:  STEP_STATUSES.failed,
plugins/workflow-engine/src/reconciler.ts:30:  STEP_STATUSES.skipped,
plugins/workflow-engine/src/reconciler.ts:31:  STEP_STATUSES.escalated,
plugins/workflow-engine/src/reconciler.ts:39:function toWorkflowRunRecord(record: PluginEntityRecord): WorkflowRunRecord {
plugins/workflow-engine/src/reconciler.ts:43:function toWorkflowStepRunRecord(record: PluginEntityRecord): WorkflowStepRunRecord {
plugins/workflow-engine/src/reconciler.ts:47:function toWorkflowDefinitionRecord(record: PluginEntityRecord): WorkflowDefinitionRecord {
plugins/workflow-engine/src/reconciler.ts:51:function findStepDefinition(definition: WorkflowDefinitionRecord, stepId: string): WorkflowStep | null {
codex
핵심 함수 위치를 확인했고, 이미 `issue.created` 핸들러가 일부 존재합니다. 이제 `worker.ts`와 `workflow-store.ts` 내용을 정밀 확인해서 요구사항과 차이를 맞춘 뒤 정확히 패치하겠습니다.
exec
/bin/zsh -lc "sed -n '260,560p' plugins/workflow-engine/src/worker.ts" in /Users/kwak/Projects/paperclip/paperclip-addonexec
/bin/zsh -lc "sed -n '1,260p' plugins/workflow-engine/src/worker.ts" in /Users/kwak/Projects/paperclip/paperclip-addon succeeded in 0ms:
import {
  definePlugin,
  runWorker,
  type PluginContext,
  type PluginEntityRecord,
  type PluginEvent,
  type PluginJobContext,
} from "@paperclipai/plugin-sdk";
import { JOB_KEYS, RUN_STATUSES, STEP_STATUSES } from "./constants.js";
import {
  getEscalationTarget,
  getNextSteps,
  getRetryInfo,
  type WorkflowStep,
} from "./dag-engine.js";
import {
  checkIdempotency,
  createStepRun,
  findStepRunByIssueId,
  getWorkflowDefinition,
 succeeded in 0ms:

  const nextSteps = getNextSteps(
    typedWorkflowDefinition.data.steps,
    completed,
    failed,
    skipped,
  );
  const stepRunsById = new Map(stepRuns.map((candidate) => [candidate.data.stepId, candidate]));

  for (const stepId of nextSteps.readyStepIds) {
    const stepDef = findStepDefinition(typedWorkflowDefinition, stepId);
    if (!stepDef) {
      ctx.logger.warn("Ready workflow step definition not found", {
        companyId,
        runId: typedWorkflowRun.id,
        stepId,
      });
      continue;
    }

exec
/bin/zsh -lc "sed -n '560,920p' plugins/workflow-engine/src/worker.ts" in /Users/kwak/Projects/paperclip/paperclip-addon succeeded in 0ms:
      completedAt: new Date().toISOString(),
      status: STEP_STATUSES.escalated,
    }));

    await activateEscalationStep(
      ctx,
      typedWorkflowRun,
      typedWorkflowDefinition,
      escalatedStepRun,
      escalationTargetId,
      event.companyId,
    );

    await markIdempotency(ctx, idempotencyKey, event.companyId);
    ctx.logger.warn("Escalated workflow step after agent run failure", {
      companyId: event.companyId,
      escalationTargetId,
      issueId,
      runId: typedWorkflowRun.id,
      stepId: typedStepRun.data.stepId,
codex
`worker.ts`는 현재 요구사항 일부만 구현된 상태입니다. 이어서 `workflow-store.ts`/타입 정의를 확인해서 `issueId/sessionId` 저장 가능 여부와 `ctx.data` 핸들러 API(`register` vs `handle`)를 맞춘 뒤 수정하겠습니다.
exec
/bin/zsh -lc "sed -n '1,260p' plugins/workflow-engine/src/dag-engine.ts" in /Users/kwak/Projects/paperclip/paperclip-addonexec
/bin/zsh -lc "sed -n '1,320p' plugins/workflow-engine/src/workflow-store.ts" in /Users/kwak/Projects/paperclip/paperclip-addon succeeded in 0ms:
export interface WorkflowStep {
  id: string;
  title: string;
  dependsOn: string[];
  onFailure?: "retry" | "skip" | "abort_workflow" | "escalate";
  escalateTo?: string;
  maxRetries?: number;
  triggerOn?: "normal" | "escalation";
}

export interface DagValidationResult {
  valid: boolean;
  errors: string[];
  topologicalOrder: string[];
}

export interface NextStepsResult {
  readyStepIds: string[];
  isWorkflowComplete: boolean;
}
 succeeded in 0ms:
import type { PluginContext, PluginEntityRecord } from "@paperclipai/plugin-sdk";

import { ENTITY_TYPES } from "./constants.js";
import type { WorkflowStep } from "./dag-engine.js";

export interface WorkflowDefinition {
  name: string;
  description: string;
  companyId: string;
  status: "active" | "paused" | "archived";
  steps: WorkflowStep[];
  timeoutMinutes?: number;
  maxConcurrentRuns?: number;
}

export interface WorkflowRun {
  workflowId: string;
  workflowName: string;
  companyId: string;
  status: "running" | "completed" | "failed" | "aborted" | "timed-out";
exec
/bin/zsh -lc "sed -n '1,240p' plugins/workflow-engine/src/constants.ts" in /Users/kwak/Projects/paperclip/paperclip-addon succeeded in 0ms:
export const PLUGIN_ID = "insightflo.workflow-engine";
export const PLUGIN_VERSION = "0.1.0";
export const PAGE_ROUTE = "workflows";

export const SLOT_IDS = {
  page: "workflow-engine-page",
  dashboardWidget: "workflow-engine-dashboard-widget",
} as const;

export const EXPORT_NAMES = {
  page: "WorkflowPage",
  dashboardWidget: "WorkflowDashboardWidget",
} as const;

export const JOB_KEYS = {
  reconciler: "workflow-reconciler",
} as const;

export const ENTITY_TYPES = {
  workflowDefinition: "workflow-definition",
exec
/bin/zsh -lc "sed -n '1,240p' plugins/workflow-engine/src/manifest.ts" in /Users/kwak/Projects/paperclip/paperclip-addon succeeded in 0ms:
import type { PaperclipPluginManifestV1 } from "@paperclipai/plugin-sdk";
import {
  EXPORT_NAMES,
  JOB_KEYS,
  PAGE_ROUTE,
  PLUGIN_ID,
  PLUGIN_VERSION,
  SLOT_IDS,
} from "./constants.js";

const manifest: PaperclipPluginManifestV1 = {
  id: PLUGIN_ID,
  apiVersion: 1,
  version: PLUGIN_VERSION,
  displayName: "Workflow Engine",
  description:
    "DAG-based workflow engine — orchestrates multi-step agent workflows with dependency resolution, reconciliation, and failure policies",
  author: "InsightFlo",
  categories: ["automation"],
  capabilities: [
codex
타입/스토어 구조를 확인했습니다. 다음으로 `reconciler.ts` 중복 헬퍼를 분리할 수 있게 확인하고, `startWorkflow` 호출 경로(UI/webhook에서 쓰는 data API)도 코드베이스 패턴과 일치시켜 구현하겠습니다.
exec
/bin/zsh -lc "sed -n '1,320p' plugins/workflow-engine/src/reconciler.ts" in /Users/kwak/Projects/paperclip/paperclip-addon succeeded in 0ms:
import type { PluginContext, PluginEntityRecord } from "@paperclipai/plugin-sdk";

import { ENTITY_TYPES, RUN_STATUSES, STEP_STATUSES } from "./constants.js";
import {
  getWorkflowDefinition,
  getWorkflowRun,
  listActiveRuns,
  listStepRuns,
  updateStepRun,
  updateWorkflowRun,
  type WorkflowDefinition,
  type WorkflowRun,
  type WorkflowStepRun,
} from "./workflow-store.js";
import type { WorkflowStep } from "./dag-engine.js";

type WorkflowRunRecord = PluginEntityRecord & { data: WorkflowRun };
type WorkflowStepRunRecord = PluginEntityRecord & { data: WorkflowStepRun };
type WorkflowDefinitionRecord = PluginEntityRecord & { data: WorkflowDefinition };
type StepAgentMetadata = WorkflowStep & {
exec
/bin/zsh -lc 'rg -n "start-workflow|workflow-overview|workflow-run-detail" plugins/workflow-engine/src -g"*.ts*"' in /Users/kwak/Projects/paperclip/paperclip-addon succeeded in 0ms:
plugins/workflow-engine/src/worker.ts:747:    ctx.data.register("workflow-overview", async (params: Record<string, unknown>) => {
plugins/workflow-engine/src/worker.ts:785:    ctx.data.register("workflow-run-detail", async (params: Record<string, unknown>) => {
plugins/workflow-engine/src/ui/index.tsx:137:  return usePluginData<OverviewData>("workflow-overview", {
exec
/bin/zsh -lc "rg -n \"ctx\\.data\\.(register|handle)\\(\" plugins -g\"*.ts\"" in /Users/kwak/Projects/paperclip/paperclip-addon succeeded in 0ms:
plugins/workflow-engine/src/worker.ts:747:    ctx.data.register("workflow-overview", async (params: Record<string, unknown>) => {
plugins/workflow-engine/src/worker.ts:785:    ctx.data.register("workflow-run-detail", async (params: Record<string, unknown>) => {
plugins/work-board/src/worker.ts:465:    ctx.data.register("work-board-overview", async (params) => {
exec
/bin/zsh -lc "sed -n '1,320p' plugins/workflow-engine/src/ui/index.tsx" in /Users/kwak/Projects/paperclip/paperclip-addon succeeded in 0ms:
import {
  useHostContext,
  usePluginData,
  type PluginPageProps,
  type PluginWidgetProps,
} from "@paperclipai/plugin-sdk/ui";
import { type CSSProperties, type JSX } from "react";
import { PLUGIN_ID } from "../constants.js";

type WorkflowOverviewData = {
  workflows: Array<{
    id: string;
    name: string;
    description: string;
    status: string;
    steps: Array<{ id: string; title: string; dependsOn: string[] }>;
  }>;
  activeRuns: Array<{
    id: string;
    workflowName: string;
exec
/bin/zsh -lc 'cat plugins/workflow-engine/package.json' in /Users/kwak/Projects/paperclip/paperclip-addonexec
/bin/zsh -lc "rg -n \"interface PluginContext|type PluginContext|data\\.register|data\\.handle|agents\\.sessions\" node_modules @paperclipai -g\"*.d.ts\"" in /Users/kwak/Projects/paperclip/paperclip-addon succeeded in 0ms:
{
  "name": "@insightflo/paperclip-workflow-engine",
  "version": "0.1.0",
  "description": "DAG-based workflow engine plugin for Paperclip — orchestrates multi-step agent workflows with dependency resolution, reconciliation, and failure policies",
  "type": "module",
  "private": true,
  "exports": {
    ".": "./src/index.ts"
  },
  "paperclipPlugin": {
    "manifest": "./dist/manifest.js",
    "worker": "./dist/worker.js",
    "ui": "./dist/ui/"
  },
  "scripts": {
    "build": "tsc && node ./scripts/build-ui.mjs",
    "clean": "rm -rf dist",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
 exited 2 in 0ms:
rg: node_modules: No such file or directory (os error 2)
rg: @paperclipai: No such file or directory (os error 2)
exec
/bin/zsh -lc "rg -n \"interface PluginContext|data\\.register|data\\.handle|agents\\.sessions\" plugins/work-board/src -g\"*.ts\"" in /Users/kwak/Projects/paperclip/paperclip-addon succeeded in 0ms:
plugins/work-board/src/worker.ts:465:    ctx.data.register("work-board-overview", async (params) => {
exec
/bin/zsh -lc 'rg --files | rg "plugin-sdk"' in /Users/kwak/Projects/paperclip/paperclip-addonexec
/bin/zsh -lc "rg -n \"interface PluginContext|type PluginContext|data:\\s*\\{|register\\(|handle\\(|sessions\" packages plugins -g\"*.ts\"" in /Users/kwak/Projects/paperclip/paperclip-addon exited 1 in 0ms:

 exited 2 in 0ms:
rg: packages: No such file or directory (os error 2)
plugins/system-garden/src/worker.ts:1:import { definePlugin, runWorker, type PluginContext } from "@paperclipai/plugin-sdk";
plugins/system-garden/src/worker.ts:543:    context.data.register("system-garden-snapshot", async (params) => {
plugins/system-garden/src/worker.ts:548:    context.data.register("system-garden-agent-detail", async (params) => {
plugins/system-garden/src/cytoscape.d.ts:4:    data: {
plugins/workflow-engine/src/worker.ts:4:  type PluginContext,
plugins/workflow-engine/src/worker.ts:743:    ctx.jobs.register(JOB_KEYS.reconciler, async (_job: PluginJobContext) => {
plugins/workflow-engine/src/worker.ts:747:    ctx.data.register("workflow-overview", async (params: Record<string, unknown>) => {
plugins/workflow-engine/src/worker.ts:785:    ctx.data.register("workflow-run-detail", async (params: Record<string, unknown>) => {
plugins/work-board/src/worker.ts:1:import { definePlugin, runWorker, type PluginContext } from "@paperclipai/plugin-sdk";
plugins/work-board/src/worker.ts:465:    ctx.data.register("work-board-overview", async (params) => {
plugins/workflow-engine/src/workflow-store.ts:314:    data: {
plugins/workflow-engine/src/manifest.ts:29:    "agent.sessions.create",
plugins/workflow-engine/src/manifest.ts:30:    "agent.sessions.send",
plugins/workflow-engine/src/manifest.ts:31:    "agent.sessions.close",
exec
/bin/zsh -lc "rg -n \"ctx\\.issues\\.create\\(\" plugins -g\"*.ts\"" in /Users/kwak/Projects/paperclip/paperclip-addonexec
/bin/zsh -lc "rg -n \"ctx\\.agents\\.sessions\\.|sessions\\.create|sendMessage\" plugins -g\"*.ts\"" in /Users/kwak/Projects/paperclip/paperclip-addon succeeded in 0ms:
plugins/workflow-engine/src/manifest.ts:29:    "agent.sessions.create",
 succeeded in 0ms:
plugins/workflow-engine/src/worker.ts:365:    const issue = await ctx.issues.create({
exec
/bin/zsh -lc "rg -n \"ctx\\.issues\\.update\\(\" plugins -g\"*.ts\"" in /Users/kwak/Projects/paperclip/paperclip-addon exited 1 in 0ms:

codex
SDK 메서드 시그니처가 코드에서 직접 확인되지 않아, 저장소 내 실제 타입 정의를 찾아 `issues.create/update`, `data`, `agent.sessions` 호출 형태를 맞춘 뒤 구현하겠습니다.
exec
/bin/zsh -lc 'ls -la ..' in /Users/kwak/Projects/paperclip/paperclip-addon succeeded in 0ms:
total 64
drwxr-xr-x@ 15 kwak  staff    480 Mar 24 09:13 .
drwxr-xr-x  15 kwak  staff    480 Mar 24 09:11 ..
-rw-r--r--@  1 kwak  staff  10244 Mar 24 09:12 .DS_Store
drwxr-xr-x@  3 kwak  staff     96 Mar 24 07:54 .agents
drwxr-xr-x@  3 kwak  staff     96 Mar 24 07:54 .claude
-rw-r--r--@  1 kwak  staff     89 Mar 24 07:54 .dockerignore
-rw-r--r--@  1 kwak  staff     94 Mar 24 07:54 .env.example
drwxr-xr-x@ 13 kwak  staff    416 Mar 24 12:13 .git
drwxr-xr-x@  4 kwak  staff    128 Mar 24 07:54 .github
-rw-r--r--@  1 kwak  staff    814 Mar 24 07:54 .gitignore
-rw-r--r--@  1 kwak  staff     76 Mar 24 07:54 .mailmap
-rw-r--r--@  1 kwak  staff     24 Mar 24 07:54 .npmrc
drwxr-xr-x@  5 kwak  staff    160 Mar 23 13:21 .paperclip
drwxr-xr-x  18 kwak  staff    576 Mar 24 11:57 paperclip-addon
drwxr-xr-x  33 kwak  staff   1056 Mar 24 11:19 paperclip-orginal
exec
/bin/zsh -lc 'find .. -maxdepth 4 -type d -name "plugin-sdk" | head' in /Users/kwak/Projects/paperclip/paperclip-addon succeeded in 0ms:

exec
/bin/zsh -lc "find . -maxdepth 6 -type f \\( -name \"*.d.ts\" -o -name \"*.ts\" \\) | rg \"plugin-sdk|@paperclipai\"" in /Users/kwak/Projects/paperclip/paperclip-addon exited 1 in 39ms:

exec
/bin/zsh -lc 'rg --files ../paperclip-orginal | rg "plugin-sdk" | head -n 30' in /Users/kwak/Projects/paperclip/paperclip-addon succeeded in 0ms:

exec
/bin/zsh -lc "rg -n \"interface PluginContext|type PluginContext|data\\.(register|handle)|issues\\.create|issues\\.update|agents\\.sessions\" ../paperclip-orginal -g\"*.ts\" -g\"*.d.ts\"" in /Users/kwak/Projects/paperclip/paperclip-addon succeeded in 0ms:
../paperclip-orginal/packages/plugins/examples/plugin-kitchen-sink-example/src/worker.ts:11:  type PluginContext,
../paperclip-orginal/packages/plugins/examples/plugin-kitchen-sink-example/src/worker.ts:251:  ctx.data.register("plugin-config", async () => {
../paperclip-orginal/packages/plugins/examples/plugin-kitchen-sink-example/src/worker.ts:255:  ctx.data.register("overview", async (params) => {
../paperclip-orginal/packages/plugins/examples/plugin-kitchen-sink-example/src/worker.ts:294:  ctx.data.register("companies", async (params) => {
../paperclip-orginal/packages/plugins/examples/plugin-kitchen-sink-example/src/worker.ts:298:  ctx.data.register("projects", async (params) => {
../paperclip-orginal/packages/plugins/examples/plugin-kitchen-sink-example/src/worker.ts:303:  ctx.data.register("issues", async (params) => {
../paperclip-orginal/packages/plugins/examples/plugin-kitchen-sink-example/src/worker.ts:308:  ctx.data.register("goals", async (params) => {
../paperclip-orginal/packages/plugins/examples/plugin-kitchen-sink-example/src/worker.ts:313:  ctx.data.register("agents", async (params) => {
../paperclip-orginal/packages/plugins/examples/plugin-kitchen-sink-example/src/worker.ts:318:  ctx.data.register("workspaces", async (params) => {
../paperclip-orginal/packages/plugins/examples/plugin-kitchen-sink-example/src/worker.ts:325:  ctx.data.register("state-value", async (params) => {
../paperclip-orginal/packages/plugins/examples/plugin-kitchen-sink-example/src/worker.ts:334:  ctx.data.register("entities", async (params) => {
../paperclip-orginal/packages/plugins/examples/plugin-kitchen-sink-example/src/worker.ts:345:  ctx.data.register("comment-context", async (params) => {
../paperclip-orginal/packages/plugins/examples/plugin-kitchen-sink-example/src/worker.ts:368:  ctx.data.register("entity-context", async (params) => {
../paperclip-orginal/packages/plugins/examples/plugin-kitchen-sink-example/src/worker.ts:474:    const issue = await ctx.issues.create({ companyId, projectId, title, description });
../paperclip-orginal/packages/plugins/examples/plugin-kitchen-sink-example/src/worker.ts:477:      source: "issues.create",
../paperclip-orginal/packages/plugins/examples/plugin-kitchen-sink-example/src/worker.ts:498:    const issue = await ctx.issues.update(issueId, { status: status as Issue["status"] }, companyId);
../paperclip-orginal/packages/plugins/examples/plugin-kitchen-sink-example/src/worker.ts:501:      source: "issues.update",
../paperclip-orginal/packages/plugins/examples/plugin-kitchen-sink-example/src/worker.ts:742:    const session = await ctx.agents.sessions.create(agentId, companyId, {
../paperclip-orginal/packages/plugins/examples/plugin-kitchen-sink-example/src/worker.ts:746:    await ctx.agents.sessions.sendMessage(session.sessionId, companyId, {
../paperclip-orginal/packages/plugins/examples/plugin-kitchen-sink-example/src/worker.ts:876:      const issue = await ctx.issues.create({
exec
/bin/zsh -lc "sed -n '1030,1170p' ../paperclip-orginal/packages/plugins/sdk/src/types.ts" in /Users/kwak/Projects/paperclip/paperclip-addon succeeded in 0ms:
 * can subscribe to the same channel concurrently.
 *
 * @example
 * ```ts
 * // Worker: stream chat tokens to the UI
 * ctx.streams.open("chat", companyId);
 * for await (const token of tokenStream) {
 *   ctx.streams.emit("chat", { type: "token", text: token });
 * }
 * ctx.streams.close("chat");
 * ```
 *
 * @see usePluginStream in `@paperclipai/plugin-sdk/ui`
 */
export interface PluginStreamsClient {
  /**
   * Open a named stream channel. Optional — `emit()` implicitly opens if needed.
   * Sends a `stream:open` event to connected UI clients.
   */
  open(channel: string, companyId: string): void;
exec
/bin/zsh -lc "sed -n '900,1030p' ../paperclip-orginal/packages/plugins/sdk/src/types.ts" in /Users/kwak/Projects/paperclip/paperclip-addon succeeded in 0ms:
export interface PluginAgentsClient {
  list(input: { companyId: string; status?: Agent["status"]; limit?: number; offset?: number }): Promise<Agent[]>;
  get(agentId: string, companyId: string): Promise<Agent | null>;
  /** Pause an agent. Throws if agent is terminated or not found. Requires `agents.pause`. */
  pause(agentId: string, companyId: string): Promise<Agent>;
  /** Resume a paused agent (sets status to idle). Throws if terminated, pending_approval, or not found. Requires `agents.resume`. */
  resume(agentId: string, companyId: string): Promise<Agent>;
  /** Invoke (wake up) an agent with a prompt payload. Throws if paused, terminated, pending_approval, or not found. Requires `agents.invoke`. */
  invoke(agentId: string, companyId: string, opts: { prompt: string; reason?: string }): Promise<{ runId: string }>;
  /** Create, message, and close agent chat sessions. Requires `agent.sessions.*` capabilities. */
  sessions: PluginAgentSessionsClient;
}

// ---------------------------------------------------------------------------
// Agent Sessions — two-way chat with agents
// ---------------------------------------------------------------------------

/**
 * Represents an active conversational session with an agent.
 * Maps to an `AgentTaskSession` row on the host.
exec
/bin/zsh -lc "sed -n '740,910p' ../paperclip-orginal/packages/plugins/sdk/src/types.ts" in /Users/kwak/Projects/paperclip/paperclip-addon succeeded in 0ms:
}

// ---------------------------------------------------------------------------
// Plugin metrics
// ---------------------------------------------------------------------------

/**
 * `ctx.metrics` — write plugin-contributed metrics.
 *
 * Requires `metrics.write` capability.
 *
 * @see PLUGIN_SPEC.md §15.1 — Capabilities: Data Write
 */
export interface PluginMetricsClient {
  /**
   * Write a numeric metric data point.
   *
   * @param name - Metric name (plugin-namespaced by the host)
   * @param value - Numeric value
   * @param tags - Optional key-value tags for filtering
exec
/bin/zsh -lc "sed -n '560,760p' ../paperclip-orginal/packages/plugins/sdk/src/protocol.ts" in /Users/kwak/Projects/paperclip/paperclip-addon succeeded in 0ms:
  // Issues
  "issues.list": [
    params: {
      companyId: string;
      projectId?: string;
      assigneeAgentId?: string;
      status?: string;
      limit?: number;
      offset?: number;
    },
    result: Issue[],
  ];
  "issues.get": [
    params: { issueId: string; companyId: string },
    result: Issue | null,
  ];
  "issues.create": [
    params: {
      companyId: string;
      projectId?: string;
exec
/bin/zsh -lc "rg -n \"interface PluginEntitiesClient|externalId|entities\\.list\" ../paperclip-orginal/packages/plugins/sdk/src/types.ts -n" in /Users/kwak/Projects/paperclip/paperclip-addon succeeded in 0ms:
219:  externalId?: string;
229: * A plugin-owned entity record as returned by `ctx.entities.list()`.
243:  externalId: string | null;
257: * Query parameters for `ctx.entities.list()`.
267:  externalId?: string;
588:export interface PluginEntitiesClient {
590:   * Create or update a plugin entity record (upsert by `externalId` within
exec
/bin/zsh -lc "sed -n '540,660p' ../paperclip-orginal/packages/plugins/sdk/src/types.ts" in /Users/kwak/Projects/paperclip/paperclip-addon succeeded in 0ms:
 * await ctx.state.set({ scopeKind: "project", scopeId: projectId, namespace: "linear", stateKey: "cursor" }, cursor);
 * await ctx.state.set({ scopeKind: "project", scopeId: projectId, namespace: "github", stateKey: "last-event" }, eventId);
 * ```
 *
 * `plugin.state.read` capability required for `get()`.
 * `plugin.state.write` capability required for `set()` and `delete()`.
 *
 * @see PLUGIN_SPEC.md §21.3 `plugin_state`
 */
export interface PluginStateClient {
  /**
   * Read a state value.
   *
   * Returns the stored JSON value as-is, or `null` if no entry has been set
   * for this scope+key combination. Falsy values (`false`, `0`, `""`) are
   * returned correctly and are not confused with "not set".
   *
   * @param input - Scope key identifying the entry to read
   * @returns The stored JSON value, or `null` if no value has been set
   */
exec
/bin/zsh -lc 'cat plugins/workflow-engine/tsconfig.json' in /Users/kwak/Projects/paperclip/paperclip-addonexec
/bin/zsh -lc 'cat tsconfig.json' in /Users/kwak/Projects/paperclip/paperclip-addonexec
/bin/zsh -lc 'cat package.json' in /Users/kwak/Projects/paperclip/paperclip-addon succeeded in 0ms:
{
  "compilerOptions": {
    "target": "ES2023",
    "module": "NodeNext",
    "moduleResolution": "NodeNext",
    "outDir": "dist",
    "rootDir": "src",
    "lib": ["ES2023", "DOM"],
    "jsx": "react-jsx",
    "strict": true,
    "esModuleInterop": true,
    "declaration": true,
    "skipLibCheck": true,
    "forceConsistentCasingInFileNames": true,
    "resolveJsonModule": true
  },
  "include": ["src"]
}
 exited 1 in 0ms:
cat: tsconfig.json: No such file or directory
 exited 1 in 0ms:
cat: package.json: No such file or directory
exec
/bin/zsh -lc 'cat plugins/workflow-engine/src/index.ts' in /Users/kwak/Projects/paperclip/paperclip-addon succeeded in 0ms:
export { default as manifest } from "./manifest.js";
export { default as worker } from "./worker.js";
exec
/bin/zsh -lc 'rg -n "createWorkflowDefinition|createWorkflowRun|createStepRun|workflow-definition|workflow-run" plugins/workflow-engine/src -g"*.ts"' in /Users/kwak/Projects/paperclip/paperclip-addon succeeded in 0ms:
plugins/workflow-engine/src/worker.ts:18:  createStepRun,
plugins/workflow-engine/src/worker.ts:292:      stepRun = toStepRunRecord(await createStepRun(ctx, companyId, {
plugins/workflow-engine/src/worker.ts:355:    escalationStepRun = toStepRunRecord(await createStepRun(ctx, companyId, {
plugins/workflow-engine/src/worker.ts:785:    ctx.data.register("workflow-run-detail", async (params: Record<string, unknown>) => {
plugins/workflow-engine/src/constants.ts:20:  workflowDefinition: "workflow-definition",
plugins/workflow-engine/src/constants.ts:21:  workflowRun: "workflow-run",
plugins/workflow-engine/src/workflow-store.ts:94:export async function createWorkflowDefinition(
plugins/workflow-engine/src/workflow-store.ts:150:export async function createWorkflowRun(
plugins/workflow-engine/src/workflow-store.ts:204:export async function createStepRun(
exec
/bin/zsh -lc "sed -n '1,220p' plugins/work-board/src/worker.ts" in /Users/kwak/Projects/paperclip/paperclip-addon succeeded in 0ms:
import { definePlugin, runWorker, type PluginContext } from "@paperclipai/plugin-sdk";
import {
  DEFAULT_COMPANY_ID,
  ISSUE_STATUS_LABELS,
  PLUGIN_DISPLAY_NAME,
  PRIORITY_WEIGHTS,
  WORKSTREAMS,
  type WorkstreamDefinition,
} from "./constants.js";

type BoardBucketKey = "overdueLastWeek" | "todo" | "inProgress" | "doneThisWeek";
type WorkstreamTone = "neutral" | "attention" | "progress" | "complete";
type MatchType = "label" | "keyword";

type IssueRecord = Awaited<ReturnType<PluginContext["issues"]["list"]>>[number];

export type BoardIssueCard = {
  id: string;
  identifier: string | null;
  title: string;
codex
이제 실제 수정을 진행합니다. 먼저 `workflow-utils.ts`를 추가해 중복 헬퍼를 공통화하고, 그 다음 `workflow-store.ts` 타입 가드/조회 패턴을 바꾼 뒤 `worker.ts`에 `startWorkflow`, 이슈 생성, 세션 모드, parentId filler를 한 번에 반영하겠습니다.
file update
A /Users/kwak/Projects/paperclip/paperclip-addon/plugins/workflow-engine/src/workflow-utils.ts
import type { PluginEntityRecord } from "@paperclipai/plugin-sdk";

import { STEP_STATUSES } from "./constants.js";
import type { WorkflowStep } from "./dag-engine.js";
import type {
  WorkflowDefinition,
  WorkflowRun,
  WorkflowStepRun,
} from "./workflow-store.js";

export type WorkflowRunRecord = PluginEntityRecord & { data: WorkflowRun };
export type WorkflowStepRunRecord = PluginEntityRecord & { data: WorkflowStepRun };
export type WorkflowDefinitionRecord = PluginEntityRecord & { data: WorkflowDefinition };
type StepAgentMetadata = WorkflowStep & {
  agent?: string;
  agentName?: string;
  assigneeAgentName?: string;
};

export const TERMINAL_STEP_STATUSES = new Set<string>([
  STEP_STATUSES.done,
  STEP_STATUSES.failed,
  STEP_STATUSES.skipped,
  STEP_STATUSES.escalated,
]);

export function toWorkflowRunRecord(record: PluginEntityRecord): WorkflowRunRecord {
  return record as WorkflowRunRecord;
}

export function toWorkflowStepRunRecord(record: PluginEntityRecord): WorkflowStepRunRecord {
  return record as WorkflowStepRunRecord;
}

export function toWorkflowDefinitionRecord(record: PluginEntityRecord): WorkflowDefinitionRecord {
  return record as WorkflowDefinitionRecord;
}

export function findStepDefinition(definition: WorkflowDefinitionRecord, stepId: string): WorkflowStep | null {
  return definition.data.steps.find((step: WorkflowStep) => step.id === stepId) ?? null;
}

export function getStepAgentNameHint(stepDef: WorkflowStep): string | null {
  const stepMeta = stepDef as StepAgentMetadata;
  const candidates = [stepMeta.agentName, stepMeta.agent, stepMeta.assigneeAgentName];

  for (const candidate of candidates) {
    if (typeof candidate === "string" && candidate.trim()) {
      return candidate.trim();
    }
  }

  return null;
}

export function getStepAgentName(stepRun: WorkflowStepRunRecord, stepDef: WorkflowStep): string | null {
  if (typeof stepRun.data.agentName === "string" && stepRun.data.agentName.trim()) {
    return stepRun.data.agentName.trim();
  }

  return getStepAgentNameHint(stepDef);
}
apply_patch(auto_approved=true) exited 0 in 50ms:
Success. Updated the following files:
A /Users/kwak/Projects/paperclip/paperclip-addon/plugins/workflow-engine/src/workflow-utils.ts
file update:
diff --git a/paperclip-addon/plugins/workflow-engine/src/workflow-utils.ts b/paperclip-addon/plugins/workflow-engine/src/workflow-utils.ts
new file mode 100644
index 0000000000000000000000000000000000000000..ec1cf6f6b9aa722b126b58c0a728b4e538ad1494
--- /dev/null
+++ b/paperclip-addon/plugins/workflow-engine/src/workflow-utils.ts
@@ -0,0 +1,62 @@
+import type { PluginEntityRecord } from "@paperclipai/plugin-sdk";
+
+import { STEP_STATUSES } from "./constants.js";
+import type { WorkflowStep } from "./dag-engine.js";
+import type {
+  WorkflowDefinition,
+  WorkflowRun,
+  WorkflowStepRun,
+} from "./workflow-store.js";
+
+export type WorkflowRunRecord = PluginEntityRecord & { data: WorkflowRun };
+export type WorkflowStepRunRecord = PluginEntityRecord & { data: WorkflowStepRun };
+export type WorkflowDefinitionRecord = PluginEntityRecord & { data: WorkflowDefinition };
+type StepAgentMetadata = WorkflowStep & {
+  agent?: string;
+  agentName?: string;
+  assigneeAgentName?: string;
+};
+
+export const TERMINAL_STEP_STATUSES = new Set<string>([
+  STEP_STATUSES.done,
+  STEP_STATUSES.failed,
+  STEP_STATUSES.skipped,
+  STEP_STATUSES.escalated,
+]);
+
+export function toWorkflowRunRecord(record: PluginEntityRecord): WorkflowRunRecord {
+  return record as WorkflowRunRecord;
+}
+
+export function toWorkflowStepRunRecord(record: PluginEntityRecord): WorkflowStepRunRecord {
+  return record as WorkflowStepRunRecord;
+}
+
+export function toWorkflowDefinitionRecord(record: PluginEntityRecord): WorkflowDefinitionRecord {
+  return record as WorkflowDefinitionRecord;
+}
+
+export function findStepDefinition(definition: WorkflowDefinitionRecord, stepId: string): WorkflowStep | null {
+  return definition.data.steps.find((step: WorkflowStep) => step.id === stepId) ?? null;
+}
+
+export function getStepAgentNameHint(stepDef: WorkflowStep): string | null {
+  const stepMeta = stepDef as StepAgentMetadata;
+  const candidates = [stepMeta.agentName, stepMeta.agent, stepMeta.assigneeAgentName];
+
+  for (const candidate of candidates) {
+    if (typeof candidate === "string" && candidate.trim()) {
+      return candidate.trim();
+    }
+  }
+
+  return null;
+}
+
+export function getStepAgentName(stepRun: WorkflowStepRunRecord, stepDef: WorkflowStep): string | null {
+  if (typeof stepRun.data.agentName === "string" && stepRun.data.agentName.trim()) {
+    return stepRun.data.agentName.trim();
+  }
+
+  return getStepAgentNameHint(stepDef);
+}

file update:
diff --git a/paperclip-addon/plugins/workflow-engine/src/workflow-utils.ts b/paperclip-addon/plugins/workflow-engine/src/workflow-utils.ts
new file mode 100644
index 0000000000000000000000000000000000000000..ec1cf6f6b9aa722b126b58c0a728b4e538ad1494
--- /dev/null
+++ b/paperclip-addon/plugins/workflow-engine/src/workflow-utils.ts
@@ -0,0 +1,62 @@
+import type { PluginEntityRecord } from "@paperclipai/plugin-sdk";
+
+import { STEP_STATUSES } from "./constants.js";
+import type { WorkflowStep } from "./dag-engine.js";
+import type {
+  WorkflowDefinition,
+  WorkflowRun,
+  WorkflowStepRun,
+} from "./workflow-store.js";
+
+export type WorkflowRunRecord = PluginEntityRecord & { data: WorkflowRun };
+export type WorkflowStepRunRecord = PluginEntityRecord & { data: WorkflowStepRun };
+export type WorkflowDefinitionRecord = PluginEntityRecord & { data: WorkflowDefinition };
+type StepAgentMetadata = WorkflowStep & {
+  agent?: string;
+  agentName?: string;
+  assigneeAgentName?: string;
+};
+
+export const TERMINAL_STEP_STATUSES = new Set<string>([
+  STEP_STATUSES.done,
+  STEP_STATUSES.failed,
+  STEP_STATUSES.skipped,
+  STEP_STATUSES.escalated,
+]);
+
+export function toWorkflowRunRecord(record: PluginEntityRecord): WorkflowRunRecord {
+  return record as WorkflowRunRecord;
+}
+
+export function toWorkflowStepRunRecord(record: PluginEntityRecord): WorkflowStepRunRecord {
+  return record as WorkflowStepRunRecord;
+}
+
+export function toWorkflowDefinitionRecord(record: PluginEntityRecord): WorkflowDefinitionRecord {
+  return record as WorkflowDefinitionRecord;
+}
+
+export function findStepDefinition(definition: WorkflowDefinitionRecord, stepId: string): WorkflowStep | null {
+  return definition.data.steps.find((step: WorkflowStep) => step.id === stepId) ?? null;
+}
+
+export function getStepAgentNameHint(stepDef: WorkflowStep): string | null {
+  const stepMeta = stepDef as StepAgentMetadata;
+  const candidates = [stepMeta.agentName, stepMeta.agent, stepMeta.assigneeAgentName];
+
+  for (const candidate of candidates) {
+    if (typeof candidate === "string" && candidate.trim()) {
+      return candidate.trim();
+    }
+  }
+
+  return null;
+}
+
+export function getStepAgentName(stepRun: WorkflowStepRunRecord, stepDef: WorkflowStep): string | null {
+  if (typeof stepRun.data.agentName === "string" && stepRun.data.agentName.trim()) {
+    return stepRun.data.agentName.trim();
+  }
+
+  return getStepAgentNameHint(stepDef);
+}

file update
M /Users/kwak/Projects/paperclip/paperclip-addon/plugins/workflow-engine/src/reconciler.ts
@@ -10,24 +10,14 @@
   updateWorkflowRun,
-  type WorkflowDefinition,
-  type WorkflowRun,
-  type WorkflowStepRun,
 } from "./workflow-store.js";
 import type { WorkflowStep } from "./dag-engine.js";
-
-type WorkflowRunRecord = PluginEntityRecord & { data: WorkflowRun };
-type WorkflowStepRunRecord = PluginEntityRecord & { data: WorkflowStepRun };
-type WorkflowDefinitionRecord = PluginEntityRecord & { data: WorkflowDefinition };
-type StepAgentMetadata = WorkflowStep & {
-  agent?: string;
-  agentName?: string;
-  assigneeAgentName?: string;
-};
+import {
+  TERMINAL_STEP_STATUSES,
+  findStepDefinition,
+  getStepAgentName,
+  toWorkflowDefinitionRecord,
+  toWorkflowRunRecord,
+  toWorkflowStepRunRecord,
+} from "./workflow-utils.js";
 
 const STALE_THRESHOLD_MS = 300_000;
-const TERMINAL_STEP_STATUSES = new Set<string>([
-  STEP_STATUSES.done,
-  STEP_STATUSES.failed,
-  STEP_STATUSES.skipped,
-  STEP_STATUSES.escalated,
-]);
 
@@ -36,35 +26,2 @@
   return Date.now() - updatedAt > thresholdMs;
-}
-
-function toWorkflowRunRecord(record: PluginEntityRecord): WorkflowRunRecord {
-  return record as WorkflowRunRecord;
-}
-
-function toWorkflowStepRunRecord(record: PluginEntityRecord): WorkflowStepRunRecord {
-  return record as WorkflowStepRunRecord;
-}
-
-function toWorkflowDefinitionRecord(record: PluginEntityRecord): WorkflowDefinitionRecord {
-  return record as WorkflowDefinitionRecord;
-}
-
-function findStepDefinition(definition: WorkflowDefinitionRecord, stepId: string): WorkflowStep | null {
-  return definition.data.steps.find((step: WorkflowStep) => step.id === stepId) ?? null;
-}
-
-function getStepAgentName(stepRun: WorkflowStepRunRecord, stepDef: WorkflowStep): string | null {
-  if (typeof stepRun.data.agentName === "string" && stepRun.data.agentName.trim()) {
-    return stepRun.data.agentName.trim();
-  }
-
-  const stepMeta = stepDef as StepAgentMetadata;
-  const candidates = [stepMeta.agentName, stepMeta.agent, stepMeta.assigneeAgentName];
-
-  for (const candidate of candidates) {
-    if (typeof candidate === "string" && candidate.trim()) {
-      return candidate.trim();
-    }
-  }
-
-  return null;
 }
apply_patch(auto_approved=true) exited 0 in 53ms:
Success. Updated the following files:
M /Users/kwak/Projects/paperclip/paperclip-addon/plugins/workflow-engine/src/reconciler.ts
file update:
diff --git a/paperclip-addon/plugins/workflow-engine/src/reconciler.ts b/paperclip-addon/plugins/workflow-engine/src/reconciler.ts
index 14ab4e766a835d145a6c2e7b1b564270285cbdc3..d2ab54817bd8185944379d2188bfbdef1722020a
--- a/paperclip-addon/plugins/workflow-engine/src/reconciler.ts
+++ b/paperclip-addon/plugins/workflow-engine/src/reconciler.ts
@@ -8,65 +8,22 @@
   listStepRuns,
   updateStepRun,
   updateWorkflowRun,
-  type WorkflowDefinition,
-  type WorkflowRun,
-  type WorkflowStepRun,
 } from "./workflow-store.js";
 import type { WorkflowStep } from "./dag-engine.js";
-
-type WorkflowRunRecord = PluginEntityRecord & { data: WorkflowRun };
-type WorkflowStepRunRecord = PluginEntityRecord & { data: WorkflowStepRun };
-type WorkflowDefinitionRecord = PluginEntityRecord & { data: WorkflowDefinition };
-type StepAgentMetadata = WorkflowStep & {
-  agent?: string;
-  agentName?: string;
-  assigneeAgentName?: string;
-};
+import {
+  TERMINAL_STEP_STATUSES,
+  findStepDefinition,
+  getStepAgentName,
+  toWorkflowDefinitionRecord,
+  toWorkflowRunRecord,
+  toWorkflowStepRunRecord,
+} from "./workflow-utils.js";
 
 const STALE_THRESHOLD_MS = 300_000;
-const TERMINAL_STEP_STATUSES = new Set<string>([
-  STEP_STATUSES.done,
-  STEP_STATUSES.failed,
-  STEP_STATUSES.skipped,
-  STEP_STATUSES.escalated,
-]);
 
 function isStale(record: PluginEntityRecord, thresholdMs: number): boolean {
   const updatedAt = new Date(record.updatedAt).getTime();
   return Date.now() - updatedAt > thresholdMs;
-}
-
-function toWorkflowRunRecord(record: PluginEntityRecord): WorkflowRunRecord {
-  return record as WorkflowRunRecord;
-}
-
-function toWorkflowStepRunRecord(record: PluginEntityRecord): WorkflowStepRunRecord {
-  return record as WorkflowStepRunRecord;
-}
-
-function toWorkflowDefinitionRecord(record: PluginEntityRecord): WorkflowDefinitionRecord {
-  return record as WorkflowDefinitionRecord;
-}
-
-function findStepDefinition(definition: WorkflowDefinitionRecord, stepId: string): WorkflowStep | null {
-  return definition.data.steps.find((step: WorkflowStep) => step.id === stepId) ?? null;
-}
-
-function getStepAgentName(stepRun: WorkflowStepRunRecord, stepDef: WorkflowStep): string | null {
-  if (typeof stepRun.data.agentName === "string" && stepRun.data.agentName.trim()) {
-    return stepRun.data.agentName.trim();
-  }
-
-  const stepMeta = stepDef as StepAgentMetadata;
-  const candidates = [stepMeta.agentName, stepMeta.agent, stepMeta.assigneeAgentName];
-
-  for (const candidate of candidates) {
-    if (typeof candidate === "string" && candidate.trim()) {
-      return candidate.trim();
-    }
-  }
-
-  return null;
 }
 
 async function invokeAgentByName(
diff --git a/paperclip-addon/plugins/workflow-engine/src/workflow-utils.ts b/paperclip-addon/plugins/workflow-engine/src/workflow-utils.ts
new file mode 100644
index 0000000000000000000000000000000000000000..ec1cf6f6b9aa722b126b58c0a728b4e538ad1494
--- /dev/null
+++ b/paperclip-addon/plugins/workflow-engine/src/workflow-utils.ts
@@ -0,0 +1,62 @@
+import type { PluginEntityRecord } from "@paperclipai/plugin-sdk";
+
+import { STEP_STATUSES } from "./constants.js";
+import type { WorkflowStep } from "./dag-engine.js";
+import type {
+  WorkflowDefinition,
+  WorkflowRun,
+  WorkflowStepRun,
+} from "./workflow-store.js";
+
+export type WorkflowRunRecord = PluginEntityRecord & { data: WorkflowRun };
+export type WorkflowStepRunRecord = PluginEntityRecord & { data: WorkflowStepRun };
+export type WorkflowDefinitionRecord = PluginEntityRecord & { data: WorkflowDefinition };
+type StepAgentMetadata = WorkflowStep & {
+  agent?: string;
+  agentName?: string;
+  assigneeAgentName?: string;
+};
+
+export const TERMINAL_STEP_STATUSES = new Set<string>([
+  STEP_STATUSES.done,
+  STEP_STATUSES.failed,
+  STEP_STATUSES.skipped,
+  STEP_STATUSES.escalated,
+]);
+
+export function toWorkflowRunRecord(record: PluginEntityRecord): WorkflowRunRecord {
+  return record as WorkflowRunRecord;
+}
+
+export function toWorkflowStepRunRecord(record: PluginEntityRecord): WorkflowStepRunRecord {
+  return record as WorkflowStepRunRecord;
+}
+
+export function toWorkflowDefinitionRecord(record: PluginEntityRecord): WorkflowDefinitionRecord {
+  return record as WorkflowDefinitionRecord;
+}
+
+export function findStepDefinition(definition: WorkflowDefinitionRecord, stepId: string): WorkflowStep | null {
+  return definition.data.steps.find((step: WorkflowStep) => step.id === stepId) ?? null;
+}
+
+export function getStepAgentNameHint(stepDef: WorkflowStep): string | null {
+  const stepMeta = stepDef as StepAgentMetadata;
+  const candidates = [stepMeta.agentName, stepMeta.agent, stepMeta.assigneeAgentName];
+
+  for (const candidate of candidates) {
+    if (typeof candidate === "string" && candidate.trim()) {
+      return candidate.trim();
+    }
+  }
+
+  return null;
+}
+
+export function getStepAgentName(stepRun: WorkflowStepRunRecord, stepDef: WorkflowStep): string | null {
+  if (typeof stepRun.data.agentName === "string" && stepRun.data.agentName.trim()) {
+    return stepRun.data.agentName.trim();
+  }
+
+  return getStepAgentNameHint(stepDef);
+}

file update:
diff --git a/paperclip-addon/plugins/workflow-engine/src/reconciler.ts b/paperclip-addon/plugins/workflow-engine/src/reconciler.ts
index 14ab4e766a835d145a6c2e7b1b564270285cbdc3..d2ab54817bd8185944379d2188bfbdef1722020a
--- a/paperclip-addon/plugins/workflow-engine/src/reconciler.ts
+++ b/paperclip-addon/plugins/workflow-engine/src/reconciler.ts
@@ -8,65 +8,22 @@
   listStepRuns,
   updateStepRun,
   updateWorkflowRun,
-  type WorkflowDefinition,
-  type WorkflowRun,
-  type WorkflowStepRun,
 } from "./workflow-store.js";
 import type { WorkflowStep } from "./dag-engine.js";
-
-type WorkflowRunRecord = PluginEntityRecord & { data: WorkflowRun };
-type WorkflowStepRunRecord = PluginEntityRecord & { data: WorkflowStepRun };
-type WorkflowDefinitionRecord = PluginEntityRecord & { data: WorkflowDefinition };
-type StepAgentMetadata = WorkflowStep & {
-  agent?: string;
-  agentName?: string;
-  assigneeAgentName?: string;
-};
+import {
+  TERMINAL_STEP_STATUSES,
+  findStepDefinition,
+  getStepAgentName,
+  toWorkflowDefinitionRecord,
+  toWorkflowRunRecord,
+  toWorkflowStepRunRecord,
+} from "./workflow-utils.js";
 
 const STALE_THRESHOLD_MS = 300_000;
-const TERMINAL_STEP_STATUSES = new Set<string>([
-  STEP_STATUSES.done,
-  STEP_STATUSES.failed,
-  STEP_STATUSES.skipped,
-  STEP_STATUSES.escalated,
-]);
 
 function isStale(record: PluginEntityRecord, thresholdMs: number): boolean {
   const updatedAt = new Date(record.updatedAt).getTime();
   return Date.now() - updatedAt > thresholdMs;
-}
-
-function toWorkflowRunRecord(record: PluginEntityRecord): WorkflowRunRecord {
-  return record as WorkflowRunRecord;
-}
-
-function toWorkflowStepRunRecord(record: PluginEntityRecord): WorkflowStepRunRecord {
-  return record as WorkflowStepRunRecord;
-}
-
-function toWorkflowDefinitionRecord(record: PluginEntityRecord): WorkflowDefinitionRecord {
-  return record as WorkflowDefinitionRecord;
-}
-
-function findStepDefinition(definition: WorkflowDefinitionRecord, stepId: string): WorkflowStep | null {
-  return definition.data.steps.find((step: WorkflowStep) => step.id === stepId) ?? null;
-}
-
-function getStepAgentName(stepRun: WorkflowStepRunRecord, stepDef: WorkflowStep): string | null {
-  if (typeof stepRun.data.agentName === "string" && stepRun.data.agentName.trim()) {
-    return stepRun.data.agentName.trim();
-  }
-
-  const stepMeta = stepDef as StepAgentMetadata;
-  const candidates = [stepMeta.agentName, stepMeta.agent, stepMeta.assigneeAgentName];
-
-  for (const candidate of candidates) {
-    if (typeof candidate === "string" && candidate.trim()) {
-      return candidate.trim();
-    }
-  }
-
-  return null;
 }
 
 async function invokeAgentByName(
diff --git a/paperclip-addon/plugins/workflow-engine/src/workflow-utils.ts b/paperclip-addon/plugins/workflow-engine/src/workflow-utils.ts
new file mode 100644
index 0000000000000000000000000000000000000000..ec1cf6f6b9aa722b126b58c0a728b4e538ad1494
--- /dev/null
+++ b/paperclip-addon/plugins/workflow-engine/src/workflow-utils.ts
@@ -0,0 +1,62 @@
+import type { PluginEntityRecord } from "@paperclipai/plugin-sdk";
+
+import { STEP_STATUSES } from "./constants.js";
+import type { WorkflowStep } from "./dag-engine.js";
+import type {
+  WorkflowDefinition,
+  WorkflowRun,
+  WorkflowStepRun,
+} from "./workflow-store.js";
+
+export type WorkflowRunRecord = PluginEntityRecord & { data: WorkflowRun };
+export type WorkflowStepRunRecord = PluginEntityRecord & { data: WorkflowStepRun };
+export type WorkflowDefinitionRecord = PluginEntityRecord & { data: WorkflowDefinition };
+type StepAgentMetadata = WorkflowStep & {
+  agent?: string;
+  agentName?: string;
+  assigneeAgentName?: string;
+};
+
+export const TERMINAL_STEP_STATUSES = new Set<string>([
+  STEP_STATUSES.done,
+  STEP_STATUSES.failed,
+  STEP_STATUSES.skipped,
+  STEP_STATUSES.escalated,
+]);
+
+export function toWorkflowRunRecord(record: PluginEntityRecord): WorkflowRunRecord {
+  return record as WorkflowRunRecord;
+}
+
+export function toWorkflowStepRunRecord(record: PluginEntityRecord): WorkflowStepRunRecord {
+  return record as WorkflowStepRunRecord;
+}
+
+export function toWorkflowDefinitionRecord(record: PluginEntityRecord): WorkflowDefinitionRecord {
+  return record as WorkflowDefinitionRecord;
+}
+
+export function findStepDefinition(definition: WorkflowDefinitionRecord, stepId: string): WorkflowStep | null {
+  return definition.data.steps.find((step: WorkflowStep) => step.id === stepId) ?? null;
+}
+
+export function getStepAgentNameHint(stepDef: WorkflowStep): string | null {
+  const stepMeta = stepDef as StepAgentMetadata;
+  const candidates = [stepMeta.agentName, stepMeta.agent, stepMeta.assigneeAgentName];
+
+  for (const candidate of candidates) {
+    if (typeof candidate === "string" && candidate.trim()) {
+      return candidate.trim();
+    }
+  }
+
+  return null;
+}
+
+export function getStepAgentName(stepRun: WorkflowStepRunRecord, stepDef: WorkflowStep): string | null {
+  if (typeof stepRun.data.agentName === "string" && stepRun.data.agentName.trim()) {
+    return stepRun.data.agentName.trim();
+  }
+
+  return getStepAgentNameHint(stepDef);
+}

codex
공통 유틸 추출은 적용했습니다. 이제 `worker.ts`에서 핵심 로직(시작 진입점, backlog 활성화 시 이슈 생성, 세션 재사용, parentId filler)을 순차적으로 연결하겠습니다.
exec
/bin/zsh -lc "nl -ba plugins/workflow-engine/src/worker.ts | sed -n '260,540p'" in /Users/kwak/Projects/paperclip/paperclip-addonexec
/bin/zsh -lc "nl -ba plugins/workflow-engine/src/worker.ts | sed -n '1,260p'" in /Users/kwak/Projects/paperclip/paperclip-addon succeeded in 0ms:
   260	
   261	  const nextSteps = getNextSteps(
   262	    typedWorkflowDefinition.data.steps,
   263	    completed,
   264	    failed,
   265	    skipped,
   266	  );
   267	  const stepRunsById = new Map(stepRuns.map((candidate) => [candidate.data.stepId, candidate]));
   268	
   269	  for (const stepId of nextSteps.readyStepIds) {
   270	    const stepDef = findStepDefinition(typedWorkflowDefinition, stepId);
   271	    if (!stepDef) {
   272	      ctx.logger.warn("Ready workflow step definition not found", {
   273	        companyId,
   274	        runId: typedWorkflowRun.id,
   275	        stepId,
   276	      });
   277	      continue;
   278	    }
   279	
 succeeded in 0ms:
     1	import {
     2	  definePlugin,
     3	  runWorker,
     4	  type PluginContext,
     5	  type PluginEntityRecord,
     6	  type PluginEvent,
     7	  type PluginJobContext,
     8	} from "@paperclipai/plugin-sdk";
     9	import { JOB_KEYS, RUN_STATUSES, STEP_STATUSES } from "./constants.js";
    10	import {
    11	  getEscalationTarget,
    12	  getNextSteps,
    13	  getRetryInfo,
    14	  type WorkflowStep,
    15	} from "./dag-engine.js";
    16	import {
    17	  checkIdempotency,
    18	  createStepRun,
    19	  findStepRunByIssueId,
    20	  getWorkflowDefinition,
exec
/bin/zsh -lc "nl -ba plugins/workflow-engine/src/worker.ts | sed -n '540,920p'" in /Users/kwak/Projects/paperclip/paperclip-addon succeeded in 0ms:
   540	    if (!escalationTargetId) {
   541	      await updateStepRun(ctx, typedStepRun.id, {
   542	        completedAt: new Date().toISOString(),
   543	        status: STEP_STATUSES.failed,
   544	      });
   545	      await updateWorkflowRun(ctx, typedWorkflowRun.id, {
   546	        completedAt: typedWorkflowRun.data.completedAt ?? new Date().toISOString(),
   547	        status: RUN_STATUSES.failed,
   548	      });
   549	      await markIdempotency(ctx, idempotencyKey, event.companyId);
   550	      ctx.logger.warn("Escalation target missing; workflow marked failed", {
   551	        companyId: event.companyId,
   552	        issueId,
   553	        runId: typedWorkflowRun.id,
   554	        stepId: typedStepRun.data.stepId,
   555	      });
   556	      return;
   557	    }
   558	
   559	    const escalatedStepRun = toStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
file update:
diff --git a/paperclip-addon/plugins/workflow-engine/src/reconciler.ts b/paperclip-addon/plugins/workflow-engine/src/reconciler.ts
index 14ab4e766a835d145a6c2e7b1b564270285cbdc3..d2ab54817bd8185944379d2188bfbdef1722020a
--- a/paperclip-addon/plugins/workflow-engine/src/reconciler.ts
+++ b/paperclip-addon/plugins/workflow-engine/src/reconciler.ts
@@ -8,65 +8,22 @@
   listStepRuns,
   updateStepRun,
   updateWorkflowRun,
-  type WorkflowDefinition,
-  type WorkflowRun,
-  type WorkflowStepRun,
 } from "./workflow-store.js";
 import type { WorkflowStep } from "./dag-engine.js";
-
-type WorkflowRunRecord = PluginEntityRecord & { data: WorkflowRun };
-type WorkflowStepRunRecord = PluginEntityRecord & { data: WorkflowStepRun };
-type WorkflowDefinitionRecord = PluginEntityRecord & { data: WorkflowDefinition };
-type StepAgentMetadata = WorkflowStep & {
-  agent?: string;
-  agentName?: string;
-  assigneeAgentName?: string;
-};
+import {
+  TERMINAL_STEP_STATUSES,
+  findStepDefinition,
+  getStepAgentName,
+  toWorkflowDefinitionRecord,
+  toWorkflowRunRecord,
+  toWorkflowStepRunRecord,
+} from "./workflow-utils.js";
 
 const STALE_THRESHOLD_MS = 300_000;
-const TERMINAL_STEP_STATUSES = new Set<string>([
-  STEP_STATUSES.done,
-  STEP_STATUSES.failed,
-  STEP_STATUSES.skipped,
-  STEP_STATUSES.escalated,
-]);
 
 function isStale(record: PluginEntityRecord, thresholdMs: number): boolean {
   const updatedAt = new Date(record.updatedAt).getTime();
   return Date.now() - updatedAt > thresholdMs;
-}
-
-function toWorkflowRunRecord(record: PluginEntityRecord): WorkflowRunRecord {
-  return record as WorkflowRunRecord;
-}
-
-function toWorkflowStepRunRecord(record: PluginEntityRecord): WorkflowStepRunRecord {
-  return record as WorkflowStepRunRecord;
-}
-
-function toWorkflowDefinitionRecord(record: PluginEntityRecord): WorkflowDefinitionRecord {
-  return record as WorkflowDefinitionRecord;
-}
-
-function findStepDefinition(definition: WorkflowDefinitionRecord, stepId: string): WorkflowStep | null {
-  return definition.data.steps.find((step: WorkflowStep) => step.id === stepId) ?? null;
-}
-
-function getStepAgentName(stepRun: WorkflowStepRunRecord, stepDef: WorkflowStep): string | null {
-  if (typeof stepRun.data.agentName === "string" && stepRun.data.agentName.trim()) {
-    return stepRun.data.agentName.trim();
-  }
-
-  const stepMeta = stepDef as StepAgentMetadata;
-  const candidates = [stepMeta.agentName, stepMeta.agent, stepMeta.assigneeAgentName];
-
-  for (const candidate of candidates) {
-    if (typeof candidate === "string" && candidate.trim()) {
-      return candidate.trim();
-    }
-  }
-
-  return null;
 }
 
 async function invokeAgentByName(
diff --git a/paperclip-addon/plugins/workflow-engine/src/workflow-utils.ts b/paperclip-addon/plugins/workflow-engine/src/workflow-utils.ts
new file mode 100644
index 0000000000000000000000000000000000000000..ec1cf6f6b9aa722b126b58c0a728b4e538ad1494
--- /dev/null
+++ b/paperclip-addon/plugins/workflow-engine/src/workflow-utils.ts
@@ -0,0 +1,62 @@
+import type { PluginEntityRecord } from "@paperclipai/plugin-sdk";
+
+import { STEP_STATUSES } from "./constants.js";
+import type { WorkflowStep } from "./dag-engine.js";
+import type {
+  WorkflowDefinition,
+  WorkflowRun,
+  WorkflowStepRun,
+} from "./workflow-store.js";
+
+export type WorkflowRunRecord = PluginEntityRecord & { data: WorkflowRun };
+export type WorkflowStepRunRecord = PluginEntityRecord & { data: WorkflowStepRun };
+export type WorkflowDefinitionRecord = PluginEntityRecord & { data: WorkflowDefinition };
+type StepAgentMetadata = WorkflowStep & {
+  agent?: string;
+  agentName?: string;
+  assigneeAgentName?: string;
+};
+
+export const TERMINAL_STEP_STATUSES = new Set<string>([
+  STEP_STATUSES.done,
+  STEP_STATUSES.failed,
+  STEP_STATUSES.skipped,
+  STEP_STATUSES.escalated,
+]);
+
+export function toWorkflowRunRecord(record: PluginEntityRecord): WorkflowRunRecord {
+  return record as WorkflowRunRecord;
+}
+
+export function toWorkflowStepRunRecord(record: PluginEntityRecord): WorkflowStepRunRecord {
+  return record as WorkflowStepRunRecord;
+}
+
+export function toWorkflowDefinitionRecord(record: PluginEntityRecord): WorkflowDefinitionRecord {
+  return record as WorkflowDefinitionRecord;
+}
+
+export function findStepDefinition(definition: WorkflowDefinitionRecord, stepId: string): WorkflowStep | null {
+  return definition.data.steps.find((step: WorkflowStep) => step.id === stepId) ?? null;
+}
+
+export function getStepAgentNameHint(stepDef: WorkflowStep): string | null {
+  const stepMeta = stepDef as StepAgentMetadata;
+  const candidates = [stepMeta.agentName, stepMeta.agent, stepMeta.assigneeAgentName];
+
+  for (const candidate of candidates) {
+    if (typeof candidate === "string" && candidate.trim()) {
+      return candidate.trim();
+    }
+  }
+
+  return null;
+}
+
+export function getStepAgentName(stepRun: WorkflowStepRunRecord, stepDef: WorkflowStep): string | null {
+  if (typeof stepRun.data.agentName === "string" && stepRun.data.agentName.trim()) {
+    return stepRun.data.agentName.trim();
+  }
+
+  return getStepAgentNameHint(stepDef);
+}

exec
/bin/zsh -lc "nl -ba plugins/workflow-engine/src/workflow-store.ts | sed -n '1,220p'" in /Users/kwak/Projects/paperclip/paperclip-addon succeeded in 0ms:
     1	import type { PluginContext, PluginEntityRecord } from "@paperclipai/plugin-sdk";
     2	
     3	import { ENTITY_TYPES } from "./constants.js";
     4	import type { WorkflowStep } from "./dag-engine.js";
     5	
     6	export interface WorkflowDefinition {
     7	  name: string;
     8	  description: string;
     9	  companyId: string;
    10	  status: "active" | "paused" | "archived";
    11	  steps: WorkflowStep[];
    12	  timeoutMinutes?: number;
    13	  maxConcurrentRuns?: number;
    14	}
    15	
    16	export interface WorkflowRun {
    17	  workflowId: string;
    18	  workflowName: string;
    19	  companyId: string;
    20	  status: "running" | "completed" | "failed" | "aborted" | "timed-out";
file update:
diff --git a/paperclip-addon/plugins/workflow-engine/src/reconciler.ts b/paperclip-addon/plugins/workflow-engine/src/reconciler.ts
index 14ab4e766a835d145a6c2e7b1b564270285cbdc3..d2ab54817bd8185944379d2188bfbdef1722020a
--- a/paperclip-addon/plugins/workflow-engine/src/reconciler.ts
+++ b/paperclip-addon/plugins/workflow-engine/src/reconciler.ts
@@ -8,65 +8,22 @@
   listStepRuns,
   updateStepRun,
   updateWorkflowRun,
-  type WorkflowDefinition,
-  type WorkflowRun,
-  type WorkflowStepRun,
 } from "./workflow-store.js";
 import type { WorkflowStep } from "./dag-engine.js";
-
-type WorkflowRunRecord = PluginEntityRecord & { data: WorkflowRun };
-type WorkflowStepRunRecord = PluginEntityRecord & { data: WorkflowStepRun };
-type WorkflowDefinitionRecord = PluginEntityRecord & { data: WorkflowDefinition };
-type StepAgentMetadata = WorkflowStep & {
-  agent?: string;
-  agentName?: string;
-  assigneeAgentName?: string;
-};
+import {
+  TERMINAL_STEP_STATUSES,
+  findStepDefinition,
+  getStepAgentName,
+  toWorkflowDefinitionRecord,
+  toWorkflowRunRecord,
+  toWorkflowStepRunRecord,
+} from "./workflow-utils.js";
 
 const STALE_THRESHOLD_MS = 300_000;
-const TERMINAL_STEP_STATUSES = new Set<string>([
-  STEP_STATUSES.done,
-  STEP_STATUSES.failed,
-  STEP_STATUSES.skipped,
-  STEP_STATUSES.escalated,
-]);
 
 function isStale(record: PluginEntityRecord, thresholdMs: number): boolean {
   const updatedAt = new Date(record.updatedAt).getTime();
   return Date.now() - updatedAt > thresholdMs;
-}
-
-function toWorkflowRunRecord(record: PluginEntityRecord): WorkflowRunRecord {
-  return record as WorkflowRunRecord;
-}
-
-function toWorkflowStepRunRecord(record: PluginEntityRecord): WorkflowStepRunRecord {
-  return record as WorkflowStepRunRecord;
-}
-
-function toWorkflowDefinitionRecord(record: PluginEntityRecord): WorkflowDefinitionRecord {
-  return record as WorkflowDefinitionRecord;
-}
-
-function findStepDefinition(definition: WorkflowDefinitionRecord, stepId: string): WorkflowStep | null {
-  return definition.data.steps.find((step: WorkflowStep) => step.id === stepId) ?? null;
-}
-
-function getStepAgentName(stepRun: WorkflowStepRunRecord, stepDef: WorkflowStep): string | null {
-  if (typeof stepRun.data.agentName === "string" && stepRun.data.agentName.trim()) {
-    return stepRun.data.agentName.trim();
-  }
-
-  const stepMeta = stepDef as StepAgentMetadata;
-  const candidates = [stepMeta.agentName, stepMeta.agent, stepMeta.assigneeAgentName];
-
-  for (const candidate of candidates) {
-    if (typeof candidate === "string" && candidate.trim()) {
-      return candidate.trim();
-    }
-  }
-
-  return null;
 }
 
 async function invokeAgentByName(
diff --git a/paperclip-addon/plugins/workflow-engine/src/workflow-utils.ts b/paperclip-addon/plugins/workflow-engine/src/workflow-utils.ts
new file mode 100644
index 0000000000000000000000000000000000000000..ec1cf6f6b9aa722b126b58c0a728b4e538ad1494
--- /dev/null
+++ b/paperclip-addon/plugins/workflow-engine/src/workflow-utils.ts
@@ -0,0 +1,62 @@
+import type { PluginEntityRecord } from "@paperclipai/plugin-sdk";
+
+import { STEP_STATUSES } from "./constants.js";
+import type { WorkflowStep } from "./dag-engine.js";
+import type {
+  WorkflowDefinition,
+  WorkflowRun,
+  WorkflowStepRun,
+} from "./workflow-store.js";
+
+export type WorkflowRunRecord = PluginEntityRecord & { data: WorkflowRun };
+export type WorkflowStepRunRecord = PluginEntityRecord & { data: WorkflowStepRun };
+export type WorkflowDefinitionRecord = PluginEntityRecord & { data: WorkflowDefinition };
+type StepAgentMetadata = WorkflowStep & {
+  agent?: string;
+  agentName?: string;
+  assigneeAgentName?: string;
+};
+
+export const TERMINAL_STEP_STATUSES = new Set<string>([
+  STEP_STATUSES.done,
+  STEP_STATUSES.failed,
+  STEP_STATUSES.skipped,
+  STEP_STATUSES.escalated,
+]);
+
+export function toWorkflowRunRecord(record: PluginEntityRecord): WorkflowRunRecord {
+  return record as WorkflowRunRecord;
+}
+
+export function toWorkflowStepRunRecord(record: PluginEntityRecord): WorkflowStepRunRecord {
+  return record as WorkflowStepRunRecord;
+}
+
+export function toWorkflowDefinitionRecord(record: PluginEntityRecord): WorkflowDefinitionRecord {
+  return record as WorkflowDefinitionRecord;
+}
+
+export function findStepDefinition(definition: WorkflowDefinitionRecord, stepId: string): WorkflowStep | null {
+  return definition.data.steps.find((step: WorkflowStep) => step.id === stepId) ?? null;
+}
+
+export function getStepAgentNameHint(stepDef: WorkflowStep): string | null {
+  const stepMeta = stepDef as StepAgentMetadata;
+  const candidates = [stepMeta.agentName, stepMeta.agent, stepMeta.assigneeAgentName];
+
+  for (const candidate of candidates) {
+    if (typeof candidate === "string" && candidate.trim()) {
+      return candidate.trim();
+    }
+  }
+
+  return null;
+}
+
+export function getStepAgentName(stepRun: WorkflowStepRunRecord, stepDef: WorkflowStep): string | null {
+  if (typeof stepRun.data.agentName === "string" && stepRun.data.agentName.trim()) {
+    return stepRun.data.agentName.trim();
+  }
+
+  return getStepAgentNameHint(stepDef);
+}

exec
/bin/zsh -lc "nl -ba plugins/workflow-engine/src/workflow-store.ts | sed -n '220,360p'" in /Users/kwak/Projects/paperclip/paperclip-addon succeeded in 0ms:
   220	export async function getStepRun(
   221	  ctx: PluginContext,
   222	  id: string,
   223	): Promise<(PluginEntityRecord & { data: WorkflowStepRun }) | null> {
   224	  return await getEntityByType<WorkflowStepRun>(
   225	    ctx,
   226	    id,
   227	    ENTITY_TYPES.workflowStepRun,
   228	  );
   229	}
   230	
   231	export async function listStepRuns(
   232	  ctx: PluginContext,
   233	  runId: string,
   234	  companyId: string,
   235	): Promise<PluginEntityRecord[]> {
   236	  const stepRuns = await ctx.entities.list({
   237	    entityType: ENTITY_TYPES.workflowStepRun,
   238	    scopeKind: "company",
   239	    scopeId: companyId,
file update:
diff --git a/paperclip-addon/plugins/workflow-engine/src/reconciler.ts b/paperclip-addon/plugins/workflow-engine/src/reconciler.ts
index 14ab4e766a835d145a6c2e7b1b564270285cbdc3..d2ab54817bd8185944379d2188bfbdef1722020a
--- a/paperclip-addon/plugins/workflow-engine/src/reconciler.ts
+++ b/paperclip-addon/plugins/workflow-engine/src/reconciler.ts
@@ -8,65 +8,22 @@
   listStepRuns,
   updateStepRun,
   updateWorkflowRun,
-  type WorkflowDefinition,
-  type WorkflowRun,
-  type WorkflowStepRun,
 } from "./workflow-store.js";
 import type { WorkflowStep } from "./dag-engine.js";
-
-type WorkflowRunRecord = PluginEntityRecord & { data: WorkflowRun };
-type WorkflowStepRunRecord = PluginEntityRecord & { data: WorkflowStepRun };
-type WorkflowDefinitionRecord = PluginEntityRecord & { data: WorkflowDefinition };
-type StepAgentMetadata = WorkflowStep & {
-  agent?: string;
-  agentName?: string;
-  assigneeAgentName?: string;
-};
+import {
+  TERMINAL_STEP_STATUSES,
+  findStepDefinition,
+  getStepAgentName,
+  toWorkflowDefinitionRecord,
+  toWorkflowRunRecord,
+  toWorkflowStepRunRecord,
+} from "./workflow-utils.js";
 
 const STALE_THRESHOLD_MS = 300_000;
-const TERMINAL_STEP_STATUSES = new Set<string>([
-  STEP_STATUSES.done,
-  STEP_STATUSES.failed,
-  STEP_STATUSES.skipped,
-  STEP_STATUSES.escalated,
-]);
 
 function isStale(record: PluginEntityRecord, thresholdMs: number): boolean {
   const updatedAt = new Date(record.updatedAt).getTime();
   return Date.now() - updatedAt > thresholdMs;
-}
-
-function toWorkflowRunRecord(record: PluginEntityRecord): WorkflowRunRecord {
-  return record as WorkflowRunRecord;
-}
-
-function toWorkflowStepRunRecord(record: PluginEntityRecord): WorkflowStepRunRecord {
-  return record as WorkflowStepRunRecord;
-}
-
-function toWorkflowDefinitionRecord(record: PluginEntityRecord): WorkflowDefinitionRecord {
-  return record as WorkflowDefinitionRecord;
-}
-
-function findStepDefinition(definition: WorkflowDefinitionRecord, stepId: string): WorkflowStep | null {
-  return definition.data.steps.find((step: WorkflowStep) => step.id === stepId) ?? null;
-}
-
-function getStepAgentName(stepRun: WorkflowStepRunRecord, stepDef: WorkflowStep): string | null {
-  if (typeof stepRun.data.agentName === "string" && stepRun.data.agentName.trim()) {
-    return stepRun.data.agentName.trim();
-  }
-
-  const stepMeta = stepDef as StepAgentMetadata;
-  const candidates = [stepMeta.agentName, stepMeta.agent, stepMeta.assigneeAgentName];
-
-  for (const candidate of candidates) {
-    if (typeof candidate === "string" && candidate.trim()) {
-      return candidate.trim();
-    }
-  }
-
-  return null;
 }
 
 async function invokeAgentByName(
diff --git a/paperclip-addon/plugins/workflow-engine/src/workflow-utils.ts b/paperclip-addon/plugins/workflow-engine/src/workflow-utils.ts
new file mode 100644
index 0000000000000000000000000000000000000000..ec1cf6f6b9aa722b126b58c0a728b4e538ad1494
--- /dev/null
+++ b/paperclip-addon/plugins/workflow-engine/src/workflow-utils.ts
@@ -0,0 +1,62 @@
+import type { PluginEntityRecord } from "@paperclipai/plugin-sdk";
+
+import { STEP_STATUSES } from "./constants.js";
+import type { WorkflowStep } from "./dag-engine.js";
+import type {
+  WorkflowDefinition,
+  WorkflowRun,
+  WorkflowStepRun,
+} from "./workflow-store.js";
+
+export type WorkflowRunRecord = PluginEntityRecord & { data: WorkflowRun };
+export type WorkflowStepRunRecord = PluginEntityRecord & { data: WorkflowStepRun };
+export type WorkflowDefinitionRecord = PluginEntityRecord & { data: WorkflowDefinition };
+type StepAgentMetadata = WorkflowStep & {
+  agent?: string;
+  agentName?: string;
+  assigneeAgentName?: string;
+};
+
+export const TERMINAL_STEP_STATUSES = new Set<string>([
+  STEP_STATUSES.done,
+  STEP_STATUSES.failed,
+  STEP_STATUSES.skipped,
+  STEP_STATUSES.escalated,
+]);
+
+export function toWorkflowRunRecord(record: PluginEntityRecord): WorkflowRunRecord {
+  return record as WorkflowRunRecord;
+}
+
+export function toWorkflowStepRunRecord(record: PluginEntityRecord): WorkflowStepRunRecord {
+  return record as WorkflowStepRunRecord;
+}
+
+export function toWorkflowDefinitionRecord(record: PluginEntityRecord): WorkflowDefinitionRecord {
+  return record as WorkflowDefinitionRecord;
+}
+
+export function findStepDefinition(definition: WorkflowDefinitionRecord, stepId: string): WorkflowStep | null {
+  return definition.data.steps.find((step: WorkflowStep) => step.id === stepId) ?? null;
+}
+
+export function getStepAgentNameHint(stepDef: WorkflowStep): string | null {
+  const stepMeta = stepDef as StepAgentMetadata;
+  const candidates = [stepMeta.agentName, stepMeta.agent, stepMeta.assigneeAgentName];
+
+  for (const candidate of candidates) {
+    if (typeof candidate === "string" && candidate.trim()) {
+      return candidate.trim();
+    }
+  }
+
+  return null;
+}
+
+export function getStepAgentName(stepRun: WorkflowStepRunRecord, stepDef: WorkflowStep): string | null {
+  if (typeof stepRun.data.agentName === "string" && stepRun.data.agentName.trim()) {
+    return stepRun.data.agentName.trim();
+  }
+
+  return getStepAgentNameHint(stepDef);
+}

file update
M /Users/kwak/Projects/paperclip/paperclip-addon/plugins/workflow-engine/src/workflow-store.ts
@@ -46,3 +46,10 @@
 
-function toTypedRecord<T>(record: PluginEntityRecord): TypedEntityRecord<T> {
+function toTypedRecord<T>(
+  record: PluginEntityRecord,
+  entityType: string,
+): TypedEntityRecord<T> {
+  if (record.entityType !== entityType) {
+    throw new Error(`Expected entity type "${entityType}", got "${record.entityType}"`);
+  }
+
   return record as TypedEntityRecord<T>;
@@ -69,5 +76,18 @@
 ): Promise<TypedEntityRecord<T> | null> {
+  const listQuery: Parameters<PluginContext["entities"]["list"]>[0] & { id?: string } = {
+    entityType,
+    id,
+    limit: 1,
+  };
+
+  const listed = await ctx.entities.list(listQuery);
+  const listedRecord = listed.find((record) => record.id === id) ?? null;
+  if (listedRecord) {
+    return toTypedRecord<T>(listedRecord, entityType);
+  }
+
+  // Fallback: some host SDK variants ignore `id` on entities.list().
+  // Keep defensive type verification on direct get() until id-filter support is universal.
   const record = await ctx.entities.get(id);
-
-  if (!record || record.entityType !== entityType) {
+  if (!record) {
     return null;
@@ -75,3 +95,3 @@
 
-  return toTypedRecord<T>(record);
+  return toTypedRecord<T>(record, entityType);
 }
apply_patch(auto_approved=true) exited 0 in 71ms:
Success. Updated the following files:
M /Users/kwak/Projects/paperclip/paperclip-addon/plugins/workflow-engine/src/workflow-store.ts
file update:
diff --git a/paperclip-addon/plugins/workflow-engine/src/reconciler.ts b/paperclip-addon/plugins/workflow-engine/src/reconciler.ts
index 14ab4e766a835d145a6c2e7b1b564270285cbdc3..d2ab54817bd8185944379d2188bfbdef1722020a
--- a/paperclip-addon/plugins/workflow-engine/src/reconciler.ts
+++ b/paperclip-addon/plugins/workflow-engine/src/reconciler.ts
@@ -8,65 +8,22 @@
   listStepRuns,
   updateStepRun,
   updateWorkflowRun,
-  type WorkflowDefinition,
-  type WorkflowRun,
-  type WorkflowStepRun,
 } from "./workflow-store.js";
 import type { WorkflowStep } from "./dag-engine.js";
-
-type WorkflowRunRecord = PluginEntityRecord & { data: WorkflowRun };
-type WorkflowStepRunRecord = PluginEntityRecord & { data: WorkflowStepRun };
-type WorkflowDefinitionRecord = PluginEntityRecord & { data: WorkflowDefinition };
-type StepAgentMetadata = WorkflowStep & {
-  agent?: string;
-  agentName?: string;
-  assigneeAgentName?: string;
-};
+import {
+  TERMINAL_STEP_STATUSES,
+  findStepDefinition,
+  getStepAgentName,
+  toWorkflowDefinitionRecord,
+  toWorkflowRunRecord,
+  toWorkflowStepRunRecord,
+} from "./workflow-utils.js";
 
 const STALE_THRESHOLD_MS = 300_000;
-const TERMINAL_STEP_STATUSES = new Set<string>([
-  STEP_STATUSES.done,
-  STEP_STATUSES.failed,
-  STEP_STATUSES.skipped,
-  STEP_STATUSES.escalated,
-]);
 
 function isStale(record: PluginEntityRecord, thresholdMs: number): boolean {
   const updatedAt = new Date(record.updatedAt).getTime();
   return Date.now() - updatedAt > thresholdMs;
-}
-
-function toWorkflowRunRecord(record: PluginEntityRecord): WorkflowRunRecord {
-  return record as WorkflowRunRecord;
-}
-
-function toWorkflowStepRunRecord(record: PluginEntityRecord): WorkflowStepRunRecord {
-  return record as WorkflowStepRunRecord;
-}
-
-function toWorkflowDefinitionRecord(record: PluginEntityRecord): WorkflowDefinitionRecord {
-  return record as WorkflowDefinitionRecord;
-}
-
-function findStepDefinition(definition: WorkflowDefinitionRecord, stepId: string): WorkflowStep | null {
-  return definition.data.steps.find((step: WorkflowStep) => step.id === stepId) ?? null;
-}
-
-function getStepAgentName(stepRun: WorkflowStepRunRecord, stepDef: WorkflowStep): string | null {
-  if (typeof stepRun.data.agentName === "string" && stepRun.data.agentName.trim()) {
-    return stepRun.data.agentName.trim();
-  }
-
-  const stepMeta = stepDef as StepAgentMetadata;
-  const candidates = [stepMeta.agentName, stepMeta.agent, stepMeta.assigneeAgentName];
-
-  for (const candidate of candidates) {
-    if (typeof candidate === "string" && candidate.trim()) {
-      return candidate.trim();
-    }
-  }
-
-  return null;
 }
 
 async function invokeAgentByName(
diff --git a/paperclip-addon/plugins/workflow-engine/src/workflow-store.ts b/paperclip-addon/plugins/workflow-engine/src/workflow-store.ts
index 5871a2082258cebcfe8b69ebafbb0814cf303d50..3d7fb2b3bc75911cfd9093eb3a4054cc73196bb9
--- a/paperclip-addon/plugins/workflow-engine/src/workflow-store.ts
+++ b/paperclip-addon/plugins/workflow-engine/src/workflow-store.ts
@@ -44,7 +44,14 @@
 
 type TypedEntityRecord<T> = Omit<PluginEntityRecord, "data"> & { data: T };
 
-function toTypedRecord<T>(record: PluginEntityRecord): TypedEntityRecord<T> {
+function toTypedRecord<T>(
+  record: PluginEntityRecord,
+  entityType: string,
+): TypedEntityRecord<T> {
+  if (record.entityType !== entityType) {
+    throw new Error(`Expected entity type "${entityType}", got "${record.entityType}"`);
+  }
+
   return record as TypedEntityRecord<T>;
 }
 
@@ -67,13 +74,26 @@
   id: string,
   entityType: string,
 ): Promise<TypedEntityRecord<T> | null> {
+  const listQuery: Parameters<PluginContext["entities"]["list"]>[0] & { id?: string } = {
+    entityType,
+    id,
+    limit: 1,
+  };
+
+  const listed = await ctx.entities.list(listQuery);
+  const listedRecord = listed.find((record) => record.id === id) ?? null;
+  if (listedRecord) {
+    return toTypedRecord<T>(listedRecord, entityType);
+  }
+
+  // Fallback: some host SDK variants ignore `id` on entities.list().
+  // Keep defensive type verification on direct get() until id-filter support is universal.
   const record = await ctx.entities.get(id);
-
-  if (!record || record.entityType !== entityType) {
+  if (!record) {
     return null;
   }
 
-  return toTypedRecord<T>(record);
+  return toTypedRecord<T>(record, entityType);
 }
 
 async function requireEntityByType<T>(
diff --git a/paperclip-addon/plugins/workflow-engine/src/workflow-utils.ts b/paperclip-addon/plugins/workflow-engine/src/workflow-utils.ts
new file mode 100644
index 0000000000000000000000000000000000000000..ec1cf6f6b9aa722b126b58c0a728b4e538ad1494
--- /dev/null
+++ b/paperclip-addon/plugins/workflow-engine/src/workflow-utils.ts
@@ -0,0 +1,62 @@
+import type { PluginEntityRecord } from "@paperclipai/plugin-sdk";
+
+import { STEP_STATUSES } from "./constants.js";
+import type { WorkflowStep } from "./dag-engine.js";
+import type {
+  WorkflowDefinition,
+  WorkflowRun,
+  WorkflowStepRun,
+} from "./workflow-store.js";
+
+export type WorkflowRunRecord = PluginEntityRecord & { data: WorkflowRun };
+export type WorkflowStepRunRecord = PluginEntityRecord & { data: WorkflowStepRun };
+export type WorkflowDefinitionRecord = PluginEntityRecord & { data: WorkflowDefinition };
+type StepAgentMetadata = WorkflowStep & {
+  agent?: string;
+  agentName?: string;
+  assigneeAgentName?: string;
+};
+
+export const TERMINAL_STEP_STATUSES = new Set<string>([
+  STEP_STATUSES.done,
+  STEP_STATUSES.failed,
+  STEP_STATUSES.skipped,
+  STEP_STATUSES.escalated,
+]);
+
+export function toWorkflowRunRecord(record: PluginEntityRecord): WorkflowRunRecord {
+  return record as WorkflowRunRecord;
+}
+
+export function toWorkflowStepRunRecord(record: PluginEntityRecord): WorkflowStepRunRecord {
+  return record as WorkflowStepRunRecord;
+}
+
+export function toWorkflowDefinitionRecord(record: PluginEntityRecord): WorkflowDefinitionRecord {
+  return record as WorkflowDefinitionRecord;
+}
+
+export function findStepDefinition(definition: WorkflowDefinitionRecord, stepId: string): WorkflowStep | null {
+  return definition.data.steps.find((step: WorkflowStep) => step.id === stepId) ?? null;
+}
+
+export function getStepAgentNameHint(stepDef: WorkflowStep): string | null {
+  const stepMeta = stepDef as StepAgentMetadata;
+  const candidates = [stepMeta.agentName, stepMeta.agent, stepMeta.assigneeAgentName];
+
+  for (const candidate of candidates) {
+    if (typeof candidate === "string" && candidate.trim()) {
+      return candidate.trim();
+    }
+  }
+
+  return null;
+}
+
+export function getStepAgentName(stepRun: WorkflowStepRunRecord, stepDef: WorkflowStep): string | null {
+  if (typeof stepRun.data.agentName === "string" && stepRun.data.agentName.trim()) {
+    return stepRun.data.agentName.trim();
+  }
+
+  return getStepAgentNameHint(stepDef);
+}

file update:
diff --git a/paperclip-addon/plugins/workflow-engine/src/reconciler.ts b/paperclip-addon/plugins/workflow-engine/src/reconciler.ts
index 14ab4e766a835d145a6c2e7b1b564270285cbdc3..d2ab54817bd8185944379d2188bfbdef1722020a
--- a/paperclip-addon/plugins/workflow-engine/src/reconciler.ts
+++ b/paperclip-addon/plugins/workflow-engine/src/reconciler.ts
@@ -8,65 +8,22 @@
   listStepRuns,
   updateStepRun,
   updateWorkflowRun,
-  type WorkflowDefinition,
-  type WorkflowRun,
-  type WorkflowStepRun,
 } from "./workflow-store.js";
 import type { WorkflowStep } from "./dag-engine.js";
-
-type WorkflowRunRecord = PluginEntityRecord & { data: WorkflowRun };
-type WorkflowStepRunRecord = PluginEntityRecord & { data: WorkflowStepRun };
-type WorkflowDefinitionRecord = PluginEntityRecord & { data: WorkflowDefinition };
-type StepAgentMetadata = WorkflowStep & {
-  agent?: string;
-  agentName?: string;
-  assigneeAgentName?: string;
-};
+import {
+  TERMINAL_STEP_STATUSES,
+  findStepDefinition,
+  getStepAgentName,
+  toWorkflowDefinitionRecord,
+  toWorkflowRunRecord,
+  toWorkflowStepRunRecord,
+} from "./workflow-utils.js";
 
 const STALE_THRESHOLD_MS = 300_000;
-const TERMINAL_STEP_STATUSES = new Set<string>([
-  STEP_STATUSES.done,
-  STEP_STATUSES.failed,
-  STEP_STATUSES.skipped,
-  STEP_STATUSES.escalated,
-]);
 
 function isStale(record: PluginEntityRecord, thresholdMs: number): boolean {
   const updatedAt = new Date(record.updatedAt).getTime();
   return Date.now() - updatedAt > thresholdMs;
-}
-
-function toWorkflowRunRecord(record: PluginEntityRecord): WorkflowRunRecord {
-  return record as WorkflowRunRecord;
-}
-
-function toWorkflowStepRunRecord(record: PluginEntityRecord): WorkflowStepRunRecord {
-  return record as WorkflowStepRunRecord;
-}
-
-function toWorkflowDefinitionRecord(record: PluginEntityRecord): WorkflowDefinitionRecord {
-  return record as WorkflowDefinitionRecord;
-}
-
-function findStepDefinition(definition: WorkflowDefinitionRecord, stepId: string): WorkflowStep | null {
-  return definition.data.steps.find((step: WorkflowStep) => step.id === stepId) ?? null;
-}
-
-function getStepAgentName(stepRun: WorkflowStepRunRecord, stepDef: WorkflowStep): string | null {
-  if (typeof stepRun.data.agentName === "string" && stepRun.data.agentName.trim()) {
-    return stepRun.data.agentName.trim();
-  }
-
-  const stepMeta = stepDef as StepAgentMetadata;
-  const candidates = [stepMeta.agentName, stepMeta.agent, stepMeta.assigneeAgentName];
-
-  for (const candidate of candidates) {
-    if (typeof candidate === "string" && candidate.trim()) {
-      return candidate.trim();
-    }
-  }
-
-  return null;
 }
 
 async function invokeAgentByName(
diff --git a/paperclip-addon/plugins/workflow-engine/src/workflow-store.ts b/paperclip-addon/plugins/workflow-engine/src/workflow-store.ts
index 5871a2082258cebcfe8b69ebafbb0814cf303d50..3d7fb2b3bc75911cfd9093eb3a4054cc73196bb9
--- a/paperclip-addon/plugins/workflow-engine/src/workflow-store.ts
+++ b/paperclip-addon/plugins/workflow-engine/src/workflow-store.ts
@@ -44,7 +44,14 @@
 
 type TypedEntityRecord<T> = Omit<PluginEntityRecord, "data"> & { data: T };
 
-function toTypedRecord<T>(record: PluginEntityRecord): TypedEntityRecord<T> {
+function toTypedRecord<T>(
+  record: PluginEntityRecord,
+  entityType: string,
+): TypedEntityRecord<T> {
+  if (record.entityType !== entityType) {
+    throw new Error(`Expected entity type "${entityType}", got "${record.entityType}"`);
+  }
+
   return record as TypedEntityRecord<T>;
 }
 
@@ -67,13 +74,26 @@
   id: string,
   entityType: string,
 ): Promise<TypedEntityRecord<T> | null> {
+  const listQuery: Parameters<PluginContext["entities"]["list"]>[0] & { id?: string } = {
+    entityType,
+    id,
+    limit: 1,
+  };
+
+  const listed = await ctx.entities.list(listQuery);
+  const listedRecord = listed.find((record) => record.id === id) ?? null;
+  if (listedRecord) {
+    return toTypedRecord<T>(listedRecord, entityType);
+  }
+
+  // Fallback: some host SDK variants ignore `id` on entities.list().
+  // Keep defensive type verification on direct get() until id-filter support is universal.
   const record = await ctx.entities.get(id);
-
-  if (!record || record.entityType !== entityType) {
+  if (!record) {
     return null;
   }
 
-  return toTypedRecord<T>(record);
+  return toTypedRecord<T>(record, entityType);
 }
 
 async function requireEntityByType<T>(
diff --git a/paperclip-addon/plugins/workflow-engine/src/workflow-utils.ts b/paperclip-addon/plugins/workflow-engine/src/workflow-utils.ts
new file mode 100644
index 0000000000000000000000000000000000000000..ec1cf6f6b9aa722b126b58c0a728b4e538ad1494
--- /dev/null
+++ b/paperclip-addon/plugins/workflow-engine/src/workflow-utils.ts
@@ -0,0 +1,62 @@
+import type { PluginEntityRecord } from "@paperclipai/plugin-sdk";
+
+import { STEP_STATUSES } from "./constants.js";
+import type { WorkflowStep } from "./dag-engine.js";
+import type {
+  WorkflowDefinition,
+  WorkflowRun,
+  WorkflowStepRun,
+} from "./workflow-store.js";
+
+export type WorkflowRunRecord = PluginEntityRecord & { data: WorkflowRun };
+export type WorkflowStepRunRecord = PluginEntityRecord & { data: WorkflowStepRun };
+export type WorkflowDefinitionRecord = PluginEntityRecord & { data: WorkflowDefinition };
+type StepAgentMetadata = WorkflowStep & {
+  agent?: string;
+  agentName?: string;
+  assigneeAgentName?: string;
+};
+
+export const TERMINAL_STEP_STATUSES = new Set<string>([
+  STEP_STATUSES.done,
+  STEP_STATUSES.failed,
+  STEP_STATUSES.skipped,
+  STEP_STATUSES.escalated,
+]);
+
+export function toWorkflowRunRecord(record: PluginEntityRecord): WorkflowRunRecord {
+  return record as WorkflowRunRecord;
+}
+
+export function toWorkflowStepRunRecord(record: PluginEntityRecord): WorkflowStepRunRecord {
+  return record as WorkflowStepRunRecord;
+}
+
+export function toWorkflowDefinitionRecord(record: PluginEntityRecord): WorkflowDefinitionRecord {
+  return record as WorkflowDefinitionRecord;
+}
+
+export function findStepDefinition(definition: WorkflowDefinitionRecord, stepId: string): WorkflowStep | null {
+  return definition.data.steps.find((step: WorkflowStep) => step.id === stepId) ?? null;
+}
+
+export function getStepAgentNameHint(stepDef: WorkflowStep): string | null {
+  const stepMeta = stepDef as StepAgentMetadata;
+  const candidates = [stepMeta.agentName, stepMeta.agent, stepMeta.assigneeAgentName];
+
+  for (const candidate of candidates) {
+    if (typeof candidate === "string" && candidate.trim()) {
+      return candidate.trim();
+    }
+  }
+
+  return null;
+}
+
+export function getStepAgentName(stepRun: WorkflowStepRunRecord, stepDef: WorkflowStep): string | null {
+  if (typeof stepRun.data.agentName === "string" && stepRun.data.agentName.trim()) {
+    return stepRun.data.agentName.trim();
+  }
+
+  return getStepAgentNameHint(stepDef);
+}

codex
`workflow-store.ts`에 타입 가드와 안전 조회(fallback 포함)를 반영했습니다. 다음 단계로 `worker.ts` 핵심 함수들을 리팩터링하면서 `sessionMode` 타입도 맞추겠습니다.
file update
M /Users/kwak/Projects/paperclip/paperclip-addon/plugins/workflow-engine/src/dag-engine.ts
@@ -4,2 +4,3 @@
   dependsOn: string[];
+  sessionMode?: "fresh" | "reuse";
   onFailure?: "retry" | "skip" | "abort_workflow" | "escalate";
apply_patch(auto_approved=true) exited 0 in 82ms:
Success. Updated the following files:
M /Users/kwak/Projects/paperclip/paperclip-addon/plugins/workflow-engine/src/dag-engine.ts
file update:
diff --git a/paperclip-addon/plugins/workflow-engine/src/dag-engine.ts b/paperclip-addon/plugins/workflow-engine/src/dag-engine.ts
index b59510f0473171038be77277286d3b5c2063e6c5..e7f21ff998cab78b9325a8aeacf4fda9c5e06553
--- a/paperclip-addon/plugins/workflow-engine/src/dag-engine.ts
+++ b/paperclip-addon/plugins/workflow-engine/src/dag-engine.ts
@@ -2,6 +2,7 @@
   id: string;
   title: string;
   dependsOn: string[];
+  sessionMode?: "fresh" | "reuse";
   onFailure?: "retry" | "skip" | "abort_workflow" | "escalate";
   escalateTo?: string;
   maxRetries?: number;
diff --git a/paperclip-addon/plugins/workflow-engine/src/reconciler.ts b/paperclip-addon/plugins/workflow-engine/src/reconciler.ts
index 14ab4e766a835d145a6c2e7b1b564270285cbdc3..d2ab54817bd8185944379d2188bfbdef1722020a
--- a/paperclip-addon/plugins/workflow-engine/src/reconciler.ts
+++ b/paperclip-addon/plugins/workflow-engine/src/reconciler.ts
@@ -8,65 +8,22 @@
   listStepRuns,
   updateStepRun,
   updateWorkflowRun,
-  type WorkflowDefinition,
-  type WorkflowRun,
-  type WorkflowStepRun,
 } from "./workflow-store.js";
 import type { WorkflowStep } from "./dag-engine.js";
-
-type WorkflowRunRecord = PluginEntityRecord & { data: WorkflowRun };
-type WorkflowStepRunRecord = PluginEntityRecord & { data: WorkflowStepRun };
-type WorkflowDefinitionRecord = PluginEntityRecord & { data: WorkflowDefinition };
-type StepAgentMetadata = WorkflowStep & {
-  agent?: string;
-  agentName?: string;
-  assigneeAgentName?: string;
-};
+import {
+  TERMINAL_STEP_STATUSES,
+  findStepDefinition,
+  getStepAgentName,
+  toWorkflowDefinitionRecord,
+  toWorkflowRunRecord,
+  toWorkflowStepRunRecord,
+} from "./workflow-utils.js";
 
 const STALE_THRESHOLD_MS = 300_000;
-const TERMINAL_STEP_STATUSES = new Set<string>([
-  STEP_STATUSES.done,
-  STEP_STATUSES.failed,
-  STEP_STATUSES.skipped,
-  STEP_STATUSES.escalated,
-]);
 
 function isStale(record: PluginEntityRecord, thresholdMs: number): boolean {
   const updatedAt = new Date(record.updatedAt).getTime();
   return Date.now() - updatedAt > thresholdMs;
-}
-
-function toWorkflowRunRecord(record: PluginEntityRecord): WorkflowRunRecord {
-  return record as WorkflowRunRecord;
-}
-
-function toWorkflowStepRunRecord(record: PluginEntityRecord): WorkflowStepRunRecord {
-  return record as WorkflowStepRunRecord;
-}
-
-function toWorkflowDefinitionRecord(record: PluginEntityRecord): WorkflowDefinitionRecord {
-  return record as WorkflowDefinitionRecord;
-}
-
-function findStepDefinition(definition: WorkflowDefinitionRecord, stepId: string): WorkflowStep | null {
-  return definition.data.steps.find((step: WorkflowStep) => step.id === stepId) ?? null;
-}
-
-function getStepAgentName(stepRun: WorkflowStepRunRecord, stepDef: WorkflowStep): string | null {
-  if (typeof stepRun.data.agentName === "string" && stepRun.data.agentName.trim()) {
-    return stepRun.data.agentName.trim();
-  }
-
-  const stepMeta = stepDef as StepAgentMetadata;
-  const candidates = [stepMeta.agentName, stepMeta.agent, stepMeta.assigneeAgentName];
-
-  for (const candidate of candidates) {
-    if (typeof candidate === "string" && candidate.trim()) {
-      return candidate.trim();
-    }
-  }
-
-  return null;
 }
 
 async function invokeAgentByName(
diff --git a/paperclip-addon/plugins/workflow-engine/src/workflow-store.ts b/paperclip-addon/plugins/workflow-engine/src/workflow-store.ts
index 5871a2082258cebcfe8b69ebafbb0814cf303d50..3d7fb2b3bc75911cfd9093eb3a4054cc73196bb9
--- a/paperclip-addon/plugins/workflow-engine/src/workflow-store.ts
+++ b/paperclip-addon/plugins/workflow-engine/src/workflow-store.ts
@@ -44,7 +44,14 @@
 
 type TypedEntityRecord<T> = Omit<PluginEntityRecord, "data"> & { data: T };
 
-function toTypedRecord<T>(record: PluginEntityRecord): TypedEntityRecord<T> {
+function toTypedRecord<T>(
+  record: PluginEntityRecord,
+  entityType: string,
+): TypedEntityRecord<T> {
+  if (record.entityType !== entityType) {
+    throw new Error(`Expected entity type "${entityType}", got "${record.entityType}"`);
+  }
+
   return record as TypedEntityRecord<T>;
 }
 
@@ -67,13 +74,26 @@
   id: string,
   entityType: string,
 ): Promise<TypedEntityRecord<T> | null> {
+  const listQuery: Parameters<PluginContext["entities"]["list"]>[0] & { id?: string } = {
+    entityType,
+    id,
+    limit: 1,
+  };
+
+  const listed = await ctx.entities.list(listQuery);
+  const listedRecord = listed.find((record) => record.id === id) ?? null;
+  if (listedRecord) {
+    return toTypedRecord<T>(listedRecord, entityType);
+  }
+
+  // Fallback: some host SDK variants ignore `id` on entities.list().
+  // Keep defensive type verification on direct get() until id-filter support is universal.
   const record = await ctx.entities.get(id);
-
-  if (!record || record.entityType !== entityType) {
+  if (!record) {
     return null;
   }
 
-  return toTypedRecord<T>(record);
+  return toTypedRecord<T>(record, entityType);
 }
 
 async function requireEntityByType<T>(
diff --git a/paperclip-addon/plugins/workflow-engine/src/workflow-utils.ts b/paperclip-addon/plugins/workflow-engine/src/workflow-utils.ts
new file mode 100644
index 0000000000000000000000000000000000000000..ec1cf6f6b9aa722b126b58c0a728b4e538ad1494
--- /dev/null
+++ b/paperclip-addon/plugins/workflow-engine/src/workflow-utils.ts
@@ -0,0 +1,62 @@
+import type { PluginEntityRecord } from "@paperclipai/plugin-sdk";
+
+import { STEP_STATUSES } from "./constants.js";
+import type { WorkflowStep } from "./dag-engine.js";
+import type {
+  WorkflowDefinition,
+  WorkflowRun,
+  WorkflowStepRun,
+} from "./workflow-store.js";
+
+export type WorkflowRunRecord = PluginEntityRecord & { data: WorkflowRun };
+export type WorkflowStepRunRecord = PluginEntityRecord & { data: WorkflowStepRun };
+export type WorkflowDefinitionRecord = PluginEntityRecord & { data: WorkflowDefinition };
+type StepAgentMetadata = WorkflowStep & {
+  agent?: string;
+  agentName?: string;
+  assigneeAgentName?: string;
+};
+
+export const TERMINAL_STEP_STATUSES = new Set<string>([
+  STEP_STATUSES.done,
+  STEP_STATUSES.failed,
+  STEP_STATUSES.skipped,
+  STEP_STATUSES.escalated,
+]);
+
+export function toWorkflowRunRecord(record: PluginEntityRecord): WorkflowRunRecord {
+  return record as WorkflowRunRecord;
+}
+
+export function toWorkflowStepRunRecord(record: PluginEntityRecord): WorkflowStepRunRecord {
+  return record as WorkflowStepRunRecord;
+}
+
+export function toWorkflowDefinitionRecord(record: PluginEntityRecord): WorkflowDefinitionRecord {
+  return record as WorkflowDefinitionRecord;
+}
+
+export function findStepDefinition(definition: WorkflowDefinitionRecord, stepId: string): WorkflowStep | null {
+  return definition.data.steps.find((step: WorkflowStep) => step.id === stepId) ?? null;
+}
+
+export function getStepAgentNameHint(stepDef: WorkflowStep): string | null {
+  const stepMeta = stepDef as StepAgentMetadata;
+  const candidates = [stepMeta.agentName, stepMeta.agent, stepMeta.assigneeAgentName];
+
+  for (const candidate of candidates) {
+    if (typeof candidate === "string" && candidate.trim()) {
+      return candidate.trim();
+    }
+  }
+
+  return null;
+}
+
+export function getStepAgentName(stepRun: WorkflowStepRunRecord, stepDef: WorkflowStep): string | null {
+  if (typeof stepRun.data.agentName === "string" && stepRun.data.agentName.trim()) {
+    return stepRun.data.agentName.trim();
+  }
+
+  return getStepAgentNameHint(stepDef);
+}

file update:
diff --git a/paperclip-addon/plugins/workflow-engine/src/dag-engine.ts b/paperclip-addon/plugins/workflow-engine/src/dag-engine.ts
index b59510f0473171038be77277286d3b5c2063e6c5..e7f21ff998cab78b9325a8aeacf4fda9c5e06553
--- a/paperclip-addon/plugins/workflow-engine/src/dag-engine.ts
+++ b/paperclip-addon/plugins/workflow-engine/src/dag-engine.ts
@@ -2,6 +2,7 @@
   id: string;
   title: string;
   dependsOn: string[];
+  sessionMode?: "fresh" | "reuse";
   onFailure?: "retry" | "skip" | "abort_workflow" | "escalate";
   escalateTo?: string;
   maxRetries?: number;
diff --git a/paperclip-addon/plugins/workflow-engine/src/reconciler.ts b/paperclip-addon/plugins/workflow-engine/src/reconciler.ts
index 14ab4e766a835d145a6c2e7b1b564270285cbdc3..d2ab54817bd8185944379d2188bfbdef1722020a
--- a/paperclip-addon/plugins/workflow-engine/src/reconciler.ts
+++ b/paperclip-addon/plugins/workflow-engine/src/reconciler.ts
@@ -8,65 +8,22 @@
   listStepRuns,
   updateStepRun,
   updateWorkflowRun,
-  type WorkflowDefinition,
-  type WorkflowRun,
-  type WorkflowStepRun,
 } from "./workflow-store.js";
 import type { WorkflowStep } from "./dag-engine.js";
-
-type WorkflowRunRecord = PluginEntityRecord & { data: WorkflowRun };
-type WorkflowStepRunRecord = PluginEntityRecord & { data: WorkflowStepRun };
-type WorkflowDefinitionRecord = PluginEntityRecord & { data: WorkflowDefinition };
-type StepAgentMetadata = WorkflowStep & {
-  agent?: string;
-  agentName?: string;
-  assigneeAgentName?: string;
-};
+import {
+  TERMINAL_STEP_STATUSES,
+  findStepDefinition,
+  getStepAgentName,
+  toWorkflowDefinitionRecord,
+  toWorkflowRunRecord,
+  toWorkflowStepRunRecord,
+} from "./workflow-utils.js";
 
 const STALE_THRESHOLD_MS = 300_000;
-const TERMINAL_STEP_STATUSES = new Set<string>([
-  STEP_STATUSES.done,
-  STEP_STATUSES.failed,
-  STEP_STATUSES.skipped,
-  STEP_STATUSES.escalated,
-]);
 
 function isStale(record: PluginEntityRecord, thresholdMs: number): boolean {
   const updatedAt = new Date(record.updatedAt).getTime();
   return Date.now() - updatedAt > thresholdMs;
-}
-
-function toWorkflowRunRecord(record: PluginEntityRecord): WorkflowRunRecord {
-  return record as WorkflowRunRecord;
-}
-
-function toWorkflowStepRunRecord(record: PluginEntityRecord): WorkflowStepRunRecord {
-  return record as WorkflowStepRunRecord;
-}
-
-function toWorkflowDefinitionRecord(record: PluginEntityRecord): WorkflowDefinitionRecord {
-  return record as WorkflowDefinitionRecord;
-}
-
-function findStepDefinition(definition: WorkflowDefinitionRecord, stepId: string): WorkflowStep | null {
-  return definition.data.steps.find((step: WorkflowStep) => step.id === stepId) ?? null;
-}
-
-function getStepAgentName(stepRun: WorkflowStepRunRecord, stepDef: WorkflowStep): string | null {
-  if (typeof stepRun.data.agentName === "string" && stepRun.data.agentName.trim()) {
-    return stepRun.data.agentName.trim();
-  }
-
-  const stepMeta = stepDef as StepAgentMetadata;
-  const candidates = [stepMeta.agentName, stepMeta.agent, stepMeta.assigneeAgentName];
-
-  for (const candidate of candidates) {
-    if (typeof candidate === "string" && candidate.trim()) {
-      return candidate.trim();
-    }
-  }
-
-  return null;
 }
 
 async function invokeAgentByName(
diff --git a/paperclip-addon/plugins/workflow-engine/src/workflow-store.ts b/paperclip-addon/plugins/workflow-engine/src/workflow-store.ts
index 5871a2082258cebcfe8b69ebafbb0814cf303d50..3d7fb2b3bc75911cfd9093eb3a4054cc73196bb9
--- a/paperclip-addon/plugins/workflow-engine/src/workflow-store.ts
+++ b/paperclip-addon/plugins/workflow-engine/src/workflow-store.ts
@@ -44,7 +44,14 @@
 
 type TypedEntityRecord<T> = Omit<PluginEntityRecord, "data"> & { data: T };
 
-function toTypedRecord<T>(record: PluginEntityRecord): TypedEntityRecord<T> {
+function toTypedRecord<T>(
+  record: PluginEntityRecord,
+  entityType: string,
+): TypedEntityRecord<T> {
+  if (record.entityType !== entityType) {
+    throw new Error(`Expected entity type "${entityType}", got "${record.entityType}"`);
+  }
+
   return record as TypedEntityRecord<T>;
 }
 
@@ -67,13 +74,26 @@
   id: string,
   entityType: string,
 ): Promise<TypedEntityRecord<T> | null> {
+  const listQuery: Parameters<PluginContext["entities"]["list"]>[0] & { id?: string } = {
+    entityType,
+    id,
+    limit: 1,
+  };
+
+  const listed = await ctx.entities.list(listQuery);
+  const listedRecord = listed.find((record) => record.id === id) ?? null;
+  if (listedRecord) {
+    return toTypedRecord<T>(listedRecord, entityType);
+  }
+
+  // Fallback: some host SDK variants ignore `id` on entities.list().
+  // Keep defensive type verification on direct get() until id-filter support is universal.
   const record = await ctx.entities.get(id);
-
-  if (!record || record.entityType !== entityType) {
+  if (!record) {
     return null;
   }
 
-  return toTypedRecord<T>(record);
+  return toTypedRecord<T>(record, entityType);
 }
 
 async function requireEntityByType<T>(
diff --git a/paperclip-addon/plugins/workflow-engine/src/workflow-utils.ts b/paperclip-addon/plugins/workflow-engine/src/workflow-utils.ts
new file mode 100644
index 0000000000000000000000000000000000000000..ec1cf6f6b9aa722b126b58c0a728b4e538ad1494
--- /dev/null
+++ b/paperclip-addon/plugins/workflow-engine/src/workflow-utils.ts
@@ -0,0 +1,62 @@
+import type { PluginEntityRecord } from "@paperclipai/plugin-sdk";
+
+import { STEP_STATUSES } from "./constants.js";
+import type { WorkflowStep } from "./dag-engine.js";
+import type {
+  WorkflowDefinition,
+  WorkflowRun,
+  WorkflowStepRun,
+} from "./workflow-store.js";
+
+export type WorkflowRunRecord = PluginEntityRecord & { data: WorkflowRun };
+export type WorkflowStepRunRecord = PluginEntityRecord & { data: WorkflowStepRun };
+export type WorkflowDefinitionRecord = PluginEntityRecord & { data: WorkflowDefinition };
+type StepAgentMetadata = WorkflowStep & {
+  agent?: string;
+  agentName?: string;
+  assigneeAgentName?: string;
+};
+
+export const TERMINAL_STEP_STATUSES = new Set<string>([
+  STEP_STATUSES.done,
+  STEP_STATUSES.failed,
+  STEP_STATUSES.skipped,
+  STEP_STATUSES.escalated,
+]);
+
+export function toWorkflowRunRecord(record: PluginEntityRecord): WorkflowRunRecord {
+  return record as WorkflowRunRecord;
+}
+
+export function toWorkflowStepRunRecord(record: PluginEntityRecord): WorkflowStepRunRecord {
+  return record as WorkflowStepRunRecord;
+}
+
+export function toWorkflowDefinitionRecord(record: PluginEntityRecord): WorkflowDefinitionRecord {
+  return record as WorkflowDefinitionRecord;
+}
+
+export function findStepDefinition(definition: WorkflowDefinitionRecord, stepId: string): WorkflowStep | null {
+  return definition.data.steps.find((step: WorkflowStep) => step.id === stepId) ?? null;
+}
+
+export function getStepAgentNameHint(stepDef: WorkflowStep): string | null {
+  const stepMeta = stepDef as StepAgentMetadata;
+  const candidates = [stepMeta.agentName, stepMeta.agent, stepMeta.assigneeAgentName];
+
+  for (const candidate of candidates) {
+    if (typeof candidate === "string" && candidate.trim()) {
+      return candidate.trim();
+    }
+  }
+
+  return null;
+}
+
+export function getStepAgentName(stepRun: WorkflowStepRunRecord, stepDef: WorkflowStep): string | null {
+  if (typeof stepRun.data.agentName === "string" && stepRun.data.agentName.trim()) {
+    return stepRun.data.agentName.trim();
+  }
+
+  return getStepAgentNameHint(stepDef);
+}

file update
M /Users/kwak/Projects/paperclip/paperclip-addon/plugins/workflow-engine/src/worker.ts
@@ -17,2 +17,3 @@
   checkIdempotency,
+  createWorkflowRun,
   createStepRun,
@@ -27,16 +28,21 @@
   updateWorkflowRun,
-  type WorkflowDefinition,
-  type WorkflowRun,
   type WorkflowStepRun,
 } from "./workflow-store.js";
+import {
+  TERMINAL_STEP_STATUSES,
+  findStepDefinition,
+  getStepAgentName,
+  getStepAgentNameHint,
+  toWorkflowDefinitionRecord,
+  toWorkflowRunRecord,
+  toWorkflowStepRunRecord,
+  type WorkflowDefinitionRecord,
+  type WorkflowRunRecord,
+  type WorkflowStepRunRecord,
+} from "./workflow-utils.js";
 
-type WorkflowStepRecord = PluginEntityRecord & { data: WorkflowStepRun };
-type WorkflowRunRecord = PluginEntityRecord & { data: WorkflowRun };
-type WorkflowDefinitionRecord = PluginEntityRecord & { data: WorkflowDefinition };
 type WorkflowStepMetadata = WorkflowStep & {
   description?: string;
-  agent?: string;
-  agentName?: string;
-  assigneeAgentName?: string;
   assigneeAgentId?: string;
+  sessionMode?: "fresh" | "reuse";
 };
@@ -46,9 +52,2 @@
 };
-
-const TERMINAL_STEP_STATUSES = new Set<string>([
-  STEP_STATUSES.done,
-  STEP_STATUSES.failed,
-  STEP_STATUSES.skipped,
-  STEP_STATUSES.escalated,
-]);
 
@@ -62,14 +61,2 @@
 
-function toStepRunRecord(record: PluginEntityRecord): WorkflowStepRecord {
-  return record as WorkflowStepRecord;
-}
-
-function toWorkflowRunRecord(record: PluginEntityRecord): WorkflowRunRecord {
-  return record as WorkflowRunRecord;
-}
-
-function toWorkflowDefinitionRecord(record: PluginEntityRecord): WorkflowDefinitionRecord {
-  return record as WorkflowDefinitionRecord;
-}
-
 function buildIdempotencyKey(event: PluginEvent): string {
@@ -82,6 +69,2 @@
 
-function findStepDefinition(definition: WorkflowDefinitionRecord, stepId: string): WorkflowStep | null {
-  return definition.data.steps.find((step: WorkflowStep) => step.id === stepId) ?? null;
-}
-
 function getStepDescription(stepDef: WorkflowStep): string | undefined {
@@ -89,15 +72,2 @@
   return typeof description === "string" && description.trim() ? description.trim() : undefined;
-}
-
-function getStepAgentNameHint(stepDef: WorkflowStep): string | null {
-  const meta = getStepMetadata(stepDef);
-  const candidates = [meta.agentName, meta.agent, meta.assigneeAgentName];
-
-  for (const candidate of candidates) {
-    if (typeof candidate === "string" && candidate.trim()) {
-      return candidate.trim();
-    }
-  }
-
-  return null;
 }
apply_patch(auto_approved=true) exited 0 in 99ms:
Success. Updated the following files:
M /Users/kwak/Projects/paperclip/paperclip-addon/plugins/workflow-engine/src/worker.ts
file update:
diff --git a/paperclip-addon/plugins/workflow-engine/src/dag-engine.ts b/paperclip-addon/plugins/workflow-engine/src/dag-engine.ts
index b59510f0473171038be77277286d3b5c2063e6c5..e7f21ff998cab78b9325a8aeacf4fda9c5e06553
--- a/paperclip-addon/plugins/workflow-engine/src/dag-engine.ts
+++ b/paperclip-addon/plugins/workflow-engine/src/dag-engine.ts
@@ -2,6 +2,7 @@
   id: string;
   title: string;
   dependsOn: string[];
+  sessionMode?: "fresh" | "reuse";
   onFailure?: "retry" | "skip" | "abort_workflow" | "escalate";
   escalateTo?: string;
   maxRetries?: number;
diff --git a/paperclip-addon/plugins/workflow-engine/src/reconciler.ts b/paperclip-addon/plugins/workflow-engine/src/reconciler.ts
index 14ab4e766a835d145a6c2e7b1b564270285cbdc3..d2ab54817bd8185944379d2188bfbdef1722020a
--- a/paperclip-addon/plugins/workflow-engine/src/reconciler.ts
+++ b/paperclip-addon/plugins/workflow-engine/src/reconciler.ts
@@ -8,65 +8,22 @@
   listStepRuns,
   updateStepRun,
   updateWorkflowRun,
-  type WorkflowDefinition,
-  type WorkflowRun,
-  type WorkflowStepRun,
 } from "./workflow-store.js";
 import type { WorkflowStep } from "./dag-engine.js";
-
-type WorkflowRunRecord = PluginEntityRecord & { data: WorkflowRun };
-type WorkflowStepRunRecord = PluginEntityRecord & { data: WorkflowStepRun };
-type WorkflowDefinitionRecord = PluginEntityRecord & { data: WorkflowDefinition };
-type StepAgentMetadata = WorkflowStep & {
-  agent?: string;
-  agentName?: string;
-  assigneeAgentName?: string;
-};
+import {
+  TERMINAL_STEP_STATUSES,
+  findStepDefinition,
+  getStepAgentName,
+  toWorkflowDefinitionRecord,
+  toWorkflowRunRecord,
+  toWorkflowStepRunRecord,
+} from "./workflow-utils.js";
 
 const STALE_THRESHOLD_MS = 300_000;
-const TERMINAL_STEP_STATUSES = new Set<string>([
-  STEP_STATUSES.done,
-  STEP_STATUSES.failed,
-  STEP_STATUSES.skipped,
-  STEP_STATUSES.escalated,
-]);
 
 function isStale(record: PluginEntityRecord, thresholdMs: number): boolean {
   const updatedAt = new Date(record.updatedAt).getTime();
   return Date.now() - updatedAt > thresholdMs;
-}
-
-function toWorkflowRunRecord(record: PluginEntityRecord): WorkflowRunRecord {
-  return record as WorkflowRunRecord;
-}
-
-function toWorkflowStepRunRecord(record: PluginEntityRecord): WorkflowStepRunRecord {
-  return record as WorkflowStepRunRecord;
-}
-
-function toWorkflowDefinitionRecord(record: PluginEntityRecord): WorkflowDefinitionRecord {
-  return record as WorkflowDefinitionRecord;
-}
-
-function findStepDefinition(definition: WorkflowDefinitionRecord, stepId: string): WorkflowStep | null {
-  return definition.data.steps.find((step: WorkflowStep) => step.id === stepId) ?? null;
-}
-
-function getStepAgentName(stepRun: WorkflowStepRunRecord, stepDef: WorkflowStep): string | null {
-  if (typeof stepRun.data.agentName === "string" && stepRun.data.agentName.trim()) {
-    return stepRun.data.agentName.trim();
-  }
-
-  const stepMeta = stepDef as StepAgentMetadata;
-  const candidates = [stepMeta.agentName, stepMeta.agent, stepMeta.assigneeAgentName];
-
-  for (const candidate of candidates) {
-    if (typeof candidate === "string" && candidate.trim()) {
-      return candidate.trim();
-    }
-  }
-
-  return null;
 }
 
 async function invokeAgentByName(
diff --git a/paperclip-addon/plugins/workflow-engine/src/worker.ts b/paperclip-addon/plugins/workflow-engine/src/worker.ts
index c5ac3fbb4ca7f767eb29331bd3630b635449b335..720dd3be2f380c021efa24351198ec0fad5696e1
--- a/paperclip-addon/plugins/workflow-engine/src/worker.ts
+++ b/paperclip-addon/plugins/workflow-engine/src/worker.ts
@@ -15,6 +15,7 @@
 } from "./dag-engine.js";
 import {
   checkIdempotency,
+  createWorkflowRun,
   createStepRun,
   findStepRunByIssueId,
   getWorkflowDefinition,
@@ -25,32 +26,30 @@
   markIdempotency,
   updateStepRun,
   updateWorkflowRun,
-  type WorkflowDefinition,
-  type WorkflowRun,
   type WorkflowStepRun,
 } from "./workflow-store.js";
+import {
+  TERMINAL_STEP_STATUSES,
+  findStepDefinition,
+  getStepAgentName,
+  getStepAgentNameHint,
+  toWorkflowDefinitionRecord,
+  toWorkflowRunRecord,
+  toWorkflowStepRunRecord,
+  type WorkflowDefinitionRecord,
+  type WorkflowRunRecord,
+  type WorkflowStepRunRecord,
+} from "./workflow-utils.js";
 
-type WorkflowStepRecord = PluginEntityRecord & { data: WorkflowStepRun };
-type WorkflowRunRecord = PluginEntityRecord & { data: WorkflowRun };
-type WorkflowDefinitionRecord = PluginEntityRecord & { data: WorkflowDefinition };
 type WorkflowStepMetadata = WorkflowStep & {
   description?: string;
-  agent?: string;
-  agentName?: string;
-  assigneeAgentName?: string;
   assigneeAgentId?: string;
+  sessionMode?: "fresh" | "reuse";
 };
 type AgentRecord = Awaited<ReturnType<PluginContext["agents"]["list"]>>[number];
 type ReconcilerModule = {
   reconcileStuckSteps?: (ctx: PluginContext) => Promise<void>;
 };
-
-const TERMINAL_STEP_STATUSES = new Set<string>([
-  STEP_STATUSES.done,
-  STEP_STATUSES.failed,
-  STEP_STATUSES.skipped,
-  STEP_STATUSES.escalated,
-]);
 
 function summarizeError(error: unknown): string {
   if (error instanceof Error) {
@@ -60,18 +59,6 @@
   return String(error);
 }
 
-function toStepRunRecord(record: PluginEntityRecord): WorkflowStepRecord {
-  return record as WorkflowStepRecord;
-}
-
-function toWorkflowRunRecord(record: PluginEntityRecord): WorkflowRunRecord {
-  return record as WorkflowRunRecord;
-}
-
-function toWorkflowDefinitionRecord(record: PluginEntityRecord): WorkflowDefinitionRecord {
-  return record as WorkflowDefinitionRecord;
-}
-
 function buildIdempotencyKey(event: PluginEvent): string {
   return `${event.eventType}:${event.eventId}`;
 }
@@ -80,26 +67,9 @@
   return stepDef as WorkflowStepMetadata;
 }
 
-function findStepDefinition(definition: WorkflowDefinitionRecord, stepId: string): WorkflowStep | null {
-  return definition.data.steps.find((step: WorkflowStep) => step.id === stepId) ?? null;
-}
-
 function getStepDescription(stepDef: WorkflowStep): string | undefined {
   const description = getStepMetadata(stepDef).description;
   return typeof description === "string" && description.trim() ? description.trim() : undefined;
-}
-
-function getStepAgentNameHint(stepDef: WorkflowStep): string | null {
-  const meta = getStepMetadata(stepDef);
-  const candidates = [meta.agentName, meta.agent, meta.assigneeAgentName];
-
-  for (const candidate of candidates) {
-    if (typeof candidate === "string" && candidate.trim()) {
-      return candidate.trim();
-    }
-  }
-
-  return null;
 }
 
 function getStepAgentIdHint(stepDef: WorkflowStep): string | null {
diff --git a/paperclip-addon/plugins/workflow-engine/src/workflow-store.ts b/paperclip-addon/plugins/workflow-engine/src/workflow-store.ts
index 5871a2082258cebcfe8b69ebafbb0814cf303d50..3d7fb2b3bc75911cfd9093eb3a4054cc73196bb9
--- a/paperclip-addon/plugins/workflow-engine/src/workflow-store.ts
+++ b/paperclip-addon/plugins/workflow-engine/src/workflow-store.ts
@@ -44,7 +44,14 @@
 
 type TypedEntityRecord<T> = Omit<PluginEntityRecord, "data"> & { data: T };
 
-function toTypedRecord<T>(record: PluginEntityRecord): TypedEntityRecord<T> {
+function toTypedRecord<T>(
+  record: PluginEntityRecord,
+  entityType: string,
+): TypedEntityRecord<T> {
+  if (record.entityType !== entityType) {
+    throw new Error(`Expected entity type "${entityType}", got "${record.entityType}"`);
+  }
+
   return record as TypedEntityRecord<T>;
 }
 
@@ -67,13 +74,26 @@
   id: string,
   entityType: string,
 ): Promise<TypedEntityRecord<T> | null> {
+  const listQuery: Parameters<PluginContext["entities"]["list"]>[0] & { id?: string } = {
+    entityType,
+    id,
+    limit: 1,
+  };
+
+  const listed = await ctx.entities.list(listQuery);
+  const listedRecord = listed.find((record) => record.id === id) ?? null;
+  if (listedRecord) {
+    return toTypedRecord<T>(listedRecord, entityType);
+  }
+
+  // Fallback: some host SDK variants ignore `id` on entities.list().
+  // Keep defensive type verification on direct get() until id-filter support is universal.
   const record = await ctx.entities.get(id);
-
-  if (!record || record.entityType !== entityType) {
+  if (!record) {
     return null;
   }
 
-  return toTypedRecord<T>(record);
+  return toTypedRecord<T>(record, entityType);
 }
 
 async function requireEntityByType<T>(
diff --git a/paperclip-addon/plugins/workflow-engine/src/workflow-utils.ts b/paperclip-addon/plugins/workflow-engine/src/workflow-utils.ts
new file mode 100644
index 0000000000000000000000000000000000000000..ec1cf6f6b9aa722b126b58c0a728b4e538ad1494
--- /dev/null
+++ b/paperclip-addon/plugins/workflow-engine/src/workflow-utils.ts
@@ -0,0 +1,62 @@
+import type { PluginEntityRecord } from "@paperclipai/plugin-sdk";
+
+import { STEP_STATUSES } from "./constants.js";
+import type { WorkflowStep } from "./dag-engine.js";
+import type {
+  WorkflowDefinition,
+  WorkflowRun,
+  WorkflowStepRun,
+} from "./workflow-store.js";
+
+export type WorkflowRunRecord = PluginEntityRecord & { data: WorkflowRun };
+export type WorkflowStepRunRecord = PluginEntityRecord & { data: WorkflowStepRun };
+export type WorkflowDefinitionRecord = PluginEntityRecord & { data: WorkflowDefinition };
+type StepAgentMetadata = WorkflowStep & {
+  agent?: string;
+  agentName?: string;
+  assigneeAgentName?: string;
+};
+
+export const TERMINAL_STEP_STATUSES = new Set<string>([
+  STEP_STATUSES.done,
+  STEP_STATUSES.failed,
+  STEP_STATUSES.skipped,
+  STEP_STATUSES.escalated,
+]);
+
+export function toWorkflowRunRecord(record: PluginEntityRecord): WorkflowRunRecord {
+  return record as WorkflowRunRecord;
+}
+
+export function toWorkflowStepRunRecord(record: PluginEntityRecord): WorkflowStepRunRecord {
+  return record as WorkflowStepRunRecord;
+}
+
+export function toWorkflowDefinitionRecord(record: PluginEntityRecord): WorkflowDefinitionRecord {
+  return record as WorkflowDefinitionRecord;
+}
+
+export function findStepDefinition(definition: WorkflowDefinitionRecord, stepId: string): WorkflowStep | null {
+  return definition.data.steps.find((step: WorkflowStep) => step.id === stepId) ?? null;
+}
+
+export function getStepAgentNameHint(stepDef: WorkflowStep): string | null {
+  const stepMeta = stepDef as StepAgentMetadata;
+  const candidates = [stepMeta.agentName, stepMeta.agent, stepMeta.assigneeAgentName];
+
+  for (const candidate of candidates) {
+    if (typeof candidate === "string" && candidate.trim()) {
+      return candidate.trim();
+    }
+  }
+
+  return null;
+}
+
+export function getStepAgentName(stepRun: WorkflowStepRunRecord, stepDef: WorkflowStep): string | null {
+  if (typeof stepRun.data.agentName === "string" && stepRun.data.agentName.trim()) {
+    return stepRun.data.agentName.trim();
+  }
+
+  return getStepAgentNameHint(stepDef);
+}

file update:
diff --git a/paperclip-addon/plugins/workflow-engine/src/dag-engine.ts b/paperclip-addon/plugins/workflow-engine/src/dag-engine.ts
index b59510f0473171038be77277286d3b5c2063e6c5..e7f21ff998cab78b9325a8aeacf4fda9c5e06553
--- a/paperclip-addon/plugins/workflow-engine/src/dag-engine.ts
+++ b/paperclip-addon/plugins/workflow-engine/src/dag-engine.ts
@@ -2,6 +2,7 @@
   id: string;
   title: string;
   dependsOn: string[];
+  sessionMode?: "fresh" | "reuse";
   onFailure?: "retry" | "skip" | "abort_workflow" | "escalate";
   escalateTo?: string;
   maxRetries?: number;
diff --git a/paperclip-addon/plugins/workflow-engine/src/reconciler.ts b/paperclip-addon/plugins/workflow-engine/src/reconciler.ts
index 14ab4e766a835d145a6c2e7b1b564270285cbdc3..d2ab54817bd8185944379d2188bfbdef1722020a
--- a/paperclip-addon/plugins/workflow-engine/src/reconciler.ts
+++ b/paperclip-addon/plugins/workflow-engine/src/reconciler.ts
@@ -8,65 +8,22 @@
   listStepRuns,
   updateStepRun,
   updateWorkflowRun,
-  type WorkflowDefinition,
-  type WorkflowRun,
-  type WorkflowStepRun,
 } from "./workflow-store.js";
 import type { WorkflowStep } from "./dag-engine.js";
-
-type WorkflowRunRecord = PluginEntityRecord & { data: WorkflowRun };
-type WorkflowStepRunRecord = PluginEntityRecord & { data: WorkflowStepRun };
-type WorkflowDefinitionRecord = PluginEntityRecord & { data: WorkflowDefinition };
-type StepAgentMetadata = WorkflowStep & {
-  agent?: string;
-  agentName?: string;
-  assigneeAgentName?: string;
-};
+import {
+  TERMINAL_STEP_STATUSES,
+  findStepDefinition,
+  getStepAgentName,
+  toWorkflowDefinitionRecord,
+  toWorkflowRunRecord,
+  toWorkflowStepRunRecord,
+} from "./workflow-utils.js";
 
 const STALE_THRESHOLD_MS = 300_000;
-const TERMINAL_STEP_STATUSES = new Set<string>([
-  STEP_STATUSES.done,
-  STEP_STATUSES.failed,
-  STEP_STATUSES.skipped,
-  STEP_STATUSES.escalated,
-]);
 
 function isStale(record: PluginEntityRecord, thresholdMs: number): boolean {
   const updatedAt = new Date(record.updatedAt).getTime();
   return Date.now() - updatedAt > thresholdMs;
-}
-
-function toWorkflowRunRecord(record: PluginEntityRecord): WorkflowRunRecord {
-  return record as WorkflowRunRecord;
-}
-
-function toWorkflowStepRunRecord(record: PluginEntityRecord): WorkflowStepRunRecord {
-  return record as WorkflowStepRunRecord;
-}
-
-function toWorkflowDefinitionRecord(record: PluginEntityRecord): WorkflowDefinitionRecord {
-  return record as WorkflowDefinitionRecord;
-}
-
-function findStepDefinition(definition: WorkflowDefinitionRecord, stepId: string): WorkflowStep | null {
-  return definition.data.steps.find((step: WorkflowStep) => step.id === stepId) ?? null;
-}
-
-function getStepAgentName(stepRun: WorkflowStepRunRecord, stepDef: WorkflowStep): string | null {
-  if (typeof stepRun.data.agentName === "string" && stepRun.data.agentName.trim()) {
-    return stepRun.data.agentName.trim();
-  }
-
-  const stepMeta = stepDef as StepAgentMetadata;
-  const candidates = [stepMeta.agentName, stepMeta.agent, stepMeta.assigneeAgentName];
-
-  for (const candidate of candidates) {
-    if (typeof candidate === "string" && candidate.trim()) {
-      return candidate.trim();
-    }
-  }
-
-  return null;
 }
 
 async function invokeAgentByName(
diff --git a/paperclip-addon/plugins/workflow-engine/src/worker.ts b/paperclip-addon/plugins/workflow-engine/src/worker.ts
index c5ac3fbb4ca7f767eb29331bd3630b635449b335..720dd3be2f380c021efa24351198ec0fad5696e1
--- a/paperclip-addon/plugins/workflow-engine/src/worker.ts
+++ b/paperclip-addon/plugins/workflow-engine/src/worker.ts
@@ -15,6 +15,7 @@
 } from "./dag-engine.js";
 import {
   checkIdempotency,
+  createWorkflowRun,
   createStepRun,
   findStepRunByIssueId,
   getWorkflowDefinition,
@@ -25,32 +26,30 @@
   markIdempotency,
   updateStepRun,
   updateWorkflowRun,
-  type WorkflowDefinition,
-  type WorkflowRun,
   type WorkflowStepRun,
 } from "./workflow-store.js";
+import {
+  TERMINAL_STEP_STATUSES,
+  findStepDefinition,
+  getStepAgentName,
+  getStepAgentNameHint,
+  toWorkflowDefinitionRecord,
+  toWorkflowRunRecord,
+  toWorkflowStepRunRecord,
+  type WorkflowDefinitionRecord,
+  type WorkflowRunRecord,
+  type WorkflowStepRunRecord,
+} from "./workflow-utils.js";
 
-type WorkflowStepRecord = PluginEntityRecord & { data: WorkflowStepRun };
-type WorkflowRunRecord = PluginEntityRecord & { data: WorkflowRun };
-type WorkflowDefinitionRecord = PluginEntityRecord & { data: WorkflowDefinition };
 type WorkflowStepMetadata = WorkflowStep & {
   description?: string;
-  agent?: string;
-  agentName?: string;
-  assigneeAgentName?: string;
   assigneeAgentId?: string;
+  sessionMode?: "fresh" | "reuse";
 };
 type AgentRecord = Awaited<ReturnType<PluginContext["agents"]["list"]>>[number];
 type ReconcilerModule = {
   reconcileStuckSteps?: (ctx: PluginContext) => Promise<void>;
 };
-
-const TERMINAL_STEP_STATUSES = new Set<string>([
-  STEP_STATUSES.done,
-  STEP_STATUSES.failed,
-  STEP_STATUSES.skipped,
-  STEP_STATUSES.escalated,
-]);
 
 function summarizeError(error: unknown): string {
   if (error instanceof Error) {
@@ -60,18 +59,6 @@
   return String(error);
 }
 
-function toStepRunRecord(record: PluginEntityRecord): WorkflowStepRecord {
-  return record as WorkflowStepRecord;
-}
-
-function toWorkflowRunRecord(record: PluginEntityRecord): WorkflowRunRecord {
-  return record as WorkflowRunRecord;
-}
-
-function toWorkflowDefinitionRecord(record: PluginEntityRecord): WorkflowDefinitionRecord {
-  return record as WorkflowDefinitionRecord;
-}
-
 function buildIdempotencyKey(event: PluginEvent): string {
   return `${event.eventType}:${event.eventId}`;
 }
@@ -80,26 +67,9 @@
   return stepDef as WorkflowStepMetadata;
 }
 
-function findStepDefinition(definition: WorkflowDefinitionRecord, stepId: string): WorkflowStep | null {
-  return definition.data.steps.find((step: WorkflowStep) => step.id === stepId) ?? null;
-}
-
 function getStepDescription(stepDef: WorkflowStep): string | undefined {
   const description = getStepMetadata(stepDef).description;
   return typeof description === "string" && description.trim() ? description.trim() : undefined;
-}
-
-function getStepAgentNameHint(stepDef: WorkflowStep): string | null {
-  const meta = getStepMetadata(stepDef);
-  const candidates = [meta.agentName, meta.agent, meta.assigneeAgentName];
-
-  for (const candidate of candidates) {
-    if (typeof candidate === "string" && candidate.trim()) {
-      return candidate.trim();
-    }
-  }
-
-  return null;
 }
 
 function getStepAgentIdHint(stepDef: WorkflowStep): string | null {
diff --git a/paperclip-addon/plugins/workflow-engine/src/workflow-store.ts b/paperclip-addon/plugins/workflow-engine/src/workflow-store.ts
index 5871a2082258cebcfe8b69ebafbb0814cf303d50..3d7fb2b3bc75911cfd9093eb3a4054cc73196bb9
--- a/paperclip-addon/plugins/workflow-engine/src/workflow-store.ts
+++ b/paperclip-addon/plugins/workflow-engine/src/workflow-store.ts
@@ -44,7 +44,14 @@
 
 type TypedEntityRecord<T> = Omit<PluginEntityRecord, "data"> & { data: T };
 
-function toTypedRecord<T>(record: PluginEntityRecord): TypedEntityRecord<T> {
+function toTypedRecord<T>(
+  record: PluginEntityRecord,
+  entityType: string,
+): TypedEntityRecord<T> {
+  if (record.entityType !== entityType) {
+    throw new Error(`Expected entity type "${entityType}", got "${record.entityType}"`);
+  }
+
   return record as TypedEntityRecord<T>;
 }
 
@@ -67,13 +74,26 @@
   id: string,
   entityType: string,
 ): Promise<TypedEntityRecord<T> | null> {
+  const listQuery: Parameters<PluginContext["entities"]["list"]>[0] & { id?: string } = {
+    entityType,
+    id,
+    limit: 1,
+  };
+
+  const listed = await ctx.entities.list(listQuery);
+  const listedRecord = listed.find((record) => record.id === id) ?? null;
+  if (listedRecord) {
+    return toTypedRecord<T>(listedRecord, entityType);
+  }
+
+  // Fallback: some host SDK variants ignore `id` on entities.list().
+  // Keep defensive type verification on direct get() until id-filter support is universal.
   const record = await ctx.entities.get(id);
-
-  if (!record || record.entityType !== entityType) {
+  if (!record) {
     return null;
   }
 
-  return toTypedRecord<T>(record);
+  return toTypedRecord<T>(record, entityType);
 }
 
 async function requireEntityByType<T>(
diff --git a/paperclip-addon/plugins/workflow-engine/src/workflow-utils.ts b/paperclip-addon/plugins/workflow-engine/src/workflow-utils.ts
new file mode 100644
index 0000000000000000000000000000000000000000..ec1cf6f6b9aa722b126b58c0a728b4e538ad1494
--- /dev/null
+++ b/paperclip-addon/plugins/workflow-engine/src/workflow-utils.ts
@@ -0,0 +1,62 @@
+import type { PluginEntityRecord } from "@paperclipai/plugin-sdk";
+
+import { STEP_STATUSES } from "./constants.js";
+import type { WorkflowStep } from "./dag-engine.js";
+import type {
+  WorkflowDefinition,
+  WorkflowRun,
+  WorkflowStepRun,
+} from "./workflow-store.js";
+
+export type WorkflowRunRecord = PluginEntityRecord & { data: WorkflowRun };
+export type WorkflowStepRunRecord = PluginEntityRecord & { data: WorkflowStepRun };
+export type WorkflowDefinitionRecord = PluginEntityRecord & { data: WorkflowDefinition };
+type StepAgentMetadata = WorkflowStep & {
+  agent?: string;
+  agentName?: string;
+  assigneeAgentName?: string;
+};
+
+export const TERMINAL_STEP_STATUSES = new Set<string>([
+  STEP_STATUSES.done,
+  STEP_STATUSES.failed,
+  STEP_STATUSES.skipped,
+  STEP_STATUSES.escalated,
+]);
+
+export function toWorkflowRunRecord(record: PluginEntityRecord): WorkflowRunRecord {
+  return record as WorkflowRunRecord;
+}
+
+export function toWorkflowStepRunRecord(record: PluginEntityRecord): WorkflowStepRunRecord {
+  return record as WorkflowStepRunRecord;
+}
+
+export function toWorkflowDefinitionRecord(record: PluginEntityRecord): WorkflowDefinitionRecord {
+  return record as WorkflowDefinitionRecord;
+}
+
+export function findStepDefinition(definition: WorkflowDefinitionRecord, stepId: string): WorkflowStep | null {
+  return definition.data.steps.find((step: WorkflowStep) => step.id === stepId) ?? null;
+}
+
+export function getStepAgentNameHint(stepDef: WorkflowStep): string | null {
+  const stepMeta = stepDef as StepAgentMetadata;
+  const candidates = [stepMeta.agentName, stepMeta.agent, stepMeta.assigneeAgentName];
+
+  for (const candidate of candidates) {
+    if (typeof candidate === "string" && candidate.trim()) {
+      return candidate.trim();
+    }
+  }
+
+  return null;
+}
+
+export function getStepAgentName(stepRun: WorkflowStepRunRecord, stepDef: WorkflowStep): string | null {
+  if (typeof stepRun.data.agentName === "string" && stepRun.data.agentName.trim()) {
+    return stepRun.data.agentName.trim();
+  }
+
+  return getStepAgentNameHint(stepDef);
+}

exec
/bin/zsh -lc "nl -ba plugins/workflow-engine/src/worker.ts | sed -n '80,260p'" in /Users/kwak/Projects/paperclip/paperclip-addon succeeded in 0ms:
    80	
    81	  return assigneeAgentId.trim();
    82	}
    83	
    84	async function resolveStepAgent(
    85	  ctx: PluginContext,
    86	  companyId: string,
    87	  stepDef: WorkflowStep,
    88	  fallbackAgentName?: string,
    89	): Promise<{ agentId: string | null; agentName: string | null }> {
    90	  const preferredName = typeof fallbackAgentName === "string" && fallbackAgentName.trim()
    91	    ? fallbackAgentName.trim()
    92	    : getStepAgentNameHint(stepDef);
    93	
    94	  if (preferredName) {
    95	    const agents = await ctx.agents.list({ companyId });
    96	    const agent = agents.find((candidate: AgentRecord) => candidate.name === preferredName) ?? null;
    97	    return {
    98	      agentId: agent?.id ?? null,
    99	      agentName: agent?.name ?? preferredName,
file update:
diff --git a/paperclip-addon/plugins/workflow-engine/src/dag-engine.ts b/paperclip-addon/plugins/workflow-engine/src/dag-engine.ts
index b59510f0473171038be77277286d3b5c2063e6c5..e7f21ff998cab78b9325a8aeacf4fda9c5e06553
--- a/paperclip-addon/plugins/workflow-engine/src/dag-engine.ts
+++ b/paperclip-addon/plugins/workflow-engine/src/dag-engine.ts
@@ -2,6 +2,7 @@
   id: string;
   title: string;
   dependsOn: string[];
+  sessionMode?: "fresh" | "reuse";
   onFailure?: "retry" | "skip" | "abort_workflow" | "escalate";
   escalateTo?: string;
   maxRetries?: number;
diff --git a/paperclip-addon/plugins/workflow-engine/src/reconciler.ts b/paperclip-addon/plugins/workflow-engine/src/reconciler.ts
index 14ab4e766a835d145a6c2e7b1b564270285cbdc3..d2ab54817bd8185944379d2188bfbdef1722020a
--- a/paperclip-addon/plugins/workflow-engine/src/reconciler.ts
+++ b/paperclip-addon/plugins/workflow-engine/src/reconciler.ts
@@ -8,65 +8,22 @@
   listStepRuns,
   updateStepRun,
   updateWorkflowRun,
-  type WorkflowDefinition,
-  type WorkflowRun,
-  type WorkflowStepRun,
 } from "./workflow-store.js";
 import type { WorkflowStep } from "./dag-engine.js";
-
-type WorkflowRunRecord = PluginEntityRecord & { data: WorkflowRun };
-type WorkflowStepRunRecord = PluginEntityRecord & { data: WorkflowStepRun };
-type WorkflowDefinitionRecord = PluginEntityRecord & { data: WorkflowDefinition };
-type StepAgentMetadata = WorkflowStep & {
-  agent?: string;
-  agentName?: string;
-  assigneeAgentName?: string;
-};
+import {
+  TERMINAL_STEP_STATUSES,
+  findStepDefinition,
+  getStepAgentName,
+  toWorkflowDefinitionRecord,
+  toWorkflowRunRecord,
+  toWorkflowStepRunRecord,
+} from "./workflow-utils.js";
 
 const STALE_THRESHOLD_MS = 300_000;
-const TERMINAL_STEP_STATUSES = new Set<string>([
-  STEP_STATUSES.done,
-  STEP_STATUSES.failed,
-  STEP_STATUSES.skipped,
-  STEP_STATUSES.escalated,
-]);
 
 function isStale(record: PluginEntityRecord, thresholdMs: number): boolean {
   const updatedAt = new Date(record.updatedAt).getTime();
   return Date.now() - updatedAt > thresholdMs;
-}
-
-function toWorkflowRunRecord(record: PluginEntityRecord): WorkflowRunRecord {
-  return record as WorkflowRunRecord;
-}
-
-function toWorkflowStepRunRecord(record: PluginEntityRecord): WorkflowStepRunRecord {
-  return record as WorkflowStepRunRecord;
-}
-
-function toWorkflowDefinitionRecord(record: PluginEntityRecord): WorkflowDefinitionRecord {
-  return record as WorkflowDefinitionRecord;
-}
-
-function findStepDefinition(definition: WorkflowDefinitionRecord, stepId: string): WorkflowStep | null {
-  return definition.data.steps.find((step: WorkflowStep) => step.id === stepId) ?? null;
-}
-
-function getStepAgentName(stepRun: WorkflowStepRunRecord, stepDef: WorkflowStep): string | null {
-  if (typeof stepRun.data.agentName === "string" && stepRun.data.agentName.trim()) {
-    return stepRun.data.agentName.trim();
-  }
-
-  const stepMeta = stepDef as StepAgentMetadata;
-  const candidates = [stepMeta.agentName, stepMeta.agent, stepMeta.assigneeAgentName];
-
-  for (const candidate of candidates) {
-    if (typeof candidate === "string" && candidate.trim()) {
-      return candidate.trim();
-    }
-  }
-
-  return null;
 }
 
 async function invokeAgentByName(
diff --git a/paperclip-addon/plugins/workflow-engine/src/worker.ts b/paperclip-addon/plugins/workflow-engine/src/worker.ts
index c5ac3fbb4ca7f767eb29331bd3630b635449b335..720dd3be2f380c021efa24351198ec0fad5696e1
--- a/paperclip-addon/plugins/workflow-engine/src/worker.ts
+++ b/paperclip-addon/plugins/workflow-engine/src/worker.ts
@@ -15,6 +15,7 @@
 } from "./dag-engine.js";
 import {
   checkIdempotency,
+  createWorkflowRun,
   createStepRun,
   findStepRunByIssueId,
   getWorkflowDefinition,
@@ -25,32 +26,30 @@
   markIdempotency,
   updateStepRun,
   updateWorkflowRun,
-  type WorkflowDefinition,
-  type WorkflowRun,
   type WorkflowStepRun,
 } from "./workflow-store.js";
+import {
+  TERMINAL_STEP_STATUSES,
+  findStepDefinition,
+  getStepAgentName,
+  getStepAgentNameHint,
+  toWorkflowDefinitionRecord,
+  toWorkflowRunRecord,
+  toWorkflowStepRunRecord,
+  type WorkflowDefinitionRecord,
+  type WorkflowRunRecord,
+  type WorkflowStepRunRecord,
+} from "./workflow-utils.js";
 
-type WorkflowStepRecord = PluginEntityRecord & { data: WorkflowStepRun };
-type WorkflowRunRecord = PluginEntityRecord & { data: WorkflowRun };
-type WorkflowDefinitionRecord = PluginEntityRecord & { data: WorkflowDefinition };
 type WorkflowStepMetadata = WorkflowStep & {
   description?: string;
-  agent?: string;
-  agentName?: string;
-  assigneeAgentName?: string;
   assigneeAgentId?: string;
+  sessionMode?: "fresh" | "reuse";
 };
 type AgentRecord = Awaited<ReturnType<PluginContext["agents"]["list"]>>[number];
 type ReconcilerModule = {
   reconcileStuckSteps?: (ctx: PluginContext) => Promise<void>;
 };
-
-const TERMINAL_STEP_STATUSES = new Set<string>([
-  STEP_STATUSES.done,
-  STEP_STATUSES.failed,
-  STEP_STATUSES.skipped,
-  STEP_STATUSES.escalated,
-]);
 
 function summarizeError(error: unknown): string {
   if (error instanceof Error) {
@@ -60,18 +59,6 @@
   return String(error);
 }
 
-function toStepRunRecord(record: PluginEntityRecord): WorkflowStepRecord {
-  return record as WorkflowStepRecord;
-}
-
-function toWorkflowRunRecord(record: PluginEntityRecord): WorkflowRunRecord {
-  return record as WorkflowRunRecord;
-}
-
-function toWorkflowDefinitionRecord(record: PluginEntityRecord): WorkflowDefinitionRecord {
-  return record as WorkflowDefinitionRecord;
-}
-
 function buildIdempotencyKey(event: PluginEvent): string {
   return `${event.eventType}:${event.eventId}`;
 }
@@ -80,26 +67,9 @@
   return stepDef as WorkflowStepMetadata;
 }
 
-function findStepDefinition(definition: WorkflowDefinitionRecord, stepId: string): WorkflowStep | null {
-  return definition.data.steps.find((step: WorkflowStep) => step.id === stepId) ?? null;
-}
-
 function getStepDescription(stepDef: WorkflowStep): string | undefined {
   const description = getStepMetadata(stepDef).description;
   return typeof description === "string" && description.trim() ? description.trim() : undefined;
-}
-
-function getStepAgentNameHint(stepDef: WorkflowStep): string | null {
-  const meta = getStepMetadata(stepDef);
-  const candidates = [meta.agentName, meta.agent, meta.assigneeAgentName];
-
-  for (const candidate of candidates) {
-    if (typeof candidate === "string" && candidate.trim()) {
-      return candidate.trim();
-    }
-  }
-
-  return null;
 }
 
 function getStepAgentIdHint(stepDef: WorkflowStep): string | null {
diff --git a/paperclip-addon/plugins/workflow-engine/src/workflow-store.ts b/paperclip-addon/plugins/workflow-engine/src/workflow-store.ts
index 5871a2082258cebcfe8b69ebafbb0814cf303d50..3d7fb2b3bc75911cfd9093eb3a4054cc73196bb9
--- a/paperclip-addon/plugins/workflow-engine/src/workflow-store.ts
+++ b/paperclip-addon/plugins/workflow-engine/src/workflow-store.ts
@@ -44,7 +44,14 @@
 
 type TypedEntityRecord<T> = Omit<PluginEntityRecord, "data"> & { data: T };
 
-function toTypedRecord<T>(record: PluginEntityRecord): TypedEntityRecord<T> {
+function toTypedRecord<T>(
+  record: PluginEntityRecord,
+  entityType: string,
+): TypedEntityRecord<T> {
+  if (record.entityType !== entityType) {
+    throw new Error(`Expected entity type "${entityType}", got "${record.entityType}"`);
+  }
+
   return record as TypedEntityRecord<T>;
 }
 
@@ -67,13 +74,26 @@
   id: string,
   entityType: string,
 ): Promise<TypedEntityRecord<T> | null> {
+  const listQuery: Parameters<PluginContext["entities"]["list"]>[0] & { id?: string } = {
+    entityType,
+    id,
+    limit: 1,
+  };
+
+  const listed = await ctx.entities.list(listQuery);
+  const listedRecord = listed.find((record) => record.id === id) ?? null;
+  if (listedRecord) {
+    return toTypedRecord<T>(listedRecord, entityType);
+  }
+
+  // Fallback: some host SDK variants ignore `id` on entities.list().
+  // Keep defensive type verification on direct get() until id-filter support is universal.
   const record = await ctx.entities.get(id);
-
-  if (!record || record.entityType !== entityType) {
+  if (!record) {
     return null;
   }
 
-  return toTypedRecord<T>(record);
+  return toTypedRecord<T>(record, entityType);
 }
 
 async function requireEntityByType<T>(
diff --git a/paperclip-addon/plugins/workflow-engine/src/workflow-utils.ts b/paperclip-addon/plugins/workflow-engine/src/workflow-utils.ts
new file mode 100644
index 0000000000000000000000000000000000000000..ec1cf6f6b9aa722b126b58c0a728b4e538ad1494
--- /dev/null
+++ b/paperclip-addon/plugins/workflow-engine/src/workflow-utils.ts
@@ -0,0 +1,62 @@
+import type { PluginEntityRecord } from "@paperclipai/plugin-sdk";
+
+import { STEP_STATUSES } from "./constants.js";
+import type { WorkflowStep } from "./dag-engine.js";
+import type {
+  WorkflowDefinition,
+  WorkflowRun,
+  WorkflowStepRun,
+} from "./workflow-store.js";
+
+export type WorkflowRunRecord = PluginEntityRecord & { data: WorkflowRun };
+export type WorkflowStepRunRecord = PluginEntityRecord & { data: WorkflowStepRun };
+export type WorkflowDefinitionRecord = PluginEntityRecord & { data: WorkflowDefinition };
+type StepAgentMetadata = WorkflowStep & {
+  agent?: string;
+  agentName?: string;
+  assigneeAgentName?: string;
+};
+
+export const TERMINAL_STEP_STATUSES = new Set<string>([
+  STEP_STATUSES.done,
+  STEP_STATUSES.failed,
+  STEP_STATUSES.skipped,
+  STEP_STATUSES.escalated,
+]);
+
+export function toWorkflowRunRecord(record: PluginEntityRecord): WorkflowRunRecord {
+  return record as WorkflowRunRecord;
+}
+
+export function toWorkflowStepRunRecord(record: PluginEntityRecord): WorkflowStepRunRecord {
+  return record as WorkflowStepRunRecord;
+}
+
+export function toWorkflowDefinitionRecord(record: PluginEntityRecord): WorkflowDefinitionRecord {
+  return record as WorkflowDefinitionRecord;
+}
+
+export function findStepDefinition(definition: WorkflowDefinitionRecord, stepId: string): WorkflowStep | null {
+  return definition.data.steps.find((step: WorkflowStep) => step.id === stepId) ?? null;
+}
+
+export function getStepAgentNameHint(stepDef: WorkflowStep): string | null {
+  const stepMeta = stepDef as StepAgentMetadata;
+  const candidates = [stepMeta.agentName, stepMeta.agent, stepMeta.assigneeAgentName];
+
+  for (const candidate of candidates) {
+    if (typeof candidate === "string" && candidate.trim()) {
+      return candidate.trim();
+    }
+  }
+
+  return null;
+}
+
+export function getStepAgentName(stepRun: WorkflowStepRunRecord, stepDef: WorkflowStep): string | null {
+  if (typeof stepRun.data.agentName === "string" && stepRun.data.agentName.trim()) {
+    return stepRun.data.agentName.trim();
+  }
+
+  return getStepAgentNameHint(stepDef);
+}

exec
/bin/zsh -lc "perl -0pi -e 's/toStepRunRecord/toWorkflowStepRunRecord/g' plugins/workflow-engine/src/worker.ts" in /Users/kwak/Projects/paperclip/paperclip-addon succeeded in 0ms:

file update:
diff --git a/paperclip-addon/plugins/workflow-engine/src/dag-engine.ts b/paperclip-addon/plugins/workflow-engine/src/dag-engine.ts
index b59510f0473171038be77277286d3b5c2063e6c5..e7f21ff998cab78b9325a8aeacf4fda9c5e06553
--- a/paperclip-addon/plugins/workflow-engine/src/dag-engine.ts
+++ b/paperclip-addon/plugins/workflow-engine/src/dag-engine.ts
@@ -2,6 +2,7 @@
   id: string;
   title: string;
   dependsOn: string[];
+  sessionMode?: "fresh" | "reuse";
   onFailure?: "retry" | "skip" | "abort_workflow" | "escalate";
   escalateTo?: string;
   maxRetries?: number;
diff --git a/paperclip-addon/plugins/workflow-engine/src/reconciler.ts b/paperclip-addon/plugins/workflow-engine/src/reconciler.ts
index 14ab4e766a835d145a6c2e7b1b564270285cbdc3..d2ab54817bd8185944379d2188bfbdef1722020a
--- a/paperclip-addon/plugins/workflow-engine/src/reconciler.ts
+++ b/paperclip-addon/plugins/workflow-engine/src/reconciler.ts
@@ -8,65 +8,22 @@
   listStepRuns,
   updateStepRun,
   updateWorkflowRun,
-  type WorkflowDefinition,
-  type WorkflowRun,
-  type WorkflowStepRun,
 } from "./workflow-store.js";
 import type { WorkflowStep } from "./dag-engine.js";
-
-type WorkflowRunRecord = PluginEntityRecord & { data: WorkflowRun };
-type WorkflowStepRunRecord = PluginEntityRecord & { data: WorkflowStepRun };
-type WorkflowDefinitionRecord = PluginEntityRecord & { data: WorkflowDefinition };
-type StepAgentMetadata = WorkflowStep & {
-  agent?: string;
-  agentName?: string;
-  assigneeAgentName?: string;
-};
+import {
+  TERMINAL_STEP_STATUSES,
+  findStepDefinition,
+  getStepAgentName,
+  toWorkflowDefinitionRecord,
+  toWorkflowRunRecord,
+  toWorkflowStepRunRecord,
+} from "./workflow-utils.js";
 
 const STALE_THRESHOLD_MS = 300_000;
-const TERMINAL_STEP_STATUSES = new Set<string>([
-  STEP_STATUSES.done,
-  STEP_STATUSES.failed,
-  STEP_STATUSES.skipped,
-  STEP_STATUSES.escalated,
-]);
 
 function isStale(record: PluginEntityRecord, thresholdMs: number): boolean {
   const updatedAt = new Date(record.updatedAt).getTime();
   return Date.now() - updatedAt > thresholdMs;
-}
-
-function toWorkflowRunRecord(record: PluginEntityRecord): WorkflowRunRecord {
-  return record as WorkflowRunRecord;
-}
-
-function toWorkflowStepRunRecord(record: PluginEntityRecord): WorkflowStepRunRecord {
-  return record as WorkflowStepRunRecord;
-}
-
-function toWorkflowDefinitionRecord(record: PluginEntityRecord): WorkflowDefinitionRecord {
-  return record as WorkflowDefinitionRecord;
-}
-
-function findStepDefinition(definition: WorkflowDefinitionRecord, stepId: string): WorkflowStep | null {
-  return definition.data.steps.find((step: WorkflowStep) => step.id === stepId) ?? null;
-}
-
-function getStepAgentName(stepRun: WorkflowStepRunRecord, stepDef: WorkflowStep): string | null {
-  if (typeof stepRun.data.agentName === "string" && stepRun.data.agentName.trim()) {
-    return stepRun.data.agentName.trim();
-  }
-
-  const stepMeta = stepDef as StepAgentMetadata;
-  const candidates = [stepMeta.agentName, stepMeta.agent, stepMeta.assigneeAgentName];
-
-  for (const candidate of candidates) {
-    if (typeof candidate === "string" && candidate.trim()) {
-      return candidate.trim();
-    }
-  }
-
-  return null;
 }
 
 async function invokeAgentByName(
diff --git a/paperclip-addon/plugins/workflow-engine/src/worker.ts b/paperclip-addon/plugins/workflow-engine/src/worker.ts
index c5ac3fbb4ca7f767eb29331bd3630b635449b335..f6e0a06e4c799858f9e0b9594a5e7a904b106fa2
--- a/paperclip-addon/plugins/workflow-engine/src/worker.ts
+++ b/paperclip-addon/plugins/workflow-engine/src/worker.ts
@@ -15,6 +15,7 @@
 } from "./dag-engine.js";
 import {
   checkIdempotency,
+  createWorkflowRun,
   createStepRun,
   findStepRunByIssueId,
   getWorkflowDefinition,
@@ -25,81 +26,50 @@
   markIdempotency,
   updateStepRun,
   updateWorkflowRun,
-  type WorkflowDefinition,
-  type WorkflowRun,
   type WorkflowStepRun,
 } from "./workflow-store.js";
+import {
+  TERMINAL_STEP_STATUSES,
+  findStepDefinition,
+  getStepAgentName,
+  getStepAgentNameHint,
+  toWorkflowDefinitionRecord,
+  toWorkflowRunRecord,
+  toWorkflowStepRunRecord,
+  type WorkflowDefinitionRecord,
+  type WorkflowRunRecord,
+  type WorkflowStepRunRecord,
+} from "./workflow-utils.js";
 
-type WorkflowStepRecord = PluginEntityRecord & { data: WorkflowStepRun };
-type WorkflowRunRecord = PluginEntityRecord & { data: WorkflowRun };
-type WorkflowDefinitionRecord = PluginEntityRecord & { data: WorkflowDefinition };
 type WorkflowStepMetadata = WorkflowStep & {
   description?: string;
-  agent?: string;
-  agentName?: string;
-  assigneeAgentName?: string;
   assigneeAgentId?: string;
+  sessionMode?: "fresh" | "reuse";
 };
 type AgentRecord = Awaited<ReturnType<PluginContext["agents"]["list"]>>[number];
 type ReconcilerModule = {
   reconcileStuckSteps?: (ctx: PluginContext) => Promise<void>;
 };
 
-const TERMINAL_STEP_STATUSES = new Set<string>([
-  STEP_STATUSES.done,
-  STEP_STATUSES.failed,
-  STEP_STATUSES.skipped,
-  STEP_STATUSES.escalated,
-]);
-
 function summarizeError(error: unknown): string {
   if (error instanceof Error) {
     return error.message;
   }
 
   return String(error);
-}
-
-function toStepRunRecord(record: PluginEntityRecord): WorkflowStepRecord {
-  return record as WorkflowStepRecord;
 }
 
-function toWorkflowRunRecord(record: PluginEntityRecord): WorkflowRunRecord {
-  return record as WorkflowRunRecord;
-}
-
-function toWorkflowDefinitionRecord(record: PluginEntityRecord): WorkflowDefinitionRecord {
-  return record as WorkflowDefinitionRecord;
-}
-
 function buildIdempotencyKey(event: PluginEvent): string {
   return `${event.eventType}:${event.eventId}`;
 }
 
 function getStepMetadata(stepDef: WorkflowStep): WorkflowStepMetadata {
   return stepDef as WorkflowStepMetadata;
-}
-
-function findStepDefinition(definition: WorkflowDefinitionRecord, stepId: string): WorkflowStep | null {
-  return definition.data.steps.find((step: WorkflowStep) => step.id === stepId) ?? null;
 }
 
 function getStepDescription(stepDef: WorkflowStep): string | undefined {
   const description = getStepMetadata(stepDef).description;
   return typeof description === "string" && description.trim() ? description.trim() : undefined;
-}
-
-function getStepAgentNameHint(stepDef: WorkflowStep): string | null {
-  const meta = getStepMetadata(stepDef);
-  const candidates = [meta.agentName, meta.agent, meta.assigneeAgentName];
-
-  for (const candidate of candidates) {
-    if (typeof candidate === "string" && candidate.trim()) {
-      return candidate.trim();
-    }
-  }
-
-  return null;
 }
 
 function getStepAgentIdHint(stepDef: WorkflowStep): string | null {
@@ -190,7 +160,7 @@
     return;
   }
 
-  const updated = toStepRunRecord(await updateStepRun(ctx, stepRunRecord.id, {
+  const updated = toWorkflowStepRunRecord(await updateStepRun(ctx, stepRunRecord.id, {
     status: STEP_STATUSES.todo,
   }));
 
@@ -208,7 +178,7 @@
   stepRunRecord: PluginEntityRecord,
   companyId: string,
 ): Promise<void> {
-  const completedStepRun = toStepRunRecord(stepRunRecord);
+  const completedStepRun = toWorkflowStepRunRecord(stepRunRecord);
   const workflowRun = await getWorkflowRun(ctx, completedStepRun.data.runId);
 
   if (!workflowRun) {
@@ -237,7 +207,7 @@
   }
 
   const typedWorkflowDefinition = toWorkflowDefinitionRecord(workflowDefinition);
-  const stepRuns = (await listStepRuns(ctx, typedWorkflowRun.id, companyId)).map(toStepRunRecord);
+  const stepRuns = (await listStepRuns(ctx, typedWorkflowRun.id, companyId)).map(toWorkflowStepRunRecord);
   const completed = new Set<string>();
   const failed = new Set<string>();
   const skipped = new Set<string>();
@@ -289,7 +259,7 @@
         continue;
       }
 
-      stepRun = toStepRunRecord(await createStepRun(ctx, companyId, {
+      stepRun = toWorkflowStepRunRecord(await createStepRun(ctx, companyId, {
         agentName: resolvedAgent.agentName,
         retryCount: 0,
         runId: typedWorkflowRun.id,
@@ -338,7 +308,7 @@
     throw new Error(`Escalation target step not found: ${escalationTargetId}`);
   }
 
-  const stepRuns = (await listStepRuns(ctx, workflowRun.id, companyId)).map(toStepRunRecord);
+  const stepRuns = (await listStepRuns(ctx, workflowRun.id, companyId)).map(toWorkflowStepRunRecord);
   let escalationStepRun = stepRuns.find((candidate) => candidate.data.stepId === escalationTargetId) ?? null;
   const resolvedAgent = await resolveStepAgent(
     ctx,
@@ -352,7 +322,7 @@
   }
 
   if (!escalationStepRun) {
-    escalationStepRun = toStepRunRecord(await createStepRun(ctx, companyId, {
+    escalationStepRun = toWorkflowStepRunRecord(await createStepRun(ctx, companyId, {
       agentName: resolvedAgent.agentName,
       retryCount: 0,
       runId: workflowRun.id,
@@ -374,14 +344,14 @@
       title: `${workflowRun.data.workflowName}: ${escalationStep.title}`,
     });
 
-    escalationStepRun = toStepRunRecord(await updateStepRun(ctx, escalationStepRun.id, {
+    escalationStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, escalationStepRun.id, {
       issueId: issue.id,
     }));
   }
 
   const shouldActivate = escalationStepRun.data.status === STEP_STATUSES.backlog;
   if (shouldActivate) {
-    escalationStepRun = toStepRunRecord(await updateStepRun(ctx, escalationStepRun.id, {
+    escalationStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, escalationStepRun.id, {
       status: STEP_STATUSES.todo,
     }));
   }
@@ -421,7 +391,7 @@
     return;
   }
 
-  const typedStepRun = toStepRunRecord(stepRunRecord);
+  const typedStepRun = toWorkflowStepRunRecord(stepRunRecord);
   if (TERMINAL_STEP_STATUSES.has(typedStepRun.data.status)) {
     return;
   }
@@ -469,7 +439,7 @@
   if (options.allowRetry) {
     const retryInfo = getRetryInfo(typedWorkflowDefinition.data.steps, typedStepRun.data.stepId);
     if (retryInfo.shouldRetry && typedStepRun.data.retryCount < retryInfo.maxRetries) {
-      const retriedStepRun = toStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
+      const retriedStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
         completedAt: undefined,
         retryCount: typedStepRun.data.retryCount + 1,
         status: STEP_STATUSES.todo,
@@ -496,7 +466,7 @@
   }
 
   if (stepDef.onFailure === "skip") {
-    const skippedStepRun = toStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
+    const skippedStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
       completedAt: new Date().toISOString(),
       status: STEP_STATUSES.skipped,
     }));
@@ -556,7 +526,7 @@
       return;
     }
 
-    const escalatedStepRun = toStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
+    const escalatedStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
       completedAt: new Date().toISOString(),
       status: STEP_STATUSES.escalated,
     }));
@@ -638,7 +608,7 @@
           return;
         }
 
-        const stepRun = toStepRunRecord(stepRunRecord);
+        const stepRun = toWorkflowStepRunRecord(stepRunRecord);
         const payload = event.payload as { status?: string };
         const issueStatus = typeof payload.status === "string" ? payload.status : undefined;
 
@@ -657,7 +627,7 @@
           return;
         }
 
-        const updatedStepRun = toStepRunRecord(await updateStepRun(ctx, stepRun.id, {
+        const updatedStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, stepRun.id, {
           completedAt: nextStepStatus === STEP_STATUSES.done ? new Date().toISOString() : undefined,
           startedAt: nextStepStatus === STEP_STATUSES.inProgress
             ? stepRun.data.startedAt ?? new Date().toISOString()
@@ -807,7 +777,7 @@
             ...typedWorkflowRun.data,
           },
           stepRuns: stepRuns.map((record: PluginEntityRecord) => {
-            const stepRun = toStepRunRecord(record);
+            const stepRun = toWorkflowStepRunRecord(record);
             return {
               id: stepRun.id,
               status: stepRun.status,
diff --git a/paperclip-addon/plugins/workflow-engine/src/workflow-store.ts b/paperclip-addon/plugins/workflow-engine/src/workflow-store.ts
index 5871a2082258cebcfe8b69ebafbb0814cf303d50..3d7fb2b3bc75911cfd9093eb3a4054cc73196bb9
--- a/paperclip-addon/plugins/workflow-engine/src/workflow-store.ts
+++ b/paperclip-addon/plugins/workflow-engine/src/workflow-store.ts
@@ -44,7 +44,14 @@
 
 type TypedEntityRecord<T> = Omit<PluginEntityRecord, "data"> & { data: T };
 
-function toTypedRecord<T>(record: PluginEntityRecord): TypedEntityRecord<T> {
+function toTypedRecord<T>(
+  record: PluginEntityRecord,
+  entityType: string,
+): TypedEntityRecord<T> {
+  if (record.entityType !== entityType) {
+    throw new Error(`Expected entity type "${entityType}", got "${record.entityType}"`);
+  }
+
   return record as TypedEntityRecord<T>;
 }
 
@@ -67,13 +74,26 @@
   id: string,
   entityType: string,
 ): Promise<TypedEntityRecord<T> | null> {
+  const listQuery: Parameters<PluginContext["entities"]["list"]>[0] & { id?: string } = {
+    entityType,
+    id,
+    limit: 1,
+  };
+
+  const listed = await ctx.entities.list(listQuery);
+  const listedRecord = listed.find((record) => record.id === id) ?? null;
+  if (listedRecord) {
+    return toTypedRecord<T>(listedRecord, entityType);
+  }
+
+  // Fallback: some host SDK variants ignore `id` on entities.list().
+  // Keep defensive type verification on direct get() until id-filter support is universal.
   const record = await ctx.entities.get(id);
-
-  if (!record || record.entityType !== entityType) {
+  if (!record) {
     return null;
   }
 
-  return toTypedRecord<T>(record);
+  return toTypedRecord<T>(record, entityType);
 }
 
 async function requireEntityByType<T>(
diff --git a/paperclip-addon/plugins/workflow-engine/src/workflow-utils.ts b/paperclip-addon/plugins/workflow-engine/src/workflow-utils.ts
new file mode 100644
index 0000000000000000000000000000000000000000..ec1cf6f6b9aa722b126b58c0a728b4e538ad1494
--- /dev/null
+++ b/paperclip-addon/plugins/workflow-engine/src/workflow-utils.ts
@@ -0,0 +1,62 @@
+import type { PluginEntityRecord } from "@paperclipai/plugin-sdk";
+
+import { STEP_STATUSES } from "./constants.js";
+import type { WorkflowStep } from "./dag-engine.js";
+import type {
+  WorkflowDefinition,
+  WorkflowRun,
+  WorkflowStepRun,
+} from "./workflow-store.js";
+
+export type WorkflowRunRecord = PluginEntityRecord & { data: WorkflowRun };
+export type WorkflowStepRunRecord = PluginEntityRecord & { data: WorkflowStepRun };
+export type WorkflowDefinitionRecord = PluginEntityRecord & { data: WorkflowDefinition };
+type StepAgentMetadata = WorkflowStep & {
+  agent?: string;
+  agentName?: string;
+  assigneeAgentName?: string;
+};
+
+export const TERMINAL_STEP_STATUSES = new Set<string>([
+  STEP_STATUSES.done,
+  STEP_STATUSES.failed,
+  STEP_STATUSES.skipped,
+  STEP_STATUSES.escalated,
+]);
+
+export function toWorkflowRunRecord(record: PluginEntityRecord): WorkflowRunRecord {
+  return record as WorkflowRunRecord;
+}
+
+export function toWorkflowStepRunRecord(record: PluginEntityRecord): WorkflowStepRunRecord {
+  return record as WorkflowStepRunRecord;
+}
+
+export function toWorkflowDefinitionRecord(record: PluginEntityRecord): WorkflowDefinitionRecord {
+  return record as WorkflowDefinitionRecord;
+}
+
+export function findStepDefinition(definition: WorkflowDefinitionRecord, stepId: string): WorkflowStep | null {
+  return definition.data.steps.find((step: WorkflowStep) => step.id === stepId) ?? null;
+}
+
+export function getStepAgentNameHint(stepDef: WorkflowStep): string | null {
+  const stepMeta = stepDef as StepAgentMetadata;
+  const candidates = [stepMeta.agentName, stepMeta.agent, stepMeta.assigneeAgentName];
+
+  for (const candidate of candidates) {
+    if (typeof candidate === "string" && candidate.trim()) {
+      return candidate.trim();
+    }
+  }
+
+  return null;
+}
+
+export function getStepAgentName(stepRun: WorkflowStepRunRecord, stepDef: WorkflowStep): string | null {
+  if (typeof stepRun.data.agentName === "string" && stepRun.data.agentName.trim()) {
+    return stepRun.data.agentName.trim();
+  }
+
+  return getStepAgentNameHint(stepDef);
+}

exec
/bin/zsh -lc "sed -n '90,230p' plugins/workflow-engine/src/worker.ts" in /Users/kwak/Projects/paperclip/paperclip-addon succeeded in 0ms:
  const preferredName = typeof fallbackAgentName === "string" && fallbackAgentName.trim()
    ? fallbackAgentName.trim()
    : getStepAgentNameHint(stepDef);

  if (preferredName) {
    const agents = await ctx.agents.list({ companyId });
    const agent = agents.find((candidate: AgentRecord) => candidate.name === preferredName) ?? null;
    return {
      agentId: agent?.id ?? null,
      agentName: agent?.name ?? preferredName,
    };
  }

  const agentIdHint = getStepAgentIdHint(stepDef);
  if (!agentIdHint) {
    return {
      agentId: null,
      agentName: null,
    };
  }
file update:
diff --git a/paperclip-addon/plugins/workflow-engine/src/dag-engine.ts b/paperclip-addon/plugins/workflow-engine/src/dag-engine.ts
index b59510f0473171038be77277286d3b5c2063e6c5..e7f21ff998cab78b9325a8aeacf4fda9c5e06553
--- a/paperclip-addon/plugins/workflow-engine/src/dag-engine.ts
+++ b/paperclip-addon/plugins/workflow-engine/src/dag-engine.ts
@@ -2,6 +2,7 @@
   id: string;
   title: string;
   dependsOn: string[];
+  sessionMode?: "fresh" | "reuse";
   onFailure?: "retry" | "skip" | "abort_workflow" | "escalate";
   escalateTo?: string;
   maxRetries?: number;
diff --git a/paperclip-addon/plugins/workflow-engine/src/reconciler.ts b/paperclip-addon/plugins/workflow-engine/src/reconciler.ts
index 14ab4e766a835d145a6c2e7b1b564270285cbdc3..d2ab54817bd8185944379d2188bfbdef1722020a
--- a/paperclip-addon/plugins/workflow-engine/src/reconciler.ts
+++ b/paperclip-addon/plugins/workflow-engine/src/reconciler.ts
@@ -8,65 +8,22 @@
   listStepRuns,
   updateStepRun,
   updateWorkflowRun,
-  type WorkflowDefinition,
-  type WorkflowRun,
-  type WorkflowStepRun,
 } from "./workflow-store.js";
 import type { WorkflowStep } from "./dag-engine.js";
-
-type WorkflowRunRecord = PluginEntityRecord & { data: WorkflowRun };
-type WorkflowStepRunRecord = PluginEntityRecord & { data: WorkflowStepRun };
-type WorkflowDefinitionRecord = PluginEntityRecord & { data: WorkflowDefinition };
-type StepAgentMetadata = WorkflowStep & {
-  agent?: string;
-  agentName?: string;
-  assigneeAgentName?: string;
-};
+import {
+  TERMINAL_STEP_STATUSES,
+  findStepDefinition,
+  getStepAgentName,
+  toWorkflowDefinitionRecord,
+  toWorkflowRunRecord,
+  toWorkflowStepRunRecord,
+} from "./workflow-utils.js";
 
 const STALE_THRESHOLD_MS = 300_000;
-const TERMINAL_STEP_STATUSES = new Set<string>([
-  STEP_STATUSES.done,
-  STEP_STATUSES.failed,
-  STEP_STATUSES.skipped,
-  STEP_STATUSES.escalated,
-]);
 
 function isStale(record: PluginEntityRecord, thresholdMs: number): boolean {
   const updatedAt = new Date(record.updatedAt).getTime();
   return Date.now() - updatedAt > thresholdMs;
-}
-
-function toWorkflowRunRecord(record: PluginEntityRecord): WorkflowRunRecord {
-  return record as WorkflowRunRecord;
-}
-
-function toWorkflowStepRunRecord(record: PluginEntityRecord): WorkflowStepRunRecord {
-  return record as WorkflowStepRunRecord;
-}
-
-function toWorkflowDefinitionRecord(record: PluginEntityRecord): WorkflowDefinitionRecord {
-  return record as WorkflowDefinitionRecord;
-}
-
-function findStepDefinition(definition: WorkflowDefinitionRecord, stepId: string): WorkflowStep | null {
-  return definition.data.steps.find((step: WorkflowStep) => step.id === stepId) ?? null;
-}
-
-function getStepAgentName(stepRun: WorkflowStepRunRecord, stepDef: WorkflowStep): string | null {
-  if (typeof stepRun.data.agentName === "string" && stepRun.data.agentName.trim()) {
-    return stepRun.data.agentName.trim();
-  }
-
-  const stepMeta = stepDef as StepAgentMetadata;
-  const candidates = [stepMeta.agentName, stepMeta.agent, stepMeta.assigneeAgentName];
-
-  for (const candidate of candidates) {
-    if (typeof candidate === "string" && candidate.trim()) {
-      return candidate.trim();
-    }
-  }
-
-  return null;
 }
 
 async function invokeAgentByName(
diff --git a/paperclip-addon/plugins/workflow-engine/src/worker.ts b/paperclip-addon/plugins/workflow-engine/src/worker.ts
index c5ac3fbb4ca7f767eb29331bd3630b635449b335..f6e0a06e4c799858f9e0b9594a5e7a904b106fa2
--- a/paperclip-addon/plugins/workflow-engine/src/worker.ts
+++ b/paperclip-addon/plugins/workflow-engine/src/worker.ts
@@ -15,6 +15,7 @@
 } from "./dag-engine.js";
 import {
   checkIdempotency,
+  createWorkflowRun,
   createStepRun,
   findStepRunByIssueId,
   getWorkflowDefinition,
@@ -25,81 +26,50 @@
   markIdempotency,
   updateStepRun,
   updateWorkflowRun,
-  type WorkflowDefinition,
-  type WorkflowRun,
   type WorkflowStepRun,
 } from "./workflow-store.js";
+import {
+  TERMINAL_STEP_STATUSES,
+  findStepDefinition,
+  getStepAgentName,
+  getStepAgentNameHint,
+  toWorkflowDefinitionRecord,
+  toWorkflowRunRecord,
+  toWorkflowStepRunRecord,
+  type WorkflowDefinitionRecord,
+  type WorkflowRunRecord,
+  type WorkflowStepRunRecord,
+} from "./workflow-utils.js";
 
-type WorkflowStepRecord = PluginEntityRecord & { data: WorkflowStepRun };
-type WorkflowRunRecord = PluginEntityRecord & { data: WorkflowRun };
-type WorkflowDefinitionRecord = PluginEntityRecord & { data: WorkflowDefinition };
 type WorkflowStepMetadata = WorkflowStep & {
   description?: string;
-  agent?: string;
-  agentName?: string;
-  assigneeAgentName?: string;
   assigneeAgentId?: string;
+  sessionMode?: "fresh" | "reuse";
 };
 type AgentRecord = Awaited<ReturnType<PluginContext["agents"]["list"]>>[number];
 type ReconcilerModule = {
   reconcileStuckSteps?: (ctx: PluginContext) => Promise<void>;
 };
 
-const TERMINAL_STEP_STATUSES = new Set<string>([
-  STEP_STATUSES.done,
-  STEP_STATUSES.failed,
-  STEP_STATUSES.skipped,
-  STEP_STATUSES.escalated,
-]);
-
 function summarizeError(error: unknown): string {
   if (error instanceof Error) {
     return error.message;
   }
 
   return String(error);
-}
-
-function toStepRunRecord(record: PluginEntityRecord): WorkflowStepRecord {
-  return record as WorkflowStepRecord;
 }
 
-function toWorkflowRunRecord(record: PluginEntityRecord): WorkflowRunRecord {
-  return record as WorkflowRunRecord;
-}
-
-function toWorkflowDefinitionRecord(record: PluginEntityRecord): WorkflowDefinitionRecord {
-  return record as WorkflowDefinitionRecord;
-}
-
 function buildIdempotencyKey(event: PluginEvent): string {
   return `${event.eventType}:${event.eventId}`;
 }
 
 function getStepMetadata(stepDef: WorkflowStep): WorkflowStepMetadata {
   return stepDef as WorkflowStepMetadata;
-}
-
-function findStepDefinition(definition: WorkflowDefinitionRecord, stepId: string): WorkflowStep | null {
-  return definition.data.steps.find((step: WorkflowStep) => step.id === stepId) ?? null;
 }
 
 function getStepDescription(stepDef: WorkflowStep): string | undefined {
   const description = getStepMetadata(stepDef).description;
   return typeof description === "string" && description.trim() ? description.trim() : undefined;
-}
-
-function getStepAgentNameHint(stepDef: WorkflowStep): string | null {
-  const meta = getStepMetadata(stepDef);
-  const candidates = [meta.agentName, meta.agent, meta.assigneeAgentName];
-
-  for (const candidate of candidates) {
-    if (typeof candidate === "string" && candidate.trim()) {
-      return candidate.trim();
-    }
-  }
-
-  return null;
 }
 
 function getStepAgentIdHint(stepDef: WorkflowStep): string | null {
@@ -190,7 +160,7 @@
     return;
   }
 
-  const updated = toStepRunRecord(await updateStepRun(ctx, stepRunRecord.id, {
+  const updated = toWorkflowStepRunRecord(await updateStepRun(ctx, stepRunRecord.id, {
     status: STEP_STATUSES.todo,
   }));
 
@@ -208,7 +178,7 @@
   stepRunRecord: PluginEntityRecord,
   companyId: string,
 ): Promise<void> {
-  const completedStepRun = toStepRunRecord(stepRunRecord);
+  const completedStepRun = toWorkflowStepRunRecord(stepRunRecord);
   const workflowRun = await getWorkflowRun(ctx, completedStepRun.data.runId);
 
   if (!workflowRun) {
@@ -237,7 +207,7 @@
   }
 
   const typedWorkflowDefinition = toWorkflowDefinitionRecord(workflowDefinition);
-  const stepRuns = (await listStepRuns(ctx, typedWorkflowRun.id, companyId)).map(toStepRunRecord);
+  const stepRuns = (await listStepRuns(ctx, typedWorkflowRun.id, companyId)).map(toWorkflowStepRunRecord);
   const completed = new Set<string>();
   const failed = new Set<string>();
   const skipped = new Set<string>();
@@ -289,7 +259,7 @@
         continue;
       }
 
-      stepRun = toStepRunRecord(await createStepRun(ctx, companyId, {
+      stepRun = toWorkflowStepRunRecord(await createStepRun(ctx, companyId, {
         agentName: resolvedAgent.agentName,
         retryCount: 0,
         runId: typedWorkflowRun.id,
@@ -338,7 +308,7 @@
     throw new Error(`Escalation target step not found: ${escalationTargetId}`);
   }
 
-  const stepRuns = (await listStepRuns(ctx, workflowRun.id, companyId)).map(toStepRunRecord);
+  const stepRuns = (await listStepRuns(ctx, workflowRun.id, companyId)).map(toWorkflowStepRunRecord);
   let escalationStepRun = stepRuns.find((candidate) => candidate.data.stepId === escalationTargetId) ?? null;
   const resolvedAgent = await resolveStepAgent(
     ctx,
@@ -352,7 +322,7 @@
   }
 
   if (!escalationStepRun) {
-    escalationStepRun = toStepRunRecord(await createStepRun(ctx, companyId, {
+    escalationStepRun = toWorkflowStepRunRecord(await createStepRun(ctx, companyId, {
       agentName: resolvedAgent.agentName,
       retryCount: 0,
       runId: workflowRun.id,
@@ -374,14 +344,14 @@
       title: `${workflowRun.data.workflowName}: ${escalationStep.title}`,
     });
 
-    escalationStepRun = toStepRunRecord(await updateStepRun(ctx, escalationStepRun.id, {
+    escalationStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, escalationStepRun.id, {
       issueId: issue.id,
     }));
   }
 
   const shouldActivate = escalationStepRun.data.status === STEP_STATUSES.backlog;
   if (shouldActivate) {
-    escalationStepRun = toStepRunRecord(await updateStepRun(ctx, escalationStepRun.id, {
+    escalationStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, escalationStepRun.id, {
       status: STEP_STATUSES.todo,
     }));
   }
@@ -421,7 +391,7 @@
     return;
   }
 
-  const typedStepRun = toStepRunRecord(stepRunRecord);
+  const typedStepRun = toWorkflowStepRunRecord(stepRunRecord);
   if (TERMINAL_STEP_STATUSES.has(typedStepRun.data.status)) {
     return;
   }
@@ -469,7 +439,7 @@
   if (options.allowRetry) {
     const retryInfo = getRetryInfo(typedWorkflowDefinition.data.steps, typedStepRun.data.stepId);
     if (retryInfo.shouldRetry && typedStepRun.data.retryCount < retryInfo.maxRetries) {
-      const retriedStepRun = toStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
+      const retriedStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
         completedAt: undefined,
         retryCount: typedStepRun.data.retryCount + 1,
         status: STEP_STATUSES.todo,
@@ -496,7 +466,7 @@
   }
 
   if (stepDef.onFailure === "skip") {
-    const skippedStepRun = toStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
+    const skippedStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
       completedAt: new Date().toISOString(),
       status: STEP_STATUSES.skipped,
     }));
@@ -556,7 +526,7 @@
       return;
     }
 
-    const escalatedStepRun = toStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
+    const escalatedStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
       completedAt: new Date().toISOString(),
       status: STEP_STATUSES.escalated,
     }));
@@ -638,7 +608,7 @@
           return;
         }
 
-        const stepRun = toStepRunRecord(stepRunRecord);
+        const stepRun = toWorkflowStepRunRecord(stepRunRecord);
         const payload = event.payload as { status?: string };
         const issueStatus = typeof payload.status === "string" ? payload.status : undefined;
 
@@ -657,7 +627,7 @@
           return;
         }
 
-        const updatedStepRun = toStepRunRecord(await updateStepRun(ctx, stepRun.id, {
+        const updatedStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, stepRun.id, {
           completedAt: nextStepStatus === STEP_STATUSES.done ? new Date().toISOString() : undefined,
           startedAt: nextStepStatus === STEP_STATUSES.inProgress
             ? stepRun.data.startedAt ?? new Date().toISOString()
@@ -807,7 +777,7 @@
             ...typedWorkflowRun.data,
           },
           stepRuns: stepRuns.map((record: PluginEntityRecord) => {
-            const stepRun = toStepRunRecord(record);
+            const stepRun = toWorkflowStepRunRecord(record);
             return {
               id: stepRun.id,
               status: stepRun.status,
diff --git a/paperclip-addon/plugins/workflow-engine/src/workflow-store.ts b/paperclip-addon/plugins/workflow-engine/src/workflow-store.ts
index 5871a2082258cebcfe8b69ebafbb0814cf303d50..3d7fb2b3bc75911cfd9093eb3a4054cc73196bb9
--- a/paperclip-addon/plugins/workflow-engine/src/workflow-store.ts
+++ b/paperclip-addon/plugins/workflow-engine/src/workflow-store.ts
@@ -44,7 +44,14 @@
 
 type TypedEntityRecord<T> = Omit<PluginEntityRecord, "data"> & { data: T };
 
-function toTypedRecord<T>(record: PluginEntityRecord): TypedEntityRecord<T> {
+function toTypedRecord<T>(
+  record: PluginEntityRecord,
+  entityType: string,
+): TypedEntityRecord<T> {
+  if (record.entityType !== entityType) {
+    throw new Error(`Expected entity type "${entityType}", got "${record.entityType}"`);
+  }
+
   return record as TypedEntityRecord<T>;
 }
 
@@ -67,13 +74,26 @@
   id: string,
   entityType: string,
 ): Promise<TypedEntityRecord<T> | null> {
+  const listQuery: Parameters<PluginContext["entities"]["list"]>[0] & { id?: string } = {
+    entityType,
+    id,
+    limit: 1,
+  };
+
+  const listed = await ctx.entities.list(listQuery);
+  const listedRecord = listed.find((record) => record.id === id) ?? null;
+  if (listedRecord) {
+    return toTypedRecord<T>(listedRecord, entityType);
+  }
+
+  // Fallback: some host SDK variants ignore `id` on entities.list().
+  // Keep defensive type verification on direct get() until id-filter support is universal.
   const record = await ctx.entities.get(id);
-
-  if (!record || record.entityType !== entityType) {
+  if (!record) {
     return null;
   }
 
-  return toTypedRecord<T>(record);
+  return toTypedRecord<T>(record, entityType);
 }
 
 async function requireEntityByType<T>(
diff --git a/paperclip-addon/plugins/workflow-engine/src/workflow-utils.ts b/paperclip-addon/plugins/workflow-engine/src/workflow-utils.ts
new file mode 100644
index 0000000000000000000000000000000000000000..ec1cf6f6b9aa722b126b58c0a728b4e538ad1494
--- /dev/null
+++ b/paperclip-addon/plugins/workflow-engine/src/workflow-utils.ts
@@ -0,0 +1,62 @@
+import type { PluginEntityRecord } from "@paperclipai/plugin-sdk";
+
+import { STEP_STATUSES } from "./constants.js";
+import type { WorkflowStep } from "./dag-engine.js";
+import type {
+  WorkflowDefinition,
+  WorkflowRun,
+  WorkflowStepRun,
+} from "./workflow-store.js";
+
+export type WorkflowRunRecord = PluginEntityRecord & { data: WorkflowRun };
+export type WorkflowStepRunRecord = PluginEntityRecord & { data: WorkflowStepRun };
+export type WorkflowDefinitionRecord = PluginEntityRecord & { data: WorkflowDefinition };
+type StepAgentMetadata = WorkflowStep & {
+  agent?: string;
+  agentName?: string;
+  assigneeAgentName?: string;
+};
+
+export const TERMINAL_STEP_STATUSES = new Set<string>([
+  STEP_STATUSES.done,
+  STEP_STATUSES.failed,
+  STEP_STATUSES.skipped,
+  STEP_STATUSES.escalated,
+]);
+
+export function toWorkflowRunRecord(record: PluginEntityRecord): WorkflowRunRecord {
+  return record as WorkflowRunRecord;
+}
+
+export function toWorkflowStepRunRecord(record: PluginEntityRecord): WorkflowStepRunRecord {
+  return record as WorkflowStepRunRecord;
+}
+
+export function toWorkflowDefinitionRecord(record: PluginEntityRecord): WorkflowDefinitionRecord {
+  return record as WorkflowDefinitionRecord;
+}
+
+export function findStepDefinition(definition: WorkflowDefinitionRecord, stepId: string): WorkflowStep | null {
+  return definition.data.steps.find((step: WorkflowStep) => step.id === stepId) ?? null;
+}
+
+export function getStepAgentNameHint(stepDef: WorkflowStep): string | null {
+  const stepMeta = stepDef as StepAgentMetadata;
+  const candidates = [stepMeta.agentName, stepMeta.agent, stepMeta.assigneeAgentName];
+
+  for (const candidate of candidates) {
+    if (typeof candidate === "string" && candidate.trim()) {
+      return candidate.trim();
+    }
+  }
+
+  return null;
+}
+
+export function getStepAgentName(stepRun: WorkflowStepRunRecord, stepDef: WorkflowStep): string | null {
+  if (typeof stepRun.data.agentName === "string" && stepRun.data.agentName.trim()) {
+    return stepRun.data.agentName.trim();
+  }
+
+  return getStepAgentNameHint(stepDef);
+}

file update
M /Users/kwak/Projects/paperclip/paperclip-addon/plugins/workflow-engine/src/worker.ts
@@ -119,9 +119,20 @@
   ctx: PluginContext,
+  stepRunRecord: WorkflowStepRunRecord,
   stepDef: WorkflowStep,
-  agentName: string,
   workflowName: string,
   companyId: string,
-): Promise<void> {
+): Promise<WorkflowStepRunRecord> {
+  const agentName = getStepAgentName(stepRunRecord, stepDef);
+  if (!agentName) {
+    ctx.logger.warn("Agent name is missing for workflow step", {
+      companyId,
+      runId: stepRunRecord.data.runId,
+      stepId: stepDef.id,
+      workflowName,
+    });
+    return stepRunRecord;
+  }
+
   const agents = await ctx.agents.list({ companyId });
-  const agent = agents.find((candidate: AgentRecord) => candidate.name === agentName);
+  const agent = agents.find((candidate: AgentRecord) => candidate.name === agentName) ?? null;
 
@@ -131,2 +142,39 @@
       companyId,
+      runId: stepRunRecord.data.runId,
+      stepId: stepDef.id,
+      workflowName,
+    });
+    return stepRunRecord;
+  }
+
+  const prompt = `workflow:${workflowName}/step:${stepDef.id} — "${stepDef.title}" is ready. Please proceed with the assigned task.`;
+  const reason = `workflow:${workflowName}/step:${stepDef.id}`;
+  const sessionMode = getStepMetadata(stepDef).sessionMode === "reuse" ? "reuse" : "fresh";
+
+  if (sessionMode === "reuse") {
+    let sessionId = typeof stepRunRecord.data.sessionId === "string" && stepRunRecord.data.sessionId.trim()
+      ? stepRunRecord.data.sessionId.trim()
+      : "";
+
+    if (!sessionId) {
+      const session = await ctx.agents.sessions.create(agent.id, companyId, {
+        reason,
+      });
+      sessionId = session.sessionId;
+      stepRunRecord = toWorkflowStepRunRecord(await updateStepRun(ctx, stepRunRecord.id, {
+        sessionId,
+      }));
+    }
+
+    await ctx.agents.sessions.sendMessage(sessionId, companyId, {
+      prompt,
+      reason,
+    });
+
+    ctx.logger.info("Sent workflow step prompt via agent session", {
+      agentId: agent.id,
+      agentName,
+      companyId,
+      runId: stepRunRecord.data.runId,
+      sessionId,
       stepId: stepDef.id,
@@ -134,3 +182,3 @@
     });
-    return;
+    return stepRunRecord;
   }
@@ -138,4 +186,4 @@
   await ctx.agents.invoke(agent.id, companyId, {
-    prompt: `workflow:${workflowName}/step:${stepDef.id} — "${stepDef.title}" is ready. Please proceed with the assigned task.`,
-    reason: `workflow:${workflowName}/step:${stepDef.id}`,
+    prompt,
+    reason,
   });
@@ -146,2 +194,3 @@
     companyId,
+    runId: stepRunRecord.data.runId,
     stepId: stepDef.id,
@@ -149,2 +198,4 @@
   });
+
+  return stepRunRecord;
 }
@@ -153,3 +204,3 @@
   ctx: PluginContext,
-  stepRunRecord: WorkflowStepRecord,
+  stepRunRecord: WorkflowStepRunRecord,
   stepDef: WorkflowStep,
@@ -157,8 +208,36 @@
   companyId: string,
-): Promise<void> {
+  options?: { parentIssueId?: string },
+): Promise<WorkflowStepRunRecord> {
   if (stepRunRecord.data.status !== STEP_STATUSES.backlog) {
-    return;
+    return stepRunRecord;
   }
 
-  const updated = toWorkflowStepRunRecord(await updateStepRun(ctx, stepRunRecord.id, {
+  const resolvedAgent = await resolveStepAgent(
+    ctx,
+    companyId,
+    stepDef,
+    stepRunRecord.data.agentName,
+  );
+
+  let issueId = typeof stepRunRecord.data.issueId === "string" && stepRunRecord.data.issueId.trim()
+    ? stepRunRecord.data.issueId.trim()
+    : "";
+  const stepDescription = getStepDescription(stepDef) ?? `Workflow step: ${stepDef.id}`;
+
+  if (!issueId) {
+    const issue = await ctx.issues.create({
+      assigneeAgentId: resolvedAgent.agentId ?? undefined,
+      companyId,
+      description: stepDescription,
+      parentId: options?.parentIssueId,
+      title: `[${workflowName}] ${stepDef.title}`,
+    });
+    issueId = issue.id;
+  }
+
+  const nextStartedAt = stepRunRecord.data.startedAt ?? new Date().toISOString();
+  let updatedStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, stepRunRecord.id, {
+    agentName: resolvedAgent.agentName ?? stepRunRecord.data.agentName,
+    issueId,
+    startedAt: nextStartedAt,
     status: STEP_STATUSES.todo,
@@ -166,6 +245,6 @@
 
-  await invokeAgentForStep(
+  updatedStepRun = await invokeAgentForStep(
     ctx,
+    updatedStepRun,
     stepDef,
-    updated.data.agentName,
     workflowName,
@@ -173,2 +252,110 @@
   );
+
+  return updatedStepRun;
+}
+
+async function startWorkflow(
+  ctx: PluginContext,
+  workflowId: string,
+  companyId: string,
+  options?: {
+    createParentIssue?: boolean;
+    parentIssueId?: string;
+  },
+): Promise<{
+  activatedStepIds: string[];
+  parentIssueId: string | null;
+  runId: string;
+  workflowId: string;
+}> {
+  const workflowDefinition = await getWorkflowDefinition(ctx, workflowId);
+  if (!workflowDefinition) {
+    throw new Error(`Workflow definition not found: ${workflowId}`);
+  }
+
+  const typedWorkflowDefinition = toWorkflowDefinitionRecord(workflowDefinition);
+  if (typedWorkflowDefinition.data.companyId !== companyId) {
+    throw new Error(`Workflow does not belong to company: ${workflowId}`);
+  }
+
+  let parentIssueId = typeof options?.parentIssueId === "string" && options.parentIssueId.trim()
+    ? options.parentIssueId.trim()
+    : "";
+  if (!parentIssueId && options?.createParentIssue) {
+    const parentIssue = await ctx.issues.create({
+      companyId,
+      description: typedWorkflowDefinition.data.description || `Workflow run: ${typedWorkflowDefinition.data.name}`,
+      title: `[${typedWorkflowDefinition.data.name}] Workflow run`,
+    });
+    parentIssueId = parentIssue.id;
+  }
+
+  const workflowRun = toWorkflowRunRecord(await createWorkflowRun(ctx, {
+    companyId,
+    parentIssueId: parentIssueId || undefined,
+    startedAt: new Date().toISOString(),
+    status: RUN_STATUSES.running,
+    workflowId: typedWorkflowDefinition.id,
+    workflowName: typedWorkflowDefinition.data.name,
+  }));
+
+  const agents = await ctx.agents.list({ companyId });
+  const agentsByName = new Map<string, AgentRecord>();
+  for (const agent of agents) {
+    agentsByName.set(agent.name, agent);
+  }
+
+  const pendingRootSteps: Array<{ stepDef: WorkflowStep; stepRun: WorkflowStepRunRecord }> = [];
+  for (const stepDef of typedWorkflowDefinition.data.steps) {
+    const agentNameHint = getStepAgentNameHint(stepDef);
+    const matchedAgent = agentNameHint ? agentsByName.get(agentNameHint) ?? null : null;
+    const resolvedAgent = matchedAgent
+      ? { agentId: matchedAgent.id, agentName: matchedAgent.name }
+      : await resolveStepAgent(ctx, companyId, stepDef, agentNameHint ?? undefined);
+
+    if (!resolvedAgent.agentName) {
+      throw new Error(`Unable to resolve step assignee for "${stepDef.id}"`);
+    }
+
+    const stepRun = toWorkflowStepRunRecord(await createStepRun(ctx, companyId, {
+      agentName: resolvedAgent.agentName,
+      retryCount: 0,
+      runId: workflowRun.id,
+      status: STEP_STATUSES.backlog,
+      stepId: stepDef.id,
+    }));
+
+    if (stepDef.dependsOn.length === 0 && stepDef.triggerOn !== "escalation") {
+      pendingRootSteps.push({ stepDef, stepRun });
+    }
+  }
+
+  const activatedStepIds: string[] = [];
+  for (const pending of pendingRootSteps) {
+    await activateBacklogStep(
+      ctx,
+      pending.stepRun,
+      pending.stepDef,
+      typedWorkflowDefinition.data.name,
+      companyId,
+      { parentIssueId: parentIssueId || undefined },
+    );
+    activatedStepIds.push(pending.stepDef.id);
+  }
+
+  ctx.logger.info("Started workflow run", {
+    activatedStepIds,
+    companyId,
+    parentIssueId: parentIssueId || null,
+    runId: workflowRun.id,
+    workflowId: typedWorkflowDefinition.id,
+    workflowName: typedWorkflowDefinition.data.name,
+  });
+
+  return {
+    activatedStepIds,
+    parentIssueId: parentIssueId || null,
+    runId: workflowRun.id,
+    workflowId: typedWorkflowDefinition.id,
+  };
 }
apply_patch(auto_approved=true) exited 0 in 112ms:
Success. Updated the following files:
M /Users/kwak/Projects/paperclip/paperclip-addon/plugins/workflow-engine/src/worker.ts
file update:
diff --git a/paperclip-addon/plugins/workflow-engine/src/dag-engine.ts b/paperclip-addon/plugins/workflow-engine/src/dag-engine.ts
index b59510f0473171038be77277286d3b5c2063e6c5..e7f21ff998cab78b9325a8aeacf4fda9c5e06553
--- a/paperclip-addon/plugins/workflow-engine/src/dag-engine.ts
+++ b/paperclip-addon/plugins/workflow-engine/src/dag-engine.ts
@@ -2,6 +2,7 @@
   id: string;
   title: string;
   dependsOn: string[];
+  sessionMode?: "fresh" | "reuse";
   onFailure?: "retry" | "skip" | "abort_workflow" | "escalate";
   escalateTo?: string;
   maxRetries?: number;
diff --git a/paperclip-addon/plugins/workflow-engine/src/reconciler.ts b/paperclip-addon/plugins/workflow-engine/src/reconciler.ts
index 14ab4e766a835d145a6c2e7b1b564270285cbdc3..d2ab54817bd8185944379d2188bfbdef1722020a
--- a/paperclip-addon/plugins/workflow-engine/src/reconciler.ts
+++ b/paperclip-addon/plugins/workflow-engine/src/reconciler.ts
@@ -8,65 +8,22 @@
   listStepRuns,
   updateStepRun,
   updateWorkflowRun,
-  type WorkflowDefinition,
-  type WorkflowRun,
-  type WorkflowStepRun,
 } from "./workflow-store.js";
 import type { WorkflowStep } from "./dag-engine.js";
-
-type WorkflowRunRecord = PluginEntityRecord & { data: WorkflowRun };
-type WorkflowStepRunRecord = PluginEntityRecord & { data: WorkflowStepRun };
-type WorkflowDefinitionRecord = PluginEntityRecord & { data: WorkflowDefinition };
-type StepAgentMetadata = WorkflowStep & {
-  agent?: string;
-  agentName?: string;
-  assigneeAgentName?: string;
-};
+import {
+  TERMINAL_STEP_STATUSES,
+  findStepDefinition,
+  getStepAgentName,
+  toWorkflowDefinitionRecord,
+  toWorkflowRunRecord,
+  toWorkflowStepRunRecord,
+} from "./workflow-utils.js";
 
 const STALE_THRESHOLD_MS = 300_000;
-const TERMINAL_STEP_STATUSES = new Set<string>([
-  STEP_STATUSES.done,
-  STEP_STATUSES.failed,
-  STEP_STATUSES.skipped,
-  STEP_STATUSES.escalated,
-]);
 
 function isStale(record: PluginEntityRecord, thresholdMs: number): boolean {
   const updatedAt = new Date(record.updatedAt).getTime();
   return Date.now() - updatedAt > thresholdMs;
-}
-
-function toWorkflowRunRecord(record: PluginEntityRecord): WorkflowRunRecord {
-  return record as WorkflowRunRecord;
-}
-
-function toWorkflowStepRunRecord(record: PluginEntityRecord): WorkflowStepRunRecord {
-  return record as WorkflowStepRunRecord;
-}
-
-function toWorkflowDefinitionRecord(record: PluginEntityRecord): WorkflowDefinitionRecord {
-  return record as WorkflowDefinitionRecord;
-}
-
-function findStepDefinition(definition: WorkflowDefinitionRecord, stepId: string): WorkflowStep | null {
-  return definition.data.steps.find((step: WorkflowStep) => step.id === stepId) ?? null;
-}
-
-function getStepAgentName(stepRun: WorkflowStepRunRecord, stepDef: WorkflowStep): string | null {
-  if (typeof stepRun.data.agentName === "string" && stepRun.data.agentName.trim()) {
-    return stepRun.data.agentName.trim();
-  }
-
-  const stepMeta = stepDef as StepAgentMetadata;
-  const candidates = [stepMeta.agentName, stepMeta.agent, stepMeta.assigneeAgentName];
-
-  for (const candidate of candidates) {
-    if (typeof candidate === "string" && candidate.trim()) {
-      return candidate.trim();
-    }
-  }
-
-  return null;
 }
 
 async function invokeAgentByName(
diff --git a/paperclip-addon/plugins/workflow-engine/src/worker.ts b/paperclip-addon/plugins/workflow-engine/src/worker.ts
index c5ac3fbb4ca7f767eb29331bd3630b635449b335..0618dc61f481ff717d3dfc0edc4adabaab9e5d19
--- a/paperclip-addon/plugins/workflow-engine/src/worker.ts
+++ b/paperclip-addon/plugins/workflow-engine/src/worker.ts
@@ -15,6 +15,7 @@
 } from "./dag-engine.js";
 import {
   checkIdempotency,
+  createWorkflowRun,
   createStepRun,
   findStepRunByIssueId,
   getWorkflowDefinition,
@@ -25,32 +26,30 @@
   markIdempotency,
   updateStepRun,
   updateWorkflowRun,
-  type WorkflowDefinition,
-  type WorkflowRun,
   type WorkflowStepRun,
 } from "./workflow-store.js";
+import {
+  TERMINAL_STEP_STATUSES,
+  findStepDefinition,
+  getStepAgentName,
+  getStepAgentNameHint,
+  toWorkflowDefinitionRecord,
+  toWorkflowRunRecord,
+  toWorkflowStepRunRecord,
+  type WorkflowDefinitionRecord,
+  type WorkflowRunRecord,
+  type WorkflowStepRunRecord,
+} from "./workflow-utils.js";
 
-type WorkflowStepRecord = PluginEntityRecord & { data: WorkflowStepRun };
-type WorkflowRunRecord = PluginEntityRecord & { data: WorkflowRun };
-type WorkflowDefinitionRecord = PluginEntityRecord & { data: WorkflowDefinition };
 type WorkflowStepMetadata = WorkflowStep & {
   description?: string;
-  agent?: string;
-  agentName?: string;
-  assigneeAgentName?: string;
   assigneeAgentId?: string;
+  sessionMode?: "fresh" | "reuse";
 };
 type AgentRecord = Awaited<ReturnType<PluginContext["agents"]["list"]>>[number];
 type ReconcilerModule = {
   reconcileStuckSteps?: (ctx: PluginContext) => Promise<void>;
 };
-
-const TERMINAL_STEP_STATUSES = new Set<string>([
-  STEP_STATUSES.done,
-  STEP_STATUSES.failed,
-  STEP_STATUSES.skipped,
-  STEP_STATUSES.escalated,
-]);
 
 function summarizeError(error: unknown): string {
   if (error instanceof Error) {
@@ -60,28 +59,12 @@
   return String(error);
 }
 
-function toStepRunRecord(record: PluginEntityRecord): WorkflowStepRecord {
-  return record as WorkflowStepRecord;
-}
-
-function toWorkflowRunRecord(record: PluginEntityRecord): WorkflowRunRecord {
-  return record as WorkflowRunRecord;
-}
-
-function toWorkflowDefinitionRecord(record: PluginEntityRecord): WorkflowDefinitionRecord {
-  return record as WorkflowDefinitionRecord;
-}
-
 function buildIdempotencyKey(event: PluginEvent): string {
   return `${event.eventType}:${event.eventId}`;
 }
 
 function getStepMetadata(stepDef: WorkflowStep): WorkflowStepMetadata {
   return stepDef as WorkflowStepMetadata;
-}
-
-function findStepDefinition(definition: WorkflowDefinitionRecord, stepId: string): WorkflowStep | null {
-  return definition.data.steps.find((step: WorkflowStep) => step.id === stepId) ?? null;
 }
 
 function getStepDescription(stepDef: WorkflowStep): string | undefined {
@@ -89,19 +72,6 @@
   return typeof description === "string" && description.trim() ? description.trim() : undefined;
 }
 
-function getStepAgentNameHint(stepDef: WorkflowStep): string | null {
-  const meta = getStepMetadata(stepDef);
-  const candidates = [meta.agentName, meta.agent, meta.assigneeAgentName];
-
-  for (const candidate of candidates) {
-    if (typeof candidate === "string" && candidate.trim()) {
-      return candidate.trim();
-    }
-  }
-
-  return null;
-}
-
 function getStepAgentIdHint(stepDef: WorkflowStep): string | null {
   const assigneeAgentId = getStepMetadata(stepDef).assigneeAgentId;
   if (typeof assigneeAgentId !== "string" || !assigneeAgentId.trim()) {
@@ -147,60 +117,247 @@
 
 async function invokeAgentForStep(
   ctx: PluginContext,
+  stepRunRecord: WorkflowStepRunRecord,
   stepDef: WorkflowStep,
-  agentName: string,
   workflowName: string,
   companyId: string,
-): Promise<void> {
+): Promise<WorkflowStepRunRecord> {
+  const agentName = getStepAgentName(stepRunRecord, stepDef);
+  if (!agentName) {
+    ctx.logger.warn("Agent name is missing for workflow step", {
+      companyId,
+      runId: stepRunRecord.data.runId,
+      stepId: stepDef.id,
+      workflowName,
+    });
+    return stepRunRecord;
+  }
+
   const agents = await ctx.agents.list({ companyId });
-  const agent = agents.find((candidate: AgentRecord) => candidate.name === agentName);
+  const agent = agents.find((candidate: AgentRecord) => candidate.name === agentName) ?? null;
 
   if (!agent) {
     ctx.logger.warn("Agent not found for step", {
       agentName,
       companyId,
+      runId: stepRunRecord.data.runId,
+      stepId: stepDef.id,
+      workflowName,
+    });
+    return stepRunRecord;
+  }
+
+  const prompt = `workflow:${workflowName}/step:${stepDef.id} — "${stepDef.title}" is ready. Please proceed with the assigned task.`;
+  const reason = `workflow:${workflowName}/step:${stepDef.id}`;
+  const sessionMode = getStepMetadata(stepDef).sessionMode === "reuse" ? "reuse" : "fresh";
+
+  if (sessionMode === "reuse") {
+    let sessionId = typeof stepRunRecord.data.sessionId === "string" && stepRunRecord.data.sessionId.trim()
+      ? stepRunRecord.data.sessionId.trim()
+      : "";
+
+    if (!sessionId) {
+      const session = await ctx.agents.sessions.create(agent.id, companyId, {
+        reason,
+      });
+      sessionId = session.sessionId;
+      stepRunRecord = toWorkflowStepRunRecord(await updateStepRun(ctx, stepRunRecord.id, {
+        sessionId,
+      }));
+    }
+
+    await ctx.agents.sessions.sendMessage(sessionId, companyId, {
+      prompt,
+      reason,
+    });
+
+    ctx.logger.info("Sent workflow step prompt via agent session", {
+      agentId: agent.id,
+      agentName,
+      companyId,
+      runId: stepRunRecord.data.runId,
+      sessionId,
       stepId: stepDef.id,
       workflowName,
     });
-    return;
+    return stepRunRecord;
   }
 
   await ctx.agents.invoke(agent.id, companyId, {
-    prompt: `workflow:${workflowName}/step:${stepDef.id} — "${stepDef.title}" is ready. Please proceed with the assigned task.`,
-    reason: `workflow:${workflowName}/step:${stepDef.id}`,
+    prompt,
+    reason,
   });
 
   ctx.logger.info("Invoked agent for workflow step", {
     agentId: agent.id,
     agentName,
     companyId,
+    runId: stepRunRecord.data.runId,
     stepId: stepDef.id,
     workflowName,
   });
+
+  return stepRunRecord;
 }
 
 async function activateBacklogStep(
   ctx: PluginContext,
-  stepRunRecord: WorkflowStepRecord,
+  stepRunRecord: WorkflowStepRunRecord,
   stepDef: WorkflowStep,
   workflowName: string,
   companyId: string,
-): Promise<void> {
+  options?: { parentIssueId?: string },
+): Promise<WorkflowStepRunRecord> {
   if (stepRunRecord.data.status !== STEP_STATUSES.backlog) {
-    return;
+    return stepRunRecord;
   }
 
-  const updated = toStepRunRecord(await updateStepRun(ctx, stepRunRecord.id, {
+  const resolvedAgent = await resolveStepAgent(
+    ctx,
+    companyId,
+    stepDef,
+    stepRunRecord.data.agentName,
+  );
+
+  let issueId = typeof stepRunRecord.data.issueId === "string" && stepRunRecord.data.issueId.trim()
+    ? stepRunRecord.data.issueId.trim()
+    : "";
+  const stepDescription = getStepDescription(stepDef) ?? `Workflow step: ${stepDef.id}`;
+
+  if (!issueId) {
+    const issue = await ctx.issues.create({
+      assigneeAgentId: resolvedAgent.agentId ?? undefined,
+      companyId,
+      description: stepDescription,
+      parentId: options?.parentIssueId,
+      title: `[${workflowName}] ${stepDef.title}`,
+    });
+    issueId = issue.id;
+  }
+
+  const nextStartedAt = stepRunRecord.data.startedAt ?? new Date().toISOString();
+  let updatedStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, stepRunRecord.id, {
+    agentName: resolvedAgent.agentName ?? stepRunRecord.data.agentName,
+    issueId,
+    startedAt: nextStartedAt,
     status: STEP_STATUSES.todo,
   }));
 
-  await invokeAgentForStep(
+  updatedStepRun = await invokeAgentForStep(
     ctx,
+    updatedStepRun,
     stepDef,
-    updated.data.agentName,
     workflowName,
     companyId,
   );
+
+  return updatedStepRun;
+}
+
+async function startWorkflow(
+  ctx: PluginContext,
+  workflowId: string,
+  companyId: string,
+  options?: {
+    createParentIssue?: boolean;
+    parentIssueId?: string;
+  },
+): Promise<{
+  activatedStepIds: string[];
+  parentIssueId: string | null;
+  runId: string;
+  workflowId: string;
+}> {
+  const workflowDefinition = await getWorkflowDefinition(ctx, workflowId);
+  if (!workflowDefinition) {
+    throw new Error(`Workflow definition not found: ${workflowId}`);
+  }
+
+  const typedWorkflowDefinition = toWorkflowDefinitionRecord(workflowDefinition);
+  if (typedWorkflowDefinition.data.companyId !== companyId) {
+    throw new Error(`Workflow does not belong to company: ${workflowId}`);
+  }
+
+  let parentIssueId = typeof options?.parentIssueId === "string" && options.parentIssueId.trim()
+    ? options.parentIssueId.trim()
+    : "";
+  if (!parentIssueId && options?.createParentIssue) {
+    const parentIssue = await ctx.issues.create({
+      companyId,
+      description: typedWorkflowDefinition.data.description || `Workflow run: ${typedWorkflowDefinition.data.name}`,
+      title: `[${typedWorkflowDefinition.data.name}] Workflow run`,
+    });
+    parentIssueId = parentIssue.id;
+  }
+
+  const workflowRun = toWorkflowRunRecord(await createWorkflowRun(ctx, {
+    companyId,
+    parentIssueId: parentIssueId || undefined,
+    startedAt: new Date().toISOString(),
+    status: RUN_STATUSES.running,
+    workflowId: typedWorkflowDefinition.id,
+    workflowName: typedWorkflowDefinition.data.name,
+  }));
+
+  const agents = await ctx.agents.list({ companyId });
+  const agentsByName = new Map<string, AgentRecord>();
+  for (const agent of agents) {
+    agentsByName.set(agent.name, agent);
+  }
+
+  const pendingRootSteps: Array<{ stepDef: WorkflowStep; stepRun: WorkflowStepRunRecord }> = [];
+  for (const stepDef of typedWorkflowDefinition.data.steps) {
+    const agentNameHint = getStepAgentNameHint(stepDef);
+    const matchedAgent = agentNameHint ? agentsByName.get(agentNameHint) ?? null : null;
+    const resolvedAgent = matchedAgent
+      ? { agentId: matchedAgent.id, agentName: matchedAgent.name }
+      : await resolveStepAgent(ctx, companyId, stepDef, agentNameHint ?? undefined);
+
+    if (!resolvedAgent.agentName) {
+      throw new Error(`Unable to resolve step assignee for "${stepDef.id}"`);
+    }
+
+    const stepRun = toWorkflowStepRunRecord(await createStepRun(ctx, companyId, {
+      agentName: resolvedAgent.agentName,
+      retryCount: 0,
+      runId: workflowRun.id,
+      status: STEP_STATUSES.backlog,
+      stepId: stepDef.id,
+    }));
+
+    if (stepDef.dependsOn.length === 0 && stepDef.triggerOn !== "escalation") {
+      pendingRootSteps.push({ stepDef, stepRun });
+    }
+  }
+
+  const activatedStepIds: string[] = [];
+  for (const pending of pendingRootSteps) {
+    await activateBacklogStep(
+      ctx,
+      pending.stepRun,
+      pending.stepDef,
+      typedWorkflowDefinition.data.name,
+      companyId,
+      { parentIssueId: parentIssueId || undefined },
+    );
+    activatedStepIds.push(pending.stepDef.id);
+  }
+
+  ctx.logger.info("Started workflow run", {
+    activatedStepIds,
+    companyId,
+    parentIssueId: parentIssueId || null,
+    runId: workflowRun.id,
+    workflowId: typedWorkflowDefinition.id,
+    workflowName: typedWorkflowDefinition.data.name,
+  });
+
+  return {
+    activatedStepIds,
+    parentIssueId: parentIssueId || null,
+    runId: workflowRun.id,
+    workflowId: typedWorkflowDefinition.id,
+  };
 }
 
 async function advanceWorkflow(
@@ -208,7 +365,7 @@
   stepRunRecord: PluginEntityRecord,
   companyId: string,
 ): Promise<void> {
-  const completedStepRun = toStepRunRecord(stepRunRecord);
+  const completedStepRun = toWorkflowStepRunRecord(stepRunRecord);
   const workflowRun = await getWorkflowRun(ctx, completedStepRun.data.runId);
 
   if (!workflowRun) {
@@ -237,7 +394,7 @@
   }
 
   const typedWorkflowDefinition = toWorkflowDefinitionRecord(workflowDefinition);
-  const stepRuns = (await listStepRuns(ctx, typedWorkflowRun.id, companyId)).map(toStepRunRecord);
+  const stepRuns = (await listStepRuns(ctx, typedWorkflowRun.id, companyId)).map(toWorkflowStepRunRecord);
   const completed = new Set<string>();
   const failed = new Set<string>();
   const skipped = new Set<string>();
@@ -289,7 +446,7 @@
         continue;
       }
 
-      stepRun = toStepRunRecord(await createStepRun(ctx, companyId, {
+      stepRun = toWorkflowStepRunRecord(await createStepRun(ctx, companyId, {
         agentName: resolvedAgent.agentName,
         retryCount: 0,
         runId: typedWorkflowRun.id,
@@ -338,7 +495,7 @@
     throw new Error(`Escalation target step not found: ${escalationTargetId}`);
   }
 
-  const stepRuns = (await listStepRuns(ctx, workflowRun.id, companyId)).map(toStepRunRecord);
+  const stepRuns = (await listStepRuns(ctx, workflowRun.id, companyId)).map(toWorkflowStepRunRecord);
   let escalationStepRun = stepRuns.find((candidate) => candidate.data.stepId === escalationTargetId) ?? null;
   const resolvedAgent = await resolveStepAgent(
     ctx,
@@ -352,7 +509,7 @@
   }
 
   if (!escalationStepRun) {
-    escalationStepRun = toStepRunRecord(await createStepRun(ctx, companyId, {
+    escalationStepRun = toWorkflowStepRunRecord(await createStepRun(ctx, companyId, {
       agentName: resolvedAgent.agentName,
       retryCount: 0,
       runId: workflowRun.id,
@@ -374,14 +531,14 @@
       title: `${workflowRun.data.workflowName}: ${escalationStep.title}`,
     });
 
-    escalationStepRun = toStepRunRecord(await updateStepRun(ctx, escalationStepRun.id, {
+    escalationStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, escalationStepRun.id, {
       issueId: issue.id,
     }));
   }
 
   const shouldActivate = escalationStepRun.data.status === STEP_STATUSES.backlog;
   if (shouldActivate) {
-    escalationStepRun = toStepRunRecord(await updateStepRun(ctx, escalationStepRun.id, {
+    escalationStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, escalationStepRun.id, {
       status: STEP_STATUSES.todo,
     }));
   }
@@ -421,7 +578,7 @@
     return;
   }
 
-  const typedStepRun = toStepRunRecord(stepRunRecord);
+  const typedStepRun = toWorkflowStepRunRecord(stepRunRecord);
   if (TERMINAL_STEP_STATUSES.has(typedStepRun.data.status)) {
     return;
   }
@@ -469,7 +626,7 @@
   if (options.allowRetry) {
     const retryInfo = getRetryInfo(typedWorkflowDefinition.data.steps, typedStepRun.data.stepId);
     if (retryInfo.shouldRetry && typedStepRun.data.retryCount < retryInfo.maxRetries) {
-      const retriedStepRun = toStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
+      const retriedStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
         completedAt: undefined,
         retryCount: typedStepRun.data.retryCount + 1,
         status: STEP_STATUSES.todo,
@@ -496,7 +653,7 @@
   }
 
   if (stepDef.onFailure === "skip") {
-    const skippedStepRun = toStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
+    const skippedStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
       completedAt: new Date().toISOString(),
       status: STEP_STATUSES.skipped,
     }));
@@ -556,7 +713,7 @@
       return;
     }
 
-    const escalatedStepRun = toStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
+    const escalatedStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
       completedAt: new Date().toISOString(),
       status: STEP_STATUSES.escalated,
     }));
@@ -638,7 +795,7 @@
           return;
         }
 
-        const stepRun = toStepRunRecord(stepRunRecord);
+        const stepRun = toWorkflowStepRunRecord(stepRunRecord);
         const payload = event.payload as { status?: string };
         const issueStatus = typeof payload.status === "string" ? payload.status : undefined;
 
@@ -657,7 +814,7 @@
           return;
         }
 
-        const updatedStepRun = toStepRunRecord(await updateStepRun(ctx, stepRun.id, {
+        const updatedStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, stepRun.id, {
           completedAt: nextStepStatus === STEP_STATUSES.done ? new Date().toISOString() : undefined,
           startedAt: nextStepStatus === STEP_STATUSES.inProgress
             ? stepRun.data.startedAt ?? new Date().toISOString()
@@ -807,7 +964,7 @@
             ...typedWorkflowRun.data,
           },
           stepRuns: stepRuns.map((record: PluginEntityRecord) => {
-            const stepRun = toStepRunRecord(record);
+            const stepRun = toWorkflowStepRunRecord(record);
             return {
               id: stepRun.id,
               status: stepRun.status,
diff --git a/paperclip-addon/plugins/workflow-engine/src/workflow-store.ts b/paperclip-addon/plugins/workflow-engine/src/workflow-store.ts
index 5871a2082258cebcfe8b69ebafbb0814cf303d50..3d7fb2b3bc75911cfd9093eb3a4054cc73196bb9
--- a/paperclip-addon/plugins/workflow-engine/src/workflow-store.ts
+++ b/paperclip-addon/plugins/workflow-engine/src/workflow-store.ts
@@ -44,7 +44,14 @@
 
 type TypedEntityRecord<T> = Omit<PluginEntityRecord, "data"> & { data: T };
 
-function toTypedRecord<T>(record: PluginEntityRecord): TypedEntityRecord<T> {
+function toTypedRecord<T>(
+  record: PluginEntityRecord,
+  entityType: string,
+): TypedEntityRecord<T> {
+  if (record.entityType !== entityType) {
+    throw new Error(`Expected entity type "${entityType}", got "${record.entityType}"`);
+  }
+
   return record as TypedEntityRecord<T>;
 }
 
@@ -67,13 +74,26 @@
   id: string,
   entityType: string,
 ): Promise<TypedEntityRecord<T> | null> {
+  const listQuery: Parameters<PluginContext["entities"]["list"]>[0] & { id?: string } = {
+    entityType,
+    id,
+    limit: 1,
+  };
+
+  const listed = await ctx.entities.list(listQuery);
+  const listedRecord = listed.find((record) => record.id === id) ?? null;
+  if (listedRecord) {
+    return toTypedRecord<T>(listedRecord, entityType);
+  }
+
+  // Fallback: some host SDK variants ignore `id` on entities.list().
+  // Keep defensive type verification on direct get() until id-filter support is universal.
   const record = await ctx.entities.get(id);
-
-  if (!record || record.entityType !== entityType) {
+  if (!record) {
     return null;
   }
 
-  return toTypedRecord<T>(record);
+  return toTypedRecord<T>(record, entityType);
 }
 
 async function requireEntityByType<T>(
diff --git a/paperclip-addon/plugins/workflow-engine/src/workflow-utils.ts b/paperclip-addon/plugins/workflow-engine/src/workflow-utils.ts
new file mode 100644
index 0000000000000000000000000000000000000000..ec1cf6f6b9aa722b126b58c0a728b4e538ad1494
--- /dev/null
+++ b/paperclip-addon/plugins/workflow-engine/src/workflow-utils.ts
@@ -0,0 +1,62 @@
+import type { PluginEntityRecord } from "@paperclipai/plugin-sdk";
+
+import { STEP_STATUSES } from "./constants.js";
+import type { WorkflowStep } from "./dag-engine.js";
+import type {
+  WorkflowDefinition,
+  WorkflowRun,
+  WorkflowStepRun,
+} from "./workflow-store.js";
+
+export type WorkflowRunRecord = PluginEntityRecord & { data: WorkflowRun };
+export type WorkflowStepRunRecord = PluginEntityRecord & { data: WorkflowStepRun };
+export type WorkflowDefinitionRecord = PluginEntityRecord & { data: WorkflowDefinition };
+type StepAgentMetadata = WorkflowStep & {
+  agent?: string;
+  agentName?: string;
+  assigneeAgentName?: string;
+};
+
+export const TERMINAL_STEP_STATUSES = new Set<string>([
+  STEP_STATUSES.done,
+  STEP_STATUSES.failed,
+  STEP_STATUSES.skipped,
+  STEP_STATUSES.escalated,
+]);
+
+export function toWorkflowRunRecord(record: PluginEntityRecord): WorkflowRunRecord {
+  return record as WorkflowRunRecord;
+}
+
+export function toWorkflowStepRunRecord(record: PluginEntityRecord): WorkflowStepRunRecord {
+  return record as WorkflowStepRunRecord;
+}
+
+export function toWorkflowDefinitionRecord(record: PluginEntityRecord): WorkflowDefinitionRecord {
+  return record as WorkflowDefinitionRecord;
+}
+
+export function findStepDefinition(definition: WorkflowDefinitionRecord, stepId: string): WorkflowStep | null {
+  return definition.data.steps.find((step: WorkflowStep) => step.id === stepId) ?? null;
+}
+
+export function getStepAgentNameHint(stepDef: WorkflowStep): string | null {
+  const stepMeta = stepDef as StepAgentMetadata;
+  const candidates = [stepMeta.agentName, stepMeta.agent, stepMeta.assigneeAgentName];
+
+  for (const candidate of candidates) {
+    if (typeof candidate === "string" && candidate.trim()) {
+      return candidate.trim();
+    }
+  }
+
+  return null;
+}
+
+export function getStepAgentName(stepRun: WorkflowStepRunRecord, stepDef: WorkflowStep): string | null {
+  if (typeof stepRun.data.agentName === "string" && stepRun.data.agentName.trim()) {
+    return stepRun.data.agentName.trim();
+  }
+
+  return getStepAgentNameHint(stepDef);
+}

file update:
diff --git a/paperclip-addon/plugins/workflow-engine/src/dag-engine.ts b/paperclip-addon/plugins/workflow-engine/src/dag-engine.ts
index b59510f0473171038be77277286d3b5c2063e6c5..e7f21ff998cab78b9325a8aeacf4fda9c5e06553
--- a/paperclip-addon/plugins/workflow-engine/src/dag-engine.ts
+++ b/paperclip-addon/plugins/workflow-engine/src/dag-engine.ts
@@ -2,6 +2,7 @@
   id: string;
   title: string;
   dependsOn: string[];
+  sessionMode?: "fresh" | "reuse";
   onFailure?: "retry" | "skip" | "abort_workflow" | "escalate";
   escalateTo?: string;
   maxRetries?: number;
diff --git a/paperclip-addon/plugins/workflow-engine/src/reconciler.ts b/paperclip-addon/plugins/workflow-engine/src/reconciler.ts
index 14ab4e766a835d145a6c2e7b1b564270285cbdc3..d2ab54817bd8185944379d2188bfbdef1722020a
--- a/paperclip-addon/plugins/workflow-engine/src/reconciler.ts
+++ b/paperclip-addon/plugins/workflow-engine/src/reconciler.ts
@@ -8,65 +8,22 @@
   listStepRuns,
   updateStepRun,
   updateWorkflowRun,
-  type WorkflowDefinition,
-  type WorkflowRun,
-  type WorkflowStepRun,
 } from "./workflow-store.js";
 import type { WorkflowStep } from "./dag-engine.js";
-
-type WorkflowRunRecord = PluginEntityRecord & { data: WorkflowRun };
-type WorkflowStepRunRecord = PluginEntityRecord & { data: WorkflowStepRun };
-type WorkflowDefinitionRecord = PluginEntityRecord & { data: WorkflowDefinition };
-type StepAgentMetadata = WorkflowStep & {
-  agent?: string;
-  agentName?: string;
-  assigneeAgentName?: string;
-};
+import {
+  TERMINAL_STEP_STATUSES,
+  findStepDefinition,
+  getStepAgentName,
+  toWorkflowDefinitionRecord,
+  toWorkflowRunRecord,
+  toWorkflowStepRunRecord,
+} from "./workflow-utils.js";
 
 const STALE_THRESHOLD_MS = 300_000;
-const TERMINAL_STEP_STATUSES = new Set<string>([
-  STEP_STATUSES.done,
-  STEP_STATUSES.failed,
-  STEP_STATUSES.skipped,
-  STEP_STATUSES.escalated,
-]);
 
 function isStale(record: PluginEntityRecord, thresholdMs: number): boolean {
   const updatedAt = new Date(record.updatedAt).getTime();
   return Date.now() - updatedAt > thresholdMs;
-}
-
-function toWorkflowRunRecord(record: PluginEntityRecord): WorkflowRunRecord {
-  return record as WorkflowRunRecord;
-}
-
-function toWorkflowStepRunRecord(record: PluginEntityRecord): WorkflowStepRunRecord {
-  return record as WorkflowStepRunRecord;
-}
-
-function toWorkflowDefinitionRecord(record: PluginEntityRecord): WorkflowDefinitionRecord {
-  return record as WorkflowDefinitionRecord;
-}
-
-function findStepDefinition(definition: WorkflowDefinitionRecord, stepId: string): WorkflowStep | null {
-  return definition.data.steps.find((step: WorkflowStep) => step.id === stepId) ?? null;
-}
-
-function getStepAgentName(stepRun: WorkflowStepRunRecord, stepDef: WorkflowStep): string | null {
-  if (typeof stepRun.data.agentName === "string" && stepRun.data.agentName.trim()) {
-    return stepRun.data.agentName.trim();
-  }
-
-  const stepMeta = stepDef as StepAgentMetadata;
-  const candidates = [stepMeta.agentName, stepMeta.agent, stepMeta.assigneeAgentName];
-
-  for (const candidate of candidates) {
-    if (typeof candidate === "string" && candidate.trim()) {
-      return candidate.trim();
-    }
-  }
-
-  return null;
 }
 
 async function invokeAgentByName(
diff --git a/paperclip-addon/plugins/workflow-engine/src/worker.ts b/paperclip-addon/plugins/workflow-engine/src/worker.ts
index c5ac3fbb4ca7f767eb29331bd3630b635449b335..0618dc61f481ff717d3dfc0edc4adabaab9e5d19
--- a/paperclip-addon/plugins/workflow-engine/src/worker.ts
+++ b/paperclip-addon/plugins/workflow-engine/src/worker.ts
@@ -15,6 +15,7 @@
 } from "./dag-engine.js";
 import {
   checkIdempotency,
+  createWorkflowRun,
   createStepRun,
   findStepRunByIssueId,
   getWorkflowDefinition,
@@ -25,32 +26,30 @@
   markIdempotency,
   updateStepRun,
   updateWorkflowRun,
-  type WorkflowDefinition,
-  type WorkflowRun,
   type WorkflowStepRun,
 } from "./workflow-store.js";
+import {
+  TERMINAL_STEP_STATUSES,
+  findStepDefinition,
+  getStepAgentName,
+  getStepAgentNameHint,
+  toWorkflowDefinitionRecord,
+  toWorkflowRunRecord,
+  toWorkflowStepRunRecord,
+  type WorkflowDefinitionRecord,
+  type WorkflowRunRecord,
+  type WorkflowStepRunRecord,
+} from "./workflow-utils.js";
 
-type WorkflowStepRecord = PluginEntityRecord & { data: WorkflowStepRun };
-type WorkflowRunRecord = PluginEntityRecord & { data: WorkflowRun };
-type WorkflowDefinitionRecord = PluginEntityRecord & { data: WorkflowDefinition };
 type WorkflowStepMetadata = WorkflowStep & {
   description?: string;
-  agent?: string;
-  agentName?: string;
-  assigneeAgentName?: string;
   assigneeAgentId?: string;
+  sessionMode?: "fresh" | "reuse";
 };
 type AgentRecord = Awaited<ReturnType<PluginContext["agents"]["list"]>>[number];
 type ReconcilerModule = {
   reconcileStuckSteps?: (ctx: PluginContext) => Promise<void>;
 };
-
-const TERMINAL_STEP_STATUSES = new Set<string>([
-  STEP_STATUSES.done,
-  STEP_STATUSES.failed,
-  STEP_STATUSES.skipped,
-  STEP_STATUSES.escalated,
-]);
 
 function summarizeError(error: unknown): string {
   if (error instanceof Error) {
@@ -60,28 +59,12 @@
   return String(error);
 }
 
-function toStepRunRecord(record: PluginEntityRecord): WorkflowStepRecord {
-  return record as WorkflowStepRecord;
-}
-
-function toWorkflowRunRecord(record: PluginEntityRecord): WorkflowRunRecord {
-  return record as WorkflowRunRecord;
-}
-
-function toWorkflowDefinitionRecord(record: PluginEntityRecord): WorkflowDefinitionRecord {
-  return record as WorkflowDefinitionRecord;
-}
-
 function buildIdempotencyKey(event: PluginEvent): string {
   return `${event.eventType}:${event.eventId}`;
 }
 
 function getStepMetadata(stepDef: WorkflowStep): WorkflowStepMetadata {
   return stepDef as WorkflowStepMetadata;
-}
-
-function findStepDefinition(definition: WorkflowDefinitionRecord, stepId: string): WorkflowStep | null {
-  return definition.data.steps.find((step: WorkflowStep) => step.id === stepId) ?? null;
 }
 
 function getStepDescription(stepDef: WorkflowStep): string | undefined {
@@ -89,19 +72,6 @@
   return typeof description === "string" && description.trim() ? description.trim() : undefined;
 }
 
-function getStepAgentNameHint(stepDef: WorkflowStep): string | null {
-  const meta = getStepMetadata(stepDef);
-  const candidates = [meta.agentName, meta.agent, meta.assigneeAgentName];
-
-  for (const candidate of candidates) {
-    if (typeof candidate === "string" && candidate.trim()) {
-      return candidate.trim();
-    }
-  }
-
-  return null;
-}
-
 function getStepAgentIdHint(stepDef: WorkflowStep): string | null {
   const assigneeAgentId = getStepMetadata(stepDef).assigneeAgentId;
   if (typeof assigneeAgentId !== "string" || !assigneeAgentId.trim()) {
@@ -147,60 +117,247 @@
 
 async function invokeAgentForStep(
   ctx: PluginContext,
+  stepRunRecord: WorkflowStepRunRecord,
   stepDef: WorkflowStep,
-  agentName: string,
   workflowName: string,
   companyId: string,
-): Promise<void> {
+): Promise<WorkflowStepRunRecord> {
+  const agentName = getStepAgentName(stepRunRecord, stepDef);
+  if (!agentName) {
+    ctx.logger.warn("Agent name is missing for workflow step", {
+      companyId,
+      runId: stepRunRecord.data.runId,
+      stepId: stepDef.id,
+      workflowName,
+    });
+    return stepRunRecord;
+  }
+
   const agents = await ctx.agents.list({ companyId });
-  const agent = agents.find((candidate: AgentRecord) => candidate.name === agentName);
+  const agent = agents.find((candidate: AgentRecord) => candidate.name === agentName) ?? null;
 
   if (!agent) {
     ctx.logger.warn("Agent not found for step", {
       agentName,
       companyId,
+      runId: stepRunRecord.data.runId,
+      stepId: stepDef.id,
+      workflowName,
+    });
+    return stepRunRecord;
+  }
+
+  const prompt = `workflow:${workflowName}/step:${stepDef.id} — "${stepDef.title}" is ready. Please proceed with the assigned task.`;
+  const reason = `workflow:${workflowName}/step:${stepDef.id}`;
+  const sessionMode = getStepMetadata(stepDef).sessionMode === "reuse" ? "reuse" : "fresh";
+
+  if (sessionMode === "reuse") {
+    let sessionId = typeof stepRunRecord.data.sessionId === "string" && stepRunRecord.data.sessionId.trim()
+      ? stepRunRecord.data.sessionId.trim()
+      : "";
+
+    if (!sessionId) {
+      const session = await ctx.agents.sessions.create(agent.id, companyId, {
+        reason,
+      });
+      sessionId = session.sessionId;
+      stepRunRecord = toWorkflowStepRunRecord(await updateStepRun(ctx, stepRunRecord.id, {
+        sessionId,
+      }));
+    }
+
+    await ctx.agents.sessions.sendMessage(sessionId, companyId, {
+      prompt,
+      reason,
+    });
+
+    ctx.logger.info("Sent workflow step prompt via agent session", {
+      agentId: agent.id,
+      agentName,
+      companyId,
+      runId: stepRunRecord.data.runId,
+      sessionId,
       stepId: stepDef.id,
       workflowName,
     });
-    return;
+    return stepRunRecord;
   }
 
   await ctx.agents.invoke(agent.id, companyId, {
-    prompt: `workflow:${workflowName}/step:${stepDef.id} — "${stepDef.title}" is ready. Please proceed with the assigned task.`,
-    reason: `workflow:${workflowName}/step:${stepDef.id}`,
+    prompt,
+    reason,
   });
 
   ctx.logger.info("Invoked agent for workflow step", {
     agentId: agent.id,
     agentName,
     companyId,
+    runId: stepRunRecord.data.runId,
     stepId: stepDef.id,
     workflowName,
   });
+
+  return stepRunRecord;
 }
 
 async function activateBacklogStep(
   ctx: PluginContext,
-  stepRunRecord: WorkflowStepRecord,
+  stepRunRecord: WorkflowStepRunRecord,
   stepDef: WorkflowStep,
   workflowName: string,
   companyId: string,
-): Promise<void> {
+  options?: { parentIssueId?: string },
+): Promise<WorkflowStepRunRecord> {
   if (stepRunRecord.data.status !== STEP_STATUSES.backlog) {
-    return;
+    return stepRunRecord;
   }
 
-  const updated = toStepRunRecord(await updateStepRun(ctx, stepRunRecord.id, {
+  const resolvedAgent = await resolveStepAgent(
+    ctx,
+    companyId,
+    stepDef,
+    stepRunRecord.data.agentName,
+  );
+
+  let issueId = typeof stepRunRecord.data.issueId === "string" && stepRunRecord.data.issueId.trim()
+    ? stepRunRecord.data.issueId.trim()
+    : "";
+  const stepDescription = getStepDescription(stepDef) ?? `Workflow step: ${stepDef.id}`;
+
+  if (!issueId) {
+    const issue = await ctx.issues.create({
+      assigneeAgentId: resolvedAgent.agentId ?? undefined,
+      companyId,
+      description: stepDescription,
+      parentId: options?.parentIssueId,
+      title: `[${workflowName}] ${stepDef.title}`,
+    });
+    issueId = issue.id;
+  }
+
+  const nextStartedAt = stepRunRecord.data.startedAt ?? new Date().toISOString();
+  let updatedStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, stepRunRecord.id, {
+    agentName: resolvedAgent.agentName ?? stepRunRecord.data.agentName,
+    issueId,
+    startedAt: nextStartedAt,
     status: STEP_STATUSES.todo,
   }));
 
-  await invokeAgentForStep(
+  updatedStepRun = await invokeAgentForStep(
     ctx,
+    updatedStepRun,
     stepDef,
-    updated.data.agentName,
     workflowName,
     companyId,
   );
+
+  return updatedStepRun;
+}
+
+async function startWorkflow(
+  ctx: PluginContext,
+  workflowId: string,
+  companyId: string,
+  options?: {
+    createParentIssue?: boolean;
+    parentIssueId?: string;
+  },
+): Promise<{
+  activatedStepIds: string[];
+  parentIssueId: string | null;
+  runId: string;
+  workflowId: string;
+}> {
+  const workflowDefinition = await getWorkflowDefinition(ctx, workflowId);
+  if (!workflowDefinition) {
+    throw new Error(`Workflow definition not found: ${workflowId}`);
+  }
+
+  const typedWorkflowDefinition = toWorkflowDefinitionRecord(workflowDefinition);
+  if (typedWorkflowDefinition.data.companyId !== companyId) {
+    throw new Error(`Workflow does not belong to company: ${workflowId}`);
+  }
+
+  let parentIssueId = typeof options?.parentIssueId === "string" && options.parentIssueId.trim()
+    ? options.parentIssueId.trim()
+    : "";
+  if (!parentIssueId && options?.createParentIssue) {
+    const parentIssue = await ctx.issues.create({
+      companyId,
+      description: typedWorkflowDefinition.data.description || `Workflow run: ${typedWorkflowDefinition.data.name}`,
+      title: `[${typedWorkflowDefinition.data.name}] Workflow run`,
+    });
+    parentIssueId = parentIssue.id;
+  }
+
+  const workflowRun = toWorkflowRunRecord(await createWorkflowRun(ctx, {
+    companyId,
+    parentIssueId: parentIssueId || undefined,
+    startedAt: new Date().toISOString(),
+    status: RUN_STATUSES.running,
+    workflowId: typedWorkflowDefinition.id,
+    workflowName: typedWorkflowDefinition.data.name,
+  }));
+
+  const agents = await ctx.agents.list({ companyId });
+  const agentsByName = new Map<string, AgentRecord>();
+  for (const agent of agents) {
+    agentsByName.set(agent.name, agent);
+  }
+
+  const pendingRootSteps: Array<{ stepDef: WorkflowStep; stepRun: WorkflowStepRunRecord }> = [];
+  for (const stepDef of typedWorkflowDefinition.data.steps) {
+    const agentNameHint = getStepAgentNameHint(stepDef);
+    const matchedAgent = agentNameHint ? agentsByName.get(agentNameHint) ?? null : null;
+    const resolvedAgent = matchedAgent
+      ? { agentId: matchedAgent.id, agentName: matchedAgent.name }
+      : await resolveStepAgent(ctx, companyId, stepDef, agentNameHint ?? undefined);
+
+    if (!resolvedAgent.agentName) {
+      throw new Error(`Unable to resolve step assignee for "${stepDef.id}"`);
+    }
+
+    const stepRun = toWorkflowStepRunRecord(await createStepRun(ctx, companyId, {
+      agentName: resolvedAgent.agentName,
+      retryCount: 0,
+      runId: workflowRun.id,
+      status: STEP_STATUSES.backlog,
+      stepId: stepDef.id,
+    }));
+
+    if (stepDef.dependsOn.length === 0 && stepDef.triggerOn !== "escalation") {
+      pendingRootSteps.push({ stepDef, stepRun });
+    }
+  }
+
+  const activatedStepIds: string[] = [];
+  for (const pending of pendingRootSteps) {
+    await activateBacklogStep(
+      ctx,
+      pending.stepRun,
+      pending.stepDef,
+      typedWorkflowDefinition.data.name,
+      companyId,
+      { parentIssueId: parentIssueId || undefined },
+    );
+    activatedStepIds.push(pending.stepDef.id);
+  }
+
+  ctx.logger.info("Started workflow run", {
+    activatedStepIds,
+    companyId,
+    parentIssueId: parentIssueId || null,
+    runId: workflowRun.id,
+    workflowId: typedWorkflowDefinition.id,
+    workflowName: typedWorkflowDefinition.data.name,
+  });
+
+  return {
+    activatedStepIds,
+    parentIssueId: parentIssueId || null,
+    runId: workflowRun.id,
+    workflowId: typedWorkflowDefinition.id,
+  };
 }
 
 async function advanceWorkflow(
@@ -208,7 +365,7 @@
   stepRunRecord: PluginEntityRecord,
   companyId: string,
 ): Promise<void> {
-  const completedStepRun = toStepRunRecord(stepRunRecord);
+  const completedStepRun = toWorkflowStepRunRecord(stepRunRecord);
   const workflowRun = await getWorkflowRun(ctx, completedStepRun.data.runId);
 
   if (!workflowRun) {
@@ -237,7 +394,7 @@
   }
 
   const typedWorkflowDefinition = toWorkflowDefinitionRecord(workflowDefinition);
-  const stepRuns = (await listStepRuns(ctx, typedWorkflowRun.id, companyId)).map(toStepRunRecord);
+  const stepRuns = (await listStepRuns(ctx, typedWorkflowRun.id, companyId)).map(toWorkflowStepRunRecord);
   const completed = new Set<string>();
   const failed = new Set<string>();
   const skipped = new Set<string>();
@@ -289,7 +446,7 @@
         continue;
       }
 
-      stepRun = toStepRunRecord(await createStepRun(ctx, companyId, {
+      stepRun = toWorkflowStepRunRecord(await createStepRun(ctx, companyId, {
         agentName: resolvedAgent.agentName,
         retryCount: 0,
         runId: typedWorkflowRun.id,
@@ -338,7 +495,7 @@
     throw new Error(`Escalation target step not found: ${escalationTargetId}`);
   }
 
-  const stepRuns = (await listStepRuns(ctx, workflowRun.id, companyId)).map(toStepRunRecord);
+  const stepRuns = (await listStepRuns(ctx, workflowRun.id, companyId)).map(toWorkflowStepRunRecord);
   let escalationStepRun = stepRuns.find((candidate) => candidate.data.stepId === escalationTargetId) ?? null;
   const resolvedAgent = await resolveStepAgent(
     ctx,
@@ -352,7 +509,7 @@
   }
 
   if (!escalationStepRun) {
-    escalationStepRun = toStepRunRecord(await createStepRun(ctx, companyId, {
+    escalationStepRun = toWorkflowStepRunRecord(await createStepRun(ctx, companyId, {
       agentName: resolvedAgent.agentName,
       retryCount: 0,
       runId: workflowRun.id,
@@ -374,14 +531,14 @@
       title: `${workflowRun.data.workflowName}: ${escalationStep.title}`,
     });
 
-    escalationStepRun = toStepRunRecord(await updateStepRun(ctx, escalationStepRun.id, {
+    escalationStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, escalationStepRun.id, {
       issueId: issue.id,
     }));
   }
 
   const shouldActivate = escalationStepRun.data.status === STEP_STATUSES.backlog;
   if (shouldActivate) {
-    escalationStepRun = toStepRunRecord(await updateStepRun(ctx, escalationStepRun.id, {
+    escalationStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, escalationStepRun.id, {
       status: STEP_STATUSES.todo,
     }));
   }
@@ -421,7 +578,7 @@
     return;
   }
 
-  const typedStepRun = toStepRunRecord(stepRunRecord);
+  const typedStepRun = toWorkflowStepRunRecord(stepRunRecord);
   if (TERMINAL_STEP_STATUSES.has(typedStepRun.data.status)) {
     return;
   }
@@ -469,7 +626,7 @@
   if (options.allowRetry) {
     const retryInfo = getRetryInfo(typedWorkflowDefinition.data.steps, typedStepRun.data.stepId);
     if (retryInfo.shouldRetry && typedStepRun.data.retryCount < retryInfo.maxRetries) {
-      const retriedStepRun = toStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
+      const retriedStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
         completedAt: undefined,
         retryCount: typedStepRun.data.retryCount + 1,
         status: STEP_STATUSES.todo,
@@ -496,7 +653,7 @@
   }
 
   if (stepDef.onFailure === "skip") {
-    const skippedStepRun = toStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
+    const skippedStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
       completedAt: new Date().toISOString(),
       status: STEP_STATUSES.skipped,
     }));
@@ -556,7 +713,7 @@
       return;
     }
 
-    const escalatedStepRun = toStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
+    const escalatedStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
       completedAt: new Date().toISOString(),
       status: STEP_STATUSES.escalated,
     }));
@@ -638,7 +795,7 @@
           return;
         }
 
-        const stepRun = toStepRunRecord(stepRunRecord);
+        const stepRun = toWorkflowStepRunRecord(stepRunRecord);
         const payload = event.payload as { status?: string };
         const issueStatus = typeof payload.status === "string" ? payload.status : undefined;
 
@@ -657,7 +814,7 @@
           return;
         }
 
-        const updatedStepRun = toStepRunRecord(await updateStepRun(ctx, stepRun.id, {
+        const updatedStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, stepRun.id, {
           completedAt: nextStepStatus === STEP_STATUSES.done ? new Date().toISOString() : undefined,
           startedAt: nextStepStatus === STEP_STATUSES.inProgress
             ? stepRun.data.startedAt ?? new Date().toISOString()
@@ -807,7 +964,7 @@
             ...typedWorkflowRun.data,
           },
           stepRuns: stepRuns.map((record: PluginEntityRecord) => {
-            const stepRun = toStepRunRecord(record);
+            const stepRun = toWorkflowStepRunRecord(record);
             return {
               id: stepRun.id,
               status: stepRun.status,
diff --git a/paperclip-addon/plugins/workflow-engine/src/workflow-store.ts b/paperclip-addon/plugins/workflow-engine/src/workflow-store.ts
index 5871a2082258cebcfe8b69ebafbb0814cf303d50..3d7fb2b3bc75911cfd9093eb3a4054cc73196bb9
--- a/paperclip-addon/plugins/workflow-engine/src/workflow-store.ts
+++ b/paperclip-addon/plugins/workflow-engine/src/workflow-store.ts
@@ -44,7 +44,14 @@
 
 type TypedEntityRecord<T> = Omit<PluginEntityRecord, "data"> & { data: T };
 
-function toTypedRecord<T>(record: PluginEntityRecord): TypedEntityRecord<T> {
+function toTypedRecord<T>(
+  record: PluginEntityRecord,
+  entityType: string,
+): TypedEntityRecord<T> {
+  if (record.entityType !== entityType) {
+    throw new Error(`Expected entity type "${entityType}", got "${record.entityType}"`);
+  }
+
   return record as TypedEntityRecord<T>;
 }
 
@@ -67,13 +74,26 @@
   id: string,
   entityType: string,
 ): Promise<TypedEntityRecord<T> | null> {
+  const listQuery: Parameters<PluginContext["entities"]["list"]>[0] & { id?: string } = {
+    entityType,
+    id,
+    limit: 1,
+  };
+
+  const listed = await ctx.entities.list(listQuery);
+  const listedRecord = listed.find((record) => record.id === id) ?? null;
+  if (listedRecord) {
+    return toTypedRecord<T>(listedRecord, entityType);
+  }
+
+  // Fallback: some host SDK variants ignore `id` on entities.list().
+  // Keep defensive type verification on direct get() until id-filter support is universal.
   const record = await ctx.entities.get(id);
-
-  if (!record || record.entityType !== entityType) {
+  if (!record) {
     return null;
   }
 
-  return toTypedRecord<T>(record);
+  return toTypedRecord<T>(record, entityType);
 }
 
 async function requireEntityByType<T>(
diff --git a/paperclip-addon/plugins/workflow-engine/src/workflow-utils.ts b/paperclip-addon/plugins/workflow-engine/src/workflow-utils.ts
new file mode 100644
index 0000000000000000000000000000000000000000..ec1cf6f6b9aa722b126b58c0a728b4e538ad1494
--- /dev/null
+++ b/paperclip-addon/plugins/workflow-engine/src/workflow-utils.ts
@@ -0,0 +1,62 @@
+import type { PluginEntityRecord } from "@paperclipai/plugin-sdk";
+
+import { STEP_STATUSES } from "./constants.js";
+import type { WorkflowStep } from "./dag-engine.js";
+import type {
+  WorkflowDefinition,
+  WorkflowRun,
+  WorkflowStepRun,
+} from "./workflow-store.js";
+
+export type WorkflowRunRecord = PluginEntityRecord & { data: WorkflowRun };
+export type WorkflowStepRunRecord = PluginEntityRecord & { data: WorkflowStepRun };
+export type WorkflowDefinitionRecord = PluginEntityRecord & { data: WorkflowDefinition };
+type StepAgentMetadata = WorkflowStep & {
+  agent?: string;
+  agentName?: string;
+  assigneeAgentName?: string;
+};
+
+export const TERMINAL_STEP_STATUSES = new Set<string>([
+  STEP_STATUSES.done,
+  STEP_STATUSES.failed,
+  STEP_STATUSES.skipped,
+  STEP_STATUSES.escalated,
+]);
+
+export function toWorkflowRunRecord(record: PluginEntityRecord): WorkflowRunRecord {
+  return record as WorkflowRunRecord;
+}
+
+export function toWorkflowStepRunRecord(record: PluginEntityRecord): WorkflowStepRunRecord {
+  return record as WorkflowStepRunRecord;
+}
+
+export function toWorkflowDefinitionRecord(record: PluginEntityRecord): WorkflowDefinitionRecord {
+  return record as WorkflowDefinitionRecord;
+}
+
+export function findStepDefinition(definition: WorkflowDefinitionRecord, stepId: string): WorkflowStep | null {
+  return definition.data.steps.find((step: WorkflowStep) => step.id === stepId) ?? null;
+}
+
+export function getStepAgentNameHint(stepDef: WorkflowStep): string | null {
+  const stepMeta = stepDef as StepAgentMetadata;
+  const candidates = [stepMeta.agentName, stepMeta.agent, stepMeta.assigneeAgentName];
+
+  for (const candidate of candidates) {
+    if (typeof candidate === "string" && candidate.trim()) {
+      return candidate.trim();
+    }
+  }
+
+  return null;
+}
+
+export function getStepAgentName(stepRun: WorkflowStepRunRecord, stepDef: WorkflowStep): string | null {
+  if (typeof stepRun.data.agentName === "string" && stepRun.data.agentName.trim()) {
+    return stepRun.data.agentName.trim();
+  }
+
+  return getStepAgentNameHint(stepDef);
+}

exec
/bin/zsh -lc "rg -n \"WorkflowStepRecord|toStepRunRecord|invokeAgentForStep\\(|activateBacklogStep\\(|startWorkflow\\(|ctx\\.data\\.register|issue\\.created|markIdempotency\\(|WorkflowStepRun\\[\\\"status\\\"\\]\" plugins/workflow-engine/src/worker.ts" in /Users/kwak/Projects/paperclip/paperclip-addon succeeded in 0ms:
118:async function invokeAgentForStep(
203:async function activateBacklogStep(
246:  updatedStepRun = await invokeAgentForStep(
257:async function startWorkflow(
335:    await activateBacklogStep(
459:    await activateBacklogStep(
489:  sourceStepRun: WorkflowStepRecord,
547:    await invokeAgentForStep(
635:      await invokeAgentForStep(
643:      await markIdempotency(ctx, idempotencyKey, event.companyId);
662:    await markIdempotency(ctx, idempotencyKey, event.companyId);
681:    await markIdempotency(ctx, idempotencyKey, event.companyId);
706:      await markIdempotency(ctx, idempotencyKey, event.companyId);
730:    await markIdempotency(ctx, idempotencyKey, event.companyId);
749:  await markIdempotency(ctx, idempotencyKey, event.companyId);
822:          status: nextStepStatus as WorkflowStepRun["status"],
829:        await markIdempotency(ctx, idempotencyKey, event.companyId);
845:    ctx.events.on("issue.created", async (event: PluginEvent) => {
868:        ctx.logger.warn("Failed to handle issue.created event", {
904:    ctx.data.register("workflow-overview", async (params: Record<string, unknown>) => {
file update:
diff --git a/paperclip-addon/plugins/workflow-engine/src/dag-engine.ts b/paperclip-addon/plugins/workflow-engine/src/dag-engine.ts
index b59510f0473171038be77277286d3b5c2063e6c5..e7f21ff998cab78b9325a8aeacf4fda9c5e06553
--- a/paperclip-addon/plugins/workflow-engine/src/dag-engine.ts
+++ b/paperclip-addon/plugins/workflow-engine/src/dag-engine.ts
@@ -2,6 +2,7 @@
   id: string;
   title: string;
   dependsOn: string[];
+  sessionMode?: "fresh" | "reuse";
   onFailure?: "retry" | "skip" | "abort_workflow" | "escalate";
   escalateTo?: string;
   maxRetries?: number;
diff --git a/paperclip-addon/plugins/workflow-engine/src/reconciler.ts b/paperclip-addon/plugins/workflow-engine/src/reconciler.ts
index 14ab4e766a835d145a6c2e7b1b564270285cbdc3..d2ab54817bd8185944379d2188bfbdef1722020a
--- a/paperclip-addon/plugins/workflow-engine/src/reconciler.ts
+++ b/paperclip-addon/plugins/workflow-engine/src/reconciler.ts
@@ -8,65 +8,22 @@
   listStepRuns,
   updateStepRun,
   updateWorkflowRun,
-  type WorkflowDefinition,
-  type WorkflowRun,
-  type WorkflowStepRun,
 } from "./workflow-store.js";
 import type { WorkflowStep } from "./dag-engine.js";
-
-type WorkflowRunRecord = PluginEntityRecord & { data: WorkflowRun };
-type WorkflowStepRunRecord = PluginEntityRecord & { data: WorkflowStepRun };
-type WorkflowDefinitionRecord = PluginEntityRecord & { data: WorkflowDefinition };
-type StepAgentMetadata = WorkflowStep & {
-  agent?: string;
-  agentName?: string;
-  assigneeAgentName?: string;
-};
+import {
+  TERMINAL_STEP_STATUSES,
+  findStepDefinition,
+  getStepAgentName,
+  toWorkflowDefinitionRecord,
+  toWorkflowRunRecord,
+  toWorkflowStepRunRecord,
+} from "./workflow-utils.js";
 
 const STALE_THRESHOLD_MS = 300_000;
-const TERMINAL_STEP_STATUSES = new Set<string>([
-  STEP_STATUSES.done,
-  STEP_STATUSES.failed,
-  STEP_STATUSES.skipped,
-  STEP_STATUSES.escalated,
-]);
 
 function isStale(record: PluginEntityRecord, thresholdMs: number): boolean {
   const updatedAt = new Date(record.updatedAt).getTime();
   return Date.now() - updatedAt > thresholdMs;
-}
-
-function toWorkflowRunRecord(record: PluginEntityRecord): WorkflowRunRecord {
-  return record as WorkflowRunRecord;
-}
-
-function toWorkflowStepRunRecord(record: PluginEntityRecord): WorkflowStepRunRecord {
-  return record as WorkflowStepRunRecord;
-}
-
-function toWorkflowDefinitionRecord(record: PluginEntityRecord): WorkflowDefinitionRecord {
-  return record as WorkflowDefinitionRecord;
-}
-
-function findStepDefinition(definition: WorkflowDefinitionRecord, stepId: string): WorkflowStep | null {
-  return definition.data.steps.find((step: WorkflowStep) => step.id === stepId) ?? null;
-}
-
-function getStepAgentName(stepRun: WorkflowStepRunRecord, stepDef: WorkflowStep): string | null {
-  if (typeof stepRun.data.agentName === "string" && stepRun.data.agentName.trim()) {
-    return stepRun.data.agentName.trim();
-  }
-
-  const stepMeta = stepDef as StepAgentMetadata;
-  const candidates = [stepMeta.agentName, stepMeta.agent, stepMeta.assigneeAgentName];
-
-  for (const candidate of candidates) {
-    if (typeof candidate === "string" && candidate.trim()) {
-      return candidate.trim();
-    }
-  }
-
-  return null;
 }
 
 async function invokeAgentByName(
diff --git a/paperclip-addon/plugins/workflow-engine/src/worker.ts b/paperclip-addon/plugins/workflow-engine/src/worker.ts
index c5ac3fbb4ca7f767eb29331bd3630b635449b335..0618dc61f481ff717d3dfc0edc4adabaab9e5d19
--- a/paperclip-addon/plugins/workflow-engine/src/worker.ts
+++ b/paperclip-addon/plugins/workflow-engine/src/worker.ts
@@ -15,6 +15,7 @@
 } from "./dag-engine.js";
 import {
   checkIdempotency,
+  createWorkflowRun,
   createStepRun,
   findStepRunByIssueId,
   getWorkflowDefinition,
@@ -25,32 +26,30 @@
   markIdempotency,
   updateStepRun,
   updateWorkflowRun,
-  type WorkflowDefinition,
-  type WorkflowRun,
   type WorkflowStepRun,
 } from "./workflow-store.js";
+import {
+  TERMINAL_STEP_STATUSES,
+  findStepDefinition,
+  getStepAgentName,
+  getStepAgentNameHint,
+  toWorkflowDefinitionRecord,
+  toWorkflowRunRecord,
+  toWorkflowStepRunRecord,
+  type WorkflowDefinitionRecord,
+  type WorkflowRunRecord,
+  type WorkflowStepRunRecord,
+} from "./workflow-utils.js";
 
-type WorkflowStepRecord = PluginEntityRecord & { data: WorkflowStepRun };
-type WorkflowRunRecord = PluginEntityRecord & { data: WorkflowRun };
-type WorkflowDefinitionRecord = PluginEntityRecord & { data: WorkflowDefinition };
 type WorkflowStepMetadata = WorkflowStep & {
   description?: string;
-  agent?: string;
-  agentName?: string;
-  assigneeAgentName?: string;
   assigneeAgentId?: string;
+  sessionMode?: "fresh" | "reuse";
 };
 type AgentRecord = Awaited<ReturnType<PluginContext["agents"]["list"]>>[number];
 type ReconcilerModule = {
   reconcileStuckSteps?: (ctx: PluginContext) => Promise<void>;
 };
-
-const TERMINAL_STEP_STATUSES = new Set<string>([
-  STEP_STATUSES.done,
-  STEP_STATUSES.failed,
-  STEP_STATUSES.skipped,
-  STEP_STATUSES.escalated,
-]);
 
 function summarizeError(error: unknown): string {
   if (error instanceof Error) {
@@ -60,28 +59,12 @@
   return String(error);
 }
 
-function toStepRunRecord(record: PluginEntityRecord): WorkflowStepRecord {
-  return record as WorkflowStepRecord;
-}
-
-function toWorkflowRunRecord(record: PluginEntityRecord): WorkflowRunRecord {
-  return record as WorkflowRunRecord;
-}
-
-function toWorkflowDefinitionRecord(record: PluginEntityRecord): WorkflowDefinitionRecord {
-  return record as WorkflowDefinitionRecord;
-}
-
 function buildIdempotencyKey(event: PluginEvent): string {
   return `${event.eventType}:${event.eventId}`;
 }
 
 function getStepMetadata(stepDef: WorkflowStep): WorkflowStepMetadata {
   return stepDef as WorkflowStepMetadata;
-}
-
-function findStepDefinition(definition: WorkflowDefinitionRecord, stepId: string): WorkflowStep | null {
-  return definition.data.steps.find((step: WorkflowStep) => step.id === stepId) ?? null;
 }
 
 function getStepDescription(stepDef: WorkflowStep): string | undefined {
@@ -89,19 +72,6 @@
   return typeof description === "string" && description.trim() ? description.trim() : undefined;
 }
 
-function getStepAgentNameHint(stepDef: WorkflowStep): string | null {
-  const meta = getStepMetadata(stepDef);
-  const candidates = [meta.agentName, meta.agent, meta.assigneeAgentName];
-
-  for (const candidate of candidates) {
-    if (typeof candidate === "string" && candidate.trim()) {
-      return candidate.trim();
-    }
-  }
-
-  return null;
-}
-
 function getStepAgentIdHint(stepDef: WorkflowStep): string | null {
   const assigneeAgentId = getStepMetadata(stepDef).assigneeAgentId;
   if (typeof assigneeAgentId !== "string" || !assigneeAgentId.trim()) {
@@ -147,60 +117,247 @@
 
 async function invokeAgentForStep(
   ctx: PluginContext,
+  stepRunRecord: WorkflowStepRunRecord,
   stepDef: WorkflowStep,
-  agentName: string,
   workflowName: string,
   companyId: string,
-): Promise<void> {
+): Promise<WorkflowStepRunRecord> {
+  const agentName = getStepAgentName(stepRunRecord, stepDef);
+  if (!agentName) {
+    ctx.logger.warn("Agent name is missing for workflow step", {
+      companyId,
+      runId: stepRunRecord.data.runId,
+      stepId: stepDef.id,
+      workflowName,
+    });
+    return stepRunRecord;
+  }
+
   const agents = await ctx.agents.list({ companyId });
-  const agent = agents.find((candidate: AgentRecord) => candidate.name === agentName);
+  const agent = agents.find((candidate: AgentRecord) => candidate.name === agentName) ?? null;
 
   if (!agent) {
     ctx.logger.warn("Agent not found for step", {
       agentName,
       companyId,
+      runId: stepRunRecord.data.runId,
+      stepId: stepDef.id,
+      workflowName,
+    });
+    return stepRunRecord;
+  }
+
+  const prompt = `workflow:${workflowName}/step:${stepDef.id} — "${stepDef.title}" is ready. Please proceed with the assigned task.`;
+  const reason = `workflow:${workflowName}/step:${stepDef.id}`;
+  const sessionMode = getStepMetadata(stepDef).sessionMode === "reuse" ? "reuse" : "fresh";
+
+  if (sessionMode === "reuse") {
+    let sessionId = typeof stepRunRecord.data.sessionId === "string" && stepRunRecord.data.sessionId.trim()
+      ? stepRunRecord.data.sessionId.trim()
+      : "";
+
+    if (!sessionId) {
+      const session = await ctx.agents.sessions.create(agent.id, companyId, {
+        reason,
+      });
+      sessionId = session.sessionId;
+      stepRunRecord = toWorkflowStepRunRecord(await updateStepRun(ctx, stepRunRecord.id, {
+        sessionId,
+      }));
+    }
+
+    await ctx.agents.sessions.sendMessage(sessionId, companyId, {
+      prompt,
+      reason,
+    });
+
+    ctx.logger.info("Sent workflow step prompt via agent session", {
+      agentId: agent.id,
+      agentName,
+      companyId,
+      runId: stepRunRecord.data.runId,
+      sessionId,
       stepId: stepDef.id,
       workflowName,
     });
-    return;
+    return stepRunRecord;
   }
 
   await ctx.agents.invoke(agent.id, companyId, {
-    prompt: `workflow:${workflowName}/step:${stepDef.id} — "${stepDef.title}" is ready. Please proceed with the assigned task.`,
-    reason: `workflow:${workflowName}/step:${stepDef.id}`,
+    prompt,
+    reason,
   });
 
   ctx.logger.info("Invoked agent for workflow step", {
     agentId: agent.id,
     agentName,
     companyId,
+    runId: stepRunRecord.data.runId,
     stepId: stepDef.id,
     workflowName,
   });
+
+  return stepRunRecord;
 }
 
 async function activateBacklogStep(
   ctx: PluginContext,
-  stepRunRecord: WorkflowStepRecord,
+  stepRunRecord: WorkflowStepRunRecord,
   stepDef: WorkflowStep,
   workflowName: string,
   companyId: string,
-): Promise<void> {
+  options?: { parentIssueId?: string },
+): Promise<WorkflowStepRunRecord> {
   if (stepRunRecord.data.status !== STEP_STATUSES.backlog) {
-    return;
+    return stepRunRecord;
   }
 
-  const updated = toStepRunRecord(await updateStepRun(ctx, stepRunRecord.id, {
+  const resolvedAgent = await resolveStepAgent(
+    ctx,
+    companyId,
+    stepDef,
+    stepRunRecord.data.agentName,
+  );
+
+  let issueId = typeof stepRunRecord.data.issueId === "string" && stepRunRecord.data.issueId.trim()
+    ? stepRunRecord.data.issueId.trim()
+    : "";
+  const stepDescription = getStepDescription(stepDef) ?? `Workflow step: ${stepDef.id}`;
+
+  if (!issueId) {
+    const issue = await ctx.issues.create({
+      assigneeAgentId: resolvedAgent.agentId ?? undefined,
+      companyId,
+      description: stepDescription,
+      parentId: options?.parentIssueId,
+      title: `[${workflowName}] ${stepDef.title}`,
+    });
+    issueId = issue.id;
+  }
+
+  const nextStartedAt = stepRunRecord.data.startedAt ?? new Date().toISOString();
+  let updatedStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, stepRunRecord.id, {
+    agentName: resolvedAgent.agentName ?? stepRunRecord.data.agentName,
+    issueId,
+    startedAt: nextStartedAt,
     status: STEP_STATUSES.todo,
   }));
 
-  await invokeAgentForStep(
+  updatedStepRun = await invokeAgentForStep(
     ctx,
+    updatedStepRun,
     stepDef,
-    updated.data.agentName,
     workflowName,
     companyId,
   );
+
+  return updatedStepRun;
+}
+
+async function startWorkflow(
+  ctx: PluginContext,
+  workflowId: string,
+  companyId: string,
+  options?: {
+    createParentIssue?: boolean;
+    parentIssueId?: string;
+  },
+): Promise<{
+  activatedStepIds: string[];
+  parentIssueId: string | null;
+  runId: string;
+  workflowId: string;
+}> {
+  const workflowDefinition = await getWorkflowDefinition(ctx, workflowId);
+  if (!workflowDefinition) {
+    throw new Error(`Workflow definition not found: ${workflowId}`);
+  }
+
+  const typedWorkflowDefinition = toWorkflowDefinitionRecord(workflowDefinition);
+  if (typedWorkflowDefinition.data.companyId !== companyId) {
+    throw new Error(`Workflow does not belong to company: ${workflowId}`);
+  }
+
+  let parentIssueId = typeof options?.parentIssueId === "string" && options.parentIssueId.trim()
+    ? options.parentIssueId.trim()
+    : "";
+  if (!parentIssueId && options?.createParentIssue) {
+    const parentIssue = await ctx.issues.create({
+      companyId,
+      description: typedWorkflowDefinition.data.description || `Workflow run: ${typedWorkflowDefinition.data.name}`,
+      title: `[${typedWorkflowDefinition.data.name}] Workflow run`,
+    });
+    parentIssueId = parentIssue.id;
+  }
+
+  const workflowRun = toWorkflowRunRecord(await createWorkflowRun(ctx, {
+    companyId,
+    parentIssueId: parentIssueId || undefined,
+    startedAt: new Date().toISOString(),
+    status: RUN_STATUSES.running,
+    workflowId: typedWorkflowDefinition.id,
+    workflowName: typedWorkflowDefinition.data.name,
+  }));
+
+  const agents = await ctx.agents.list({ companyId });
+  const agentsByName = new Map<string, AgentRecord>();
+  for (const agent of agents) {
+    agentsByName.set(agent.name, agent);
+  }
+
+  const pendingRootSteps: Array<{ stepDef: WorkflowStep; stepRun: WorkflowStepRunRecord }> = [];
+  for (const stepDef of typedWorkflowDefinition.data.steps) {
+    const agentNameHint = getStepAgentNameHint(stepDef);
+    const matchedAgent = agentNameHint ? agentsByName.get(agentNameHint) ?? null : null;
+    const resolvedAgent = matchedAgent
+      ? { agentId: matchedAgent.id, agentName: matchedAgent.name }
+      : await resolveStepAgent(ctx, companyId, stepDef, agentNameHint ?? undefined);
+
+    if (!resolvedAgent.agentName) {
+      throw new Error(`Unable to resolve step assignee for "${stepDef.id}"`);
+    }
+
+    const stepRun = toWorkflowStepRunRecord(await createStepRun(ctx, companyId, {
+      agentName: resolvedAgent.agentName,
+      retryCount: 0,
+      runId: workflowRun.id,
+      status: STEP_STATUSES.backlog,
+      stepId: stepDef.id,
+    }));
+
+    if (stepDef.dependsOn.length === 0 && stepDef.triggerOn !== "escalation") {
+      pendingRootSteps.push({ stepDef, stepRun });
+    }
+  }
+
+  const activatedStepIds: string[] = [];
+  for (const pending of pendingRootSteps) {
+    await activateBacklogStep(
+      ctx,
+      pending.stepRun,
+      pending.stepDef,
+      typedWorkflowDefinition.data.name,
+      companyId,
+      { parentIssueId: parentIssueId || undefined },
+    );
+    activatedStepIds.push(pending.stepDef.id);
+  }
+
+  ctx.logger.info("Started workflow run", {
+    activatedStepIds,
+    companyId,
+    parentIssueId: parentIssueId || null,
+    runId: workflowRun.id,
+    workflowId: typedWorkflowDefinition.id,
+    workflowName: typedWorkflowDefinition.data.name,
+  });
+
+  return {
+    activatedStepIds,
+    parentIssueId: parentIssueId || null,
+    runId: workflowRun.id,
+    workflowId: typedWorkflowDefinition.id,
+  };
 }
 
 async function advanceWorkflow(
@@ -208,7 +365,7 @@
   stepRunRecord: PluginEntityRecord,
   companyId: string,
 ): Promise<void> {
-  const completedStepRun = toStepRunRecord(stepRunRecord);
+  const completedStepRun = toWorkflowStepRunRecord(stepRunRecord);
   const workflowRun = await getWorkflowRun(ctx, completedStepRun.data.runId);
 
   if (!workflowRun) {
@@ -237,7 +394,7 @@
   }
 
   const typedWorkflowDefinition = toWorkflowDefinitionRecord(workflowDefinition);
-  const stepRuns = (await listStepRuns(ctx, typedWorkflowRun.id, companyId)).map(toStepRunRecord);
+  const stepRuns = (await listStepRuns(ctx, typedWorkflowRun.id, companyId)).map(toWorkflowStepRunRecord);
   const completed = new Set<string>();
   const failed = new Set<string>();
   const skipped = new Set<string>();
@@ -289,7 +446,7 @@
         continue;
       }
 
-      stepRun = toStepRunRecord(await createStepRun(ctx, companyId, {
+      stepRun = toWorkflowStepRunRecord(await createStepRun(ctx, companyId, {
         agentName: resolvedAgent.agentName,
         retryCount: 0,
         runId: typedWorkflowRun.id,
@@ -338,7 +495,7 @@
     throw new Error(`Escalation target step not found: ${escalationTargetId}`);
   }
 
-  const stepRuns = (await listStepRuns(ctx, workflowRun.id, companyId)).map(toStepRunRecord);
+  const stepRuns = (await listStepRuns(ctx, workflowRun.id, companyId)).map(toWorkflowStepRunRecord);
   let escalationStepRun = stepRuns.find((candidate) => candidate.data.stepId === escalationTargetId) ?? null;
   const resolvedAgent = await resolveStepAgent(
     ctx,
@@ -352,7 +509,7 @@
   }
 
   if (!escalationStepRun) {
-    escalationStepRun = toStepRunRecord(await createStepRun(ctx, companyId, {
+    escalationStepRun = toWorkflowStepRunRecord(await createStepRun(ctx, companyId, {
       agentName: resolvedAgent.agentName,
       retryCount: 0,
       runId: workflowRun.id,
@@ -374,14 +531,14 @@
       title: `${workflowRun.data.workflowName}: ${escalationStep.title}`,
     });
 
-    escalationStepRun = toStepRunRecord(await updateStepRun(ctx, escalationStepRun.id, {
+    escalationStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, escalationStepRun.id, {
       issueId: issue.id,
     }));
   }
 
   const shouldActivate = escalationStepRun.data.status === STEP_STATUSES.backlog;
   if (shouldActivate) {
-    escalationStepRun = toStepRunRecord(await updateStepRun(ctx, escalationStepRun.id, {
+    escalationStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, escalationStepRun.id, {
       status: STEP_STATUSES.todo,
     }));
   }
@@ -421,7 +578,7 @@
     return;
   }
 
-  const typedStepRun = toStepRunRecord(stepRunRecord);
+  const typedStepRun = toWorkflowStepRunRecord(stepRunRecord);
   if (TERMINAL_STEP_STATUSES.has(typedStepRun.data.status)) {
     return;
   }
@@ -469,7 +626,7 @@
   if (options.allowRetry) {
     const retryInfo = getRetryInfo(typedWorkflowDefinition.data.steps, typedStepRun.data.stepId);
     if (retryInfo.shouldRetry && typedStepRun.data.retryCount < retryInfo.maxRetries) {
-      const retriedStepRun = toStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
+      const retriedStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
         completedAt: undefined,
         retryCount: typedStepRun.data.retryCount + 1,
         status: STEP_STATUSES.todo,
@@ -496,7 +653,7 @@
   }
 
   if (stepDef.onFailure === "skip") {
-    const skippedStepRun = toStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
+    const skippedStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
       completedAt: new Date().toISOString(),
       status: STEP_STATUSES.skipped,
     }));
@@ -556,7 +713,7 @@
       return;
     }
 
-    const escalatedStepRun = toStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
+    const escalatedStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
       completedAt: new Date().toISOString(),
       status: STEP_STATUSES.escalated,
     }));
@@ -638,7 +795,7 @@
           return;
         }
 
-        const stepRun = toStepRunRecord(stepRunRecord);
+        const stepRun = toWorkflowStepRunRecord(stepRunRecord);
         const payload = event.payload as { status?: string };
         const issueStatus = typeof payload.status === "string" ? payload.status : undefined;
 
@@ -657,7 +814,7 @@
           return;
         }
 
-        const updatedStepRun = toStepRunRecord(await updateStepRun(ctx, stepRun.id, {
+        const updatedStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, stepRun.id, {
           completedAt: nextStepStatus === STEP_STATUSES.done ? new Date().toISOString() : undefined,
           startedAt: nextStepStatus === STEP_STATUSES.inProgress
             ? stepRun.data.startedAt ?? new Date().toISOString()
@@ -807,7 +964,7 @@
             ...typedWorkflowRun.data,
           },
           stepRuns: stepRuns.map((record: PluginEntityRecord) => {
-            const stepRun = toStepRunRecord(record);
+            const stepRun = toWorkflowStepRunRecord(record);
             return {
               id: stepRun.id,
               status: stepRun.status,
diff --git a/paperclip-addon/plugins/workflow-engine/src/workflow-store.ts b/paperclip-addon/plugins/workflow-engine/src/workflow-store.ts
index 5871a2082258cebcfe8b69ebafbb0814cf303d50..3d7fb2b3bc75911cfd9093eb3a4054cc73196bb9
--- a/paperclip-addon/plugins/workflow-engine/src/workflow-store.ts
+++ b/paperclip-addon/plugins/workflow-engine/src/workflow-store.ts
@@ -44,7 +44,14 @@
 
 type TypedEntityRecord<T> = Omit<PluginEntityRecord, "data"> & { data: T };
 
-function toTypedRecord<T>(record: PluginEntityRecord): TypedEntityRecord<T> {
+function toTypedRecord<T>(
+  record: PluginEntityRecord,
+  entityType: string,
+): TypedEntityRecord<T> {
+  if (record.entityType !== entityType) {
+    throw new Error(`Expected entity type "${entityType}", got "${record.entityType}"`);
+  }
+
   return record as TypedEntityRecord<T>;
 }
 
@@ -67,13 +74,26 @@
   id: string,
   entityType: string,
 ): Promise<TypedEntityRecord<T> | null> {
+  const listQuery: Parameters<PluginContext["entities"]["list"]>[0] & { id?: string } = {
+    entityType,
+    id,
+    limit: 1,
+  };
+
+  const listed = await ctx.entities.list(listQuery);
+  const listedRecord = listed.find((record) => record.id === id) ?? null;
+  if (listedRecord) {
+    return toTypedRecord<T>(listedRecord, entityType);
+  }
+
+  // Fallback: some host SDK variants ignore `id` on entities.list().
+  // Keep defensive type verification on direct get() until id-filter support is universal.
   const record = await ctx.entities.get(id);
-
-  if (!record || record.entityType !== entityType) {
+  if (!record) {
     return null;
   }
 
-  return toTypedRecord<T>(record);
+  return toTypedRecord<T>(record, entityType);
 }
 
 async function requireEntityByType<T>(
diff --git a/paperclip-addon/plugins/workflow-engine/src/workflow-utils.ts b/paperclip-addon/plugins/workflow-engine/src/workflow-utils.ts
new file mode 100644
index 0000000000000000000000000000000000000000..ec1cf6f6b9aa722b126b58c0a728b4e538ad1494
--- /dev/null
+++ b/paperclip-addon/plugins/workflow-engine/src/workflow-utils.ts
@@ -0,0 +1,62 @@
+import type { PluginEntityRecord } from "@paperclipai/plugin-sdk";
+
+import { STEP_STATUSES } from "./constants.js";
+import type { WorkflowStep } from "./dag-engine.js";
+import type {
+  WorkflowDefinition,
+  WorkflowRun,
+  WorkflowStepRun,
+} from "./workflow-store.js";
+
+export type WorkflowRunRecord = PluginEntityRecord & { data: WorkflowRun };
+export type WorkflowStepRunRecord = PluginEntityRecord & { data: WorkflowStepRun };
+export type WorkflowDefinitionRecord = PluginEntityRecord & { data: WorkflowDefinition };
+type StepAgentMetadata = WorkflowStep & {
+  agent?: string;
+  agentName?: string;
+  assigneeAgentName?: string;
+};
+
+export const TERMINAL_STEP_STATUSES = new Set<string>([
+  STEP_STATUSES.done,
+  STEP_STATUSES.failed,
+  STEP_STATUSES.skipped,
+  STEP_STATUSES.escalated,
+]);
+
+export function toWorkflowRunRecord(record: PluginEntityRecord): WorkflowRunRecord {
+  return record as WorkflowRunRecord;
+}
+
+export function toWorkflowStepRunRecord(record: PluginEntityRecord): WorkflowStepRunRecord {
+  return record as WorkflowStepRunRecord;
+}
+
+export function toWorkflowDefinitionRecord(record: PluginEntityRecord): WorkflowDefinitionRecord {
+  return record as WorkflowDefinitionRecord;
+}
+
+export function findStepDefinition(definition: WorkflowDefinitionRecord, stepId: string): WorkflowStep | null {
+  return definition.data.steps.find((step: WorkflowStep) => step.id === stepId) ?? null;
+}
+
+export function getStepAgentNameHint(stepDef: WorkflowStep): string | null {
+  const stepMeta = stepDef as StepAgentMetadata;
+  const candidates = [stepMeta.agentName, stepMeta.agent, stepMeta.assigneeAgentName];
+
+  for (const candidate of candidates) {
+    if (typeof candidate === "string" && candidate.trim()) {
+      return candidate.trim();
+    }
+  }
+
+  return null;
+}
+
+export function getStepAgentName(stepRun: WorkflowStepRunRecord, stepDef: WorkflowStep): string | null {
+  if (typeof stepRun.data.agentName === "string" && stepRun.data.agentName.trim()) {
+    return stepRun.data.agentName.trim();
+  }
+
+  return getStepAgentNameHint(stepDef);
+}

codex
`startWorkflow`와 backlog 이슈 생성/세션 분기 로직을 넣었습니다. 지금 남은 호출부 시그니처(`invokeAgentForStep` 인자, 타입 참조, data handler 등록, issue.created 멱등 처리)를 정리하고 빌드 확인하겠습니다.
exec
/bin/zsh -lc "nl -ba plugins/workflow-engine/src/worker.ts | sed -n '620,980p'" in /Users/kwak/Projects/paperclip/paperclip-addonexec
/bin/zsh -lc "nl -ba plugins/workflow-engine/src/worker.ts | sed -n '300,620p'" in /Users/kwak/Projects/paperclip/paperclip-addon succeeded in 0ms:
   620	      runId: typedWorkflowRun.id,
   621	      stepId: typedStepRun.data.stepId,
   622	    });
   623	    return;
   624	  }
   625	
   626	  if (options.allowRetry) {
   627	    const retryInfo = getRetryInfo(typedWorkflowDefinition.data.steps, typedStepRun.data.stepId);
   628	    if (retryInfo.shouldRetry && typedStepRun.data.retryCount < retryInfo.maxRetries) {
   629	      const retriedStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
   630	        completedAt: undefined,
   631	        retryCount: typedStepRun.data.retryCount + 1,
   632	        status: STEP_STATUSES.todo,
   633	      }));
   634	
   635	      await invokeAgentForStep(
   636	        ctx,
   637	        stepDef,
   638	        retriedStepRun.data.agentName,
   639	        typedWorkflowRun.data.workflowName,
 succeeded in 0ms:
   300	  }));
   301	
   302	  const agents = await ctx.agents.list({ companyId });
   303	  const agentsByName = new Map<string, AgentRecord>();
   304	  for (const agent of agents) {
   305	    agentsByName.set(agent.name, agent);
   306	  }
   307	
   308	  const pendingRootSteps: Array<{ stepDef: WorkflowStep; stepRun: WorkflowStepRunRecord }> = [];
   309	  for (const stepDef of typedWorkflowDefinition.data.steps) {
   310	    const agentNameHint = getStepAgentNameHint(stepDef);
   311	    const matchedAgent = agentNameHint ? agentsByName.get(agentNameHint) ?? null : null;
   312	    const resolvedAgent = matchedAgent
   313	      ? { agentId: matchedAgent.id, agentName: matchedAgent.name }
   314	      : await resolveStepAgent(ctx, companyId, stepDef, agentNameHint ?? undefined);
   315	
   316	    if (!resolvedAgent.agentName) {
   317	      throw new Error(`Unable to resolve step assignee for "${stepDef.id}"`);
   318	    }
   319	
file update:
diff --git a/paperclip-addon/plugins/workflow-engine/src/dag-engine.ts b/paperclip-addon/plugins/workflow-engine/src/dag-engine.ts
index b59510f0473171038be77277286d3b5c2063e6c5..e7f21ff998cab78b9325a8aeacf4fda9c5e06553
--- a/paperclip-addon/plugins/workflow-engine/src/dag-engine.ts
+++ b/paperclip-addon/plugins/workflow-engine/src/dag-engine.ts
@@ -2,6 +2,7 @@
   id: string;
   title: string;
   dependsOn: string[];
+  sessionMode?: "fresh" | "reuse";
   onFailure?: "retry" | "skip" | "abort_workflow" | "escalate";
   escalateTo?: string;
   maxRetries?: number;
diff --git a/paperclip-addon/plugins/workflow-engine/src/reconciler.ts b/paperclip-addon/plugins/workflow-engine/src/reconciler.ts
index 14ab4e766a835d145a6c2e7b1b564270285cbdc3..d2ab54817bd8185944379d2188bfbdef1722020a
--- a/paperclip-addon/plugins/workflow-engine/src/reconciler.ts
+++ b/paperclip-addon/plugins/workflow-engine/src/reconciler.ts
@@ -8,65 +8,22 @@
   listStepRuns,
   updateStepRun,
   updateWorkflowRun,
-  type WorkflowDefinition,
-  type WorkflowRun,
-  type WorkflowStepRun,
 } from "./workflow-store.js";
 import type { WorkflowStep } from "./dag-engine.js";
-
-type WorkflowRunRecord = PluginEntityRecord & { data: WorkflowRun };
-type WorkflowStepRunRecord = PluginEntityRecord & { data: WorkflowStepRun };
-type WorkflowDefinitionRecord = PluginEntityRecord & { data: WorkflowDefinition };
-type StepAgentMetadata = WorkflowStep & {
-  agent?: string;
-  agentName?: string;
-  assigneeAgentName?: string;
-};
+import {
+  TERMINAL_STEP_STATUSES,
+  findStepDefinition,
+  getStepAgentName,
+  toWorkflowDefinitionRecord,
+  toWorkflowRunRecord,
+  toWorkflowStepRunRecord,
+} from "./workflow-utils.js";
 
 const STALE_THRESHOLD_MS = 300_000;
-const TERMINAL_STEP_STATUSES = new Set<string>([
-  STEP_STATUSES.done,
-  STEP_STATUSES.failed,
-  STEP_STATUSES.skipped,
-  STEP_STATUSES.escalated,
-]);
 
 function isStale(record: PluginEntityRecord, thresholdMs: number): boolean {
   const updatedAt = new Date(record.updatedAt).getTime();
   return Date.now() - updatedAt > thresholdMs;
-}
-
-function toWorkflowRunRecord(record: PluginEntityRecord): WorkflowRunRecord {
-  return record as WorkflowRunRecord;
-}
-
-function toWorkflowStepRunRecord(record: PluginEntityRecord): WorkflowStepRunRecord {
-  return record as WorkflowStepRunRecord;
-}
-
-function toWorkflowDefinitionRecord(record: PluginEntityRecord): WorkflowDefinitionRecord {
-  return record as WorkflowDefinitionRecord;
-}
-
-function findStepDefinition(definition: WorkflowDefinitionRecord, stepId: string): WorkflowStep | null {
-  return definition.data.steps.find((step: WorkflowStep) => step.id === stepId) ?? null;
-}
-
-function getStepAgentName(stepRun: WorkflowStepRunRecord, stepDef: WorkflowStep): string | null {
-  if (typeof stepRun.data.agentName === "string" && stepRun.data.agentName.trim()) {
-    return stepRun.data.agentName.trim();
-  }
-
-  const stepMeta = stepDef as StepAgentMetadata;
-  const candidates = [stepMeta.agentName, stepMeta.agent, stepMeta.assigneeAgentName];
-
-  for (const candidate of candidates) {
-    if (typeof candidate === "string" && candidate.trim()) {
-      return candidate.trim();
-    }
-  }
-
-  return null;
 }
 
 async function invokeAgentByName(
diff --git a/paperclip-addon/plugins/workflow-engine/src/worker.ts b/paperclip-addon/plugins/workflow-engine/src/worker.ts
index c5ac3fbb4ca7f767eb29331bd3630b635449b335..0618dc61f481ff717d3dfc0edc4adabaab9e5d19
--- a/paperclip-addon/plugins/workflow-engine/src/worker.ts
+++ b/paperclip-addon/plugins/workflow-engine/src/worker.ts
@@ -15,6 +15,7 @@
 } from "./dag-engine.js";
 import {
   checkIdempotency,
+  createWorkflowRun,
   createStepRun,
   findStepRunByIssueId,
   getWorkflowDefinition,
@@ -25,32 +26,30 @@
   markIdempotency,
   updateStepRun,
   updateWorkflowRun,
-  type WorkflowDefinition,
-  type WorkflowRun,
   type WorkflowStepRun,
 } from "./workflow-store.js";
+import {
+  TERMINAL_STEP_STATUSES,
+  findStepDefinition,
+  getStepAgentName,
+  getStepAgentNameHint,
+  toWorkflowDefinitionRecord,
+  toWorkflowRunRecord,
+  toWorkflowStepRunRecord,
+  type WorkflowDefinitionRecord,
+  type WorkflowRunRecord,
+  type WorkflowStepRunRecord,
+} from "./workflow-utils.js";
 
-type WorkflowStepRecord = PluginEntityRecord & { data: WorkflowStepRun };
-type WorkflowRunRecord = PluginEntityRecord & { data: WorkflowRun };
-type WorkflowDefinitionRecord = PluginEntityRecord & { data: WorkflowDefinition };
 type WorkflowStepMetadata = WorkflowStep & {
   description?: string;
-  agent?: string;
-  agentName?: string;
-  assigneeAgentName?: string;
   assigneeAgentId?: string;
+  sessionMode?: "fresh" | "reuse";
 };
 type AgentRecord = Awaited<ReturnType<PluginContext["agents"]["list"]>>[number];
 type ReconcilerModule = {
   reconcileStuckSteps?: (ctx: PluginContext) => Promise<void>;
 };
-
-const TERMINAL_STEP_STATUSES = new Set<string>([
-  STEP_STATUSES.done,
-  STEP_STATUSES.failed,
-  STEP_STATUSES.skipped,
-  STEP_STATUSES.escalated,
-]);
 
 function summarizeError(error: unknown): string {
   if (error instanceof Error) {
@@ -60,28 +59,12 @@
   return String(error);
 }
 
-function toStepRunRecord(record: PluginEntityRecord): WorkflowStepRecord {
-  return record as WorkflowStepRecord;
-}
-
-function toWorkflowRunRecord(record: PluginEntityRecord): WorkflowRunRecord {
-  return record as WorkflowRunRecord;
-}
-
-function toWorkflowDefinitionRecord(record: PluginEntityRecord): WorkflowDefinitionRecord {
-  return record as WorkflowDefinitionRecord;
-}
-
 function buildIdempotencyKey(event: PluginEvent): string {
   return `${event.eventType}:${event.eventId}`;
 }
 
 function getStepMetadata(stepDef: WorkflowStep): WorkflowStepMetadata {
   return stepDef as WorkflowStepMetadata;
-}
-
-function findStepDefinition(definition: WorkflowDefinitionRecord, stepId: string): WorkflowStep | null {
-  return definition.data.steps.find((step: WorkflowStep) => step.id === stepId) ?? null;
 }
 
 function getStepDescription(stepDef: WorkflowStep): string | undefined {
@@ -89,19 +72,6 @@
   return typeof description === "string" && description.trim() ? description.trim() : undefined;
 }
 
-function getStepAgentNameHint(stepDef: WorkflowStep): string | null {
-  const meta = getStepMetadata(stepDef);
-  const candidates = [meta.agentName, meta.agent, meta.assigneeAgentName];
-
-  for (const candidate of candidates) {
-    if (typeof candidate === "string" && candidate.trim()) {
-      return candidate.trim();
-    }
-  }
-
-  return null;
-}
-
 function getStepAgentIdHint(stepDef: WorkflowStep): string | null {
   const assigneeAgentId = getStepMetadata(stepDef).assigneeAgentId;
   if (typeof assigneeAgentId !== "string" || !assigneeAgentId.trim()) {
@@ -147,60 +117,247 @@
 
 async function invokeAgentForStep(
   ctx: PluginContext,
+  stepRunRecord: WorkflowStepRunRecord,
   stepDef: WorkflowStep,
-  agentName: string,
   workflowName: string,
   companyId: string,
-): Promise<void> {
+): Promise<WorkflowStepRunRecord> {
+  const agentName = getStepAgentName(stepRunRecord, stepDef);
+  if (!agentName) {
+    ctx.logger.warn("Agent name is missing for workflow step", {
+      companyId,
+      runId: stepRunRecord.data.runId,
+      stepId: stepDef.id,
+      workflowName,
+    });
+    return stepRunRecord;
+  }
+
   const agents = await ctx.agents.list({ companyId });
-  const agent = agents.find((candidate: AgentRecord) => candidate.name === agentName);
+  const agent = agents.find((candidate: AgentRecord) => candidate.name === agentName) ?? null;
 
   if (!agent) {
     ctx.logger.warn("Agent not found for step", {
       agentName,
       companyId,
+      runId: stepRunRecord.data.runId,
+      stepId: stepDef.id,
+      workflowName,
+    });
+    return stepRunRecord;
+  }
+
+  const prompt = `workflow:${workflowName}/step:${stepDef.id} — "${stepDef.title}" is ready. Please proceed with the assigned task.`;
+  const reason = `workflow:${workflowName}/step:${stepDef.id}`;
+  const sessionMode = getStepMetadata(stepDef).sessionMode === "reuse" ? "reuse" : "fresh";
+
+  if (sessionMode === "reuse") {
+    let sessionId = typeof stepRunRecord.data.sessionId === "string" && stepRunRecord.data.sessionId.trim()
+      ? stepRunRecord.data.sessionId.trim()
+      : "";
+
+    if (!sessionId) {
+      const session = await ctx.agents.sessions.create(agent.id, companyId, {
+        reason,
+      });
+      sessionId = session.sessionId;
+      stepRunRecord = toWorkflowStepRunRecord(await updateStepRun(ctx, stepRunRecord.id, {
+        sessionId,
+      }));
+    }
+
+    await ctx.agents.sessions.sendMessage(sessionId, companyId, {
+      prompt,
+      reason,
+    });
+
+    ctx.logger.info("Sent workflow step prompt via agent session", {
+      agentId: agent.id,
+      agentName,
+      companyId,
+      runId: stepRunRecord.data.runId,
+      sessionId,
       stepId: stepDef.id,
       workflowName,
     });
-    return;
+    return stepRunRecord;
   }
 
   await ctx.agents.invoke(agent.id, companyId, {
-    prompt: `workflow:${workflowName}/step:${stepDef.id} — "${stepDef.title}" is ready. Please proceed with the assigned task.`,
-    reason: `workflow:${workflowName}/step:${stepDef.id}`,
+    prompt,
+    reason,
   });
 
   ctx.logger.info("Invoked agent for workflow step", {
     agentId: agent.id,
     agentName,
     companyId,
+    runId: stepRunRecord.data.runId,
     stepId: stepDef.id,
     workflowName,
   });
+
+  return stepRunRecord;
 }
 
 async function activateBacklogStep(
   ctx: PluginContext,
-  stepRunRecord: WorkflowStepRecord,
+  stepRunRecord: WorkflowStepRunRecord,
   stepDef: WorkflowStep,
   workflowName: string,
   companyId: string,
-): Promise<void> {
+  options?: { parentIssueId?: string },
+): Promise<WorkflowStepRunRecord> {
   if (stepRunRecord.data.status !== STEP_STATUSES.backlog) {
-    return;
+    return stepRunRecord;
   }
 
-  const updated = toStepRunRecord(await updateStepRun(ctx, stepRunRecord.id, {
+  const resolvedAgent = await resolveStepAgent(
+    ctx,
+    companyId,
+    stepDef,
+    stepRunRecord.data.agentName,
+  );
+
+  let issueId = typeof stepRunRecord.data.issueId === "string" && stepRunRecord.data.issueId.trim()
+    ? stepRunRecord.data.issueId.trim()
+    : "";
+  const stepDescription = getStepDescription(stepDef) ?? `Workflow step: ${stepDef.id}`;
+
+  if (!issueId) {
+    const issue = await ctx.issues.create({
+      assigneeAgentId: resolvedAgent.agentId ?? undefined,
+      companyId,
+      description: stepDescription,
+      parentId: options?.parentIssueId,
+      title: `[${workflowName}] ${stepDef.title}`,
+    });
+    issueId = issue.id;
+  }
+
+  const nextStartedAt = stepRunRecord.data.startedAt ?? new Date().toISOString();
+  let updatedStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, stepRunRecord.id, {
+    agentName: resolvedAgent.agentName ?? stepRunRecord.data.agentName,
+    issueId,
+    startedAt: nextStartedAt,
     status: STEP_STATUSES.todo,
   }));
 
-  await invokeAgentForStep(
+  updatedStepRun = await invokeAgentForStep(
     ctx,
+    updatedStepRun,
     stepDef,
-    updated.data.agentName,
     workflowName,
     companyId,
   );
+
+  return updatedStepRun;
+}
+
+async function startWorkflow(
+  ctx: PluginContext,
+  workflowId: string,
+  companyId: string,
+  options?: {
+    createParentIssue?: boolean;
+    parentIssueId?: string;
+  },
+): Promise<{
+  activatedStepIds: string[];
+  parentIssueId: string | null;
+  runId: string;
+  workflowId: string;
+}> {
+  const workflowDefinition = await getWorkflowDefinition(ctx, workflowId);
+  if (!workflowDefinition) {
+    throw new Error(`Workflow definition not found: ${workflowId}`);
+  }
+
+  const typedWorkflowDefinition = toWorkflowDefinitionRecord(workflowDefinition);
+  if (typedWorkflowDefinition.data.companyId !== companyId) {
+    throw new Error(`Workflow does not belong to company: ${workflowId}`);
+  }
+
+  let parentIssueId = typeof options?.parentIssueId === "string" && options.parentIssueId.trim()
+    ? options.parentIssueId.trim()
+    : "";
+  if (!parentIssueId && options?.createParentIssue) {
+    const parentIssue = await ctx.issues.create({
+      companyId,
+      description: typedWorkflowDefinition.data.description || `Workflow run: ${typedWorkflowDefinition.data.name}`,
+      title: `[${typedWorkflowDefinition.data.name}] Workflow run`,
+    });
+    parentIssueId = parentIssue.id;
+  }
+
+  const workflowRun = toWorkflowRunRecord(await createWorkflowRun(ctx, {
+    companyId,
+    parentIssueId: parentIssueId || undefined,
+    startedAt: new Date().toISOString(),
+    status: RUN_STATUSES.running,
+    workflowId: typedWorkflowDefinition.id,
+    workflowName: typedWorkflowDefinition.data.name,
+  }));
+
+  const agents = await ctx.agents.list({ companyId });
+  const agentsByName = new Map<string, AgentRecord>();
+  for (const agent of agents) {
+    agentsByName.set(agent.name, agent);
+  }
+
+  const pendingRootSteps: Array<{ stepDef: WorkflowStep; stepRun: WorkflowStepRunRecord }> = [];
+  for (const stepDef of typedWorkflowDefinition.data.steps) {
+    const agentNameHint = getStepAgentNameHint(stepDef);
+    const matchedAgent = agentNameHint ? agentsByName.get(agentNameHint) ?? null : null;
+    const resolvedAgent = matchedAgent
+      ? { agentId: matchedAgent.id, agentName: matchedAgent.name }
+      : await resolveStepAgent(ctx, companyId, stepDef, agentNameHint ?? undefined);
+
+    if (!resolvedAgent.agentName) {
+      throw new Error(`Unable to resolve step assignee for "${stepDef.id}"`);
+    }
+
+    const stepRun = toWorkflowStepRunRecord(await createStepRun(ctx, companyId, {
+      agentName: resolvedAgent.agentName,
+      retryCount: 0,
+      runId: workflowRun.id,
+      status: STEP_STATUSES.backlog,
+      stepId: stepDef.id,
+    }));
+
+    if (stepDef.dependsOn.length === 0 && stepDef.triggerOn !== "escalation") {
+      pendingRootSteps.push({ stepDef, stepRun });
+    }
+  }
+
+  const activatedStepIds: string[] = [];
+  for (const pending of pendingRootSteps) {
+    await activateBacklogStep(
+      ctx,
+      pending.stepRun,
+      pending.stepDef,
+      typedWorkflowDefinition.data.name,
+      companyId,
+      { parentIssueId: parentIssueId || undefined },
+    );
+    activatedStepIds.push(pending.stepDef.id);
+  }
+
+  ctx.logger.info("Started workflow run", {
+    activatedStepIds,
+    companyId,
+    parentIssueId: parentIssueId || null,
+    runId: workflowRun.id,
+    workflowId: typedWorkflowDefinition.id,
+    workflowName: typedWorkflowDefinition.data.name,
+  });
+
+  return {
+    activatedStepIds,
+    parentIssueId: parentIssueId || null,
+    runId: workflowRun.id,
+    workflowId: typedWorkflowDefinition.id,
+  };
 }
 
 async function advanceWorkflow(
@@ -208,7 +365,7 @@
   stepRunRecord: PluginEntityRecord,
   companyId: string,
 ): Promise<void> {
-  const completedStepRun = toStepRunRecord(stepRunRecord);
+  const completedStepRun = toWorkflowStepRunRecord(stepRunRecord);
   const workflowRun = await getWorkflowRun(ctx, completedStepRun.data.runId);
 
   if (!workflowRun) {
@@ -237,7 +394,7 @@
   }
 
   const typedWorkflowDefinition = toWorkflowDefinitionRecord(workflowDefinition);
-  const stepRuns = (await listStepRuns(ctx, typedWorkflowRun.id, companyId)).map(toStepRunRecord);
+  const stepRuns = (await listStepRuns(ctx, typedWorkflowRun.id, companyId)).map(toWorkflowStepRunRecord);
   const completed = new Set<string>();
   const failed = new Set<string>();
   const skipped = new Set<string>();
@@ -289,7 +446,7 @@
         continue;
       }
 
-      stepRun = toStepRunRecord(await createStepRun(ctx, companyId, {
+      stepRun = toWorkflowStepRunRecord(await createStepRun(ctx, companyId, {
         agentName: resolvedAgent.agentName,
         retryCount: 0,
         runId: typedWorkflowRun.id,
@@ -338,7 +495,7 @@
     throw new Error(`Escalation target step not found: ${escalationTargetId}`);
   }
 
-  const stepRuns = (await listStepRuns(ctx, workflowRun.id, companyId)).map(toStepRunRecord);
+  const stepRuns = (await listStepRuns(ctx, workflowRun.id, companyId)).map(toWorkflowStepRunRecord);
   let escalationStepRun = stepRuns.find((candidate) => candidate.data.stepId === escalationTargetId) ?? null;
   const resolvedAgent = await resolveStepAgent(
     ctx,
@@ -352,7 +509,7 @@
   }
 
   if (!escalationStepRun) {
-    escalationStepRun = toStepRunRecord(await createStepRun(ctx, companyId, {
+    escalationStepRun = toWorkflowStepRunRecord(await createStepRun(ctx, companyId, {
       agentName: resolvedAgent.agentName,
       retryCount: 0,
       runId: workflowRun.id,
@@ -374,14 +531,14 @@
       title: `${workflowRun.data.workflowName}: ${escalationStep.title}`,
     });
 
-    escalationStepRun = toStepRunRecord(await updateStepRun(ctx, escalationStepRun.id, {
+    escalationStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, escalationStepRun.id, {
       issueId: issue.id,
     }));
   }
 
   const shouldActivate = escalationStepRun.data.status === STEP_STATUSES.backlog;
   if (shouldActivate) {
-    escalationStepRun = toStepRunRecord(await updateStepRun(ctx, escalationStepRun.id, {
+    escalationStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, escalationStepRun.id, {
       status: STEP_STATUSES.todo,
     }));
   }
@@ -421,7 +578,7 @@
     return;
   }
 
-  const typedStepRun = toStepRunRecord(stepRunRecord);
+  const typedStepRun = toWorkflowStepRunRecord(stepRunRecord);
   if (TERMINAL_STEP_STATUSES.has(typedStepRun.data.status)) {
     return;
   }
@@ -469,7 +626,7 @@
   if (options.allowRetry) {
     const retryInfo = getRetryInfo(typedWorkflowDefinition.data.steps, typedStepRun.data.stepId);
     if (retryInfo.shouldRetry && typedStepRun.data.retryCount < retryInfo.maxRetries) {
-      const retriedStepRun = toStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
+      const retriedStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
         completedAt: undefined,
         retryCount: typedStepRun.data.retryCount + 1,
         status: STEP_STATUSES.todo,
@@ -496,7 +653,7 @@
   }
 
   if (stepDef.onFailure === "skip") {
-    const skippedStepRun = toStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
+    const skippedStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
       completedAt: new Date().toISOString(),
       status: STEP_STATUSES.skipped,
     }));
@@ -556,7 +713,7 @@
       return;
     }
 
-    const escalatedStepRun = toStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
+    const escalatedStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
       completedAt: new Date().toISOString(),
       status: STEP_STATUSES.escalated,
     }));
@@ -638,7 +795,7 @@
           return;
         }
 
-        const stepRun = toStepRunRecord(stepRunRecord);
+        const stepRun = toWorkflowStepRunRecord(stepRunRecord);
         const payload = event.payload as { status?: string };
         const issueStatus = typeof payload.status === "string" ? payload.status : undefined;
 
@@ -657,7 +814,7 @@
           return;
         }
 
-        const updatedStepRun = toStepRunRecord(await updateStepRun(ctx, stepRun.id, {
+        const updatedStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, stepRun.id, {
           completedAt: nextStepStatus === STEP_STATUSES.done ? new Date().toISOString() : undefined,
           startedAt: nextStepStatus === STEP_STATUSES.inProgress
             ? stepRun.data.startedAt ?? new Date().toISOString()
@@ -807,7 +964,7 @@
             ...typedWorkflowRun.data,
           },
           stepRuns: stepRuns.map((record: PluginEntityRecord) => {
-            const stepRun = toStepRunRecord(record);
+            const stepRun = toWorkflowStepRunRecord(record);
             return {
               id: stepRun.id,
               status: stepRun.status,
diff --git a/paperclip-addon/plugins/workflow-engine/src/workflow-store.ts b/paperclip-addon/plugins/workflow-engine/src/workflow-store.ts
index 5871a2082258cebcfe8b69ebafbb0814cf303d50..3d7fb2b3bc75911cfd9093eb3a4054cc73196bb9
--- a/paperclip-addon/plugins/workflow-engine/src/workflow-store.ts
+++ b/paperclip-addon/plugins/workflow-engine/src/workflow-store.ts
@@ -44,7 +44,14 @@
 
 type TypedEntityRecord<T> = Omit<PluginEntityRecord, "data"> & { data: T };
 
-function toTypedRecord<T>(record: PluginEntityRecord): TypedEntityRecord<T> {
+function toTypedRecord<T>(
+  record: PluginEntityRecord,
+  entityType: string,
+): TypedEntityRecord<T> {
+  if (record.entityType !== entityType) {
+    throw new Error(`Expected entity type "${entityType}", got "${record.entityType}"`);
+  }
+
   return record as TypedEntityRecord<T>;
 }
 
@@ -67,13 +74,26 @@
   id: string,
   entityType: string,
 ): Promise<TypedEntityRecord<T> | null> {
+  const listQuery: Parameters<PluginContext["entities"]["list"]>[0] & { id?: string } = {
+    entityType,
+    id,
+    limit: 1,
+  };
+
+  const listed = await ctx.entities.list(listQuery);
+  const listedRecord = listed.find((record) => record.id === id) ?? null;
+  if (listedRecord) {
+    return toTypedRecord<T>(listedRecord, entityType);
+  }
+
+  // Fallback: some host SDK variants ignore `id` on entities.list().
+  // Keep defensive type verification on direct get() until id-filter support is universal.
   const record = await ctx.entities.get(id);
-
-  if (!record || record.entityType !== entityType) {
+  if (!record) {
     return null;
   }
 
-  return toTypedRecord<T>(record);
+  return toTypedRecord<T>(record, entityType);
 }
 
 async function requireEntityByType<T>(
diff --git a/paperclip-addon/plugins/workflow-engine/src/workflow-utils.ts b/paperclip-addon/plugins/workflow-engine/src/workflow-utils.ts
new file mode 100644
index 0000000000000000000000000000000000000000..ec1cf6f6b9aa722b126b58c0a728b4e538ad1494
--- /dev/null
+++ b/paperclip-addon/plugins/workflow-engine/src/workflow-utils.ts
@@ -0,0 +1,62 @@
+import type { PluginEntityRecord } from "@paperclipai/plugin-sdk";
+
+import { STEP_STATUSES } from "./constants.js";
+import type { WorkflowStep } from "./dag-engine.js";
+import type {
+  WorkflowDefinition,
+  WorkflowRun,
+  WorkflowStepRun,
+} from "./workflow-store.js";
+
+export type WorkflowRunRecord = PluginEntityRecord & { data: WorkflowRun };
+export type WorkflowStepRunRecord = PluginEntityRecord & { data: WorkflowStepRun };
+export type WorkflowDefinitionRecord = PluginEntityRecord & { data: WorkflowDefinition };
+type StepAgentMetadata = WorkflowStep & {
+  agent?: string;
+  agentName?: string;
+  assigneeAgentName?: string;
+};
+
+export const TERMINAL_STEP_STATUSES = new Set<string>([
+  STEP_STATUSES.done,
+  STEP_STATUSES.failed,
+  STEP_STATUSES.skipped,
+  STEP_STATUSES.escalated,
+]);
+
+export function toWorkflowRunRecord(record: PluginEntityRecord): WorkflowRunRecord {
+  return record as WorkflowRunRecord;
+}
+
+export function toWorkflowStepRunRecord(record: PluginEntityRecord): WorkflowStepRunRecord {
+  return record as WorkflowStepRunRecord;
+}
+
+export function toWorkflowDefinitionRecord(record: PluginEntityRecord): WorkflowDefinitionRecord {
+  return record as WorkflowDefinitionRecord;
+}
+
+export function findStepDefinition(definition: WorkflowDefinitionRecord, stepId: string): WorkflowStep | null {
+  return definition.data.steps.find((step: WorkflowStep) => step.id === stepId) ?? null;
+}
+
+export function getStepAgentNameHint(stepDef: WorkflowStep): string | null {
+  const stepMeta = stepDef as StepAgentMetadata;
+  const candidates = [stepMeta.agentName, stepMeta.agent, stepMeta.assigneeAgentName];
+
+  for (const candidate of candidates) {
+    if (typeof candidate === "string" && candidate.trim()) {
+      return candidate.trim();
+    }
+  }
+
+  return null;
+}
+
+export function getStepAgentName(stepRun: WorkflowStepRunRecord, stepDef: WorkflowStep): string | null {
+  if (typeof stepRun.data.agentName === "string" && stepRun.data.agentName.trim()) {
+    return stepRun.data.agentName.trim();
+  }
+
+  return getStepAgentNameHint(stepDef);
+}

file update
M /Users/kwak/Projects/paperclip/paperclip-addon/plugins/workflow-engine/src/worker.ts
@@ -464,2 +464,3 @@
       companyId,
+      { parentIssueId: typedWorkflowRun.data.parentIssueId },
     );
@@ -488,3 +489,3 @@
   workflowDefinition: WorkflowDefinitionRecord,
-  sourceStepRun: WorkflowStepRecord,
+  sourceStepRun: WorkflowStepRunRecord,
   escalationTargetId: string,
@@ -548,4 +549,4 @@
       ctx,
+      escalationStepRun,
       escalationStep,
-      escalationStepRun.data.agentName,
       workflowRun.data.workflowName,
@@ -636,4 +637,4 @@
         ctx,
+        retriedStepRun,
         stepDef,
-        retriedStepRun.data.agentName,
         typedWorkflowRun.data.workflowName,
apply_patch(auto_approved=true) exited 0 in 112ms:
Success. Updated the following files:
M /Users/kwak/Projects/paperclip/paperclip-addon/plugins/workflow-engine/src/worker.ts
file update:
diff --git a/paperclip-addon/plugins/workflow-engine/src/dag-engine.ts b/paperclip-addon/plugins/workflow-engine/src/dag-engine.ts
index b59510f0473171038be77277286d3b5c2063e6c5..e7f21ff998cab78b9325a8aeacf4fda9c5e06553
--- a/paperclip-addon/plugins/workflow-engine/src/dag-engine.ts
+++ b/paperclip-addon/plugins/workflow-engine/src/dag-engine.ts
@@ -2,6 +2,7 @@
   id: string;
   title: string;
   dependsOn: string[];
+  sessionMode?: "fresh" | "reuse";
   onFailure?: "retry" | "skip" | "abort_workflow" | "escalate";
   escalateTo?: string;
   maxRetries?: number;
diff --git a/paperclip-addon/plugins/workflow-engine/src/reconciler.ts b/paperclip-addon/plugins/workflow-engine/src/reconciler.ts
index 14ab4e766a835d145a6c2e7b1b564270285cbdc3..d2ab54817bd8185944379d2188bfbdef1722020a
--- a/paperclip-addon/plugins/workflow-engine/src/reconciler.ts
+++ b/paperclip-addon/plugins/workflow-engine/src/reconciler.ts
@@ -8,65 +8,22 @@
   listStepRuns,
   updateStepRun,
   updateWorkflowRun,
-  type WorkflowDefinition,
-  type WorkflowRun,
-  type WorkflowStepRun,
 } from "./workflow-store.js";
 import type { WorkflowStep } from "./dag-engine.js";
-
-type WorkflowRunRecord = PluginEntityRecord & { data: WorkflowRun };
-type WorkflowStepRunRecord = PluginEntityRecord & { data: WorkflowStepRun };
-type WorkflowDefinitionRecord = PluginEntityRecord & { data: WorkflowDefinition };
-type StepAgentMetadata = WorkflowStep & {
-  agent?: string;
-  agentName?: string;
-  assigneeAgentName?: string;
-};
+import {
+  TERMINAL_STEP_STATUSES,
+  findStepDefinition,
+  getStepAgentName,
+  toWorkflowDefinitionRecord,
+  toWorkflowRunRecord,
+  toWorkflowStepRunRecord,
+} from "./workflow-utils.js";
 
 const STALE_THRESHOLD_MS = 300_000;
-const TERMINAL_STEP_STATUSES = new Set<string>([
-  STEP_STATUSES.done,
-  STEP_STATUSES.failed,
-  STEP_STATUSES.skipped,
-  STEP_STATUSES.escalated,
-]);
 
 function isStale(record: PluginEntityRecord, thresholdMs: number): boolean {
   const updatedAt = new Date(record.updatedAt).getTime();
   return Date.now() - updatedAt > thresholdMs;
-}
-
-function toWorkflowRunRecord(record: PluginEntityRecord): WorkflowRunRecord {
-  return record as WorkflowRunRecord;
-}
-
-function toWorkflowStepRunRecord(record: PluginEntityRecord): WorkflowStepRunRecord {
-  return record as WorkflowStepRunRecord;
-}
-
-function toWorkflowDefinitionRecord(record: PluginEntityRecord): WorkflowDefinitionRecord {
-  return record as WorkflowDefinitionRecord;
-}
-
-function findStepDefinition(definition: WorkflowDefinitionRecord, stepId: string): WorkflowStep | null {
-  return definition.data.steps.find((step: WorkflowStep) => step.id === stepId) ?? null;
-}
-
-function getStepAgentName(stepRun: WorkflowStepRunRecord, stepDef: WorkflowStep): string | null {
-  if (typeof stepRun.data.agentName === "string" && stepRun.data.agentName.trim()) {
-    return stepRun.data.agentName.trim();
-  }
-
-  const stepMeta = stepDef as StepAgentMetadata;
-  const candidates = [stepMeta.agentName, stepMeta.agent, stepMeta.assigneeAgentName];
-
-  for (const candidate of candidates) {
-    if (typeof candidate === "string" && candidate.trim()) {
-      return candidate.trim();
-    }
-  }
-
-  return null;
 }
 
 async function invokeAgentByName(
diff --git a/paperclip-addon/plugins/workflow-engine/src/worker.ts b/paperclip-addon/plugins/workflow-engine/src/worker.ts
index c5ac3fbb4ca7f767eb29331bd3630b635449b335..7921b6a88326f9a7d8eb0492f204ae84295e6f1d
--- a/paperclip-addon/plugins/workflow-engine/src/worker.ts
+++ b/paperclip-addon/plugins/workflow-engine/src/worker.ts
@@ -15,6 +15,7 @@
 } from "./dag-engine.js";
 import {
   checkIdempotency,
+  createWorkflowRun,
   createStepRun,
   findStepRunByIssueId,
   getWorkflowDefinition,
@@ -25,32 +26,30 @@
   markIdempotency,
   updateStepRun,
   updateWorkflowRun,
-  type WorkflowDefinition,
-  type WorkflowRun,
   type WorkflowStepRun,
 } from "./workflow-store.js";
+import {
+  TERMINAL_STEP_STATUSES,
+  findStepDefinition,
+  getStepAgentName,
+  getStepAgentNameHint,
+  toWorkflowDefinitionRecord,
+  toWorkflowRunRecord,
+  toWorkflowStepRunRecord,
+  type WorkflowDefinitionRecord,
+  type WorkflowRunRecord,
+  type WorkflowStepRunRecord,
+} from "./workflow-utils.js";
 
-type WorkflowStepRecord = PluginEntityRecord & { data: WorkflowStepRun };
-type WorkflowRunRecord = PluginEntityRecord & { data: WorkflowRun };
-type WorkflowDefinitionRecord = PluginEntityRecord & { data: WorkflowDefinition };
 type WorkflowStepMetadata = WorkflowStep & {
   description?: string;
-  agent?: string;
-  agentName?: string;
-  assigneeAgentName?: string;
   assigneeAgentId?: string;
+  sessionMode?: "fresh" | "reuse";
 };
 type AgentRecord = Awaited<ReturnType<PluginContext["agents"]["list"]>>[number];
 type ReconcilerModule = {
   reconcileStuckSteps?: (ctx: PluginContext) => Promise<void>;
 };
-
-const TERMINAL_STEP_STATUSES = new Set<string>([
-  STEP_STATUSES.done,
-  STEP_STATUSES.failed,
-  STEP_STATUSES.skipped,
-  STEP_STATUSES.escalated,
-]);
 
 function summarizeError(error: unknown): string {
   if (error instanceof Error) {
@@ -58,18 +57,6 @@
   }
 
   return String(error);
-}
-
-function toStepRunRecord(record: PluginEntityRecord): WorkflowStepRecord {
-  return record as WorkflowStepRecord;
-}
-
-function toWorkflowRunRecord(record: PluginEntityRecord): WorkflowRunRecord {
-  return record as WorkflowRunRecord;
-}
-
-function toWorkflowDefinitionRecord(record: PluginEntityRecord): WorkflowDefinitionRecord {
-  return record as WorkflowDefinitionRecord;
 }
 
 function buildIdempotencyKey(event: PluginEvent): string {
@@ -78,28 +65,11 @@
 
 function getStepMetadata(stepDef: WorkflowStep): WorkflowStepMetadata {
   return stepDef as WorkflowStepMetadata;
-}
-
-function findStepDefinition(definition: WorkflowDefinitionRecord, stepId: string): WorkflowStep | null {
-  return definition.data.steps.find((step: WorkflowStep) => step.id === stepId) ?? null;
 }
 
 function getStepDescription(stepDef: WorkflowStep): string | undefined {
   const description = getStepMetadata(stepDef).description;
   return typeof description === "string" && description.trim() ? description.trim() : undefined;
-}
-
-function getStepAgentNameHint(stepDef: WorkflowStep): string | null {
-  const meta = getStepMetadata(stepDef);
-  const candidates = [meta.agentName, meta.agent, meta.assigneeAgentName];
-
-  for (const candidate of candidates) {
-    if (typeof candidate === "string" && candidate.trim()) {
-      return candidate.trim();
-    }
-  }
-
-  return null;
 }
 
 function getStepAgentIdHint(stepDef: WorkflowStep): string | null {
@@ -147,60 +117,247 @@
 
 async function invokeAgentForStep(
   ctx: PluginContext,
+  stepRunRecord: WorkflowStepRunRecord,
   stepDef: WorkflowStep,
-  agentName: string,
   workflowName: string,
   companyId: string,
-): Promise<void> {
+): Promise<WorkflowStepRunRecord> {
+  const agentName = getStepAgentName(stepRunRecord, stepDef);
+  if (!agentName) {
+    ctx.logger.warn("Agent name is missing for workflow step", {
+      companyId,
+      runId: stepRunRecord.data.runId,
+      stepId: stepDef.id,
+      workflowName,
+    });
+    return stepRunRecord;
+  }
+
   const agents = await ctx.agents.list({ companyId });
-  const agent = agents.find((candidate: AgentRecord) => candidate.name === agentName);
+  const agent = agents.find((candidate: AgentRecord) => candidate.name === agentName) ?? null;
 
   if (!agent) {
     ctx.logger.warn("Agent not found for step", {
       agentName,
       companyId,
+      runId: stepRunRecord.data.runId,
+      stepId: stepDef.id,
+      workflowName,
+    });
+    return stepRunRecord;
+  }
+
+  const prompt = `workflow:${workflowName}/step:${stepDef.id} — "${stepDef.title}" is ready. Please proceed with the assigned task.`;
+  const reason = `workflow:${workflowName}/step:${stepDef.id}`;
+  const sessionMode = getStepMetadata(stepDef).sessionMode === "reuse" ? "reuse" : "fresh";
+
+  if (sessionMode === "reuse") {
+    let sessionId = typeof stepRunRecord.data.sessionId === "string" && stepRunRecord.data.sessionId.trim()
+      ? stepRunRecord.data.sessionId.trim()
+      : "";
+
+    if (!sessionId) {
+      const session = await ctx.agents.sessions.create(agent.id, companyId, {
+        reason,
+      });
+      sessionId = session.sessionId;
+      stepRunRecord = toWorkflowStepRunRecord(await updateStepRun(ctx, stepRunRecord.id, {
+        sessionId,
+      }));
+    }
+
+    await ctx.agents.sessions.sendMessage(sessionId, companyId, {
+      prompt,
+      reason,
+    });
+
+    ctx.logger.info("Sent workflow step prompt via agent session", {
+      agentId: agent.id,
+      agentName,
+      companyId,
+      runId: stepRunRecord.data.runId,
+      sessionId,
       stepId: stepDef.id,
       workflowName,
     });
-    return;
+    return stepRunRecord;
   }
 
   await ctx.agents.invoke(agent.id, companyId, {
-    prompt: `workflow:${workflowName}/step:${stepDef.id} — "${stepDef.title}" is ready. Please proceed with the assigned task.`,
-    reason: `workflow:${workflowName}/step:${stepDef.id}`,
+    prompt,
+    reason,
   });
 
   ctx.logger.info("Invoked agent for workflow step", {
     agentId: agent.id,
     agentName,
     companyId,
+    runId: stepRunRecord.data.runId,
     stepId: stepDef.id,
     workflowName,
   });
+
+  return stepRunRecord;
 }
 
 async function activateBacklogStep(
   ctx: PluginContext,
-  stepRunRecord: WorkflowStepRecord,
+  stepRunRecord: WorkflowStepRunRecord,
   stepDef: WorkflowStep,
   workflowName: string,
   companyId: string,
-): Promise<void> {
+  options?: { parentIssueId?: string },
+): Promise<WorkflowStepRunRecord> {
   if (stepRunRecord.data.status !== STEP_STATUSES.backlog) {
-    return;
+    return stepRunRecord;
+  }
+
+  const resolvedAgent = await resolveStepAgent(
+    ctx,
+    companyId,
+    stepDef,
+    stepRunRecord.data.agentName,
+  );
+
+  let issueId = typeof stepRunRecord.data.issueId === "string" && stepRunRecord.data.issueId.trim()
+    ? stepRunRecord.data.issueId.trim()
+    : "";
+  const stepDescription = getStepDescription(stepDef) ?? `Workflow step: ${stepDef.id}`;
+
+  if (!issueId) {
+    const issue = await ctx.issues.create({
+      assigneeAgentId: resolvedAgent.agentId ?? undefined,
+      companyId,
+      description: stepDescription,
+      parentId: options?.parentIssueId,
+      title: `[${workflowName}] ${stepDef.title}`,
+    });
+    issueId = issue.id;
   }
 
-  const updated = toStepRunRecord(await updateStepRun(ctx, stepRunRecord.id, {
+  const nextStartedAt = stepRunRecord.data.startedAt ?? new Date().toISOString();
+  let updatedStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, stepRunRecord.id, {
+    agentName: resolvedAgent.agentName ?? stepRunRecord.data.agentName,
+    issueId,
+    startedAt: nextStartedAt,
     status: STEP_STATUSES.todo,
   }));
 
-  await invokeAgentForStep(
+  updatedStepRun = await invokeAgentForStep(
     ctx,
+    updatedStepRun,
     stepDef,
-    updated.data.agentName,
     workflowName,
     companyId,
   );
+
+  return updatedStepRun;
+}
+
+async function startWorkflow(
+  ctx: PluginContext,
+  workflowId: string,
+  companyId: string,
+  options?: {
+    createParentIssue?: boolean;
+    parentIssueId?: string;
+  },
+): Promise<{
+  activatedStepIds: string[];
+  parentIssueId: string | null;
+  runId: string;
+  workflowId: string;
+}> {
+  const workflowDefinition = await getWorkflowDefinition(ctx, workflowId);
+  if (!workflowDefinition) {
+    throw new Error(`Workflow definition not found: ${workflowId}`);
+  }
+
+  const typedWorkflowDefinition = toWorkflowDefinitionRecord(workflowDefinition);
+  if (typedWorkflowDefinition.data.companyId !== companyId) {
+    throw new Error(`Workflow does not belong to company: ${workflowId}`);
+  }
+
+  let parentIssueId = typeof options?.parentIssueId === "string" && options.parentIssueId.trim()
+    ? options.parentIssueId.trim()
+    : "";
+  if (!parentIssueId && options?.createParentIssue) {
+    const parentIssue = await ctx.issues.create({
+      companyId,
+      description: typedWorkflowDefinition.data.description || `Workflow run: ${typedWorkflowDefinition.data.name}`,
+      title: `[${typedWorkflowDefinition.data.name}] Workflow run`,
+    });
+    parentIssueId = parentIssue.id;
+  }
+
+  const workflowRun = toWorkflowRunRecord(await createWorkflowRun(ctx, {
+    companyId,
+    parentIssueId: parentIssueId || undefined,
+    startedAt: new Date().toISOString(),
+    status: RUN_STATUSES.running,
+    workflowId: typedWorkflowDefinition.id,
+    workflowName: typedWorkflowDefinition.data.name,
+  }));
+
+  const agents = await ctx.agents.list({ companyId });
+  const agentsByName = new Map<string, AgentRecord>();
+  for (const agent of agents) {
+    agentsByName.set(agent.name, agent);
+  }
+
+  const pendingRootSteps: Array<{ stepDef: WorkflowStep; stepRun: WorkflowStepRunRecord }> = [];
+  for (const stepDef of typedWorkflowDefinition.data.steps) {
+    const agentNameHint = getStepAgentNameHint(stepDef);
+    const matchedAgent = agentNameHint ? agentsByName.get(agentNameHint) ?? null : null;
+    const resolvedAgent = matchedAgent
+      ? { agentId: matchedAgent.id, agentName: matchedAgent.name }
+      : await resolveStepAgent(ctx, companyId, stepDef, agentNameHint ?? undefined);
+
+    if (!resolvedAgent.agentName) {
+      throw new Error(`Unable to resolve step assignee for "${stepDef.id}"`);
+    }
+
+    const stepRun = toWorkflowStepRunRecord(await createStepRun(ctx, companyId, {
+      agentName: resolvedAgent.agentName,
+      retryCount: 0,
+      runId: workflowRun.id,
+      status: STEP_STATUSES.backlog,
+      stepId: stepDef.id,
+    }));
+
+    if (stepDef.dependsOn.length === 0 && stepDef.triggerOn !== "escalation") {
+      pendingRootSteps.push({ stepDef, stepRun });
+    }
+  }
+
+  const activatedStepIds: string[] = [];
+  for (const pending of pendingRootSteps) {
+    await activateBacklogStep(
+      ctx,
+      pending.stepRun,
+      pending.stepDef,
+      typedWorkflowDefinition.data.name,
+      companyId,
+      { parentIssueId: parentIssueId || undefined },
+    );
+    activatedStepIds.push(pending.stepDef.id);
+  }
+
+  ctx.logger.info("Started workflow run", {
+    activatedStepIds,
+    companyId,
+    parentIssueId: parentIssueId || null,
+    runId: workflowRun.id,
+    workflowId: typedWorkflowDefinition.id,
+    workflowName: typedWorkflowDefinition.data.name,
+  });
+
+  return {
+    activatedStepIds,
+    parentIssueId: parentIssueId || null,
+    runId: workflowRun.id,
+    workflowId: typedWorkflowDefinition.id,
+  };
 }
 
 async function advanceWorkflow(
@@ -208,7 +365,7 @@
   stepRunRecord: PluginEntityRecord,
   companyId: string,
 ): Promise<void> {
-  const completedStepRun = toStepRunRecord(stepRunRecord);
+  const completedStepRun = toWorkflowStepRunRecord(stepRunRecord);
   const workflowRun = await getWorkflowRun(ctx, completedStepRun.data.runId);
 
   if (!workflowRun) {
@@ -237,7 +394,7 @@
   }
 
   const typedWorkflowDefinition = toWorkflowDefinitionRecord(workflowDefinition);
-  const stepRuns = (await listStepRuns(ctx, typedWorkflowRun.id, companyId)).map(toStepRunRecord);
+  const stepRuns = (await listStepRuns(ctx, typedWorkflowRun.id, companyId)).map(toWorkflowStepRunRecord);
   const completed = new Set<string>();
   const failed = new Set<string>();
   const skipped = new Set<string>();
@@ -289,7 +446,7 @@
         continue;
       }
 
-      stepRun = toStepRunRecord(await createStepRun(ctx, companyId, {
+      stepRun = toWorkflowStepRunRecord(await createStepRun(ctx, companyId, {
         agentName: resolvedAgent.agentName,
         retryCount: 0,
         runId: typedWorkflowRun.id,
@@ -305,6 +462,7 @@
       stepDef,
       typedWorkflowRun.data.workflowName,
       companyId,
+      { parentIssueId: typedWorkflowRun.data.parentIssueId },
     );
   }
 
@@ -329,7 +487,7 @@
   ctx: PluginContext,
   workflowRun: WorkflowRunRecord,
   workflowDefinition: WorkflowDefinitionRecord,
-  sourceStepRun: WorkflowStepRecord,
+  sourceStepRun: WorkflowStepRunRecord,
   escalationTargetId: string,
   companyId: string,
 ): Promise<void> {
@@ -338,7 +496,7 @@
     throw new Error(`Escalation target step not found: ${escalationTargetId}`);
   }
 
-  const stepRuns = (await listStepRuns(ctx, workflowRun.id, companyId)).map(toStepRunRecord);
+  const stepRuns = (await listStepRuns(ctx, workflowRun.id, companyId)).map(toWorkflowStepRunRecord);
   let escalationStepRun = stepRuns.find((candidate) => candidate.data.stepId === escalationTargetId) ?? null;
   const resolvedAgent = await resolveStepAgent(
     ctx,
@@ -352,7 +510,7 @@
   }
 
   if (!escalationStepRun) {
-    escalationStepRun = toStepRunRecord(await createStepRun(ctx, companyId, {
+    escalationStepRun = toWorkflowStepRunRecord(await createStepRun(ctx, companyId, {
       agentName: resolvedAgent.agentName,
       retryCount: 0,
       runId: workflowRun.id,
@@ -374,14 +532,14 @@
       title: `${workflowRun.data.workflowName}: ${escalationStep.title}`,
     });
 
-    escalationStepRun = toStepRunRecord(await updateStepRun(ctx, escalationStepRun.id, {
+    escalationStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, escalationStepRun.id, {
       issueId: issue.id,
     }));
   }
 
   const shouldActivate = escalationStepRun.data.status === STEP_STATUSES.backlog;
   if (shouldActivate) {
-    escalationStepRun = toStepRunRecord(await updateStepRun(ctx, escalationStepRun.id, {
+    escalationStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, escalationStepRun.id, {
       status: STEP_STATUSES.todo,
     }));
   }
@@ -389,8 +547,8 @@
   if (shouldActivate) {
     await invokeAgentForStep(
       ctx,
+      escalationStepRun,
       escalationStep,
-      escalationStepRun.data.agentName,
       workflowRun.data.workflowName,
       companyId,
     );
@@ -421,7 +579,7 @@
     return;
   }
 
-  const typedStepRun = toStepRunRecord(stepRunRecord);
+  const typedStepRun = toWorkflowStepRunRecord(stepRunRecord);
   if (TERMINAL_STEP_STATUSES.has(typedStepRun.data.status)) {
     return;
   }
@@ -469,7 +627,7 @@
   if (options.allowRetry) {
     const retryInfo = getRetryInfo(typedWorkflowDefinition.data.steps, typedStepRun.data.stepId);
     if (retryInfo.shouldRetry && typedStepRun.data.retryCount < retryInfo.maxRetries) {
-      const retriedStepRun = toStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
+      const retriedStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
         completedAt: undefined,
         retryCount: typedStepRun.data.retryCount + 1,
         status: STEP_STATUSES.todo,
@@ -477,8 +635,8 @@
 
       await invokeAgentForStep(
         ctx,
+        retriedStepRun,
         stepDef,
-        retriedStepRun.data.agentName,
         typedWorkflowRun.data.workflowName,
         event.companyId,
       );
@@ -496,7 +654,7 @@
   }
 
   if (stepDef.onFailure === "skip") {
-    const skippedStepRun = toStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
+    const skippedStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
       completedAt: new Date().toISOString(),
       status: STEP_STATUSES.skipped,
     }));
@@ -556,7 +714,7 @@
       return;
     }
 
-    const escalatedStepRun = toStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
+    const escalatedStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
       completedAt: new Date().toISOString(),
       status: STEP_STATUSES.escalated,
     }));
@@ -638,7 +796,7 @@
           return;
         }
 
-        const stepRun = toStepRunRecord(stepRunRecord);
+        const stepRun = toWorkflowStepRunRecord(stepRunRecord);
         const payload = event.payload as { status?: string };
         const issueStatus = typeof payload.status === "string" ? payload.status : undefined;
 
@@ -657,7 +815,7 @@
           return;
         }
 
-        const updatedStepRun = toStepRunRecord(await updateStepRun(ctx, stepRun.id, {
+        const updatedStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, stepRun.id, {
           completedAt: nextStepStatus === STEP_STATUSES.done ? new Date().toISOString() : undefined,
           startedAt: nextStepStatus === STEP_STATUSES.inProgress
             ? stepRun.data.startedAt ?? new Date().toISOString()
@@ -807,7 +965,7 @@
             ...typedWorkflowRun.data,
           },
           stepRuns: stepRuns.map((record: PluginEntityRecord) => {
-            const stepRun = toStepRunRecord(record);
+            const stepRun = toWorkflowStepRunRecord(record);
             return {
               id: stepRun.id,
               status: stepRun.status,
diff --git a/paperclip-addon/plugins/workflow-engine/src/workflow-store.ts b/paperclip-addon/plugins/workflow-engine/src/workflow-store.ts
index 5871a2082258cebcfe8b69ebafbb0814cf303d50..3d7fb2b3bc75911cfd9093eb3a4054cc73196bb9
--- a/paperclip-addon/plugins/workflow-engine/src/workflow-store.ts
+++ b/paperclip-addon/plugins/workflow-engine/src/workflow-store.ts
@@ -44,7 +44,14 @@
 
 type TypedEntityRecord<T> = Omit<PluginEntityRecord, "data"> & { data: T };
 
-function toTypedRecord<T>(record: PluginEntityRecord): TypedEntityRecord<T> {
+function toTypedRecord<T>(
+  record: PluginEntityRecord,
+  entityType: string,
+): TypedEntityRecord<T> {
+  if (record.entityType !== entityType) {
+    throw new Error(`Expected entity type "${entityType}", got "${record.entityType}"`);
+  }
+
   return record as TypedEntityRecord<T>;
 }
 
@@ -67,13 +74,26 @@
   id: string,
   entityType: string,
 ): Promise<TypedEntityRecord<T> | null> {
+  const listQuery: Parameters<PluginContext["entities"]["list"]>[0] & { id?: string } = {
+    entityType,
+    id,
+    limit: 1,
+  };
+
+  const listed = await ctx.entities.list(listQuery);
+  const listedRecord = listed.find((record) => record.id === id) ?? null;
+  if (listedRecord) {
+    return toTypedRecord<T>(listedRecord, entityType);
+  }
+
+  // Fallback: some host SDK variants ignore `id` on entities.list().
+  // Keep defensive type verification on direct get() until id-filter support is universal.
   const record = await ctx.entities.get(id);
-
-  if (!record || record.entityType !== entityType) {
+  if (!record) {
     return null;
   }
 
-  return toTypedRecord<T>(record);
+  return toTypedRecord<T>(record, entityType);
 }
 
 async function requireEntityByType<T>(
diff --git a/paperclip-addon/plugins/workflow-engine/src/workflow-utils.ts b/paperclip-addon/plugins/workflow-engine/src/workflow-utils.ts
new file mode 100644
index 0000000000000000000000000000000000000000..ec1cf6f6b9aa722b126b58c0a728b4e538ad1494
--- /dev/null
+++ b/paperclip-addon/plugins/workflow-engine/src/workflow-utils.ts
@@ -0,0 +1,62 @@
+import type { PluginEntityRecord } from "@paperclipai/plugin-sdk";
+
+import { STEP_STATUSES } from "./constants.js";
+import type { WorkflowStep } from "./dag-engine.js";
+import type {
+  WorkflowDefinition,
+  WorkflowRun,
+  WorkflowStepRun,
+} from "./workflow-store.js";
+
+export type WorkflowRunRecord = PluginEntityRecord & { data: WorkflowRun };
+export type WorkflowStepRunRecord = PluginEntityRecord & { data: WorkflowStepRun };
+export type WorkflowDefinitionRecord = PluginEntityRecord & { data: WorkflowDefinition };
+type StepAgentMetadata = WorkflowStep & {
+  agent?: string;
+  agentName?: string;
+  assigneeAgentName?: string;
+};
+
+export const TERMINAL_STEP_STATUSES = new Set<string>([
+  STEP_STATUSES.done,
+  STEP_STATUSES.failed,
+  STEP_STATUSES.skipped,
+  STEP_STATUSES.escalated,
+]);
+
+export function toWorkflowRunRecord(record: PluginEntityRecord): WorkflowRunRecord {
+  return record as WorkflowRunRecord;
+}
+
+export function toWorkflowStepRunRecord(record: PluginEntityRecord): WorkflowStepRunRecord {
+  return record as WorkflowStepRunRecord;
+}
+
+export function toWorkflowDefinitionRecord(record: PluginEntityRecord): WorkflowDefinitionRecord {
+  return record as WorkflowDefinitionRecord;
+}
+
+export function findStepDefinition(definition: WorkflowDefinitionRecord, stepId: string): WorkflowStep | null {
+  return definition.data.steps.find((step: WorkflowStep) => step.id === stepId) ?? null;
+}
+
+export function getStepAgentNameHint(stepDef: WorkflowStep): string | null {
+  const stepMeta = stepDef as StepAgentMetadata;
+  const candidates = [stepMeta.agentName, stepMeta.agent, stepMeta.assigneeAgentName];
+
+  for (const candidate of candidates) {
+    if (typeof candidate === "string" && candidate.trim()) {
+      return candidate.trim();
+    }
+  }
+
+  return null;
+}
+
+export function getStepAgentName(stepRun: WorkflowStepRunRecord, stepDef: WorkflowStep): string | null {
+  if (typeof stepRun.data.agentName === "string" && stepRun.data.agentName.trim()) {
+    return stepRun.data.agentName.trim();
+  }
+
+  return getStepAgentNameHint(stepDef);
+}

file update:
diff --git a/paperclip-addon/plugins/workflow-engine/src/dag-engine.ts b/paperclip-addon/plugins/workflow-engine/src/dag-engine.ts
index b59510f0473171038be77277286d3b5c2063e6c5..e7f21ff998cab78b9325a8aeacf4fda9c5e06553
--- a/paperclip-addon/plugins/workflow-engine/src/dag-engine.ts
+++ b/paperclip-addon/plugins/workflow-engine/src/dag-engine.ts
@@ -2,6 +2,7 @@
   id: string;
   title: string;
   dependsOn: string[];
+  sessionMode?: "fresh" | "reuse";
   onFailure?: "retry" | "skip" | "abort_workflow" | "escalate";
   escalateTo?: string;
   maxRetries?: number;
diff --git a/paperclip-addon/plugins/workflow-engine/src/reconciler.ts b/paperclip-addon/plugins/workflow-engine/src/reconciler.ts
index 14ab4e766a835d145a6c2e7b1b564270285cbdc3..d2ab54817bd8185944379d2188bfbdef1722020a
--- a/paperclip-addon/plugins/workflow-engine/src/reconciler.ts
+++ b/paperclip-addon/plugins/workflow-engine/src/reconciler.ts
@@ -8,65 +8,22 @@
   listStepRuns,
   updateStepRun,
   updateWorkflowRun,
-  type WorkflowDefinition,
-  type WorkflowRun,
-  type WorkflowStepRun,
 } from "./workflow-store.js";
 import type { WorkflowStep } from "./dag-engine.js";
-
-type WorkflowRunRecord = PluginEntityRecord & { data: WorkflowRun };
-type WorkflowStepRunRecord = PluginEntityRecord & { data: WorkflowStepRun };
-type WorkflowDefinitionRecord = PluginEntityRecord & { data: WorkflowDefinition };
-type StepAgentMetadata = WorkflowStep & {
-  agent?: string;
-  agentName?: string;
-  assigneeAgentName?: string;
-};
+import {
+  TERMINAL_STEP_STATUSES,
+  findStepDefinition,
+  getStepAgentName,
+  toWorkflowDefinitionRecord,
+  toWorkflowRunRecord,
+  toWorkflowStepRunRecord,
+} from "./workflow-utils.js";
 
 const STALE_THRESHOLD_MS = 300_000;
-const TERMINAL_STEP_STATUSES = new Set<string>([
-  STEP_STATUSES.done,
-  STEP_STATUSES.failed,
-  STEP_STATUSES.skipped,
-  STEP_STATUSES.escalated,
-]);
 
 function isStale(record: PluginEntityRecord, thresholdMs: number): boolean {
   const updatedAt = new Date(record.updatedAt).getTime();
   return Date.now() - updatedAt > thresholdMs;
-}
-
-function toWorkflowRunRecord(record: PluginEntityRecord): WorkflowRunRecord {
-  return record as WorkflowRunRecord;
-}
-
-function toWorkflowStepRunRecord(record: PluginEntityRecord): WorkflowStepRunRecord {
-  return record as WorkflowStepRunRecord;
-}
-
-function toWorkflowDefinitionRecord(record: PluginEntityRecord): WorkflowDefinitionRecord {
-  return record as WorkflowDefinitionRecord;
-}
-
-function findStepDefinition(definition: WorkflowDefinitionRecord, stepId: string): WorkflowStep | null {
-  return definition.data.steps.find((step: WorkflowStep) => step.id === stepId) ?? null;
-}
-
-function getStepAgentName(stepRun: WorkflowStepRunRecord, stepDef: WorkflowStep): string | null {
-  if (typeof stepRun.data.agentName === "string" && stepRun.data.agentName.trim()) {
-    return stepRun.data.agentName.trim();
-  }
-
-  const stepMeta = stepDef as StepAgentMetadata;
-  const candidates = [stepMeta.agentName, stepMeta.agent, stepMeta.assigneeAgentName];
-
-  for (const candidate of candidates) {
-    if (typeof candidate === "string" && candidate.trim()) {
-      return candidate.trim();
-    }
-  }
-
-  return null;
 }
 
 async function invokeAgentByName(
diff --git a/paperclip-addon/plugins/workflow-engine/src/worker.ts b/paperclip-addon/plugins/workflow-engine/src/worker.ts
index c5ac3fbb4ca7f767eb29331bd3630b635449b335..7921b6a88326f9a7d8eb0492f204ae84295e6f1d
--- a/paperclip-addon/plugins/workflow-engine/src/worker.ts
+++ b/paperclip-addon/plugins/workflow-engine/src/worker.ts
@@ -15,6 +15,7 @@
 } from "./dag-engine.js";
 import {
   checkIdempotency,
+  createWorkflowRun,
   createStepRun,
   findStepRunByIssueId,
   getWorkflowDefinition,
@@ -25,32 +26,30 @@
   markIdempotency,
   updateStepRun,
   updateWorkflowRun,
-  type WorkflowDefinition,
-  type WorkflowRun,
   type WorkflowStepRun,
 } from "./workflow-store.js";
+import {
+  TERMINAL_STEP_STATUSES,
+  findStepDefinition,
+  getStepAgentName,
+  getStepAgentNameHint,
+  toWorkflowDefinitionRecord,
+  toWorkflowRunRecord,
+  toWorkflowStepRunRecord,
+  type WorkflowDefinitionRecord,
+  type WorkflowRunRecord,
+  type WorkflowStepRunRecord,
+} from "./workflow-utils.js";
 
-type WorkflowStepRecord = PluginEntityRecord & { data: WorkflowStepRun };
-type WorkflowRunRecord = PluginEntityRecord & { data: WorkflowRun };
-type WorkflowDefinitionRecord = PluginEntityRecord & { data: WorkflowDefinition };
 type WorkflowStepMetadata = WorkflowStep & {
   description?: string;
-  agent?: string;
-  agentName?: string;
-  assigneeAgentName?: string;
   assigneeAgentId?: string;
+  sessionMode?: "fresh" | "reuse";
 };
 type AgentRecord = Awaited<ReturnType<PluginContext["agents"]["list"]>>[number];
 type ReconcilerModule = {
   reconcileStuckSteps?: (ctx: PluginContext) => Promise<void>;
 };
-
-const TERMINAL_STEP_STATUSES = new Set<string>([
-  STEP_STATUSES.done,
-  STEP_STATUSES.failed,
-  STEP_STATUSES.skipped,
-  STEP_STATUSES.escalated,
-]);
 
 function summarizeError(error: unknown): string {
   if (error instanceof Error) {
@@ -58,18 +57,6 @@
   }
 
   return String(error);
-}
-
-function toStepRunRecord(record: PluginEntityRecord): WorkflowStepRecord {
-  return record as WorkflowStepRecord;
-}
-
-function toWorkflowRunRecord(record: PluginEntityRecord): WorkflowRunRecord {
-  return record as WorkflowRunRecord;
-}
-
-function toWorkflowDefinitionRecord(record: PluginEntityRecord): WorkflowDefinitionRecord {
-  return record as WorkflowDefinitionRecord;
 }
 
 function buildIdempotencyKey(event: PluginEvent): string {
@@ -78,28 +65,11 @@
 
 function getStepMetadata(stepDef: WorkflowStep): WorkflowStepMetadata {
   return stepDef as WorkflowStepMetadata;
-}
-
-function findStepDefinition(definition: WorkflowDefinitionRecord, stepId: string): WorkflowStep | null {
-  return definition.data.steps.find((step: WorkflowStep) => step.id === stepId) ?? null;
 }
 
 function getStepDescription(stepDef: WorkflowStep): string | undefined {
   const description = getStepMetadata(stepDef).description;
   return typeof description === "string" && description.trim() ? description.trim() : undefined;
-}
-
-function getStepAgentNameHint(stepDef: WorkflowStep): string | null {
-  const meta = getStepMetadata(stepDef);
-  const candidates = [meta.agentName, meta.agent, meta.assigneeAgentName];
-
-  for (const candidate of candidates) {
-    if (typeof candidate === "string" && candidate.trim()) {
-      return candidate.trim();
-    }
-  }
-
-  return null;
 }
 
 function getStepAgentIdHint(stepDef: WorkflowStep): string | null {
@@ -147,60 +117,247 @@
 
 async function invokeAgentForStep(
   ctx: PluginContext,
+  stepRunRecord: WorkflowStepRunRecord,
   stepDef: WorkflowStep,
-  agentName: string,
   workflowName: string,
   companyId: string,
-): Promise<void> {
+): Promise<WorkflowStepRunRecord> {
+  const agentName = getStepAgentName(stepRunRecord, stepDef);
+  if (!agentName) {
+    ctx.logger.warn("Agent name is missing for workflow step", {
+      companyId,
+      runId: stepRunRecord.data.runId,
+      stepId: stepDef.id,
+      workflowName,
+    });
+    return stepRunRecord;
+  }
+
   const agents = await ctx.agents.list({ companyId });
-  const agent = agents.find((candidate: AgentRecord) => candidate.name === agentName);
+  const agent = agents.find((candidate: AgentRecord) => candidate.name === agentName) ?? null;
 
   if (!agent) {
     ctx.logger.warn("Agent not found for step", {
       agentName,
       companyId,
+      runId: stepRunRecord.data.runId,
+      stepId: stepDef.id,
+      workflowName,
+    });
+    return stepRunRecord;
+  }
+
+  const prompt = `workflow:${workflowName}/step:${stepDef.id} — "${stepDef.title}" is ready. Please proceed with the assigned task.`;
+  const reason = `workflow:${workflowName}/step:${stepDef.id}`;
+  const sessionMode = getStepMetadata(stepDef).sessionMode === "reuse" ? "reuse" : "fresh";
+
+  if (sessionMode === "reuse") {
+    let sessionId = typeof stepRunRecord.data.sessionId === "string" && stepRunRecord.data.sessionId.trim()
+      ? stepRunRecord.data.sessionId.trim()
+      : "";
+
+    if (!sessionId) {
+      const session = await ctx.agents.sessions.create(agent.id, companyId, {
+        reason,
+      });
+      sessionId = session.sessionId;
+      stepRunRecord = toWorkflowStepRunRecord(await updateStepRun(ctx, stepRunRecord.id, {
+        sessionId,
+      }));
+    }
+
+    await ctx.agents.sessions.sendMessage(sessionId, companyId, {
+      prompt,
+      reason,
+    });
+
+    ctx.logger.info("Sent workflow step prompt via agent session", {
+      agentId: agent.id,
+      agentName,
+      companyId,
+      runId: stepRunRecord.data.runId,
+      sessionId,
       stepId: stepDef.id,
       workflowName,
     });
-    return;
+    return stepRunRecord;
   }
 
   await ctx.agents.invoke(agent.id, companyId, {
-    prompt: `workflow:${workflowName}/step:${stepDef.id} — "${stepDef.title}" is ready. Please proceed with the assigned task.`,
-    reason: `workflow:${workflowName}/step:${stepDef.id}`,
+    prompt,
+    reason,
   });
 
   ctx.logger.info("Invoked agent for workflow step", {
     agentId: agent.id,
     agentName,
     companyId,
+    runId: stepRunRecord.data.runId,
     stepId: stepDef.id,
     workflowName,
   });
+
+  return stepRunRecord;
 }
 
 async function activateBacklogStep(
   ctx: PluginContext,
-  stepRunRecord: WorkflowStepRecord,
+  stepRunRecord: WorkflowStepRunRecord,
   stepDef: WorkflowStep,
   workflowName: string,
   companyId: string,
-): Promise<void> {
+  options?: { parentIssueId?: string },
+): Promise<WorkflowStepRunRecord> {
   if (stepRunRecord.data.status !== STEP_STATUSES.backlog) {
-    return;
+    return stepRunRecord;
+  }
+
+  const resolvedAgent = await resolveStepAgent(
+    ctx,
+    companyId,
+    stepDef,
+    stepRunRecord.data.agentName,
+  );
+
+  let issueId = typeof stepRunRecord.data.issueId === "string" && stepRunRecord.data.issueId.trim()
+    ? stepRunRecord.data.issueId.trim()
+    : "";
+  const stepDescription = getStepDescription(stepDef) ?? `Workflow step: ${stepDef.id}`;
+
+  if (!issueId) {
+    const issue = await ctx.issues.create({
+      assigneeAgentId: resolvedAgent.agentId ?? undefined,
+      companyId,
+      description: stepDescription,
+      parentId: options?.parentIssueId,
+      title: `[${workflowName}] ${stepDef.title}`,
+    });
+    issueId = issue.id;
   }
 
-  const updated = toStepRunRecord(await updateStepRun(ctx, stepRunRecord.id, {
+  const nextStartedAt = stepRunRecord.data.startedAt ?? new Date().toISOString();
+  let updatedStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, stepRunRecord.id, {
+    agentName: resolvedAgent.agentName ?? stepRunRecord.data.agentName,
+    issueId,
+    startedAt: nextStartedAt,
     status: STEP_STATUSES.todo,
   }));
 
-  await invokeAgentForStep(
+  updatedStepRun = await invokeAgentForStep(
     ctx,
+    updatedStepRun,
     stepDef,
-    updated.data.agentName,
     workflowName,
     companyId,
   );
+
+  return updatedStepRun;
+}
+
+async function startWorkflow(
+  ctx: PluginContext,
+  workflowId: string,
+  companyId: string,
+  options?: {
+    createParentIssue?: boolean;
+    parentIssueId?: string;
+  },
+): Promise<{
+  activatedStepIds: string[];
+  parentIssueId: string | null;
+  runId: string;
+  workflowId: string;
+}> {
+  const workflowDefinition = await getWorkflowDefinition(ctx, workflowId);
+  if (!workflowDefinition) {
+    throw new Error(`Workflow definition not found: ${workflowId}`);
+  }
+
+  const typedWorkflowDefinition = toWorkflowDefinitionRecord(workflowDefinition);
+  if (typedWorkflowDefinition.data.companyId !== companyId) {
+    throw new Error(`Workflow does not belong to company: ${workflowId}`);
+  }
+
+  let parentIssueId = typeof options?.parentIssueId === "string" && options.parentIssueId.trim()
+    ? options.parentIssueId.trim()
+    : "";
+  if (!parentIssueId && options?.createParentIssue) {
+    const parentIssue = await ctx.issues.create({
+      companyId,
+      description: typedWorkflowDefinition.data.description || `Workflow run: ${typedWorkflowDefinition.data.name}`,
+      title: `[${typedWorkflowDefinition.data.name}] Workflow run`,
+    });
+    parentIssueId = parentIssue.id;
+  }
+
+  const workflowRun = toWorkflowRunRecord(await createWorkflowRun(ctx, {
+    companyId,
+    parentIssueId: parentIssueId || undefined,
+    startedAt: new Date().toISOString(),
+    status: RUN_STATUSES.running,
+    workflowId: typedWorkflowDefinition.id,
+    workflowName: typedWorkflowDefinition.data.name,
+  }));
+
+  const agents = await ctx.agents.list({ companyId });
+  const agentsByName = new Map<string, AgentRecord>();
+  for (const agent of agents) {
+    agentsByName.set(agent.name, agent);
+  }
+
+  const pendingRootSteps: Array<{ stepDef: WorkflowStep; stepRun: WorkflowStepRunRecord }> = [];
+  for (const stepDef of typedWorkflowDefinition.data.steps) {
+    const agentNameHint = getStepAgentNameHint(stepDef);
+    const matchedAgent = agentNameHint ? agentsByName.get(agentNameHint) ?? null : null;
+    const resolvedAgent = matchedAgent
+      ? { agentId: matchedAgent.id, agentName: matchedAgent.name }
+      : await resolveStepAgent(ctx, companyId, stepDef, agentNameHint ?? undefined);
+
+    if (!resolvedAgent.agentName) {
+      throw new Error(`Unable to resolve step assignee for "${stepDef.id}"`);
+    }
+
+    const stepRun = toWorkflowStepRunRecord(await createStepRun(ctx, companyId, {
+      agentName: resolvedAgent.agentName,
+      retryCount: 0,
+      runId: workflowRun.id,
+      status: STEP_STATUSES.backlog,
+      stepId: stepDef.id,
+    }));
+
+    if (stepDef.dependsOn.length === 0 && stepDef.triggerOn !== "escalation") {
+      pendingRootSteps.push({ stepDef, stepRun });
+    }
+  }
+
+  const activatedStepIds: string[] = [];
+  for (const pending of pendingRootSteps) {
+    await activateBacklogStep(
+      ctx,
+      pending.stepRun,
+      pending.stepDef,
+      typedWorkflowDefinition.data.name,
+      companyId,
+      { parentIssueId: parentIssueId || undefined },
+    );
+    activatedStepIds.push(pending.stepDef.id);
+  }
+
+  ctx.logger.info("Started workflow run", {
+    activatedStepIds,
+    companyId,
+    parentIssueId: parentIssueId || null,
+    runId: workflowRun.id,
+    workflowId: typedWorkflowDefinition.id,
+    workflowName: typedWorkflowDefinition.data.name,
+  });
+
+  return {
+    activatedStepIds,
+    parentIssueId: parentIssueId || null,
+    runId: workflowRun.id,
+    workflowId: typedWorkflowDefinition.id,
+  };
 }
 
 async function advanceWorkflow(
@@ -208,7 +365,7 @@
   stepRunRecord: PluginEntityRecord,
   companyId: string,
 ): Promise<void> {
-  const completedStepRun = toStepRunRecord(stepRunRecord);
+  const completedStepRun = toWorkflowStepRunRecord(stepRunRecord);
   const workflowRun = await getWorkflowRun(ctx, completedStepRun.data.runId);
 
   if (!workflowRun) {
@@ -237,7 +394,7 @@
   }
 
   const typedWorkflowDefinition = toWorkflowDefinitionRecord(workflowDefinition);
-  const stepRuns = (await listStepRuns(ctx, typedWorkflowRun.id, companyId)).map(toStepRunRecord);
+  const stepRuns = (await listStepRuns(ctx, typedWorkflowRun.id, companyId)).map(toWorkflowStepRunRecord);
   const completed = new Set<string>();
   const failed = new Set<string>();
   const skipped = new Set<string>();
@@ -289,7 +446,7 @@
         continue;
       }
 
-      stepRun = toStepRunRecord(await createStepRun(ctx, companyId, {
+      stepRun = toWorkflowStepRunRecord(await createStepRun(ctx, companyId, {
         agentName: resolvedAgent.agentName,
         retryCount: 0,
         runId: typedWorkflowRun.id,
@@ -305,6 +462,7 @@
       stepDef,
       typedWorkflowRun.data.workflowName,
       companyId,
+      { parentIssueId: typedWorkflowRun.data.parentIssueId },
     );
   }
 
@@ -329,7 +487,7 @@
   ctx: PluginContext,
   workflowRun: WorkflowRunRecord,
   workflowDefinition: WorkflowDefinitionRecord,
-  sourceStepRun: WorkflowStepRecord,
+  sourceStepRun: WorkflowStepRunRecord,
   escalationTargetId: string,
   companyId: string,
 ): Promise<void> {
@@ -338,7 +496,7 @@
     throw new Error(`Escalation target step not found: ${escalationTargetId}`);
   }
 
-  const stepRuns = (await listStepRuns(ctx, workflowRun.id, companyId)).map(toStepRunRecord);
+  const stepRuns = (await listStepRuns(ctx, workflowRun.id, companyId)).map(toWorkflowStepRunRecord);
   let escalationStepRun = stepRuns.find((candidate) => candidate.data.stepId === escalationTargetId) ?? null;
   const resolvedAgent = await resolveStepAgent(
     ctx,
@@ -352,7 +510,7 @@
   }
 
   if (!escalationStepRun) {
-    escalationStepRun = toStepRunRecord(await createStepRun(ctx, companyId, {
+    escalationStepRun = toWorkflowStepRunRecord(await createStepRun(ctx, companyId, {
       agentName: resolvedAgent.agentName,
       retryCount: 0,
       runId: workflowRun.id,
@@ -374,14 +532,14 @@
       title: `${workflowRun.data.workflowName}: ${escalationStep.title}`,
     });
 
-    escalationStepRun = toStepRunRecord(await updateStepRun(ctx, escalationStepRun.id, {
+    escalationStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, escalationStepRun.id, {
       issueId: issue.id,
     }));
   }
 
   const shouldActivate = escalationStepRun.data.status === STEP_STATUSES.backlog;
   if (shouldActivate) {
-    escalationStepRun = toStepRunRecord(await updateStepRun(ctx, escalationStepRun.id, {
+    escalationStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, escalationStepRun.id, {
       status: STEP_STATUSES.todo,
     }));
   }
@@ -389,8 +547,8 @@
   if (shouldActivate) {
     await invokeAgentForStep(
       ctx,
+      escalationStepRun,
       escalationStep,
-      escalationStepRun.data.agentName,
       workflowRun.data.workflowName,
       companyId,
     );
@@ -421,7 +579,7 @@
     return;
   }
 
-  const typedStepRun = toStepRunRecord(stepRunRecord);
+  const typedStepRun = toWorkflowStepRunRecord(stepRunRecord);
   if (TERMINAL_STEP_STATUSES.has(typedStepRun.data.status)) {
     return;
   }
@@ -469,7 +627,7 @@
   if (options.allowRetry) {
     const retryInfo = getRetryInfo(typedWorkflowDefinition.data.steps, typedStepRun.data.stepId);
     if (retryInfo.shouldRetry && typedStepRun.data.retryCount < retryInfo.maxRetries) {
-      const retriedStepRun = toStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
+      const retriedStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
         completedAt: undefined,
         retryCount: typedStepRun.data.retryCount + 1,
         status: STEP_STATUSES.todo,
@@ -477,8 +635,8 @@
 
       await invokeAgentForStep(
         ctx,
+        retriedStepRun,
         stepDef,
-        retriedStepRun.data.agentName,
         typedWorkflowRun.data.workflowName,
         event.companyId,
       );
@@ -496,7 +654,7 @@
   }
 
   if (stepDef.onFailure === "skip") {
-    const skippedStepRun = toStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
+    const skippedStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
       completedAt: new Date().toISOString(),
       status: STEP_STATUSES.skipped,
     }));
@@ -556,7 +714,7 @@
       return;
     }
 
-    const escalatedStepRun = toStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
+    const escalatedStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
       completedAt: new Date().toISOString(),
       status: STEP_STATUSES.escalated,
     }));
@@ -638,7 +796,7 @@
           return;
         }
 
-        const stepRun = toStepRunRecord(stepRunRecord);
+        const stepRun = toWorkflowStepRunRecord(stepRunRecord);
         const payload = event.payload as { status?: string };
         const issueStatus = typeof payload.status === "string" ? payload.status : undefined;
 
@@ -657,7 +815,7 @@
           return;
         }
 
-        const updatedStepRun = toStepRunRecord(await updateStepRun(ctx, stepRun.id, {
+        const updatedStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, stepRun.id, {
           completedAt: nextStepStatus === STEP_STATUSES.done ? new Date().toISOString() : undefined,
           startedAt: nextStepStatus === STEP_STATUSES.inProgress
             ? stepRun.data.startedAt ?? new Date().toISOString()
@@ -807,7 +965,7 @@
             ...typedWorkflowRun.data,
           },
           stepRuns: stepRuns.map((record: PluginEntityRecord) => {
-            const stepRun = toStepRunRecord(record);
+            const stepRun = toWorkflowStepRunRecord(record);
             return {
               id: stepRun.id,
               status: stepRun.status,
diff --git a/paperclip-addon/plugins/workflow-engine/src/workflow-store.ts b/paperclip-addon/plugins/workflow-engine/src/workflow-store.ts
index 5871a2082258cebcfe8b69ebafbb0814cf303d50..3d7fb2b3bc75911cfd9093eb3a4054cc73196bb9
--- a/paperclip-addon/plugins/workflow-engine/src/workflow-store.ts
+++ b/paperclip-addon/plugins/workflow-engine/src/workflow-store.ts
@@ -44,7 +44,14 @@
 
 type TypedEntityRecord<T> = Omit<PluginEntityRecord, "data"> & { data: T };
 
-function toTypedRecord<T>(record: PluginEntityRecord): TypedEntityRecord<T> {
+function toTypedRecord<T>(
+  record: PluginEntityRecord,
+  entityType: string,
+): TypedEntityRecord<T> {
+  if (record.entityType !== entityType) {
+    throw new Error(`Expected entity type "${entityType}", got "${record.entityType}"`);
+  }
+
   return record as TypedEntityRecord<T>;
 }
 
@@ -67,13 +74,26 @@
   id: string,
   entityType: string,
 ): Promise<TypedEntityRecord<T> | null> {
+  const listQuery: Parameters<PluginContext["entities"]["list"]>[0] & { id?: string } = {
+    entityType,
+    id,
+    limit: 1,
+  };
+
+  const listed = await ctx.entities.list(listQuery);
+  const listedRecord = listed.find((record) => record.id === id) ?? null;
+  if (listedRecord) {
+    return toTypedRecord<T>(listedRecord, entityType);
+  }
+
+  // Fallback: some host SDK variants ignore `id` on entities.list().
+  // Keep defensive type verification on direct get() until id-filter support is universal.
   const record = await ctx.entities.get(id);
-
-  if (!record || record.entityType !== entityType) {
+  if (!record) {
     return null;
   }
 
-  return toTypedRecord<T>(record);
+  return toTypedRecord<T>(record, entityType);
 }
 
 async function requireEntityByType<T>(
diff --git a/paperclip-addon/plugins/workflow-engine/src/workflow-utils.ts b/paperclip-addon/plugins/workflow-engine/src/workflow-utils.ts
new file mode 100644
index 0000000000000000000000000000000000000000..ec1cf6f6b9aa722b126b58c0a728b4e538ad1494
--- /dev/null
+++ b/paperclip-addon/plugins/workflow-engine/src/workflow-utils.ts
@@ -0,0 +1,62 @@
+import type { PluginEntityRecord } from "@paperclipai/plugin-sdk";
+
+import { STEP_STATUSES } from "./constants.js";
+import type { WorkflowStep } from "./dag-engine.js";
+import type {
+  WorkflowDefinition,
+  WorkflowRun,
+  WorkflowStepRun,
+} from "./workflow-store.js";
+
+export type WorkflowRunRecord = PluginEntityRecord & { data: WorkflowRun };
+export type WorkflowStepRunRecord = PluginEntityRecord & { data: WorkflowStepRun };
+export type WorkflowDefinitionRecord = PluginEntityRecord & { data: WorkflowDefinition };
+type StepAgentMetadata = WorkflowStep & {
+  agent?: string;
+  agentName?: string;
+  assigneeAgentName?: string;
+};
+
+export const TERMINAL_STEP_STATUSES = new Set<string>([
+  STEP_STATUSES.done,
+  STEP_STATUSES.failed,
+  STEP_STATUSES.skipped,
+  STEP_STATUSES.escalated,
+]);
+
+export function toWorkflowRunRecord(record: PluginEntityRecord): WorkflowRunRecord {
+  return record as WorkflowRunRecord;
+}
+
+export function toWorkflowStepRunRecord(record: PluginEntityRecord): WorkflowStepRunRecord {
+  return record as WorkflowStepRunRecord;
+}
+
+export function toWorkflowDefinitionRecord(record: PluginEntityRecord): WorkflowDefinitionRecord {
+  return record as WorkflowDefinitionRecord;
+}
+
+export function findStepDefinition(definition: WorkflowDefinitionRecord, stepId: string): WorkflowStep | null {
+  return definition.data.steps.find((step: WorkflowStep) => step.id === stepId) ?? null;
+}
+
+export function getStepAgentNameHint(stepDef: WorkflowStep): string | null {
+  const stepMeta = stepDef as StepAgentMetadata;
+  const candidates = [stepMeta.agentName, stepMeta.agent, stepMeta.assigneeAgentName];
+
+  for (const candidate of candidates) {
+    if (typeof candidate === "string" && candidate.trim()) {
+      return candidate.trim();
+    }
+  }
+
+  return null;
+}
+
+export function getStepAgentName(stepRun: WorkflowStepRunRecord, stepDef: WorkflowStep): string | null {
+  if (typeof stepRun.data.agentName === "string" && stepRun.data.agentName.trim()) {
+    return stepRun.data.agentName.trim();
+  }
+
+  return getStepAgentNameHint(stepDef);
+}

exec
/bin/zsh -lc "rg -n \"invokeAgentForStep\\(\" plugins/workflow-engine/src/worker.ts" in /Users/kwak/Projects/paperclip/paperclip-addon succeeded in 0ms:
118:async function invokeAgentForStep(
246:  updatedStepRun = await invokeAgentForStep(
548:    await invokeAgentForStep(
636:      await invokeAgentForStep(
file update:
diff --git a/paperclip-addon/plugins/workflow-engine/src/dag-engine.ts b/paperclip-addon/plugins/workflow-engine/src/dag-engine.ts
index b59510f0473171038be77277286d3b5c2063e6c5..e7f21ff998cab78b9325a8aeacf4fda9c5e06553
--- a/paperclip-addon/plugins/workflow-engine/src/dag-engine.ts
+++ b/paperclip-addon/plugins/workflow-engine/src/dag-engine.ts
@@ -2,6 +2,7 @@
   id: string;
   title: string;
   dependsOn: string[];
+  sessionMode?: "fresh" | "reuse";
   onFailure?: "retry" | "skip" | "abort_workflow" | "escalate";
   escalateTo?: string;
   maxRetries?: number;
diff --git a/paperclip-addon/plugins/workflow-engine/src/reconciler.ts b/paperclip-addon/plugins/workflow-engine/src/reconciler.ts
index 14ab4e766a835d145a6c2e7b1b564270285cbdc3..d2ab54817bd8185944379d2188bfbdef1722020a
--- a/paperclip-addon/plugins/workflow-engine/src/reconciler.ts
+++ b/paperclip-addon/plugins/workflow-engine/src/reconciler.ts
@@ -8,65 +8,22 @@
   listStepRuns,
   updateStepRun,
   updateWorkflowRun,
-  type WorkflowDefinition,
-  type WorkflowRun,
-  type WorkflowStepRun,
 } from "./workflow-store.js";
 import type { WorkflowStep } from "./dag-engine.js";
-
-type WorkflowRunRecord = PluginEntityRecord & { data: WorkflowRun };
-type WorkflowStepRunRecord = PluginEntityRecord & { data: WorkflowStepRun };
-type WorkflowDefinitionRecord = PluginEntityRecord & { data: WorkflowDefinition };
-type StepAgentMetadata = WorkflowStep & {
-  agent?: string;
-  agentName?: string;
-  assigneeAgentName?: string;
-};
+import {
+  TERMINAL_STEP_STATUSES,
+  findStepDefinition,
+  getStepAgentName,
+  toWorkflowDefinitionRecord,
+  toWorkflowRunRecord,
+  toWorkflowStepRunRecord,
+} from "./workflow-utils.js";
 
 const STALE_THRESHOLD_MS = 300_000;
-const TERMINAL_STEP_STATUSES = new Set<string>([
-  STEP_STATUSES.done,
-  STEP_STATUSES.failed,
-  STEP_STATUSES.skipped,
-  STEP_STATUSES.escalated,
-]);
 
 function isStale(record: PluginEntityRecord, thresholdMs: number): boolean {
   const updatedAt = new Date(record.updatedAt).getTime();
   return Date.now() - updatedAt > thresholdMs;
-}
-
-function toWorkflowRunRecord(record: PluginEntityRecord): WorkflowRunRecord {
-  return record as WorkflowRunRecord;
-}
-
-function toWorkflowStepRunRecord(record: PluginEntityRecord): WorkflowStepRunRecord {
-  return record as WorkflowStepRunRecord;
-}
-
-function toWorkflowDefinitionRecord(record: PluginEntityRecord): WorkflowDefinitionRecord {
-  return record as WorkflowDefinitionRecord;
-}
-
-function findStepDefinition(definition: WorkflowDefinitionRecord, stepId: string): WorkflowStep | null {
-  return definition.data.steps.find((step: WorkflowStep) => step.id === stepId) ?? null;
-}
-
-function getStepAgentName(stepRun: WorkflowStepRunRecord, stepDef: WorkflowStep): string | null {
-  if (typeof stepRun.data.agentName === "string" && stepRun.data.agentName.trim()) {
-    return stepRun.data.agentName.trim();
-  }
-
-  const stepMeta = stepDef as StepAgentMetadata;
-  const candidates = [stepMeta.agentName, stepMeta.agent, stepMeta.assigneeAgentName];
-
-  for (const candidate of candidates) {
-    if (typeof candidate === "string" && candidate.trim()) {
-      return candidate.trim();
-    }
-  }
-
-  return null;
 }
 
 async function invokeAgentByName(
diff --git a/paperclip-addon/plugins/workflow-engine/src/worker.ts b/paperclip-addon/plugins/workflow-engine/src/worker.ts
index c5ac3fbb4ca7f767eb29331bd3630b635449b335..7921b6a88326f9a7d8eb0492f204ae84295e6f1d
--- a/paperclip-addon/plugins/workflow-engine/src/worker.ts
+++ b/paperclip-addon/plugins/workflow-engine/src/worker.ts
@@ -15,6 +15,7 @@
 } from "./dag-engine.js";
 import {
   checkIdempotency,
+  createWorkflowRun,
   createStepRun,
   findStepRunByIssueId,
   getWorkflowDefinition,
@@ -25,32 +26,30 @@
   markIdempotency,
   updateStepRun,
   updateWorkflowRun,
-  type WorkflowDefinition,
-  type WorkflowRun,
   type WorkflowStepRun,
 } from "./workflow-store.js";
+import {
+  TERMINAL_STEP_STATUSES,
+  findStepDefinition,
+  getStepAgentName,
+  getStepAgentNameHint,
+  toWorkflowDefinitionRecord,
+  toWorkflowRunRecord,
+  toWorkflowStepRunRecord,
+  type WorkflowDefinitionRecord,
+  type WorkflowRunRecord,
+  type WorkflowStepRunRecord,
+} from "./workflow-utils.js";
 
-type WorkflowStepRecord = PluginEntityRecord & { data: WorkflowStepRun };
-type WorkflowRunRecord = PluginEntityRecord & { data: WorkflowRun };
-type WorkflowDefinitionRecord = PluginEntityRecord & { data: WorkflowDefinition };
 type WorkflowStepMetadata = WorkflowStep & {
   description?: string;
-  agent?: string;
-  agentName?: string;
-  assigneeAgentName?: string;
   assigneeAgentId?: string;
+  sessionMode?: "fresh" | "reuse";
 };
 type AgentRecord = Awaited<ReturnType<PluginContext["agents"]["list"]>>[number];
 type ReconcilerModule = {
   reconcileStuckSteps?: (ctx: PluginContext) => Promise<void>;
 };
-
-const TERMINAL_STEP_STATUSES = new Set<string>([
-  STEP_STATUSES.done,
-  STEP_STATUSES.failed,
-  STEP_STATUSES.skipped,
-  STEP_STATUSES.escalated,
-]);
 
 function summarizeError(error: unknown): string {
   if (error instanceof Error) {
@@ -58,18 +57,6 @@
   }
 
   return String(error);
-}
-
-function toStepRunRecord(record: PluginEntityRecord): WorkflowStepRecord {
-  return record as WorkflowStepRecord;
-}
-
-function toWorkflowRunRecord(record: PluginEntityRecord): WorkflowRunRecord {
-  return record as WorkflowRunRecord;
-}
-
-function toWorkflowDefinitionRecord(record: PluginEntityRecord): WorkflowDefinitionRecord {
-  return record as WorkflowDefinitionRecord;
 }
 
 function buildIdempotencyKey(event: PluginEvent): string {
@@ -78,28 +65,11 @@
 
 function getStepMetadata(stepDef: WorkflowStep): WorkflowStepMetadata {
   return stepDef as WorkflowStepMetadata;
-}
-
-function findStepDefinition(definition: WorkflowDefinitionRecord, stepId: string): WorkflowStep | null {
-  return definition.data.steps.find((step: WorkflowStep) => step.id === stepId) ?? null;
 }
 
 function getStepDescription(stepDef: WorkflowStep): string | undefined {
   const description = getStepMetadata(stepDef).description;
   return typeof description === "string" && description.trim() ? description.trim() : undefined;
-}
-
-function getStepAgentNameHint(stepDef: WorkflowStep): string | null {
-  const meta = getStepMetadata(stepDef);
-  const candidates = [meta.agentName, meta.agent, meta.assigneeAgentName];
-
-  for (const candidate of candidates) {
-    if (typeof candidate === "string" && candidate.trim()) {
-      return candidate.trim();
-    }
-  }
-
-  return null;
 }
 
 function getStepAgentIdHint(stepDef: WorkflowStep): string | null {
@@ -147,60 +117,247 @@
 
 async function invokeAgentForStep(
   ctx: PluginContext,
+  stepRunRecord: WorkflowStepRunRecord,
   stepDef: WorkflowStep,
-  agentName: string,
   workflowName: string,
   companyId: string,
-): Promise<void> {
+): Promise<WorkflowStepRunRecord> {
+  const agentName = getStepAgentName(stepRunRecord, stepDef);
+  if (!agentName) {
+    ctx.logger.warn("Agent name is missing for workflow step", {
+      companyId,
+      runId: stepRunRecord.data.runId,
+      stepId: stepDef.id,
+      workflowName,
+    });
+    return stepRunRecord;
+  }
+
   const agents = await ctx.agents.list({ companyId });
-  const agent = agents.find((candidate: AgentRecord) => candidate.name === agentName);
+  const agent = agents.find((candidate: AgentRecord) => candidate.name === agentName) ?? null;
 
   if (!agent) {
     ctx.logger.warn("Agent not found for step", {
       agentName,
       companyId,
+      runId: stepRunRecord.data.runId,
+      stepId: stepDef.id,
+      workflowName,
+    });
+    return stepRunRecord;
+  }
+
+  const prompt = `workflow:${workflowName}/step:${stepDef.id} — "${stepDef.title}" is ready. Please proceed with the assigned task.`;
+  const reason = `workflow:${workflowName}/step:${stepDef.id}`;
+  const sessionMode = getStepMetadata(stepDef).sessionMode === "reuse" ? "reuse" : "fresh";
+
+  if (sessionMode === "reuse") {
+    let sessionId = typeof stepRunRecord.data.sessionId === "string" && stepRunRecord.data.sessionId.trim()
+      ? stepRunRecord.data.sessionId.trim()
+      : "";
+
+    if (!sessionId) {
+      const session = await ctx.agents.sessions.create(agent.id, companyId, {
+        reason,
+      });
+      sessionId = session.sessionId;
+      stepRunRecord = toWorkflowStepRunRecord(await updateStepRun(ctx, stepRunRecord.id, {
+        sessionId,
+      }));
+    }
+
+    await ctx.agents.sessions.sendMessage(sessionId, companyId, {
+      prompt,
+      reason,
+    });
+
+    ctx.logger.info("Sent workflow step prompt via agent session", {
+      agentId: agent.id,
+      agentName,
+      companyId,
+      runId: stepRunRecord.data.runId,
+      sessionId,
       stepId: stepDef.id,
       workflowName,
     });
-    return;
+    return stepRunRecord;
   }
 
   await ctx.agents.invoke(agent.id, companyId, {
-    prompt: `workflow:${workflowName}/step:${stepDef.id} — "${stepDef.title}" is ready. Please proceed with the assigned task.`,
-    reason: `workflow:${workflowName}/step:${stepDef.id}`,
+    prompt,
+    reason,
   });
 
   ctx.logger.info("Invoked agent for workflow step", {
     agentId: agent.id,
     agentName,
     companyId,
+    runId: stepRunRecord.data.runId,
     stepId: stepDef.id,
     workflowName,
   });
+
+  return stepRunRecord;
 }
 
 async function activateBacklogStep(
   ctx: PluginContext,
-  stepRunRecord: WorkflowStepRecord,
+  stepRunRecord: WorkflowStepRunRecord,
   stepDef: WorkflowStep,
   workflowName: string,
   companyId: string,
-): Promise<void> {
+  options?: { parentIssueId?: string },
+): Promise<WorkflowStepRunRecord> {
   if (stepRunRecord.data.status !== STEP_STATUSES.backlog) {
-    return;
+    return stepRunRecord;
+  }
+
+  const resolvedAgent = await resolveStepAgent(
+    ctx,
+    companyId,
+    stepDef,
+    stepRunRecord.data.agentName,
+  );
+
+  let issueId = typeof stepRunRecord.data.issueId === "string" && stepRunRecord.data.issueId.trim()
+    ? stepRunRecord.data.issueId.trim()
+    : "";
+  const stepDescription = getStepDescription(stepDef) ?? `Workflow step: ${stepDef.id}`;
+
+  if (!issueId) {
+    const issue = await ctx.issues.create({
+      assigneeAgentId: resolvedAgent.agentId ?? undefined,
+      companyId,
+      description: stepDescription,
+      parentId: options?.parentIssueId,
+      title: `[${workflowName}] ${stepDef.title}`,
+    });
+    issueId = issue.id;
   }
 
-  const updated = toStepRunRecord(await updateStepRun(ctx, stepRunRecord.id, {
+  const nextStartedAt = stepRunRecord.data.startedAt ?? new Date().toISOString();
+  let updatedStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, stepRunRecord.id, {
+    agentName: resolvedAgent.agentName ?? stepRunRecord.data.agentName,
+    issueId,
+    startedAt: nextStartedAt,
     status: STEP_STATUSES.todo,
   }));
 
-  await invokeAgentForStep(
+  updatedStepRun = await invokeAgentForStep(
     ctx,
+    updatedStepRun,
     stepDef,
-    updated.data.agentName,
     workflowName,
     companyId,
   );
+
+  return updatedStepRun;
+}
+
+async function startWorkflow(
+  ctx: PluginContext,
+  workflowId: string,
+  companyId: string,
+  options?: {
+    createParentIssue?: boolean;
+    parentIssueId?: string;
+  },
+): Promise<{
+  activatedStepIds: string[];
+  parentIssueId: string | null;
+  runId: string;
+  workflowId: string;
+}> {
+  const workflowDefinition = await getWorkflowDefinition(ctx, workflowId);
+  if (!workflowDefinition) {
+    throw new Error(`Workflow definition not found: ${workflowId}`);
+  }
+
+  const typedWorkflowDefinition = toWorkflowDefinitionRecord(workflowDefinition);
+  if (typedWorkflowDefinition.data.companyId !== companyId) {
+    throw new Error(`Workflow does not belong to company: ${workflowId}`);
+  }
+
+  let parentIssueId = typeof options?.parentIssueId === "string" && options.parentIssueId.trim()
+    ? options.parentIssueId.trim()
+    : "";
+  if (!parentIssueId && options?.createParentIssue) {
+    const parentIssue = await ctx.issues.create({
+      companyId,
+      description: typedWorkflowDefinition.data.description || `Workflow run: ${typedWorkflowDefinition.data.name}`,
+      title: `[${typedWorkflowDefinition.data.name}] Workflow run`,
+    });
+    parentIssueId = parentIssue.id;
+  }
+
+  const workflowRun = toWorkflowRunRecord(await createWorkflowRun(ctx, {
+    companyId,
+    parentIssueId: parentIssueId || undefined,
+    startedAt: new Date().toISOString(),
+    status: RUN_STATUSES.running,
+    workflowId: typedWorkflowDefinition.id,
+    workflowName: typedWorkflowDefinition.data.name,
+  }));
+
+  const agents = await ctx.agents.list({ companyId });
+  const agentsByName = new Map<string, AgentRecord>();
+  for (const agent of agents) {
+    agentsByName.set(agent.name, agent);
+  }
+
+  const pendingRootSteps: Array<{ stepDef: WorkflowStep; stepRun: WorkflowStepRunRecord }> = [];
+  for (const stepDef of typedWorkflowDefinition.data.steps) {
+    const agentNameHint = getStepAgentNameHint(stepDef);
+    const matchedAgent = agentNameHint ? agentsByName.get(agentNameHint) ?? null : null;
+    const resolvedAgent = matchedAgent
+      ? { agentId: matchedAgent.id, agentName: matchedAgent.name }
+      : await resolveStepAgent(ctx, companyId, stepDef, agentNameHint ?? undefined);
+
+    if (!resolvedAgent.agentName) {
+      throw new Error(`Unable to resolve step assignee for "${stepDef.id}"`);
+    }
+
+    const stepRun = toWorkflowStepRunRecord(await createStepRun(ctx, companyId, {
+      agentName: resolvedAgent.agentName,
+      retryCount: 0,
+      runId: workflowRun.id,
+      status: STEP_STATUSES.backlog,
+      stepId: stepDef.id,
+    }));
+
+    if (stepDef.dependsOn.length === 0 && stepDef.triggerOn !== "escalation") {
+      pendingRootSteps.push({ stepDef, stepRun });
+    }
+  }
+
+  const activatedStepIds: string[] = [];
+  for (const pending of pendingRootSteps) {
+    await activateBacklogStep(
+      ctx,
+      pending.stepRun,
+      pending.stepDef,
+      typedWorkflowDefinition.data.name,
+      companyId,
+      { parentIssueId: parentIssueId || undefined },
+    );
+    activatedStepIds.push(pending.stepDef.id);
+  }
+
+  ctx.logger.info("Started workflow run", {
+    activatedStepIds,
+    companyId,
+    parentIssueId: parentIssueId || null,
+    runId: workflowRun.id,
+    workflowId: typedWorkflowDefinition.id,
+    workflowName: typedWorkflowDefinition.data.name,
+  });
+
+  return {
+    activatedStepIds,
+    parentIssueId: parentIssueId || null,
+    runId: workflowRun.id,
+    workflowId: typedWorkflowDefinition.id,
+  };
 }
 
 async function advanceWorkflow(
@@ -208,7 +365,7 @@
   stepRunRecord: PluginEntityRecord,
   companyId: string,
 ): Promise<void> {
-  const completedStepRun = toStepRunRecord(stepRunRecord);
+  const completedStepRun = toWorkflowStepRunRecord(stepRunRecord);
   const workflowRun = await getWorkflowRun(ctx, completedStepRun.data.runId);
 
   if (!workflowRun) {
@@ -237,7 +394,7 @@
   }
 
   const typedWorkflowDefinition = toWorkflowDefinitionRecord(workflowDefinition);
-  const stepRuns = (await listStepRuns(ctx, typedWorkflowRun.id, companyId)).map(toStepRunRecord);
+  const stepRuns = (await listStepRuns(ctx, typedWorkflowRun.id, companyId)).map(toWorkflowStepRunRecord);
   const completed = new Set<string>();
   const failed = new Set<string>();
   const skipped = new Set<string>();
@@ -289,7 +446,7 @@
         continue;
       }
 
-      stepRun = toStepRunRecord(await createStepRun(ctx, companyId, {
+      stepRun = toWorkflowStepRunRecord(await createStepRun(ctx, companyId, {
         agentName: resolvedAgent.agentName,
         retryCount: 0,
         runId: typedWorkflowRun.id,
@@ -305,6 +462,7 @@
       stepDef,
       typedWorkflowRun.data.workflowName,
       companyId,
+      { parentIssueId: typedWorkflowRun.data.parentIssueId },
     );
   }
 
@@ -329,7 +487,7 @@
   ctx: PluginContext,
   workflowRun: WorkflowRunRecord,
   workflowDefinition: WorkflowDefinitionRecord,
-  sourceStepRun: WorkflowStepRecord,
+  sourceStepRun: WorkflowStepRunRecord,
   escalationTargetId: string,
   companyId: string,
 ): Promise<void> {
@@ -338,7 +496,7 @@
     throw new Error(`Escalation target step not found: ${escalationTargetId}`);
   }
 
-  const stepRuns = (await listStepRuns(ctx, workflowRun.id, companyId)).map(toStepRunRecord);
+  const stepRuns = (await listStepRuns(ctx, workflowRun.id, companyId)).map(toWorkflowStepRunRecord);
   let escalationStepRun = stepRuns.find((candidate) => candidate.data.stepId === escalationTargetId) ?? null;
   const resolvedAgent = await resolveStepAgent(
     ctx,
@@ -352,7 +510,7 @@
   }
 
   if (!escalationStepRun) {
-    escalationStepRun = toStepRunRecord(await createStepRun(ctx, companyId, {
+    escalationStepRun = toWorkflowStepRunRecord(await createStepRun(ctx, companyId, {
       agentName: resolvedAgent.agentName,
       retryCount: 0,
       runId: workflowRun.id,
@@ -374,14 +532,14 @@
       title: `${workflowRun.data.workflowName}: ${escalationStep.title}`,
     });
 
-    escalationStepRun = toStepRunRecord(await updateStepRun(ctx, escalationStepRun.id, {
+    escalationStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, escalationStepRun.id, {
       issueId: issue.id,
     }));
   }
 
   const shouldActivate = escalationStepRun.data.status === STEP_STATUSES.backlog;
   if (shouldActivate) {
-    escalationStepRun = toStepRunRecord(await updateStepRun(ctx, escalationStepRun.id, {
+    escalationStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, escalationStepRun.id, {
       status: STEP_STATUSES.todo,
     }));
   }
@@ -389,8 +547,8 @@
   if (shouldActivate) {
     await invokeAgentForStep(
       ctx,
+      escalationStepRun,
       escalationStep,
-      escalationStepRun.data.agentName,
       workflowRun.data.workflowName,
       companyId,
     );
@@ -421,7 +579,7 @@
     return;
   }
 
-  const typedStepRun = toStepRunRecord(stepRunRecord);
+  const typedStepRun = toWorkflowStepRunRecord(stepRunRecord);
   if (TERMINAL_STEP_STATUSES.has(typedStepRun.data.status)) {
     return;
   }
@@ -469,7 +627,7 @@
   if (options.allowRetry) {
     const retryInfo = getRetryInfo(typedWorkflowDefinition.data.steps, typedStepRun.data.stepId);
     if (retryInfo.shouldRetry && typedStepRun.data.retryCount < retryInfo.maxRetries) {
-      const retriedStepRun = toStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
+      const retriedStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
         completedAt: undefined,
         retryCount: typedStepRun.data.retryCount + 1,
         status: STEP_STATUSES.todo,
@@ -477,8 +635,8 @@
 
       await invokeAgentForStep(
         ctx,
+        retriedStepRun,
         stepDef,
-        retriedStepRun.data.agentName,
         typedWorkflowRun.data.workflowName,
         event.companyId,
       );
@@ -496,7 +654,7 @@
   }
 
   if (stepDef.onFailure === "skip") {
-    const skippedStepRun = toStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
+    const skippedStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
       completedAt: new Date().toISOString(),
       status: STEP_STATUSES.skipped,
     }));
@@ -556,7 +714,7 @@
       return;
     }
 
-    const escalatedStepRun = toStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
+    const escalatedStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
       completedAt: new Date().toISOString(),
       status: STEP_STATUSES.escalated,
     }));
@@ -638,7 +796,7 @@
           return;
         }
 
-        const stepRun = toStepRunRecord(stepRunRecord);
+        const stepRun = toWorkflowStepRunRecord(stepRunRecord);
         const payload = event.payload as { status?: string };
         const issueStatus = typeof payload.status === "string" ? payload.status : undefined;
 
@@ -657,7 +815,7 @@
           return;
         }
 
-        const updatedStepRun = toStepRunRecord(await updateStepRun(ctx, stepRun.id, {
+        const updatedStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, stepRun.id, {
           completedAt: nextStepStatus === STEP_STATUSES.done ? new Date().toISOString() : undefined,
           startedAt: nextStepStatus === STEP_STATUSES.inProgress
             ? stepRun.data.startedAt ?? new Date().toISOString()
@@ -807,7 +965,7 @@
             ...typedWorkflowRun.data,
           },
           stepRuns: stepRuns.map((record: PluginEntityRecord) => {
-            const stepRun = toStepRunRecord(record);
+            const stepRun = toWorkflowStepRunRecord(record);
             return {
               id: stepRun.id,
               status: stepRun.status,
diff --git a/paperclip-addon/plugins/workflow-engine/src/workflow-store.ts b/paperclip-addon/plugins/workflow-engine/src/workflow-store.ts
index 5871a2082258cebcfe8b69ebafbb0814cf303d50..3d7fb2b3bc75911cfd9093eb3a4054cc73196bb9
--- a/paperclip-addon/plugins/workflow-engine/src/workflow-store.ts
+++ b/paperclip-addon/plugins/workflow-engine/src/workflow-store.ts
@@ -44,7 +44,14 @@
 
 type TypedEntityRecord<T> = Omit<PluginEntityRecord, "data"> & { data: T };
 
-function toTypedRecord<T>(record: PluginEntityRecord): TypedEntityRecord<T> {
+function toTypedRecord<T>(
+  record: PluginEntityRecord,
+  entityType: string,
+): TypedEntityRecord<T> {
+  if (record.entityType !== entityType) {
+    throw new Error(`Expected entity type "${entityType}", got "${record.entityType}"`);
+  }
+
   return record as TypedEntityRecord<T>;
 }
 
@@ -67,13 +74,26 @@
   id: string,
   entityType: string,
 ): Promise<TypedEntityRecord<T> | null> {
+  const listQuery: Parameters<PluginContext["entities"]["list"]>[0] & { id?: string } = {
+    entityType,
+    id,
+    limit: 1,
+  };
+
+  const listed = await ctx.entities.list(listQuery);
+  const listedRecord = listed.find((record) => record.id === id) ?? null;
+  if (listedRecord) {
+    return toTypedRecord<T>(listedRecord, entityType);
+  }
+
+  // Fallback: some host SDK variants ignore `id` on entities.list().
+  // Keep defensive type verification on direct get() until id-filter support is universal.
   const record = await ctx.entities.get(id);
-
-  if (!record || record.entityType !== entityType) {
+  if (!record) {
     return null;
   }
 
-  return toTypedRecord<T>(record);
+  return toTypedRecord<T>(record, entityType);
 }
 
 async function requireEntityByType<T>(
diff --git a/paperclip-addon/plugins/workflow-engine/src/workflow-utils.ts b/paperclip-addon/plugins/workflow-engine/src/workflow-utils.ts
new file mode 100644
index 0000000000000000000000000000000000000000..ec1cf6f6b9aa722b126b58c0a728b4e538ad1494
--- /dev/null
+++ b/paperclip-addon/plugins/workflow-engine/src/workflow-utils.ts
@@ -0,0 +1,62 @@
+import type { PluginEntityRecord } from "@paperclipai/plugin-sdk";
+
+import { STEP_STATUSES } from "./constants.js";
+import type { WorkflowStep } from "./dag-engine.js";
+import type {
+  WorkflowDefinition,
+  WorkflowRun,
+  WorkflowStepRun,
+} from "./workflow-store.js";
+
+export type WorkflowRunRecord = PluginEntityRecord & { data: WorkflowRun };
+export type WorkflowStepRunRecord = PluginEntityRecord & { data: WorkflowStepRun };
+export type WorkflowDefinitionRecord = PluginEntityRecord & { data: WorkflowDefinition };
+type StepAgentMetadata = WorkflowStep & {
+  agent?: string;
+  agentName?: string;
+  assigneeAgentName?: string;
+};
+
+export const TERMINAL_STEP_STATUSES = new Set<string>([
+  STEP_STATUSES.done,
+  STEP_STATUSES.failed,
+  STEP_STATUSES.skipped,
+  STEP_STATUSES.escalated,
+]);
+
+export function toWorkflowRunRecord(record: PluginEntityRecord): WorkflowRunRecord {
+  return record as WorkflowRunRecord;
+}
+
+export function toWorkflowStepRunRecord(record: PluginEntityRecord): WorkflowStepRunRecord {
+  return record as WorkflowStepRunRecord;
+}
+
+export function toWorkflowDefinitionRecord(record: PluginEntityRecord): WorkflowDefinitionRecord {
+  return record as WorkflowDefinitionRecord;
+}
+
+export function findStepDefinition(definition: WorkflowDefinitionRecord, stepId: string): WorkflowStep | null {
+  return definition.data.steps.find((step: WorkflowStep) => step.id === stepId) ?? null;
+}
+
+export function getStepAgentNameHint(stepDef: WorkflowStep): string | null {
+  const stepMeta = stepDef as StepAgentMetadata;
+  const candidates = [stepMeta.agentName, stepMeta.agent, stepMeta.assigneeAgentName];
+
+  for (const candidate of candidates) {
+    if (typeof candidate === "string" && candidate.trim()) {
+      return candidate.trim();
+    }
+  }
+
+  return null;
+}
+
+export function getStepAgentName(stepRun: WorkflowStepRunRecord, stepDef: WorkflowStep): string | null {
+  if (typeof stepRun.data.agentName === "string" && stepRun.data.agentName.trim()) {
+    return stepRun.data.agentName.trim();
+  }
+
+  return getStepAgentNameHint(stepDef);
+}

exec
/bin/zsh -lc "sed -n '520,660p' plugins/workflow-engine/src/worker.ts" in /Users/kwak/Projects/paperclip/paperclip-addon succeeded in 0ms:
  }

  if (!escalationStepRun.data.issueId) {
    const issue = await ctx.issues.create({
      assigneeAgentId: resolvedAgent.agentId ?? undefined,
      companyId,
      description: [
        getStepDescription(escalationStep),
        `Escalated from workflow "${workflowRun.data.workflowName}" step "${sourceStepRun.data.stepId}".`,
        sourceStepRun.data.issueId ? `Origin issue: ${sourceStepRun.data.issueId}.` : undefined,
      ].filter((value): value is string => typeof value === "string" && value.trim().length > 0).join("\n\n"),
      parentId: workflowRun.data.parentIssueId ?? sourceStepRun.data.issueId,
      title: `${workflowRun.data.workflowName}: ${escalationStep.title}`,
    });

    escalationStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, escalationStepRun.id, {
      issueId: issue.id,
    }));
  }

file update:
diff --git a/paperclip-addon/plugins/workflow-engine/src/dag-engine.ts b/paperclip-addon/plugins/workflow-engine/src/dag-engine.ts
index b59510f0473171038be77277286d3b5c2063e6c5..e7f21ff998cab78b9325a8aeacf4fda9c5e06553
--- a/paperclip-addon/plugins/workflow-engine/src/dag-engine.ts
+++ b/paperclip-addon/plugins/workflow-engine/src/dag-engine.ts
@@ -2,6 +2,7 @@
   id: string;
   title: string;
   dependsOn: string[];
+  sessionMode?: "fresh" | "reuse";
   onFailure?: "retry" | "skip" | "abort_workflow" | "escalate";
   escalateTo?: string;
   maxRetries?: number;
diff --git a/paperclip-addon/plugins/workflow-engine/src/reconciler.ts b/paperclip-addon/plugins/workflow-engine/src/reconciler.ts
index 14ab4e766a835d145a6c2e7b1b564270285cbdc3..d2ab54817bd8185944379d2188bfbdef1722020a
--- a/paperclip-addon/plugins/workflow-engine/src/reconciler.ts
+++ b/paperclip-addon/plugins/workflow-engine/src/reconciler.ts
@@ -8,65 +8,22 @@
   listStepRuns,
   updateStepRun,
   updateWorkflowRun,
-  type WorkflowDefinition,
-  type WorkflowRun,
-  type WorkflowStepRun,
 } from "./workflow-store.js";
 import type { WorkflowStep } from "./dag-engine.js";
-
-type WorkflowRunRecord = PluginEntityRecord & { data: WorkflowRun };
-type WorkflowStepRunRecord = PluginEntityRecord & { data: WorkflowStepRun };
-type WorkflowDefinitionRecord = PluginEntityRecord & { data: WorkflowDefinition };
-type StepAgentMetadata = WorkflowStep & {
-  agent?: string;
-  agentName?: string;
-  assigneeAgentName?: string;
-};
+import {
+  TERMINAL_STEP_STATUSES,
+  findStepDefinition,
+  getStepAgentName,
+  toWorkflowDefinitionRecord,
+  toWorkflowRunRecord,
+  toWorkflowStepRunRecord,
+} from "./workflow-utils.js";
 
 const STALE_THRESHOLD_MS = 300_000;
-const TERMINAL_STEP_STATUSES = new Set<string>([
-  STEP_STATUSES.done,
-  STEP_STATUSES.failed,
-  STEP_STATUSES.skipped,
-  STEP_STATUSES.escalated,
-]);
 
 function isStale(record: PluginEntityRecord, thresholdMs: number): boolean {
   const updatedAt = new Date(record.updatedAt).getTime();
   return Date.now() - updatedAt > thresholdMs;
-}
-
-function toWorkflowRunRecord(record: PluginEntityRecord): WorkflowRunRecord {
-  return record as WorkflowRunRecord;
-}
-
-function toWorkflowStepRunRecord(record: PluginEntityRecord): WorkflowStepRunRecord {
-  return record as WorkflowStepRunRecord;
-}
-
-function toWorkflowDefinitionRecord(record: PluginEntityRecord): WorkflowDefinitionRecord {
-  return record as WorkflowDefinitionRecord;
-}
-
-function findStepDefinition(definition: WorkflowDefinitionRecord, stepId: string): WorkflowStep | null {
-  return definition.data.steps.find((step: WorkflowStep) => step.id === stepId) ?? null;
-}
-
-function getStepAgentName(stepRun: WorkflowStepRunRecord, stepDef: WorkflowStep): string | null {
-  if (typeof stepRun.data.agentName === "string" && stepRun.data.agentName.trim()) {
-    return stepRun.data.agentName.trim();
-  }
-
-  const stepMeta = stepDef as StepAgentMetadata;
-  const candidates = [stepMeta.agentName, stepMeta.agent, stepMeta.assigneeAgentName];
-
-  for (const candidate of candidates) {
-    if (typeof candidate === "string" && candidate.trim()) {
-      return candidate.trim();
-    }
-  }
-
-  return null;
 }
 
 async function invokeAgentByName(
diff --git a/paperclip-addon/plugins/workflow-engine/src/worker.ts b/paperclip-addon/plugins/workflow-engine/src/worker.ts
index c5ac3fbb4ca7f767eb29331bd3630b635449b335..7921b6a88326f9a7d8eb0492f204ae84295e6f1d
--- a/paperclip-addon/plugins/workflow-engine/src/worker.ts
+++ b/paperclip-addon/plugins/workflow-engine/src/worker.ts
@@ -15,6 +15,7 @@
 } from "./dag-engine.js";
 import {
   checkIdempotency,
+  createWorkflowRun,
   createStepRun,
   findStepRunByIssueId,
   getWorkflowDefinition,
@@ -25,32 +26,30 @@
   markIdempotency,
   updateStepRun,
   updateWorkflowRun,
-  type WorkflowDefinition,
-  type WorkflowRun,
   type WorkflowStepRun,
 } from "./workflow-store.js";
+import {
+  TERMINAL_STEP_STATUSES,
+  findStepDefinition,
+  getStepAgentName,
+  getStepAgentNameHint,
+  toWorkflowDefinitionRecord,
+  toWorkflowRunRecord,
+  toWorkflowStepRunRecord,
+  type WorkflowDefinitionRecord,
+  type WorkflowRunRecord,
+  type WorkflowStepRunRecord,
+} from "./workflow-utils.js";
 
-type WorkflowStepRecord = PluginEntityRecord & { data: WorkflowStepRun };
-type WorkflowRunRecord = PluginEntityRecord & { data: WorkflowRun };
-type WorkflowDefinitionRecord = PluginEntityRecord & { data: WorkflowDefinition };
 type WorkflowStepMetadata = WorkflowStep & {
   description?: string;
-  agent?: string;
-  agentName?: string;
-  assigneeAgentName?: string;
   assigneeAgentId?: string;
+  sessionMode?: "fresh" | "reuse";
 };
 type AgentRecord = Awaited<ReturnType<PluginContext["agents"]["list"]>>[number];
 type ReconcilerModule = {
   reconcileStuckSteps?: (ctx: PluginContext) => Promise<void>;
 };
-
-const TERMINAL_STEP_STATUSES = new Set<string>([
-  STEP_STATUSES.done,
-  STEP_STATUSES.failed,
-  STEP_STATUSES.skipped,
-  STEP_STATUSES.escalated,
-]);
 
 function summarizeError(error: unknown): string {
   if (error instanceof Error) {
@@ -58,18 +57,6 @@
   }
 
   return String(error);
-}
-
-function toStepRunRecord(record: PluginEntityRecord): WorkflowStepRecord {
-  return record as WorkflowStepRecord;
-}
-
-function toWorkflowRunRecord(record: PluginEntityRecord): WorkflowRunRecord {
-  return record as WorkflowRunRecord;
-}
-
-function toWorkflowDefinitionRecord(record: PluginEntityRecord): WorkflowDefinitionRecord {
-  return record as WorkflowDefinitionRecord;
 }
 
 function buildIdempotencyKey(event: PluginEvent): string {
@@ -78,28 +65,11 @@
 
 function getStepMetadata(stepDef: WorkflowStep): WorkflowStepMetadata {
   return stepDef as WorkflowStepMetadata;
-}
-
-function findStepDefinition(definition: WorkflowDefinitionRecord, stepId: string): WorkflowStep | null {
-  return definition.data.steps.find((step: WorkflowStep) => step.id === stepId) ?? null;
 }
 
 function getStepDescription(stepDef: WorkflowStep): string | undefined {
   const description = getStepMetadata(stepDef).description;
   return typeof description === "string" && description.trim() ? description.trim() : undefined;
-}
-
-function getStepAgentNameHint(stepDef: WorkflowStep): string | null {
-  const meta = getStepMetadata(stepDef);
-  const candidates = [meta.agentName, meta.agent, meta.assigneeAgentName];
-
-  for (const candidate of candidates) {
-    if (typeof candidate === "string" && candidate.trim()) {
-      return candidate.trim();
-    }
-  }
-
-  return null;
 }
 
 function getStepAgentIdHint(stepDef: WorkflowStep): string | null {
@@ -147,60 +117,247 @@
 
 async function invokeAgentForStep(
   ctx: PluginContext,
+  stepRunRecord: WorkflowStepRunRecord,
   stepDef: WorkflowStep,
-  agentName: string,
   workflowName: string,
   companyId: string,
-): Promise<void> {
+): Promise<WorkflowStepRunRecord> {
+  const agentName = getStepAgentName(stepRunRecord, stepDef);
+  if (!agentName) {
+    ctx.logger.warn("Agent name is missing for workflow step", {
+      companyId,
+      runId: stepRunRecord.data.runId,
+      stepId: stepDef.id,
+      workflowName,
+    });
+    return stepRunRecord;
+  }
+
   const agents = await ctx.agents.list({ companyId });
-  const agent = agents.find((candidate: AgentRecord) => candidate.name === agentName);
+  const agent = agents.find((candidate: AgentRecord) => candidate.name === agentName) ?? null;
 
   if (!agent) {
     ctx.logger.warn("Agent not found for step", {
       agentName,
       companyId,
+      runId: stepRunRecord.data.runId,
+      stepId: stepDef.id,
+      workflowName,
+    });
+    return stepRunRecord;
+  }
+
+  const prompt = `workflow:${workflowName}/step:${stepDef.id} — "${stepDef.title}" is ready. Please proceed with the assigned task.`;
+  const reason = `workflow:${workflowName}/step:${stepDef.id}`;
+  const sessionMode = getStepMetadata(stepDef).sessionMode === "reuse" ? "reuse" : "fresh";
+
+  if (sessionMode === "reuse") {
+    let sessionId = typeof stepRunRecord.data.sessionId === "string" && stepRunRecord.data.sessionId.trim()
+      ? stepRunRecord.data.sessionId.trim()
+      : "";
+
+    if (!sessionId) {
+      const session = await ctx.agents.sessions.create(agent.id, companyId, {
+        reason,
+      });
+      sessionId = session.sessionId;
+      stepRunRecord = toWorkflowStepRunRecord(await updateStepRun(ctx, stepRunRecord.id, {
+        sessionId,
+      }));
+    }
+
+    await ctx.agents.sessions.sendMessage(sessionId, companyId, {
+      prompt,
+      reason,
+    });
+
+    ctx.logger.info("Sent workflow step prompt via agent session", {
+      agentId: agent.id,
+      agentName,
+      companyId,
+      runId: stepRunRecord.data.runId,
+      sessionId,
       stepId: stepDef.id,
       workflowName,
     });
-    return;
+    return stepRunRecord;
   }
 
   await ctx.agents.invoke(agent.id, companyId, {
-    prompt: `workflow:${workflowName}/step:${stepDef.id} — "${stepDef.title}" is ready. Please proceed with the assigned task.`,
-    reason: `workflow:${workflowName}/step:${stepDef.id}`,
+    prompt,
+    reason,
   });
 
   ctx.logger.info("Invoked agent for workflow step", {
     agentId: agent.id,
     agentName,
     companyId,
+    runId: stepRunRecord.data.runId,
     stepId: stepDef.id,
     workflowName,
   });
+
+  return stepRunRecord;
 }
 
 async function activateBacklogStep(
   ctx: PluginContext,
-  stepRunRecord: WorkflowStepRecord,
+  stepRunRecord: WorkflowStepRunRecord,
   stepDef: WorkflowStep,
   workflowName: string,
   companyId: string,
-): Promise<void> {
+  options?: { parentIssueId?: string },
+): Promise<WorkflowStepRunRecord> {
   if (stepRunRecord.data.status !== STEP_STATUSES.backlog) {
-    return;
+    return stepRunRecord;
+  }
+
+  const resolvedAgent = await resolveStepAgent(
+    ctx,
+    companyId,
+    stepDef,
+    stepRunRecord.data.agentName,
+  );
+
+  let issueId = typeof stepRunRecord.data.issueId === "string" && stepRunRecord.data.issueId.trim()
+    ? stepRunRecord.data.issueId.trim()
+    : "";
+  const stepDescription = getStepDescription(stepDef) ?? `Workflow step: ${stepDef.id}`;
+
+  if (!issueId) {
+    const issue = await ctx.issues.create({
+      assigneeAgentId: resolvedAgent.agentId ?? undefined,
+      companyId,
+      description: stepDescription,
+      parentId: options?.parentIssueId,
+      title: `[${workflowName}] ${stepDef.title}`,
+    });
+    issueId = issue.id;
   }
 
-  const updated = toStepRunRecord(await updateStepRun(ctx, stepRunRecord.id, {
+  const nextStartedAt = stepRunRecord.data.startedAt ?? new Date().toISOString();
+  let updatedStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, stepRunRecord.id, {
+    agentName: resolvedAgent.agentName ?? stepRunRecord.data.agentName,
+    issueId,
+    startedAt: nextStartedAt,
     status: STEP_STATUSES.todo,
   }));
 
-  await invokeAgentForStep(
+  updatedStepRun = await invokeAgentForStep(
     ctx,
+    updatedStepRun,
     stepDef,
-    updated.data.agentName,
     workflowName,
     companyId,
   );
+
+  return updatedStepRun;
+}
+
+async function startWorkflow(
+  ctx: PluginContext,
+  workflowId: string,
+  companyId: string,
+  options?: {
+    createParentIssue?: boolean;
+    parentIssueId?: string;
+  },
+): Promise<{
+  activatedStepIds: string[];
+  parentIssueId: string | null;
+  runId: string;
+  workflowId: string;
+}> {
+  const workflowDefinition = await getWorkflowDefinition(ctx, workflowId);
+  if (!workflowDefinition) {
+    throw new Error(`Workflow definition not found: ${workflowId}`);
+  }
+
+  const typedWorkflowDefinition = toWorkflowDefinitionRecord(workflowDefinition);
+  if (typedWorkflowDefinition.data.companyId !== companyId) {
+    throw new Error(`Workflow does not belong to company: ${workflowId}`);
+  }
+
+  let parentIssueId = typeof options?.parentIssueId === "string" && options.parentIssueId.trim()
+    ? options.parentIssueId.trim()
+    : "";
+  if (!parentIssueId && options?.createParentIssue) {
+    const parentIssue = await ctx.issues.create({
+      companyId,
+      description: typedWorkflowDefinition.data.description || `Workflow run: ${typedWorkflowDefinition.data.name}`,
+      title: `[${typedWorkflowDefinition.data.name}] Workflow run`,
+    });
+    parentIssueId = parentIssue.id;
+  }
+
+  const workflowRun = toWorkflowRunRecord(await createWorkflowRun(ctx, {
+    companyId,
+    parentIssueId: parentIssueId || undefined,
+    startedAt: new Date().toISOString(),
+    status: RUN_STATUSES.running,
+    workflowId: typedWorkflowDefinition.id,
+    workflowName: typedWorkflowDefinition.data.name,
+  }));
+
+  const agents = await ctx.agents.list({ companyId });
+  const agentsByName = new Map<string, AgentRecord>();
+  for (const agent of agents) {
+    agentsByName.set(agent.name, agent);
+  }
+
+  const pendingRootSteps: Array<{ stepDef: WorkflowStep; stepRun: WorkflowStepRunRecord }> = [];
+  for (const stepDef of typedWorkflowDefinition.data.steps) {
+    const agentNameHint = getStepAgentNameHint(stepDef);
+    const matchedAgent = agentNameHint ? agentsByName.get(agentNameHint) ?? null : null;
+    const resolvedAgent = matchedAgent
+      ? { agentId: matchedAgent.id, agentName: matchedAgent.name }
+      : await resolveStepAgent(ctx, companyId, stepDef, agentNameHint ?? undefined);
+
+    if (!resolvedAgent.agentName) {
+      throw new Error(`Unable to resolve step assignee for "${stepDef.id}"`);
+    }
+
+    const stepRun = toWorkflowStepRunRecord(await createStepRun(ctx, companyId, {
+      agentName: resolvedAgent.agentName,
+      retryCount: 0,
+      runId: workflowRun.id,
+      status: STEP_STATUSES.backlog,
+      stepId: stepDef.id,
+    }));
+
+    if (stepDef.dependsOn.length === 0 && stepDef.triggerOn !== "escalation") {
+      pendingRootSteps.push({ stepDef, stepRun });
+    }
+  }
+
+  const activatedStepIds: string[] = [];
+  for (const pending of pendingRootSteps) {
+    await activateBacklogStep(
+      ctx,
+      pending.stepRun,
+      pending.stepDef,
+      typedWorkflowDefinition.data.name,
+      companyId,
+      { parentIssueId: parentIssueId || undefined },
+    );
+    activatedStepIds.push(pending.stepDef.id);
+  }
+
+  ctx.logger.info("Started workflow run", {
+    activatedStepIds,
+    companyId,
+    parentIssueId: parentIssueId || null,
+    runId: workflowRun.id,
+    workflowId: typedWorkflowDefinition.id,
+    workflowName: typedWorkflowDefinition.data.name,
+  });
+
+  return {
+    activatedStepIds,
+    parentIssueId: parentIssueId || null,
+    runId: workflowRun.id,
+    workflowId: typedWorkflowDefinition.id,
+  };
 }
 
 async function advanceWorkflow(
@@ -208,7 +365,7 @@
   stepRunRecord: PluginEntityRecord,
   companyId: string,
 ): Promise<void> {
-  const completedStepRun = toStepRunRecord(stepRunRecord);
+  const completedStepRun = toWorkflowStepRunRecord(stepRunRecord);
   const workflowRun = await getWorkflowRun(ctx, completedStepRun.data.runId);
 
   if (!workflowRun) {
@@ -237,7 +394,7 @@
   }
 
   const typedWorkflowDefinition = toWorkflowDefinitionRecord(workflowDefinition);
-  const stepRuns = (await listStepRuns(ctx, typedWorkflowRun.id, companyId)).map(toStepRunRecord);
+  const stepRuns = (await listStepRuns(ctx, typedWorkflowRun.id, companyId)).map(toWorkflowStepRunRecord);
   const completed = new Set<string>();
   const failed = new Set<string>();
   const skipped = new Set<string>();
@@ -289,7 +446,7 @@
         continue;
       }
 
-      stepRun = toStepRunRecord(await createStepRun(ctx, companyId, {
+      stepRun = toWorkflowStepRunRecord(await createStepRun(ctx, companyId, {
         agentName: resolvedAgent.agentName,
         retryCount: 0,
         runId: typedWorkflowRun.id,
@@ -305,6 +462,7 @@
       stepDef,
       typedWorkflowRun.data.workflowName,
       companyId,
+      { parentIssueId: typedWorkflowRun.data.parentIssueId },
     );
   }
 
@@ -329,7 +487,7 @@
   ctx: PluginContext,
   workflowRun: WorkflowRunRecord,
   workflowDefinition: WorkflowDefinitionRecord,
-  sourceStepRun: WorkflowStepRecord,
+  sourceStepRun: WorkflowStepRunRecord,
   escalationTargetId: string,
   companyId: string,
 ): Promise<void> {
@@ -338,7 +496,7 @@
     throw new Error(`Escalation target step not found: ${escalationTargetId}`);
   }
 
-  const stepRuns = (await listStepRuns(ctx, workflowRun.id, companyId)).map(toStepRunRecord);
+  const stepRuns = (await listStepRuns(ctx, workflowRun.id, companyId)).map(toWorkflowStepRunRecord);
   let escalationStepRun = stepRuns.find((candidate) => candidate.data.stepId === escalationTargetId) ?? null;
   const resolvedAgent = await resolveStepAgent(
     ctx,
@@ -352,7 +510,7 @@
   }
 
   if (!escalationStepRun) {
-    escalationStepRun = toStepRunRecord(await createStepRun(ctx, companyId, {
+    escalationStepRun = toWorkflowStepRunRecord(await createStepRun(ctx, companyId, {
       agentName: resolvedAgent.agentName,
       retryCount: 0,
       runId: workflowRun.id,
@@ -374,14 +532,14 @@
       title: `${workflowRun.data.workflowName}: ${escalationStep.title}`,
     });
 
-    escalationStepRun = toStepRunRecord(await updateStepRun(ctx, escalationStepRun.id, {
+    escalationStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, escalationStepRun.id, {
       issueId: issue.id,
     }));
   }
 
   const shouldActivate = escalationStepRun.data.status === STEP_STATUSES.backlog;
   if (shouldActivate) {
-    escalationStepRun = toStepRunRecord(await updateStepRun(ctx, escalationStepRun.id, {
+    escalationStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, escalationStepRun.id, {
       status: STEP_STATUSES.todo,
     }));
   }
@@ -389,8 +547,8 @@
   if (shouldActivate) {
     await invokeAgentForStep(
       ctx,
+      escalationStepRun,
       escalationStep,
-      escalationStepRun.data.agentName,
       workflowRun.data.workflowName,
       companyId,
     );
@@ -421,7 +579,7 @@
     return;
   }
 
-  const typedStepRun = toStepRunRecord(stepRunRecord);
+  const typedStepRun = toWorkflowStepRunRecord(stepRunRecord);
   if (TERMINAL_STEP_STATUSES.has(typedStepRun.data.status)) {
     return;
   }
@@ -469,7 +627,7 @@
   if (options.allowRetry) {
     const retryInfo = getRetryInfo(typedWorkflowDefinition.data.steps, typedStepRun.data.stepId);
     if (retryInfo.shouldRetry && typedStepRun.data.retryCount < retryInfo.maxRetries) {
-      const retriedStepRun = toStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
+      const retriedStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
         completedAt: undefined,
         retryCount: typedStepRun.data.retryCount + 1,
         status: STEP_STATUSES.todo,
@@ -477,8 +635,8 @@
 
       await invokeAgentForStep(
         ctx,
+        retriedStepRun,
         stepDef,
-        retriedStepRun.data.agentName,
         typedWorkflowRun.data.workflowName,
         event.companyId,
       );
@@ -496,7 +654,7 @@
   }
 
   if (stepDef.onFailure === "skip") {
-    const skippedStepRun = toStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
+    const skippedStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
       completedAt: new Date().toISOString(),
       status: STEP_STATUSES.skipped,
     }));
@@ -556,7 +714,7 @@
       return;
     }
 
-    const escalatedStepRun = toStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
+    const escalatedStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
       completedAt: new Date().toISOString(),
       status: STEP_STATUSES.escalated,
     }));
@@ -638,7 +796,7 @@
           return;
         }
 
-        const stepRun = toStepRunRecord(stepRunRecord);
+        const stepRun = toWorkflowStepRunRecord(stepRunRecord);
         const payload = event.payload as { status?: string };
         const issueStatus = typeof payload.status === "string" ? payload.status : undefined;
 
@@ -657,7 +815,7 @@
           return;
         }
 
-        const updatedStepRun = toStepRunRecord(await updateStepRun(ctx, stepRun.id, {
+        const updatedStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, stepRun.id, {
           completedAt: nextStepStatus === STEP_STATUSES.done ? new Date().toISOString() : undefined,
           startedAt: nextStepStatus === STEP_STATUSES.inProgress
             ? stepRun.data.startedAt ?? new Date().toISOString()
@@ -807,7 +965,7 @@
             ...typedWorkflowRun.data,
           },
           stepRuns: stepRuns.map((record: PluginEntityRecord) => {
-            const stepRun = toStepRunRecord(record);
+            const stepRun = toWorkflowStepRunRecord(record);
             return {
               id: stepRun.id,
               status: stepRun.status,
diff --git a/paperclip-addon/plugins/workflow-engine/src/workflow-store.ts b/paperclip-addon/plugins/workflow-engine/src/workflow-store.ts
index 5871a2082258cebcfe8b69ebafbb0814cf303d50..3d7fb2b3bc75911cfd9093eb3a4054cc73196bb9
--- a/paperclip-addon/plugins/workflow-engine/src/workflow-store.ts
+++ b/paperclip-addon/plugins/workflow-engine/src/workflow-store.ts
@@ -44,7 +44,14 @@
 
 type TypedEntityRecord<T> = Omit<PluginEntityRecord, "data"> & { data: T };
 
-function toTypedRecord<T>(record: PluginEntityRecord): TypedEntityRecord<T> {
+function toTypedRecord<T>(
+  record: PluginEntityRecord,
+  entityType: string,
+): TypedEntityRecord<T> {
+  if (record.entityType !== entityType) {
+    throw new Error(`Expected entity type "${entityType}", got "${record.entityType}"`);
+  }
+
   return record as TypedEntityRecord<T>;
 }
 
@@ -67,13 +74,26 @@
   id: string,
   entityType: string,
 ): Promise<TypedEntityRecord<T> | null> {
+  const listQuery: Parameters<PluginContext["entities"]["list"]>[0] & { id?: string } = {
+    entityType,
+    id,
+    limit: 1,
+  };
+
+  const listed = await ctx.entities.list(listQuery);
+  const listedRecord = listed.find((record) => record.id === id) ?? null;
+  if (listedRecord) {
+    return toTypedRecord<T>(listedRecord, entityType);
+  }
+
+  // Fallback: some host SDK variants ignore `id` on entities.list().
+  // Keep defensive type verification on direct get() until id-filter support is universal.
   const record = await ctx.entities.get(id);
-
-  if (!record || record.entityType !== entityType) {
+  if (!record) {
     return null;
   }
 
-  return toTypedRecord<T>(record);
+  return toTypedRecord<T>(record, entityType);
 }
 
 async function requireEntityByType<T>(
diff --git a/paperclip-addon/plugins/workflow-engine/src/workflow-utils.ts b/paperclip-addon/plugins/workflow-engine/src/workflow-utils.ts
new file mode 100644
index 0000000000000000000000000000000000000000..ec1cf6f6b9aa722b126b58c0a728b4e538ad1494
--- /dev/null
+++ b/paperclip-addon/plugins/workflow-engine/src/workflow-utils.ts
@@ -0,0 +1,62 @@
+import type { PluginEntityRecord } from "@paperclipai/plugin-sdk";
+
+import { STEP_STATUSES } from "./constants.js";
+import type { WorkflowStep } from "./dag-engine.js";
+import type {
+  WorkflowDefinition,
+  WorkflowRun,
+  WorkflowStepRun,
+} from "./workflow-store.js";
+
+export type WorkflowRunRecord = PluginEntityRecord & { data: WorkflowRun };
+export type WorkflowStepRunRecord = PluginEntityRecord & { data: WorkflowStepRun };
+export type WorkflowDefinitionRecord = PluginEntityRecord & { data: WorkflowDefinition };
+type StepAgentMetadata = WorkflowStep & {
+  agent?: string;
+  agentName?: string;
+  assigneeAgentName?: string;
+};
+
+export const TERMINAL_STEP_STATUSES = new Set<string>([
+  STEP_STATUSES.done,
+  STEP_STATUSES.failed,
+  STEP_STATUSES.skipped,
+  STEP_STATUSES.escalated,
+]);
+
+export function toWorkflowRunRecord(record: PluginEntityRecord): WorkflowRunRecord {
+  return record as WorkflowRunRecord;
+}
+
+export function toWorkflowStepRunRecord(record: PluginEntityRecord): WorkflowStepRunRecord {
+  return record as WorkflowStepRunRecord;
+}
+
+export function toWorkflowDefinitionRecord(record: PluginEntityRecord): WorkflowDefinitionRecord {
+  return record as WorkflowDefinitionRecord;
+}
+
+export function findStepDefinition(definition: WorkflowDefinitionRecord, stepId: string): WorkflowStep | null {
+  return definition.data.steps.find((step: WorkflowStep) => step.id === stepId) ?? null;
+}
+
+export function getStepAgentNameHint(stepDef: WorkflowStep): string | null {
+  const stepMeta = stepDef as StepAgentMetadata;
+  const candidates = [stepMeta.agentName, stepMeta.agent, stepMeta.assigneeAgentName];
+
+  for (const candidate of candidates) {
+    if (typeof candidate === "string" && candidate.trim()) {
+      return candidate.trim();
+    }
+  }
+
+  return null;
+}
+
+export function getStepAgentName(stepRun: WorkflowStepRunRecord, stepDef: WorkflowStep): string | null {
+  if (typeof stepRun.data.agentName === "string" && stepRun.data.agentName.trim()) {
+    return stepRun.data.agentName.trim();
+  }
+
+  return getStepAgentNameHint(stepDef);
+}

file update
M /Users/kwak/Projects/paperclip/paperclip-addon/plugins/workflow-engine/src/worker.ts
@@ -49,2 +49,3 @@
 type AgentRecord = Awaited<ReturnType<PluginContext["agents"]["list"]>>[number];
+type IssueUpdatePatch = Parameters<PluginContext["issues"]["update"]>[1];
 type ReconcilerModule = {
@@ -777,2 +778,25 @@
 
+function registerDataHandler(
+  ctx: PluginContext,
+  key: string,
+  handler: (params: Record<string, unknown>) => Promise<unknown>,
+): void {
+  const dataClient = ctx.data as {
+    handle?: (handlerKey: string, handlerFn: (params: Record<string, unknown>) => Promise<unknown>) => void;
+    register?: (handlerKey: string, handlerFn: (params: Record<string, unknown>) => Promise<unknown>) => void;
+  };
+
+  if (typeof dataClient.handle === "function") {
+    dataClient.handle(key, handler);
+    return;
+  }
+
+  if (typeof dataClient.register === "function") {
+    dataClient.register(key, handler);
+    return;
+  }
+
+  throw new Error("Plugin data client does not support handler registration");
+}
+
 const plugin = definePlugin({
@@ -847,2 +871,7 @@
       try {
+        const idempotencyKey = buildIdempotencyKey(event);
+        if (await checkIdempotency(ctx, idempotencyKey, event.companyId)) {
+          return;
+        }
+
         const payload = event.payload as {
@@ -852,2 +881,7 @@
         };
+        const issueId = typeof event.entityId === "string" && event.entityId.trim()
+          ? event.entityId.trim()
+          : typeof payload.id === "string" && payload.id.trim()
+            ? payload.id.trim()
+            : "";
 
@@ -860,3 +894,4 @@
 
-        if (parentId || !assigneeAgentId) {
+        if (!issueId || parentId || !assigneeAgentId) {
+          await markIdempotency(ctx, idempotencyKey, event.companyId);
           return;
@@ -864,4 +899,25 @@
 
-        ctx.logger.info("parentId filler: issue created without parent", {
-          issueId: event.entityId ?? payload.id ?? null,
+        const agent = await ctx.agents.get(assigneeAgentId, event.companyId);
+        const metadata = agent?.metadata as Record<string, unknown> | undefined;
+        const defaultParentIssueId = typeof metadata?.defaultParentIssueId === "string" && metadata.defaultParentIssueId.trim()
+          ? metadata.defaultParentIssueId.trim()
+          : "";
+
+        if (!defaultParentIssueId) {
+          await markIdempotency(ctx, idempotencyKey, event.companyId);
+          return;
+        }
+
+        await ctx.issues.update(
+          issueId,
+          { parentId: defaultParentIssueId } as IssueUpdatePatch,
+          event.companyId,
+        );
+        await markIdempotency(ctx, idempotencyKey, event.companyId);
+
+        ctx.logger.info("parentId filler: populated issue parentId from agent metadata", {
+          assigneeAgentId,
+          companyId: event.companyId,
+          issueId,
+          parentId: defaultParentIssueId,
         });
apply_patch(auto_approved=true) exited 0 in 116ms:
Success. Updated the following files:
M /Users/kwak/Projects/paperclip/paperclip-addon/plugins/workflow-engine/src/worker.ts
file update:
diff --git a/paperclip-addon/plugins/workflow-engine/src/dag-engine.ts b/paperclip-addon/plugins/workflow-engine/src/dag-engine.ts
index b59510f0473171038be77277286d3b5c2063e6c5..e7f21ff998cab78b9325a8aeacf4fda9c5e06553
--- a/paperclip-addon/plugins/workflow-engine/src/dag-engine.ts
+++ b/paperclip-addon/plugins/workflow-engine/src/dag-engine.ts
@@ -2,6 +2,7 @@
   id: string;
   title: string;
   dependsOn: string[];
+  sessionMode?: "fresh" | "reuse";
   onFailure?: "retry" | "skip" | "abort_workflow" | "escalate";
   escalateTo?: string;
   maxRetries?: number;
diff --git a/paperclip-addon/plugins/workflow-engine/src/reconciler.ts b/paperclip-addon/plugins/workflow-engine/src/reconciler.ts
index 14ab4e766a835d145a6c2e7b1b564270285cbdc3..d2ab54817bd8185944379d2188bfbdef1722020a
--- a/paperclip-addon/plugins/workflow-engine/src/reconciler.ts
+++ b/paperclip-addon/plugins/workflow-engine/src/reconciler.ts
@@ -8,65 +8,22 @@
   listStepRuns,
   updateStepRun,
   updateWorkflowRun,
-  type WorkflowDefinition,
-  type WorkflowRun,
-  type WorkflowStepRun,
 } from "./workflow-store.js";
 import type { WorkflowStep } from "./dag-engine.js";
-
-type WorkflowRunRecord = PluginEntityRecord & { data: WorkflowRun };
-type WorkflowStepRunRecord = PluginEntityRecord & { data: WorkflowStepRun };
-type WorkflowDefinitionRecord = PluginEntityRecord & { data: WorkflowDefinition };
-type StepAgentMetadata = WorkflowStep & {
-  agent?: string;
-  agentName?: string;
-  assigneeAgentName?: string;
-};
+import {
+  TERMINAL_STEP_STATUSES,
+  findStepDefinition,
+  getStepAgentName,
+  toWorkflowDefinitionRecord,
+  toWorkflowRunRecord,
+  toWorkflowStepRunRecord,
+} from "./workflow-utils.js";
 
 const STALE_THRESHOLD_MS = 300_000;
-const TERMINAL_STEP_STATUSES = new Set<string>([
-  STEP_STATUSES.done,
-  STEP_STATUSES.failed,
-  STEP_STATUSES.skipped,
-  STEP_STATUSES.escalated,
-]);
 
 function isStale(record: PluginEntityRecord, thresholdMs: number): boolean {
   const updatedAt = new Date(record.updatedAt).getTime();
   return Date.now() - updatedAt > thresholdMs;
-}
-
-function toWorkflowRunRecord(record: PluginEntityRecord): WorkflowRunRecord {
-  return record as WorkflowRunRecord;
-}
-
-function toWorkflowStepRunRecord(record: PluginEntityRecord): WorkflowStepRunRecord {
-  return record as WorkflowStepRunRecord;
-}
-
-function toWorkflowDefinitionRecord(record: PluginEntityRecord): WorkflowDefinitionRecord {
-  return record as WorkflowDefinitionRecord;
-}
-
-function findStepDefinition(definition: WorkflowDefinitionRecord, stepId: string): WorkflowStep | null {
-  return definition.data.steps.find((step: WorkflowStep) => step.id === stepId) ?? null;
-}
-
-function getStepAgentName(stepRun: WorkflowStepRunRecord, stepDef: WorkflowStep): string | null {
-  if (typeof stepRun.data.agentName === "string" && stepRun.data.agentName.trim()) {
-    return stepRun.data.agentName.trim();
-  }
-
-  const stepMeta = stepDef as StepAgentMetadata;
-  const candidates = [stepMeta.agentName, stepMeta.agent, stepMeta.assigneeAgentName];
-
-  for (const candidate of candidates) {
-    if (typeof candidate === "string" && candidate.trim()) {
-      return candidate.trim();
-    }
-  }
-
-  return null;
 }
 
 async function invokeAgentByName(
diff --git a/paperclip-addon/plugins/workflow-engine/src/worker.ts b/paperclip-addon/plugins/workflow-engine/src/worker.ts
index c5ac3fbb4ca7f767eb29331bd3630b635449b335..1cd809524f0a14eb2304f9e85d0ad51185891787
--- a/paperclip-addon/plugins/workflow-engine/src/worker.ts
+++ b/paperclip-addon/plugins/workflow-engine/src/worker.ts
@@ -15,6 +15,7 @@
 } from "./dag-engine.js";
 import {
   checkIdempotency,
+  createWorkflowRun,
   createStepRun,
   findStepRunByIssueId,
   getWorkflowDefinition,
@@ -25,63 +26,46 @@
   markIdempotency,
   updateStepRun,
   updateWorkflowRun,
-  type WorkflowDefinition,
-  type WorkflowRun,
   type WorkflowStepRun,
 } from "./workflow-store.js";
+import {
+  TERMINAL_STEP_STATUSES,
+  findStepDefinition,
+  getStepAgentName,
+  getStepAgentNameHint,
+  toWorkflowDefinitionRecord,
+  toWorkflowRunRecord,
+  toWorkflowStepRunRecord,
+  type WorkflowDefinitionRecord,
+  type WorkflowRunRecord,
+  type WorkflowStepRunRecord,
+} from "./workflow-utils.js";
 
-type WorkflowStepRecord = PluginEntityRecord & { data: WorkflowStepRun };
-type WorkflowRunRecord = PluginEntityRecord & { data: WorkflowRun };
-type WorkflowDefinitionRecord = PluginEntityRecord & { data: WorkflowDefinition };
 type WorkflowStepMetadata = WorkflowStep & {
   description?: string;
-  agent?: string;
-  agentName?: string;
-  assigneeAgentName?: string;
   assigneeAgentId?: string;
+  sessionMode?: "fresh" | "reuse";
 };
 type AgentRecord = Awaited<ReturnType<PluginContext["agents"]["list"]>>[number];
+type IssueUpdatePatch = Parameters<PluginContext["issues"]["update"]>[1];
 type ReconcilerModule = {
   reconcileStuckSteps?: (ctx: PluginContext) => Promise<void>;
 };
 
-const TERMINAL_STEP_STATUSES = new Set<string>([
-  STEP_STATUSES.done,
-  STEP_STATUSES.failed,
-  STEP_STATUSES.skipped,
-  STEP_STATUSES.escalated,
-]);
-
 function summarizeError(error: unknown): string {
   if (error instanceof Error) {
     return error.message;
   }
 
   return String(error);
-}
-
-function toStepRunRecord(record: PluginEntityRecord): WorkflowStepRecord {
-  return record as WorkflowStepRecord;
 }
 
-function toWorkflowRunRecord(record: PluginEntityRecord): WorkflowRunRecord {
-  return record as WorkflowRunRecord;
-}
-
-function toWorkflowDefinitionRecord(record: PluginEntityRecord): WorkflowDefinitionRecord {
-  return record as WorkflowDefinitionRecord;
-}
-
 function buildIdempotencyKey(event: PluginEvent): string {
   return `${event.eventType}:${event.eventId}`;
 }
 
 function getStepMetadata(stepDef: WorkflowStep): WorkflowStepMetadata {
   return stepDef as WorkflowStepMetadata;
-}
-
-function findStepDefinition(definition: WorkflowDefinitionRecord, stepId: string): WorkflowStep | null {
-  return definition.data.steps.find((step: WorkflowStep) => step.id === stepId) ?? null;
 }
 
 function getStepDescription(stepDef: WorkflowStep): string | undefined {
@@ -89,19 +73,6 @@
   return typeof description === "string" && description.trim() ? description.trim() : undefined;
 }
 
-function getStepAgentNameHint(stepDef: WorkflowStep): string | null {
-  const meta = getStepMetadata(stepDef);
-  const candidates = [meta.agentName, meta.agent, meta.assigneeAgentName];
-
-  for (const candidate of candidates) {
-    if (typeof candidate === "string" && candidate.trim()) {
-      return candidate.trim();
-    }
-  }
-
-  return null;
-}
-
 function getStepAgentIdHint(stepDef: WorkflowStep): string | null {
   const assigneeAgentId = getStepMetadata(stepDef).assigneeAgentId;
   if (typeof assigneeAgentId !== "string" || !assigneeAgentId.trim()) {
@@ -147,60 +118,247 @@
 
 async function invokeAgentForStep(
   ctx: PluginContext,
+  stepRunRecord: WorkflowStepRunRecord,
   stepDef: WorkflowStep,
-  agentName: string,
   workflowName: string,
   companyId: string,
-): Promise<void> {
+): Promise<WorkflowStepRunRecord> {
+  const agentName = getStepAgentName(stepRunRecord, stepDef);
+  if (!agentName) {
+    ctx.logger.warn("Agent name is missing for workflow step", {
+      companyId,
+      runId: stepRunRecord.data.runId,
+      stepId: stepDef.id,
+      workflowName,
+    });
+    return stepRunRecord;
+  }
+
   const agents = await ctx.agents.list({ companyId });
-  const agent = agents.find((candidate: AgentRecord) => candidate.name === agentName);
+  const agent = agents.find((candidate: AgentRecord) => candidate.name === agentName) ?? null;
 
   if (!agent) {
     ctx.logger.warn("Agent not found for step", {
       agentName,
       companyId,
+      runId: stepRunRecord.data.runId,
       stepId: stepDef.id,
       workflowName,
     });
-    return;
+    return stepRunRecord;
   }
 
+  const prompt = `workflow:${workflowName}/step:${stepDef.id} — "${stepDef.title}" is ready. Please proceed with the assigned task.`;
+  const reason = `workflow:${workflowName}/step:${stepDef.id}`;
+  const sessionMode = getStepMetadata(stepDef).sessionMode === "reuse" ? "reuse" : "fresh";
+
+  if (sessionMode === "reuse") {
+    let sessionId = typeof stepRunRecord.data.sessionId === "string" && stepRunRecord.data.sessionId.trim()
+      ? stepRunRecord.data.sessionId.trim()
+      : "";
+
+    if (!sessionId) {
+      const session = await ctx.agents.sessions.create(agent.id, companyId, {
+        reason,
+      });
+      sessionId = session.sessionId;
+      stepRunRecord = toWorkflowStepRunRecord(await updateStepRun(ctx, stepRunRecord.id, {
+        sessionId,
+      }));
+    }
+
+    await ctx.agents.sessions.sendMessage(sessionId, companyId, {
+      prompt,
+      reason,
+    });
+
+    ctx.logger.info("Sent workflow step prompt via agent session", {
+      agentId: agent.id,
+      agentName,
+      companyId,
+      runId: stepRunRecord.data.runId,
+      sessionId,
+      stepId: stepDef.id,
+      workflowName,
+    });
+    return stepRunRecord;
+  }
+
   await ctx.agents.invoke(agent.id, companyId, {
-    prompt: `workflow:${workflowName}/step:${stepDef.id} — "${stepDef.title}" is ready. Please proceed with the assigned task.`,
-    reason: `workflow:${workflowName}/step:${stepDef.id}`,
+    prompt,
+    reason,
   });
 
   ctx.logger.info("Invoked agent for workflow step", {
     agentId: agent.id,
     agentName,
     companyId,
+    runId: stepRunRecord.data.runId,
     stepId: stepDef.id,
     workflowName,
   });
+
+  return stepRunRecord;
 }
 
 async function activateBacklogStep(
   ctx: PluginContext,
-  stepRunRecord: WorkflowStepRecord,
+  stepRunRecord: WorkflowStepRunRecord,
   stepDef: WorkflowStep,
   workflowName: string,
   companyId: string,
-): Promise<void> {
+  options?: { parentIssueId?: string },
+): Promise<WorkflowStepRunRecord> {
   if (stepRunRecord.data.status !== STEP_STATUSES.backlog) {
-    return;
+    return stepRunRecord;
   }
 
-  const updated = toStepRunRecord(await updateStepRun(ctx, stepRunRecord.id, {
+  const resolvedAgent = await resolveStepAgent(
+    ctx,
+    companyId,
+    stepDef,
+    stepRunRecord.data.agentName,
+  );
+
+  let issueId = typeof stepRunRecord.data.issueId === "string" && stepRunRecord.data.issueId.trim()
+    ? stepRunRecord.data.issueId.trim()
+    : "";
+  const stepDescription = getStepDescription(stepDef) ?? `Workflow step: ${stepDef.id}`;
+
+  if (!issueId) {
+    const issue = await ctx.issues.create({
+      assigneeAgentId: resolvedAgent.agentId ?? undefined,
+      companyId,
+      description: stepDescription,
+      parentId: options?.parentIssueId,
+      title: `[${workflowName}] ${stepDef.title}`,
+    });
+    issueId = issue.id;
+  }
+
+  const nextStartedAt = stepRunRecord.data.startedAt ?? new Date().toISOString();
+  let updatedStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, stepRunRecord.id, {
+    agentName: resolvedAgent.agentName ?? stepRunRecord.data.agentName,
+    issueId,
+    startedAt: nextStartedAt,
     status: STEP_STATUSES.todo,
   }));
 
-  await invokeAgentForStep(
+  updatedStepRun = await invokeAgentForStep(
     ctx,
+    updatedStepRun,
     stepDef,
-    updated.data.agentName,
     workflowName,
     companyId,
   );
+
+  return updatedStepRun;
+}
+
+async function startWorkflow(
+  ctx: PluginContext,
+  workflowId: string,
+  companyId: string,
+  options?: {
+    createParentIssue?: boolean;
+    parentIssueId?: string;
+  },
+): Promise<{
+  activatedStepIds: string[];
+  parentIssueId: string | null;
+  runId: string;
+  workflowId: string;
+}> {
+  const workflowDefinition = await getWorkflowDefinition(ctx, workflowId);
+  if (!workflowDefinition) {
+    throw new Error(`Workflow definition not found: ${workflowId}`);
+  }
+
+  const typedWorkflowDefinition = toWorkflowDefinitionRecord(workflowDefinition);
+  if (typedWorkflowDefinition.data.companyId !== companyId) {
+    throw new Error(`Workflow does not belong to company: ${workflowId}`);
+  }
+
+  let parentIssueId = typeof options?.parentIssueId === "string" && options.parentIssueId.trim()
+    ? options.parentIssueId.trim()
+    : "";
+  if (!parentIssueId && options?.createParentIssue) {
+    const parentIssue = await ctx.issues.create({
+      companyId,
+      description: typedWorkflowDefinition.data.description || `Workflow run: ${typedWorkflowDefinition.data.name}`,
+      title: `[${typedWorkflowDefinition.data.name}] Workflow run`,
+    });
+    parentIssueId = parentIssue.id;
+  }
+
+  const workflowRun = toWorkflowRunRecord(await createWorkflowRun(ctx, {
+    companyId,
+    parentIssueId: parentIssueId || undefined,
+    startedAt: new Date().toISOString(),
+    status: RUN_STATUSES.running,
+    workflowId: typedWorkflowDefinition.id,
+    workflowName: typedWorkflowDefinition.data.name,
+  }));
+
+  const agents = await ctx.agents.list({ companyId });
+  const agentsByName = new Map<string, AgentRecord>();
+  for (const agent of agents) {
+    agentsByName.set(agent.name, agent);
+  }
+
+  const pendingRootSteps: Array<{ stepDef: WorkflowStep; stepRun: WorkflowStepRunRecord }> = [];
+  for (const stepDef of typedWorkflowDefinition.data.steps) {
+    const agentNameHint = getStepAgentNameHint(stepDef);
+    const matchedAgent = agentNameHint ? agentsByName.get(agentNameHint) ?? null : null;
+    const resolvedAgent = matchedAgent
+      ? { agentId: matchedAgent.id, agentName: matchedAgent.name }
+      : await resolveStepAgent(ctx, companyId, stepDef, agentNameHint ?? undefined);
+
+    if (!resolvedAgent.agentName) {
+      throw new Error(`Unable to resolve step assignee for "${stepDef.id}"`);
+    }
+
+    const stepRun = toWorkflowStepRunRecord(await createStepRun(ctx, companyId, {
+      agentName: resolvedAgent.agentName,
+      retryCount: 0,
+      runId: workflowRun.id,
+      status: STEP_STATUSES.backlog,
+      stepId: stepDef.id,
+    }));
+
+    if (stepDef.dependsOn.length === 0 && stepDef.triggerOn !== "escalation") {
+      pendingRootSteps.push({ stepDef, stepRun });
+    }
+  }
+
+  const activatedStepIds: string[] = [];
+  for (const pending of pendingRootSteps) {
+    await activateBacklogStep(
+      ctx,
+      pending.stepRun,
+      pending.stepDef,
+      typedWorkflowDefinition.data.name,
+      companyId,
+      { parentIssueId: parentIssueId || undefined },
+    );
+    activatedStepIds.push(pending.stepDef.id);
+  }
+
+  ctx.logger.info("Started workflow run", {
+    activatedStepIds,
+    companyId,
+    parentIssueId: parentIssueId || null,
+    runId: workflowRun.id,
+    workflowId: typedWorkflowDefinition.id,
+    workflowName: typedWorkflowDefinition.data.name,
+  });
+
+  return {
+    activatedStepIds,
+    parentIssueId: parentIssueId || null,
+    runId: workflowRun.id,
+    workflowId: typedWorkflowDefinition.id,
+  };
 }
 
 async function advanceWorkflow(
@@ -208,7 +366,7 @@
   stepRunRecord: PluginEntityRecord,
   companyId: string,
 ): Promise<void> {
-  const completedStepRun = toStepRunRecord(stepRunRecord);
+  const completedStepRun = toWorkflowStepRunRecord(stepRunRecord);
   const workflowRun = await getWorkflowRun(ctx, completedStepRun.data.runId);
 
   if (!workflowRun) {
@@ -237,7 +395,7 @@
   }
 
   const typedWorkflowDefinition = toWorkflowDefinitionRecord(workflowDefinition);
-  const stepRuns = (await listStepRuns(ctx, typedWorkflowRun.id, companyId)).map(toStepRunRecord);
+  const stepRuns = (await listStepRuns(ctx, typedWorkflowRun.id, companyId)).map(toWorkflowStepRunRecord);
   const completed = new Set<string>();
   const failed = new Set<string>();
   const skipped = new Set<string>();
@@ -289,7 +447,7 @@
         continue;
       }
 
-      stepRun = toStepRunRecord(await createStepRun(ctx, companyId, {
+      stepRun = toWorkflowStepRunRecord(await createStepRun(ctx, companyId, {
         agentName: resolvedAgent.agentName,
         retryCount: 0,
         runId: typedWorkflowRun.id,
@@ -305,6 +463,7 @@
       stepDef,
       typedWorkflowRun.data.workflowName,
       companyId,
+      { parentIssueId: typedWorkflowRun.data.parentIssueId },
     );
   }
 
@@ -329,7 +488,7 @@
   ctx: PluginContext,
   workflowRun: WorkflowRunRecord,
   workflowDefinition: WorkflowDefinitionRecord,
-  sourceStepRun: WorkflowStepRecord,
+  sourceStepRun: WorkflowStepRunRecord,
   escalationTargetId: string,
   companyId: string,
 ): Promise<void> {
@@ -338,7 +497,7 @@
     throw new Error(`Escalation target step not found: ${escalationTargetId}`);
   }
 
-  const stepRuns = (await listStepRuns(ctx, workflowRun.id, companyId)).map(toStepRunRecord);
+  const stepRuns = (await listStepRuns(ctx, workflowRun.id, companyId)).map(toWorkflowStepRunRecord);
   let escalationStepRun = stepRuns.find((candidate) => candidate.data.stepId === escalationTargetId) ?? null;
   const resolvedAgent = await resolveStepAgent(
     ctx,
@@ -352,7 +511,7 @@
   }
 
   if (!escalationStepRun) {
-    escalationStepRun = toStepRunRecord(await createStepRun(ctx, companyId, {
+    escalationStepRun = toWorkflowStepRunRecord(await createStepRun(ctx, companyId, {
       agentName: resolvedAgent.agentName,
       retryCount: 0,
       runId: workflowRun.id,
@@ -374,14 +533,14 @@
       title: `${workflowRun.data.workflowName}: ${escalationStep.title}`,
     });
 
-    escalationStepRun = toStepRunRecord(await updateStepRun(ctx, escalationStepRun.id, {
+    escalationStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, escalationStepRun.id, {
       issueId: issue.id,
     }));
   }
 
   const shouldActivate = escalationStepRun.data.status === STEP_STATUSES.backlog;
   if (shouldActivate) {
-    escalationStepRun = toStepRunRecord(await updateStepRun(ctx, escalationStepRun.id, {
+    escalationStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, escalationStepRun.id, {
       status: STEP_STATUSES.todo,
     }));
   }
@@ -389,8 +548,8 @@
   if (shouldActivate) {
     await invokeAgentForStep(
       ctx,
+      escalationStepRun,
       escalationStep,
-      escalationStepRun.data.agentName,
       workflowRun.data.workflowName,
       companyId,
     );
@@ -421,7 +580,7 @@
     return;
   }
 
-  const typedStepRun = toStepRunRecord(stepRunRecord);
+  const typedStepRun = toWorkflowStepRunRecord(stepRunRecord);
   if (TERMINAL_STEP_STATUSES.has(typedStepRun.data.status)) {
     return;
   }
@@ -469,7 +628,7 @@
   if (options.allowRetry) {
     const retryInfo = getRetryInfo(typedWorkflowDefinition.data.steps, typedStepRun.data.stepId);
     if (retryInfo.shouldRetry && typedStepRun.data.retryCount < retryInfo.maxRetries) {
-      const retriedStepRun = toStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
+      const retriedStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
         completedAt: undefined,
         retryCount: typedStepRun.data.retryCount + 1,
         status: STEP_STATUSES.todo,
@@ -477,8 +636,8 @@
 
       await invokeAgentForStep(
         ctx,
+        retriedStepRun,
         stepDef,
-        retriedStepRun.data.agentName,
         typedWorkflowRun.data.workflowName,
         event.companyId,
       );
@@ -496,7 +655,7 @@
   }
 
   if (stepDef.onFailure === "skip") {
-    const skippedStepRun = toStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
+    const skippedStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
       completedAt: new Date().toISOString(),
       status: STEP_STATUSES.skipped,
     }));
@@ -556,7 +715,7 @@
       return;
     }
 
-    const escalatedStepRun = toStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
+    const escalatedStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
       completedAt: new Date().toISOString(),
       status: STEP_STATUSES.escalated,
     }));
@@ -617,6 +776,29 @@
   }
 }
 
+function registerDataHandler(
+  ctx: PluginContext,
+  key: string,
+  handler: (params: Record<string, unknown>) => Promise<unknown>,
+): void {
+  const dataClient = ctx.data as {
+    handle?: (handlerKey: string, handlerFn: (params: Record<string, unknown>) => Promise<unknown>) => void;
+    register?: (handlerKey: string, handlerFn: (params: Record<string, unknown>) => Promise<unknown>) => void;
+  };
+
+  if (typeof dataClient.handle === "function") {
+    dataClient.handle(key, handler);
+    return;
+  }
+
+  if (typeof dataClient.register === "function") {
+    dataClient.register(key, handler);
+    return;
+  }
+
+  throw new Error("Plugin data client does not support handler registration");
+}
+
 const plugin = definePlugin({
   async setup(ctx: PluginContext) {
     ctx.events.on("issue.updated", async (event: PluginEvent) => {
@@ -638,7 +820,7 @@
           return;
         }
 
-        const stepRun = toStepRunRecord(stepRunRecord);
+        const stepRun = toWorkflowStepRunRecord(stepRunRecord);
         const payload = event.payload as { status?: string };
         const issueStatus = typeof payload.status === "string" ? payload.status : undefined;
 
@@ -657,7 +839,7 @@
           return;
         }
 
-        const updatedStepRun = toStepRunRecord(await updateStepRun(ctx, stepRun.id, {
+        const updatedStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, stepRun.id, {
           completedAt: nextStepStatus === STEP_STATUSES.done ? new Date().toISOString() : undefined,
           startedAt: nextStepStatus === STEP_STATUSES.inProgress
             ? stepRun.data.startedAt ?? new Date().toISOString()
@@ -687,11 +869,21 @@
 
     ctx.events.on("issue.created", async (event: PluginEvent) => {
       try {
+        const idempotencyKey = buildIdempotencyKey(event);
+        if (await checkIdempotency(ctx, idempotencyKey, event.companyId)) {
+          return;
+        }
+
         const payload = event.payload as {
           id?: string;
           parentId?: string;
           assigneeAgentId?: string;
         };
+        const issueId = typeof event.entityId === "string" && event.entityId.trim()
+          ? event.entityId.trim()
+          : typeof payload.id === "string" && payload.id.trim()
+            ? payload.id.trim()
+            : "";
 
         const parentId = typeof payload.parentId === "string" && payload.parentId.trim()
           ? payload.parentId.trim()
@@ -700,12 +892,34 @@
           ? payload.assigneeAgentId.trim()
           : "";
 
-        if (parentId || !assigneeAgentId) {
+        if (!issueId || parentId || !assigneeAgentId) {
+          await markIdempotency(ctx, idempotencyKey, event.companyId);
           return;
         }
 
-        ctx.logger.info("parentId filler: issue created without parent", {
-          issueId: event.entityId ?? payload.id ?? null,
+        const agent = await ctx.agents.get(assigneeAgentId, event.companyId);
+        const metadata = agent?.metadata as Record<string, unknown> | undefined;
+        const defaultParentIssueId = typeof metadata?.defaultParentIssueId === "string" && metadata.defaultParentIssueId.trim()
+          ? metadata.defaultParentIssueId.trim()
+          : "";
+
+        if (!defaultParentIssueId) {
+          await markIdempotency(ctx, idempotencyKey, event.companyId);
+          return;
+        }
+
+        await ctx.issues.update(
+          issueId,
+          { parentId: defaultParentIssueId } as IssueUpdatePatch,
+          event.companyId,
+        );
+        await markIdempotency(ctx, idempotencyKey, event.companyId);
+
+        ctx.logger.info("parentId filler: populated issue parentId from agent metadata", {
+          assigneeAgentId,
+          companyId: event.companyId,
+          issueId,
+          parentId: defaultParentIssueId,
         });
       } catch (error) {
         ctx.logger.warn("Failed to handle issue.created event", {
@@ -807,7 +1021,7 @@
             ...typedWorkflowRun.data,
           },
           stepRuns: stepRuns.map((record: PluginEntityRecord) => {
-            const stepRun = toStepRunRecord(record);
+            const stepRun = toWorkflowStepRunRecord(record);
             return {
               id: stepRun.id,
               status: stepRun.status,
diff --git a/paperclip-addon/plugins/workflow-engine/src/workflow-store.ts b/paperclip-addon/plugins/workflow-engine/src/workflow-store.ts
index 5871a2082258cebcfe8b69ebafbb0814cf303d50..3d7fb2b3bc75911cfd9093eb3a4054cc73196bb9
--- a/paperclip-addon/plugins/workflow-engine/src/workflow-store.ts
+++ b/paperclip-addon/plugins/workflow-engine/src/workflow-store.ts
@@ -44,7 +44,14 @@
 
 type TypedEntityRecord<T> = Omit<PluginEntityRecord, "data"> & { data: T };
 
-function toTypedRecord<T>(record: PluginEntityRecord): TypedEntityRecord<T> {
+function toTypedRecord<T>(
+  record: PluginEntityRecord,
+  entityType: string,
+): TypedEntityRecord<T> {
+  if (record.entityType !== entityType) {
+    throw new Error(`Expected entity type "${entityType}", got "${record.entityType}"`);
+  }
+
   return record as TypedEntityRecord<T>;
 }
 
@@ -67,13 +74,26 @@
   id: string,
   entityType: string,
 ): Promise<TypedEntityRecord<T> | null> {
+  const listQuery: Parameters<PluginContext["entities"]["list"]>[0] & { id?: string } = {
+    entityType,
+    id,
+    limit: 1,
+  };
+
+  const listed = await ctx.entities.list(listQuery);
+  const listedRecord = listed.find((record) => record.id === id) ?? null;
+  if (listedRecord) {
+    return toTypedRecord<T>(listedRecord, entityType);
+  }
+
+  // Fallback: some host SDK variants ignore `id` on entities.list().
+  // Keep defensive type verification on direct get() until id-filter support is universal.
   const record = await ctx.entities.get(id);
-
-  if (!record || record.entityType !== entityType) {
+  if (!record) {
     return null;
   }
 
-  return toTypedRecord<T>(record);
+  return toTypedRecord<T>(record, entityType);
 }
 
 async function requireEntityByType<T>(
diff --git a/paperclip-addon/plugins/workflow-engine/src/workflow-utils.ts b/paperclip-addon/plugins/workflow-engine/src/workflow-utils.ts
new file mode 100644
index 0000000000000000000000000000000000000000..ec1cf6f6b9aa722b126b58c0a728b4e538ad1494
--- /dev/null
+++ b/paperclip-addon/plugins/workflow-engine/src/workflow-utils.ts
@@ -0,0 +1,62 @@
+import type { PluginEntityRecord } from "@paperclipai/plugin-sdk";
+
+import { STEP_STATUSES } from "./constants.js";
+import type { WorkflowStep } from "./dag-engine.js";
+import type {
+  WorkflowDefinition,
+  WorkflowRun,
+  WorkflowStepRun,
+} from "./workflow-store.js";
+
+export type WorkflowRunRecord = PluginEntityRecord & { data: WorkflowRun };
+export type WorkflowStepRunRecord = PluginEntityRecord & { data: WorkflowStepRun };
+export type WorkflowDefinitionRecord = PluginEntityRecord & { data: WorkflowDefinition };
+type StepAgentMetadata = WorkflowStep & {
+  agent?: string;
+  agentName?: string;
+  assigneeAgentName?: string;
+};
+
+export const TERMINAL_STEP_STATUSES = new Set<string>([
+  STEP_STATUSES.done,
+  STEP_STATUSES.failed,
+  STEP_STATUSES.skipped,
+  STEP_STATUSES.escalated,
+]);
+
+export function toWorkflowRunRecord(record: PluginEntityRecord): WorkflowRunRecord {
+  return record as WorkflowRunRecord;
+}
+
+export function toWorkflowStepRunRecord(record: PluginEntityRecord): WorkflowStepRunRecord {
+  return record as WorkflowStepRunRecord;
+}
+
+export function toWorkflowDefinitionRecord(record: PluginEntityRecord): WorkflowDefinitionRecord {
+  return record as WorkflowDefinitionRecord;
+}
+
+export function findStepDefinition(definition: WorkflowDefinitionRecord, stepId: string): WorkflowStep | null {
+  return definition.data.steps.find((step: WorkflowStep) => step.id === stepId) ?? null;
+}
+
+export function getStepAgentNameHint(stepDef: WorkflowStep): string | null {
+  const stepMeta = stepDef as StepAgentMetadata;
+  const candidates = [stepMeta.agentName, stepMeta.agent, stepMeta.assigneeAgentName];
+
+  for (const candidate of candidates) {
+    if (typeof candidate === "string" && candidate.trim()) {
+      return candidate.trim();
+    }
+  }
+
+  return null;
+}
+
+export function getStepAgentName(stepRun: WorkflowStepRunRecord, stepDef: WorkflowStep): string | null {
+  if (typeof stepRun.data.agentName === "string" && stepRun.data.agentName.trim()) {
+    return stepRun.data.agentName.trim();
+  }
+
+  return getStepAgentNameHint(stepDef);
+}

file update:
diff --git a/paperclip-addon/plugins/workflow-engine/src/dag-engine.ts b/paperclip-addon/plugins/workflow-engine/src/dag-engine.ts
index b59510f0473171038be77277286d3b5c2063e6c5..e7f21ff998cab78b9325a8aeacf4fda9c5e06553
--- a/paperclip-addon/plugins/workflow-engine/src/dag-engine.ts
+++ b/paperclip-addon/plugins/workflow-engine/src/dag-engine.ts
@@ -2,6 +2,7 @@
   id: string;
   title: string;
   dependsOn: string[];
+  sessionMode?: "fresh" | "reuse";
   onFailure?: "retry" | "skip" | "abort_workflow" | "escalate";
   escalateTo?: string;
   maxRetries?: number;
diff --git a/paperclip-addon/plugins/workflow-engine/src/reconciler.ts b/paperclip-addon/plugins/workflow-engine/src/reconciler.ts
index 14ab4e766a835d145a6c2e7b1b564270285cbdc3..d2ab54817bd8185944379d2188bfbdef1722020a
--- a/paperclip-addon/plugins/workflow-engine/src/reconciler.ts
+++ b/paperclip-addon/plugins/workflow-engine/src/reconciler.ts
@@ -8,65 +8,22 @@
   listStepRuns,
   updateStepRun,
   updateWorkflowRun,
-  type WorkflowDefinition,
-  type WorkflowRun,
-  type WorkflowStepRun,
 } from "./workflow-store.js";
 import type { WorkflowStep } from "./dag-engine.js";
-
-type WorkflowRunRecord = PluginEntityRecord & { data: WorkflowRun };
-type WorkflowStepRunRecord = PluginEntityRecord & { data: WorkflowStepRun };
-type WorkflowDefinitionRecord = PluginEntityRecord & { data: WorkflowDefinition };
-type StepAgentMetadata = WorkflowStep & {
-  agent?: string;
-  agentName?: string;
-  assigneeAgentName?: string;
-};
+import {
+  TERMINAL_STEP_STATUSES,
+  findStepDefinition,
+  getStepAgentName,
+  toWorkflowDefinitionRecord,
+  toWorkflowRunRecord,
+  toWorkflowStepRunRecord,
+} from "./workflow-utils.js";
 
 const STALE_THRESHOLD_MS = 300_000;
-const TERMINAL_STEP_STATUSES = new Set<string>([
-  STEP_STATUSES.done,
-  STEP_STATUSES.failed,
-  STEP_STATUSES.skipped,
-  STEP_STATUSES.escalated,
-]);
 
 function isStale(record: PluginEntityRecord, thresholdMs: number): boolean {
   const updatedAt = new Date(record.updatedAt).getTime();
   return Date.now() - updatedAt > thresholdMs;
-}
-
-function toWorkflowRunRecord(record: PluginEntityRecord): WorkflowRunRecord {
-  return record as WorkflowRunRecord;
-}
-
-function toWorkflowStepRunRecord(record: PluginEntityRecord): WorkflowStepRunRecord {
-  return record as WorkflowStepRunRecord;
-}
-
-function toWorkflowDefinitionRecord(record: PluginEntityRecord): WorkflowDefinitionRecord {
-  return record as WorkflowDefinitionRecord;
-}
-
-function findStepDefinition(definition: WorkflowDefinitionRecord, stepId: string): WorkflowStep | null {
-  return definition.data.steps.find((step: WorkflowStep) => step.id === stepId) ?? null;
-}
-
-function getStepAgentName(stepRun: WorkflowStepRunRecord, stepDef: WorkflowStep): string | null {
-  if (typeof stepRun.data.agentName === "string" && stepRun.data.agentName.trim()) {
-    return stepRun.data.agentName.trim();
-  }
-
-  const stepMeta = stepDef as StepAgentMetadata;
-  const candidates = [stepMeta.agentName, stepMeta.agent, stepMeta.assigneeAgentName];
-
-  for (const candidate of candidates) {
-    if (typeof candidate === "string" && candidate.trim()) {
-      return candidate.trim();
-    }
-  }
-
-  return null;
 }
 
 async function invokeAgentByName(
diff --git a/paperclip-addon/plugins/workflow-engine/src/worker.ts b/paperclip-addon/plugins/workflow-engine/src/worker.ts
index c5ac3fbb4ca7f767eb29331bd3630b635449b335..1cd809524f0a14eb2304f9e85d0ad51185891787
--- a/paperclip-addon/plugins/workflow-engine/src/worker.ts
+++ b/paperclip-addon/plugins/workflow-engine/src/worker.ts
@@ -15,6 +15,7 @@
 } from "./dag-engine.js";
 import {
   checkIdempotency,
+  createWorkflowRun,
   createStepRun,
   findStepRunByIssueId,
   getWorkflowDefinition,
@@ -25,63 +26,46 @@
   markIdempotency,
   updateStepRun,
   updateWorkflowRun,
-  type WorkflowDefinition,
-  type WorkflowRun,
   type WorkflowStepRun,
 } from "./workflow-store.js";
+import {
+  TERMINAL_STEP_STATUSES,
+  findStepDefinition,
+  getStepAgentName,
+  getStepAgentNameHint,
+  toWorkflowDefinitionRecord,
+  toWorkflowRunRecord,
+  toWorkflowStepRunRecord,
+  type WorkflowDefinitionRecord,
+  type WorkflowRunRecord,
+  type WorkflowStepRunRecord,
+} from "./workflow-utils.js";
 
-type WorkflowStepRecord = PluginEntityRecord & { data: WorkflowStepRun };
-type WorkflowRunRecord = PluginEntityRecord & { data: WorkflowRun };
-type WorkflowDefinitionRecord = PluginEntityRecord & { data: WorkflowDefinition };
 type WorkflowStepMetadata = WorkflowStep & {
   description?: string;
-  agent?: string;
-  agentName?: string;
-  assigneeAgentName?: string;
   assigneeAgentId?: string;
+  sessionMode?: "fresh" | "reuse";
 };
 type AgentRecord = Awaited<ReturnType<PluginContext["agents"]["list"]>>[number];
+type IssueUpdatePatch = Parameters<PluginContext["issues"]["update"]>[1];
 type ReconcilerModule = {
   reconcileStuckSteps?: (ctx: PluginContext) => Promise<void>;
 };
 
-const TERMINAL_STEP_STATUSES = new Set<string>([
-  STEP_STATUSES.done,
-  STEP_STATUSES.failed,
-  STEP_STATUSES.skipped,
-  STEP_STATUSES.escalated,
-]);
-
 function summarizeError(error: unknown): string {
   if (error instanceof Error) {
     return error.message;
   }
 
   return String(error);
-}
-
-function toStepRunRecord(record: PluginEntityRecord): WorkflowStepRecord {
-  return record as WorkflowStepRecord;
 }
 
-function toWorkflowRunRecord(record: PluginEntityRecord): WorkflowRunRecord {
-  return record as WorkflowRunRecord;
-}
-
-function toWorkflowDefinitionRecord(record: PluginEntityRecord): WorkflowDefinitionRecord {
-  return record as WorkflowDefinitionRecord;
-}
-
 function buildIdempotencyKey(event: PluginEvent): string {
   return `${event.eventType}:${event.eventId}`;
 }
 
 function getStepMetadata(stepDef: WorkflowStep): WorkflowStepMetadata {
   return stepDef as WorkflowStepMetadata;
-}
-
-function findStepDefinition(definition: WorkflowDefinitionRecord, stepId: string): WorkflowStep | null {
-  return definition.data.steps.find((step: WorkflowStep) => step.id === stepId) ?? null;
 }
 
 function getStepDescription(stepDef: WorkflowStep): string | undefined {
@@ -89,19 +73,6 @@
   return typeof description === "string" && description.trim() ? description.trim() : undefined;
 }
 
-function getStepAgentNameHint(stepDef: WorkflowStep): string | null {
-  const meta = getStepMetadata(stepDef);
-  const candidates = [meta.agentName, meta.agent, meta.assigneeAgentName];
-
-  for (const candidate of candidates) {
-    if (typeof candidate === "string" && candidate.trim()) {
-      return candidate.trim();
-    }
-  }
-
-  return null;
-}
-
 function getStepAgentIdHint(stepDef: WorkflowStep): string | null {
   const assigneeAgentId = getStepMetadata(stepDef).assigneeAgentId;
   if (typeof assigneeAgentId !== "string" || !assigneeAgentId.trim()) {
@@ -147,60 +118,247 @@
 
 async function invokeAgentForStep(
   ctx: PluginContext,
+  stepRunRecord: WorkflowStepRunRecord,
   stepDef: WorkflowStep,
-  agentName: string,
   workflowName: string,
   companyId: string,
-): Promise<void> {
+): Promise<WorkflowStepRunRecord> {
+  const agentName = getStepAgentName(stepRunRecord, stepDef);
+  if (!agentName) {
+    ctx.logger.warn("Agent name is missing for workflow step", {
+      companyId,
+      runId: stepRunRecord.data.runId,
+      stepId: stepDef.id,
+      workflowName,
+    });
+    return stepRunRecord;
+  }
+
   const agents = await ctx.agents.list({ companyId });
-  const agent = agents.find((candidate: AgentRecord) => candidate.name === agentName);
+  const agent = agents.find((candidate: AgentRecord) => candidate.name === agentName) ?? null;
 
   if (!agent) {
     ctx.logger.warn("Agent not found for step", {
       agentName,
       companyId,
+      runId: stepRunRecord.data.runId,
       stepId: stepDef.id,
       workflowName,
     });
-    return;
+    return stepRunRecord;
   }
 
+  const prompt = `workflow:${workflowName}/step:${stepDef.id} — "${stepDef.title}" is ready. Please proceed with the assigned task.`;
+  const reason = `workflow:${workflowName}/step:${stepDef.id}`;
+  const sessionMode = getStepMetadata(stepDef).sessionMode === "reuse" ? "reuse" : "fresh";
+
+  if (sessionMode === "reuse") {
+    let sessionId = typeof stepRunRecord.data.sessionId === "string" && stepRunRecord.data.sessionId.trim()
+      ? stepRunRecord.data.sessionId.trim()
+      : "";
+
+    if (!sessionId) {
+      const session = await ctx.agents.sessions.create(agent.id, companyId, {
+        reason,
+      });
+      sessionId = session.sessionId;
+      stepRunRecord = toWorkflowStepRunRecord(await updateStepRun(ctx, stepRunRecord.id, {
+        sessionId,
+      }));
+    }
+
+    await ctx.agents.sessions.sendMessage(sessionId, companyId, {
+      prompt,
+      reason,
+    });
+
+    ctx.logger.info("Sent workflow step prompt via agent session", {
+      agentId: agent.id,
+      agentName,
+      companyId,
+      runId: stepRunRecord.data.runId,
+      sessionId,
+      stepId: stepDef.id,
+      workflowName,
+    });
+    return stepRunRecord;
+  }
+
   await ctx.agents.invoke(agent.id, companyId, {
-    prompt: `workflow:${workflowName}/step:${stepDef.id} — "${stepDef.title}" is ready. Please proceed with the assigned task.`,
-    reason: `workflow:${workflowName}/step:${stepDef.id}`,
+    prompt,
+    reason,
   });
 
   ctx.logger.info("Invoked agent for workflow step", {
     agentId: agent.id,
     agentName,
     companyId,
+    runId: stepRunRecord.data.runId,
     stepId: stepDef.id,
     workflowName,
   });
+
+  return stepRunRecord;
 }
 
 async function activateBacklogStep(
   ctx: PluginContext,
-  stepRunRecord: WorkflowStepRecord,
+  stepRunRecord: WorkflowStepRunRecord,
   stepDef: WorkflowStep,
   workflowName: string,
   companyId: string,
-): Promise<void> {
+  options?: { parentIssueId?: string },
+): Promise<WorkflowStepRunRecord> {
   if (stepRunRecord.data.status !== STEP_STATUSES.backlog) {
-    return;
+    return stepRunRecord;
   }
 
-  const updated = toStepRunRecord(await updateStepRun(ctx, stepRunRecord.id, {
+  const resolvedAgent = await resolveStepAgent(
+    ctx,
+    companyId,
+    stepDef,
+    stepRunRecord.data.agentName,
+  );
+
+  let issueId = typeof stepRunRecord.data.issueId === "string" && stepRunRecord.data.issueId.trim()
+    ? stepRunRecord.data.issueId.trim()
+    : "";
+  const stepDescription = getStepDescription(stepDef) ?? `Workflow step: ${stepDef.id}`;
+
+  if (!issueId) {
+    const issue = await ctx.issues.create({
+      assigneeAgentId: resolvedAgent.agentId ?? undefined,
+      companyId,
+      description: stepDescription,
+      parentId: options?.parentIssueId,
+      title: `[${workflowName}] ${stepDef.title}`,
+    });
+    issueId = issue.id;
+  }
+
+  const nextStartedAt = stepRunRecord.data.startedAt ?? new Date().toISOString();
+  let updatedStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, stepRunRecord.id, {
+    agentName: resolvedAgent.agentName ?? stepRunRecord.data.agentName,
+    issueId,
+    startedAt: nextStartedAt,
     status: STEP_STATUSES.todo,
   }));
 
-  await invokeAgentForStep(
+  updatedStepRun = await invokeAgentForStep(
     ctx,
+    updatedStepRun,
     stepDef,
-    updated.data.agentName,
     workflowName,
     companyId,
   );
+
+  return updatedStepRun;
+}
+
+async function startWorkflow(
+  ctx: PluginContext,
+  workflowId: string,
+  companyId: string,
+  options?: {
+    createParentIssue?: boolean;
+    parentIssueId?: string;
+  },
+): Promise<{
+  activatedStepIds: string[];
+  parentIssueId: string | null;
+  runId: string;
+  workflowId: string;
+}> {
+  const workflowDefinition = await getWorkflowDefinition(ctx, workflowId);
+  if (!workflowDefinition) {
+    throw new Error(`Workflow definition not found: ${workflowId}`);
+  }
+
+  const typedWorkflowDefinition = toWorkflowDefinitionRecord(workflowDefinition);
+  if (typedWorkflowDefinition.data.companyId !== companyId) {
+    throw new Error(`Workflow does not belong to company: ${workflowId}`);
+  }
+
+  let parentIssueId = typeof options?.parentIssueId === "string" && options.parentIssueId.trim()
+    ? options.parentIssueId.trim()
+    : "";
+  if (!parentIssueId && options?.createParentIssue) {
+    const parentIssue = await ctx.issues.create({
+      companyId,
+      description: typedWorkflowDefinition.data.description || `Workflow run: ${typedWorkflowDefinition.data.name}`,
+      title: `[${typedWorkflowDefinition.data.name}] Workflow run`,
+    });
+    parentIssueId = parentIssue.id;
+  }
+
+  const workflowRun = toWorkflowRunRecord(await createWorkflowRun(ctx, {
+    companyId,
+    parentIssueId: parentIssueId || undefined,
+    startedAt: new Date().toISOString(),
+    status: RUN_STATUSES.running,
+    workflowId: typedWorkflowDefinition.id,
+    workflowName: typedWorkflowDefinition.data.name,
+  }));
+
+  const agents = await ctx.agents.list({ companyId });
+  const agentsByName = new Map<string, AgentRecord>();
+  for (const agent of agents) {
+    agentsByName.set(agent.name, agent);
+  }
+
+  const pendingRootSteps: Array<{ stepDef: WorkflowStep; stepRun: WorkflowStepRunRecord }> = [];
+  for (const stepDef of typedWorkflowDefinition.data.steps) {
+    const agentNameHint = getStepAgentNameHint(stepDef);
+    const matchedAgent = agentNameHint ? agentsByName.get(agentNameHint) ?? null : null;
+    const resolvedAgent = matchedAgent
+      ? { agentId: matchedAgent.id, agentName: matchedAgent.name }
+      : await resolveStepAgent(ctx, companyId, stepDef, agentNameHint ?? undefined);
+
+    if (!resolvedAgent.agentName) {
+      throw new Error(`Unable to resolve step assignee for "${stepDef.id}"`);
+    }
+
+    const stepRun = toWorkflowStepRunRecord(await createStepRun(ctx, companyId, {
+      agentName: resolvedAgent.agentName,
+      retryCount: 0,
+      runId: workflowRun.id,
+      status: STEP_STATUSES.backlog,
+      stepId: stepDef.id,
+    }));
+
+    if (stepDef.dependsOn.length === 0 && stepDef.triggerOn !== "escalation") {
+      pendingRootSteps.push({ stepDef, stepRun });
+    }
+  }
+
+  const activatedStepIds: string[] = [];
+  for (const pending of pendingRootSteps) {
+    await activateBacklogStep(
+      ctx,
+      pending.stepRun,
+      pending.stepDef,
+      typedWorkflowDefinition.data.name,
+      companyId,
+      { parentIssueId: parentIssueId || undefined },
+    );
+    activatedStepIds.push(pending.stepDef.id);
+  }
+
+  ctx.logger.info("Started workflow run", {
+    activatedStepIds,
+    companyId,
+    parentIssueId: parentIssueId || null,
+    runId: workflowRun.id,
+    workflowId: typedWorkflowDefinition.id,
+    workflowName: typedWorkflowDefinition.data.name,
+  });
+
+  return {
+    activatedStepIds,
+    parentIssueId: parentIssueId || null,
+    runId: workflowRun.id,
+    workflowId: typedWorkflowDefinition.id,
+  };
 }
 
 async function advanceWorkflow(
@@ -208,7 +366,7 @@
   stepRunRecord: PluginEntityRecord,
   companyId: string,
 ): Promise<void> {
-  const completedStepRun = toStepRunRecord(stepRunRecord);
+  const completedStepRun = toWorkflowStepRunRecord(stepRunRecord);
   const workflowRun = await getWorkflowRun(ctx, completedStepRun.data.runId);
 
   if (!workflowRun) {
@@ -237,7 +395,7 @@
   }
 
   const typedWorkflowDefinition = toWorkflowDefinitionRecord(workflowDefinition);
-  const stepRuns = (await listStepRuns(ctx, typedWorkflowRun.id, companyId)).map(toStepRunRecord);
+  const stepRuns = (await listStepRuns(ctx, typedWorkflowRun.id, companyId)).map(toWorkflowStepRunRecord);
   const completed = new Set<string>();
   const failed = new Set<string>();
   const skipped = new Set<string>();
@@ -289,7 +447,7 @@
         continue;
       }
 
-      stepRun = toStepRunRecord(await createStepRun(ctx, companyId, {
+      stepRun = toWorkflowStepRunRecord(await createStepRun(ctx, companyId, {
         agentName: resolvedAgent.agentName,
         retryCount: 0,
         runId: typedWorkflowRun.id,
@@ -305,6 +463,7 @@
       stepDef,
       typedWorkflowRun.data.workflowName,
       companyId,
+      { parentIssueId: typedWorkflowRun.data.parentIssueId },
     );
   }
 
@@ -329,7 +488,7 @@
   ctx: PluginContext,
   workflowRun: WorkflowRunRecord,
   workflowDefinition: WorkflowDefinitionRecord,
-  sourceStepRun: WorkflowStepRecord,
+  sourceStepRun: WorkflowStepRunRecord,
   escalationTargetId: string,
   companyId: string,
 ): Promise<void> {
@@ -338,7 +497,7 @@
     throw new Error(`Escalation target step not found: ${escalationTargetId}`);
   }
 
-  const stepRuns = (await listStepRuns(ctx, workflowRun.id, companyId)).map(toStepRunRecord);
+  const stepRuns = (await listStepRuns(ctx, workflowRun.id, companyId)).map(toWorkflowStepRunRecord);
   let escalationStepRun = stepRuns.find((candidate) => candidate.data.stepId === escalationTargetId) ?? null;
   const resolvedAgent = await resolveStepAgent(
     ctx,
@@ -352,7 +511,7 @@
   }
 
   if (!escalationStepRun) {
-    escalationStepRun = toStepRunRecord(await createStepRun(ctx, companyId, {
+    escalationStepRun = toWorkflowStepRunRecord(await createStepRun(ctx, companyId, {
       agentName: resolvedAgent.agentName,
       retryCount: 0,
       runId: workflowRun.id,
@@ -374,14 +533,14 @@
       title: `${workflowRun.data.workflowName}: ${escalationStep.title}`,
     });
 
-    escalationStepRun = toStepRunRecord(await updateStepRun(ctx, escalationStepRun.id, {
+    escalationStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, escalationStepRun.id, {
       issueId: issue.id,
     }));
   }
 
   const shouldActivate = escalationStepRun.data.status === STEP_STATUSES.backlog;
   if (shouldActivate) {
-    escalationStepRun = toStepRunRecord(await updateStepRun(ctx, escalationStepRun.id, {
+    escalationStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, escalationStepRun.id, {
       status: STEP_STATUSES.todo,
     }));
   }
@@ -389,8 +548,8 @@
   if (shouldActivate) {
     await invokeAgentForStep(
       ctx,
+      escalationStepRun,
       escalationStep,
-      escalationStepRun.data.agentName,
       workflowRun.data.workflowName,
       companyId,
     );
@@ -421,7 +580,7 @@
     return;
   }
 
-  const typedStepRun = toStepRunRecord(stepRunRecord);
+  const typedStepRun = toWorkflowStepRunRecord(stepRunRecord);
   if (TERMINAL_STEP_STATUSES.has(typedStepRun.data.status)) {
     return;
   }
@@ -469,7 +628,7 @@
   if (options.allowRetry) {
     const retryInfo = getRetryInfo(typedWorkflowDefinition.data.steps, typedStepRun.data.stepId);
     if (retryInfo.shouldRetry && typedStepRun.data.retryCount < retryInfo.maxRetries) {
-      const retriedStepRun = toStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
+      const retriedStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
         completedAt: undefined,
         retryCount: typedStepRun.data.retryCount + 1,
         status: STEP_STATUSES.todo,
@@ -477,8 +636,8 @@
 
       await invokeAgentForStep(
         ctx,
+        retriedStepRun,
         stepDef,
-        retriedStepRun.data.agentName,
         typedWorkflowRun.data.workflowName,
         event.companyId,
       );
@@ -496,7 +655,7 @@
   }
 
   if (stepDef.onFailure === "skip") {
-    const skippedStepRun = toStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
+    const skippedStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
       completedAt: new Date().toISOString(),
       status: STEP_STATUSES.skipped,
     }));
@@ -556,7 +715,7 @@
       return;
     }
 
-    const escalatedStepRun = toStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
+    const escalatedStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
       completedAt: new Date().toISOString(),
       status: STEP_STATUSES.escalated,
     }));
@@ -617,6 +776,29 @@
   }
 }
 
+function registerDataHandler(
+  ctx: PluginContext,
+  key: string,
+  handler: (params: Record<string, unknown>) => Promise<unknown>,
+): void {
+  const dataClient = ctx.data as {
+    handle?: (handlerKey: string, handlerFn: (params: Record<string, unknown>) => Promise<unknown>) => void;
+    register?: (handlerKey: string, handlerFn: (params: Record<string, unknown>) => Promise<unknown>) => void;
+  };
+
+  if (typeof dataClient.handle === "function") {
+    dataClient.handle(key, handler);
+    return;
+  }
+
+  if (typeof dataClient.register === "function") {
+    dataClient.register(key, handler);
+    return;
+  }
+
+  throw new Error("Plugin data client does not support handler registration");
+}
+
 const plugin = definePlugin({
   async setup(ctx: PluginContext) {
     ctx.events.on("issue.updated", async (event: PluginEvent) => {
@@ -638,7 +820,7 @@
           return;
         }
 
-        const stepRun = toStepRunRecord(stepRunRecord);
+        const stepRun = toWorkflowStepRunRecord(stepRunRecord);
         const payload = event.payload as { status?: string };
         const issueStatus = typeof payload.status === "string" ? payload.status : undefined;
 
@@ -657,7 +839,7 @@
           return;
         }
 
-        const updatedStepRun = toStepRunRecord(await updateStepRun(ctx, stepRun.id, {
+        const updatedStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, stepRun.id, {
           completedAt: nextStepStatus === STEP_STATUSES.done ? new Date().toISOString() : undefined,
           startedAt: nextStepStatus === STEP_STATUSES.inProgress
             ? stepRun.data.startedAt ?? new Date().toISOString()
@@ -687,11 +869,21 @@
 
     ctx.events.on("issue.created", async (event: PluginEvent) => {
       try {
+        const idempotencyKey = buildIdempotencyKey(event);
+        if (await checkIdempotency(ctx, idempotencyKey, event.companyId)) {
+          return;
+        }
+
         const payload = event.payload as {
           id?: string;
           parentId?: string;
           assigneeAgentId?: string;
         };
+        const issueId = typeof event.entityId === "string" && event.entityId.trim()
+          ? event.entityId.trim()
+          : typeof payload.id === "string" && payload.id.trim()
+            ? payload.id.trim()
+            : "";
 
         const parentId = typeof payload.parentId === "string" && payload.parentId.trim()
           ? payload.parentId.trim()
@@ -700,12 +892,34 @@
           ? payload.assigneeAgentId.trim()
           : "";
 
-        if (parentId || !assigneeAgentId) {
+        if (!issueId || parentId || !assigneeAgentId) {
+          await markIdempotency(ctx, idempotencyKey, event.companyId);
           return;
         }
 
-        ctx.logger.info("parentId filler: issue created without parent", {
-          issueId: event.entityId ?? payload.id ?? null,
+        const agent = await ctx.agents.get(assigneeAgentId, event.companyId);
+        const metadata = agent?.metadata as Record<string, unknown> | undefined;
+        const defaultParentIssueId = typeof metadata?.defaultParentIssueId === "string" && metadata.defaultParentIssueId.trim()
+          ? metadata.defaultParentIssueId.trim()
+          : "";
+
+        if (!defaultParentIssueId) {
+          await markIdempotency(ctx, idempotencyKey, event.companyId);
+          return;
+        }
+
+        await ctx.issues.update(
+          issueId,
+          { parentId: defaultParentIssueId } as IssueUpdatePatch,
+          event.companyId,
+        );
+        await markIdempotency(ctx, idempotencyKey, event.companyId);
+
+        ctx.logger.info("parentId filler: populated issue parentId from agent metadata", {
+          assigneeAgentId,
+          companyId: event.companyId,
+          issueId,
+          parentId: defaultParentIssueId,
         });
       } catch (error) {
         ctx.logger.warn("Failed to handle issue.created event", {
@@ -807,7 +1021,7 @@
             ...typedWorkflowRun.data,
           },
           stepRuns: stepRuns.map((record: PluginEntityRecord) => {
-            const stepRun = toStepRunRecord(record);
+            const stepRun = toWorkflowStepRunRecord(record);
             return {
               id: stepRun.id,
               status: stepRun.status,
diff --git a/paperclip-addon/plugins/workflow-engine/src/workflow-store.ts b/paperclip-addon/plugins/workflow-engine/src/workflow-store.ts
index 5871a2082258cebcfe8b69ebafbb0814cf303d50..3d7fb2b3bc75911cfd9093eb3a4054cc73196bb9
--- a/paperclip-addon/plugins/workflow-engine/src/workflow-store.ts
+++ b/paperclip-addon/plugins/workflow-engine/src/workflow-store.ts
@@ -44,7 +44,14 @@
 
 type TypedEntityRecord<T> = Omit<PluginEntityRecord, "data"> & { data: T };
 
-function toTypedRecord<T>(record: PluginEntityRecord): TypedEntityRecord<T> {
+function toTypedRecord<T>(
+  record: PluginEntityRecord,
+  entityType: string,
+): TypedEntityRecord<T> {
+  if (record.entityType !== entityType) {
+    throw new Error(`Expected entity type "${entityType}", got "${record.entityType}"`);
+  }
+
   return record as TypedEntityRecord<T>;
 }
 
@@ -67,13 +74,26 @@
   id: string,
   entityType: string,
 ): Promise<TypedEntityRecord<T> | null> {
+  const listQuery: Parameters<PluginContext["entities"]["list"]>[0] & { id?: string } = {
+    entityType,
+    id,
+    limit: 1,
+  };
+
+  const listed = await ctx.entities.list(listQuery);
+  const listedRecord = listed.find((record) => record.id === id) ?? null;
+  if (listedRecord) {
+    return toTypedRecord<T>(listedRecord, entityType);
+  }
+
+  // Fallback: some host SDK variants ignore `id` on entities.list().
+  // Keep defensive type verification on direct get() until id-filter support is universal.
   const record = await ctx.entities.get(id);
-
-  if (!record || record.entityType !== entityType) {
+  if (!record) {
     return null;
   }
 
-  return toTypedRecord<T>(record);
+  return toTypedRecord<T>(record, entityType);
 }
 
 async function requireEntityByType<T>(
diff --git a/paperclip-addon/plugins/workflow-engine/src/workflow-utils.ts b/paperclip-addon/plugins/workflow-engine/src/workflow-utils.ts
new file mode 100644
index 0000000000000000000000000000000000000000..ec1cf6f6b9aa722b126b58c0a728b4e538ad1494
--- /dev/null
+++ b/paperclip-addon/plugins/workflow-engine/src/workflow-utils.ts
@@ -0,0 +1,62 @@
+import type { PluginEntityRecord } from "@paperclipai/plugin-sdk";
+
+import { STEP_STATUSES } from "./constants.js";
+import type { WorkflowStep } from "./dag-engine.js";
+import type {
+  WorkflowDefinition,
+  WorkflowRun,
+  WorkflowStepRun,
+} from "./workflow-store.js";
+
+export type WorkflowRunRecord = PluginEntityRecord & { data: WorkflowRun };
+export type WorkflowStepRunRecord = PluginEntityRecord & { data: WorkflowStepRun };
+export type WorkflowDefinitionRecord = PluginEntityRecord & { data: WorkflowDefinition };
+type StepAgentMetadata = WorkflowStep & {
+  agent?: string;
+  agentName?: string;
+  assigneeAgentName?: string;
+};
+
+export const TERMINAL_STEP_STATUSES = new Set<string>([
+  STEP_STATUSES.done,
+  STEP_STATUSES.failed,
+  STEP_STATUSES.skipped,
+  STEP_STATUSES.escalated,
+]);
+
+export function toWorkflowRunRecord(record: PluginEntityRecord): WorkflowRunRecord {
+  return record as WorkflowRunRecord;
+}
+
+export function toWorkflowStepRunRecord(record: PluginEntityRecord): WorkflowStepRunRecord {
+  return record as WorkflowStepRunRecord;
+}
+
+export function toWorkflowDefinitionRecord(record: PluginEntityRecord): WorkflowDefinitionRecord {
+  return record as WorkflowDefinitionRecord;
+}
+
+export function findStepDefinition(definition: WorkflowDefinitionRecord, stepId: string): WorkflowStep | null {
+  return definition.data.steps.find((step: WorkflowStep) => step.id === stepId) ?? null;
+}
+
+export function getStepAgentNameHint(stepDef: WorkflowStep): string | null {
+  const stepMeta = stepDef as StepAgentMetadata;
+  const candidates = [stepMeta.agentName, stepMeta.agent, stepMeta.assigneeAgentName];
+
+  for (const candidate of candidates) {
+    if (typeof candidate === "string" && candidate.trim()) {
+      return candidate.trim();
+    }
+  }
+
+  return null;
+}
+
+export function getStepAgentName(stepRun: WorkflowStepRunRecord, stepDef: WorkflowStep): string | null {
+  if (typeof stepRun.data.agentName === "string" && stepRun.data.agentName.trim()) {
+    return stepRun.data.agentName.trim();
+  }
+
+  return getStepAgentNameHint(stepDef);
+}

file update
M /Users/kwak/Projects/paperclip/paperclip-addon/plugins/workflow-engine/src/worker.ts
@@ -960,3 +960,19 @@
 
-    ctx.data.register("workflow-overview", async (params: Record<string, unknown>) => {
+    registerDataHandler(ctx, "start-workflow", async (params: Record<string, unknown>) => {
+      const workflowId = typeof params.workflowId === "string" ? params.workflowId.trim() : "";
+      const companyId = typeof params.companyId === "string" ? params.companyId.trim() : "";
+      const parentIssueId = typeof params.parentIssueId === "string" ? params.parentIssueId.trim() : "";
+      const createParentIssue = params.createParentIssue === true;
+
+      if (!workflowId || !companyId) {
+        throw new Error("start-workflow requires workflowId and companyId");
+      }
+
+      return await startWorkflow(ctx, workflowId, companyId, {
+        createParentIssue,
+        parentIssueId: parentIssueId || undefined,
+      });
+    });
+
+    registerDataHandler(ctx, "workflow-overview", async (params: Record<string, unknown>) => {
       try {
@@ -998,3 +1014,3 @@
 
-    ctx.data.register("workflow-run-detail", async (params: Record<string, unknown>) => {
+    registerDataHandler(ctx, "workflow-run-detail", async (params: Record<string, unknown>) => {
       try {
apply_patch(auto_approved=true) exited 0 in 86ms:
Success. Updated the following files:
M /Users/kwak/Projects/paperclip/paperclip-addon/plugins/workflow-engine/src/worker.ts
file update:
diff --git a/paperclip-addon/plugins/workflow-engine/src/dag-engine.ts b/paperclip-addon/plugins/workflow-engine/src/dag-engine.ts
index b59510f0473171038be77277286d3b5c2063e6c5..e7f21ff998cab78b9325a8aeacf4fda9c5e06553
--- a/paperclip-addon/plugins/workflow-engine/src/dag-engine.ts
+++ b/paperclip-addon/plugins/workflow-engine/src/dag-engine.ts
@@ -2,6 +2,7 @@
   id: string;
   title: string;
   dependsOn: string[];
+  sessionMode?: "fresh" | "reuse";
   onFailure?: "retry" | "skip" | "abort_workflow" | "escalate";
   escalateTo?: string;
   maxRetries?: number;
diff --git a/paperclip-addon/plugins/workflow-engine/src/reconciler.ts b/paperclip-addon/plugins/workflow-engine/src/reconciler.ts
index 14ab4e766a835d145a6c2e7b1b564270285cbdc3..d2ab54817bd8185944379d2188bfbdef1722020a
--- a/paperclip-addon/plugins/workflow-engine/src/reconciler.ts
+++ b/paperclip-addon/plugins/workflow-engine/src/reconciler.ts
@@ -8,65 +8,22 @@
   listStepRuns,
   updateStepRun,
   updateWorkflowRun,
-  type WorkflowDefinition,
-  type WorkflowRun,
-  type WorkflowStepRun,
 } from "./workflow-store.js";
 import type { WorkflowStep } from "./dag-engine.js";
-
-type WorkflowRunRecord = PluginEntityRecord & { data: WorkflowRun };
-type WorkflowStepRunRecord = PluginEntityRecord & { data: WorkflowStepRun };
-type WorkflowDefinitionRecord = PluginEntityRecord & { data: WorkflowDefinition };
-type StepAgentMetadata = WorkflowStep & {
-  agent?: string;
-  agentName?: string;
-  assigneeAgentName?: string;
-};
+import {
+  TERMINAL_STEP_STATUSES,
+  findStepDefinition,
+  getStepAgentName,
+  toWorkflowDefinitionRecord,
+  toWorkflowRunRecord,
+  toWorkflowStepRunRecord,
+} from "./workflow-utils.js";
 
 const STALE_THRESHOLD_MS = 300_000;
-const TERMINAL_STEP_STATUSES = new Set<string>([
-  STEP_STATUSES.done,
-  STEP_STATUSES.failed,
-  STEP_STATUSES.skipped,
-  STEP_STATUSES.escalated,
-]);
 
 function isStale(record: PluginEntityRecord, thresholdMs: number): boolean {
   const updatedAt = new Date(record.updatedAt).getTime();
   return Date.now() - updatedAt > thresholdMs;
-}
-
-function toWorkflowRunRecord(record: PluginEntityRecord): WorkflowRunRecord {
-  return record as WorkflowRunRecord;
-}
-
-function toWorkflowStepRunRecord(record: PluginEntityRecord): WorkflowStepRunRecord {
-  return record as WorkflowStepRunRecord;
-}
-
-function toWorkflowDefinitionRecord(record: PluginEntityRecord): WorkflowDefinitionRecord {
-  return record as WorkflowDefinitionRecord;
-}
-
-function findStepDefinition(definition: WorkflowDefinitionRecord, stepId: string): WorkflowStep | null {
-  return definition.data.steps.find((step: WorkflowStep) => step.id === stepId) ?? null;
-}
-
-function getStepAgentName(stepRun: WorkflowStepRunRecord, stepDef: WorkflowStep): string | null {
-  if (typeof stepRun.data.agentName === "string" && stepRun.data.agentName.trim()) {
-    return stepRun.data.agentName.trim();
-  }
-
-  const stepMeta = stepDef as StepAgentMetadata;
-  const candidates = [stepMeta.agentName, stepMeta.agent, stepMeta.assigneeAgentName];
-
-  for (const candidate of candidates) {
-    if (typeof candidate === "string" && candidate.trim()) {
-      return candidate.trim();
-    }
-  }
-
-  return null;
 }
 
 async function invokeAgentByName(
diff --git a/paperclip-addon/plugins/workflow-engine/src/worker.ts b/paperclip-addon/plugins/workflow-engine/src/worker.ts
index c5ac3fbb4ca7f767eb29331bd3630b635449b335..2bfb4dd37a3cc1e88b833a49d3e4d39c9abf1505
--- a/paperclip-addon/plugins/workflow-engine/src/worker.ts
+++ b/paperclip-addon/plugins/workflow-engine/src/worker.ts
@@ -15,6 +15,7 @@
 } from "./dag-engine.js";
 import {
   checkIdempotency,
+  createWorkflowRun,
   createStepRun,
   findStepRunByIssueId,
   getWorkflowDefinition,
@@ -25,51 +26,38 @@
   markIdempotency,
   updateStepRun,
   updateWorkflowRun,
-  type WorkflowDefinition,
-  type WorkflowRun,
   type WorkflowStepRun,
 } from "./workflow-store.js";
+import {
+  TERMINAL_STEP_STATUSES,
+  findStepDefinition,
+  getStepAgentName,
+  getStepAgentNameHint,
+  toWorkflowDefinitionRecord,
+  toWorkflowRunRecord,
+  toWorkflowStepRunRecord,
+  type WorkflowDefinitionRecord,
+  type WorkflowRunRecord,
+  type WorkflowStepRunRecord,
+} from "./workflow-utils.js";
 
-type WorkflowStepRecord = PluginEntityRecord & { data: WorkflowStepRun };
-type WorkflowRunRecord = PluginEntityRecord & { data: WorkflowRun };
-type WorkflowDefinitionRecord = PluginEntityRecord & { data: WorkflowDefinition };
 type WorkflowStepMetadata = WorkflowStep & {
   description?: string;
-  agent?: string;
-  agentName?: string;
-  assigneeAgentName?: string;
   assigneeAgentId?: string;
+  sessionMode?: "fresh" | "reuse";
 };
 type AgentRecord = Awaited<ReturnType<PluginContext["agents"]["list"]>>[number];
+type IssueUpdatePatch = Parameters<PluginContext["issues"]["update"]>[1];
 type ReconcilerModule = {
   reconcileStuckSteps?: (ctx: PluginContext) => Promise<void>;
 };
 
-const TERMINAL_STEP_STATUSES = new Set<string>([
-  STEP_STATUSES.done,
-  STEP_STATUSES.failed,
-  STEP_STATUSES.skipped,
-  STEP_STATUSES.escalated,
-]);
-
 function summarizeError(error: unknown): string {
   if (error instanceof Error) {
     return error.message;
   }
 
   return String(error);
-}
-
-function toStepRunRecord(record: PluginEntityRecord): WorkflowStepRecord {
-  return record as WorkflowStepRecord;
-}
-
-function toWorkflowRunRecord(record: PluginEntityRecord): WorkflowRunRecord {
-  return record as WorkflowRunRecord;
-}
-
-function toWorkflowDefinitionRecord(record: PluginEntityRecord): WorkflowDefinitionRecord {
-  return record as WorkflowDefinitionRecord;
 }
 
 function buildIdempotencyKey(event: PluginEvent): string {
@@ -78,10 +66,6 @@
 
 function getStepMetadata(stepDef: WorkflowStep): WorkflowStepMetadata {
   return stepDef as WorkflowStepMetadata;
-}
-
-function findStepDefinition(definition: WorkflowDefinitionRecord, stepId: string): WorkflowStep | null {
-  return definition.data.steps.find((step: WorkflowStep) => step.id === stepId) ?? null;
 }
 
 function getStepDescription(stepDef: WorkflowStep): string | undefined {
@@ -89,19 +73,6 @@
   return typeof description === "string" && description.trim() ? description.trim() : undefined;
 }
 
-function getStepAgentNameHint(stepDef: WorkflowStep): string | null {
-  const meta = getStepMetadata(stepDef);
-  const candidates = [meta.agentName, meta.agent, meta.assigneeAgentName];
-
-  for (const candidate of candidates) {
-    if (typeof candidate === "string" && candidate.trim()) {
-      return candidate.trim();
-    }
-  }
-
-  return null;
-}
-
 function getStepAgentIdHint(stepDef: WorkflowStep): string | null {
   const assigneeAgentId = getStepMetadata(stepDef).assigneeAgentId;
   if (typeof assigneeAgentId !== "string" || !assigneeAgentId.trim()) {
@@ -147,60 +118,247 @@
 
 async function invokeAgentForStep(
   ctx: PluginContext,
+  stepRunRecord: WorkflowStepRunRecord,
   stepDef: WorkflowStep,
-  agentName: string,
   workflowName: string,
   companyId: string,
-): Promise<void> {
+): Promise<WorkflowStepRunRecord> {
+  const agentName = getStepAgentName(stepRunRecord, stepDef);
+  if (!agentName) {
+    ctx.logger.warn("Agent name is missing for workflow step", {
+      companyId,
+      runId: stepRunRecord.data.runId,
+      stepId: stepDef.id,
+      workflowName,
+    });
+    return stepRunRecord;
+  }
+
   const agents = await ctx.agents.list({ companyId });
-  const agent = agents.find((candidate: AgentRecord) => candidate.name === agentName);
+  const agent = agents.find((candidate: AgentRecord) => candidate.name === agentName) ?? null;
 
   if (!agent) {
     ctx.logger.warn("Agent not found for step", {
       agentName,
       companyId,
+      runId: stepRunRecord.data.runId,
       stepId: stepDef.id,
       workflowName,
     });
-    return;
+    return stepRunRecord;
   }
 
+  const prompt = `workflow:${workflowName}/step:${stepDef.id} — "${stepDef.title}" is ready. Please proceed with the assigned task.`;
+  const reason = `workflow:${workflowName}/step:${stepDef.id}`;
+  const sessionMode = getStepMetadata(stepDef).sessionMode === "reuse" ? "reuse" : "fresh";
+
+  if (sessionMode === "reuse") {
+    let sessionId = typeof stepRunRecord.data.sessionId === "string" && stepRunRecord.data.sessionId.trim()
+      ? stepRunRecord.data.sessionId.trim()
+      : "";
+
+    if (!sessionId) {
+      const session = await ctx.agents.sessions.create(agent.id, companyId, {
+        reason,
+      });
+      sessionId = session.sessionId;
+      stepRunRecord = toWorkflowStepRunRecord(await updateStepRun(ctx, stepRunRecord.id, {
+        sessionId,
+      }));
+    }
+
+    await ctx.agents.sessions.sendMessage(sessionId, companyId, {
+      prompt,
+      reason,
+    });
+
+    ctx.logger.info("Sent workflow step prompt via agent session", {
+      agentId: agent.id,
+      agentName,
+      companyId,
+      runId: stepRunRecord.data.runId,
+      sessionId,
+      stepId: stepDef.id,
+      workflowName,
+    });
+    return stepRunRecord;
+  }
+
   await ctx.agents.invoke(agent.id, companyId, {
-    prompt: `workflow:${workflowName}/step:${stepDef.id} — "${stepDef.title}" is ready. Please proceed with the assigned task.`,
-    reason: `workflow:${workflowName}/step:${stepDef.id}`,
+    prompt,
+    reason,
   });
 
   ctx.logger.info("Invoked agent for workflow step", {
     agentId: agent.id,
     agentName,
     companyId,
+    runId: stepRunRecord.data.runId,
     stepId: stepDef.id,
     workflowName,
   });
+
+  return stepRunRecord;
 }
 
 async function activateBacklogStep(
   ctx: PluginContext,
-  stepRunRecord: WorkflowStepRecord,
+  stepRunRecord: WorkflowStepRunRecord,
   stepDef: WorkflowStep,
   workflowName: string,
   companyId: string,
-): Promise<void> {
+  options?: { parentIssueId?: string },
+): Promise<WorkflowStepRunRecord> {
   if (stepRunRecord.data.status !== STEP_STATUSES.backlog) {
-    return;
+    return stepRunRecord;
+  }
+
+  const resolvedAgent = await resolveStepAgent(
+    ctx,
+    companyId,
+    stepDef,
+    stepRunRecord.data.agentName,
+  );
+
+  let issueId = typeof stepRunRecord.data.issueId === "string" && stepRunRecord.data.issueId.trim()
+    ? stepRunRecord.data.issueId.trim()
+    : "";
+  const stepDescription = getStepDescription(stepDef) ?? `Workflow step: ${stepDef.id}`;
+
+  if (!issueId) {
+    const issue = await ctx.issues.create({
+      assigneeAgentId: resolvedAgent.agentId ?? undefined,
+      companyId,
+      description: stepDescription,
+      parentId: options?.parentIssueId,
+      title: `[${workflowName}] ${stepDef.title}`,
+    });
+    issueId = issue.id;
   }
 
-  const updated = toStepRunRecord(await updateStepRun(ctx, stepRunRecord.id, {
+  const nextStartedAt = stepRunRecord.data.startedAt ?? new Date().toISOString();
+  let updatedStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, stepRunRecord.id, {
+    agentName: resolvedAgent.agentName ?? stepRunRecord.data.agentName,
+    issueId,
+    startedAt: nextStartedAt,
     status: STEP_STATUSES.todo,
   }));
 
-  await invokeAgentForStep(
+  updatedStepRun = await invokeAgentForStep(
     ctx,
+    updatedStepRun,
     stepDef,
-    updated.data.agentName,
     workflowName,
     companyId,
   );
+
+  return updatedStepRun;
+}
+
+async function startWorkflow(
+  ctx: PluginContext,
+  workflowId: string,
+  companyId: string,
+  options?: {
+    createParentIssue?: boolean;
+    parentIssueId?: string;
+  },
+): Promise<{
+  activatedStepIds: string[];
+  parentIssueId: string | null;
+  runId: string;
+  workflowId: string;
+}> {
+  const workflowDefinition = await getWorkflowDefinition(ctx, workflowId);
+  if (!workflowDefinition) {
+    throw new Error(`Workflow definition not found: ${workflowId}`);
+  }
+
+  const typedWorkflowDefinition = toWorkflowDefinitionRecord(workflowDefinition);
+  if (typedWorkflowDefinition.data.companyId !== companyId) {
+    throw new Error(`Workflow does not belong to company: ${workflowId}`);
+  }
+
+  let parentIssueId = typeof options?.parentIssueId === "string" && options.parentIssueId.trim()
+    ? options.parentIssueId.trim()
+    : "";
+  if (!parentIssueId && options?.createParentIssue) {
+    const parentIssue = await ctx.issues.create({
+      companyId,
+      description: typedWorkflowDefinition.data.description || `Workflow run: ${typedWorkflowDefinition.data.name}`,
+      title: `[${typedWorkflowDefinition.data.name}] Workflow run`,
+    });
+    parentIssueId = parentIssue.id;
+  }
+
+  const workflowRun = toWorkflowRunRecord(await createWorkflowRun(ctx, {
+    companyId,
+    parentIssueId: parentIssueId || undefined,
+    startedAt: new Date().toISOString(),
+    status: RUN_STATUSES.running,
+    workflowId: typedWorkflowDefinition.id,
+    workflowName: typedWorkflowDefinition.data.name,
+  }));
+
+  const agents = await ctx.agents.list({ companyId });
+  const agentsByName = new Map<string, AgentRecord>();
+  for (const agent of agents) {
+    agentsByName.set(agent.name, agent);
+  }
+
+  const pendingRootSteps: Array<{ stepDef: WorkflowStep; stepRun: WorkflowStepRunRecord }> = [];
+  for (const stepDef of typedWorkflowDefinition.data.steps) {
+    const agentNameHint = getStepAgentNameHint(stepDef);
+    const matchedAgent = agentNameHint ? agentsByName.get(agentNameHint) ?? null : null;
+    const resolvedAgent = matchedAgent
+      ? { agentId: matchedAgent.id, agentName: matchedAgent.name }
+      : await resolveStepAgent(ctx, companyId, stepDef, agentNameHint ?? undefined);
+
+    if (!resolvedAgent.agentName) {
+      throw new Error(`Unable to resolve step assignee for "${stepDef.id}"`);
+    }
+
+    const stepRun = toWorkflowStepRunRecord(await createStepRun(ctx, companyId, {
+      agentName: resolvedAgent.agentName,
+      retryCount: 0,
+      runId: workflowRun.id,
+      status: STEP_STATUSES.backlog,
+      stepId: stepDef.id,
+    }));
+
+    if (stepDef.dependsOn.length === 0 && stepDef.triggerOn !== "escalation") {
+      pendingRootSteps.push({ stepDef, stepRun });
+    }
+  }
+
+  const activatedStepIds: string[] = [];
+  for (const pending of pendingRootSteps) {
+    await activateBacklogStep(
+      ctx,
+      pending.stepRun,
+      pending.stepDef,
+      typedWorkflowDefinition.data.name,
+      companyId,
+      { parentIssueId: parentIssueId || undefined },
+    );
+    activatedStepIds.push(pending.stepDef.id);
+  }
+
+  ctx.logger.info("Started workflow run", {
+    activatedStepIds,
+    companyId,
+    parentIssueId: parentIssueId || null,
+    runId: workflowRun.id,
+    workflowId: typedWorkflowDefinition.id,
+    workflowName: typedWorkflowDefinition.data.name,
+  });
+
+  return {
+    activatedStepIds,
+    parentIssueId: parentIssueId || null,
+    runId: workflowRun.id,
+    workflowId: typedWorkflowDefinition.id,
+  };
 }
 
 async function advanceWorkflow(
@@ -208,7 +366,7 @@
   stepRunRecord: PluginEntityRecord,
   companyId: string,
 ): Promise<void> {
-  const completedStepRun = toStepRunRecord(stepRunRecord);
+  const completedStepRun = toWorkflowStepRunRecord(stepRunRecord);
   const workflowRun = await getWorkflowRun(ctx, completedStepRun.data.runId);
 
   if (!workflowRun) {
@@ -237,7 +395,7 @@
   }
 
   const typedWorkflowDefinition = toWorkflowDefinitionRecord(workflowDefinition);
-  const stepRuns = (await listStepRuns(ctx, typedWorkflowRun.id, companyId)).map(toStepRunRecord);
+  const stepRuns = (await listStepRuns(ctx, typedWorkflowRun.id, companyId)).map(toWorkflowStepRunRecord);
   const completed = new Set<string>();
   const failed = new Set<string>();
   const skipped = new Set<string>();
@@ -289,7 +447,7 @@
         continue;
       }
 
-      stepRun = toStepRunRecord(await createStepRun(ctx, companyId, {
+      stepRun = toWorkflowStepRunRecord(await createStepRun(ctx, companyId, {
         agentName: resolvedAgent.agentName,
         retryCount: 0,
         runId: typedWorkflowRun.id,
@@ -305,6 +463,7 @@
       stepDef,
       typedWorkflowRun.data.workflowName,
       companyId,
+      { parentIssueId: typedWorkflowRun.data.parentIssueId },
     );
   }
 
@@ -329,7 +488,7 @@
   ctx: PluginContext,
   workflowRun: WorkflowRunRecord,
   workflowDefinition: WorkflowDefinitionRecord,
-  sourceStepRun: WorkflowStepRecord,
+  sourceStepRun: WorkflowStepRunRecord,
   escalationTargetId: string,
   companyId: string,
 ): Promise<void> {
@@ -338,7 +497,7 @@
     throw new Error(`Escalation target step not found: ${escalationTargetId}`);
   }
 
-  const stepRuns = (await listStepRuns(ctx, workflowRun.id, companyId)).map(toStepRunRecord);
+  const stepRuns = (await listStepRuns(ctx, workflowRun.id, companyId)).map(toWorkflowStepRunRecord);
   let escalationStepRun = stepRuns.find((candidate) => candidate.data.stepId === escalationTargetId) ?? null;
   const resolvedAgent = await resolveStepAgent(
     ctx,
@@ -352,7 +511,7 @@
   }
 
   if (!escalationStepRun) {
-    escalationStepRun = toStepRunRecord(await createStepRun(ctx, companyId, {
+    escalationStepRun = toWorkflowStepRunRecord(await createStepRun(ctx, companyId, {
       agentName: resolvedAgent.agentName,
       retryCount: 0,
       runId: workflowRun.id,
@@ -374,14 +533,14 @@
       title: `${workflowRun.data.workflowName}: ${escalationStep.title}`,
     });
 
-    escalationStepRun = toStepRunRecord(await updateStepRun(ctx, escalationStepRun.id, {
+    escalationStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, escalationStepRun.id, {
       issueId: issue.id,
     }));
   }
 
   const shouldActivate = escalationStepRun.data.status === STEP_STATUSES.backlog;
   if (shouldActivate) {
-    escalationStepRun = toStepRunRecord(await updateStepRun(ctx, escalationStepRun.id, {
+    escalationStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, escalationStepRun.id, {
       status: STEP_STATUSES.todo,
     }));
   }
@@ -389,8 +548,8 @@
   if (shouldActivate) {
     await invokeAgentForStep(
       ctx,
+      escalationStepRun,
       escalationStep,
-      escalationStepRun.data.agentName,
       workflowRun.data.workflowName,
       companyId,
     );
@@ -421,7 +580,7 @@
     return;
   }
 
-  const typedStepRun = toStepRunRecord(stepRunRecord);
+  const typedStepRun = toWorkflowStepRunRecord(stepRunRecord);
   if (TERMINAL_STEP_STATUSES.has(typedStepRun.data.status)) {
     return;
   }
@@ -469,7 +628,7 @@
   if (options.allowRetry) {
     const retryInfo = getRetryInfo(typedWorkflowDefinition.data.steps, typedStepRun.data.stepId);
     if (retryInfo.shouldRetry && typedStepRun.data.retryCount < retryInfo.maxRetries) {
-      const retriedStepRun = toStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
+      const retriedStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
         completedAt: undefined,
         retryCount: typedStepRun.data.retryCount + 1,
         status: STEP_STATUSES.todo,
@@ -477,8 +636,8 @@
 
       await invokeAgentForStep(
         ctx,
+        retriedStepRun,
         stepDef,
-        retriedStepRun.data.agentName,
         typedWorkflowRun.data.workflowName,
         event.companyId,
       );
@@ -496,7 +655,7 @@
   }
 
   if (stepDef.onFailure === "skip") {
-    const skippedStepRun = toStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
+    const skippedStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
       completedAt: new Date().toISOString(),
       status: STEP_STATUSES.skipped,
     }));
@@ -556,7 +715,7 @@
       return;
     }
 
-    const escalatedStepRun = toStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
+    const escalatedStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
       completedAt: new Date().toISOString(),
       status: STEP_STATUSES.escalated,
     }));
@@ -617,6 +776,29 @@
   }
 }
 
+function registerDataHandler(
+  ctx: PluginContext,
+  key: string,
+  handler: (params: Record<string, unknown>) => Promise<unknown>,
+): void {
+  const dataClient = ctx.data as {
+    handle?: (handlerKey: string, handlerFn: (params: Record<string, unknown>) => Promise<unknown>) => void;
+    register?: (handlerKey: string, handlerFn: (params: Record<string, unknown>) => Promise<unknown>) => void;
+  };
+
+  if (typeof dataClient.handle === "function") {
+    dataClient.handle(key, handler);
+    return;
+  }
+
+  if (typeof dataClient.register === "function") {
+    dataClient.register(key, handler);
+    return;
+  }
+
+  throw new Error("Plugin data client does not support handler registration");
+}
+
 const plugin = definePlugin({
   async setup(ctx: PluginContext) {
     ctx.events.on("issue.updated", async (event: PluginEvent) => {
@@ -638,7 +820,7 @@
           return;
         }
 
-        const stepRun = toStepRunRecord(stepRunRecord);
+        const stepRun = toWorkflowStepRunRecord(stepRunRecord);
         const payload = event.payload as { status?: string };
         const issueStatus = typeof payload.status === "string" ? payload.status : undefined;
 
@@ -657,7 +839,7 @@
           return;
         }
 
-        const updatedStepRun = toStepRunRecord(await updateStepRun(ctx, stepRun.id, {
+        const updatedStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, stepRun.id, {
           completedAt: nextStepStatus === STEP_STATUSES.done ? new Date().toISOString() : undefined,
           startedAt: nextStepStatus === STEP_STATUSES.inProgress
             ? stepRun.data.startedAt ?? new Date().toISOString()
@@ -687,11 +869,21 @@
 
     ctx.events.on("issue.created", async (event: PluginEvent) => {
       try {
+        const idempotencyKey = buildIdempotencyKey(event);
+        if (await checkIdempotency(ctx, idempotencyKey, event.companyId)) {
+          return;
+        }
+
         const payload = event.payload as {
           id?: string;
           parentId?: string;
           assigneeAgentId?: string;
         };
+        const issueId = typeof event.entityId === "string" && event.entityId.trim()
+          ? event.entityId.trim()
+          : typeof payload.id === "string" && payload.id.trim()
+            ? payload.id.trim()
+            : "";
 
         const parentId = typeof payload.parentId === "string" && payload.parentId.trim()
           ? payload.parentId.trim()
@@ -700,12 +892,34 @@
           ? payload.assigneeAgentId.trim()
           : "";
 
-        if (parentId || !assigneeAgentId) {
+        if (!issueId || parentId || !assigneeAgentId) {
+          await markIdempotency(ctx, idempotencyKey, event.companyId);
+          return;
+        }
+
+        const agent = await ctx.agents.get(assigneeAgentId, event.companyId);
+        const metadata = agent?.metadata as Record<string, unknown> | undefined;
+        const defaultParentIssueId = typeof metadata?.defaultParentIssueId === "string" && metadata.defaultParentIssueId.trim()
+          ? metadata.defaultParentIssueId.trim()
+          : "";
+
+        if (!defaultParentIssueId) {
+          await markIdempotency(ctx, idempotencyKey, event.companyId);
           return;
         }
 
-        ctx.logger.info("parentId filler: issue created without parent", {
-          issueId: event.entityId ?? payload.id ?? null,
+        await ctx.issues.update(
+          issueId,
+          { parentId: defaultParentIssueId } as IssueUpdatePatch,
+          event.companyId,
+        );
+        await markIdempotency(ctx, idempotencyKey, event.companyId);
+
+        ctx.logger.info("parentId filler: populated issue parentId from agent metadata", {
+          assigneeAgentId,
+          companyId: event.companyId,
+          issueId,
+          parentId: defaultParentIssueId,
         });
       } catch (error) {
         ctx.logger.warn("Failed to handle issue.created event", {
@@ -744,7 +958,23 @@
       await runReconciler(ctx);
     });
 
-    ctx.data.register("workflow-overview", async (params: Record<string, unknown>) => {
+    registerDataHandler(ctx, "start-workflow", async (params: Record<string, unknown>) => {
+      const workflowId = typeof params.workflowId === "string" ? params.workflowId.trim() : "";
+      const companyId = typeof params.companyId === "string" ? params.companyId.trim() : "";
+      const parentIssueId = typeof params.parentIssueId === "string" ? params.parentIssueId.trim() : "";
+      const createParentIssue = params.createParentIssue === true;
+
+      if (!workflowId || !companyId) {
+        throw new Error("start-workflow requires workflowId and companyId");
+      }
+
+      return await startWorkflow(ctx, workflowId, companyId, {
+        createParentIssue,
+        parentIssueId: parentIssueId || undefined,
+      });
+    });
+
+    registerDataHandler(ctx, "workflow-overview", async (params: Record<string, unknown>) => {
       try {
         const companyId = typeof params.companyId === "string" ? params.companyId.trim() : "";
         if (!companyId) {
@@ -782,7 +1012,7 @@
       }
     });
 
-    ctx.data.register("workflow-run-detail", async (params: Record<string, unknown>) => {
+    registerDataHandler(ctx, "workflow-run-detail", async (params: Record<string, unknown>) => {
       try {
         const runId = typeof params.runId === "string" ? params.runId.trim() : "";
         if (!runId) {
@@ -807,7 +1037,7 @@
             ...typedWorkflowRun.data,
           },
           stepRuns: stepRuns.map((record: PluginEntityRecord) => {
-            const stepRun = toStepRunRecord(record);
+            const stepRun = toWorkflowStepRunRecord(record);
             return {
               id: stepRun.id,
               status: stepRun.status,
diff --git a/paperclip-addon/plugins/workflow-engine/src/workflow-store.ts b/paperclip-addon/plugins/workflow-engine/src/workflow-store.ts
index 5871a2082258cebcfe8b69ebafbb0814cf303d50..3d7fb2b3bc75911cfd9093eb3a4054cc73196bb9
--- a/paperclip-addon/plugins/workflow-engine/src/workflow-store.ts
+++ b/paperclip-addon/plugins/workflow-engine/src/workflow-store.ts
@@ -44,7 +44,14 @@
 
 type TypedEntityRecord<T> = Omit<PluginEntityRecord, "data"> & { data: T };
 
-function toTypedRecord<T>(record: PluginEntityRecord): TypedEntityRecord<T> {
+function toTypedRecord<T>(
+  record: PluginEntityRecord,
+  entityType: string,
+): TypedEntityRecord<T> {
+  if (record.entityType !== entityType) {
+    throw new Error(`Expected entity type "${entityType}", got "${record.entityType}"`);
+  }
+
   return record as TypedEntityRecord<T>;
 }
 
@@ -67,13 +74,26 @@
   id: string,
   entityType: string,
 ): Promise<TypedEntityRecord<T> | null> {
+  const listQuery: Parameters<PluginContext["entities"]["list"]>[0] & { id?: string } = {
+    entityType,
+    id,
+    limit: 1,
+  };
+
+  const listed = await ctx.entities.list(listQuery);
+  const listedRecord = listed.find((record) => record.id === id) ?? null;
+  if (listedRecord) {
+    return toTypedRecord<T>(listedRecord, entityType);
+  }
+
+  // Fallback: some host SDK variants ignore `id` on entities.list().
+  // Keep defensive type verification on direct get() until id-filter support is universal.
   const record = await ctx.entities.get(id);
-
-  if (!record || record.entityType !== entityType) {
+  if (!record) {
     return null;
   }
 
-  return toTypedRecord<T>(record);
+  return toTypedRecord<T>(record, entityType);
 }
 
 async function requireEntityByType<T>(
diff --git a/paperclip-addon/plugins/workflow-engine/src/workflow-utils.ts b/paperclip-addon/plugins/workflow-engine/src/workflow-utils.ts
new file mode 100644
index 0000000000000000000000000000000000000000..ec1cf6f6b9aa722b126b58c0a728b4e538ad1494
--- /dev/null
+++ b/paperclip-addon/plugins/workflow-engine/src/workflow-utils.ts
@@ -0,0 +1,62 @@
+import type { PluginEntityRecord } from "@paperclipai/plugin-sdk";
+
+import { STEP_STATUSES } from "./constants.js";
+import type { WorkflowStep } from "./dag-engine.js";
+import type {
+  WorkflowDefinition,
+  WorkflowRun,
+  WorkflowStepRun,
+} from "./workflow-store.js";
+
+export type WorkflowRunRecord = PluginEntityRecord & { data: WorkflowRun };
+export type WorkflowStepRunRecord = PluginEntityRecord & { data: WorkflowStepRun };
+export type WorkflowDefinitionRecord = PluginEntityRecord & { data: WorkflowDefinition };
+type StepAgentMetadata = WorkflowStep & {
+  agent?: string;
+  agentName?: string;
+  assigneeAgentName?: string;
+};
+
+export const TERMINAL_STEP_STATUSES = new Set<string>([
+  STEP_STATUSES.done,
+  STEP_STATUSES.failed,
+  STEP_STATUSES.skipped,
+  STEP_STATUSES.escalated,
+]);
+
+export function toWorkflowRunRecord(record: PluginEntityRecord): WorkflowRunRecord {
+  return record as WorkflowRunRecord;
+}
+
+export function toWorkflowStepRunRecord(record: PluginEntityRecord): WorkflowStepRunRecord {
+  return record as WorkflowStepRunRecord;
+}
+
+export function toWorkflowDefinitionRecord(record: PluginEntityRecord): WorkflowDefinitionRecord {
+  return record as WorkflowDefinitionRecord;
+}
+
+export function findStepDefinition(definition: WorkflowDefinitionRecord, stepId: string): WorkflowStep | null {
+  return definition.data.steps.find((step: WorkflowStep) => step.id === stepId) ?? null;
+}
+
+export function getStepAgentNameHint(stepDef: WorkflowStep): string | null {
+  const stepMeta = stepDef as StepAgentMetadata;
+  const candidates = [stepMeta.agentName, stepMeta.agent, stepMeta.assigneeAgentName];
+
+  for (const candidate of candidates) {
+    if (typeof candidate === "string" && candidate.trim()) {
+      return candidate.trim();
+    }
+  }
+
+  return null;
+}
+
+export function getStepAgentName(stepRun: WorkflowStepRunRecord, stepDef: WorkflowStep): string | null {
+  if (typeof stepRun.data.agentName === "string" && stepRun.data.agentName.trim()) {
+    return stepRun.data.agentName.trim();
+  }
+
+  return getStepAgentNameHint(stepDef);
+}

file update:
diff --git a/paperclip-addon/plugins/workflow-engine/src/dag-engine.ts b/paperclip-addon/plugins/workflow-engine/src/dag-engine.ts
index b59510f0473171038be77277286d3b5c2063e6c5..e7f21ff998cab78b9325a8aeacf4fda9c5e06553
--- a/paperclip-addon/plugins/workflow-engine/src/dag-engine.ts
+++ b/paperclip-addon/plugins/workflow-engine/src/dag-engine.ts
@@ -2,6 +2,7 @@
   id: string;
   title: string;
   dependsOn: string[];
+  sessionMode?: "fresh" | "reuse";
   onFailure?: "retry" | "skip" | "abort_workflow" | "escalate";
   escalateTo?: string;
   maxRetries?: number;
diff --git a/paperclip-addon/plugins/workflow-engine/src/reconciler.ts b/paperclip-addon/plugins/workflow-engine/src/reconciler.ts
index 14ab4e766a835d145a6c2e7b1b564270285cbdc3..d2ab54817bd8185944379d2188bfbdef1722020a
--- a/paperclip-addon/plugins/workflow-engine/src/reconciler.ts
+++ b/paperclip-addon/plugins/workflow-engine/src/reconciler.ts
@@ -8,65 +8,22 @@
   listStepRuns,
   updateStepRun,
   updateWorkflowRun,
-  type WorkflowDefinition,
-  type WorkflowRun,
-  type WorkflowStepRun,
 } from "./workflow-store.js";
 import type { WorkflowStep } from "./dag-engine.js";
-
-type WorkflowRunRecord = PluginEntityRecord & { data: WorkflowRun };
-type WorkflowStepRunRecord = PluginEntityRecord & { data: WorkflowStepRun };
-type WorkflowDefinitionRecord = PluginEntityRecord & { data: WorkflowDefinition };
-type StepAgentMetadata = WorkflowStep & {
-  agent?: string;
-  agentName?: string;
-  assigneeAgentName?: string;
-};
+import {
+  TERMINAL_STEP_STATUSES,
+  findStepDefinition,
+  getStepAgentName,
+  toWorkflowDefinitionRecord,
+  toWorkflowRunRecord,
+  toWorkflowStepRunRecord,
+} from "./workflow-utils.js";
 
 const STALE_THRESHOLD_MS = 300_000;
-const TERMINAL_STEP_STATUSES = new Set<string>([
-  STEP_STATUSES.done,
-  STEP_STATUSES.failed,
-  STEP_STATUSES.skipped,
-  STEP_STATUSES.escalated,
-]);
 
 function isStale(record: PluginEntityRecord, thresholdMs: number): boolean {
   const updatedAt = new Date(record.updatedAt).getTime();
   return Date.now() - updatedAt > thresholdMs;
-}
-
-function toWorkflowRunRecord(record: PluginEntityRecord): WorkflowRunRecord {
-  return record as WorkflowRunRecord;
-}
-
-function toWorkflowStepRunRecord(record: PluginEntityRecord): WorkflowStepRunRecord {
-  return record as WorkflowStepRunRecord;
-}
-
-function toWorkflowDefinitionRecord(record: PluginEntityRecord): WorkflowDefinitionRecord {
-  return record as WorkflowDefinitionRecord;
-}
-
-function findStepDefinition(definition: WorkflowDefinitionRecord, stepId: string): WorkflowStep | null {
-  return definition.data.steps.find((step: WorkflowStep) => step.id === stepId) ?? null;
-}
-
-function getStepAgentName(stepRun: WorkflowStepRunRecord, stepDef: WorkflowStep): string | null {
-  if (typeof stepRun.data.agentName === "string" && stepRun.data.agentName.trim()) {
-    return stepRun.data.agentName.trim();
-  }
-
-  const stepMeta = stepDef as StepAgentMetadata;
-  const candidates = [stepMeta.agentName, stepMeta.agent, stepMeta.assigneeAgentName];
-
-  for (const candidate of candidates) {
-    if (typeof candidate === "string" && candidate.trim()) {
-      return candidate.trim();
-    }
-  }
-
-  return null;
 }
 
 async function invokeAgentByName(
diff --git a/paperclip-addon/plugins/workflow-engine/src/worker.ts b/paperclip-addon/plugins/workflow-engine/src/worker.ts
index c5ac3fbb4ca7f767eb29331bd3630b635449b335..2bfb4dd37a3cc1e88b833a49d3e4d39c9abf1505
--- a/paperclip-addon/plugins/workflow-engine/src/worker.ts
+++ b/paperclip-addon/plugins/workflow-engine/src/worker.ts
@@ -15,6 +15,7 @@
 } from "./dag-engine.js";
 import {
   checkIdempotency,
+  createWorkflowRun,
   createStepRun,
   findStepRunByIssueId,
   getWorkflowDefinition,
@@ -25,51 +26,38 @@
   markIdempotency,
   updateStepRun,
   updateWorkflowRun,
-  type WorkflowDefinition,
-  type WorkflowRun,
   type WorkflowStepRun,
 } from "./workflow-store.js";
+import {
+  TERMINAL_STEP_STATUSES,
+  findStepDefinition,
+  getStepAgentName,
+  getStepAgentNameHint,
+  toWorkflowDefinitionRecord,
+  toWorkflowRunRecord,
+  toWorkflowStepRunRecord,
+  type WorkflowDefinitionRecord,
+  type WorkflowRunRecord,
+  type WorkflowStepRunRecord,
+} from "./workflow-utils.js";
 
-type WorkflowStepRecord = PluginEntityRecord & { data: WorkflowStepRun };
-type WorkflowRunRecord = PluginEntityRecord & { data: WorkflowRun };
-type WorkflowDefinitionRecord = PluginEntityRecord & { data: WorkflowDefinition };
 type WorkflowStepMetadata = WorkflowStep & {
   description?: string;
-  agent?: string;
-  agentName?: string;
-  assigneeAgentName?: string;
   assigneeAgentId?: string;
+  sessionMode?: "fresh" | "reuse";
 };
 type AgentRecord = Awaited<ReturnType<PluginContext["agents"]["list"]>>[number];
+type IssueUpdatePatch = Parameters<PluginContext["issues"]["update"]>[1];
 type ReconcilerModule = {
   reconcileStuckSteps?: (ctx: PluginContext) => Promise<void>;
 };
 
-const TERMINAL_STEP_STATUSES = new Set<string>([
-  STEP_STATUSES.done,
-  STEP_STATUSES.failed,
-  STEP_STATUSES.skipped,
-  STEP_STATUSES.escalated,
-]);
-
 function summarizeError(error: unknown): string {
   if (error instanceof Error) {
     return error.message;
   }
 
   return String(error);
-}
-
-function toStepRunRecord(record: PluginEntityRecord): WorkflowStepRecord {
-  return record as WorkflowStepRecord;
-}
-
-function toWorkflowRunRecord(record: PluginEntityRecord): WorkflowRunRecord {
-  return record as WorkflowRunRecord;
-}
-
-function toWorkflowDefinitionRecord(record: PluginEntityRecord): WorkflowDefinitionRecord {
-  return record as WorkflowDefinitionRecord;
 }
 
 function buildIdempotencyKey(event: PluginEvent): string {
@@ -78,10 +66,6 @@
 
 function getStepMetadata(stepDef: WorkflowStep): WorkflowStepMetadata {
   return stepDef as WorkflowStepMetadata;
-}
-
-function findStepDefinition(definition: WorkflowDefinitionRecord, stepId: string): WorkflowStep | null {
-  return definition.data.steps.find((step: WorkflowStep) => step.id === stepId) ?? null;
 }
 
 function getStepDescription(stepDef: WorkflowStep): string | undefined {
@@ -89,19 +73,6 @@
   return typeof description === "string" && description.trim() ? description.trim() : undefined;
 }
 
-function getStepAgentNameHint(stepDef: WorkflowStep): string | null {
-  const meta = getStepMetadata(stepDef);
-  const candidates = [meta.agentName, meta.agent, meta.assigneeAgentName];
-
-  for (const candidate of candidates) {
-    if (typeof candidate === "string" && candidate.trim()) {
-      return candidate.trim();
-    }
-  }
-
-  return null;
-}
-
 function getStepAgentIdHint(stepDef: WorkflowStep): string | null {
   const assigneeAgentId = getStepMetadata(stepDef).assigneeAgentId;
   if (typeof assigneeAgentId !== "string" || !assigneeAgentId.trim()) {
@@ -147,60 +118,247 @@
 
 async function invokeAgentForStep(
   ctx: PluginContext,
+  stepRunRecord: WorkflowStepRunRecord,
   stepDef: WorkflowStep,
-  agentName: string,
   workflowName: string,
   companyId: string,
-): Promise<void> {
+): Promise<WorkflowStepRunRecord> {
+  const agentName = getStepAgentName(stepRunRecord, stepDef);
+  if (!agentName) {
+    ctx.logger.warn("Agent name is missing for workflow step", {
+      companyId,
+      runId: stepRunRecord.data.runId,
+      stepId: stepDef.id,
+      workflowName,
+    });
+    return stepRunRecord;
+  }
+
   const agents = await ctx.agents.list({ companyId });
-  const agent = agents.find((candidate: AgentRecord) => candidate.name === agentName);
+  const agent = agents.find((candidate: AgentRecord) => candidate.name === agentName) ?? null;
 
   if (!agent) {
     ctx.logger.warn("Agent not found for step", {
       agentName,
       companyId,
+      runId: stepRunRecord.data.runId,
       stepId: stepDef.id,
       workflowName,
     });
-    return;
+    return stepRunRecord;
   }
 
+  const prompt = `workflow:${workflowName}/step:${stepDef.id} — "${stepDef.title}" is ready. Please proceed with the assigned task.`;
+  const reason = `workflow:${workflowName}/step:${stepDef.id}`;
+  const sessionMode = getStepMetadata(stepDef).sessionMode === "reuse" ? "reuse" : "fresh";
+
+  if (sessionMode === "reuse") {
+    let sessionId = typeof stepRunRecord.data.sessionId === "string" && stepRunRecord.data.sessionId.trim()
+      ? stepRunRecord.data.sessionId.trim()
+      : "";
+
+    if (!sessionId) {
+      const session = await ctx.agents.sessions.create(agent.id, companyId, {
+        reason,
+      });
+      sessionId = session.sessionId;
+      stepRunRecord = toWorkflowStepRunRecord(await updateStepRun(ctx, stepRunRecord.id, {
+        sessionId,
+      }));
+    }
+
+    await ctx.agents.sessions.sendMessage(sessionId, companyId, {
+      prompt,
+      reason,
+    });
+
+    ctx.logger.info("Sent workflow step prompt via agent session", {
+      agentId: agent.id,
+      agentName,
+      companyId,
+      runId: stepRunRecord.data.runId,
+      sessionId,
+      stepId: stepDef.id,
+      workflowName,
+    });
+    return stepRunRecord;
+  }
+
   await ctx.agents.invoke(agent.id, companyId, {
-    prompt: `workflow:${workflowName}/step:${stepDef.id} — "${stepDef.title}" is ready. Please proceed with the assigned task.`,
-    reason: `workflow:${workflowName}/step:${stepDef.id}`,
+    prompt,
+    reason,
   });
 
   ctx.logger.info("Invoked agent for workflow step", {
     agentId: agent.id,
     agentName,
     companyId,
+    runId: stepRunRecord.data.runId,
     stepId: stepDef.id,
     workflowName,
   });
+
+  return stepRunRecord;
 }
 
 async function activateBacklogStep(
   ctx: PluginContext,
-  stepRunRecord: WorkflowStepRecord,
+  stepRunRecord: WorkflowStepRunRecord,
   stepDef: WorkflowStep,
   workflowName: string,
   companyId: string,
-): Promise<void> {
+  options?: { parentIssueId?: string },
+): Promise<WorkflowStepRunRecord> {
   if (stepRunRecord.data.status !== STEP_STATUSES.backlog) {
-    return;
+    return stepRunRecord;
+  }
+
+  const resolvedAgent = await resolveStepAgent(
+    ctx,
+    companyId,
+    stepDef,
+    stepRunRecord.data.agentName,
+  );
+
+  let issueId = typeof stepRunRecord.data.issueId === "string" && stepRunRecord.data.issueId.trim()
+    ? stepRunRecord.data.issueId.trim()
+    : "";
+  const stepDescription = getStepDescription(stepDef) ?? `Workflow step: ${stepDef.id}`;
+
+  if (!issueId) {
+    const issue = await ctx.issues.create({
+      assigneeAgentId: resolvedAgent.agentId ?? undefined,
+      companyId,
+      description: stepDescription,
+      parentId: options?.parentIssueId,
+      title: `[${workflowName}] ${stepDef.title}`,
+    });
+    issueId = issue.id;
   }
 
-  const updated = toStepRunRecord(await updateStepRun(ctx, stepRunRecord.id, {
+  const nextStartedAt = stepRunRecord.data.startedAt ?? new Date().toISOString();
+  let updatedStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, stepRunRecord.id, {
+    agentName: resolvedAgent.agentName ?? stepRunRecord.data.agentName,
+    issueId,
+    startedAt: nextStartedAt,
     status: STEP_STATUSES.todo,
   }));
 
-  await invokeAgentForStep(
+  updatedStepRun = await invokeAgentForStep(
     ctx,
+    updatedStepRun,
     stepDef,
-    updated.data.agentName,
     workflowName,
     companyId,
   );
+
+  return updatedStepRun;
+}
+
+async function startWorkflow(
+  ctx: PluginContext,
+  workflowId: string,
+  companyId: string,
+  options?: {
+    createParentIssue?: boolean;
+    parentIssueId?: string;
+  },
+): Promise<{
+  activatedStepIds: string[];
+  parentIssueId: string | null;
+  runId: string;
+  workflowId: string;
+}> {
+  const workflowDefinition = await getWorkflowDefinition(ctx, workflowId);
+  if (!workflowDefinition) {
+    throw new Error(`Workflow definition not found: ${workflowId}`);
+  }
+
+  const typedWorkflowDefinition = toWorkflowDefinitionRecord(workflowDefinition);
+  if (typedWorkflowDefinition.data.companyId !== companyId) {
+    throw new Error(`Workflow does not belong to company: ${workflowId}`);
+  }
+
+  let parentIssueId = typeof options?.parentIssueId === "string" && options.parentIssueId.trim()
+    ? options.parentIssueId.trim()
+    : "";
+  if (!parentIssueId && options?.createParentIssue) {
+    const parentIssue = await ctx.issues.create({
+      companyId,
+      description: typedWorkflowDefinition.data.description || `Workflow run: ${typedWorkflowDefinition.data.name}`,
+      title: `[${typedWorkflowDefinition.data.name}] Workflow run`,
+    });
+    parentIssueId = parentIssue.id;
+  }
+
+  const workflowRun = toWorkflowRunRecord(await createWorkflowRun(ctx, {
+    companyId,
+    parentIssueId: parentIssueId || undefined,
+    startedAt: new Date().toISOString(),
+    status: RUN_STATUSES.running,
+    workflowId: typedWorkflowDefinition.id,
+    workflowName: typedWorkflowDefinition.data.name,
+  }));
+
+  const agents = await ctx.agents.list({ companyId });
+  const agentsByName = new Map<string, AgentRecord>();
+  for (const agent of agents) {
+    agentsByName.set(agent.name, agent);
+  }
+
+  const pendingRootSteps: Array<{ stepDef: WorkflowStep; stepRun: WorkflowStepRunRecord }> = [];
+  for (const stepDef of typedWorkflowDefinition.data.steps) {
+    const agentNameHint = getStepAgentNameHint(stepDef);
+    const matchedAgent = agentNameHint ? agentsByName.get(agentNameHint) ?? null : null;
+    const resolvedAgent = matchedAgent
+      ? { agentId: matchedAgent.id, agentName: matchedAgent.name }
+      : await resolveStepAgent(ctx, companyId, stepDef, agentNameHint ?? undefined);
+
+    if (!resolvedAgent.agentName) {
+      throw new Error(`Unable to resolve step assignee for "${stepDef.id}"`);
+    }
+
+    const stepRun = toWorkflowStepRunRecord(await createStepRun(ctx, companyId, {
+      agentName: resolvedAgent.agentName,
+      retryCount: 0,
+      runId: workflowRun.id,
+      status: STEP_STATUSES.backlog,
+      stepId: stepDef.id,
+    }));
+
+    if (stepDef.dependsOn.length === 0 && stepDef.triggerOn !== "escalation") {
+      pendingRootSteps.push({ stepDef, stepRun });
+    }
+  }
+
+  const activatedStepIds: string[] = [];
+  for (const pending of pendingRootSteps) {
+    await activateBacklogStep(
+      ctx,
+      pending.stepRun,
+      pending.stepDef,
+      typedWorkflowDefinition.data.name,
+      companyId,
+      { parentIssueId: parentIssueId || undefined },
+    );
+    activatedStepIds.push(pending.stepDef.id);
+  }
+
+  ctx.logger.info("Started workflow run", {
+    activatedStepIds,
+    companyId,
+    parentIssueId: parentIssueId || null,
+    runId: workflowRun.id,
+    workflowId: typedWorkflowDefinition.id,
+    workflowName: typedWorkflowDefinition.data.name,
+  });
+
+  return {
+    activatedStepIds,
+    parentIssueId: parentIssueId || null,
+    runId: workflowRun.id,
+    workflowId: typedWorkflowDefinition.id,
+  };
 }
 
 async function advanceWorkflow(
@@ -208,7 +366,7 @@
   stepRunRecord: PluginEntityRecord,
   companyId: string,
 ): Promise<void> {
-  const completedStepRun = toStepRunRecord(stepRunRecord);
+  const completedStepRun = toWorkflowStepRunRecord(stepRunRecord);
   const workflowRun = await getWorkflowRun(ctx, completedStepRun.data.runId);
 
   if (!workflowRun) {
@@ -237,7 +395,7 @@
   }
 
   const typedWorkflowDefinition = toWorkflowDefinitionRecord(workflowDefinition);
-  const stepRuns = (await listStepRuns(ctx, typedWorkflowRun.id, companyId)).map(toStepRunRecord);
+  const stepRuns = (await listStepRuns(ctx, typedWorkflowRun.id, companyId)).map(toWorkflowStepRunRecord);
   const completed = new Set<string>();
   const failed = new Set<string>();
   const skipped = new Set<string>();
@@ -289,7 +447,7 @@
         continue;
       }
 
-      stepRun = toStepRunRecord(await createStepRun(ctx, companyId, {
+      stepRun = toWorkflowStepRunRecord(await createStepRun(ctx, companyId, {
         agentName: resolvedAgent.agentName,
         retryCount: 0,
         runId: typedWorkflowRun.id,
@@ -305,6 +463,7 @@
       stepDef,
       typedWorkflowRun.data.workflowName,
       companyId,
+      { parentIssueId: typedWorkflowRun.data.parentIssueId },
     );
   }
 
@@ -329,7 +488,7 @@
   ctx: PluginContext,
   workflowRun: WorkflowRunRecord,
   workflowDefinition: WorkflowDefinitionRecord,
-  sourceStepRun: WorkflowStepRecord,
+  sourceStepRun: WorkflowStepRunRecord,
   escalationTargetId: string,
   companyId: string,
 ): Promise<void> {
@@ -338,7 +497,7 @@
     throw new Error(`Escalation target step not found: ${escalationTargetId}`);
   }
 
-  const stepRuns = (await listStepRuns(ctx, workflowRun.id, companyId)).map(toStepRunRecord);
+  const stepRuns = (await listStepRuns(ctx, workflowRun.id, companyId)).map(toWorkflowStepRunRecord);
   let escalationStepRun = stepRuns.find((candidate) => candidate.data.stepId === escalationTargetId) ?? null;
   const resolvedAgent = await resolveStepAgent(
     ctx,
@@ -352,7 +511,7 @@
   }
 
   if (!escalationStepRun) {
-    escalationStepRun = toStepRunRecord(await createStepRun(ctx, companyId, {
+    escalationStepRun = toWorkflowStepRunRecord(await createStepRun(ctx, companyId, {
       agentName: resolvedAgent.agentName,
       retryCount: 0,
       runId: workflowRun.id,
@@ -374,14 +533,14 @@
       title: `${workflowRun.data.workflowName}: ${escalationStep.title}`,
     });
 
-    escalationStepRun = toStepRunRecord(await updateStepRun(ctx, escalationStepRun.id, {
+    escalationStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, escalationStepRun.id, {
       issueId: issue.id,
     }));
   }
 
   const shouldActivate = escalationStepRun.data.status === STEP_STATUSES.backlog;
   if (shouldActivate) {
-    escalationStepRun = toStepRunRecord(await updateStepRun(ctx, escalationStepRun.id, {
+    escalationStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, escalationStepRun.id, {
       status: STEP_STATUSES.todo,
     }));
   }
@@ -389,8 +548,8 @@
   if (shouldActivate) {
     await invokeAgentForStep(
       ctx,
+      escalationStepRun,
       escalationStep,
-      escalationStepRun.data.agentName,
       workflowRun.data.workflowName,
       companyId,
     );
@@ -421,7 +580,7 @@
     return;
   }
 
-  const typedStepRun = toStepRunRecord(stepRunRecord);
+  const typedStepRun = toWorkflowStepRunRecord(stepRunRecord);
   if (TERMINAL_STEP_STATUSES.has(typedStepRun.data.status)) {
     return;
   }
@@ -469,7 +628,7 @@
   if (options.allowRetry) {
     const retryInfo = getRetryInfo(typedWorkflowDefinition.data.steps, typedStepRun.data.stepId);
     if (retryInfo.shouldRetry && typedStepRun.data.retryCount < retryInfo.maxRetries) {
-      const retriedStepRun = toStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
+      const retriedStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
         completedAt: undefined,
         retryCount: typedStepRun.data.retryCount + 1,
         status: STEP_STATUSES.todo,
@@ -477,8 +636,8 @@
 
       await invokeAgentForStep(
         ctx,
+        retriedStepRun,
         stepDef,
-        retriedStepRun.data.agentName,
         typedWorkflowRun.data.workflowName,
         event.companyId,
       );
@@ -496,7 +655,7 @@
   }
 
   if (stepDef.onFailure === "skip") {
-    const skippedStepRun = toStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
+    const skippedStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
       completedAt: new Date().toISOString(),
       status: STEP_STATUSES.skipped,
     }));
@@ -556,7 +715,7 @@
       return;
     }
 
-    const escalatedStepRun = toStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
+    const escalatedStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
       completedAt: new Date().toISOString(),
       status: STEP_STATUSES.escalated,
     }));
@@ -617,6 +776,29 @@
   }
 }
 
+function registerDataHandler(
+  ctx: PluginContext,
+  key: string,
+  handler: (params: Record<string, unknown>) => Promise<unknown>,
+): void {
+  const dataClient = ctx.data as {
+    handle?: (handlerKey: string, handlerFn: (params: Record<string, unknown>) => Promise<unknown>) => void;
+    register?: (handlerKey: string, handlerFn: (params: Record<string, unknown>) => Promise<unknown>) => void;
+  };
+
+  if (typeof dataClient.handle === "function") {
+    dataClient.handle(key, handler);
+    return;
+  }
+
+  if (typeof dataClient.register === "function") {
+    dataClient.register(key, handler);
+    return;
+  }
+
+  throw new Error("Plugin data client does not support handler registration");
+}
+
 const plugin = definePlugin({
   async setup(ctx: PluginContext) {
     ctx.events.on("issue.updated", async (event: PluginEvent) => {
@@ -638,7 +820,7 @@
           return;
         }
 
-        const stepRun = toStepRunRecord(stepRunRecord);
+        const stepRun = toWorkflowStepRunRecord(stepRunRecord);
         const payload = event.payload as { status?: string };
         const issueStatus = typeof payload.status === "string" ? payload.status : undefined;
 
@@ -657,7 +839,7 @@
           return;
         }
 
-        const updatedStepRun = toStepRunRecord(await updateStepRun(ctx, stepRun.id, {
+        const updatedStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, stepRun.id, {
           completedAt: nextStepStatus === STEP_STATUSES.done ? new Date().toISOString() : undefined,
           startedAt: nextStepStatus === STEP_STATUSES.inProgress
             ? stepRun.data.startedAt ?? new Date().toISOString()
@@ -687,11 +869,21 @@
 
     ctx.events.on("issue.created", async (event: PluginEvent) => {
       try {
+        const idempotencyKey = buildIdempotencyKey(event);
+        if (await checkIdempotency(ctx, idempotencyKey, event.companyId)) {
+          return;
+        }
+
         const payload = event.payload as {
           id?: string;
           parentId?: string;
           assigneeAgentId?: string;
         };
+        const issueId = typeof event.entityId === "string" && event.entityId.trim()
+          ? event.entityId.trim()
+          : typeof payload.id === "string" && payload.id.trim()
+            ? payload.id.trim()
+            : "";
 
         const parentId = typeof payload.parentId === "string" && payload.parentId.trim()
           ? payload.parentId.trim()
@@ -700,12 +892,34 @@
           ? payload.assigneeAgentId.trim()
           : "";
 
-        if (parentId || !assigneeAgentId) {
+        if (!issueId || parentId || !assigneeAgentId) {
+          await markIdempotency(ctx, idempotencyKey, event.companyId);
+          return;
+        }
+
+        const agent = await ctx.agents.get(assigneeAgentId, event.companyId);
+        const metadata = agent?.metadata as Record<string, unknown> | undefined;
+        const defaultParentIssueId = typeof metadata?.defaultParentIssueId === "string" && metadata.defaultParentIssueId.trim()
+          ? metadata.defaultParentIssueId.trim()
+          : "";
+
+        if (!defaultParentIssueId) {
+          await markIdempotency(ctx, idempotencyKey, event.companyId);
           return;
         }
 
-        ctx.logger.info("parentId filler: issue created without parent", {
-          issueId: event.entityId ?? payload.id ?? null,
+        await ctx.issues.update(
+          issueId,
+          { parentId: defaultParentIssueId } as IssueUpdatePatch,
+          event.companyId,
+        );
+        await markIdempotency(ctx, idempotencyKey, event.companyId);
+
+        ctx.logger.info("parentId filler: populated issue parentId from agent metadata", {
+          assigneeAgentId,
+          companyId: event.companyId,
+          issueId,
+          parentId: defaultParentIssueId,
         });
       } catch (error) {
         ctx.logger.warn("Failed to handle issue.created event", {
@@ -744,7 +958,23 @@
       await runReconciler(ctx);
     });
 
-    ctx.data.register("workflow-overview", async (params: Record<string, unknown>) => {
+    registerDataHandler(ctx, "start-workflow", async (params: Record<string, unknown>) => {
+      const workflowId = typeof params.workflowId === "string" ? params.workflowId.trim() : "";
+      const companyId = typeof params.companyId === "string" ? params.companyId.trim() : "";
+      const parentIssueId = typeof params.parentIssueId === "string" ? params.parentIssueId.trim() : "";
+      const createParentIssue = params.createParentIssue === true;
+
+      if (!workflowId || !companyId) {
+        throw new Error("start-workflow requires workflowId and companyId");
+      }
+
+      return await startWorkflow(ctx, workflowId, companyId, {
+        createParentIssue,
+        parentIssueId: parentIssueId || undefined,
+      });
+    });
+
+    registerDataHandler(ctx, "workflow-overview", async (params: Record<string, unknown>) => {
       try {
         const companyId = typeof params.companyId === "string" ? params.companyId.trim() : "";
         if (!companyId) {
@@ -782,7 +1012,7 @@
       }
     });
 
-    ctx.data.register("workflow-run-detail", async (params: Record<string, unknown>) => {
+    registerDataHandler(ctx, "workflow-run-detail", async (params: Record<string, unknown>) => {
       try {
         const runId = typeof params.runId === "string" ? params.runId.trim() : "";
         if (!runId) {
@@ -807,7 +1037,7 @@
             ...typedWorkflowRun.data,
           },
           stepRuns: stepRuns.map((record: PluginEntityRecord) => {
-            const stepRun = toStepRunRecord(record);
+            const stepRun = toWorkflowStepRunRecord(record);
             return {
               id: stepRun.id,
               status: stepRun.status,
diff --git a/paperclip-addon/plugins/workflow-engine/src/workflow-store.ts b/paperclip-addon/plugins/workflow-engine/src/workflow-store.ts
index 5871a2082258cebcfe8b69ebafbb0814cf303d50..3d7fb2b3bc75911cfd9093eb3a4054cc73196bb9
--- a/paperclip-addon/plugins/workflow-engine/src/workflow-store.ts
+++ b/paperclip-addon/plugins/workflow-engine/src/workflow-store.ts
@@ -44,7 +44,14 @@
 
 type TypedEntityRecord<T> = Omit<PluginEntityRecord, "data"> & { data: T };
 
-function toTypedRecord<T>(record: PluginEntityRecord): TypedEntityRecord<T> {
+function toTypedRecord<T>(
+  record: PluginEntityRecord,
+  entityType: string,
+): TypedEntityRecord<T> {
+  if (record.entityType !== entityType) {
+    throw new Error(`Expected entity type "${entityType}", got "${record.entityType}"`);
+  }
+
   return record as TypedEntityRecord<T>;
 }
 
@@ -67,13 +74,26 @@
   id: string,
   entityType: string,
 ): Promise<TypedEntityRecord<T> | null> {
+  const listQuery: Parameters<PluginContext["entities"]["list"]>[0] & { id?: string } = {
+    entityType,
+    id,
+    limit: 1,
+  };
+
+  const listed = await ctx.entities.list(listQuery);
+  const listedRecord = listed.find((record) => record.id === id) ?? null;
+  if (listedRecord) {
+    return toTypedRecord<T>(listedRecord, entityType);
+  }
+
+  // Fallback: some host SDK variants ignore `id` on entities.list().
+  // Keep defensive type verification on direct get() until id-filter support is universal.
   const record = await ctx.entities.get(id);
-
-  if (!record || record.entityType !== entityType) {
+  if (!record) {
     return null;
   }
 
-  return toTypedRecord<T>(record);
+  return toTypedRecord<T>(record, entityType);
 }
 
 async function requireEntityByType<T>(
diff --git a/paperclip-addon/plugins/workflow-engine/src/workflow-utils.ts b/paperclip-addon/plugins/workflow-engine/src/workflow-utils.ts
new file mode 100644
index 0000000000000000000000000000000000000000..ec1cf6f6b9aa722b126b58c0a728b4e538ad1494
--- /dev/null
+++ b/paperclip-addon/plugins/workflow-engine/src/workflow-utils.ts
@@ -0,0 +1,62 @@
+import type { PluginEntityRecord } from "@paperclipai/plugin-sdk";
+
+import { STEP_STATUSES } from "./constants.js";
+import type { WorkflowStep } from "./dag-engine.js";
+import type {
+  WorkflowDefinition,
+  WorkflowRun,
+  WorkflowStepRun,
+} from "./workflow-store.js";
+
+export type WorkflowRunRecord = PluginEntityRecord & { data: WorkflowRun };
+export type WorkflowStepRunRecord = PluginEntityRecord & { data: WorkflowStepRun };
+export type WorkflowDefinitionRecord = PluginEntityRecord & { data: WorkflowDefinition };
+type StepAgentMetadata = WorkflowStep & {
+  agent?: string;
+  agentName?: string;
+  assigneeAgentName?: string;
+};
+
+export const TERMINAL_STEP_STATUSES = new Set<string>([
+  STEP_STATUSES.done,
+  STEP_STATUSES.failed,
+  STEP_STATUSES.skipped,
+  STEP_STATUSES.escalated,
+]);
+
+export function toWorkflowRunRecord(record: PluginEntityRecord): WorkflowRunRecord {
+  return record as WorkflowRunRecord;
+}
+
+export function toWorkflowStepRunRecord(record: PluginEntityRecord): WorkflowStepRunRecord {
+  return record as WorkflowStepRunRecord;
+}
+
+export function toWorkflowDefinitionRecord(record: PluginEntityRecord): WorkflowDefinitionRecord {
+  return record as WorkflowDefinitionRecord;
+}
+
+export function findStepDefinition(definition: WorkflowDefinitionRecord, stepId: string): WorkflowStep | null {
+  return definition.data.steps.find((step: WorkflowStep) => step.id === stepId) ?? null;
+}
+
+export function getStepAgentNameHint(stepDef: WorkflowStep): string | null {
+  const stepMeta = stepDef as StepAgentMetadata;
+  const candidates = [stepMeta.agentName, stepMeta.agent, stepMeta.assigneeAgentName];
+
+  for (const candidate of candidates) {
+    if (typeof candidate === "string" && candidate.trim()) {
+      return candidate.trim();
+    }
+  }
+
+  return null;
+}
+
+export function getStepAgentName(stepRun: WorkflowStepRunRecord, stepDef: WorkflowStep): string | null {
+  if (typeof stepRun.data.agentName === "string" && stepRun.data.agentName.trim()) {
+    return stepRun.data.agentName.trim();
+  }
+
+  return getStepAgentNameHint(stepDef);
+}

codex
이제 `start-workflow` 데이터 핸들러를 추가했고, `issue.created` parentId filler에 idempotency+metadata 적용까지 넣었습니다. 마지막으로 `activateBacklogStep`의 생성 이슈 상태를 명시적으로 `todo`로 맞추고 남은 타입/빌드 오류를 정리하겠습니다.
file update
M /Users/kwak/Projects/paperclip/paperclip-addon/plugins/workflow-engine/src/worker.ts
@@ -235,2 +235,3 @@
     });
+    await ctx.issues.update(issue.id, { status: "todo" } as IssueUpdatePatch, companyId);
     issueId = issue.id;
apply_patch(auto_approved=true) exited 0 in 113ms:
Success. Updated the following files:
M /Users/kwak/Projects/paperclip/paperclip-addon/plugins/workflow-engine/src/worker.ts
file update:
diff --git a/paperclip-addon/plugins/workflow-engine/src/dag-engine.ts b/paperclip-addon/plugins/workflow-engine/src/dag-engine.ts
index b59510f0473171038be77277286d3b5c2063e6c5..e7f21ff998cab78b9325a8aeacf4fda9c5e06553
--- a/paperclip-addon/plugins/workflow-engine/src/dag-engine.ts
+++ b/paperclip-addon/plugins/workflow-engine/src/dag-engine.ts
@@ -2,6 +2,7 @@
   id: string;
   title: string;
   dependsOn: string[];
+  sessionMode?: "fresh" | "reuse";
   onFailure?: "retry" | "skip" | "abort_workflow" | "escalate";
   escalateTo?: string;
   maxRetries?: number;
diff --git a/paperclip-addon/plugins/workflow-engine/src/reconciler.ts b/paperclip-addon/plugins/workflow-engine/src/reconciler.ts
index 14ab4e766a835d145a6c2e7b1b564270285cbdc3..d2ab54817bd8185944379d2188bfbdef1722020a
--- a/paperclip-addon/plugins/workflow-engine/src/reconciler.ts
+++ b/paperclip-addon/plugins/workflow-engine/src/reconciler.ts
@@ -8,65 +8,22 @@
   listStepRuns,
   updateStepRun,
   updateWorkflowRun,
-  type WorkflowDefinition,
-  type WorkflowRun,
-  type WorkflowStepRun,
 } from "./workflow-store.js";
 import type { WorkflowStep } from "./dag-engine.js";
-
-type WorkflowRunRecord = PluginEntityRecord & { data: WorkflowRun };
-type WorkflowStepRunRecord = PluginEntityRecord & { data: WorkflowStepRun };
-type WorkflowDefinitionRecord = PluginEntityRecord & { data: WorkflowDefinition };
-type StepAgentMetadata = WorkflowStep & {
-  agent?: string;
-  agentName?: string;
-  assigneeAgentName?: string;
-};
+import {
+  TERMINAL_STEP_STATUSES,
+  findStepDefinition,
+  getStepAgentName,
+  toWorkflowDefinitionRecord,
+  toWorkflowRunRecord,
+  toWorkflowStepRunRecord,
+} from "./workflow-utils.js";
 
 const STALE_THRESHOLD_MS = 300_000;
-const TERMINAL_STEP_STATUSES = new Set<string>([
-  STEP_STATUSES.done,
-  STEP_STATUSES.failed,
-  STEP_STATUSES.skipped,
-  STEP_STATUSES.escalated,
-]);
 
 function isStale(record: PluginEntityRecord, thresholdMs: number): boolean {
   const updatedAt = new Date(record.updatedAt).getTime();
   return Date.now() - updatedAt > thresholdMs;
-}
-
-function toWorkflowRunRecord(record: PluginEntityRecord): WorkflowRunRecord {
-  return record as WorkflowRunRecord;
-}
-
-function toWorkflowStepRunRecord(record: PluginEntityRecord): WorkflowStepRunRecord {
-  return record as WorkflowStepRunRecord;
-}
-
-function toWorkflowDefinitionRecord(record: PluginEntityRecord): WorkflowDefinitionRecord {
-  return record as WorkflowDefinitionRecord;
-}
-
-function findStepDefinition(definition: WorkflowDefinitionRecord, stepId: string): WorkflowStep | null {
-  return definition.data.steps.find((step: WorkflowStep) => step.id === stepId) ?? null;
-}
-
-function getStepAgentName(stepRun: WorkflowStepRunRecord, stepDef: WorkflowStep): string | null {
-  if (typeof stepRun.data.agentName === "string" && stepRun.data.agentName.trim()) {
-    return stepRun.data.agentName.trim();
-  }
-
-  const stepMeta = stepDef as StepAgentMetadata;
-  const candidates = [stepMeta.agentName, stepMeta.agent, stepMeta.assigneeAgentName];
-
-  for (const candidate of candidates) {
-    if (typeof candidate === "string" && candidate.trim()) {
-      return candidate.trim();
-    }
-  }
-
-  return null;
 }
 
 async function invokeAgentByName(
diff --git a/paperclip-addon/plugins/workflow-engine/src/worker.ts b/paperclip-addon/plugins/workflow-engine/src/worker.ts
index c5ac3fbb4ca7f767eb29331bd3630b635449b335..303e35958890fa1f66db3f2b0ff1fa1f134102a8
--- a/paperclip-addon/plugins/workflow-engine/src/worker.ts
+++ b/paperclip-addon/plugins/workflow-engine/src/worker.ts
@@ -15,6 +15,7 @@
 } from "./dag-engine.js";
 import {
   checkIdempotency,
+  createWorkflowRun,
   createStepRun,
   findStepRunByIssueId,
   getWorkflowDefinition,
@@ -25,51 +26,38 @@
   markIdempotency,
   updateStepRun,
   updateWorkflowRun,
-  type WorkflowDefinition,
-  type WorkflowRun,
   type WorkflowStepRun,
 } from "./workflow-store.js";
+import {
+  TERMINAL_STEP_STATUSES,
+  findStepDefinition,
+  getStepAgentName,
+  getStepAgentNameHint,
+  toWorkflowDefinitionRecord,
+  toWorkflowRunRecord,
+  toWorkflowStepRunRecord,
+  type WorkflowDefinitionRecord,
+  type WorkflowRunRecord,
+  type WorkflowStepRunRecord,
+} from "./workflow-utils.js";
 
-type WorkflowStepRecord = PluginEntityRecord & { data: WorkflowStepRun };
-type WorkflowRunRecord = PluginEntityRecord & { data: WorkflowRun };
-type WorkflowDefinitionRecord = PluginEntityRecord & { data: WorkflowDefinition };
 type WorkflowStepMetadata = WorkflowStep & {
   description?: string;
-  agent?: string;
-  agentName?: string;
-  assigneeAgentName?: string;
   assigneeAgentId?: string;
+  sessionMode?: "fresh" | "reuse";
 };
 type AgentRecord = Awaited<ReturnType<PluginContext["agents"]["list"]>>[number];
+type IssueUpdatePatch = Parameters<PluginContext["issues"]["update"]>[1];
 type ReconcilerModule = {
   reconcileStuckSteps?: (ctx: PluginContext) => Promise<void>;
 };
 
-const TERMINAL_STEP_STATUSES = new Set<string>([
-  STEP_STATUSES.done,
-  STEP_STATUSES.failed,
-  STEP_STATUSES.skipped,
-  STEP_STATUSES.escalated,
-]);
-
 function summarizeError(error: unknown): string {
   if (error instanceof Error) {
     return error.message;
   }
 
   return String(error);
-}
-
-function toStepRunRecord(record: PluginEntityRecord): WorkflowStepRecord {
-  return record as WorkflowStepRecord;
-}
-
-function toWorkflowRunRecord(record: PluginEntityRecord): WorkflowRunRecord {
-  return record as WorkflowRunRecord;
-}
-
-function toWorkflowDefinitionRecord(record: PluginEntityRecord): WorkflowDefinitionRecord {
-  return record as WorkflowDefinitionRecord;
 }
 
 function buildIdempotencyKey(event: PluginEvent): string {
@@ -78,10 +66,6 @@
 
 function getStepMetadata(stepDef: WorkflowStep): WorkflowStepMetadata {
   return stepDef as WorkflowStepMetadata;
-}
-
-function findStepDefinition(definition: WorkflowDefinitionRecord, stepId: string): WorkflowStep | null {
-  return definition.data.steps.find((step: WorkflowStep) => step.id === stepId) ?? null;
 }
 
 function getStepDescription(stepDef: WorkflowStep): string | undefined {
@@ -89,19 +73,6 @@
   return typeof description === "string" && description.trim() ? description.trim() : undefined;
 }
 
-function getStepAgentNameHint(stepDef: WorkflowStep): string | null {
-  const meta = getStepMetadata(stepDef);
-  const candidates = [meta.agentName, meta.agent, meta.assigneeAgentName];
-
-  for (const candidate of candidates) {
-    if (typeof candidate === "string" && candidate.trim()) {
-      return candidate.trim();
-    }
-  }
-
-  return null;
-}
-
 function getStepAgentIdHint(stepDef: WorkflowStep): string | null {
   const assigneeAgentId = getStepMetadata(stepDef).assigneeAgentId;
   if (typeof assigneeAgentId !== "string" || !assigneeAgentId.trim()) {
@@ -147,68 +118,256 @@
 
 async function invokeAgentForStep(
   ctx: PluginContext,
+  stepRunRecord: WorkflowStepRunRecord,
   stepDef: WorkflowStep,
-  agentName: string,
   workflowName: string,
   companyId: string,
-): Promise<void> {
+): Promise<WorkflowStepRunRecord> {
+  const agentName = getStepAgentName(stepRunRecord, stepDef);
+  if (!agentName) {
+    ctx.logger.warn("Agent name is missing for workflow step", {
+      companyId,
+      runId: stepRunRecord.data.runId,
+      stepId: stepDef.id,
+      workflowName,
+    });
+    return stepRunRecord;
+  }
+
   const agents = await ctx.agents.list({ companyId });
-  const agent = agents.find((candidate: AgentRecord) => candidate.name === agentName);
+  const agent = agents.find((candidate: AgentRecord) => candidate.name === agentName) ?? null;
 
   if (!agent) {
     ctx.logger.warn("Agent not found for step", {
       agentName,
       companyId,
+      runId: stepRunRecord.data.runId,
+      stepId: stepDef.id,
+      workflowName,
+    });
+    return stepRunRecord;
+  }
+
+  const prompt = `workflow:${workflowName}/step:${stepDef.id} — "${stepDef.title}" is ready. Please proceed with the assigned task.`;
+  const reason = `workflow:${workflowName}/step:${stepDef.id}`;
+  const sessionMode = getStepMetadata(stepDef).sessionMode === "reuse" ? "reuse" : "fresh";
+
+  if (sessionMode === "reuse") {
+    let sessionId = typeof stepRunRecord.data.sessionId === "string" && stepRunRecord.data.sessionId.trim()
+      ? stepRunRecord.data.sessionId.trim()
+      : "";
+
+    if (!sessionId) {
+      const session = await ctx.agents.sessions.create(agent.id, companyId, {
+        reason,
+      });
+      sessionId = session.sessionId;
+      stepRunRecord = toWorkflowStepRunRecord(await updateStepRun(ctx, stepRunRecord.id, {
+        sessionId,
+      }));
+    }
+
+    await ctx.agents.sessions.sendMessage(sessionId, companyId, {
+      prompt,
+      reason,
+    });
+
+    ctx.logger.info("Sent workflow step prompt via agent session", {
+      agentId: agent.id,
+      agentName,
+      companyId,
+      runId: stepRunRecord.data.runId,
+      sessionId,
       stepId: stepDef.id,
       workflowName,
     });
-    return;
+    return stepRunRecord;
   }
 
   await ctx.agents.invoke(agent.id, companyId, {
-    prompt: `workflow:${workflowName}/step:${stepDef.id} — "${stepDef.title}" is ready. Please proceed with the assigned task.`,
-    reason: `workflow:${workflowName}/step:${stepDef.id}`,
+    prompt,
+    reason,
   });
 
   ctx.logger.info("Invoked agent for workflow step", {
     agentId: agent.id,
     agentName,
     companyId,
+    runId: stepRunRecord.data.runId,
     stepId: stepDef.id,
     workflowName,
   });
+
+  return stepRunRecord;
 }
 
 async function activateBacklogStep(
   ctx: PluginContext,
-  stepRunRecord: WorkflowStepRecord,
+  stepRunRecord: WorkflowStepRunRecord,
   stepDef: WorkflowStep,
   workflowName: string,
   companyId: string,
-): Promise<void> {
+  options?: { parentIssueId?: string },
+): Promise<WorkflowStepRunRecord> {
   if (stepRunRecord.data.status !== STEP_STATUSES.backlog) {
-    return;
+    return stepRunRecord;
   }
 
-  const updated = toStepRunRecord(await updateStepRun(ctx, stepRunRecord.id, {
+  const resolvedAgent = await resolveStepAgent(
+    ctx,
+    companyId,
+    stepDef,
+    stepRunRecord.data.agentName,
+  );
+
+  let issueId = typeof stepRunRecord.data.issueId === "string" && stepRunRecord.data.issueId.trim()
+    ? stepRunRecord.data.issueId.trim()
+    : "";
+  const stepDescription = getStepDescription(stepDef) ?? `Workflow step: ${stepDef.id}`;
+
+  if (!issueId) {
+    const issue = await ctx.issues.create({
+      assigneeAgentId: resolvedAgent.agentId ?? undefined,
+      companyId,
+      description: stepDescription,
+      parentId: options?.parentIssueId,
+      title: `[${workflowName}] ${stepDef.title}`,
+    });
+    await ctx.issues.update(issue.id, { status: "todo" } as IssueUpdatePatch, companyId);
+    issueId = issue.id;
+  }
+
+  const nextStartedAt = stepRunRecord.data.startedAt ?? new Date().toISOString();
+  let updatedStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, stepRunRecord.id, {
+    agentName: resolvedAgent.agentName ?? stepRunRecord.data.agentName,
+    issueId,
+    startedAt: nextStartedAt,
     status: STEP_STATUSES.todo,
   }));
 
-  await invokeAgentForStep(
+  updatedStepRun = await invokeAgentForStep(
     ctx,
+    updatedStepRun,
     stepDef,
-    updated.data.agentName,
     workflowName,
     companyId,
   );
+
+  return updatedStepRun;
 }
 
+async function startWorkflow(
+  ctx: PluginContext,
+  workflowId: string,
+  companyId: string,
+  options?: {
+    createParentIssue?: boolean;
+    parentIssueId?: string;
+  },
+): Promise<{
+  activatedStepIds: string[];
+  parentIssueId: string | null;
+  runId: string;
+  workflowId: string;
+}> {
+  const workflowDefinition = await getWorkflowDefinition(ctx, workflowId);
+  if (!workflowDefinition) {
+    throw new Error(`Workflow definition not found: ${workflowId}`);
+  }
+
+  const typedWorkflowDefinition = toWorkflowDefinitionRecord(workflowDefinition);
+  if (typedWorkflowDefinition.data.companyId !== companyId) {
+    throw new Error(`Workflow does not belong to company: ${workflowId}`);
+  }
+
+  let parentIssueId = typeof options?.parentIssueId === "string" && options.parentIssueId.trim()
+    ? options.parentIssueId.trim()
+    : "";
+  if (!parentIssueId && options?.createParentIssue) {
+    const parentIssue = await ctx.issues.create({
+      companyId,
+      description: typedWorkflowDefinition.data.description || `Workflow run: ${typedWorkflowDefinition.data.name}`,
+      title: `[${typedWorkflowDefinition.data.name}] Workflow run`,
+    });
+    parentIssueId = parentIssue.id;
+  }
+
+  const workflowRun = toWorkflowRunRecord(await createWorkflowRun(ctx, {
+    companyId,
+    parentIssueId: parentIssueId || undefined,
+    startedAt: new Date().toISOString(),
+    status: RUN_STATUSES.running,
+    workflowId: typedWorkflowDefinition.id,
+    workflowName: typedWorkflowDefinition.data.name,
+  }));
+
+  const agents = await ctx.agents.list({ companyId });
+  const agentsByName = new Map<string, AgentRecord>();
+  for (const agent of agents) {
+    agentsByName.set(agent.name, agent);
+  }
+
+  const pendingRootSteps: Array<{ stepDef: WorkflowStep; stepRun: WorkflowStepRunRecord }> = [];
+  for (const stepDef of typedWorkflowDefinition.data.steps) {
+    const agentNameHint = getStepAgentNameHint(stepDef);
+    const matchedAgent = agentNameHint ? agentsByName.get(agentNameHint) ?? null : null;
+    const resolvedAgent = matchedAgent
+      ? { agentId: matchedAgent.id, agentName: matchedAgent.name }
+      : await resolveStepAgent(ctx, companyId, stepDef, agentNameHint ?? undefined);
+
+    if (!resolvedAgent.agentName) {
+      throw new Error(`Unable to resolve step assignee for "${stepDef.id}"`);
+    }
+
+    const stepRun = toWorkflowStepRunRecord(await createStepRun(ctx, companyId, {
+      agentName: resolvedAgent.agentName,
+      retryCount: 0,
+      runId: workflowRun.id,
+      status: STEP_STATUSES.backlog,
+      stepId: stepDef.id,
+    }));
+
+    if (stepDef.dependsOn.length === 0 && stepDef.triggerOn !== "escalation") {
+      pendingRootSteps.push({ stepDef, stepRun });
+    }
+  }
+
+  const activatedStepIds: string[] = [];
+  for (const pending of pendingRootSteps) {
+    await activateBacklogStep(
+      ctx,
+      pending.stepRun,
+      pending.stepDef,
+      typedWorkflowDefinition.data.name,
+      companyId,
+      { parentIssueId: parentIssueId || undefined },
+    );
+    activatedStepIds.push(pending.stepDef.id);
+  }
+
+  ctx.logger.info("Started workflow run", {
+    activatedStepIds,
+    companyId,
+    parentIssueId: parentIssueId || null,
+    runId: workflowRun.id,
+    workflowId: typedWorkflowDefinition.id,
+    workflowName: typedWorkflowDefinition.data.name,
+  });
+
+  return {
+    activatedStepIds,
+    parentIssueId: parentIssueId || null,
+    runId: workflowRun.id,
+    workflowId: typedWorkflowDefinition.id,
+  };
+}
+
 async function advanceWorkflow(
   ctx: PluginContext,
   stepRunRecord: PluginEntityRecord,
   companyId: string,
 ): Promise<void> {
-  const completedStepRun = toStepRunRecord(stepRunRecord);
+  const completedStepRun = toWorkflowStepRunRecord(stepRunRecord);
   const workflowRun = await getWorkflowRun(ctx, completedStepRun.data.runId);
 
   if (!workflowRun) {
@@ -237,7 +396,7 @@
   }
 
   const typedWorkflowDefinition = toWorkflowDefinitionRecord(workflowDefinition);
-  const stepRuns = (await listStepRuns(ctx, typedWorkflowRun.id, companyId)).map(toStepRunRecord);
+  const stepRuns = (await listStepRuns(ctx, typedWorkflowRun.id, companyId)).map(toWorkflowStepRunRecord);
   const completed = new Set<string>();
   const failed = new Set<string>();
   const skipped = new Set<string>();
@@ -289,7 +448,7 @@
         continue;
       }
 
-      stepRun = toStepRunRecord(await createStepRun(ctx, companyId, {
+      stepRun = toWorkflowStepRunRecord(await createStepRun(ctx, companyId, {
         agentName: resolvedAgent.agentName,
         retryCount: 0,
         runId: typedWorkflowRun.id,
@@ -305,6 +464,7 @@
       stepDef,
       typedWorkflowRun.data.workflowName,
       companyId,
+      { parentIssueId: typedWorkflowRun.data.parentIssueId },
     );
   }
 
@@ -329,7 +489,7 @@
   ctx: PluginContext,
   workflowRun: WorkflowRunRecord,
   workflowDefinition: WorkflowDefinitionRecord,
-  sourceStepRun: WorkflowStepRecord,
+  sourceStepRun: WorkflowStepRunRecord,
   escalationTargetId: string,
   companyId: string,
 ): Promise<void> {
@@ -338,7 +498,7 @@
     throw new Error(`Escalation target step not found: ${escalationTargetId}`);
   }
 
-  const stepRuns = (await listStepRuns(ctx, workflowRun.id, companyId)).map(toStepRunRecord);
+  const stepRuns = (await listStepRuns(ctx, workflowRun.id, companyId)).map(toWorkflowStepRunRecord);
   let escalationStepRun = stepRuns.find((candidate) => candidate.data.stepId === escalationTargetId) ?? null;
   const resolvedAgent = await resolveStepAgent(
     ctx,
@@ -352,7 +512,7 @@
   }
 
   if (!escalationStepRun) {
-    escalationStepRun = toStepRunRecord(await createStepRun(ctx, companyId, {
+    escalationStepRun = toWorkflowStepRunRecord(await createStepRun(ctx, companyId, {
       agentName: resolvedAgent.agentName,
       retryCount: 0,
       runId: workflowRun.id,
@@ -374,14 +534,14 @@
       title: `${workflowRun.data.workflowName}: ${escalationStep.title}`,
     });
 
-    escalationStepRun = toStepRunRecord(await updateStepRun(ctx, escalationStepRun.id, {
+    escalationStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, escalationStepRun.id, {
       issueId: issue.id,
     }));
   }
 
   const shouldActivate = escalationStepRun.data.status === STEP_STATUSES.backlog;
   if (shouldActivate) {
-    escalationStepRun = toStepRunRecord(await updateStepRun(ctx, escalationStepRun.id, {
+    escalationStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, escalationStepRun.id, {
       status: STEP_STATUSES.todo,
     }));
   }
@@ -389,8 +549,8 @@
   if (shouldActivate) {
     await invokeAgentForStep(
       ctx,
+      escalationStepRun,
       escalationStep,
-      escalationStepRun.data.agentName,
       workflowRun.data.workflowName,
       companyId,
     );
@@ -421,7 +581,7 @@
     return;
   }
 
-  const typedStepRun = toStepRunRecord(stepRunRecord);
+  const typedStepRun = toWorkflowStepRunRecord(stepRunRecord);
   if (TERMINAL_STEP_STATUSES.has(typedStepRun.data.status)) {
     return;
   }
@@ -469,7 +629,7 @@
   if (options.allowRetry) {
     const retryInfo = getRetryInfo(typedWorkflowDefinition.data.steps, typedStepRun.data.stepId);
     if (retryInfo.shouldRetry && typedStepRun.data.retryCount < retryInfo.maxRetries) {
-      const retriedStepRun = toStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
+      const retriedStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
         completedAt: undefined,
         retryCount: typedStepRun.data.retryCount + 1,
         status: STEP_STATUSES.todo,
@@ -477,8 +637,8 @@
 
       await invokeAgentForStep(
         ctx,
+        retriedStepRun,
         stepDef,
-        retriedStepRun.data.agentName,
         typedWorkflowRun.data.workflowName,
         event.companyId,
       );
@@ -496,7 +656,7 @@
   }
 
   if (stepDef.onFailure === "skip") {
-    const skippedStepRun = toStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
+    const skippedStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
       completedAt: new Date().toISOString(),
       status: STEP_STATUSES.skipped,
     }));
@@ -556,7 +716,7 @@
       return;
     }
 
-    const escalatedStepRun = toStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
+    const escalatedStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
       completedAt: new Date().toISOString(),
       status: STEP_STATUSES.escalated,
     }));
@@ -617,6 +777,29 @@
   }
 }
 
+function registerDataHandler(
+  ctx: PluginContext,
+  key: string,
+  handler: (params: Record<string, unknown>) => Promise<unknown>,
+): void {
+  const dataClient = ctx.data as {
+    handle?: (handlerKey: string, handlerFn: (params: Record<string, unknown>) => Promise<unknown>) => void;
+    register?: (handlerKey: string, handlerFn: (params: Record<string, unknown>) => Promise<unknown>) => void;
+  };
+
+  if (typeof dataClient.handle === "function") {
+    dataClient.handle(key, handler);
+    return;
+  }
+
+  if (typeof dataClient.register === "function") {
+    dataClient.register(key, handler);
+    return;
+  }
+
+  throw new Error("Plugin data client does not support handler registration");
+}
+
 const plugin = definePlugin({
   async setup(ctx: PluginContext) {
     ctx.events.on("issue.updated", async (event: PluginEvent) => {
@@ -638,7 +821,7 @@
           return;
         }
 
-        const stepRun = toStepRunRecord(stepRunRecord);
+        const stepRun = toWorkflowStepRunRecord(stepRunRecord);
         const payload = event.payload as { status?: string };
         const issueStatus = typeof payload.status === "string" ? payload.status : undefined;
 
@@ -657,7 +840,7 @@
           return;
         }
 
-        const updatedStepRun = toStepRunRecord(await updateStepRun(ctx, stepRun.id, {
+        const updatedStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, stepRun.id, {
           completedAt: nextStepStatus === STEP_STATUSES.done ? new Date().toISOString() : undefined,
           startedAt: nextStepStatus === STEP_STATUSES.inProgress
             ? stepRun.data.startedAt ?? new Date().toISOString()
@@ -687,11 +870,21 @@
 
     ctx.events.on("issue.created", async (event: PluginEvent) => {
       try {
+        const idempotencyKey = buildIdempotencyKey(event);
+        if (await checkIdempotency(ctx, idempotencyKey, event.companyId)) {
+          return;
+        }
+
         const payload = event.payload as {
           id?: string;
           parentId?: string;
           assigneeAgentId?: string;
         };
+        const issueId = typeof event.entityId === "string" && event.entityId.trim()
+          ? event.entityId.trim()
+          : typeof payload.id === "string" && payload.id.trim()
+            ? payload.id.trim()
+            : "";
 
         const parentId = typeof payload.parentId === "string" && payload.parentId.trim()
           ? payload.parentId.trim()
@@ -700,12 +893,34 @@
           ? payload.assigneeAgentId.trim()
           : "";
 
-        if (parentId || !assigneeAgentId) {
+        if (!issueId || parentId || !assigneeAgentId) {
+          await markIdempotency(ctx, idempotencyKey, event.companyId);
+          return;
+        }
+
+        const agent = await ctx.agents.get(assigneeAgentId, event.companyId);
+        const metadata = agent?.metadata as Record<string, unknown> | undefined;
+        const defaultParentIssueId = typeof metadata?.defaultParentIssueId === "string" && metadata.defaultParentIssueId.trim()
+          ? metadata.defaultParentIssueId.trim()
+          : "";
+
+        if (!defaultParentIssueId) {
+          await markIdempotency(ctx, idempotencyKey, event.companyId);
           return;
         }
 
-        ctx.logger.info("parentId filler: issue created without parent", {
-          issueId: event.entityId ?? payload.id ?? null,
+        await ctx.issues.update(
+          issueId,
+          { parentId: defaultParentIssueId } as IssueUpdatePatch,
+          event.companyId,
+        );
+        await markIdempotency(ctx, idempotencyKey, event.companyId);
+
+        ctx.logger.info("parentId filler: populated issue parentId from agent metadata", {
+          assigneeAgentId,
+          companyId: event.companyId,
+          issueId,
+          parentId: defaultParentIssueId,
         });
       } catch (error) {
         ctx.logger.warn("Failed to handle issue.created event", {
@@ -744,7 +959,23 @@
       await runReconciler(ctx);
     });
 
-    ctx.data.register("workflow-overview", async (params: Record<string, unknown>) => {
+    registerDataHandler(ctx, "start-workflow", async (params: Record<string, unknown>) => {
+      const workflowId = typeof params.workflowId === "string" ? params.workflowId.trim() : "";
+      const companyId = typeof params.companyId === "string" ? params.companyId.trim() : "";
+      const parentIssueId = typeof params.parentIssueId === "string" ? params.parentIssueId.trim() : "";
+      const createParentIssue = params.createParentIssue === true;
+
+      if (!workflowId || !companyId) {
+        throw new Error("start-workflow requires workflowId and companyId");
+      }
+
+      return await startWorkflow(ctx, workflowId, companyId, {
+        createParentIssue,
+        parentIssueId: parentIssueId || undefined,
+      });
+    });
+
+    registerDataHandler(ctx, "workflow-overview", async (params: Record<string, unknown>) => {
       try {
         const companyId = typeof params.companyId === "string" ? params.companyId.trim() : "";
         if (!companyId) {
@@ -782,7 +1013,7 @@
       }
     });
 
-    ctx.data.register("workflow-run-detail", async (params: Record<string, unknown>) => {
+    registerDataHandler(ctx, "workflow-run-detail", async (params: Record<string, unknown>) => {
       try {
         const runId = typeof params.runId === "string" ? params.runId.trim() : "";
         if (!runId) {
@@ -807,7 +1038,7 @@
             ...typedWorkflowRun.data,
           },
           stepRuns: stepRuns.map((record: PluginEntityRecord) => {
-            const stepRun = toStepRunRecord(record);
+            const stepRun = toWorkflowStepRunRecord(record);
             return {
               id: stepRun.id,
               status: stepRun.status,
diff --git a/paperclip-addon/plugins/workflow-engine/src/workflow-store.ts b/paperclip-addon/plugins/workflow-engine/src/workflow-store.ts
index 5871a2082258cebcfe8b69ebafbb0814cf303d50..3d7fb2b3bc75911cfd9093eb3a4054cc73196bb9
--- a/paperclip-addon/plugins/workflow-engine/src/workflow-store.ts
+++ b/paperclip-addon/plugins/workflow-engine/src/workflow-store.ts
@@ -44,7 +44,14 @@
 
 type TypedEntityRecord<T> = Omit<PluginEntityRecord, "data"> & { data: T };
 
-function toTypedRecord<T>(record: PluginEntityRecord): TypedEntityRecord<T> {
+function toTypedRecord<T>(
+  record: PluginEntityRecord,
+  entityType: string,
+): TypedEntityRecord<T> {
+  if (record.entityType !== entityType) {
+    throw new Error(`Expected entity type "${entityType}", got "${record.entityType}"`);
+  }
+
   return record as TypedEntityRecord<T>;
 }
 
@@ -67,13 +74,26 @@
   id: string,
   entityType: string,
 ): Promise<TypedEntityRecord<T> | null> {
+  const listQuery: Parameters<PluginContext["entities"]["list"]>[0] & { id?: string } = {
+    entityType,
+    id,
+    limit: 1,
+  };
+
+  const listed = await ctx.entities.list(listQuery);
+  const listedRecord = listed.find((record) => record.id === id) ?? null;
+  if (listedRecord) {
+    return toTypedRecord<T>(listedRecord, entityType);
+  }
+
+  // Fallback: some host SDK variants ignore `id` on entities.list().
+  // Keep defensive type verification on direct get() until id-filter support is universal.
   const record = await ctx.entities.get(id);
-
-  if (!record || record.entityType !== entityType) {
+  if (!record) {
     return null;
   }
 
-  return toTypedRecord<T>(record);
+  return toTypedRecord<T>(record, entityType);
 }
 
 async function requireEntityByType<T>(
diff --git a/paperclip-addon/plugins/workflow-engine/src/workflow-utils.ts b/paperclip-addon/plugins/workflow-engine/src/workflow-utils.ts
new file mode 100644
index 0000000000000000000000000000000000000000..ec1cf6f6b9aa722b126b58c0a728b4e538ad1494
--- /dev/null
+++ b/paperclip-addon/plugins/workflow-engine/src/workflow-utils.ts
@@ -0,0 +1,62 @@
+import type { PluginEntityRecord } from "@paperclipai/plugin-sdk";
+
+import { STEP_STATUSES } from "./constants.js";
+import type { WorkflowStep } from "./dag-engine.js";
+import type {
+  WorkflowDefinition,
+  WorkflowRun,
+  WorkflowStepRun,
+} from "./workflow-store.js";
+
+export type WorkflowRunRecord = PluginEntityRecord & { data: WorkflowRun };
+export type WorkflowStepRunRecord = PluginEntityRecord & { data: WorkflowStepRun };
+export type WorkflowDefinitionRecord = PluginEntityRecord & { data: WorkflowDefinition };
+type StepAgentMetadata = WorkflowStep & {
+  agent?: string;
+  agentName?: string;
+  assigneeAgentName?: string;
+};
+
+export const TERMINAL_STEP_STATUSES = new Set<string>([
+  STEP_STATUSES.done,
+  STEP_STATUSES.failed,
+  STEP_STATUSES.skipped,
+  STEP_STATUSES.escalated,
+]);
+
+export function toWorkflowRunRecord(record: PluginEntityRecord): WorkflowRunRecord {
+  return record as WorkflowRunRecord;
+}
+
+export function toWorkflowStepRunRecord(record: PluginEntityRecord): WorkflowStepRunRecord {
+  return record as WorkflowStepRunRecord;
+}
+
+export function toWorkflowDefinitionRecord(record: PluginEntityRecord): WorkflowDefinitionRecord {
+  return record as WorkflowDefinitionRecord;
+}
+
+export function findStepDefinition(definition: WorkflowDefinitionRecord, stepId: string): WorkflowStep | null {
+  return definition.data.steps.find((step: WorkflowStep) => step.id === stepId) ?? null;
+}
+
+export function getStepAgentNameHint(stepDef: WorkflowStep): string | null {
+  const stepMeta = stepDef as StepAgentMetadata;
+  const candidates = [stepMeta.agentName, stepMeta.agent, stepMeta.assigneeAgentName];
+
+  for (const candidate of candidates) {
+    if (typeof candidate === "string" && candidate.trim()) {
+      return candidate.trim();
+    }
+  }
+
+  return null;
+}
+
+export function getStepAgentName(stepRun: WorkflowStepRunRecord, stepDef: WorkflowStep): string | null {
+  if (typeof stepRun.data.agentName === "string" && stepRun.data.agentName.trim()) {
+    return stepRun.data.agentName.trim();
+  }
+
+  return getStepAgentNameHint(stepDef);
+}

file update:
diff --git a/paperclip-addon/plugins/workflow-engine/src/dag-engine.ts b/paperclip-addon/plugins/workflow-engine/src/dag-engine.ts
index b59510f0473171038be77277286d3b5c2063e6c5..e7f21ff998cab78b9325a8aeacf4fda9c5e06553
--- a/paperclip-addon/plugins/workflow-engine/src/dag-engine.ts
+++ b/paperclip-addon/plugins/workflow-engine/src/dag-engine.ts
@@ -2,6 +2,7 @@
   id: string;
   title: string;
   dependsOn: string[];
+  sessionMode?: "fresh" | "reuse";
   onFailure?: "retry" | "skip" | "abort_workflow" | "escalate";
   escalateTo?: string;
   maxRetries?: number;
diff --git a/paperclip-addon/plugins/workflow-engine/src/reconciler.ts b/paperclip-addon/plugins/workflow-engine/src/reconciler.ts
index 14ab4e766a835d145a6c2e7b1b564270285cbdc3..d2ab54817bd8185944379d2188bfbdef1722020a
--- a/paperclip-addon/plugins/workflow-engine/src/reconciler.ts
+++ b/paperclip-addon/plugins/workflow-engine/src/reconciler.ts
@@ -8,65 +8,22 @@
   listStepRuns,
   updateStepRun,
   updateWorkflowRun,
-  type WorkflowDefinition,
-  type WorkflowRun,
-  type WorkflowStepRun,
 } from "./workflow-store.js";
 import type { WorkflowStep } from "./dag-engine.js";
-
-type WorkflowRunRecord = PluginEntityRecord & { data: WorkflowRun };
-type WorkflowStepRunRecord = PluginEntityRecord & { data: WorkflowStepRun };
-type WorkflowDefinitionRecord = PluginEntityRecord & { data: WorkflowDefinition };
-type StepAgentMetadata = WorkflowStep & {
-  agent?: string;
-  agentName?: string;
-  assigneeAgentName?: string;
-};
+import {
+  TERMINAL_STEP_STATUSES,
+  findStepDefinition,
+  getStepAgentName,
+  toWorkflowDefinitionRecord,
+  toWorkflowRunRecord,
+  toWorkflowStepRunRecord,
+} from "./workflow-utils.js";
 
 const STALE_THRESHOLD_MS = 300_000;
-const TERMINAL_STEP_STATUSES = new Set<string>([
-  STEP_STATUSES.done,
-  STEP_STATUSES.failed,
-  STEP_STATUSES.skipped,
-  STEP_STATUSES.escalated,
-]);
 
 function isStale(record: PluginEntityRecord, thresholdMs: number): boolean {
   const updatedAt = new Date(record.updatedAt).getTime();
   return Date.now() - updatedAt > thresholdMs;
-}
-
-function toWorkflowRunRecord(record: PluginEntityRecord): WorkflowRunRecord {
-  return record as WorkflowRunRecord;
-}
-
-function toWorkflowStepRunRecord(record: PluginEntityRecord): WorkflowStepRunRecord {
-  return record as WorkflowStepRunRecord;
-}
-
-function toWorkflowDefinitionRecord(record: PluginEntityRecord): WorkflowDefinitionRecord {
-  return record as WorkflowDefinitionRecord;
-}
-
-function findStepDefinition(definition: WorkflowDefinitionRecord, stepId: string): WorkflowStep | null {
-  return definition.data.steps.find((step: WorkflowStep) => step.id === stepId) ?? null;
-}
-
-function getStepAgentName(stepRun: WorkflowStepRunRecord, stepDef: WorkflowStep): string | null {
-  if (typeof stepRun.data.agentName === "string" && stepRun.data.agentName.trim()) {
-    return stepRun.data.agentName.trim();
-  }
-
-  const stepMeta = stepDef as StepAgentMetadata;
-  const candidates = [stepMeta.agentName, stepMeta.agent, stepMeta.assigneeAgentName];
-
-  for (const candidate of candidates) {
-    if (typeof candidate === "string" && candidate.trim()) {
-      return candidate.trim();
-    }
-  }
-
-  return null;
 }
 
 async function invokeAgentByName(
diff --git a/paperclip-addon/plugins/workflow-engine/src/worker.ts b/paperclip-addon/plugins/workflow-engine/src/worker.ts
index c5ac3fbb4ca7f767eb29331bd3630b635449b335..303e35958890fa1f66db3f2b0ff1fa1f134102a8
--- a/paperclip-addon/plugins/workflow-engine/src/worker.ts
+++ b/paperclip-addon/plugins/workflow-engine/src/worker.ts
@@ -15,6 +15,7 @@
 } from "./dag-engine.js";
 import {
   checkIdempotency,
+  createWorkflowRun,
   createStepRun,
   findStepRunByIssueId,
   getWorkflowDefinition,
@@ -25,51 +26,38 @@
   markIdempotency,
   updateStepRun,
   updateWorkflowRun,
-  type WorkflowDefinition,
-  type WorkflowRun,
   type WorkflowStepRun,
 } from "./workflow-store.js";
+import {
+  TERMINAL_STEP_STATUSES,
+  findStepDefinition,
+  getStepAgentName,
+  getStepAgentNameHint,
+  toWorkflowDefinitionRecord,
+  toWorkflowRunRecord,
+  toWorkflowStepRunRecord,
+  type WorkflowDefinitionRecord,
+  type WorkflowRunRecord,
+  type WorkflowStepRunRecord,
+} from "./workflow-utils.js";
 
-type WorkflowStepRecord = PluginEntityRecord & { data: WorkflowStepRun };
-type WorkflowRunRecord = PluginEntityRecord & { data: WorkflowRun };
-type WorkflowDefinitionRecord = PluginEntityRecord & { data: WorkflowDefinition };
 type WorkflowStepMetadata = WorkflowStep & {
   description?: string;
-  agent?: string;
-  agentName?: string;
-  assigneeAgentName?: string;
   assigneeAgentId?: string;
+  sessionMode?: "fresh" | "reuse";
 };
 type AgentRecord = Awaited<ReturnType<PluginContext["agents"]["list"]>>[number];
+type IssueUpdatePatch = Parameters<PluginContext["issues"]["update"]>[1];
 type ReconcilerModule = {
   reconcileStuckSteps?: (ctx: PluginContext) => Promise<void>;
 };
 
-const TERMINAL_STEP_STATUSES = new Set<string>([
-  STEP_STATUSES.done,
-  STEP_STATUSES.failed,
-  STEP_STATUSES.skipped,
-  STEP_STATUSES.escalated,
-]);
-
 function summarizeError(error: unknown): string {
   if (error instanceof Error) {
     return error.message;
   }
 
   return String(error);
-}
-
-function toStepRunRecord(record: PluginEntityRecord): WorkflowStepRecord {
-  return record as WorkflowStepRecord;
-}
-
-function toWorkflowRunRecord(record: PluginEntityRecord): WorkflowRunRecord {
-  return record as WorkflowRunRecord;
-}
-
-function toWorkflowDefinitionRecord(record: PluginEntityRecord): WorkflowDefinitionRecord {
-  return record as WorkflowDefinitionRecord;
 }
 
 function buildIdempotencyKey(event: PluginEvent): string {
@@ -78,10 +66,6 @@
 
 function getStepMetadata(stepDef: WorkflowStep): WorkflowStepMetadata {
   return stepDef as WorkflowStepMetadata;
-}
-
-function findStepDefinition(definition: WorkflowDefinitionRecord, stepId: string): WorkflowStep | null {
-  return definition.data.steps.find((step: WorkflowStep) => step.id === stepId) ?? null;
 }
 
 function getStepDescription(stepDef: WorkflowStep): string | undefined {
@@ -89,19 +73,6 @@
   return typeof description === "string" && description.trim() ? description.trim() : undefined;
 }
 
-function getStepAgentNameHint(stepDef: WorkflowStep): string | null {
-  const meta = getStepMetadata(stepDef);
-  const candidates = [meta.agentName, meta.agent, meta.assigneeAgentName];
-
-  for (const candidate of candidates) {
-    if (typeof candidate === "string" && candidate.trim()) {
-      return candidate.trim();
-    }
-  }
-
-  return null;
-}
-
 function getStepAgentIdHint(stepDef: WorkflowStep): string | null {
   const assigneeAgentId = getStepMetadata(stepDef).assigneeAgentId;
   if (typeof assigneeAgentId !== "string" || !assigneeAgentId.trim()) {
@@ -147,68 +118,256 @@
 
 async function invokeAgentForStep(
   ctx: PluginContext,
+  stepRunRecord: WorkflowStepRunRecord,
   stepDef: WorkflowStep,
-  agentName: string,
   workflowName: string,
   companyId: string,
-): Promise<void> {
+): Promise<WorkflowStepRunRecord> {
+  const agentName = getStepAgentName(stepRunRecord, stepDef);
+  if (!agentName) {
+    ctx.logger.warn("Agent name is missing for workflow step", {
+      companyId,
+      runId: stepRunRecord.data.runId,
+      stepId: stepDef.id,
+      workflowName,
+    });
+    return stepRunRecord;
+  }
+
   const agents = await ctx.agents.list({ companyId });
-  const agent = agents.find((candidate: AgentRecord) => candidate.name === agentName);
+  const agent = agents.find((candidate: AgentRecord) => candidate.name === agentName) ?? null;
 
   if (!agent) {
     ctx.logger.warn("Agent not found for step", {
       agentName,
       companyId,
+      runId: stepRunRecord.data.runId,
+      stepId: stepDef.id,
+      workflowName,
+    });
+    return stepRunRecord;
+  }
+
+  const prompt = `workflow:${workflowName}/step:${stepDef.id} — "${stepDef.title}" is ready. Please proceed with the assigned task.`;
+  const reason = `workflow:${workflowName}/step:${stepDef.id}`;
+  const sessionMode = getStepMetadata(stepDef).sessionMode === "reuse" ? "reuse" : "fresh";
+
+  if (sessionMode === "reuse") {
+    let sessionId = typeof stepRunRecord.data.sessionId === "string" && stepRunRecord.data.sessionId.trim()
+      ? stepRunRecord.data.sessionId.trim()
+      : "";
+
+    if (!sessionId) {
+      const session = await ctx.agents.sessions.create(agent.id, companyId, {
+        reason,
+      });
+      sessionId = session.sessionId;
+      stepRunRecord = toWorkflowStepRunRecord(await updateStepRun(ctx, stepRunRecord.id, {
+        sessionId,
+      }));
+    }
+
+    await ctx.agents.sessions.sendMessage(sessionId, companyId, {
+      prompt,
+      reason,
+    });
+
+    ctx.logger.info("Sent workflow step prompt via agent session", {
+      agentId: agent.id,
+      agentName,
+      companyId,
+      runId: stepRunRecord.data.runId,
+      sessionId,
       stepId: stepDef.id,
       workflowName,
     });
-    return;
+    return stepRunRecord;
   }
 
   await ctx.agents.invoke(agent.id, companyId, {
-    prompt: `workflow:${workflowName}/step:${stepDef.id} — "${stepDef.title}" is ready. Please proceed with the assigned task.`,
-    reason: `workflow:${workflowName}/step:${stepDef.id}`,
+    prompt,
+    reason,
   });
 
   ctx.logger.info("Invoked agent for workflow step", {
     agentId: agent.id,
     agentName,
     companyId,
+    runId: stepRunRecord.data.runId,
     stepId: stepDef.id,
     workflowName,
   });
+
+  return stepRunRecord;
 }
 
 async function activateBacklogStep(
   ctx: PluginContext,
-  stepRunRecord: WorkflowStepRecord,
+  stepRunRecord: WorkflowStepRunRecord,
   stepDef: WorkflowStep,
   workflowName: string,
   companyId: string,
-): Promise<void> {
+  options?: { parentIssueId?: string },
+): Promise<WorkflowStepRunRecord> {
   if (stepRunRecord.data.status !== STEP_STATUSES.backlog) {
-    return;
+    return stepRunRecord;
   }
 
-  const updated = toStepRunRecord(await updateStepRun(ctx, stepRunRecord.id, {
+  const resolvedAgent = await resolveStepAgent(
+    ctx,
+    companyId,
+    stepDef,
+    stepRunRecord.data.agentName,
+  );
+
+  let issueId = typeof stepRunRecord.data.issueId === "string" && stepRunRecord.data.issueId.trim()
+    ? stepRunRecord.data.issueId.trim()
+    : "";
+  const stepDescription = getStepDescription(stepDef) ?? `Workflow step: ${stepDef.id}`;
+
+  if (!issueId) {
+    const issue = await ctx.issues.create({
+      assigneeAgentId: resolvedAgent.agentId ?? undefined,
+      companyId,
+      description: stepDescription,
+      parentId: options?.parentIssueId,
+      title: `[${workflowName}] ${stepDef.title}`,
+    });
+    await ctx.issues.update(issue.id, { status: "todo" } as IssueUpdatePatch, companyId);
+    issueId = issue.id;
+  }
+
+  const nextStartedAt = stepRunRecord.data.startedAt ?? new Date().toISOString();
+  let updatedStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, stepRunRecord.id, {
+    agentName: resolvedAgent.agentName ?? stepRunRecord.data.agentName,
+    issueId,
+    startedAt: nextStartedAt,
     status: STEP_STATUSES.todo,
   }));
 
-  await invokeAgentForStep(
+  updatedStepRun = await invokeAgentForStep(
     ctx,
+    updatedStepRun,
     stepDef,
-    updated.data.agentName,
     workflowName,
     companyId,
   );
+
+  return updatedStepRun;
 }
 
+async function startWorkflow(
+  ctx: PluginContext,
+  workflowId: string,
+  companyId: string,
+  options?: {
+    createParentIssue?: boolean;
+    parentIssueId?: string;
+  },
+): Promise<{
+  activatedStepIds: string[];
+  parentIssueId: string | null;
+  runId: string;
+  workflowId: string;
+}> {
+  const workflowDefinition = await getWorkflowDefinition(ctx, workflowId);
+  if (!workflowDefinition) {
+    throw new Error(`Workflow definition not found: ${workflowId}`);
+  }
+
+  const typedWorkflowDefinition = toWorkflowDefinitionRecord(workflowDefinition);
+  if (typedWorkflowDefinition.data.companyId !== companyId) {
+    throw new Error(`Workflow does not belong to company: ${workflowId}`);
+  }
+
+  let parentIssueId = typeof options?.parentIssueId === "string" && options.parentIssueId.trim()
+    ? options.parentIssueId.trim()
+    : "";
+  if (!parentIssueId && options?.createParentIssue) {
+    const parentIssue = await ctx.issues.create({
+      companyId,
+      description: typedWorkflowDefinition.data.description || `Workflow run: ${typedWorkflowDefinition.data.name}`,
+      title: `[${typedWorkflowDefinition.data.name}] Workflow run`,
+    });
+    parentIssueId = parentIssue.id;
+  }
+
+  const workflowRun = toWorkflowRunRecord(await createWorkflowRun(ctx, {
+    companyId,
+    parentIssueId: parentIssueId || undefined,
+    startedAt: new Date().toISOString(),
+    status: RUN_STATUSES.running,
+    workflowId: typedWorkflowDefinition.id,
+    workflowName: typedWorkflowDefinition.data.name,
+  }));
+
+  const agents = await ctx.agents.list({ companyId });
+  const agentsByName = new Map<string, AgentRecord>();
+  for (const agent of agents) {
+    agentsByName.set(agent.name, agent);
+  }
+
+  const pendingRootSteps: Array<{ stepDef: WorkflowStep; stepRun: WorkflowStepRunRecord }> = [];
+  for (const stepDef of typedWorkflowDefinition.data.steps) {
+    const agentNameHint = getStepAgentNameHint(stepDef);
+    const matchedAgent = agentNameHint ? agentsByName.get(agentNameHint) ?? null : null;
+    const resolvedAgent = matchedAgent
+      ? { agentId: matchedAgent.id, agentName: matchedAgent.name }
+      : await resolveStepAgent(ctx, companyId, stepDef, agentNameHint ?? undefined);
+
+    if (!resolvedAgent.agentName) {
+      throw new Error(`Unable to resolve step assignee for "${stepDef.id}"`);
+    }
+
+    const stepRun = toWorkflowStepRunRecord(await createStepRun(ctx, companyId, {
+      agentName: resolvedAgent.agentName,
+      retryCount: 0,
+      runId: workflowRun.id,
+      status: STEP_STATUSES.backlog,
+      stepId: stepDef.id,
+    }));
+
+    if (stepDef.dependsOn.length === 0 && stepDef.triggerOn !== "escalation") {
+      pendingRootSteps.push({ stepDef, stepRun });
+    }
+  }
+
+  const activatedStepIds: string[] = [];
+  for (const pending of pendingRootSteps) {
+    await activateBacklogStep(
+      ctx,
+      pending.stepRun,
+      pending.stepDef,
+      typedWorkflowDefinition.data.name,
+      companyId,
+      { parentIssueId: parentIssueId || undefined },
+    );
+    activatedStepIds.push(pending.stepDef.id);
+  }
+
+  ctx.logger.info("Started workflow run", {
+    activatedStepIds,
+    companyId,
+    parentIssueId: parentIssueId || null,
+    runId: workflowRun.id,
+    workflowId: typedWorkflowDefinition.id,
+    workflowName: typedWorkflowDefinition.data.name,
+  });
+
+  return {
+    activatedStepIds,
+    parentIssueId: parentIssueId || null,
+    runId: workflowRun.id,
+    workflowId: typedWorkflowDefinition.id,
+  };
+}
+
 async function advanceWorkflow(
   ctx: PluginContext,
   stepRunRecord: PluginEntityRecord,
   companyId: string,
 ): Promise<void> {
-  const completedStepRun = toStepRunRecord(stepRunRecord);
+  const completedStepRun = toWorkflowStepRunRecord(stepRunRecord);
   const workflowRun = await getWorkflowRun(ctx, completedStepRun.data.runId);
 
   if (!workflowRun) {
@@ -237,7 +396,7 @@
   }
 
   const typedWorkflowDefinition = toWorkflowDefinitionRecord(workflowDefinition);
-  const stepRuns = (await listStepRuns(ctx, typedWorkflowRun.id, companyId)).map(toStepRunRecord);
+  const stepRuns = (await listStepRuns(ctx, typedWorkflowRun.id, companyId)).map(toWorkflowStepRunRecord);
   const completed = new Set<string>();
   const failed = new Set<string>();
   const skipped = new Set<string>();
@@ -289,7 +448,7 @@
         continue;
       }
 
-      stepRun = toStepRunRecord(await createStepRun(ctx, companyId, {
+      stepRun = toWorkflowStepRunRecord(await createStepRun(ctx, companyId, {
         agentName: resolvedAgent.agentName,
         retryCount: 0,
         runId: typedWorkflowRun.id,
@@ -305,6 +464,7 @@
       stepDef,
       typedWorkflowRun.data.workflowName,
       companyId,
+      { parentIssueId: typedWorkflowRun.data.parentIssueId },
     );
   }
 
@@ -329,7 +489,7 @@
   ctx: PluginContext,
   workflowRun: WorkflowRunRecord,
   workflowDefinition: WorkflowDefinitionRecord,
-  sourceStepRun: WorkflowStepRecord,
+  sourceStepRun: WorkflowStepRunRecord,
   escalationTargetId: string,
   companyId: string,
 ): Promise<void> {
@@ -338,7 +498,7 @@
     throw new Error(`Escalation target step not found: ${escalationTargetId}`);
   }
 
-  const stepRuns = (await listStepRuns(ctx, workflowRun.id, companyId)).map(toStepRunRecord);
+  const stepRuns = (await listStepRuns(ctx, workflowRun.id, companyId)).map(toWorkflowStepRunRecord);
   let escalationStepRun = stepRuns.find((candidate) => candidate.data.stepId === escalationTargetId) ?? null;
   const resolvedAgent = await resolveStepAgent(
     ctx,
@@ -352,7 +512,7 @@
   }
 
   if (!escalationStepRun) {
-    escalationStepRun = toStepRunRecord(await createStepRun(ctx, companyId, {
+    escalationStepRun = toWorkflowStepRunRecord(await createStepRun(ctx, companyId, {
       agentName: resolvedAgent.agentName,
       retryCount: 0,
       runId: workflowRun.id,
@@ -374,14 +534,14 @@
       title: `${workflowRun.data.workflowName}: ${escalationStep.title}`,
     });
 
-    escalationStepRun = toStepRunRecord(await updateStepRun(ctx, escalationStepRun.id, {
+    escalationStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, escalationStepRun.id, {
       issueId: issue.id,
     }));
   }
 
   const shouldActivate = escalationStepRun.data.status === STEP_STATUSES.backlog;
   if (shouldActivate) {
-    escalationStepRun = toStepRunRecord(await updateStepRun(ctx, escalationStepRun.id, {
+    escalationStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, escalationStepRun.id, {
       status: STEP_STATUSES.todo,
     }));
   }
@@ -389,8 +549,8 @@
   if (shouldActivate) {
     await invokeAgentForStep(
       ctx,
+      escalationStepRun,
       escalationStep,
-      escalationStepRun.data.agentName,
       workflowRun.data.workflowName,
       companyId,
     );
@@ -421,7 +581,7 @@
     return;
   }
 
-  const typedStepRun = toStepRunRecord(stepRunRecord);
+  const typedStepRun = toWorkflowStepRunRecord(stepRunRecord);
   if (TERMINAL_STEP_STATUSES.has(typedStepRun.data.status)) {
     return;
   }
@@ -469,7 +629,7 @@
   if (options.allowRetry) {
     const retryInfo = getRetryInfo(typedWorkflowDefinition.data.steps, typedStepRun.data.stepId);
     if (retryInfo.shouldRetry && typedStepRun.data.retryCount < retryInfo.maxRetries) {
-      const retriedStepRun = toStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
+      const retriedStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
         completedAt: undefined,
         retryCount: typedStepRun.data.retryCount + 1,
         status: STEP_STATUSES.todo,
@@ -477,8 +637,8 @@
 
       await invokeAgentForStep(
         ctx,
+        retriedStepRun,
         stepDef,
-        retriedStepRun.data.agentName,
         typedWorkflowRun.data.workflowName,
         event.companyId,
       );
@@ -496,7 +656,7 @@
   }
 
   if (stepDef.onFailure === "skip") {
-    const skippedStepRun = toStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
+    const skippedStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
       completedAt: new Date().toISOString(),
       status: STEP_STATUSES.skipped,
     }));
@@ -556,7 +716,7 @@
       return;
     }
 
-    const escalatedStepRun = toStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
+    const escalatedStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
       completedAt: new Date().toISOString(),
       status: STEP_STATUSES.escalated,
     }));
@@ -617,6 +777,29 @@
   }
 }
 
+function registerDataHandler(
+  ctx: PluginContext,
+  key: string,
+  handler: (params: Record<string, unknown>) => Promise<unknown>,
+): void {
+  const dataClient = ctx.data as {
+    handle?: (handlerKey: string, handlerFn: (params: Record<string, unknown>) => Promise<unknown>) => void;
+    register?: (handlerKey: string, handlerFn: (params: Record<string, unknown>) => Promise<unknown>) => void;
+  };
+
+  if (typeof dataClient.handle === "function") {
+    dataClient.handle(key, handler);
+    return;
+  }
+
+  if (typeof dataClient.register === "function") {
+    dataClient.register(key, handler);
+    return;
+  }
+
+  throw new Error("Plugin data client does not support handler registration");
+}
+
 const plugin = definePlugin({
   async setup(ctx: PluginContext) {
     ctx.events.on("issue.updated", async (event: PluginEvent) => {
@@ -638,7 +821,7 @@
           return;
         }
 
-        const stepRun = toStepRunRecord(stepRunRecord);
+        const stepRun = toWorkflowStepRunRecord(stepRunRecord);
         const payload = event.payload as { status?: string };
         const issueStatus = typeof payload.status === "string" ? payload.status : undefined;
 
@@ -657,7 +840,7 @@
           return;
         }
 
-        const updatedStepRun = toStepRunRecord(await updateStepRun(ctx, stepRun.id, {
+        const updatedStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, stepRun.id, {
           completedAt: nextStepStatus === STEP_STATUSES.done ? new Date().toISOString() : undefined,
           startedAt: nextStepStatus === STEP_STATUSES.inProgress
             ? stepRun.data.startedAt ?? new Date().toISOString()
@@ -687,11 +870,21 @@
 
     ctx.events.on("issue.created", async (event: PluginEvent) => {
       try {
+        const idempotencyKey = buildIdempotencyKey(event);
+        if (await checkIdempotency(ctx, idempotencyKey, event.companyId)) {
+          return;
+        }
+
         const payload = event.payload as {
           id?: string;
           parentId?: string;
           assigneeAgentId?: string;
         };
+        const issueId = typeof event.entityId === "string" && event.entityId.trim()
+          ? event.entityId.trim()
+          : typeof payload.id === "string" && payload.id.trim()
+            ? payload.id.trim()
+            : "";
 
         const parentId = typeof payload.parentId === "string" && payload.parentId.trim()
           ? payload.parentId.trim()
@@ -700,12 +893,34 @@
           ? payload.assigneeAgentId.trim()
           : "";
 
-        if (parentId || !assigneeAgentId) {
+        if (!issueId || parentId || !assigneeAgentId) {
+          await markIdempotency(ctx, idempotencyKey, event.companyId);
+          return;
+        }
+
+        const agent = await ctx.agents.get(assigneeAgentId, event.companyId);
+        const metadata = agent?.metadata as Record<string, unknown> | undefined;
+        const defaultParentIssueId = typeof metadata?.defaultParentIssueId === "string" && metadata.defaultParentIssueId.trim()
+          ? metadata.defaultParentIssueId.trim()
+          : "";
+
+        if (!defaultParentIssueId) {
+          await markIdempotency(ctx, idempotencyKey, event.companyId);
           return;
         }
 
-        ctx.logger.info("parentId filler: issue created without parent", {
-          issueId: event.entityId ?? payload.id ?? null,
+        await ctx.issues.update(
+          issueId,
+          { parentId: defaultParentIssueId } as IssueUpdatePatch,
+          event.companyId,
+        );
+        await markIdempotency(ctx, idempotencyKey, event.companyId);
+
+        ctx.logger.info("parentId filler: populated issue parentId from agent metadata", {
+          assigneeAgentId,
+          companyId: event.companyId,
+          issueId,
+          parentId: defaultParentIssueId,
         });
       } catch (error) {
         ctx.logger.warn("Failed to handle issue.created event", {
@@ -744,7 +959,23 @@
       await runReconciler(ctx);
     });
 
-    ctx.data.register("workflow-overview", async (params: Record<string, unknown>) => {
+    registerDataHandler(ctx, "start-workflow", async (params: Record<string, unknown>) => {
+      const workflowId = typeof params.workflowId === "string" ? params.workflowId.trim() : "";
+      const companyId = typeof params.companyId === "string" ? params.companyId.trim() : "";
+      const parentIssueId = typeof params.parentIssueId === "string" ? params.parentIssueId.trim() : "";
+      const createParentIssue = params.createParentIssue === true;
+
+      if (!workflowId || !companyId) {
+        throw new Error("start-workflow requires workflowId and companyId");
+      }
+
+      return await startWorkflow(ctx, workflowId, companyId, {
+        createParentIssue,
+        parentIssueId: parentIssueId || undefined,
+      });
+    });
+
+    registerDataHandler(ctx, "workflow-overview", async (params: Record<string, unknown>) => {
       try {
         const companyId = typeof params.companyId === "string" ? params.companyId.trim() : "";
         if (!companyId) {
@@ -782,7 +1013,7 @@
       }
     });
 
-    ctx.data.register("workflow-run-detail", async (params: Record<string, unknown>) => {
+    registerDataHandler(ctx, "workflow-run-detail", async (params: Record<string, unknown>) => {
       try {
         const runId = typeof params.runId === "string" ? params.runId.trim() : "";
         if (!runId) {
@@ -807,7 +1038,7 @@
             ...typedWorkflowRun.data,
           },
           stepRuns: stepRuns.map((record: PluginEntityRecord) => {
-            const stepRun = toStepRunRecord(record);
+            const stepRun = toWorkflowStepRunRecord(record);
             return {
               id: stepRun.id,
               status: stepRun.status,
diff --git a/paperclip-addon/plugins/workflow-engine/src/workflow-store.ts b/paperclip-addon/plugins/workflow-engine/src/workflow-store.ts
index 5871a2082258cebcfe8b69ebafbb0814cf303d50..3d7fb2b3bc75911cfd9093eb3a4054cc73196bb9
--- a/paperclip-addon/plugins/workflow-engine/src/workflow-store.ts
+++ b/paperclip-addon/plugins/workflow-engine/src/workflow-store.ts
@@ -44,7 +44,14 @@
 
 type TypedEntityRecord<T> = Omit<PluginEntityRecord, "data"> & { data: T };
 
-function toTypedRecord<T>(record: PluginEntityRecord): TypedEntityRecord<T> {
+function toTypedRecord<T>(
+  record: PluginEntityRecord,
+  entityType: string,
+): TypedEntityRecord<T> {
+  if (record.entityType !== entityType) {
+    throw new Error(`Expected entity type "${entityType}", got "${record.entityType}"`);
+  }
+
   return record as TypedEntityRecord<T>;
 }
 
@@ -67,13 +74,26 @@
   id: string,
   entityType: string,
 ): Promise<TypedEntityRecord<T> | null> {
+  const listQuery: Parameters<PluginContext["entities"]["list"]>[0] & { id?: string } = {
+    entityType,
+    id,
+    limit: 1,
+  };
+
+  const listed = await ctx.entities.list(listQuery);
+  const listedRecord = listed.find((record) => record.id === id) ?? null;
+  if (listedRecord) {
+    return toTypedRecord<T>(listedRecord, entityType);
+  }
+
+  // Fallback: some host SDK variants ignore `id` on entities.list().
+  // Keep defensive type verification on direct get() until id-filter support is universal.
   const record = await ctx.entities.get(id);
-
-  if (!record || record.entityType !== entityType) {
+  if (!record) {
     return null;
   }
 
-  return toTypedRecord<T>(record);
+  return toTypedRecord<T>(record, entityType);
 }
 
 async function requireEntityByType<T>(
diff --git a/paperclip-addon/plugins/workflow-engine/src/workflow-utils.ts b/paperclip-addon/plugins/workflow-engine/src/workflow-utils.ts
new file mode 100644
index 0000000000000000000000000000000000000000..ec1cf6f6b9aa722b126b58c0a728b4e538ad1494
--- /dev/null
+++ b/paperclip-addon/plugins/workflow-engine/src/workflow-utils.ts
@@ -0,0 +1,62 @@
+import type { PluginEntityRecord } from "@paperclipai/plugin-sdk";
+
+import { STEP_STATUSES } from "./constants.js";
+import type { WorkflowStep } from "./dag-engine.js";
+import type {
+  WorkflowDefinition,
+  WorkflowRun,
+  WorkflowStepRun,
+} from "./workflow-store.js";
+
+export type WorkflowRunRecord = PluginEntityRecord & { data: WorkflowRun };
+export type WorkflowStepRunRecord = PluginEntityRecord & { data: WorkflowStepRun };
+export type WorkflowDefinitionRecord = PluginEntityRecord & { data: WorkflowDefinition };
+type StepAgentMetadata = WorkflowStep & {
+  agent?: string;
+  agentName?: string;
+  assigneeAgentName?: string;
+};
+
+export const TERMINAL_STEP_STATUSES = new Set<string>([
+  STEP_STATUSES.done,
+  STEP_STATUSES.failed,
+  STEP_STATUSES.skipped,
+  STEP_STATUSES.escalated,
+]);
+
+export function toWorkflowRunRecord(record: PluginEntityRecord): WorkflowRunRecord {
+  return record as WorkflowRunRecord;
+}
+
+export function toWorkflowStepRunRecord(record: PluginEntityRecord): WorkflowStepRunRecord {
+  return record as WorkflowStepRunRecord;
+}
+
+export function toWorkflowDefinitionRecord(record: PluginEntityRecord): WorkflowDefinitionRecord {
+  return record as WorkflowDefinitionRecord;
+}
+
+export function findStepDefinition(definition: WorkflowDefinitionRecord, stepId: string): WorkflowStep | null {
+  return definition.data.steps.find((step: WorkflowStep) => step.id === stepId) ?? null;
+}
+
+export function getStepAgentNameHint(stepDef: WorkflowStep): string | null {
+  const stepMeta = stepDef as StepAgentMetadata;
+  const candidates = [stepMeta.agentName, stepMeta.agent, stepMeta.assigneeAgentName];
+
+  for (const candidate of candidates) {
+    if (typeof candidate === "string" && candidate.trim()) {
+      return candidate.trim();
+    }
+  }
+
+  return null;
+}
+
+export function getStepAgentName(stepRun: WorkflowStepRunRecord, stepDef: WorkflowStep): string | null {
+  if (typeof stepRun.data.agentName === "string" && stepRun.data.agentName.trim()) {
+    return stepRun.data.agentName.trim();
+  }
+
+  return getStepAgentNameHint(stepDef);
+}

exec
/bin/zsh -lc "rg -n \"WorkflowStepRecord|toStepRunRecord|ctx\\.data\\.register|start-workflow|parentId filler|IssueUpdatePatch|registerDataHandler|WorkflowDefinitionRecord|WorkflowRunRecord|WorkflowStepRunRecord\" plugins/workflow-engine/src/worker.ts" in /Users/kwak/Projects/paperclip/paperclip-addon succeeded in 0ms:
36:  toWorkflowDefinitionRecord,
37:  toWorkflowRunRecord,
38:  toWorkflowStepRunRecord,
39:  type WorkflowDefinitionRecord,
40:  type WorkflowRunRecord,
41:  type WorkflowStepRunRecord,
50:type IssueUpdatePatch = Parameters<PluginContext["issues"]["update"]>[1];
121:  stepRunRecord: WorkflowStepRunRecord,
125:): Promise<WorkflowStepRunRecord> {
165:      stepRunRecord = toWorkflowStepRunRecord(await updateStepRun(ctx, stepRunRecord.id, {
206:  stepRunRecord: WorkflowStepRunRecord,
211:): Promise<WorkflowStepRunRecord> {
236:    await ctx.issues.update(issue.id, { status: "todo" } as IssueUpdatePatch, companyId);
241:  let updatedStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, stepRunRecord.id, {
278:  const typedWorkflowDefinition = toWorkflowDefinitionRecord(workflowDefinition);
295:  const workflowRun = toWorkflowRunRecord(await createWorkflowRun(ctx, {
310:  const pendingRootSteps: Array<{ stepDef: WorkflowStep; stepRun: WorkflowStepRunRecord }> = [];
322:    const stepRun = toWorkflowStepRunRecord(await createStepRun(ctx, companyId, {
370:  const completedStepRun = toWorkflowStepRunRecord(stepRunRecord);
382:  const typedWorkflowRun = toWorkflowRunRecord(workflowRun);
file update:
diff --git a/paperclip-addon/plugins/workflow-engine/src/dag-engine.ts b/paperclip-addon/plugins/workflow-engine/src/dag-engine.ts
index b59510f0473171038be77277286d3b5c2063e6c5..e7f21ff998cab78b9325a8aeacf4fda9c5e06553
--- a/paperclip-addon/plugins/workflow-engine/src/dag-engine.ts
+++ b/paperclip-addon/plugins/workflow-engine/src/dag-engine.ts
@@ -2,6 +2,7 @@
   id: string;
   title: string;
   dependsOn: string[];
+  sessionMode?: "fresh" | "reuse";
   onFailure?: "retry" | "skip" | "abort_workflow" | "escalate";
   escalateTo?: string;
   maxRetries?: number;
diff --git a/paperclip-addon/plugins/workflow-engine/src/reconciler.ts b/paperclip-addon/plugins/workflow-engine/src/reconciler.ts
index 14ab4e766a835d145a6c2e7b1b564270285cbdc3..d2ab54817bd8185944379d2188bfbdef1722020a
--- a/paperclip-addon/plugins/workflow-engine/src/reconciler.ts
+++ b/paperclip-addon/plugins/workflow-engine/src/reconciler.ts
@@ -8,65 +8,22 @@
   listStepRuns,
   updateStepRun,
   updateWorkflowRun,
-  type WorkflowDefinition,
-  type WorkflowRun,
-  type WorkflowStepRun,
 } from "./workflow-store.js";
 import type { WorkflowStep } from "./dag-engine.js";
-
-type WorkflowRunRecord = PluginEntityRecord & { data: WorkflowRun };
-type WorkflowStepRunRecord = PluginEntityRecord & { data: WorkflowStepRun };
-type WorkflowDefinitionRecord = PluginEntityRecord & { data: WorkflowDefinition };
-type StepAgentMetadata = WorkflowStep & {
-  agent?: string;
-  agentName?: string;
-  assigneeAgentName?: string;
-};
+import {
+  TERMINAL_STEP_STATUSES,
+  findStepDefinition,
+  getStepAgentName,
+  toWorkflowDefinitionRecord,
+  toWorkflowRunRecord,
+  toWorkflowStepRunRecord,
+} from "./workflow-utils.js";
 
 const STALE_THRESHOLD_MS = 300_000;
-const TERMINAL_STEP_STATUSES = new Set<string>([
-  STEP_STATUSES.done,
-  STEP_STATUSES.failed,
-  STEP_STATUSES.skipped,
-  STEP_STATUSES.escalated,
-]);
 
 function isStale(record: PluginEntityRecord, thresholdMs: number): boolean {
   const updatedAt = new Date(record.updatedAt).getTime();
   return Date.now() - updatedAt > thresholdMs;
-}
-
-function toWorkflowRunRecord(record: PluginEntityRecord): WorkflowRunRecord {
-  return record as WorkflowRunRecord;
-}
-
-function toWorkflowStepRunRecord(record: PluginEntityRecord): WorkflowStepRunRecord {
-  return record as WorkflowStepRunRecord;
-}
-
-function toWorkflowDefinitionRecord(record: PluginEntityRecord): WorkflowDefinitionRecord {
-  return record as WorkflowDefinitionRecord;
-}
-
-function findStepDefinition(definition: WorkflowDefinitionRecord, stepId: string): WorkflowStep | null {
-  return definition.data.steps.find((step: WorkflowStep) => step.id === stepId) ?? null;
-}
-
-function getStepAgentName(stepRun: WorkflowStepRunRecord, stepDef: WorkflowStep): string | null {
-  if (typeof stepRun.data.agentName === "string" && stepRun.data.agentName.trim()) {
-    return stepRun.data.agentName.trim();
-  }
-
-  const stepMeta = stepDef as StepAgentMetadata;
-  const candidates = [stepMeta.agentName, stepMeta.agent, stepMeta.assigneeAgentName];
-
-  for (const candidate of candidates) {
-    if (typeof candidate === "string" && candidate.trim()) {
-      return candidate.trim();
-    }
-  }
-
-  return null;
 }
 
 async function invokeAgentByName(
diff --git a/paperclip-addon/plugins/workflow-engine/src/worker.ts b/paperclip-addon/plugins/workflow-engine/src/worker.ts
index c5ac3fbb4ca7f767eb29331bd3630b635449b335..303e35958890fa1f66db3f2b0ff1fa1f134102a8
--- a/paperclip-addon/plugins/workflow-engine/src/worker.ts
+++ b/paperclip-addon/plugins/workflow-engine/src/worker.ts
@@ -15,6 +15,7 @@
 } from "./dag-engine.js";
 import {
   checkIdempotency,
+  createWorkflowRun,
   createStepRun,
   findStepRunByIssueId,
   getWorkflowDefinition,
@@ -25,51 +26,38 @@
   markIdempotency,
   updateStepRun,
   updateWorkflowRun,
-  type WorkflowDefinition,
-  type WorkflowRun,
   type WorkflowStepRun,
 } from "./workflow-store.js";
+import {
+  TERMINAL_STEP_STATUSES,
+  findStepDefinition,
+  getStepAgentName,
+  getStepAgentNameHint,
+  toWorkflowDefinitionRecord,
+  toWorkflowRunRecord,
+  toWorkflowStepRunRecord,
+  type WorkflowDefinitionRecord,
+  type WorkflowRunRecord,
+  type WorkflowStepRunRecord,
+} from "./workflow-utils.js";
 
-type WorkflowStepRecord = PluginEntityRecord & { data: WorkflowStepRun };
-type WorkflowRunRecord = PluginEntityRecord & { data: WorkflowRun };
-type WorkflowDefinitionRecord = PluginEntityRecord & { data: WorkflowDefinition };
 type WorkflowStepMetadata = WorkflowStep & {
   description?: string;
-  agent?: string;
-  agentName?: string;
-  assigneeAgentName?: string;
   assigneeAgentId?: string;
+  sessionMode?: "fresh" | "reuse";
 };
 type AgentRecord = Awaited<ReturnType<PluginContext["agents"]["list"]>>[number];
+type IssueUpdatePatch = Parameters<PluginContext["issues"]["update"]>[1];
 type ReconcilerModule = {
   reconcileStuckSteps?: (ctx: PluginContext) => Promise<void>;
 };
 
-const TERMINAL_STEP_STATUSES = new Set<string>([
-  STEP_STATUSES.done,
-  STEP_STATUSES.failed,
-  STEP_STATUSES.skipped,
-  STEP_STATUSES.escalated,
-]);
-
 function summarizeError(error: unknown): string {
   if (error instanceof Error) {
     return error.message;
   }
 
   return String(error);
-}
-
-function toStepRunRecord(record: PluginEntityRecord): WorkflowStepRecord {
-  return record as WorkflowStepRecord;
-}
-
-function toWorkflowRunRecord(record: PluginEntityRecord): WorkflowRunRecord {
-  return record as WorkflowRunRecord;
-}
-
-function toWorkflowDefinitionRecord(record: PluginEntityRecord): WorkflowDefinitionRecord {
-  return record as WorkflowDefinitionRecord;
 }
 
 function buildIdempotencyKey(event: PluginEvent): string {
@@ -78,10 +66,6 @@
 
 function getStepMetadata(stepDef: WorkflowStep): WorkflowStepMetadata {
   return stepDef as WorkflowStepMetadata;
-}
-
-function findStepDefinition(definition: WorkflowDefinitionRecord, stepId: string): WorkflowStep | null {
-  return definition.data.steps.find((step: WorkflowStep) => step.id === stepId) ?? null;
 }
 
 function getStepDescription(stepDef: WorkflowStep): string | undefined {
@@ -89,19 +73,6 @@
   return typeof description === "string" && description.trim() ? description.trim() : undefined;
 }
 
-function getStepAgentNameHint(stepDef: WorkflowStep): string | null {
-  const meta = getStepMetadata(stepDef);
-  const candidates = [meta.agentName, meta.agent, meta.assigneeAgentName];
-
-  for (const candidate of candidates) {
-    if (typeof candidate === "string" && candidate.trim()) {
-      return candidate.trim();
-    }
-  }
-
-  return null;
-}
-
 function getStepAgentIdHint(stepDef: WorkflowStep): string | null {
   const assigneeAgentId = getStepMetadata(stepDef).assigneeAgentId;
   if (typeof assigneeAgentId !== "string" || !assigneeAgentId.trim()) {
@@ -147,68 +118,256 @@
 
 async function invokeAgentForStep(
   ctx: PluginContext,
+  stepRunRecord: WorkflowStepRunRecord,
   stepDef: WorkflowStep,
-  agentName: string,
   workflowName: string,
   companyId: string,
-): Promise<void> {
+): Promise<WorkflowStepRunRecord> {
+  const agentName = getStepAgentName(stepRunRecord, stepDef);
+  if (!agentName) {
+    ctx.logger.warn("Agent name is missing for workflow step", {
+      companyId,
+      runId: stepRunRecord.data.runId,
+      stepId: stepDef.id,
+      workflowName,
+    });
+    return stepRunRecord;
+  }
+
   const agents = await ctx.agents.list({ companyId });
-  const agent = agents.find((candidate: AgentRecord) => candidate.name === agentName);
+  const agent = agents.find((candidate: AgentRecord) => candidate.name === agentName) ?? null;
 
   if (!agent) {
     ctx.logger.warn("Agent not found for step", {
       agentName,
       companyId,
+      runId: stepRunRecord.data.runId,
+      stepId: stepDef.id,
+      workflowName,
+    });
+    return stepRunRecord;
+  }
+
+  const prompt = `workflow:${workflowName}/step:${stepDef.id} — "${stepDef.title}" is ready. Please proceed with the assigned task.`;
+  const reason = `workflow:${workflowName}/step:${stepDef.id}`;
+  const sessionMode = getStepMetadata(stepDef).sessionMode === "reuse" ? "reuse" : "fresh";
+
+  if (sessionMode === "reuse") {
+    let sessionId = typeof stepRunRecord.data.sessionId === "string" && stepRunRecord.data.sessionId.trim()
+      ? stepRunRecord.data.sessionId.trim()
+      : "";
+
+    if (!sessionId) {
+      const session = await ctx.agents.sessions.create(agent.id, companyId, {
+        reason,
+      });
+      sessionId = session.sessionId;
+      stepRunRecord = toWorkflowStepRunRecord(await updateStepRun(ctx, stepRunRecord.id, {
+        sessionId,
+      }));
+    }
+
+    await ctx.agents.sessions.sendMessage(sessionId, companyId, {
+      prompt,
+      reason,
+    });
+
+    ctx.logger.info("Sent workflow step prompt via agent session", {
+      agentId: agent.id,
+      agentName,
+      companyId,
+      runId: stepRunRecord.data.runId,
+      sessionId,
       stepId: stepDef.id,
       workflowName,
     });
-    return;
+    return stepRunRecord;
   }
 
   await ctx.agents.invoke(agent.id, companyId, {
-    prompt: `workflow:${workflowName}/step:${stepDef.id} — "${stepDef.title}" is ready. Please proceed with the assigned task.`,
-    reason: `workflow:${workflowName}/step:${stepDef.id}`,
+    prompt,
+    reason,
   });
 
   ctx.logger.info("Invoked agent for workflow step", {
     agentId: agent.id,
     agentName,
     companyId,
+    runId: stepRunRecord.data.runId,
     stepId: stepDef.id,
     workflowName,
   });
+
+  return stepRunRecord;
 }
 
 async function activateBacklogStep(
   ctx: PluginContext,
-  stepRunRecord: WorkflowStepRecord,
+  stepRunRecord: WorkflowStepRunRecord,
   stepDef: WorkflowStep,
   workflowName: string,
   companyId: string,
-): Promise<void> {
+  options?: { parentIssueId?: string },
+): Promise<WorkflowStepRunRecord> {
   if (stepRunRecord.data.status !== STEP_STATUSES.backlog) {
-    return;
+    return stepRunRecord;
   }
 
-  const updated = toStepRunRecord(await updateStepRun(ctx, stepRunRecord.id, {
+  const resolvedAgent = await resolveStepAgent(
+    ctx,
+    companyId,
+    stepDef,
+    stepRunRecord.data.agentName,
+  );
+
+  let issueId = typeof stepRunRecord.data.issueId === "string" && stepRunRecord.data.issueId.trim()
+    ? stepRunRecord.data.issueId.trim()
+    : "";
+  const stepDescription = getStepDescription(stepDef) ?? `Workflow step: ${stepDef.id}`;
+
+  if (!issueId) {
+    const issue = await ctx.issues.create({
+      assigneeAgentId: resolvedAgent.agentId ?? undefined,
+      companyId,
+      description: stepDescription,
+      parentId: options?.parentIssueId,
+      title: `[${workflowName}] ${stepDef.title}`,
+    });
+    await ctx.issues.update(issue.id, { status: "todo" } as IssueUpdatePatch, companyId);
+    issueId = issue.id;
+  }
+
+  const nextStartedAt = stepRunRecord.data.startedAt ?? new Date().toISOString();
+  let updatedStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, stepRunRecord.id, {
+    agentName: resolvedAgent.agentName ?? stepRunRecord.data.agentName,
+    issueId,
+    startedAt: nextStartedAt,
     status: STEP_STATUSES.todo,
   }));
 
-  await invokeAgentForStep(
+  updatedStepRun = await invokeAgentForStep(
     ctx,
+    updatedStepRun,
     stepDef,
-    updated.data.agentName,
     workflowName,
     companyId,
   );
+
+  return updatedStepRun;
 }
 
+async function startWorkflow(
+  ctx: PluginContext,
+  workflowId: string,
+  companyId: string,
+  options?: {
+    createParentIssue?: boolean;
+    parentIssueId?: string;
+  },
+): Promise<{
+  activatedStepIds: string[];
+  parentIssueId: string | null;
+  runId: string;
+  workflowId: string;
+}> {
+  const workflowDefinition = await getWorkflowDefinition(ctx, workflowId);
+  if (!workflowDefinition) {
+    throw new Error(`Workflow definition not found: ${workflowId}`);
+  }
+
+  const typedWorkflowDefinition = toWorkflowDefinitionRecord(workflowDefinition);
+  if (typedWorkflowDefinition.data.companyId !== companyId) {
+    throw new Error(`Workflow does not belong to company: ${workflowId}`);
+  }
+
+  let parentIssueId = typeof options?.parentIssueId === "string" && options.parentIssueId.trim()
+    ? options.parentIssueId.trim()
+    : "";
+  if (!parentIssueId && options?.createParentIssue) {
+    const parentIssue = await ctx.issues.create({
+      companyId,
+      description: typedWorkflowDefinition.data.description || `Workflow run: ${typedWorkflowDefinition.data.name}`,
+      title: `[${typedWorkflowDefinition.data.name}] Workflow run`,
+    });
+    parentIssueId = parentIssue.id;
+  }
+
+  const workflowRun = toWorkflowRunRecord(await createWorkflowRun(ctx, {
+    companyId,
+    parentIssueId: parentIssueId || undefined,
+    startedAt: new Date().toISOString(),
+    status: RUN_STATUSES.running,
+    workflowId: typedWorkflowDefinition.id,
+    workflowName: typedWorkflowDefinition.data.name,
+  }));
+
+  const agents = await ctx.agents.list({ companyId });
+  const agentsByName = new Map<string, AgentRecord>();
+  for (const agent of agents) {
+    agentsByName.set(agent.name, agent);
+  }
+
+  const pendingRootSteps: Array<{ stepDef: WorkflowStep; stepRun: WorkflowStepRunRecord }> = [];
+  for (const stepDef of typedWorkflowDefinition.data.steps) {
+    const agentNameHint = getStepAgentNameHint(stepDef);
+    const matchedAgent = agentNameHint ? agentsByName.get(agentNameHint) ?? null : null;
+    const resolvedAgent = matchedAgent
+      ? { agentId: matchedAgent.id, agentName: matchedAgent.name }
+      : await resolveStepAgent(ctx, companyId, stepDef, agentNameHint ?? undefined);
+
+    if (!resolvedAgent.agentName) {
+      throw new Error(`Unable to resolve step assignee for "${stepDef.id}"`);
+    }
+
+    const stepRun = toWorkflowStepRunRecord(await createStepRun(ctx, companyId, {
+      agentName: resolvedAgent.agentName,
+      retryCount: 0,
+      runId: workflowRun.id,
+      status: STEP_STATUSES.backlog,
+      stepId: stepDef.id,
+    }));
+
+    if (stepDef.dependsOn.length === 0 && stepDef.triggerOn !== "escalation") {
+      pendingRootSteps.push({ stepDef, stepRun });
+    }
+  }
+
+  const activatedStepIds: string[] = [];
+  for (const pending of pendingRootSteps) {
+    await activateBacklogStep(
+      ctx,
+      pending.stepRun,
+      pending.stepDef,
+      typedWorkflowDefinition.data.name,
+      companyId,
+      { parentIssueId: parentIssueId || undefined },
+    );
+    activatedStepIds.push(pending.stepDef.id);
+  }
+
+  ctx.logger.info("Started workflow run", {
+    activatedStepIds,
+    companyId,
+    parentIssueId: parentIssueId || null,
+    runId: workflowRun.id,
+    workflowId: typedWorkflowDefinition.id,
+    workflowName: typedWorkflowDefinition.data.name,
+  });
+
+  return {
+    activatedStepIds,
+    parentIssueId: parentIssueId || null,
+    runId: workflowRun.id,
+    workflowId: typedWorkflowDefinition.id,
+  };
+}
+
 async function advanceWorkflow(
   ctx: PluginContext,
   stepRunRecord: PluginEntityRecord,
   companyId: string,
 ): Promise<void> {
-  const completedStepRun = toStepRunRecord(stepRunRecord);
+  const completedStepRun = toWorkflowStepRunRecord(stepRunRecord);
   const workflowRun = await getWorkflowRun(ctx, completedStepRun.data.runId);
 
   if (!workflowRun) {
@@ -237,7 +396,7 @@
   }
 
   const typedWorkflowDefinition = toWorkflowDefinitionRecord(workflowDefinition);
-  const stepRuns = (await listStepRuns(ctx, typedWorkflowRun.id, companyId)).map(toStepRunRecord);
+  const stepRuns = (await listStepRuns(ctx, typedWorkflowRun.id, companyId)).map(toWorkflowStepRunRecord);
   const completed = new Set<string>();
   const failed = new Set<string>();
   const skipped = new Set<string>();
@@ -289,7 +448,7 @@
         continue;
       }
 
-      stepRun = toStepRunRecord(await createStepRun(ctx, companyId, {
+      stepRun = toWorkflowStepRunRecord(await createStepRun(ctx, companyId, {
         agentName: resolvedAgent.agentName,
         retryCount: 0,
         runId: typedWorkflowRun.id,
@@ -305,6 +464,7 @@
       stepDef,
       typedWorkflowRun.data.workflowName,
       companyId,
+      { parentIssueId: typedWorkflowRun.data.parentIssueId },
     );
   }
 
@@ -329,7 +489,7 @@
   ctx: PluginContext,
   workflowRun: WorkflowRunRecord,
   workflowDefinition: WorkflowDefinitionRecord,
-  sourceStepRun: WorkflowStepRecord,
+  sourceStepRun: WorkflowStepRunRecord,
   escalationTargetId: string,
   companyId: string,
 ): Promise<void> {
@@ -338,7 +498,7 @@
     throw new Error(`Escalation target step not found: ${escalationTargetId}`);
   }
 
-  const stepRuns = (await listStepRuns(ctx, workflowRun.id, companyId)).map(toStepRunRecord);
+  const stepRuns = (await listStepRuns(ctx, workflowRun.id, companyId)).map(toWorkflowStepRunRecord);
   let escalationStepRun = stepRuns.find((candidate) => candidate.data.stepId === escalationTargetId) ?? null;
   const resolvedAgent = await resolveStepAgent(
     ctx,
@@ -352,7 +512,7 @@
   }
 
   if (!escalationStepRun) {
-    escalationStepRun = toStepRunRecord(await createStepRun(ctx, companyId, {
+    escalationStepRun = toWorkflowStepRunRecord(await createStepRun(ctx, companyId, {
       agentName: resolvedAgent.agentName,
       retryCount: 0,
       runId: workflowRun.id,
@@ -374,14 +534,14 @@
       title: `${workflowRun.data.workflowName}: ${escalationStep.title}`,
     });
 
-    escalationStepRun = toStepRunRecord(await updateStepRun(ctx, escalationStepRun.id, {
+    escalationStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, escalationStepRun.id, {
       issueId: issue.id,
     }));
   }
 
   const shouldActivate = escalationStepRun.data.status === STEP_STATUSES.backlog;
   if (shouldActivate) {
-    escalationStepRun = toStepRunRecord(await updateStepRun(ctx, escalationStepRun.id, {
+    escalationStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, escalationStepRun.id, {
       status: STEP_STATUSES.todo,
     }));
   }
@@ -389,8 +549,8 @@
   if (shouldActivate) {
     await invokeAgentForStep(
       ctx,
+      escalationStepRun,
       escalationStep,
-      escalationStepRun.data.agentName,
       workflowRun.data.workflowName,
       companyId,
     );
@@ -421,7 +581,7 @@
     return;
   }
 
-  const typedStepRun = toStepRunRecord(stepRunRecord);
+  const typedStepRun = toWorkflowStepRunRecord(stepRunRecord);
   if (TERMINAL_STEP_STATUSES.has(typedStepRun.data.status)) {
     return;
   }
@@ -469,7 +629,7 @@
   if (options.allowRetry) {
     const retryInfo = getRetryInfo(typedWorkflowDefinition.data.steps, typedStepRun.data.stepId);
     if (retryInfo.shouldRetry && typedStepRun.data.retryCount < retryInfo.maxRetries) {
-      const retriedStepRun = toStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
+      const retriedStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
         completedAt: undefined,
         retryCount: typedStepRun.data.retryCount + 1,
         status: STEP_STATUSES.todo,
@@ -477,8 +637,8 @@
 
       await invokeAgentForStep(
         ctx,
+        retriedStepRun,
         stepDef,
-        retriedStepRun.data.agentName,
         typedWorkflowRun.data.workflowName,
         event.companyId,
       );
@@ -496,7 +656,7 @@
   }
 
   if (stepDef.onFailure === "skip") {
-    const skippedStepRun = toStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
+    const skippedStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
       completedAt: new Date().toISOString(),
       status: STEP_STATUSES.skipped,
     }));
@@ -556,7 +716,7 @@
       return;
     }
 
-    const escalatedStepRun = toStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
+    const escalatedStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
       completedAt: new Date().toISOString(),
       status: STEP_STATUSES.escalated,
     }));
@@ -617,6 +777,29 @@
   }
 }
 
+function registerDataHandler(
+  ctx: PluginContext,
+  key: string,
+  handler: (params: Record<string, unknown>) => Promise<unknown>,
+): void {
+  const dataClient = ctx.data as {
+    handle?: (handlerKey: string, handlerFn: (params: Record<string, unknown>) => Promise<unknown>) => void;
+    register?: (handlerKey: string, handlerFn: (params: Record<string, unknown>) => Promise<unknown>) => void;
+  };
+
+  if (typeof dataClient.handle === "function") {
+    dataClient.handle(key, handler);
+    return;
+  }
+
+  if (typeof dataClient.register === "function") {
+    dataClient.register(key, handler);
+    return;
+  }
+
+  throw new Error("Plugin data client does not support handler registration");
+}
+
 const plugin = definePlugin({
   async setup(ctx: PluginContext) {
     ctx.events.on("issue.updated", async (event: PluginEvent) => {
@@ -638,7 +821,7 @@
           return;
         }
 
-        const stepRun = toStepRunRecord(stepRunRecord);
+        const stepRun = toWorkflowStepRunRecord(stepRunRecord);
         const payload = event.payload as { status?: string };
         const issueStatus = typeof payload.status === "string" ? payload.status : undefined;
 
@@ -657,7 +840,7 @@
           return;
         }
 
-        const updatedStepRun = toStepRunRecord(await updateStepRun(ctx, stepRun.id, {
+        const updatedStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, stepRun.id, {
           completedAt: nextStepStatus === STEP_STATUSES.done ? new Date().toISOString() : undefined,
           startedAt: nextStepStatus === STEP_STATUSES.inProgress
             ? stepRun.data.startedAt ?? new Date().toISOString()
@@ -687,11 +870,21 @@
 
     ctx.events.on("issue.created", async (event: PluginEvent) => {
       try {
+        const idempotencyKey = buildIdempotencyKey(event);
+        if (await checkIdempotency(ctx, idempotencyKey, event.companyId)) {
+          return;
+        }
+
         const payload = event.payload as {
           id?: string;
           parentId?: string;
           assigneeAgentId?: string;
         };
+        const issueId = typeof event.entityId === "string" && event.entityId.trim()
+          ? event.entityId.trim()
+          : typeof payload.id === "string" && payload.id.trim()
+            ? payload.id.trim()
+            : "";
 
         const parentId = typeof payload.parentId === "string" && payload.parentId.trim()
           ? payload.parentId.trim()
@@ -700,12 +893,34 @@
           ? payload.assigneeAgentId.trim()
           : "";
 
-        if (parentId || !assigneeAgentId) {
+        if (!issueId || parentId || !assigneeAgentId) {
+          await markIdempotency(ctx, idempotencyKey, event.companyId);
+          return;
+        }
+
+        const agent = await ctx.agents.get(assigneeAgentId, event.companyId);
+        const metadata = agent?.metadata as Record<string, unknown> | undefined;
+        const defaultParentIssueId = typeof metadata?.defaultParentIssueId === "string" && metadata.defaultParentIssueId.trim()
+          ? metadata.defaultParentIssueId.trim()
+          : "";
+
+        if (!defaultParentIssueId) {
+          await markIdempotency(ctx, idempotencyKey, event.companyId);
           return;
         }
 
-        ctx.logger.info("parentId filler: issue created without parent", {
-          issueId: event.entityId ?? payload.id ?? null,
+        await ctx.issues.update(
+          issueId,
+          { parentId: defaultParentIssueId } as IssueUpdatePatch,
+          event.companyId,
+        );
+        await markIdempotency(ctx, idempotencyKey, event.companyId);
+
+        ctx.logger.info("parentId filler: populated issue parentId from agent metadata", {
+          assigneeAgentId,
+          companyId: event.companyId,
+          issueId,
+          parentId: defaultParentIssueId,
         });
       } catch (error) {
         ctx.logger.warn("Failed to handle issue.created event", {
@@ -744,7 +959,23 @@
       await runReconciler(ctx);
     });
 
-    ctx.data.register("workflow-overview", async (params: Record<string, unknown>) => {
+    registerDataHandler(ctx, "start-workflow", async (params: Record<string, unknown>) => {
+      const workflowId = typeof params.workflowId === "string" ? params.workflowId.trim() : "";
+      const companyId = typeof params.companyId === "string" ? params.companyId.trim() : "";
+      const parentIssueId = typeof params.parentIssueId === "string" ? params.parentIssueId.trim() : "";
+      const createParentIssue = params.createParentIssue === true;
+
+      if (!workflowId || !companyId) {
+        throw new Error("start-workflow requires workflowId and companyId");
+      }
+
+      return await startWorkflow(ctx, workflowId, companyId, {
+        createParentIssue,
+        parentIssueId: parentIssueId || undefined,
+      });
+    });
+
+    registerDataHandler(ctx, "workflow-overview", async (params: Record<string, unknown>) => {
       try {
         const companyId = typeof params.companyId === "string" ? params.companyId.trim() : "";
         if (!companyId) {
@@ -782,7 +1013,7 @@
       }
     });
 
-    ctx.data.register("workflow-run-detail", async (params: Record<string, unknown>) => {
+    registerDataHandler(ctx, "workflow-run-detail", async (params: Record<string, unknown>) => {
       try {
         const runId = typeof params.runId === "string" ? params.runId.trim() : "";
         if (!runId) {
@@ -807,7 +1038,7 @@
             ...typedWorkflowRun.data,
           },
           stepRuns: stepRuns.map((record: PluginEntityRecord) => {
-            const stepRun = toStepRunRecord(record);
+            const stepRun = toWorkflowStepRunRecord(record);
             return {
               id: stepRun.id,
               status: stepRun.status,
diff --git a/paperclip-addon/plugins/workflow-engine/src/workflow-store.ts b/paperclip-addon/plugins/workflow-engine/src/workflow-store.ts
index 5871a2082258cebcfe8b69ebafbb0814cf303d50..3d7fb2b3bc75911cfd9093eb3a4054cc73196bb9
--- a/paperclip-addon/plugins/workflow-engine/src/workflow-store.ts
+++ b/paperclip-addon/plugins/workflow-engine/src/workflow-store.ts
@@ -44,7 +44,14 @@
 
 type TypedEntityRecord<T> = Omit<PluginEntityRecord, "data"> & { data: T };
 
-function toTypedRecord<T>(record: PluginEntityRecord): TypedEntityRecord<T> {
+function toTypedRecord<T>(
+  record: PluginEntityRecord,
+  entityType: string,
+): TypedEntityRecord<T> {
+  if (record.entityType !== entityType) {
+    throw new Error(`Expected entity type "${entityType}", got "${record.entityType}"`);
+  }
+
   return record as TypedEntityRecord<T>;
 }
 
@@ -67,13 +74,26 @@
   id: string,
   entityType: string,
 ): Promise<TypedEntityRecord<T> | null> {
+  const listQuery: Parameters<PluginContext["entities"]["list"]>[0] & { id?: string } = {
+    entityType,
+    id,
+    limit: 1,
+  };
+
+  const listed = await ctx.entities.list(listQuery);
+  const listedRecord = listed.find((record) => record.id === id) ?? null;
+  if (listedRecord) {
+    return toTypedRecord<T>(listedRecord, entityType);
+  }
+
+  // Fallback: some host SDK variants ignore `id` on entities.list().
+  // Keep defensive type verification on direct get() until id-filter support is universal.
   const record = await ctx.entities.get(id);
-
-  if (!record || record.entityType !== entityType) {
+  if (!record) {
     return null;
   }
 
-  return toTypedRecord<T>(record);
+  return toTypedRecord<T>(record, entityType);
 }
 
 async function requireEntityByType<T>(
diff --git a/paperclip-addon/plugins/workflow-engine/src/workflow-utils.ts b/paperclip-addon/plugins/workflow-engine/src/workflow-utils.ts
new file mode 100644
index 0000000000000000000000000000000000000000..ec1cf6f6b9aa722b126b58c0a728b4e538ad1494
--- /dev/null
+++ b/paperclip-addon/plugins/workflow-engine/src/workflow-utils.ts
@@ -0,0 +1,62 @@
+import type { PluginEntityRecord } from "@paperclipai/plugin-sdk";
+
+import { STEP_STATUSES } from "./constants.js";
+import type { WorkflowStep } from "./dag-engine.js";
+import type {
+  WorkflowDefinition,
+  WorkflowRun,
+  WorkflowStepRun,
+} from "./workflow-store.js";
+
+export type WorkflowRunRecord = PluginEntityRecord & { data: WorkflowRun };
+export type WorkflowStepRunRecord = PluginEntityRecord & { data: WorkflowStepRun };
+export type WorkflowDefinitionRecord = PluginEntityRecord & { data: WorkflowDefinition };
+type StepAgentMetadata = WorkflowStep & {
+  agent?: string;
+  agentName?: string;
+  assigneeAgentName?: string;
+};
+
+export const TERMINAL_STEP_STATUSES = new Set<string>([
+  STEP_STATUSES.done,
+  STEP_STATUSES.failed,
+  STEP_STATUSES.skipped,
+  STEP_STATUSES.escalated,
+]);
+
+export function toWorkflowRunRecord(record: PluginEntityRecord): WorkflowRunRecord {
+  return record as WorkflowRunRecord;
+}
+
+export function toWorkflowStepRunRecord(record: PluginEntityRecord): WorkflowStepRunRecord {
+  return record as WorkflowStepRunRecord;
+}
+
+export function toWorkflowDefinitionRecord(record: PluginEntityRecord): WorkflowDefinitionRecord {
+  return record as WorkflowDefinitionRecord;
+}
+
+export function findStepDefinition(definition: WorkflowDefinitionRecord, stepId: string): WorkflowStep | null {
+  return definition.data.steps.find((step: WorkflowStep) => step.id === stepId) ?? null;
+}
+
+export function getStepAgentNameHint(stepDef: WorkflowStep): string | null {
+  const stepMeta = stepDef as StepAgentMetadata;
+  const candidates = [stepMeta.agentName, stepMeta.agent, stepMeta.assigneeAgentName];
+
+  for (const candidate of candidates) {
+    if (typeof candidate === "string" && candidate.trim()) {
+      return candidate.trim();
+    }
+  }
+
+  return null;
+}
+
+export function getStepAgentName(stepRun: WorkflowStepRunRecord, stepDef: WorkflowStep): string | null {
+  if (typeof stepRun.data.agentName === "string" && stepRun.data.agentName.trim()) {
+    return stepRun.data.agentName.trim();
+  }
+
+  return getStepAgentNameHint(stepDef);
+}

codex
핵심 코드 연결은 끝났고, 지금은 타입/사용되지 않는 심볼 정리와 `typecheck` 실행으로 회귀 여부를 확인하는 단계입니다. 실패하더라도 원인을 정확히 남기겠습니다.
exec
/bin/zsh -lc 'rg -n "''^import .*WorkflowStep.*$|''^import .*PluginEntityRecord.*$" plugins/workflow-engine/src/reconciler.ts plugins/workflow-engine/src/worker.ts' in /Users/kwak/Projects/paperclip/paperclip-addon succeeded in 0ms:
plugins/workflow-engine/src/reconciler.ts:1:import type { PluginContext, PluginEntityRecord } from "@paperclipai/plugin-sdk";
plugins/workflow-engine/src/reconciler.ts:12:import type { WorkflowStep } from "./dag-engine.js";
exec
/bin/zsh -lc 'cd plugins/workflow-engine && npm run build' in /Users/kwak/Projects/paperclip/paperclip-addonexec
/bin/zsh -lc 'cd plugins/workflow-engine && npm run typecheck' in /Users/kwak/Projects/paperclip/paperclip-addon exited 2 in 400ms:

> @insightflo/paperclip-workflow-engine@0.1.0 typecheck
> tsc --noEmit

src/manifest.ts(1,48): error TS2307: Cannot find module '@paperclipai/plugin-sdk' or its corresponding type declarations.
src/reconciler.ts(1,56): error TS2307: Cannot find module '@paperclipai/plugin-sdk' or its corresponding type declarations.
src/ui/index.tsx(6,8): error TS2307: Cannot find module '@paperclipai/plugin-sdk/ui' or its corresponding type declarations.
src/ui/index.tsx(7,46): error TS2307: Cannot find module 'react' or its corresponding type declarations.
src/ui/index.tsx(242,5): error TS7026: JSX element implicitly has type 'any' because no interface 'JSX.IntrinsicElements' exists.
src/ui/index.tsx(242,5): error TS2875: This JSX tag requires the module path 'react/jsx-runtime' to exist, but none could be found. Make sure you have types for the appropriate package installed.
src/ui/index.tsx(243,7): error TS7026: JSX element implicitly has type 'any' because no interface 'JSX.IntrinsicElements' exists.
src/ui/index.tsx(243,42): error TS7026: JSX element implicitly has type 'any' because no interface 'JSX.IntrinsicElements' exists.
src/ui/index.tsx(244,7): error TS7026: JSX element implicitly has type 'any' because no interface 'JSX.IntrinsicElements' exists.
src/ui/index.tsx(245,9): error TS7026: JSX element implicitly has type 'any' because no interface 'JSX.IntrinsicElements' exists.
src/ui/index.tsx(247,9): error TS7026: JSX element implicitly has type 'any' because no interface 'JSX.IntrinsicElements' exists.
src/ui/index.tsx(248,7): error TS7026: JSX element implicitly has type 'any' because no interface 'JSX.IntrinsicElements' exists.
src/ui/index.tsx(249,5): error TS7026: JSX element implicitly has type 'any' because no interface 'JSX.IntrinsicElements' exists.
src/ui/index.tsx(255,12): error TS7026: JSX element implicitly has type 'any' because no interface 'JSX.IntrinsicElements' exists.
src/ui/index.tsx(255,63): error TS7026: JSX element implicitly has type 'any' because no interface 'JSX.IntrinsicElements' exists.
src/ui/index.tsx(259,5): error TS7026: JSX element implicitly has type 'any' because no interface 'JSX.IntrinsicElements' exists.
 exited 2 in 449ms:

> @insightflo/paperclip-workflow-engine@0.1.0 build
> tsc && node ./scripts/build-ui.mjs

src/manifest.ts(1,48): error TS2307: Cannot find module '@paperclipai/plugin-sdk' or its corresponding type declarations.
src/reconciler.ts(1,56): error TS2307: Cannot find module '@paperclipai/plugin-sdk' or its corresponding type declarations.
src/ui/index.tsx(6,8): error TS2307: Cannot find module '@paperclipai/plugin-sdk/ui' or its corresponding type declarations.
src/ui/index.tsx(7,46): error TS2307: Cannot find module 'react' or its corresponding type declarations.
src/ui/index.tsx(242,5): error TS7026: JSX element implicitly has type 'any' because no interface 'JSX.IntrinsicElements' exists.
src/ui/index.tsx(242,5): error TS2875: This JSX tag requires the module path 'react/jsx-runtime' to exist, but none could be found. Make sure you have types for the appropriate package installed.
src/ui/index.tsx(243,7): error TS7026: JSX element implicitly has type 'any' because no interface 'JSX.IntrinsicElements' exists.
src/ui/index.tsx(243,42): error TS7026: JSX element implicitly has type 'any' because no interface 'JSX.IntrinsicElements' exists.
src/ui/index.tsx(244,7): error TS7026: JSX element implicitly has type 'any' because no interface 'JSX.IntrinsicElements' exists.
src/ui/index.tsx(245,9): error TS7026: JSX element implicitly has type 'any' because no interface 'JSX.IntrinsicElements' exists.
src/ui/index.tsx(247,9): error TS7026: JSX element implicitly has type 'any' because no interface 'JSX.IntrinsicElements' exists.
src/ui/index.tsx(248,7): error TS7026: JSX element implicitly has type 'any' because no interface 'JSX.IntrinsicElements' exists.
src/ui/index.tsx(249,5): error TS7026: JSX element implicitly has type 'any' because no interface 'JSX.IntrinsicElements' exists.
src/ui/index.tsx(255,12): error TS7026: JSX element implicitly has type 'any' because no interface 'JSX.IntrinsicElements' exists.
src/ui/index.tsx(255,63): error TS7026: JSX element implicitly has type 'any' because no interface 'JSX.IntrinsicElements' exists.
src/ui/index.tsx(259,5): error TS7026: JSX element implicitly has type 'any' because no interface 'JSX.IntrinsicElements' exists.
file update:
diff --git a/paperclip-addon/plugins/workflow-engine/src/dag-engine.ts b/paperclip-addon/plugins/workflow-engine/src/dag-engine.ts
index b59510f0473171038be77277286d3b5c2063e6c5..e7f21ff998cab78b9325a8aeacf4fda9c5e06553
--- a/paperclip-addon/plugins/workflow-engine/src/dag-engine.ts
+++ b/paperclip-addon/plugins/workflow-engine/src/dag-engine.ts
@@ -2,6 +2,7 @@
   id: string;
   title: string;
   dependsOn: string[];
+  sessionMode?: "fresh" | "reuse";
   onFailure?: "retry" | "skip" | "abort_workflow" | "escalate";
   escalateTo?: string;
   maxRetries?: number;
diff --git a/paperclip-addon/plugins/workflow-engine/src/reconciler.ts b/paperclip-addon/plugins/workflow-engine/src/reconciler.ts
index 14ab4e766a835d145a6c2e7b1b564270285cbdc3..d2ab54817bd8185944379d2188bfbdef1722020a
--- a/paperclip-addon/plugins/workflow-engine/src/reconciler.ts
+++ b/paperclip-addon/plugins/workflow-engine/src/reconciler.ts
@@ -8,65 +8,22 @@
   listStepRuns,
   updateStepRun,
   updateWorkflowRun,
-  type WorkflowDefinition,
-  type WorkflowRun,
-  type WorkflowStepRun,
 } from "./workflow-store.js";
 import type { WorkflowStep } from "./dag-engine.js";
-
-type WorkflowRunRecord = PluginEntityRecord & { data: WorkflowRun };
-type WorkflowStepRunRecord = PluginEntityRecord & { data: WorkflowStepRun };
-type WorkflowDefinitionRecord = PluginEntityRecord & { data: WorkflowDefinition };
-type StepAgentMetadata = WorkflowStep & {
-  agent?: string;
-  agentName?: string;
-  assigneeAgentName?: string;
-};
+import {
+  TERMINAL_STEP_STATUSES,
+  findStepDefinition,
+  getStepAgentName,
+  toWorkflowDefinitionRecord,
+  toWorkflowRunRecord,
+  toWorkflowStepRunRecord,
+} from "./workflow-utils.js";
 
 const STALE_THRESHOLD_MS = 300_000;
-const TERMINAL_STEP_STATUSES = new Set<string>([
-  STEP_STATUSES.done,
-  STEP_STATUSES.failed,
-  STEP_STATUSES.skipped,
-  STEP_STATUSES.escalated,
-]);
 
 function isStale(record: PluginEntityRecord, thresholdMs: number): boolean {
   const updatedAt = new Date(record.updatedAt).getTime();
   return Date.now() - updatedAt > thresholdMs;
-}
-
-function toWorkflowRunRecord(record: PluginEntityRecord): WorkflowRunRecord {
-  return record as WorkflowRunRecord;
-}
-
-function toWorkflowStepRunRecord(record: PluginEntityRecord): WorkflowStepRunRecord {
-  return record as WorkflowStepRunRecord;
-}
-
-function toWorkflowDefinitionRecord(record: PluginEntityRecord): WorkflowDefinitionRecord {
-  return record as WorkflowDefinitionRecord;
-}
-
-function findStepDefinition(definition: WorkflowDefinitionRecord, stepId: string): WorkflowStep | null {
-  return definition.data.steps.find((step: WorkflowStep) => step.id === stepId) ?? null;
-}
-
-function getStepAgentName(stepRun: WorkflowStepRunRecord, stepDef: WorkflowStep): string | null {
-  if (typeof stepRun.data.agentName === "string" && stepRun.data.agentName.trim()) {
-    return stepRun.data.agentName.trim();
-  }
-
-  const stepMeta = stepDef as StepAgentMetadata;
-  const candidates = [stepMeta.agentName, stepMeta.agent, stepMeta.assigneeAgentName];
-
-  for (const candidate of candidates) {
-    if (typeof candidate === "string" && candidate.trim()) {
-      return candidate.trim();
-    }
-  }
-
-  return null;
 }
 
 async function invokeAgentByName(
diff --git a/paperclip-addon/plugins/workflow-engine/src/worker.ts b/paperclip-addon/plugins/workflow-engine/src/worker.ts
index c5ac3fbb4ca7f767eb29331bd3630b635449b335..303e35958890fa1f66db3f2b0ff1fa1f134102a8
--- a/paperclip-addon/plugins/workflow-engine/src/worker.ts
+++ b/paperclip-addon/plugins/workflow-engine/src/worker.ts
@@ -15,6 +15,7 @@
 } from "./dag-engine.js";
 import {
   checkIdempotency,
+  createWorkflowRun,
   createStepRun,
   findStepRunByIssueId,
   getWorkflowDefinition,
@@ -25,51 +26,38 @@
   markIdempotency,
   updateStepRun,
   updateWorkflowRun,
-  type WorkflowDefinition,
-  type WorkflowRun,
   type WorkflowStepRun,
 } from "./workflow-store.js";
+import {
+  TERMINAL_STEP_STATUSES,
+  findStepDefinition,
+  getStepAgentName,
+  getStepAgentNameHint,
+  toWorkflowDefinitionRecord,
+  toWorkflowRunRecord,
+  toWorkflowStepRunRecord,
+  type WorkflowDefinitionRecord,
+  type WorkflowRunRecord,
+  type WorkflowStepRunRecord,
+} from "./workflow-utils.js";
 
-type WorkflowStepRecord = PluginEntityRecord & { data: WorkflowStepRun };
-type WorkflowRunRecord = PluginEntityRecord & { data: WorkflowRun };
-type WorkflowDefinitionRecord = PluginEntityRecord & { data: WorkflowDefinition };
 type WorkflowStepMetadata = WorkflowStep & {
   description?: string;
-  agent?: string;
-  agentName?: string;
-  assigneeAgentName?: string;
   assigneeAgentId?: string;
+  sessionMode?: "fresh" | "reuse";
 };
 type AgentRecord = Awaited<ReturnType<PluginContext["agents"]["list"]>>[number];
+type IssueUpdatePatch = Parameters<PluginContext["issues"]["update"]>[1];
 type ReconcilerModule = {
   reconcileStuckSteps?: (ctx: PluginContext) => Promise<void>;
 };
 
-const TERMINAL_STEP_STATUSES = new Set<string>([
-  STEP_STATUSES.done,
-  STEP_STATUSES.failed,
-  STEP_STATUSES.skipped,
-  STEP_STATUSES.escalated,
-]);
-
 function summarizeError(error: unknown): string {
   if (error instanceof Error) {
     return error.message;
   }
 
   return String(error);
-}
-
-function toStepRunRecord(record: PluginEntityRecord): WorkflowStepRecord {
-  return record as WorkflowStepRecord;
-}
-
-function toWorkflowRunRecord(record: PluginEntityRecord): WorkflowRunRecord {
-  return record as WorkflowRunRecord;
-}
-
-function toWorkflowDefinitionRecord(record: PluginEntityRecord): WorkflowDefinitionRecord {
-  return record as WorkflowDefinitionRecord;
 }
 
 function buildIdempotencyKey(event: PluginEvent): string {
@@ -78,10 +66,6 @@
 
 function getStepMetadata(stepDef: WorkflowStep): WorkflowStepMetadata {
   return stepDef as WorkflowStepMetadata;
-}
-
-function findStepDefinition(definition: WorkflowDefinitionRecord, stepId: string): WorkflowStep | null {
-  return definition.data.steps.find((step: WorkflowStep) => step.id === stepId) ?? null;
 }
 
 function getStepDescription(stepDef: WorkflowStep): string | undefined {
@@ -89,19 +73,6 @@
   return typeof description === "string" && description.trim() ? description.trim() : undefined;
 }
 
-function getStepAgentNameHint(stepDef: WorkflowStep): string | null {
-  const meta = getStepMetadata(stepDef);
-  const candidates = [meta.agentName, meta.agent, meta.assigneeAgentName];
-
-  for (const candidate of candidates) {
-    if (typeof candidate === "string" && candidate.trim()) {
-      return candidate.trim();
-    }
-  }
-
-  return null;
-}
-
 function getStepAgentIdHint(stepDef: WorkflowStep): string | null {
   const assigneeAgentId = getStepMetadata(stepDef).assigneeAgentId;
   if (typeof assigneeAgentId !== "string" || !assigneeAgentId.trim()) {
@@ -147,68 +118,256 @@
 
 async function invokeAgentForStep(
   ctx: PluginContext,
+  stepRunRecord: WorkflowStepRunRecord,
   stepDef: WorkflowStep,
-  agentName: string,
   workflowName: string,
   companyId: string,
-): Promise<void> {
+): Promise<WorkflowStepRunRecord> {
+  const agentName = getStepAgentName(stepRunRecord, stepDef);
+  if (!agentName) {
+    ctx.logger.warn("Agent name is missing for workflow step", {
+      companyId,
+      runId: stepRunRecord.data.runId,
+      stepId: stepDef.id,
+      workflowName,
+    });
+    return stepRunRecord;
+  }
+
   const agents = await ctx.agents.list({ companyId });
-  const agent = agents.find((candidate: AgentRecord) => candidate.name === agentName);
+  const agent = agents.find((candidate: AgentRecord) => candidate.name === agentName) ?? null;
 
   if (!agent) {
     ctx.logger.warn("Agent not found for step", {
       agentName,
       companyId,
+      runId: stepRunRecord.data.runId,
+      stepId: stepDef.id,
+      workflowName,
+    });
+    return stepRunRecord;
+  }
+
+  const prompt = `workflow:${workflowName}/step:${stepDef.id} — "${stepDef.title}" is ready. Please proceed with the assigned task.`;
+  const reason = `workflow:${workflowName}/step:${stepDef.id}`;
+  const sessionMode = getStepMetadata(stepDef).sessionMode === "reuse" ? "reuse" : "fresh";
+
+  if (sessionMode === "reuse") {
+    let sessionId = typeof stepRunRecord.data.sessionId === "string" && stepRunRecord.data.sessionId.trim()
+      ? stepRunRecord.data.sessionId.trim()
+      : "";
+
+    if (!sessionId) {
+      const session = await ctx.agents.sessions.create(agent.id, companyId, {
+        reason,
+      });
+      sessionId = session.sessionId;
+      stepRunRecord = toWorkflowStepRunRecord(await updateStepRun(ctx, stepRunRecord.id, {
+        sessionId,
+      }));
+    }
+
+    await ctx.agents.sessions.sendMessage(sessionId, companyId, {
+      prompt,
+      reason,
+    });
+
+    ctx.logger.info("Sent workflow step prompt via agent session", {
+      agentId: agent.id,
+      agentName,
+      companyId,
+      runId: stepRunRecord.data.runId,
+      sessionId,
       stepId: stepDef.id,
       workflowName,
     });
-    return;
+    return stepRunRecord;
   }
 
   await ctx.agents.invoke(agent.id, companyId, {
-    prompt: `workflow:${workflowName}/step:${stepDef.id} — "${stepDef.title}" is ready. Please proceed with the assigned task.`,
-    reason: `workflow:${workflowName}/step:${stepDef.id}`,
+    prompt,
+    reason,
   });
 
   ctx.logger.info("Invoked agent for workflow step", {
     agentId: agent.id,
     agentName,
     companyId,
+    runId: stepRunRecord.data.runId,
     stepId: stepDef.id,
     workflowName,
   });
+
+  return stepRunRecord;
 }
 
 async function activateBacklogStep(
   ctx: PluginContext,
-  stepRunRecord: WorkflowStepRecord,
+  stepRunRecord: WorkflowStepRunRecord,
   stepDef: WorkflowStep,
   workflowName: string,
   companyId: string,
-): Promise<void> {
+  options?: { parentIssueId?: string },
+): Promise<WorkflowStepRunRecord> {
   if (stepRunRecord.data.status !== STEP_STATUSES.backlog) {
-    return;
+    return stepRunRecord;
   }
 
-  const updated = toStepRunRecord(await updateStepRun(ctx, stepRunRecord.id, {
+  const resolvedAgent = await resolveStepAgent(
+    ctx,
+    companyId,
+    stepDef,
+    stepRunRecord.data.agentName,
+  );
+
+  let issueId = typeof stepRunRecord.data.issueId === "string" && stepRunRecord.data.issueId.trim()
+    ? stepRunRecord.data.issueId.trim()
+    : "";
+  const stepDescription = getStepDescription(stepDef) ?? `Workflow step: ${stepDef.id}`;
+
+  if (!issueId) {
+    const issue = await ctx.issues.create({
+      assigneeAgentId: resolvedAgent.agentId ?? undefined,
+      companyId,
+      description: stepDescription,
+      parentId: options?.parentIssueId,
+      title: `[${workflowName}] ${stepDef.title}`,
+    });
+    await ctx.issues.update(issue.id, { status: "todo" } as IssueUpdatePatch, companyId);
+    issueId = issue.id;
+  }
+
+  const nextStartedAt = stepRunRecord.data.startedAt ?? new Date().toISOString();
+  let updatedStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, stepRunRecord.id, {
+    agentName: resolvedAgent.agentName ?? stepRunRecord.data.agentName,
+    issueId,
+    startedAt: nextStartedAt,
     status: STEP_STATUSES.todo,
   }));
 
-  await invokeAgentForStep(
+  updatedStepRun = await invokeAgentForStep(
     ctx,
+    updatedStepRun,
     stepDef,
-    updated.data.agentName,
     workflowName,
     companyId,
   );
+
+  return updatedStepRun;
 }
 
+async function startWorkflow(
+  ctx: PluginContext,
+  workflowId: string,
+  companyId: string,
+  options?: {
+    createParentIssue?: boolean;
+    parentIssueId?: string;
+  },
+): Promise<{
+  activatedStepIds: string[];
+  parentIssueId: string | null;
+  runId: string;
+  workflowId: string;
+}> {
+  const workflowDefinition = await getWorkflowDefinition(ctx, workflowId);
+  if (!workflowDefinition) {
+    throw new Error(`Workflow definition not found: ${workflowId}`);
+  }
+
+  const typedWorkflowDefinition = toWorkflowDefinitionRecord(workflowDefinition);
+  if (typedWorkflowDefinition.data.companyId !== companyId) {
+    throw new Error(`Workflow does not belong to company: ${workflowId}`);
+  }
+
+  let parentIssueId = typeof options?.parentIssueId === "string" && options.parentIssueId.trim()
+    ? options.parentIssueId.trim()
+    : "";
+  if (!parentIssueId && options?.createParentIssue) {
+    const parentIssue = await ctx.issues.create({
+      companyId,
+      description: typedWorkflowDefinition.data.description || `Workflow run: ${typedWorkflowDefinition.data.name}`,
+      title: `[${typedWorkflowDefinition.data.name}] Workflow run`,
+    });
+    parentIssueId = parentIssue.id;
+  }
+
+  const workflowRun = toWorkflowRunRecord(await createWorkflowRun(ctx, {
+    companyId,
+    parentIssueId: parentIssueId || undefined,
+    startedAt: new Date().toISOString(),
+    status: RUN_STATUSES.running,
+    workflowId: typedWorkflowDefinition.id,
+    workflowName: typedWorkflowDefinition.data.name,
+  }));
+
+  const agents = await ctx.agents.list({ companyId });
+  const agentsByName = new Map<string, AgentRecord>();
+  for (const agent of agents) {
+    agentsByName.set(agent.name, agent);
+  }
+
+  const pendingRootSteps: Array<{ stepDef: WorkflowStep; stepRun: WorkflowStepRunRecord }> = [];
+  for (const stepDef of typedWorkflowDefinition.data.steps) {
+    const agentNameHint = getStepAgentNameHint(stepDef);
+    const matchedAgent = agentNameHint ? agentsByName.get(agentNameHint) ?? null : null;
+    const resolvedAgent = matchedAgent
+      ? { agentId: matchedAgent.id, agentName: matchedAgent.name }
+      : await resolveStepAgent(ctx, companyId, stepDef, agentNameHint ?? undefined);
+
+    if (!resolvedAgent.agentName) {
+      throw new Error(`Unable to resolve step assignee for "${stepDef.id}"`);
+    }
+
+    const stepRun = toWorkflowStepRunRecord(await createStepRun(ctx, companyId, {
+      agentName: resolvedAgent.agentName,
+      retryCount: 0,
+      runId: workflowRun.id,
+      status: STEP_STATUSES.backlog,
+      stepId: stepDef.id,
+    }));
+
+    if (stepDef.dependsOn.length === 0 && stepDef.triggerOn !== "escalation") {
+      pendingRootSteps.push({ stepDef, stepRun });
+    }
+  }
+
+  const activatedStepIds: string[] = [];
+  for (const pending of pendingRootSteps) {
+    await activateBacklogStep(
+      ctx,
+      pending.stepRun,
+      pending.stepDef,
+      typedWorkflowDefinition.data.name,
+      companyId,
+      { parentIssueId: parentIssueId || undefined },
+    );
+    activatedStepIds.push(pending.stepDef.id);
+  }
+
+  ctx.logger.info("Started workflow run", {
+    activatedStepIds,
+    companyId,
+    parentIssueId: parentIssueId || null,
+    runId: workflowRun.id,
+    workflowId: typedWorkflowDefinition.id,
+    workflowName: typedWorkflowDefinition.data.name,
+  });
+
+  return {
+    activatedStepIds,
+    parentIssueId: parentIssueId || null,
+    runId: workflowRun.id,
+    workflowId: typedWorkflowDefinition.id,
+  };
+}
+
 async function advanceWorkflow(
   ctx: PluginContext,
   stepRunRecord: PluginEntityRecord,
   companyId: string,
 ): Promise<void> {
-  const completedStepRun = toStepRunRecord(stepRunRecord);
+  const completedStepRun = toWorkflowStepRunRecord(stepRunRecord);
   const workflowRun = await getWorkflowRun(ctx, completedStepRun.data.runId);
 
   if (!workflowRun) {
@@ -237,7 +396,7 @@
   }
 
   const typedWorkflowDefinition = toWorkflowDefinitionRecord(workflowDefinition);
-  const stepRuns = (await listStepRuns(ctx, typedWorkflowRun.id, companyId)).map(toStepRunRecord);
+  const stepRuns = (await listStepRuns(ctx, typedWorkflowRun.id, companyId)).map(toWorkflowStepRunRecord);
   const completed = new Set<string>();
   const failed = new Set<string>();
   const skipped = new Set<string>();
@@ -289,7 +448,7 @@
         continue;
       }
 
-      stepRun = toStepRunRecord(await createStepRun(ctx, companyId, {
+      stepRun = toWorkflowStepRunRecord(await createStepRun(ctx, companyId, {
         agentName: resolvedAgent.agentName,
         retryCount: 0,
         runId: typedWorkflowRun.id,
@@ -305,6 +464,7 @@
       stepDef,
       typedWorkflowRun.data.workflowName,
       companyId,
+      { parentIssueId: typedWorkflowRun.data.parentIssueId },
     );
   }
 
@@ -329,7 +489,7 @@
   ctx: PluginContext,
   workflowRun: WorkflowRunRecord,
   workflowDefinition: WorkflowDefinitionRecord,
-  sourceStepRun: WorkflowStepRecord,
+  sourceStepRun: WorkflowStepRunRecord,
   escalationTargetId: string,
   companyId: string,
 ): Promise<void> {
@@ -338,7 +498,7 @@
     throw new Error(`Escalation target step not found: ${escalationTargetId}`);
   }
 
-  const stepRuns = (await listStepRuns(ctx, workflowRun.id, companyId)).map(toStepRunRecord);
+  const stepRuns = (await listStepRuns(ctx, workflowRun.id, companyId)).map(toWorkflowStepRunRecord);
   let escalationStepRun = stepRuns.find((candidate) => candidate.data.stepId === escalationTargetId) ?? null;
   const resolvedAgent = await resolveStepAgent(
     ctx,
@@ -352,7 +512,7 @@
   }
 
   if (!escalationStepRun) {
-    escalationStepRun = toStepRunRecord(await createStepRun(ctx, companyId, {
+    escalationStepRun = toWorkflowStepRunRecord(await createStepRun(ctx, companyId, {
       agentName: resolvedAgent.agentName,
       retryCount: 0,
       runId: workflowRun.id,
@@ -374,14 +534,14 @@
       title: `${workflowRun.data.workflowName}: ${escalationStep.title}`,
     });
 
-    escalationStepRun = toStepRunRecord(await updateStepRun(ctx, escalationStepRun.id, {
+    escalationStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, escalationStepRun.id, {
       issueId: issue.id,
     }));
   }
 
   const shouldActivate = escalationStepRun.data.status === STEP_STATUSES.backlog;
   if (shouldActivate) {
-    escalationStepRun = toStepRunRecord(await updateStepRun(ctx, escalationStepRun.id, {
+    escalationStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, escalationStepRun.id, {
       status: STEP_STATUSES.todo,
     }));
   }
@@ -389,8 +549,8 @@
   if (shouldActivate) {
     await invokeAgentForStep(
       ctx,
+      escalationStepRun,
       escalationStep,
-      escalationStepRun.data.agentName,
       workflowRun.data.workflowName,
       companyId,
     );
@@ -421,7 +581,7 @@
     return;
   }
 
-  const typedStepRun = toStepRunRecord(stepRunRecord);
+  const typedStepRun = toWorkflowStepRunRecord(stepRunRecord);
   if (TERMINAL_STEP_STATUSES.has(typedStepRun.data.status)) {
     return;
   }
@@ -469,7 +629,7 @@
   if (options.allowRetry) {
     const retryInfo = getRetryInfo(typedWorkflowDefinition.data.steps, typedStepRun.data.stepId);
     if (retryInfo.shouldRetry && typedStepRun.data.retryCount < retryInfo.maxRetries) {
-      const retriedStepRun = toStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
+      const retriedStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
         completedAt: undefined,
         retryCount: typedStepRun.data.retryCount + 1,
         status: STEP_STATUSES.todo,
@@ -477,8 +637,8 @@
 
       await invokeAgentForStep(
         ctx,
+        retriedStepRun,
         stepDef,
-        retriedStepRun.data.agentName,
         typedWorkflowRun.data.workflowName,
         event.companyId,
       );
@@ -496,7 +656,7 @@
   }
 
   if (stepDef.onFailure === "skip") {
-    const skippedStepRun = toStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
+    const skippedStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
       completedAt: new Date().toISOString(),
       status: STEP_STATUSES.skipped,
     }));
@@ -556,7 +716,7 @@
       return;
     }
 
-    const escalatedStepRun = toStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
+    const escalatedStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
       completedAt: new Date().toISOString(),
       status: STEP_STATUSES.escalated,
     }));
@@ -617,6 +777,29 @@
   }
 }
 
+function registerDataHandler(
+  ctx: PluginContext,
+  key: string,
+  handler: (params: Record<string, unknown>) => Promise<unknown>,
+): void {
+  const dataClient = ctx.data as {
+    handle?: (handlerKey: string, handlerFn: (params: Record<string, unknown>) => Promise<unknown>) => void;
+    register?: (handlerKey: string, handlerFn: (params: Record<string, unknown>) => Promise<unknown>) => void;
+  };
+
+  if (typeof dataClient.handle === "function") {
+    dataClient.handle(key, handler);
+    return;
+  }
+
+  if (typeof dataClient.register === "function") {
+    dataClient.register(key, handler);
+    return;
+  }
+
+  throw new Error("Plugin data client does not support handler registration");
+}
+
 const plugin = definePlugin({
   async setup(ctx: PluginContext) {
     ctx.events.on("issue.updated", async (event: PluginEvent) => {
@@ -638,7 +821,7 @@
           return;
         }
 
-        const stepRun = toStepRunRecord(stepRunRecord);
+        const stepRun = toWorkflowStepRunRecord(stepRunRecord);
         const payload = event.payload as { status?: string };
         const issueStatus = typeof payload.status === "string" ? payload.status : undefined;
 
@@ -657,7 +840,7 @@
           return;
         }
 
-        const updatedStepRun = toStepRunRecord(await updateStepRun(ctx, stepRun.id, {
+        const updatedStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, stepRun.id, {
           completedAt: nextStepStatus === STEP_STATUSES.done ? new Date().toISOString() : undefined,
           startedAt: nextStepStatus === STEP_STATUSES.inProgress
             ? stepRun.data.startedAt ?? new Date().toISOString()
@@ -687,11 +870,21 @@
 
     ctx.events.on("issue.created", async (event: PluginEvent) => {
       try {
+        const idempotencyKey = buildIdempotencyKey(event);
+        if (await checkIdempotency(ctx, idempotencyKey, event.companyId)) {
+          return;
+        }
+
         const payload = event.payload as {
           id?: string;
           parentId?: string;
           assigneeAgentId?: string;
         };
+        const issueId = typeof event.entityId === "string" && event.entityId.trim()
+          ? event.entityId.trim()
+          : typeof payload.id === "string" && payload.id.trim()
+            ? payload.id.trim()
+            : "";
 
         const parentId = typeof payload.parentId === "string" && payload.parentId.trim()
           ? payload.parentId.trim()
@@ -700,12 +893,34 @@
           ? payload.assigneeAgentId.trim()
           : "";
 
-        if (parentId || !assigneeAgentId) {
+        if (!issueId || parentId || !assigneeAgentId) {
+          await markIdempotency(ctx, idempotencyKey, event.companyId);
+          return;
+        }
+
+        const agent = await ctx.agents.get(assigneeAgentId, event.companyId);
+        const metadata = agent?.metadata as Record<string, unknown> | undefined;
+        const defaultParentIssueId = typeof metadata?.defaultParentIssueId === "string" && metadata.defaultParentIssueId.trim()
+          ? metadata.defaultParentIssueId.trim()
+          : "";
+
+        if (!defaultParentIssueId) {
+          await markIdempotency(ctx, idempotencyKey, event.companyId);
           return;
         }
 
-        ctx.logger.info("parentId filler: issue created without parent", {
-          issueId: event.entityId ?? payload.id ?? null,
+        await ctx.issues.update(
+          issueId,
+          { parentId: defaultParentIssueId } as IssueUpdatePatch,
+          event.companyId,
+        );
+        await markIdempotency(ctx, idempotencyKey, event.companyId);
+
+        ctx.logger.info("parentId filler: populated issue parentId from agent metadata", {
+          assigneeAgentId,
+          companyId: event.companyId,
+          issueId,
+          parentId: defaultParentIssueId,
         });
       } catch (error) {
         ctx.logger.warn("Failed to handle issue.created event", {
@@ -744,7 +959,23 @@
       await runReconciler(ctx);
     });
 
-    ctx.data.register("workflow-overview", async (params: Record<string, unknown>) => {
+    registerDataHandler(ctx, "start-workflow", async (params: Record<string, unknown>) => {
+      const workflowId = typeof params.workflowId === "string" ? params.workflowId.trim() : "";
+      const companyId = typeof params.companyId === "string" ? params.companyId.trim() : "";
+      const parentIssueId = typeof params.parentIssueId === "string" ? params.parentIssueId.trim() : "";
+      const createParentIssue = params.createParentIssue === true;
+
+      if (!workflowId || !companyId) {
+        throw new Error("start-workflow requires workflowId and companyId");
+      }
+
+      return await startWorkflow(ctx, workflowId, companyId, {
+        createParentIssue,
+        parentIssueId: parentIssueId || undefined,
+      });
+    });
+
+    registerDataHandler(ctx, "workflow-overview", async (params: Record<string, unknown>) => {
       try {
         const companyId = typeof params.companyId === "string" ? params.companyId.trim() : "";
         if (!companyId) {
@@ -782,7 +1013,7 @@
       }
     });
 
-    ctx.data.register("workflow-run-detail", async (params: Record<string, unknown>) => {
+    registerDataHandler(ctx, "workflow-run-detail", async (params: Record<string, unknown>) => {
       try {
         const runId = typeof params.runId === "string" ? params.runId.trim() : "";
         if (!runId) {
@@ -807,7 +1038,7 @@
             ...typedWorkflowRun.data,
           },
           stepRuns: stepRuns.map((record: PluginEntityRecord) => {
-            const stepRun = toStepRunRecord(record);
+            const stepRun = toWorkflowStepRunRecord(record);
             return {
               id: stepRun.id,
               status: stepRun.status,
diff --git a/paperclip-addon/plugins/workflow-engine/src/workflow-store.ts b/paperclip-addon/plugins/workflow-engine/src/workflow-store.ts
index 5871a2082258cebcfe8b69ebafbb0814cf303d50..3d7fb2b3bc75911cfd9093eb3a4054cc73196bb9
--- a/paperclip-addon/plugins/workflow-engine/src/workflow-store.ts
+++ b/paperclip-addon/plugins/workflow-engine/src/workflow-store.ts
@@ -44,7 +44,14 @@
 
 type TypedEntityRecord<T> = Omit<PluginEntityRecord, "data"> & { data: T };
 
-function toTypedRecord<T>(record: PluginEntityRecord): TypedEntityRecord<T> {
+function toTypedRecord<T>(
+  record: PluginEntityRecord,
+  entityType: string,
+): TypedEntityRecord<T> {
+  if (record.entityType !== entityType) {
+    throw new Error(`Expected entity type "${entityType}", got "${record.entityType}"`);
+  }
+
   return record as TypedEntityRecord<T>;
 }
 
@@ -67,13 +74,26 @@
   id: string,
   entityType: string,
 ): Promise<TypedEntityRecord<T> | null> {
+  const listQuery: Parameters<PluginContext["entities"]["list"]>[0] & { id?: string } = {
+    entityType,
+    id,
+    limit: 1,
+  };
+
+  const listed = await ctx.entities.list(listQuery);
+  const listedRecord = listed.find((record) => record.id === id) ?? null;
+  if (listedRecord) {
+    return toTypedRecord<T>(listedRecord, entityType);
+  }
+
+  // Fallback: some host SDK variants ignore `id` on entities.list().
+  // Keep defensive type verification on direct get() until id-filter support is universal.
   const record = await ctx.entities.get(id);
-
-  if (!record || record.entityType !== entityType) {
+  if (!record) {
     return null;
   }
 
-  return toTypedRecord<T>(record);
+  return toTypedRecord<T>(record, entityType);
 }
 
 async function requireEntityByType<T>(
diff --git a/paperclip-addon/plugins/workflow-engine/src/workflow-utils.ts b/paperclip-addon/plugins/workflow-engine/src/workflow-utils.ts
new file mode 100644
index 0000000000000000000000000000000000000000..ec1cf6f6b9aa722b126b58c0a728b4e538ad1494
--- /dev/null
+++ b/paperclip-addon/plugins/workflow-engine/src/workflow-utils.ts
@@ -0,0 +1,62 @@
+import type { PluginEntityRecord } from "@paperclipai/plugin-sdk";
+
+import { STEP_STATUSES } from "./constants.js";
+import type { WorkflowStep } from "./dag-engine.js";
+import type {
+  WorkflowDefinition,
+  WorkflowRun,
+  WorkflowStepRun,
+} from "./workflow-store.js";
+
+export type WorkflowRunRecord = PluginEntityRecord & { data: WorkflowRun };
+export type WorkflowStepRunRecord = PluginEntityRecord & { data: WorkflowStepRun };
+export type WorkflowDefinitionRecord = PluginEntityRecord & { data: WorkflowDefinition };
+type StepAgentMetadata = WorkflowStep & {
+  agent?: string;
+  agentName?: string;
+  assigneeAgentName?: string;
+};
+
+export const TERMINAL_STEP_STATUSES = new Set<string>([
+  STEP_STATUSES.done,
+  STEP_STATUSES.failed,
+  STEP_STATUSES.skipped,
+  STEP_STATUSES.escalated,
+]);
+
+export function toWorkflowRunRecord(record: PluginEntityRecord): WorkflowRunRecord {
+  return record as WorkflowRunRecord;
+}
+
+export function toWorkflowStepRunRecord(record: PluginEntityRecord): WorkflowStepRunRecord {
+  return record as WorkflowStepRunRecord;
+}
+
+export function toWorkflowDefinitionRecord(record: PluginEntityRecord): WorkflowDefinitionRecord {
+  return record as WorkflowDefinitionRecord;
+}
+
+export function findStepDefinition(definition: WorkflowDefinitionRecord, stepId: string): WorkflowStep | null {
+  return definition.data.steps.find((step: WorkflowStep) => step.id === stepId) ?? null;
+}
+
+export function getStepAgentNameHint(stepDef: WorkflowStep): string | null {
+  const stepMeta = stepDef as StepAgentMetadata;
+  const candidates = [stepMeta.agentName, stepMeta.agent, stepMeta.assigneeAgentName];
+
+  for (const candidate of candidates) {
+    if (typeof candidate === "string" && candidate.trim()) {
+      return candidate.trim();
+    }
+  }
+
+  return null;
+}
+
+export function getStepAgentName(stepRun: WorkflowStepRunRecord, stepDef: WorkflowStep): string | null {
+  if (typeof stepRun.data.agentName === "string" && stepRun.data.agentName.trim()) {
+    return stepRun.data.agentName.trim();
+  }
+
+  return getStepAgentNameHint(stepDef);
+}

file update
M /Users/kwak/Projects/paperclip/paperclip-addon/plugins/workflow-engine/src/workflow-store.ts
@@ -76,3 +76,3 @@
 ): Promise<TypedEntityRecord<T> | null> {
-  const listQuery: Parameters<PluginContext["entities"]["list"]>[0] & { id?: string } = {
+  const listQuery = {
     entityType,
@@ -80,9 +80,14 @@
     limit: 1,
-  };
+  } as Record<string, unknown>;
 
-  const listed = await ctx.entities.list(listQuery);
-  const listedRecord = listed.find((record) => record.id === id) ?? null;
+  const listed = await ctx.entities.list(
+    listQuery as Parameters<PluginContext["entities"]["list"]>[0],
+  );
+  const listedRecord = listed.find(
+    (record: PluginEntityRecord) => record.id === id,
+  ) ?? null;
+
   if (listedRecord) {
     return toTypedRecord<T>(listedRecord, entityType);
-  }
+  };
 
apply_patch(auto_approved=true) exited 0 in 108ms:
Success. Updated the following files:
M /Users/kwak/Projects/paperclip/paperclip-addon/plugins/workflow-engine/src/workflow-store.ts
file update:
diff --git a/paperclip-addon/plugins/workflow-engine/src/dag-engine.ts b/paperclip-addon/plugins/workflow-engine/src/dag-engine.ts
index b59510f0473171038be77277286d3b5c2063e6c5..e7f21ff998cab78b9325a8aeacf4fda9c5e06553
--- a/paperclip-addon/plugins/workflow-engine/src/dag-engine.ts
+++ b/paperclip-addon/plugins/workflow-engine/src/dag-engine.ts
@@ -2,6 +2,7 @@
   id: string;
   title: string;
   dependsOn: string[];
+  sessionMode?: "fresh" | "reuse";
   onFailure?: "retry" | "skip" | "abort_workflow" | "escalate";
   escalateTo?: string;
   maxRetries?: number;
diff --git a/paperclip-addon/plugins/workflow-engine/src/reconciler.ts b/paperclip-addon/plugins/workflow-engine/src/reconciler.ts
index 14ab4e766a835d145a6c2e7b1b564270285cbdc3..d2ab54817bd8185944379d2188bfbdef1722020a
--- a/paperclip-addon/plugins/workflow-engine/src/reconciler.ts
+++ b/paperclip-addon/plugins/workflow-engine/src/reconciler.ts
@@ -8,65 +8,22 @@
   listStepRuns,
   updateStepRun,
   updateWorkflowRun,
-  type WorkflowDefinition,
-  type WorkflowRun,
-  type WorkflowStepRun,
 } from "./workflow-store.js";
 import type { WorkflowStep } from "./dag-engine.js";
-
-type WorkflowRunRecord = PluginEntityRecord & { data: WorkflowRun };
-type WorkflowStepRunRecord = PluginEntityRecord & { data: WorkflowStepRun };
-type WorkflowDefinitionRecord = PluginEntityRecord & { data: WorkflowDefinition };
-type StepAgentMetadata = WorkflowStep & {
-  agent?: string;
-  agentName?: string;
-  assigneeAgentName?: string;
-};
+import {
+  TERMINAL_STEP_STATUSES,
+  findStepDefinition,
+  getStepAgentName,
+  toWorkflowDefinitionRecord,
+  toWorkflowRunRecord,
+  toWorkflowStepRunRecord,
+} from "./workflow-utils.js";
 
 const STALE_THRESHOLD_MS = 300_000;
-const TERMINAL_STEP_STATUSES = new Set<string>([
-  STEP_STATUSES.done,
-  STEP_STATUSES.failed,
-  STEP_STATUSES.skipped,
-  STEP_STATUSES.escalated,
-]);
 
 function isStale(record: PluginEntityRecord, thresholdMs: number): boolean {
   const updatedAt = new Date(record.updatedAt).getTime();
   return Date.now() - updatedAt > thresholdMs;
-}
-
-function toWorkflowRunRecord(record: PluginEntityRecord): WorkflowRunRecord {
-  return record as WorkflowRunRecord;
-}
-
-function toWorkflowStepRunRecord(record: PluginEntityRecord): WorkflowStepRunRecord {
-  return record as WorkflowStepRunRecord;
-}
-
-function toWorkflowDefinitionRecord(record: PluginEntityRecord): WorkflowDefinitionRecord {
-  return record as WorkflowDefinitionRecord;
-}
-
-function findStepDefinition(definition: WorkflowDefinitionRecord, stepId: string): WorkflowStep | null {
-  return definition.data.steps.find((step: WorkflowStep) => step.id === stepId) ?? null;
-}
-
-function getStepAgentName(stepRun: WorkflowStepRunRecord, stepDef: WorkflowStep): string | null {
-  if (typeof stepRun.data.agentName === "string" && stepRun.data.agentName.trim()) {
-    return stepRun.data.agentName.trim();
-  }
-
-  const stepMeta = stepDef as StepAgentMetadata;
-  const candidates = [stepMeta.agentName, stepMeta.agent, stepMeta.assigneeAgentName];
-
-  for (const candidate of candidates) {
-    if (typeof candidate === "string" && candidate.trim()) {
-      return candidate.trim();
-    }
-  }
-
-  return null;
 }
 
 async function invokeAgentByName(
diff --git a/paperclip-addon/plugins/workflow-engine/src/worker.ts b/paperclip-addon/plugins/workflow-engine/src/worker.ts
index c5ac3fbb4ca7f767eb29331bd3630b635449b335..303e35958890fa1f66db3f2b0ff1fa1f134102a8
--- a/paperclip-addon/plugins/workflow-engine/src/worker.ts
+++ b/paperclip-addon/plugins/workflow-engine/src/worker.ts
@@ -15,6 +15,7 @@
 } from "./dag-engine.js";
 import {
   checkIdempotency,
+  createWorkflowRun,
   createStepRun,
   findStepRunByIssueId,
   getWorkflowDefinition,
@@ -25,51 +26,38 @@
   markIdempotency,
   updateStepRun,
   updateWorkflowRun,
-  type WorkflowDefinition,
-  type WorkflowRun,
   type WorkflowStepRun,
 } from "./workflow-store.js";
+import {
+  TERMINAL_STEP_STATUSES,
+  findStepDefinition,
+  getStepAgentName,
+  getStepAgentNameHint,
+  toWorkflowDefinitionRecord,
+  toWorkflowRunRecord,
+  toWorkflowStepRunRecord,
+  type WorkflowDefinitionRecord,
+  type WorkflowRunRecord,
+  type WorkflowStepRunRecord,
+} from "./workflow-utils.js";
 
-type WorkflowStepRecord = PluginEntityRecord & { data: WorkflowStepRun };
-type WorkflowRunRecord = PluginEntityRecord & { data: WorkflowRun };
-type WorkflowDefinitionRecord = PluginEntityRecord & { data: WorkflowDefinition };
 type WorkflowStepMetadata = WorkflowStep & {
   description?: string;
-  agent?: string;
-  agentName?: string;
-  assigneeAgentName?: string;
   assigneeAgentId?: string;
+  sessionMode?: "fresh" | "reuse";
 };
 type AgentRecord = Awaited<ReturnType<PluginContext["agents"]["list"]>>[number];
+type IssueUpdatePatch = Parameters<PluginContext["issues"]["update"]>[1];
 type ReconcilerModule = {
   reconcileStuckSteps?: (ctx: PluginContext) => Promise<void>;
 };
 
-const TERMINAL_STEP_STATUSES = new Set<string>([
-  STEP_STATUSES.done,
-  STEP_STATUSES.failed,
-  STEP_STATUSES.skipped,
-  STEP_STATUSES.escalated,
-]);
-
 function summarizeError(error: unknown): string {
   if (error instanceof Error) {
     return error.message;
   }
 
   return String(error);
-}
-
-function toStepRunRecord(record: PluginEntityRecord): WorkflowStepRecord {
-  return record as WorkflowStepRecord;
-}
-
-function toWorkflowRunRecord(record: PluginEntityRecord): WorkflowRunRecord {
-  return record as WorkflowRunRecord;
-}
-
-function toWorkflowDefinitionRecord(record: PluginEntityRecord): WorkflowDefinitionRecord {
-  return record as WorkflowDefinitionRecord;
 }
 
 function buildIdempotencyKey(event: PluginEvent): string {
@@ -78,10 +66,6 @@
 
 function getStepMetadata(stepDef: WorkflowStep): WorkflowStepMetadata {
   return stepDef as WorkflowStepMetadata;
-}
-
-function findStepDefinition(definition: WorkflowDefinitionRecord, stepId: string): WorkflowStep | null {
-  return definition.data.steps.find((step: WorkflowStep) => step.id === stepId) ?? null;
 }
 
 function getStepDescription(stepDef: WorkflowStep): string | undefined {
@@ -89,19 +73,6 @@
   return typeof description === "string" && description.trim() ? description.trim() : undefined;
 }
 
-function getStepAgentNameHint(stepDef: WorkflowStep): string | null {
-  const meta = getStepMetadata(stepDef);
-  const candidates = [meta.agentName, meta.agent, meta.assigneeAgentName];
-
-  for (const candidate of candidates) {
-    if (typeof candidate === "string" && candidate.trim()) {
-      return candidate.trim();
-    }
-  }
-
-  return null;
-}
-
 function getStepAgentIdHint(stepDef: WorkflowStep): string | null {
   const assigneeAgentId = getStepMetadata(stepDef).assigneeAgentId;
   if (typeof assigneeAgentId !== "string" || !assigneeAgentId.trim()) {
@@ -147,68 +118,256 @@
 
 async function invokeAgentForStep(
   ctx: PluginContext,
+  stepRunRecord: WorkflowStepRunRecord,
   stepDef: WorkflowStep,
-  agentName: string,
   workflowName: string,
   companyId: string,
-): Promise<void> {
+): Promise<WorkflowStepRunRecord> {
+  const agentName = getStepAgentName(stepRunRecord, stepDef);
+  if (!agentName) {
+    ctx.logger.warn("Agent name is missing for workflow step", {
+      companyId,
+      runId: stepRunRecord.data.runId,
+      stepId: stepDef.id,
+      workflowName,
+    });
+    return stepRunRecord;
+  }
+
   const agents = await ctx.agents.list({ companyId });
-  const agent = agents.find((candidate: AgentRecord) => candidate.name === agentName);
+  const agent = agents.find((candidate: AgentRecord) => candidate.name === agentName) ?? null;
 
   if (!agent) {
     ctx.logger.warn("Agent not found for step", {
       agentName,
       companyId,
+      runId: stepRunRecord.data.runId,
+      stepId: stepDef.id,
+      workflowName,
+    });
+    return stepRunRecord;
+  }
+
+  const prompt = `workflow:${workflowName}/step:${stepDef.id} — "${stepDef.title}" is ready. Please proceed with the assigned task.`;
+  const reason = `workflow:${workflowName}/step:${stepDef.id}`;
+  const sessionMode = getStepMetadata(stepDef).sessionMode === "reuse" ? "reuse" : "fresh";
+
+  if (sessionMode === "reuse") {
+    let sessionId = typeof stepRunRecord.data.sessionId === "string" && stepRunRecord.data.sessionId.trim()
+      ? stepRunRecord.data.sessionId.trim()
+      : "";
+
+    if (!sessionId) {
+      const session = await ctx.agents.sessions.create(agent.id, companyId, {
+        reason,
+      });
+      sessionId = session.sessionId;
+      stepRunRecord = toWorkflowStepRunRecord(await updateStepRun(ctx, stepRunRecord.id, {
+        sessionId,
+      }));
+    }
+
+    await ctx.agents.sessions.sendMessage(sessionId, companyId, {
+      prompt,
+      reason,
+    });
+
+    ctx.logger.info("Sent workflow step prompt via agent session", {
+      agentId: agent.id,
+      agentName,
+      companyId,
+      runId: stepRunRecord.data.runId,
+      sessionId,
       stepId: stepDef.id,
       workflowName,
     });
-    return;
+    return stepRunRecord;
   }
 
   await ctx.agents.invoke(agent.id, companyId, {
-    prompt: `workflow:${workflowName}/step:${stepDef.id} — "${stepDef.title}" is ready. Please proceed with the assigned task.`,
-    reason: `workflow:${workflowName}/step:${stepDef.id}`,
+    prompt,
+    reason,
   });
 
   ctx.logger.info("Invoked agent for workflow step", {
     agentId: agent.id,
     agentName,
     companyId,
+    runId: stepRunRecord.data.runId,
     stepId: stepDef.id,
     workflowName,
   });
+
+  return stepRunRecord;
 }
 
 async function activateBacklogStep(
   ctx: PluginContext,
-  stepRunRecord: WorkflowStepRecord,
+  stepRunRecord: WorkflowStepRunRecord,
   stepDef: WorkflowStep,
   workflowName: string,
   companyId: string,
-): Promise<void> {
+  options?: { parentIssueId?: string },
+): Promise<WorkflowStepRunRecord> {
   if (stepRunRecord.data.status !== STEP_STATUSES.backlog) {
-    return;
+    return stepRunRecord;
   }
 
-  const updated = toStepRunRecord(await updateStepRun(ctx, stepRunRecord.id, {
+  const resolvedAgent = await resolveStepAgent(
+    ctx,
+    companyId,
+    stepDef,
+    stepRunRecord.data.agentName,
+  );
+
+  let issueId = typeof stepRunRecord.data.issueId === "string" && stepRunRecord.data.issueId.trim()
+    ? stepRunRecord.data.issueId.trim()
+    : "";
+  const stepDescription = getStepDescription(stepDef) ?? `Workflow step: ${stepDef.id}`;
+
+  if (!issueId) {
+    const issue = await ctx.issues.create({
+      assigneeAgentId: resolvedAgent.agentId ?? undefined,
+      companyId,
+      description: stepDescription,
+      parentId: options?.parentIssueId,
+      title: `[${workflowName}] ${stepDef.title}`,
+    });
+    await ctx.issues.update(issue.id, { status: "todo" } as IssueUpdatePatch, companyId);
+    issueId = issue.id;
+  }
+
+  const nextStartedAt = stepRunRecord.data.startedAt ?? new Date().toISOString();
+  let updatedStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, stepRunRecord.id, {
+    agentName: resolvedAgent.agentName ?? stepRunRecord.data.agentName,
+    issueId,
+    startedAt: nextStartedAt,
     status: STEP_STATUSES.todo,
   }));
 
-  await invokeAgentForStep(
+  updatedStepRun = await invokeAgentForStep(
     ctx,
+    updatedStepRun,
     stepDef,
-    updated.data.agentName,
     workflowName,
     companyId,
   );
+
+  return updatedStepRun;
 }
 
+async function startWorkflow(
+  ctx: PluginContext,
+  workflowId: string,
+  companyId: string,
+  options?: {
+    createParentIssue?: boolean;
+    parentIssueId?: string;
+  },
+): Promise<{
+  activatedStepIds: string[];
+  parentIssueId: string | null;
+  runId: string;
+  workflowId: string;
+}> {
+  const workflowDefinition = await getWorkflowDefinition(ctx, workflowId);
+  if (!workflowDefinition) {
+    throw new Error(`Workflow definition not found: ${workflowId}`);
+  }
+
+  const typedWorkflowDefinition = toWorkflowDefinitionRecord(workflowDefinition);
+  if (typedWorkflowDefinition.data.companyId !== companyId) {
+    throw new Error(`Workflow does not belong to company: ${workflowId}`);
+  }
+
+  let parentIssueId = typeof options?.parentIssueId === "string" && options.parentIssueId.trim()
+    ? options.parentIssueId.trim()
+    : "";
+  if (!parentIssueId && options?.createParentIssue) {
+    const parentIssue = await ctx.issues.create({
+      companyId,
+      description: typedWorkflowDefinition.data.description || `Workflow run: ${typedWorkflowDefinition.data.name}`,
+      title: `[${typedWorkflowDefinition.data.name}] Workflow run`,
+    });
+    parentIssueId = parentIssue.id;
+  }
+
+  const workflowRun = toWorkflowRunRecord(await createWorkflowRun(ctx, {
+    companyId,
+    parentIssueId: parentIssueId || undefined,
+    startedAt: new Date().toISOString(),
+    status: RUN_STATUSES.running,
+    workflowId: typedWorkflowDefinition.id,
+    workflowName: typedWorkflowDefinition.data.name,
+  }));
+
+  const agents = await ctx.agents.list({ companyId });
+  const agentsByName = new Map<string, AgentRecord>();
+  for (const agent of agents) {
+    agentsByName.set(agent.name, agent);
+  }
+
+  const pendingRootSteps: Array<{ stepDef: WorkflowStep; stepRun: WorkflowStepRunRecord }> = [];
+  for (const stepDef of typedWorkflowDefinition.data.steps) {
+    const agentNameHint = getStepAgentNameHint(stepDef);
+    const matchedAgent = agentNameHint ? agentsByName.get(agentNameHint) ?? null : null;
+    const resolvedAgent = matchedAgent
+      ? { agentId: matchedAgent.id, agentName: matchedAgent.name }
+      : await resolveStepAgent(ctx, companyId, stepDef, agentNameHint ?? undefined);
+
+    if (!resolvedAgent.agentName) {
+      throw new Error(`Unable to resolve step assignee for "${stepDef.id}"`);
+    }
+
+    const stepRun = toWorkflowStepRunRecord(await createStepRun(ctx, companyId, {
+      agentName: resolvedAgent.agentName,
+      retryCount: 0,
+      runId: workflowRun.id,
+      status: STEP_STATUSES.backlog,
+      stepId: stepDef.id,
+    }));
+
+    if (stepDef.dependsOn.length === 0 && stepDef.triggerOn !== "escalation") {
+      pendingRootSteps.push({ stepDef, stepRun });
+    }
+  }
+
+  const activatedStepIds: string[] = [];
+  for (const pending of pendingRootSteps) {
+    await activateBacklogStep(
+      ctx,
+      pending.stepRun,
+      pending.stepDef,
+      typedWorkflowDefinition.data.name,
+      companyId,
+      { parentIssueId: parentIssueId || undefined },
+    );
+    activatedStepIds.push(pending.stepDef.id);
+  }
+
+  ctx.logger.info("Started workflow run", {
+    activatedStepIds,
+    companyId,
+    parentIssueId: parentIssueId || null,
+    runId: workflowRun.id,
+    workflowId: typedWorkflowDefinition.id,
+    workflowName: typedWorkflowDefinition.data.name,
+  });
+
+  return {
+    activatedStepIds,
+    parentIssueId: parentIssueId || null,
+    runId: workflowRun.id,
+    workflowId: typedWorkflowDefinition.id,
+  };
+}
+
 async function advanceWorkflow(
   ctx: PluginContext,
   stepRunRecord: PluginEntityRecord,
   companyId: string,
 ): Promise<void> {
-  const completedStepRun = toStepRunRecord(stepRunRecord);
+  const completedStepRun = toWorkflowStepRunRecord(stepRunRecord);
   const workflowRun = await getWorkflowRun(ctx, completedStepRun.data.runId);
 
   if (!workflowRun) {
@@ -237,7 +396,7 @@
   }
 
   const typedWorkflowDefinition = toWorkflowDefinitionRecord(workflowDefinition);
-  const stepRuns = (await listStepRuns(ctx, typedWorkflowRun.id, companyId)).map(toStepRunRecord);
+  const stepRuns = (await listStepRuns(ctx, typedWorkflowRun.id, companyId)).map(toWorkflowStepRunRecord);
   const completed = new Set<string>();
   const failed = new Set<string>();
   const skipped = new Set<string>();
@@ -289,7 +448,7 @@
         continue;
       }
 
-      stepRun = toStepRunRecord(await createStepRun(ctx, companyId, {
+      stepRun = toWorkflowStepRunRecord(await createStepRun(ctx, companyId, {
         agentName: resolvedAgent.agentName,
         retryCount: 0,
         runId: typedWorkflowRun.id,
@@ -305,6 +464,7 @@
       stepDef,
       typedWorkflowRun.data.workflowName,
       companyId,
+      { parentIssueId: typedWorkflowRun.data.parentIssueId },
     );
   }
 
@@ -329,7 +489,7 @@
   ctx: PluginContext,
   workflowRun: WorkflowRunRecord,
   workflowDefinition: WorkflowDefinitionRecord,
-  sourceStepRun: WorkflowStepRecord,
+  sourceStepRun: WorkflowStepRunRecord,
   escalationTargetId: string,
   companyId: string,
 ): Promise<void> {
@@ -338,7 +498,7 @@
     throw new Error(`Escalation target step not found: ${escalationTargetId}`);
   }
 
-  const stepRuns = (await listStepRuns(ctx, workflowRun.id, companyId)).map(toStepRunRecord);
+  const stepRuns = (await listStepRuns(ctx, workflowRun.id, companyId)).map(toWorkflowStepRunRecord);
   let escalationStepRun = stepRuns.find((candidate) => candidate.data.stepId === escalationTargetId) ?? null;
   const resolvedAgent = await resolveStepAgent(
     ctx,
@@ -352,7 +512,7 @@
   }
 
   if (!escalationStepRun) {
-    escalationStepRun = toStepRunRecord(await createStepRun(ctx, companyId, {
+    escalationStepRun = toWorkflowStepRunRecord(await createStepRun(ctx, companyId, {
       agentName: resolvedAgent.agentName,
       retryCount: 0,
       runId: workflowRun.id,
@@ -374,14 +534,14 @@
       title: `${workflowRun.data.workflowName}: ${escalationStep.title}`,
     });
 
-    escalationStepRun = toStepRunRecord(await updateStepRun(ctx, escalationStepRun.id, {
+    escalationStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, escalationStepRun.id, {
       issueId: issue.id,
     }));
   }
 
   const shouldActivate = escalationStepRun.data.status === STEP_STATUSES.backlog;
   if (shouldActivate) {
-    escalationStepRun = toStepRunRecord(await updateStepRun(ctx, escalationStepRun.id, {
+    escalationStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, escalationStepRun.id, {
       status: STEP_STATUSES.todo,
     }));
   }
@@ -389,8 +549,8 @@
   if (shouldActivate) {
     await invokeAgentForStep(
       ctx,
+      escalationStepRun,
       escalationStep,
-      escalationStepRun.data.agentName,
       workflowRun.data.workflowName,
       companyId,
     );
@@ -421,7 +581,7 @@
     return;
   }
 
-  const typedStepRun = toStepRunRecord(stepRunRecord);
+  const typedStepRun = toWorkflowStepRunRecord(stepRunRecord);
   if (TERMINAL_STEP_STATUSES.has(typedStepRun.data.status)) {
     return;
   }
@@ -469,7 +629,7 @@
   if (options.allowRetry) {
     const retryInfo = getRetryInfo(typedWorkflowDefinition.data.steps, typedStepRun.data.stepId);
     if (retryInfo.shouldRetry && typedStepRun.data.retryCount < retryInfo.maxRetries) {
-      const retriedStepRun = toStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
+      const retriedStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
         completedAt: undefined,
         retryCount: typedStepRun.data.retryCount + 1,
         status: STEP_STATUSES.todo,
@@ -477,8 +637,8 @@
 
       await invokeAgentForStep(
         ctx,
+        retriedStepRun,
         stepDef,
-        retriedStepRun.data.agentName,
         typedWorkflowRun.data.workflowName,
         event.companyId,
       );
@@ -496,7 +656,7 @@
   }
 
   if (stepDef.onFailure === "skip") {
-    const skippedStepRun = toStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
+    const skippedStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
       completedAt: new Date().toISOString(),
       status: STEP_STATUSES.skipped,
     }));
@@ -556,7 +716,7 @@
       return;
     }
 
-    const escalatedStepRun = toStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
+    const escalatedStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
       completedAt: new Date().toISOString(),
       status: STEP_STATUSES.escalated,
     }));
@@ -617,6 +777,29 @@
   }
 }
 
+function registerDataHandler(
+  ctx: PluginContext,
+  key: string,
+  handler: (params: Record<string, unknown>) => Promise<unknown>,
+): void {
+  const dataClient = ctx.data as {
+    handle?: (handlerKey: string, handlerFn: (params: Record<string, unknown>) => Promise<unknown>) => void;
+    register?: (handlerKey: string, handlerFn: (params: Record<string, unknown>) => Promise<unknown>) => void;
+  };
+
+  if (typeof dataClient.handle === "function") {
+    dataClient.handle(key, handler);
+    return;
+  }
+
+  if (typeof dataClient.register === "function") {
+    dataClient.register(key, handler);
+    return;
+  }
+
+  throw new Error("Plugin data client does not support handler registration");
+}
+
 const plugin = definePlugin({
   async setup(ctx: PluginContext) {
     ctx.events.on("issue.updated", async (event: PluginEvent) => {
@@ -638,7 +821,7 @@
           return;
         }
 
-        const stepRun = toStepRunRecord(stepRunRecord);
+        const stepRun = toWorkflowStepRunRecord(stepRunRecord);
         const payload = event.payload as { status?: string };
         const issueStatus = typeof payload.status === "string" ? payload.status : undefined;
 
@@ -657,7 +840,7 @@
           return;
         }
 
-        const updatedStepRun = toStepRunRecord(await updateStepRun(ctx, stepRun.id, {
+        const updatedStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, stepRun.id, {
           completedAt: nextStepStatus === STEP_STATUSES.done ? new Date().toISOString() : undefined,
           startedAt: nextStepStatus === STEP_STATUSES.inProgress
             ? stepRun.data.startedAt ?? new Date().toISOString()
@@ -687,11 +870,21 @@
 
     ctx.events.on("issue.created", async (event: PluginEvent) => {
       try {
+        const idempotencyKey = buildIdempotencyKey(event);
+        if (await checkIdempotency(ctx, idempotencyKey, event.companyId)) {
+          return;
+        }
+
         const payload = event.payload as {
           id?: string;
           parentId?: string;
           assigneeAgentId?: string;
         };
+        const issueId = typeof event.entityId === "string" && event.entityId.trim()
+          ? event.entityId.trim()
+          : typeof payload.id === "string" && payload.id.trim()
+            ? payload.id.trim()
+            : "";
 
         const parentId = typeof payload.parentId === "string" && payload.parentId.trim()
           ? payload.parentId.trim()
@@ -700,12 +893,34 @@
           ? payload.assigneeAgentId.trim()
           : "";
 
-        if (parentId || !assigneeAgentId) {
+        if (!issueId || parentId || !assigneeAgentId) {
+          await markIdempotency(ctx, idempotencyKey, event.companyId);
+          return;
+        }
+
+        const agent = await ctx.agents.get(assigneeAgentId, event.companyId);
+        const metadata = agent?.metadata as Record<string, unknown> | undefined;
+        const defaultParentIssueId = typeof metadata?.defaultParentIssueId === "string" && metadata.defaultParentIssueId.trim()
+          ? metadata.defaultParentIssueId.trim()
+          : "";
+
+        if (!defaultParentIssueId) {
+          await markIdempotency(ctx, idempotencyKey, event.companyId);
           return;
         }
 
-        ctx.logger.info("parentId filler: issue created without parent", {
-          issueId: event.entityId ?? payload.id ?? null,
+        await ctx.issues.update(
+          issueId,
+          { parentId: defaultParentIssueId } as IssueUpdatePatch,
+          event.companyId,
+        );
+        await markIdempotency(ctx, idempotencyKey, event.companyId);
+
+        ctx.logger.info("parentId filler: populated issue parentId from agent metadata", {
+          assigneeAgentId,
+          companyId: event.companyId,
+          issueId,
+          parentId: defaultParentIssueId,
         });
       } catch (error) {
         ctx.logger.warn("Failed to handle issue.created event", {
@@ -744,7 +959,23 @@
       await runReconciler(ctx);
     });
 
-    ctx.data.register("workflow-overview", async (params: Record<string, unknown>) => {
+    registerDataHandler(ctx, "start-workflow", async (params: Record<string, unknown>) => {
+      const workflowId = typeof params.workflowId === "string" ? params.workflowId.trim() : "";
+      const companyId = typeof params.companyId === "string" ? params.companyId.trim() : "";
+      const parentIssueId = typeof params.parentIssueId === "string" ? params.parentIssueId.trim() : "";
+      const createParentIssue = params.createParentIssue === true;
+
+      if (!workflowId || !companyId) {
+        throw new Error("start-workflow requires workflowId and companyId");
+      }
+
+      return await startWorkflow(ctx, workflowId, companyId, {
+        createParentIssue,
+        parentIssueId: parentIssueId || undefined,
+      });
+    });
+
+    registerDataHandler(ctx, "workflow-overview", async (params: Record<string, unknown>) => {
       try {
         const companyId = typeof params.companyId === "string" ? params.companyId.trim() : "";
         if (!companyId) {
@@ -782,7 +1013,7 @@
       }
     });
 
-    ctx.data.register("workflow-run-detail", async (params: Record<string, unknown>) => {
+    registerDataHandler(ctx, "workflow-run-detail", async (params: Record<string, unknown>) => {
       try {
         const runId = typeof params.runId === "string" ? params.runId.trim() : "";
         if (!runId) {
@@ -807,7 +1038,7 @@
             ...typedWorkflowRun.data,
           },
           stepRuns: stepRuns.map((record: PluginEntityRecord) => {
-            const stepRun = toStepRunRecord(record);
+            const stepRun = toWorkflowStepRunRecord(record);
             return {
               id: stepRun.id,
               status: stepRun.status,
diff --git a/paperclip-addon/plugins/workflow-engine/src/workflow-store.ts b/paperclip-addon/plugins/workflow-engine/src/workflow-store.ts
index 5871a2082258cebcfe8b69ebafbb0814cf303d50..0f6dba89ca559d4f5821749b25241d5a3be3e85d
--- a/paperclip-addon/plugins/workflow-engine/src/workflow-store.ts
+++ b/paperclip-addon/plugins/workflow-engine/src/workflow-store.ts
@@ -44,7 +44,14 @@
 
 type TypedEntityRecord<T> = Omit<PluginEntityRecord, "data"> & { data: T };
 
-function toTypedRecord<T>(record: PluginEntityRecord): TypedEntityRecord<T> {
+function toTypedRecord<T>(
+  record: PluginEntityRecord,
+  entityType: string,
+): TypedEntityRecord<T> {
+  if (record.entityType !== entityType) {
+    throw new Error(`Expected entity type "${entityType}", got "${record.entityType}"`);
+  }
+
   return record as TypedEntityRecord<T>;
 }
 
@@ -67,13 +74,31 @@
   id: string,
   entityType: string,
 ): Promise<TypedEntityRecord<T> | null> {
-  const record = await ctx.entities.get(id);
+  const listQuery = {
+    entityType,
+    id,
+    limit: 1,
+  } as Record<string, unknown>;
 
-  if (!record || record.entityType !== entityType) {
+  const listed = await ctx.entities.list(
+    listQuery as Parameters<PluginContext["entities"]["list"]>[0],
+  );
+  const listedRecord = listed.find(
+    (record: PluginEntityRecord) => record.id === id,
+  ) ?? null;
+
+  if (listedRecord) {
+    return toTypedRecord<T>(listedRecord, entityType);
+  };
+
+  // Fallback: some host SDK variants ignore `id` on entities.list().
+  // Keep defensive type verification on direct get() until id-filter support is universal.
+  const record = await ctx.entities.get(id);
+  if (!record) {
     return null;
   }
 
-  return toTypedRecord<T>(record);
+  return toTypedRecord<T>(record, entityType);
 }
 
 async function requireEntityByType<T>(
diff --git a/paperclip-addon/plugins/workflow-engine/src/workflow-utils.ts b/paperclip-addon/plugins/workflow-engine/src/workflow-utils.ts
new file mode 100644
index 0000000000000000000000000000000000000000..ec1cf6f6b9aa722b126b58c0a728b4e538ad1494
--- /dev/null
+++ b/paperclip-addon/plugins/workflow-engine/src/workflow-utils.ts
@@ -0,0 +1,62 @@
+import type { PluginEntityRecord } from "@paperclipai/plugin-sdk";
+
+import { STEP_STATUSES } from "./constants.js";
+import type { WorkflowStep } from "./dag-engine.js";
+import type {
+  WorkflowDefinition,
+  WorkflowRun,
+  WorkflowStepRun,
+} from "./workflow-store.js";
+
+export type WorkflowRunRecord = PluginEntityRecord & { data: WorkflowRun };
+export type WorkflowStepRunRecord = PluginEntityRecord & { data: WorkflowStepRun };
+export type WorkflowDefinitionRecord = PluginEntityRecord & { data: WorkflowDefinition };
+type StepAgentMetadata = WorkflowStep & {
+  agent?: string;
+  agentName?: string;
+  assigneeAgentName?: string;
+};
+
+export const TERMINAL_STEP_STATUSES = new Set<string>([
+  STEP_STATUSES.done,
+  STEP_STATUSES.failed,
+  STEP_STATUSES.skipped,
+  STEP_STATUSES.escalated,
+]);
+
+export function toWorkflowRunRecord(record: PluginEntityRecord): WorkflowRunRecord {
+  return record as WorkflowRunRecord;
+}
+
+export function toWorkflowStepRunRecord(record: PluginEntityRecord): WorkflowStepRunRecord {
+  return record as WorkflowStepRunRecord;
+}
+
+export function toWorkflowDefinitionRecord(record: PluginEntityRecord): WorkflowDefinitionRecord {
+  return record as WorkflowDefinitionRecord;
+}
+
+export function findStepDefinition(definition: WorkflowDefinitionRecord, stepId: string): WorkflowStep | null {
+  return definition.data.steps.find((step: WorkflowStep) => step.id === stepId) ?? null;
+}
+
+export function getStepAgentNameHint(stepDef: WorkflowStep): string | null {
+  const stepMeta = stepDef as StepAgentMetadata;
+  const candidates = [stepMeta.agentName, stepMeta.agent, stepMeta.assigneeAgentName];
+
+  for (const candidate of candidates) {
+    if (typeof candidate === "string" && candidate.trim()) {
+      return candidate.trim();
+    }
+  }
+
+  return null;
+}
+
+export function getStepAgentName(stepRun: WorkflowStepRunRecord, stepDef: WorkflowStep): string | null {
+  if (typeof stepRun.data.agentName === "string" && stepRun.data.agentName.trim()) {
+    return stepRun.data.agentName.trim();
+  }
+
+  return getStepAgentNameHint(stepDef);
+}

file update:
diff --git a/paperclip-addon/plugins/workflow-engine/src/dag-engine.ts b/paperclip-addon/plugins/workflow-engine/src/dag-engine.ts
index b59510f0473171038be77277286d3b5c2063e6c5..e7f21ff998cab78b9325a8aeacf4fda9c5e06553
--- a/paperclip-addon/plugins/workflow-engine/src/dag-engine.ts
+++ b/paperclip-addon/plugins/workflow-engine/src/dag-engine.ts
@@ -2,6 +2,7 @@
   id: string;
   title: string;
   dependsOn: string[];
+  sessionMode?: "fresh" | "reuse";
   onFailure?: "retry" | "skip" | "abort_workflow" | "escalate";
   escalateTo?: string;
   maxRetries?: number;
diff --git a/paperclip-addon/plugins/workflow-engine/src/reconciler.ts b/paperclip-addon/plugins/workflow-engine/src/reconciler.ts
index 14ab4e766a835d145a6c2e7b1b564270285cbdc3..d2ab54817bd8185944379d2188bfbdef1722020a
--- a/paperclip-addon/plugins/workflow-engine/src/reconciler.ts
+++ b/paperclip-addon/plugins/workflow-engine/src/reconciler.ts
@@ -8,65 +8,22 @@
   listStepRuns,
   updateStepRun,
   updateWorkflowRun,
-  type WorkflowDefinition,
-  type WorkflowRun,
-  type WorkflowStepRun,
 } from "./workflow-store.js";
 import type { WorkflowStep } from "./dag-engine.js";
-
-type WorkflowRunRecord = PluginEntityRecord & { data: WorkflowRun };
-type WorkflowStepRunRecord = PluginEntityRecord & { data: WorkflowStepRun };
-type WorkflowDefinitionRecord = PluginEntityRecord & { data: WorkflowDefinition };
-type StepAgentMetadata = WorkflowStep & {
-  agent?: string;
-  agentName?: string;
-  assigneeAgentName?: string;
-};
+import {
+  TERMINAL_STEP_STATUSES,
+  findStepDefinition,
+  getStepAgentName,
+  toWorkflowDefinitionRecord,
+  toWorkflowRunRecord,
+  toWorkflowStepRunRecord,
+} from "./workflow-utils.js";
 
 const STALE_THRESHOLD_MS = 300_000;
-const TERMINAL_STEP_STATUSES = new Set<string>([
-  STEP_STATUSES.done,
-  STEP_STATUSES.failed,
-  STEP_STATUSES.skipped,
-  STEP_STATUSES.escalated,
-]);
 
 function isStale(record: PluginEntityRecord, thresholdMs: number): boolean {
   const updatedAt = new Date(record.updatedAt).getTime();
   return Date.now() - updatedAt > thresholdMs;
-}
-
-function toWorkflowRunRecord(record: PluginEntityRecord): WorkflowRunRecord {
-  return record as WorkflowRunRecord;
-}
-
-function toWorkflowStepRunRecord(record: PluginEntityRecord): WorkflowStepRunRecord {
-  return record as WorkflowStepRunRecord;
-}
-
-function toWorkflowDefinitionRecord(record: PluginEntityRecord): WorkflowDefinitionRecord {
-  return record as WorkflowDefinitionRecord;
-}
-
-function findStepDefinition(definition: WorkflowDefinitionRecord, stepId: string): WorkflowStep | null {
-  return definition.data.steps.find((step: WorkflowStep) => step.id === stepId) ?? null;
-}
-
-function getStepAgentName(stepRun: WorkflowStepRunRecord, stepDef: WorkflowStep): string | null {
-  if (typeof stepRun.data.agentName === "string" && stepRun.data.agentName.trim()) {
-    return stepRun.data.agentName.trim();
-  }
-
-  const stepMeta = stepDef as StepAgentMetadata;
-  const candidates = [stepMeta.agentName, stepMeta.agent, stepMeta.assigneeAgentName];
-
-  for (const candidate of candidates) {
-    if (typeof candidate === "string" && candidate.trim()) {
-      return candidate.trim();
-    }
-  }
-
-  return null;
 }
 
 async function invokeAgentByName(
diff --git a/paperclip-addon/plugins/workflow-engine/src/worker.ts b/paperclip-addon/plugins/workflow-engine/src/worker.ts
index c5ac3fbb4ca7f767eb29331bd3630b635449b335..303e35958890fa1f66db3f2b0ff1fa1f134102a8
--- a/paperclip-addon/plugins/workflow-engine/src/worker.ts
+++ b/paperclip-addon/plugins/workflow-engine/src/worker.ts
@@ -15,6 +15,7 @@
 } from "./dag-engine.js";
 import {
   checkIdempotency,
+  createWorkflowRun,
   createStepRun,
   findStepRunByIssueId,
   getWorkflowDefinition,
@@ -25,51 +26,38 @@
   markIdempotency,
   updateStepRun,
   updateWorkflowRun,
-  type WorkflowDefinition,
-  type WorkflowRun,
   type WorkflowStepRun,
 } from "./workflow-store.js";
+import {
+  TERMINAL_STEP_STATUSES,
+  findStepDefinition,
+  getStepAgentName,
+  getStepAgentNameHint,
+  toWorkflowDefinitionRecord,
+  toWorkflowRunRecord,
+  toWorkflowStepRunRecord,
+  type WorkflowDefinitionRecord,
+  type WorkflowRunRecord,
+  type WorkflowStepRunRecord,
+} from "./workflow-utils.js";
 
-type WorkflowStepRecord = PluginEntityRecord & { data: WorkflowStepRun };
-type WorkflowRunRecord = PluginEntityRecord & { data: WorkflowRun };
-type WorkflowDefinitionRecord = PluginEntityRecord & { data: WorkflowDefinition };
 type WorkflowStepMetadata = WorkflowStep & {
   description?: string;
-  agent?: string;
-  agentName?: string;
-  assigneeAgentName?: string;
   assigneeAgentId?: string;
+  sessionMode?: "fresh" | "reuse";
 };
 type AgentRecord = Awaited<ReturnType<PluginContext["agents"]["list"]>>[number];
+type IssueUpdatePatch = Parameters<PluginContext["issues"]["update"]>[1];
 type ReconcilerModule = {
   reconcileStuckSteps?: (ctx: PluginContext) => Promise<void>;
 };
 
-const TERMINAL_STEP_STATUSES = new Set<string>([
-  STEP_STATUSES.done,
-  STEP_STATUSES.failed,
-  STEP_STATUSES.skipped,
-  STEP_STATUSES.escalated,
-]);
-
 function summarizeError(error: unknown): string {
   if (error instanceof Error) {
     return error.message;
   }
 
   return String(error);
-}
-
-function toStepRunRecord(record: PluginEntityRecord): WorkflowStepRecord {
-  return record as WorkflowStepRecord;
-}
-
-function toWorkflowRunRecord(record: PluginEntityRecord): WorkflowRunRecord {
-  return record as WorkflowRunRecord;
-}
-
-function toWorkflowDefinitionRecord(record: PluginEntityRecord): WorkflowDefinitionRecord {
-  return record as WorkflowDefinitionRecord;
 }
 
 function buildIdempotencyKey(event: PluginEvent): string {
@@ -78,10 +66,6 @@
 
 function getStepMetadata(stepDef: WorkflowStep): WorkflowStepMetadata {
   return stepDef as WorkflowStepMetadata;
-}
-
-function findStepDefinition(definition: WorkflowDefinitionRecord, stepId: string): WorkflowStep | null {
-  return definition.data.steps.find((step: WorkflowStep) => step.id === stepId) ?? null;
 }
 
 function getStepDescription(stepDef: WorkflowStep): string | undefined {
@@ -89,19 +73,6 @@
   return typeof description === "string" && description.trim() ? description.trim() : undefined;
 }
 
-function getStepAgentNameHint(stepDef: WorkflowStep): string | null {
-  const meta = getStepMetadata(stepDef);
-  const candidates = [meta.agentName, meta.agent, meta.assigneeAgentName];
-
-  for (const candidate of candidates) {
-    if (typeof candidate === "string" && candidate.trim()) {
-      return candidate.trim();
-    }
-  }
-
-  return null;
-}
-
 function getStepAgentIdHint(stepDef: WorkflowStep): string | null {
   const assigneeAgentId = getStepMetadata(stepDef).assigneeAgentId;
   if (typeof assigneeAgentId !== "string" || !assigneeAgentId.trim()) {
@@ -147,68 +118,256 @@
 
 async function invokeAgentForStep(
   ctx: PluginContext,
+  stepRunRecord: WorkflowStepRunRecord,
   stepDef: WorkflowStep,
-  agentName: string,
   workflowName: string,
   companyId: string,
-): Promise<void> {
+): Promise<WorkflowStepRunRecord> {
+  const agentName = getStepAgentName(stepRunRecord, stepDef);
+  if (!agentName) {
+    ctx.logger.warn("Agent name is missing for workflow step", {
+      companyId,
+      runId: stepRunRecord.data.runId,
+      stepId: stepDef.id,
+      workflowName,
+    });
+    return stepRunRecord;
+  }
+
   const agents = await ctx.agents.list({ companyId });
-  const agent = agents.find((candidate: AgentRecord) => candidate.name === agentName);
+  const agent = agents.find((candidate: AgentRecord) => candidate.name === agentName) ?? null;
 
   if (!agent) {
     ctx.logger.warn("Agent not found for step", {
       agentName,
       companyId,
+      runId: stepRunRecord.data.runId,
+      stepId: stepDef.id,
+      workflowName,
+    });
+    return stepRunRecord;
+  }
+
+  const prompt = `workflow:${workflowName}/step:${stepDef.id} — "${stepDef.title}" is ready. Please proceed with the assigned task.`;
+  const reason = `workflow:${workflowName}/step:${stepDef.id}`;
+  const sessionMode = getStepMetadata(stepDef).sessionMode === "reuse" ? "reuse" : "fresh";
+
+  if (sessionMode === "reuse") {
+    let sessionId = typeof stepRunRecord.data.sessionId === "string" && stepRunRecord.data.sessionId.trim()
+      ? stepRunRecord.data.sessionId.trim()
+      : "";
+
+    if (!sessionId) {
+      const session = await ctx.agents.sessions.create(agent.id, companyId, {
+        reason,
+      });
+      sessionId = session.sessionId;
+      stepRunRecord = toWorkflowStepRunRecord(await updateStepRun(ctx, stepRunRecord.id, {
+        sessionId,
+      }));
+    }
+
+    await ctx.agents.sessions.sendMessage(sessionId, companyId, {
+      prompt,
+      reason,
+    });
+
+    ctx.logger.info("Sent workflow step prompt via agent session", {
+      agentId: agent.id,
+      agentName,
+      companyId,
+      runId: stepRunRecord.data.runId,
+      sessionId,
       stepId: stepDef.id,
       workflowName,
     });
-    return;
+    return stepRunRecord;
   }
 
   await ctx.agents.invoke(agent.id, companyId, {
-    prompt: `workflow:${workflowName}/step:${stepDef.id} — "${stepDef.title}" is ready. Please proceed with the assigned task.`,
-    reason: `workflow:${workflowName}/step:${stepDef.id}`,
+    prompt,
+    reason,
   });
 
   ctx.logger.info("Invoked agent for workflow step", {
     agentId: agent.id,
     agentName,
     companyId,
+    runId: stepRunRecord.data.runId,
     stepId: stepDef.id,
     workflowName,
   });
+
+  return stepRunRecord;
 }
 
 async function activateBacklogStep(
   ctx: PluginContext,
-  stepRunRecord: WorkflowStepRecord,
+  stepRunRecord: WorkflowStepRunRecord,
   stepDef: WorkflowStep,
   workflowName: string,
   companyId: string,
-): Promise<void> {
+  options?: { parentIssueId?: string },
+): Promise<WorkflowStepRunRecord> {
   if (stepRunRecord.data.status !== STEP_STATUSES.backlog) {
-    return;
+    return stepRunRecord;
   }
 
-  const updated = toStepRunRecord(await updateStepRun(ctx, stepRunRecord.id, {
+  const resolvedAgent = await resolveStepAgent(
+    ctx,
+    companyId,
+    stepDef,
+    stepRunRecord.data.agentName,
+  );
+
+  let issueId = typeof stepRunRecord.data.issueId === "string" && stepRunRecord.data.issueId.trim()
+    ? stepRunRecord.data.issueId.trim()
+    : "";
+  const stepDescription = getStepDescription(stepDef) ?? `Workflow step: ${stepDef.id}`;
+
+  if (!issueId) {
+    const issue = await ctx.issues.create({
+      assigneeAgentId: resolvedAgent.agentId ?? undefined,
+      companyId,
+      description: stepDescription,
+      parentId: options?.parentIssueId,
+      title: `[${workflowName}] ${stepDef.title}`,
+    });
+    await ctx.issues.update(issue.id, { status: "todo" } as IssueUpdatePatch, companyId);
+    issueId = issue.id;
+  }
+
+  const nextStartedAt = stepRunRecord.data.startedAt ?? new Date().toISOString();
+  let updatedStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, stepRunRecord.id, {
+    agentName: resolvedAgent.agentName ?? stepRunRecord.data.agentName,
+    issueId,
+    startedAt: nextStartedAt,
     status: STEP_STATUSES.todo,
   }));
 
-  await invokeAgentForStep(
+  updatedStepRun = await invokeAgentForStep(
     ctx,
+    updatedStepRun,
     stepDef,
-    updated.data.agentName,
     workflowName,
     companyId,
   );
+
+  return updatedStepRun;
 }
 
+async function startWorkflow(
+  ctx: PluginContext,
+  workflowId: string,
+  companyId: string,
+  options?: {
+    createParentIssue?: boolean;
+    parentIssueId?: string;
+  },
+): Promise<{
+  activatedStepIds: string[];
+  parentIssueId: string | null;
+  runId: string;
+  workflowId: string;
+}> {
+  const workflowDefinition = await getWorkflowDefinition(ctx, workflowId);
+  if (!workflowDefinition) {
+    throw new Error(`Workflow definition not found: ${workflowId}`);
+  }
+
+  const typedWorkflowDefinition = toWorkflowDefinitionRecord(workflowDefinition);
+  if (typedWorkflowDefinition.data.companyId !== companyId) {
+    throw new Error(`Workflow does not belong to company: ${workflowId}`);
+  }
+
+  let parentIssueId = typeof options?.parentIssueId === "string" && options.parentIssueId.trim()
+    ? options.parentIssueId.trim()
+    : "";
+  if (!parentIssueId && options?.createParentIssue) {
+    const parentIssue = await ctx.issues.create({
+      companyId,
+      description: typedWorkflowDefinition.data.description || `Workflow run: ${typedWorkflowDefinition.data.name}`,
+      title: `[${typedWorkflowDefinition.data.name}] Workflow run`,
+    });
+    parentIssueId = parentIssue.id;
+  }
+
+  const workflowRun = toWorkflowRunRecord(await createWorkflowRun(ctx, {
+    companyId,
+    parentIssueId: parentIssueId || undefined,
+    startedAt: new Date().toISOString(),
+    status: RUN_STATUSES.running,
+    workflowId: typedWorkflowDefinition.id,
+    workflowName: typedWorkflowDefinition.data.name,
+  }));
+
+  const agents = await ctx.agents.list({ companyId });
+  const agentsByName = new Map<string, AgentRecord>();
+  for (const agent of agents) {
+    agentsByName.set(agent.name, agent);
+  }
+
+  const pendingRootSteps: Array<{ stepDef: WorkflowStep; stepRun: WorkflowStepRunRecord }> = [];
+  for (const stepDef of typedWorkflowDefinition.data.steps) {
+    const agentNameHint = getStepAgentNameHint(stepDef);
+    const matchedAgent = agentNameHint ? agentsByName.get(agentNameHint) ?? null : null;
+    const resolvedAgent = matchedAgent
+      ? { agentId: matchedAgent.id, agentName: matchedAgent.name }
+      : await resolveStepAgent(ctx, companyId, stepDef, agentNameHint ?? undefined);
+
+    if (!resolvedAgent.agentName) {
+      throw new Error(`Unable to resolve step assignee for "${stepDef.id}"`);
+    }
+
+    const stepRun = toWorkflowStepRunRecord(await createStepRun(ctx, companyId, {
+      agentName: resolvedAgent.agentName,
+      retryCount: 0,
+      runId: workflowRun.id,
+      status: STEP_STATUSES.backlog,
+      stepId: stepDef.id,
+    }));
+
+    if (stepDef.dependsOn.length === 0 && stepDef.triggerOn !== "escalation") {
+      pendingRootSteps.push({ stepDef, stepRun });
+    }
+  }
+
+  const activatedStepIds: string[] = [];
+  for (const pending of pendingRootSteps) {
+    await activateBacklogStep(
+      ctx,
+      pending.stepRun,
+      pending.stepDef,
+      typedWorkflowDefinition.data.name,
+      companyId,
+      { parentIssueId: parentIssueId || undefined },
+    );
+    activatedStepIds.push(pending.stepDef.id);
+  }
+
+  ctx.logger.info("Started workflow run", {
+    activatedStepIds,
+    companyId,
+    parentIssueId: parentIssueId || null,
+    runId: workflowRun.id,
+    workflowId: typedWorkflowDefinition.id,
+    workflowName: typedWorkflowDefinition.data.name,
+  });
+
+  return {
+    activatedStepIds,
+    parentIssueId: parentIssueId || null,
+    runId: workflowRun.id,
+    workflowId: typedWorkflowDefinition.id,
+  };
+}
+
 async function advanceWorkflow(
   ctx: PluginContext,
   stepRunRecord: PluginEntityRecord,
   companyId: string,
 ): Promise<void> {
-  const completedStepRun = toStepRunRecord(stepRunRecord);
+  const completedStepRun = toWorkflowStepRunRecord(stepRunRecord);
   const workflowRun = await getWorkflowRun(ctx, completedStepRun.data.runId);
 
   if (!workflowRun) {
@@ -237,7 +396,7 @@
   }
 
   const typedWorkflowDefinition = toWorkflowDefinitionRecord(workflowDefinition);
-  const stepRuns = (await listStepRuns(ctx, typedWorkflowRun.id, companyId)).map(toStepRunRecord);
+  const stepRuns = (await listStepRuns(ctx, typedWorkflowRun.id, companyId)).map(toWorkflowStepRunRecord);
   const completed = new Set<string>();
   const failed = new Set<string>();
   const skipped = new Set<string>();
@@ -289,7 +448,7 @@
         continue;
       }
 
-      stepRun = toStepRunRecord(await createStepRun(ctx, companyId, {
+      stepRun = toWorkflowStepRunRecord(await createStepRun(ctx, companyId, {
         agentName: resolvedAgent.agentName,
         retryCount: 0,
         runId: typedWorkflowRun.id,
@@ -305,6 +464,7 @@
       stepDef,
       typedWorkflowRun.data.workflowName,
       companyId,
+      { parentIssueId: typedWorkflowRun.data.parentIssueId },
     );
   }
 
@@ -329,7 +489,7 @@
   ctx: PluginContext,
   workflowRun: WorkflowRunRecord,
   workflowDefinition: WorkflowDefinitionRecord,
-  sourceStepRun: WorkflowStepRecord,
+  sourceStepRun: WorkflowStepRunRecord,
   escalationTargetId: string,
   companyId: string,
 ): Promise<void> {
@@ -338,7 +498,7 @@
     throw new Error(`Escalation target step not found: ${escalationTargetId}`);
   }
 
-  const stepRuns = (await listStepRuns(ctx, workflowRun.id, companyId)).map(toStepRunRecord);
+  const stepRuns = (await listStepRuns(ctx, workflowRun.id, companyId)).map(toWorkflowStepRunRecord);
   let escalationStepRun = stepRuns.find((candidate) => candidate.data.stepId === escalationTargetId) ?? null;
   const resolvedAgent = await resolveStepAgent(
     ctx,
@@ -352,7 +512,7 @@
   }
 
   if (!escalationStepRun) {
-    escalationStepRun = toStepRunRecord(await createStepRun(ctx, companyId, {
+    escalationStepRun = toWorkflowStepRunRecord(await createStepRun(ctx, companyId, {
       agentName: resolvedAgent.agentName,
       retryCount: 0,
       runId: workflowRun.id,
@@ -374,14 +534,14 @@
       title: `${workflowRun.data.workflowName}: ${escalationStep.title}`,
     });
 
-    escalationStepRun = toStepRunRecord(await updateStepRun(ctx, escalationStepRun.id, {
+    escalationStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, escalationStepRun.id, {
       issueId: issue.id,
     }));
   }
 
   const shouldActivate = escalationStepRun.data.status === STEP_STATUSES.backlog;
   if (shouldActivate) {
-    escalationStepRun = toStepRunRecord(await updateStepRun(ctx, escalationStepRun.id, {
+    escalationStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, escalationStepRun.id, {
       status: STEP_STATUSES.todo,
     }));
   }
@@ -389,8 +549,8 @@
   if (shouldActivate) {
     await invokeAgentForStep(
       ctx,
+      escalationStepRun,
       escalationStep,
-      escalationStepRun.data.agentName,
       workflowRun.data.workflowName,
       companyId,
     );
@@ -421,7 +581,7 @@
     return;
   }
 
-  const typedStepRun = toStepRunRecord(stepRunRecord);
+  const typedStepRun = toWorkflowStepRunRecord(stepRunRecord);
   if (TERMINAL_STEP_STATUSES.has(typedStepRun.data.status)) {
     return;
   }
@@ -469,7 +629,7 @@
   if (options.allowRetry) {
     const retryInfo = getRetryInfo(typedWorkflowDefinition.data.steps, typedStepRun.data.stepId);
     if (retryInfo.shouldRetry && typedStepRun.data.retryCount < retryInfo.maxRetries) {
-      const retriedStepRun = toStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
+      const retriedStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
         completedAt: undefined,
         retryCount: typedStepRun.data.retryCount + 1,
         status: STEP_STATUSES.todo,
@@ -477,8 +637,8 @@
 
       await invokeAgentForStep(
         ctx,
+        retriedStepRun,
         stepDef,
-        retriedStepRun.data.agentName,
         typedWorkflowRun.data.workflowName,
         event.companyId,
       );
@@ -496,7 +656,7 @@
   }
 
   if (stepDef.onFailure === "skip") {
-    const skippedStepRun = toStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
+    const skippedStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
       completedAt: new Date().toISOString(),
       status: STEP_STATUSES.skipped,
     }));
@@ -556,7 +716,7 @@
       return;
     }
 
-    const escalatedStepRun = toStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
+    const escalatedStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
       completedAt: new Date().toISOString(),
       status: STEP_STATUSES.escalated,
     }));
@@ -617,6 +777,29 @@
   }
 }
 
+function registerDataHandler(
+  ctx: PluginContext,
+  key: string,
+  handler: (params: Record<string, unknown>) => Promise<unknown>,
+): void {
+  const dataClient = ctx.data as {
+    handle?: (handlerKey: string, handlerFn: (params: Record<string, unknown>) => Promise<unknown>) => void;
+    register?: (handlerKey: string, handlerFn: (params: Record<string, unknown>) => Promise<unknown>) => void;
+  };
+
+  if (typeof dataClient.handle === "function") {
+    dataClient.handle(key, handler);
+    return;
+  }
+
+  if (typeof dataClient.register === "function") {
+    dataClient.register(key, handler);
+    return;
+  }
+
+  throw new Error("Plugin data client does not support handler registration");
+}
+
 const plugin = definePlugin({
   async setup(ctx: PluginContext) {
     ctx.events.on("issue.updated", async (event: PluginEvent) => {
@@ -638,7 +821,7 @@
           return;
         }
 
-        const stepRun = toStepRunRecord(stepRunRecord);
+        const stepRun = toWorkflowStepRunRecord(stepRunRecord);
         const payload = event.payload as { status?: string };
         const issueStatus = typeof payload.status === "string" ? payload.status : undefined;
 
@@ -657,7 +840,7 @@
           return;
         }
 
-        const updatedStepRun = toStepRunRecord(await updateStepRun(ctx, stepRun.id, {
+        const updatedStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, stepRun.id, {
           completedAt: nextStepStatus === STEP_STATUSES.done ? new Date().toISOString() : undefined,
           startedAt: nextStepStatus === STEP_STATUSES.inProgress
             ? stepRun.data.startedAt ?? new Date().toISOString()
@@ -687,11 +870,21 @@
 
     ctx.events.on("issue.created", async (event: PluginEvent) => {
       try {
+        const idempotencyKey = buildIdempotencyKey(event);
+        if (await checkIdempotency(ctx, idempotencyKey, event.companyId)) {
+          return;
+        }
+
         const payload = event.payload as {
           id?: string;
           parentId?: string;
           assigneeAgentId?: string;
         };
+        const issueId = typeof event.entityId === "string" && event.entityId.trim()
+          ? event.entityId.trim()
+          : typeof payload.id === "string" && payload.id.trim()
+            ? payload.id.trim()
+            : "";
 
         const parentId = typeof payload.parentId === "string" && payload.parentId.trim()
           ? payload.parentId.trim()
@@ -700,12 +893,34 @@
           ? payload.assigneeAgentId.trim()
           : "";
 
-        if (parentId || !assigneeAgentId) {
+        if (!issueId || parentId || !assigneeAgentId) {
+          await markIdempotency(ctx, idempotencyKey, event.companyId);
+          return;
+        }
+
+        const agent = await ctx.agents.get(assigneeAgentId, event.companyId);
+        const metadata = agent?.metadata as Record<string, unknown> | undefined;
+        const defaultParentIssueId = typeof metadata?.defaultParentIssueId === "string" && metadata.defaultParentIssueId.trim()
+          ? metadata.defaultParentIssueId.trim()
+          : "";
+
+        if (!defaultParentIssueId) {
+          await markIdempotency(ctx, idempotencyKey, event.companyId);
           return;
         }
 
-        ctx.logger.info("parentId filler: issue created without parent", {
-          issueId: event.entityId ?? payload.id ?? null,
+        await ctx.issues.update(
+          issueId,
+          { parentId: defaultParentIssueId } as IssueUpdatePatch,
+          event.companyId,
+        );
+        await markIdempotency(ctx, idempotencyKey, event.companyId);
+
+        ctx.logger.info("parentId filler: populated issue parentId from agent metadata", {
+          assigneeAgentId,
+          companyId: event.companyId,
+          issueId,
+          parentId: defaultParentIssueId,
         });
       } catch (error) {
         ctx.logger.warn("Failed to handle issue.created event", {
@@ -744,7 +959,23 @@
       await runReconciler(ctx);
     });
 
-    ctx.data.register("workflow-overview", async (params: Record<string, unknown>) => {
+    registerDataHandler(ctx, "start-workflow", async (params: Record<string, unknown>) => {
+      const workflowId = typeof params.workflowId === "string" ? params.workflowId.trim() : "";
+      const companyId = typeof params.companyId === "string" ? params.companyId.trim() : "";
+      const parentIssueId = typeof params.parentIssueId === "string" ? params.parentIssueId.trim() : "";
+      const createParentIssue = params.createParentIssue === true;
+
+      if (!workflowId || !companyId) {
+        throw new Error("start-workflow requires workflowId and companyId");
+      }
+
+      return await startWorkflow(ctx, workflowId, companyId, {
+        createParentIssue,
+        parentIssueId: parentIssueId || undefined,
+      });
+    });
+
+    registerDataHandler(ctx, "workflow-overview", async (params: Record<string, unknown>) => {
       try {
         const companyId = typeof params.companyId === "string" ? params.companyId.trim() : "";
         if (!companyId) {
@@ -782,7 +1013,7 @@
       }
     });
 
-    ctx.data.register("workflow-run-detail", async (params: Record<string, unknown>) => {
+    registerDataHandler(ctx, "workflow-run-detail", async (params: Record<string, unknown>) => {
       try {
         const runId = typeof params.runId === "string" ? params.runId.trim() : "";
         if (!runId) {
@@ -807,7 +1038,7 @@
             ...typedWorkflowRun.data,
           },
           stepRuns: stepRuns.map((record: PluginEntityRecord) => {
-            const stepRun = toStepRunRecord(record);
+            const stepRun = toWorkflowStepRunRecord(record);
             return {
               id: stepRun.id,
               status: stepRun.status,
diff --git a/paperclip-addon/plugins/workflow-engine/src/workflow-store.ts b/paperclip-addon/plugins/workflow-engine/src/workflow-store.ts
index 5871a2082258cebcfe8b69ebafbb0814cf303d50..0f6dba89ca559d4f5821749b25241d5a3be3e85d
--- a/paperclip-addon/plugins/workflow-engine/src/workflow-store.ts
+++ b/paperclip-addon/plugins/workflow-engine/src/workflow-store.ts
@@ -44,7 +44,14 @@
 
 type TypedEntityRecord<T> = Omit<PluginEntityRecord, "data"> & { data: T };
 
-function toTypedRecord<T>(record: PluginEntityRecord): TypedEntityRecord<T> {
+function toTypedRecord<T>(
+  record: PluginEntityRecord,
+  entityType: string,
+): TypedEntityRecord<T> {
+  if (record.entityType !== entityType) {
+    throw new Error(`Expected entity type "${entityType}", got "${record.entityType}"`);
+  }
+
   return record as TypedEntityRecord<T>;
 }
 
@@ -67,13 +74,31 @@
   id: string,
   entityType: string,
 ): Promise<TypedEntityRecord<T> | null> {
-  const record = await ctx.entities.get(id);
+  const listQuery = {
+    entityType,
+    id,
+    limit: 1,
+  } as Record<string, unknown>;
 
-  if (!record || record.entityType !== entityType) {
+  const listed = await ctx.entities.list(
+    listQuery as Parameters<PluginContext["entities"]["list"]>[0],
+  );
+  const listedRecord = listed.find(
+    (record: PluginEntityRecord) => record.id === id,
+  ) ?? null;
+
+  if (listedRecord) {
+    return toTypedRecord<T>(listedRecord, entityType);
+  };
+
+  // Fallback: some host SDK variants ignore `id` on entities.list().
+  // Keep defensive type verification on direct get() until id-filter support is universal.
+  const record = await ctx.entities.get(id);
+  if (!record) {
     return null;
   }
 
-  return toTypedRecord<T>(record);
+  return toTypedRecord<T>(record, entityType);
 }
 
 async function requireEntityByType<T>(
diff --git a/paperclip-addon/plugins/workflow-engine/src/workflow-utils.ts b/paperclip-addon/plugins/workflow-engine/src/workflow-utils.ts
new file mode 100644
index 0000000000000000000000000000000000000000..ec1cf6f6b9aa722b126b58c0a728b4e538ad1494
--- /dev/null
+++ b/paperclip-addon/plugins/workflow-engine/src/workflow-utils.ts
@@ -0,0 +1,62 @@
+import type { PluginEntityRecord } from "@paperclipai/plugin-sdk";
+
+import { STEP_STATUSES } from "./constants.js";
+import type { WorkflowStep } from "./dag-engine.js";
+import type {
+  WorkflowDefinition,
+  WorkflowRun,
+  WorkflowStepRun,
+} from "./workflow-store.js";
+
+export type WorkflowRunRecord = PluginEntityRecord & { data: WorkflowRun };
+export type WorkflowStepRunRecord = PluginEntityRecord & { data: WorkflowStepRun };
+export type WorkflowDefinitionRecord = PluginEntityRecord & { data: WorkflowDefinition };
+type StepAgentMetadata = WorkflowStep & {
+  agent?: string;
+  agentName?: string;
+  assigneeAgentName?: string;
+};
+
+export const TERMINAL_STEP_STATUSES = new Set<string>([
+  STEP_STATUSES.done,
+  STEP_STATUSES.failed,
+  STEP_STATUSES.skipped,
+  STEP_STATUSES.escalated,
+]);
+
+export function toWorkflowRunRecord(record: PluginEntityRecord): WorkflowRunRecord {
+  return record as WorkflowRunRecord;
+}
+
+export function toWorkflowStepRunRecord(record: PluginEntityRecord): WorkflowStepRunRecord {
+  return record as WorkflowStepRunRecord;
+}
+
+export function toWorkflowDefinitionRecord(record: PluginEntityRecord): WorkflowDefinitionRecord {
+  return record as WorkflowDefinitionRecord;
+}
+
+export function findStepDefinition(definition: WorkflowDefinitionRecord, stepId: string): WorkflowStep | null {
+  return definition.data.steps.find((step: WorkflowStep) => step.id === stepId) ?? null;
+}
+
+export function getStepAgentNameHint(stepDef: WorkflowStep): string | null {
+  const stepMeta = stepDef as StepAgentMetadata;
+  const candidates = [stepMeta.agentName, stepMeta.agent, stepMeta.assigneeAgentName];
+
+  for (const candidate of candidates) {
+    if (typeof candidate === "string" && candidate.trim()) {
+      return candidate.trim();
+    }
+  }
+
+  return null;
+}
+
+export function getStepAgentName(stepRun: WorkflowStepRunRecord, stepDef: WorkflowStep): string | null {
+  if (typeof stepRun.data.agentName === "string" && stepRun.data.agentName.trim()) {
+    return stepRun.data.agentName.trim();
+  }
+
+  return getStepAgentNameHint(stepDef);
+}

exec
/bin/zsh -lc "sed -n '60,110p' plugins/workflow-engine/src/workflow-store.ts" in /Users/kwak/Projects/paperclip/paperclip-addon succeeded in 0ms:
}

function mergeEntityData<T extends object>(
  record: PluginEntityRecord,
  updates: Partial<T>,
): T {
  return {
    ...(record.data as T),
    ...updates,
  };
}

async function getEntityByType<T>(
  ctx: PluginContext,
  id: string,
  entityType: string,
): Promise<TypedEntityRecord<T> | null> {
  const listQuery = {
    entityType,
    id,
file update:
diff --git a/paperclip-addon/plugins/workflow-engine/src/dag-engine.ts b/paperclip-addon/plugins/workflow-engine/src/dag-engine.ts
index b59510f0473171038be77277286d3b5c2063e6c5..e7f21ff998cab78b9325a8aeacf4fda9c5e06553
--- a/paperclip-addon/plugins/workflow-engine/src/dag-engine.ts
+++ b/paperclip-addon/plugins/workflow-engine/src/dag-engine.ts
@@ -2,6 +2,7 @@
   id: string;
   title: string;
   dependsOn: string[];
+  sessionMode?: "fresh" | "reuse";
   onFailure?: "retry" | "skip" | "abort_workflow" | "escalate";
   escalateTo?: string;
   maxRetries?: number;
diff --git a/paperclip-addon/plugins/workflow-engine/src/reconciler.ts b/paperclip-addon/plugins/workflow-engine/src/reconciler.ts
index 14ab4e766a835d145a6c2e7b1b564270285cbdc3..d2ab54817bd8185944379d2188bfbdef1722020a
--- a/paperclip-addon/plugins/workflow-engine/src/reconciler.ts
+++ b/paperclip-addon/plugins/workflow-engine/src/reconciler.ts
@@ -8,65 +8,22 @@
   listStepRuns,
   updateStepRun,
   updateWorkflowRun,
-  type WorkflowDefinition,
-  type WorkflowRun,
-  type WorkflowStepRun,
 } from "./workflow-store.js";
 import type { WorkflowStep } from "./dag-engine.js";
-
-type WorkflowRunRecord = PluginEntityRecord & { data: WorkflowRun };
-type WorkflowStepRunRecord = PluginEntityRecord & { data: WorkflowStepRun };
-type WorkflowDefinitionRecord = PluginEntityRecord & { data: WorkflowDefinition };
-type StepAgentMetadata = WorkflowStep & {
-  agent?: string;
-  agentName?: string;
-  assigneeAgentName?: string;
-};
+import {
+  TERMINAL_STEP_STATUSES,
+  findStepDefinition,
+  getStepAgentName,
+  toWorkflowDefinitionRecord,
+  toWorkflowRunRecord,
+  toWorkflowStepRunRecord,
+} from "./workflow-utils.js";
 
 const STALE_THRESHOLD_MS = 300_000;
-const TERMINAL_STEP_STATUSES = new Set<string>([
-  STEP_STATUSES.done,
-  STEP_STATUSES.failed,
-  STEP_STATUSES.skipped,
-  STEP_STATUSES.escalated,
-]);
 
 function isStale(record: PluginEntityRecord, thresholdMs: number): boolean {
   const updatedAt = new Date(record.updatedAt).getTime();
   return Date.now() - updatedAt > thresholdMs;
-}
-
-function toWorkflowRunRecord(record: PluginEntityRecord): WorkflowRunRecord {
-  return record as WorkflowRunRecord;
-}
-
-function toWorkflowStepRunRecord(record: PluginEntityRecord): WorkflowStepRunRecord {
-  return record as WorkflowStepRunRecord;
-}
-
-function toWorkflowDefinitionRecord(record: PluginEntityRecord): WorkflowDefinitionRecord {
-  return record as WorkflowDefinitionRecord;
-}
-
-function findStepDefinition(definition: WorkflowDefinitionRecord, stepId: string): WorkflowStep | null {
-  return definition.data.steps.find((step: WorkflowStep) => step.id === stepId) ?? null;
-}
-
-function getStepAgentName(stepRun: WorkflowStepRunRecord, stepDef: WorkflowStep): string | null {
-  if (typeof stepRun.data.agentName === "string" && stepRun.data.agentName.trim()) {
-    return stepRun.data.agentName.trim();
-  }
-
-  const stepMeta = stepDef as StepAgentMetadata;
-  const candidates = [stepMeta.agentName, stepMeta.agent, stepMeta.assigneeAgentName];
-
-  for (const candidate of candidates) {
-    if (typeof candidate === "string" && candidate.trim()) {
-      return candidate.trim();
-    }
-  }
-
-  return null;
 }
 
 async function invokeAgentByName(
diff --git a/paperclip-addon/plugins/workflow-engine/src/worker.ts b/paperclip-addon/plugins/workflow-engine/src/worker.ts
index c5ac3fbb4ca7f767eb29331bd3630b635449b335..303e35958890fa1f66db3f2b0ff1fa1f134102a8
--- a/paperclip-addon/plugins/workflow-engine/src/worker.ts
+++ b/paperclip-addon/plugins/workflow-engine/src/worker.ts
@@ -15,6 +15,7 @@
 } from "./dag-engine.js";
 import {
   checkIdempotency,
+  createWorkflowRun,
   createStepRun,
   findStepRunByIssueId,
   getWorkflowDefinition,
@@ -25,51 +26,38 @@
   markIdempotency,
   updateStepRun,
   updateWorkflowRun,
-  type WorkflowDefinition,
-  type WorkflowRun,
   type WorkflowStepRun,
 } from "./workflow-store.js";
+import {
+  TERMINAL_STEP_STATUSES,
+  findStepDefinition,
+  getStepAgentName,
+  getStepAgentNameHint,
+  toWorkflowDefinitionRecord,
+  toWorkflowRunRecord,
+  toWorkflowStepRunRecord,
+  type WorkflowDefinitionRecord,
+  type WorkflowRunRecord,
+  type WorkflowStepRunRecord,
+} from "./workflow-utils.js";
 
-type WorkflowStepRecord = PluginEntityRecord & { data: WorkflowStepRun };
-type WorkflowRunRecord = PluginEntityRecord & { data: WorkflowRun };
-type WorkflowDefinitionRecord = PluginEntityRecord & { data: WorkflowDefinition };
 type WorkflowStepMetadata = WorkflowStep & {
   description?: string;
-  agent?: string;
-  agentName?: string;
-  assigneeAgentName?: string;
   assigneeAgentId?: string;
+  sessionMode?: "fresh" | "reuse";
 };
 type AgentRecord = Awaited<ReturnType<PluginContext["agents"]["list"]>>[number];
+type IssueUpdatePatch = Parameters<PluginContext["issues"]["update"]>[1];
 type ReconcilerModule = {
   reconcileStuckSteps?: (ctx: PluginContext) => Promise<void>;
 };
 
-const TERMINAL_STEP_STATUSES = new Set<string>([
-  STEP_STATUSES.done,
-  STEP_STATUSES.failed,
-  STEP_STATUSES.skipped,
-  STEP_STATUSES.escalated,
-]);
-
 function summarizeError(error: unknown): string {
   if (error instanceof Error) {
     return error.message;
   }
 
   return String(error);
-}
-
-function toStepRunRecord(record: PluginEntityRecord): WorkflowStepRecord {
-  return record as WorkflowStepRecord;
-}
-
-function toWorkflowRunRecord(record: PluginEntityRecord): WorkflowRunRecord {
-  return record as WorkflowRunRecord;
-}
-
-function toWorkflowDefinitionRecord(record: PluginEntityRecord): WorkflowDefinitionRecord {
-  return record as WorkflowDefinitionRecord;
 }
 
 function buildIdempotencyKey(event: PluginEvent): string {
@@ -78,10 +66,6 @@
 
 function getStepMetadata(stepDef: WorkflowStep): WorkflowStepMetadata {
   return stepDef as WorkflowStepMetadata;
-}
-
-function findStepDefinition(definition: WorkflowDefinitionRecord, stepId: string): WorkflowStep | null {
-  return definition.data.steps.find((step: WorkflowStep) => step.id === stepId) ?? null;
 }
 
 function getStepDescription(stepDef: WorkflowStep): string | undefined {
@@ -89,19 +73,6 @@
   return typeof description === "string" && description.trim() ? description.trim() : undefined;
 }
 
-function getStepAgentNameHint(stepDef: WorkflowStep): string | null {
-  const meta = getStepMetadata(stepDef);
-  const candidates = [meta.agentName, meta.agent, meta.assigneeAgentName];
-
-  for (const candidate of candidates) {
-    if (typeof candidate === "string" && candidate.trim()) {
-      return candidate.trim();
-    }
-  }
-
-  return null;
-}
-
 function getStepAgentIdHint(stepDef: WorkflowStep): string | null {
   const assigneeAgentId = getStepMetadata(stepDef).assigneeAgentId;
   if (typeof assigneeAgentId !== "string" || !assigneeAgentId.trim()) {
@@ -147,68 +118,256 @@
 
 async function invokeAgentForStep(
   ctx: PluginContext,
+  stepRunRecord: WorkflowStepRunRecord,
   stepDef: WorkflowStep,
-  agentName: string,
   workflowName: string,
   companyId: string,
-): Promise<void> {
+): Promise<WorkflowStepRunRecord> {
+  const agentName = getStepAgentName(stepRunRecord, stepDef);
+  if (!agentName) {
+    ctx.logger.warn("Agent name is missing for workflow step", {
+      companyId,
+      runId: stepRunRecord.data.runId,
+      stepId: stepDef.id,
+      workflowName,
+    });
+    return stepRunRecord;
+  }
+
   const agents = await ctx.agents.list({ companyId });
-  const agent = agents.find((candidate: AgentRecord) => candidate.name === agentName);
+  const agent = agents.find((candidate: AgentRecord) => candidate.name === agentName) ?? null;
 
   if (!agent) {
     ctx.logger.warn("Agent not found for step", {
       agentName,
       companyId,
+      runId: stepRunRecord.data.runId,
+      stepId: stepDef.id,
+      workflowName,
+    });
+    return stepRunRecord;
+  }
+
+  const prompt = `workflow:${workflowName}/step:${stepDef.id} — "${stepDef.title}" is ready. Please proceed with the assigned task.`;
+  const reason = `workflow:${workflowName}/step:${stepDef.id}`;
+  const sessionMode = getStepMetadata(stepDef).sessionMode === "reuse" ? "reuse" : "fresh";
+
+  if (sessionMode === "reuse") {
+    let sessionId = typeof stepRunRecord.data.sessionId === "string" && stepRunRecord.data.sessionId.trim()
+      ? stepRunRecord.data.sessionId.trim()
+      : "";
+
+    if (!sessionId) {
+      const session = await ctx.agents.sessions.create(agent.id, companyId, {
+        reason,
+      });
+      sessionId = session.sessionId;
+      stepRunRecord = toWorkflowStepRunRecord(await updateStepRun(ctx, stepRunRecord.id, {
+        sessionId,
+      }));
+    }
+
+    await ctx.agents.sessions.sendMessage(sessionId, companyId, {
+      prompt,
+      reason,
+    });
+
+    ctx.logger.info("Sent workflow step prompt via agent session", {
+      agentId: agent.id,
+      agentName,
+      companyId,
+      runId: stepRunRecord.data.runId,
+      sessionId,
       stepId: stepDef.id,
       workflowName,
     });
-    return;
+    return stepRunRecord;
   }
 
   await ctx.agents.invoke(agent.id, companyId, {
-    prompt: `workflow:${workflowName}/step:${stepDef.id} — "${stepDef.title}" is ready. Please proceed with the assigned task.`,
-    reason: `workflow:${workflowName}/step:${stepDef.id}`,
+    prompt,
+    reason,
   });
 
   ctx.logger.info("Invoked agent for workflow step", {
     agentId: agent.id,
     agentName,
     companyId,
+    runId: stepRunRecord.data.runId,
     stepId: stepDef.id,
     workflowName,
   });
+
+  return stepRunRecord;
 }
 
 async function activateBacklogStep(
   ctx: PluginContext,
-  stepRunRecord: WorkflowStepRecord,
+  stepRunRecord: WorkflowStepRunRecord,
   stepDef: WorkflowStep,
   workflowName: string,
   companyId: string,
-): Promise<void> {
+  options?: { parentIssueId?: string },
+): Promise<WorkflowStepRunRecord> {
   if (stepRunRecord.data.status !== STEP_STATUSES.backlog) {
-    return;
+    return stepRunRecord;
   }
 
-  const updated = toStepRunRecord(await updateStepRun(ctx, stepRunRecord.id, {
+  const resolvedAgent = await resolveStepAgent(
+    ctx,
+    companyId,
+    stepDef,
+    stepRunRecord.data.agentName,
+  );
+
+  let issueId = typeof stepRunRecord.data.issueId === "string" && stepRunRecord.data.issueId.trim()
+    ? stepRunRecord.data.issueId.trim()
+    : "";
+  const stepDescription = getStepDescription(stepDef) ?? `Workflow step: ${stepDef.id}`;
+
+  if (!issueId) {
+    const issue = await ctx.issues.create({
+      assigneeAgentId: resolvedAgent.agentId ?? undefined,
+      companyId,
+      description: stepDescription,
+      parentId: options?.parentIssueId,
+      title: `[${workflowName}] ${stepDef.title}`,
+    });
+    await ctx.issues.update(issue.id, { status: "todo" } as IssueUpdatePatch, companyId);
+    issueId = issue.id;
+  }
+
+  const nextStartedAt = stepRunRecord.data.startedAt ?? new Date().toISOString();
+  let updatedStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, stepRunRecord.id, {
+    agentName: resolvedAgent.agentName ?? stepRunRecord.data.agentName,
+    issueId,
+    startedAt: nextStartedAt,
     status: STEP_STATUSES.todo,
   }));
 
-  await invokeAgentForStep(
+  updatedStepRun = await invokeAgentForStep(
     ctx,
+    updatedStepRun,
     stepDef,
-    updated.data.agentName,
     workflowName,
     companyId,
   );
+
+  return updatedStepRun;
 }
 
+async function startWorkflow(
+  ctx: PluginContext,
+  workflowId: string,
+  companyId: string,
+  options?: {
+    createParentIssue?: boolean;
+    parentIssueId?: string;
+  },
+): Promise<{
+  activatedStepIds: string[];
+  parentIssueId: string | null;
+  runId: string;
+  workflowId: string;
+}> {
+  const workflowDefinition = await getWorkflowDefinition(ctx, workflowId);
+  if (!workflowDefinition) {
+    throw new Error(`Workflow definition not found: ${workflowId}`);
+  }
+
+  const typedWorkflowDefinition = toWorkflowDefinitionRecord(workflowDefinition);
+  if (typedWorkflowDefinition.data.companyId !== companyId) {
+    throw new Error(`Workflow does not belong to company: ${workflowId}`);
+  }
+
+  let parentIssueId = typeof options?.parentIssueId === "string" && options.parentIssueId.trim()
+    ? options.parentIssueId.trim()
+    : "";
+  if (!parentIssueId && options?.createParentIssue) {
+    const parentIssue = await ctx.issues.create({
+      companyId,
+      description: typedWorkflowDefinition.data.description || `Workflow run: ${typedWorkflowDefinition.data.name}`,
+      title: `[${typedWorkflowDefinition.data.name}] Workflow run`,
+    });
+    parentIssueId = parentIssue.id;
+  }
+
+  const workflowRun = toWorkflowRunRecord(await createWorkflowRun(ctx, {
+    companyId,
+    parentIssueId: parentIssueId || undefined,
+    startedAt: new Date().toISOString(),
+    status: RUN_STATUSES.running,
+    workflowId: typedWorkflowDefinition.id,
+    workflowName: typedWorkflowDefinition.data.name,
+  }));
+
+  const agents = await ctx.agents.list({ companyId });
+  const agentsByName = new Map<string, AgentRecord>();
+  for (const agent of agents) {
+    agentsByName.set(agent.name, agent);
+  }
+
+  const pendingRootSteps: Array<{ stepDef: WorkflowStep; stepRun: WorkflowStepRunRecord }> = [];
+  for (const stepDef of typedWorkflowDefinition.data.steps) {
+    const agentNameHint = getStepAgentNameHint(stepDef);
+    const matchedAgent = agentNameHint ? agentsByName.get(agentNameHint) ?? null : null;
+    const resolvedAgent = matchedAgent
+      ? { agentId: matchedAgent.id, agentName: matchedAgent.name }
+      : await resolveStepAgent(ctx, companyId, stepDef, agentNameHint ?? undefined);
+
+    if (!resolvedAgent.agentName) {
+      throw new Error(`Unable to resolve step assignee for "${stepDef.id}"`);
+    }
+
+    const stepRun = toWorkflowStepRunRecord(await createStepRun(ctx, companyId, {
+      agentName: resolvedAgent.agentName,
+      retryCount: 0,
+      runId: workflowRun.id,
+      status: STEP_STATUSES.backlog,
+      stepId: stepDef.id,
+    }));
+
+    if (stepDef.dependsOn.length === 0 && stepDef.triggerOn !== "escalation") {
+      pendingRootSteps.push({ stepDef, stepRun });
+    }
+  }
+
+  const activatedStepIds: string[] = [];
+  for (const pending of pendingRootSteps) {
+    await activateBacklogStep(
+      ctx,
+      pending.stepRun,
+      pending.stepDef,
+      typedWorkflowDefinition.data.name,
+      companyId,
+      { parentIssueId: parentIssueId || undefined },
+    );
+    activatedStepIds.push(pending.stepDef.id);
+  }
+
+  ctx.logger.info("Started workflow run", {
+    activatedStepIds,
+    companyId,
+    parentIssueId: parentIssueId || null,
+    runId: workflowRun.id,
+    workflowId: typedWorkflowDefinition.id,
+    workflowName: typedWorkflowDefinition.data.name,
+  });
+
+  return {
+    activatedStepIds,
+    parentIssueId: parentIssueId || null,
+    runId: workflowRun.id,
+    workflowId: typedWorkflowDefinition.id,
+  };
+}
+
 async function advanceWorkflow(
   ctx: PluginContext,
   stepRunRecord: PluginEntityRecord,
   companyId: string,
 ): Promise<void> {
-  const completedStepRun = toStepRunRecord(stepRunRecord);
+  const completedStepRun = toWorkflowStepRunRecord(stepRunRecord);
   const workflowRun = await getWorkflowRun(ctx, completedStepRun.data.runId);
 
   if (!workflowRun) {
@@ -237,7 +396,7 @@
   }
 
   const typedWorkflowDefinition = toWorkflowDefinitionRecord(workflowDefinition);
-  const stepRuns = (await listStepRuns(ctx, typedWorkflowRun.id, companyId)).map(toStepRunRecord);
+  const stepRuns = (await listStepRuns(ctx, typedWorkflowRun.id, companyId)).map(toWorkflowStepRunRecord);
   const completed = new Set<string>();
   const failed = new Set<string>();
   const skipped = new Set<string>();
@@ -289,7 +448,7 @@
         continue;
       }
 
-      stepRun = toStepRunRecord(await createStepRun(ctx, companyId, {
+      stepRun = toWorkflowStepRunRecord(await createStepRun(ctx, companyId, {
         agentName: resolvedAgent.agentName,
         retryCount: 0,
         runId: typedWorkflowRun.id,
@@ -305,6 +464,7 @@
       stepDef,
       typedWorkflowRun.data.workflowName,
       companyId,
+      { parentIssueId: typedWorkflowRun.data.parentIssueId },
     );
   }
 
@@ -329,7 +489,7 @@
   ctx: PluginContext,
   workflowRun: WorkflowRunRecord,
   workflowDefinition: WorkflowDefinitionRecord,
-  sourceStepRun: WorkflowStepRecord,
+  sourceStepRun: WorkflowStepRunRecord,
   escalationTargetId: string,
   companyId: string,
 ): Promise<void> {
@@ -338,7 +498,7 @@
     throw new Error(`Escalation target step not found: ${escalationTargetId}`);
   }
 
-  const stepRuns = (await listStepRuns(ctx, workflowRun.id, companyId)).map(toStepRunRecord);
+  const stepRuns = (await listStepRuns(ctx, workflowRun.id, companyId)).map(toWorkflowStepRunRecord);
   let escalationStepRun = stepRuns.find((candidate) => candidate.data.stepId === escalationTargetId) ?? null;
   const resolvedAgent = await resolveStepAgent(
     ctx,
@@ -352,7 +512,7 @@
   }
 
   if (!escalationStepRun) {
-    escalationStepRun = toStepRunRecord(await createStepRun(ctx, companyId, {
+    escalationStepRun = toWorkflowStepRunRecord(await createStepRun(ctx, companyId, {
       agentName: resolvedAgent.agentName,
       retryCount: 0,
       runId: workflowRun.id,
@@ -374,14 +534,14 @@
       title: `${workflowRun.data.workflowName}: ${escalationStep.title}`,
     });
 
-    escalationStepRun = toStepRunRecord(await updateStepRun(ctx, escalationStepRun.id, {
+    escalationStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, escalationStepRun.id, {
       issueId: issue.id,
     }));
   }
 
   const shouldActivate = escalationStepRun.data.status === STEP_STATUSES.backlog;
   if (shouldActivate) {
-    escalationStepRun = toStepRunRecord(await updateStepRun(ctx, escalationStepRun.id, {
+    escalationStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, escalationStepRun.id, {
       status: STEP_STATUSES.todo,
     }));
   }
@@ -389,8 +549,8 @@
   if (shouldActivate) {
     await invokeAgentForStep(
       ctx,
+      escalationStepRun,
       escalationStep,
-      escalationStepRun.data.agentName,
       workflowRun.data.workflowName,
       companyId,
     );
@@ -421,7 +581,7 @@
     return;
   }
 
-  const typedStepRun = toStepRunRecord(stepRunRecord);
+  const typedStepRun = toWorkflowStepRunRecord(stepRunRecord);
   if (TERMINAL_STEP_STATUSES.has(typedStepRun.data.status)) {
     return;
   }
@@ -469,7 +629,7 @@
   if (options.allowRetry) {
     const retryInfo = getRetryInfo(typedWorkflowDefinition.data.steps, typedStepRun.data.stepId);
     if (retryInfo.shouldRetry && typedStepRun.data.retryCount < retryInfo.maxRetries) {
-      const retriedStepRun = toStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
+      const retriedStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
         completedAt: undefined,
         retryCount: typedStepRun.data.retryCount + 1,
         status: STEP_STATUSES.todo,
@@ -477,8 +637,8 @@
 
       await invokeAgentForStep(
         ctx,
+        retriedStepRun,
         stepDef,
-        retriedStepRun.data.agentName,
         typedWorkflowRun.data.workflowName,
         event.companyId,
       );
@@ -496,7 +656,7 @@
   }
 
   if (stepDef.onFailure === "skip") {
-    const skippedStepRun = toStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
+    const skippedStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
       completedAt: new Date().toISOString(),
       status: STEP_STATUSES.skipped,
     }));
@@ -556,7 +716,7 @@
       return;
     }
 
-    const escalatedStepRun = toStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
+    const escalatedStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
       completedAt: new Date().toISOString(),
       status: STEP_STATUSES.escalated,
     }));
@@ -617,6 +777,29 @@
   }
 }
 
+function registerDataHandler(
+  ctx: PluginContext,
+  key: string,
+  handler: (params: Record<string, unknown>) => Promise<unknown>,
+): void {
+  const dataClient = ctx.data as {
+    handle?: (handlerKey: string, handlerFn: (params: Record<string, unknown>) => Promise<unknown>) => void;
+    register?: (handlerKey: string, handlerFn: (params: Record<string, unknown>) => Promise<unknown>) => void;
+  };
+
+  if (typeof dataClient.handle === "function") {
+    dataClient.handle(key, handler);
+    return;
+  }
+
+  if (typeof dataClient.register === "function") {
+    dataClient.register(key, handler);
+    return;
+  }
+
+  throw new Error("Plugin data client does not support handler registration");
+}
+
 const plugin = definePlugin({
   async setup(ctx: PluginContext) {
     ctx.events.on("issue.updated", async (event: PluginEvent) => {
@@ -638,7 +821,7 @@
           return;
         }
 
-        const stepRun = toStepRunRecord(stepRunRecord);
+        const stepRun = toWorkflowStepRunRecord(stepRunRecord);
         const payload = event.payload as { status?: string };
         const issueStatus = typeof payload.status === "string" ? payload.status : undefined;
 
@@ -657,7 +840,7 @@
           return;
         }
 
-        const updatedStepRun = toStepRunRecord(await updateStepRun(ctx, stepRun.id, {
+        const updatedStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, stepRun.id, {
           completedAt: nextStepStatus === STEP_STATUSES.done ? new Date().toISOString() : undefined,
           startedAt: nextStepStatus === STEP_STATUSES.inProgress
             ? stepRun.data.startedAt ?? new Date().toISOString()
@@ -687,11 +870,21 @@
 
     ctx.events.on("issue.created", async (event: PluginEvent) => {
       try {
+        const idempotencyKey = buildIdempotencyKey(event);
+        if (await checkIdempotency(ctx, idempotencyKey, event.companyId)) {
+          return;
+        }
+
         const payload = event.payload as {
           id?: string;
           parentId?: string;
           assigneeAgentId?: string;
         };
+        const issueId = typeof event.entityId === "string" && event.entityId.trim()
+          ? event.entityId.trim()
+          : typeof payload.id === "string" && payload.id.trim()
+            ? payload.id.trim()
+            : "";
 
         const parentId = typeof payload.parentId === "string" && payload.parentId.trim()
           ? payload.parentId.trim()
@@ -700,12 +893,34 @@
           ? payload.assigneeAgentId.trim()
           : "";
 
-        if (parentId || !assigneeAgentId) {
+        if (!issueId || parentId || !assigneeAgentId) {
+          await markIdempotency(ctx, idempotencyKey, event.companyId);
+          return;
+        }
+
+        const agent = await ctx.agents.get(assigneeAgentId, event.companyId);
+        const metadata = agent?.metadata as Record<string, unknown> | undefined;
+        const defaultParentIssueId = typeof metadata?.defaultParentIssueId === "string" && metadata.defaultParentIssueId.trim()
+          ? metadata.defaultParentIssueId.trim()
+          : "";
+
+        if (!defaultParentIssueId) {
+          await markIdempotency(ctx, idempotencyKey, event.companyId);
           return;
         }
 
-        ctx.logger.info("parentId filler: issue created without parent", {
-          issueId: event.entityId ?? payload.id ?? null,
+        await ctx.issues.update(
+          issueId,
+          { parentId: defaultParentIssueId } as IssueUpdatePatch,
+          event.companyId,
+        );
+        await markIdempotency(ctx, idempotencyKey, event.companyId);
+
+        ctx.logger.info("parentId filler: populated issue parentId from agent metadata", {
+          assigneeAgentId,
+          companyId: event.companyId,
+          issueId,
+          parentId: defaultParentIssueId,
         });
       } catch (error) {
         ctx.logger.warn("Failed to handle issue.created event", {
@@ -744,7 +959,23 @@
       await runReconciler(ctx);
     });
 
-    ctx.data.register("workflow-overview", async (params: Record<string, unknown>) => {
+    registerDataHandler(ctx, "start-workflow", async (params: Record<string, unknown>) => {
+      const workflowId = typeof params.workflowId === "string" ? params.workflowId.trim() : "";
+      const companyId = typeof params.companyId === "string" ? params.companyId.trim() : "";
+      const parentIssueId = typeof params.parentIssueId === "string" ? params.parentIssueId.trim() : "";
+      const createParentIssue = params.createParentIssue === true;
+
+      if (!workflowId || !companyId) {
+        throw new Error("start-workflow requires workflowId and companyId");
+      }
+
+      return await startWorkflow(ctx, workflowId, companyId, {
+        createParentIssue,
+        parentIssueId: parentIssueId || undefined,
+      });
+    });
+
+    registerDataHandler(ctx, "workflow-overview", async (params: Record<string, unknown>) => {
       try {
         const companyId = typeof params.companyId === "string" ? params.companyId.trim() : "";
         if (!companyId) {
@@ -782,7 +1013,7 @@
       }
     });
 
-    ctx.data.register("workflow-run-detail", async (params: Record<string, unknown>) => {
+    registerDataHandler(ctx, "workflow-run-detail", async (params: Record<string, unknown>) => {
       try {
         const runId = typeof params.runId === "string" ? params.runId.trim() : "";
         if (!runId) {
@@ -807,7 +1038,7 @@
             ...typedWorkflowRun.data,
           },
           stepRuns: stepRuns.map((record: PluginEntityRecord) => {
-            const stepRun = toStepRunRecord(record);
+            const stepRun = toWorkflowStepRunRecord(record);
             return {
               id: stepRun.id,
               status: stepRun.status,
diff --git a/paperclip-addon/plugins/workflow-engine/src/workflow-store.ts b/paperclip-addon/plugins/workflow-engine/src/workflow-store.ts
index 5871a2082258cebcfe8b69ebafbb0814cf303d50..0f6dba89ca559d4f5821749b25241d5a3be3e85d
--- a/paperclip-addon/plugins/workflow-engine/src/workflow-store.ts
+++ b/paperclip-addon/plugins/workflow-engine/src/workflow-store.ts
@@ -44,7 +44,14 @@
 
 type TypedEntityRecord<T> = Omit<PluginEntityRecord, "data"> & { data: T };
 
-function toTypedRecord<T>(record: PluginEntityRecord): TypedEntityRecord<T> {
+function toTypedRecord<T>(
+  record: PluginEntityRecord,
+  entityType: string,
+): TypedEntityRecord<T> {
+  if (record.entityType !== entityType) {
+    throw new Error(`Expected entity type "${entityType}", got "${record.entityType}"`);
+  }
+
   return record as TypedEntityRecord<T>;
 }
 
@@ -67,13 +74,31 @@
   id: string,
   entityType: string,
 ): Promise<TypedEntityRecord<T> | null> {
-  const record = await ctx.entities.get(id);
+  const listQuery = {
+    entityType,
+    id,
+    limit: 1,
+  } as Record<string, unknown>;
 
-  if (!record || record.entityType !== entityType) {
+  const listed = await ctx.entities.list(
+    listQuery as Parameters<PluginContext["entities"]["list"]>[0],
+  );
+  const listedRecord = listed.find(
+    (record: PluginEntityRecord) => record.id === id,
+  ) ?? null;
+
+  if (listedRecord) {
+    return toTypedRecord<T>(listedRecord, entityType);
+  };
+
+  // Fallback: some host SDK variants ignore `id` on entities.list().
+  // Keep defensive type verification on direct get() until id-filter support is universal.
+  const record = await ctx.entities.get(id);
+  if (!record) {
     return null;
   }
 
-  return toTypedRecord<T>(record);
+  return toTypedRecord<T>(record, entityType);
 }
 
 async function requireEntityByType<T>(
diff --git a/paperclip-addon/plugins/workflow-engine/src/workflow-utils.ts b/paperclip-addon/plugins/workflow-engine/src/workflow-utils.ts
new file mode 100644
index 0000000000000000000000000000000000000000..ec1cf6f6b9aa722b126b58c0a728b4e538ad1494
--- /dev/null
+++ b/paperclip-addon/plugins/workflow-engine/src/workflow-utils.ts
@@ -0,0 +1,62 @@
+import type { PluginEntityRecord } from "@paperclipai/plugin-sdk";
+
+import { STEP_STATUSES } from "./constants.js";
+import type { WorkflowStep } from "./dag-engine.js";
+import type {
+  WorkflowDefinition,
+  WorkflowRun,
+  WorkflowStepRun,
+} from "./workflow-store.js";
+
+export type WorkflowRunRecord = PluginEntityRecord & { data: WorkflowRun };
+export type WorkflowStepRunRecord = PluginEntityRecord & { data: WorkflowStepRun };
+export type WorkflowDefinitionRecord = PluginEntityRecord & { data: WorkflowDefinition };
+type StepAgentMetadata = WorkflowStep & {
+  agent?: string;
+  agentName?: string;
+  assigneeAgentName?: string;
+};
+
+export const TERMINAL_STEP_STATUSES = new Set<string>([
+  STEP_STATUSES.done,
+  STEP_STATUSES.failed,
+  STEP_STATUSES.skipped,
+  STEP_STATUSES.escalated,
+]);
+
+export function toWorkflowRunRecord(record: PluginEntityRecord): WorkflowRunRecord {
+  return record as WorkflowRunRecord;
+}
+
+export function toWorkflowStepRunRecord(record: PluginEntityRecord): WorkflowStepRunRecord {
+  return record as WorkflowStepRunRecord;
+}
+
+export function toWorkflowDefinitionRecord(record: PluginEntityRecord): WorkflowDefinitionRecord {
+  return record as WorkflowDefinitionRecord;
+}
+
+export function findStepDefinition(definition: WorkflowDefinitionRecord, stepId: string): WorkflowStep | null {
+  return definition.data.steps.find((step: WorkflowStep) => step.id === stepId) ?? null;
+}
+
+export function getStepAgentNameHint(stepDef: WorkflowStep): string | null {
+  const stepMeta = stepDef as StepAgentMetadata;
+  const candidates = [stepMeta.agentName, stepMeta.agent, stepMeta.assigneeAgentName];
+
+  for (const candidate of candidates) {
+    if (typeof candidate === "string" && candidate.trim()) {
+      return candidate.trim();
+    }
+  }
+
+  return null;
+}
+
+export function getStepAgentName(stepRun: WorkflowStepRunRecord, stepDef: WorkflowStep): string | null {
+  if (typeof stepRun.data.agentName === "string" && stepRun.data.agentName.trim()) {
+    return stepRun.data.agentName.trim();
+  }
+
+  return getStepAgentNameHint(stepDef);
+}

file update
M /Users/kwak/Projects/paperclip/paperclip-addon/plugins/workflow-engine/src/workflow-store.ts
@@ -91,3 +91,3 @@
     return toTypedRecord<T>(listedRecord, entityType);
-  };
+  }
 
apply_patch(auto_approved=true) exited 0 in 118ms:
Success. Updated the following files:
M /Users/kwak/Projects/paperclip/paperclip-addon/plugins/workflow-engine/src/workflow-store.ts
file update:
diff --git a/paperclip-addon/plugins/workflow-engine/src/dag-engine.ts b/paperclip-addon/plugins/workflow-engine/src/dag-engine.ts
index b59510f0473171038be77277286d3b5c2063e6c5..e7f21ff998cab78b9325a8aeacf4fda9c5e06553
--- a/paperclip-addon/plugins/workflow-engine/src/dag-engine.ts
+++ b/paperclip-addon/plugins/workflow-engine/src/dag-engine.ts
@@ -2,6 +2,7 @@
   id: string;
   title: string;
   dependsOn: string[];
+  sessionMode?: "fresh" | "reuse";
   onFailure?: "retry" | "skip" | "abort_workflow" | "escalate";
   escalateTo?: string;
   maxRetries?: number;
diff --git a/paperclip-addon/plugins/workflow-engine/src/reconciler.ts b/paperclip-addon/plugins/workflow-engine/src/reconciler.ts
index 14ab4e766a835d145a6c2e7b1b564270285cbdc3..d2ab54817bd8185944379d2188bfbdef1722020a
--- a/paperclip-addon/plugins/workflow-engine/src/reconciler.ts
+++ b/paperclip-addon/plugins/workflow-engine/src/reconciler.ts
@@ -8,65 +8,22 @@
   listStepRuns,
   updateStepRun,
   updateWorkflowRun,
-  type WorkflowDefinition,
-  type WorkflowRun,
-  type WorkflowStepRun,
 } from "./workflow-store.js";
 import type { WorkflowStep } from "./dag-engine.js";
-
-type WorkflowRunRecord = PluginEntityRecord & { data: WorkflowRun };
-type WorkflowStepRunRecord = PluginEntityRecord & { data: WorkflowStepRun };
-type WorkflowDefinitionRecord = PluginEntityRecord & { data: WorkflowDefinition };
-type StepAgentMetadata = WorkflowStep & {
-  agent?: string;
-  agentName?: string;
-  assigneeAgentName?: string;
-};
+import {
+  TERMINAL_STEP_STATUSES,
+  findStepDefinition,
+  getStepAgentName,
+  toWorkflowDefinitionRecord,
+  toWorkflowRunRecord,
+  toWorkflowStepRunRecord,
+} from "./workflow-utils.js";
 
 const STALE_THRESHOLD_MS = 300_000;
-const TERMINAL_STEP_STATUSES = new Set<string>([
-  STEP_STATUSES.done,
-  STEP_STATUSES.failed,
-  STEP_STATUSES.skipped,
-  STEP_STATUSES.escalated,
-]);
 
 function isStale(record: PluginEntityRecord, thresholdMs: number): boolean {
   const updatedAt = new Date(record.updatedAt).getTime();
   return Date.now() - updatedAt > thresholdMs;
-}
-
-function toWorkflowRunRecord(record: PluginEntityRecord): WorkflowRunRecord {
-  return record as WorkflowRunRecord;
-}
-
-function toWorkflowStepRunRecord(record: PluginEntityRecord): WorkflowStepRunRecord {
-  return record as WorkflowStepRunRecord;
-}
-
-function toWorkflowDefinitionRecord(record: PluginEntityRecord): WorkflowDefinitionRecord {
-  return record as WorkflowDefinitionRecord;
-}
-
-function findStepDefinition(definition: WorkflowDefinitionRecord, stepId: string): WorkflowStep | null {
-  return definition.data.steps.find((step: WorkflowStep) => step.id === stepId) ?? null;
-}
-
-function getStepAgentName(stepRun: WorkflowStepRunRecord, stepDef: WorkflowStep): string | null {
-  if (typeof stepRun.data.agentName === "string" && stepRun.data.agentName.trim()) {
-    return stepRun.data.agentName.trim();
-  }
-
-  const stepMeta = stepDef as StepAgentMetadata;
-  const candidates = [stepMeta.agentName, stepMeta.agent, stepMeta.assigneeAgentName];
-
-  for (const candidate of candidates) {
-    if (typeof candidate === "string" && candidate.trim()) {
-      return candidate.trim();
-    }
-  }
-
-  return null;
 }
 
 async function invokeAgentByName(
diff --git a/paperclip-addon/plugins/workflow-engine/src/worker.ts b/paperclip-addon/plugins/workflow-engine/src/worker.ts
index c5ac3fbb4ca7f767eb29331bd3630b635449b335..303e35958890fa1f66db3f2b0ff1fa1f134102a8
--- a/paperclip-addon/plugins/workflow-engine/src/worker.ts
+++ b/paperclip-addon/plugins/workflow-engine/src/worker.ts
@@ -15,6 +15,7 @@
 } from "./dag-engine.js";
 import {
   checkIdempotency,
+  createWorkflowRun,
   createStepRun,
   findStepRunByIssueId,
   getWorkflowDefinition,
@@ -25,51 +26,38 @@
   markIdempotency,
   updateStepRun,
   updateWorkflowRun,
-  type WorkflowDefinition,
-  type WorkflowRun,
   type WorkflowStepRun,
 } from "./workflow-store.js";
+import {
+  TERMINAL_STEP_STATUSES,
+  findStepDefinition,
+  getStepAgentName,
+  getStepAgentNameHint,
+  toWorkflowDefinitionRecord,
+  toWorkflowRunRecord,
+  toWorkflowStepRunRecord,
+  type WorkflowDefinitionRecord,
+  type WorkflowRunRecord,
+  type WorkflowStepRunRecord,
+} from "./workflow-utils.js";
 
-type WorkflowStepRecord = PluginEntityRecord & { data: WorkflowStepRun };
-type WorkflowRunRecord = PluginEntityRecord & { data: WorkflowRun };
-type WorkflowDefinitionRecord = PluginEntityRecord & { data: WorkflowDefinition };
 type WorkflowStepMetadata = WorkflowStep & {
   description?: string;
-  agent?: string;
-  agentName?: string;
-  assigneeAgentName?: string;
   assigneeAgentId?: string;
+  sessionMode?: "fresh" | "reuse";
 };
 type AgentRecord = Awaited<ReturnType<PluginContext["agents"]["list"]>>[number];
+type IssueUpdatePatch = Parameters<PluginContext["issues"]["update"]>[1];
 type ReconcilerModule = {
   reconcileStuckSteps?: (ctx: PluginContext) => Promise<void>;
 };
 
-const TERMINAL_STEP_STATUSES = new Set<string>([
-  STEP_STATUSES.done,
-  STEP_STATUSES.failed,
-  STEP_STATUSES.skipped,
-  STEP_STATUSES.escalated,
-]);
-
 function summarizeError(error: unknown): string {
   if (error instanceof Error) {
     return error.message;
   }
 
   return String(error);
-}
-
-function toStepRunRecord(record: PluginEntityRecord): WorkflowStepRecord {
-  return record as WorkflowStepRecord;
-}
-
-function toWorkflowRunRecord(record: PluginEntityRecord): WorkflowRunRecord {
-  return record as WorkflowRunRecord;
-}
-
-function toWorkflowDefinitionRecord(record: PluginEntityRecord): WorkflowDefinitionRecord {
-  return record as WorkflowDefinitionRecord;
 }
 
 function buildIdempotencyKey(event: PluginEvent): string {
@@ -78,10 +66,6 @@
 
 function getStepMetadata(stepDef: WorkflowStep): WorkflowStepMetadata {
   return stepDef as WorkflowStepMetadata;
-}
-
-function findStepDefinition(definition: WorkflowDefinitionRecord, stepId: string): WorkflowStep | null {
-  return definition.data.steps.find((step: WorkflowStep) => step.id === stepId) ?? null;
 }
 
 function getStepDescription(stepDef: WorkflowStep): string | undefined {
@@ -89,19 +73,6 @@
   return typeof description === "string" && description.trim() ? description.trim() : undefined;
 }
 
-function getStepAgentNameHint(stepDef: WorkflowStep): string | null {
-  const meta = getStepMetadata(stepDef);
-  const candidates = [meta.agentName, meta.agent, meta.assigneeAgentName];
-
-  for (const candidate of candidates) {
-    if (typeof candidate === "string" && candidate.trim()) {
-      return candidate.trim();
-    }
-  }
-
-  return null;
-}
-
 function getStepAgentIdHint(stepDef: WorkflowStep): string | null {
   const assigneeAgentId = getStepMetadata(stepDef).assigneeAgentId;
   if (typeof assigneeAgentId !== "string" || !assigneeAgentId.trim()) {
@@ -147,68 +118,256 @@
 
 async function invokeAgentForStep(
   ctx: PluginContext,
+  stepRunRecord: WorkflowStepRunRecord,
   stepDef: WorkflowStep,
-  agentName: string,
   workflowName: string,
   companyId: string,
-): Promise<void> {
+): Promise<WorkflowStepRunRecord> {
+  const agentName = getStepAgentName(stepRunRecord, stepDef);
+  if (!agentName) {
+    ctx.logger.warn("Agent name is missing for workflow step", {
+      companyId,
+      runId: stepRunRecord.data.runId,
+      stepId: stepDef.id,
+      workflowName,
+    });
+    return stepRunRecord;
+  }
+
   const agents = await ctx.agents.list({ companyId });
-  const agent = agents.find((candidate: AgentRecord) => candidate.name === agentName);
+  const agent = agents.find((candidate: AgentRecord) => candidate.name === agentName) ?? null;
 
   if (!agent) {
     ctx.logger.warn("Agent not found for step", {
       agentName,
       companyId,
+      runId: stepRunRecord.data.runId,
+      stepId: stepDef.id,
+      workflowName,
+    });
+    return stepRunRecord;
+  }
+
+  const prompt = `workflow:${workflowName}/step:${stepDef.id} — "${stepDef.title}" is ready. Please proceed with the assigned task.`;
+  const reason = `workflow:${workflowName}/step:${stepDef.id}`;
+  const sessionMode = getStepMetadata(stepDef).sessionMode === "reuse" ? "reuse" : "fresh";
+
+  if (sessionMode === "reuse") {
+    let sessionId = typeof stepRunRecord.data.sessionId === "string" && stepRunRecord.data.sessionId.trim()
+      ? stepRunRecord.data.sessionId.trim()
+      : "";
+
+    if (!sessionId) {
+      const session = await ctx.agents.sessions.create(agent.id, companyId, {
+        reason,
+      });
+      sessionId = session.sessionId;
+      stepRunRecord = toWorkflowStepRunRecord(await updateStepRun(ctx, stepRunRecord.id, {
+        sessionId,
+      }));
+    }
+
+    await ctx.agents.sessions.sendMessage(sessionId, companyId, {
+      prompt,
+      reason,
+    });
+
+    ctx.logger.info("Sent workflow step prompt via agent session", {
+      agentId: agent.id,
+      agentName,
+      companyId,
+      runId: stepRunRecord.data.runId,
+      sessionId,
       stepId: stepDef.id,
       workflowName,
     });
-    return;
+    return stepRunRecord;
   }
 
   await ctx.agents.invoke(agent.id, companyId, {
-    prompt: `workflow:${workflowName}/step:${stepDef.id} — "${stepDef.title}" is ready. Please proceed with the assigned task.`,
-    reason: `workflow:${workflowName}/step:${stepDef.id}`,
+    prompt,
+    reason,
   });
 
   ctx.logger.info("Invoked agent for workflow step", {
     agentId: agent.id,
     agentName,
     companyId,
+    runId: stepRunRecord.data.runId,
     stepId: stepDef.id,
     workflowName,
   });
+
+  return stepRunRecord;
 }
 
 async function activateBacklogStep(
   ctx: PluginContext,
-  stepRunRecord: WorkflowStepRecord,
+  stepRunRecord: WorkflowStepRunRecord,
   stepDef: WorkflowStep,
   workflowName: string,
   companyId: string,
-): Promise<void> {
+  options?: { parentIssueId?: string },
+): Promise<WorkflowStepRunRecord> {
   if (stepRunRecord.data.status !== STEP_STATUSES.backlog) {
-    return;
+    return stepRunRecord;
   }
 
-  const updated = toStepRunRecord(await updateStepRun(ctx, stepRunRecord.id, {
+  const resolvedAgent = await resolveStepAgent(
+    ctx,
+    companyId,
+    stepDef,
+    stepRunRecord.data.agentName,
+  );
+
+  let issueId = typeof stepRunRecord.data.issueId === "string" && stepRunRecord.data.issueId.trim()
+    ? stepRunRecord.data.issueId.trim()
+    : "";
+  const stepDescription = getStepDescription(stepDef) ?? `Workflow step: ${stepDef.id}`;
+
+  if (!issueId) {
+    const issue = await ctx.issues.create({
+      assigneeAgentId: resolvedAgent.agentId ?? undefined,
+      companyId,
+      description: stepDescription,
+      parentId: options?.parentIssueId,
+      title: `[${workflowName}] ${stepDef.title}`,
+    });
+    await ctx.issues.update(issue.id, { status: "todo" } as IssueUpdatePatch, companyId);
+    issueId = issue.id;
+  }
+
+  const nextStartedAt = stepRunRecord.data.startedAt ?? new Date().toISOString();
+  let updatedStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, stepRunRecord.id, {
+    agentName: resolvedAgent.agentName ?? stepRunRecord.data.agentName,
+    issueId,
+    startedAt: nextStartedAt,
     status: STEP_STATUSES.todo,
   }));
 
-  await invokeAgentForStep(
+  updatedStepRun = await invokeAgentForStep(
     ctx,
+    updatedStepRun,
     stepDef,
-    updated.data.agentName,
     workflowName,
     companyId,
   );
+
+  return updatedStepRun;
 }
 
+async function startWorkflow(
+  ctx: PluginContext,
+  workflowId: string,
+  companyId: string,
+  options?: {
+    createParentIssue?: boolean;
+    parentIssueId?: string;
+  },
+): Promise<{
+  activatedStepIds: string[];
+  parentIssueId: string | null;
+  runId: string;
+  workflowId: string;
+}> {
+  const workflowDefinition = await getWorkflowDefinition(ctx, workflowId);
+  if (!workflowDefinition) {
+    throw new Error(`Workflow definition not found: ${workflowId}`);
+  }
+
+  const typedWorkflowDefinition = toWorkflowDefinitionRecord(workflowDefinition);
+  if (typedWorkflowDefinition.data.companyId !== companyId) {
+    throw new Error(`Workflow does not belong to company: ${workflowId}`);
+  }
+
+  let parentIssueId = typeof options?.parentIssueId === "string" && options.parentIssueId.trim()
+    ? options.parentIssueId.trim()
+    : "";
+  if (!parentIssueId && options?.createParentIssue) {
+    const parentIssue = await ctx.issues.create({
+      companyId,
+      description: typedWorkflowDefinition.data.description || `Workflow run: ${typedWorkflowDefinition.data.name}`,
+      title: `[${typedWorkflowDefinition.data.name}] Workflow run`,
+    });
+    parentIssueId = parentIssue.id;
+  }
+
+  const workflowRun = toWorkflowRunRecord(await createWorkflowRun(ctx, {
+    companyId,
+    parentIssueId: parentIssueId || undefined,
+    startedAt: new Date().toISOString(),
+    status: RUN_STATUSES.running,
+    workflowId: typedWorkflowDefinition.id,
+    workflowName: typedWorkflowDefinition.data.name,
+  }));
+
+  const agents = await ctx.agents.list({ companyId });
+  const agentsByName = new Map<string, AgentRecord>();
+  for (const agent of agents) {
+    agentsByName.set(agent.name, agent);
+  }
+
+  const pendingRootSteps: Array<{ stepDef: WorkflowStep; stepRun: WorkflowStepRunRecord }> = [];
+  for (const stepDef of typedWorkflowDefinition.data.steps) {
+    const agentNameHint = getStepAgentNameHint(stepDef);
+    const matchedAgent = agentNameHint ? agentsByName.get(agentNameHint) ?? null : null;
+    const resolvedAgent = matchedAgent
+      ? { agentId: matchedAgent.id, agentName: matchedAgent.name }
+      : await resolveStepAgent(ctx, companyId, stepDef, agentNameHint ?? undefined);
+
+    if (!resolvedAgent.agentName) {
+      throw new Error(`Unable to resolve step assignee for "${stepDef.id}"`);
+    }
+
+    const stepRun = toWorkflowStepRunRecord(await createStepRun(ctx, companyId, {
+      agentName: resolvedAgent.agentName,
+      retryCount: 0,
+      runId: workflowRun.id,
+      status: STEP_STATUSES.backlog,
+      stepId: stepDef.id,
+    }));
+
+    if (stepDef.dependsOn.length === 0 && stepDef.triggerOn !== "escalation") {
+      pendingRootSteps.push({ stepDef, stepRun });
+    }
+  }
+
+  const activatedStepIds: string[] = [];
+  for (const pending of pendingRootSteps) {
+    await activateBacklogStep(
+      ctx,
+      pending.stepRun,
+      pending.stepDef,
+      typedWorkflowDefinition.data.name,
+      companyId,
+      { parentIssueId: parentIssueId || undefined },
+    );
+    activatedStepIds.push(pending.stepDef.id);
+  }
+
+  ctx.logger.info("Started workflow run", {
+    activatedStepIds,
+    companyId,
+    parentIssueId: parentIssueId || null,
+    runId: workflowRun.id,
+    workflowId: typedWorkflowDefinition.id,
+    workflowName: typedWorkflowDefinition.data.name,
+  });
+
+  return {
+    activatedStepIds,
+    parentIssueId: parentIssueId || null,
+    runId: workflowRun.id,
+    workflowId: typedWorkflowDefinition.id,
+  };
+}
+
 async function advanceWorkflow(
   ctx: PluginContext,
   stepRunRecord: PluginEntityRecord,
   companyId: string,
 ): Promise<void> {
-  const completedStepRun = toStepRunRecord(stepRunRecord);
+  const completedStepRun = toWorkflowStepRunRecord(stepRunRecord);
   const workflowRun = await getWorkflowRun(ctx, completedStepRun.data.runId);
 
   if (!workflowRun) {
@@ -237,7 +396,7 @@
   }
 
   const typedWorkflowDefinition = toWorkflowDefinitionRecord(workflowDefinition);
-  const stepRuns = (await listStepRuns(ctx, typedWorkflowRun.id, companyId)).map(toStepRunRecord);
+  const stepRuns = (await listStepRuns(ctx, typedWorkflowRun.id, companyId)).map(toWorkflowStepRunRecord);
   const completed = new Set<string>();
   const failed = new Set<string>();
   const skipped = new Set<string>();
@@ -289,7 +448,7 @@
         continue;
       }
 
-      stepRun = toStepRunRecord(await createStepRun(ctx, companyId, {
+      stepRun = toWorkflowStepRunRecord(await createStepRun(ctx, companyId, {
         agentName: resolvedAgent.agentName,
         retryCount: 0,
         runId: typedWorkflowRun.id,
@@ -305,6 +464,7 @@
       stepDef,
       typedWorkflowRun.data.workflowName,
       companyId,
+      { parentIssueId: typedWorkflowRun.data.parentIssueId },
     );
   }
 
@@ -329,7 +489,7 @@
   ctx: PluginContext,
   workflowRun: WorkflowRunRecord,
   workflowDefinition: WorkflowDefinitionRecord,
-  sourceStepRun: WorkflowStepRecord,
+  sourceStepRun: WorkflowStepRunRecord,
   escalationTargetId: string,
   companyId: string,
 ): Promise<void> {
@@ -338,7 +498,7 @@
     throw new Error(`Escalation target step not found: ${escalationTargetId}`);
   }
 
-  const stepRuns = (await listStepRuns(ctx, workflowRun.id, companyId)).map(toStepRunRecord);
+  const stepRuns = (await listStepRuns(ctx, workflowRun.id, companyId)).map(toWorkflowStepRunRecord);
   let escalationStepRun = stepRuns.find((candidate) => candidate.data.stepId === escalationTargetId) ?? null;
   const resolvedAgent = await resolveStepAgent(
     ctx,
@@ -352,7 +512,7 @@
   }
 
   if (!escalationStepRun) {
-    escalationStepRun = toStepRunRecord(await createStepRun(ctx, companyId, {
+    escalationStepRun = toWorkflowStepRunRecord(await createStepRun(ctx, companyId, {
       agentName: resolvedAgent.agentName,
       retryCount: 0,
       runId: workflowRun.id,
@@ -374,14 +534,14 @@
       title: `${workflowRun.data.workflowName}: ${escalationStep.title}`,
     });
 
-    escalationStepRun = toStepRunRecord(await updateStepRun(ctx, escalationStepRun.id, {
+    escalationStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, escalationStepRun.id, {
       issueId: issue.id,
     }));
   }
 
   const shouldActivate = escalationStepRun.data.status === STEP_STATUSES.backlog;
   if (shouldActivate) {
-    escalationStepRun = toStepRunRecord(await updateStepRun(ctx, escalationStepRun.id, {
+    escalationStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, escalationStepRun.id, {
       status: STEP_STATUSES.todo,
     }));
   }
@@ -389,8 +549,8 @@
   if (shouldActivate) {
     await invokeAgentForStep(
       ctx,
+      escalationStepRun,
       escalationStep,
-      escalationStepRun.data.agentName,
       workflowRun.data.workflowName,
       companyId,
     );
@@ -421,7 +581,7 @@
     return;
   }
 
-  const typedStepRun = toStepRunRecord(stepRunRecord);
+  const typedStepRun = toWorkflowStepRunRecord(stepRunRecord);
   if (TERMINAL_STEP_STATUSES.has(typedStepRun.data.status)) {
     return;
   }
@@ -469,7 +629,7 @@
   if (options.allowRetry) {
     const retryInfo = getRetryInfo(typedWorkflowDefinition.data.steps, typedStepRun.data.stepId);
     if (retryInfo.shouldRetry && typedStepRun.data.retryCount < retryInfo.maxRetries) {
-      const retriedStepRun = toStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
+      const retriedStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
         completedAt: undefined,
         retryCount: typedStepRun.data.retryCount + 1,
         status: STEP_STATUSES.todo,
@@ -477,8 +637,8 @@
 
       await invokeAgentForStep(
         ctx,
+        retriedStepRun,
         stepDef,
-        retriedStepRun.data.agentName,
         typedWorkflowRun.data.workflowName,
         event.companyId,
       );
@@ -496,7 +656,7 @@
   }
 
   if (stepDef.onFailure === "skip") {
-    const skippedStepRun = toStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
+    const skippedStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
       completedAt: new Date().toISOString(),
       status: STEP_STATUSES.skipped,
     }));
@@ -556,7 +716,7 @@
       return;
     }
 
-    const escalatedStepRun = toStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
+    const escalatedStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
       completedAt: new Date().toISOString(),
       status: STEP_STATUSES.escalated,
     }));
@@ -617,6 +777,29 @@
   }
 }
 
+function registerDataHandler(
+  ctx: PluginContext,
+  key: string,
+  handler: (params: Record<string, unknown>) => Promise<unknown>,
+): void {
+  const dataClient = ctx.data as {
+    handle?: (handlerKey: string, handlerFn: (params: Record<string, unknown>) => Promise<unknown>) => void;
+    register?: (handlerKey: string, handlerFn: (params: Record<string, unknown>) => Promise<unknown>) => void;
+  };
+
+  if (typeof dataClient.handle === "function") {
+    dataClient.handle(key, handler);
+    return;
+  }
+
+  if (typeof dataClient.register === "function") {
+    dataClient.register(key, handler);
+    return;
+  }
+
+  throw new Error("Plugin data client does not support handler registration");
+}
+
 const plugin = definePlugin({
   async setup(ctx: PluginContext) {
     ctx.events.on("issue.updated", async (event: PluginEvent) => {
@@ -638,7 +821,7 @@
           return;
         }
 
-        const stepRun = toStepRunRecord(stepRunRecord);
+        const stepRun = toWorkflowStepRunRecord(stepRunRecord);
         const payload = event.payload as { status?: string };
         const issueStatus = typeof payload.status === "string" ? payload.status : undefined;
 
@@ -657,7 +840,7 @@
           return;
         }
 
-        const updatedStepRun = toStepRunRecord(await updateStepRun(ctx, stepRun.id, {
+        const updatedStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, stepRun.id, {
           completedAt: nextStepStatus === STEP_STATUSES.done ? new Date().toISOString() : undefined,
           startedAt: nextStepStatus === STEP_STATUSES.inProgress
             ? stepRun.data.startedAt ?? new Date().toISOString()
@@ -687,11 +870,21 @@
 
     ctx.events.on("issue.created", async (event: PluginEvent) => {
       try {
+        const idempotencyKey = buildIdempotencyKey(event);
+        if (await checkIdempotency(ctx, idempotencyKey, event.companyId)) {
+          return;
+        }
+
         const payload = event.payload as {
           id?: string;
           parentId?: string;
           assigneeAgentId?: string;
         };
+        const issueId = typeof event.entityId === "string" && event.entityId.trim()
+          ? event.entityId.trim()
+          : typeof payload.id === "string" && payload.id.trim()
+            ? payload.id.trim()
+            : "";
 
         const parentId = typeof payload.parentId === "string" && payload.parentId.trim()
           ? payload.parentId.trim()
@@ -700,12 +893,34 @@
           ? payload.assigneeAgentId.trim()
           : "";
 
-        if (parentId || !assigneeAgentId) {
+        if (!issueId || parentId || !assigneeAgentId) {
+          await markIdempotency(ctx, idempotencyKey, event.companyId);
+          return;
+        }
+
+        const agent = await ctx.agents.get(assigneeAgentId, event.companyId);
+        const metadata = agent?.metadata as Record<string, unknown> | undefined;
+        const defaultParentIssueId = typeof metadata?.defaultParentIssueId === "string" && metadata.defaultParentIssueId.trim()
+          ? metadata.defaultParentIssueId.trim()
+          : "";
+
+        if (!defaultParentIssueId) {
+          await markIdempotency(ctx, idempotencyKey, event.companyId);
           return;
         }
 
-        ctx.logger.info("parentId filler: issue created without parent", {
-          issueId: event.entityId ?? payload.id ?? null,
+        await ctx.issues.update(
+          issueId,
+          { parentId: defaultParentIssueId } as IssueUpdatePatch,
+          event.companyId,
+        );
+        await markIdempotency(ctx, idempotencyKey, event.companyId);
+
+        ctx.logger.info("parentId filler: populated issue parentId from agent metadata", {
+          assigneeAgentId,
+          companyId: event.companyId,
+          issueId,
+          parentId: defaultParentIssueId,
         });
       } catch (error) {
         ctx.logger.warn("Failed to handle issue.created event", {
@@ -744,7 +959,23 @@
       await runReconciler(ctx);
     });
 
-    ctx.data.register("workflow-overview", async (params: Record<string, unknown>) => {
+    registerDataHandler(ctx, "start-workflow", async (params: Record<string, unknown>) => {
+      const workflowId = typeof params.workflowId === "string" ? params.workflowId.trim() : "";
+      const companyId = typeof params.companyId === "string" ? params.companyId.trim() : "";
+      const parentIssueId = typeof params.parentIssueId === "string" ? params.parentIssueId.trim() : "";
+      const createParentIssue = params.createParentIssue === true;
+
+      if (!workflowId || !companyId) {
+        throw new Error("start-workflow requires workflowId and companyId");
+      }
+
+      return await startWorkflow(ctx, workflowId, companyId, {
+        createParentIssue,
+        parentIssueId: parentIssueId || undefined,
+      });
+    });
+
+    registerDataHandler(ctx, "workflow-overview", async (params: Record<string, unknown>) => {
       try {
         const companyId = typeof params.companyId === "string" ? params.companyId.trim() : "";
         if (!companyId) {
@@ -782,7 +1013,7 @@
       }
     });
 
-    ctx.data.register("workflow-run-detail", async (params: Record<string, unknown>) => {
+    registerDataHandler(ctx, "workflow-run-detail", async (params: Record<string, unknown>) => {
       try {
         const runId = typeof params.runId === "string" ? params.runId.trim() : "";
         if (!runId) {
@@ -807,7 +1038,7 @@
             ...typedWorkflowRun.data,
           },
           stepRuns: stepRuns.map((record: PluginEntityRecord) => {
-            const stepRun = toStepRunRecord(record);
+            const stepRun = toWorkflowStepRunRecord(record);
             return {
               id: stepRun.id,
               status: stepRun.status,
diff --git a/paperclip-addon/plugins/workflow-engine/src/workflow-store.ts b/paperclip-addon/plugins/workflow-engine/src/workflow-store.ts
index 5871a2082258cebcfe8b69ebafbb0814cf303d50..82eb46836883940e109368e927b7e367c96616b5
--- a/paperclip-addon/plugins/workflow-engine/src/workflow-store.ts
+++ b/paperclip-addon/plugins/workflow-engine/src/workflow-store.ts
@@ -44,7 +44,14 @@
 
 type TypedEntityRecord<T> = Omit<PluginEntityRecord, "data"> & { data: T };
 
-function toTypedRecord<T>(record: PluginEntityRecord): TypedEntityRecord<T> {
+function toTypedRecord<T>(
+  record: PluginEntityRecord,
+  entityType: string,
+): TypedEntityRecord<T> {
+  if (record.entityType !== entityType) {
+    throw new Error(`Expected entity type "${entityType}", got "${record.entityType}"`);
+  }
+
   return record as TypedEntityRecord<T>;
 }
 
@@ -67,13 +74,31 @@
   id: string,
   entityType: string,
 ): Promise<TypedEntityRecord<T> | null> {
-  const record = await ctx.entities.get(id);
+  const listQuery = {
+    entityType,
+    id,
+    limit: 1,
+  } as Record<string, unknown>;
 
-  if (!record || record.entityType !== entityType) {
+  const listed = await ctx.entities.list(
+    listQuery as Parameters<PluginContext["entities"]["list"]>[0],
+  );
+  const listedRecord = listed.find(
+    (record: PluginEntityRecord) => record.id === id,
+  ) ?? null;
+
+  if (listedRecord) {
+    return toTypedRecord<T>(listedRecord, entityType);
+  }
+
+  // Fallback: some host SDK variants ignore `id` on entities.list().
+  // Keep defensive type verification on direct get() until id-filter support is universal.
+  const record = await ctx.entities.get(id);
+  if (!record) {
     return null;
   }
 
-  return toTypedRecord<T>(record);
+  return toTypedRecord<T>(record, entityType);
 }
 
 async function requireEntityByType<T>(
diff --git a/paperclip-addon/plugins/workflow-engine/src/workflow-utils.ts b/paperclip-addon/plugins/workflow-engine/src/workflow-utils.ts
new file mode 100644
index 0000000000000000000000000000000000000000..ec1cf6f6b9aa722b126b58c0a728b4e538ad1494
--- /dev/null
+++ b/paperclip-addon/plugins/workflow-engine/src/workflow-utils.ts
@@ -0,0 +1,62 @@
+import type { PluginEntityRecord } from "@paperclipai/plugin-sdk";
+
+import { STEP_STATUSES } from "./constants.js";
+import type { WorkflowStep } from "./dag-engine.js";
+import type {
+  WorkflowDefinition,
+  WorkflowRun,
+  WorkflowStepRun,
+} from "./workflow-store.js";
+
+export type WorkflowRunRecord = PluginEntityRecord & { data: WorkflowRun };
+export type WorkflowStepRunRecord = PluginEntityRecord & { data: WorkflowStepRun };
+export type WorkflowDefinitionRecord = PluginEntityRecord & { data: WorkflowDefinition };
+type StepAgentMetadata = WorkflowStep & {
+  agent?: string;
+  agentName?: string;
+  assigneeAgentName?: string;
+};
+
+export const TERMINAL_STEP_STATUSES = new Set<string>([
+  STEP_STATUSES.done,
+  STEP_STATUSES.failed,
+  STEP_STATUSES.skipped,
+  STEP_STATUSES.escalated,
+]);
+
+export function toWorkflowRunRecord(record: PluginEntityRecord): WorkflowRunRecord {
+  return record as WorkflowRunRecord;
+}
+
+export function toWorkflowStepRunRecord(record: PluginEntityRecord): WorkflowStepRunRecord {
+  return record as WorkflowStepRunRecord;
+}
+
+export function toWorkflowDefinitionRecord(record: PluginEntityRecord): WorkflowDefinitionRecord {
+  return record as WorkflowDefinitionRecord;
+}
+
+export function findStepDefinition(definition: WorkflowDefinitionRecord, stepId: string): WorkflowStep | null {
+  return definition.data.steps.find((step: WorkflowStep) => step.id === stepId) ?? null;
+}
+
+export function getStepAgentNameHint(stepDef: WorkflowStep): string | null {
+  const stepMeta = stepDef as StepAgentMetadata;
+  const candidates = [stepMeta.agentName, stepMeta.agent, stepMeta.assigneeAgentName];
+
+  for (const candidate of candidates) {
+    if (typeof candidate === "string" && candidate.trim()) {
+      return candidate.trim();
+    }
+  }
+
+  return null;
+}
+
+export function getStepAgentName(stepRun: WorkflowStepRunRecord, stepDef: WorkflowStep): string | null {
+  if (typeof stepRun.data.agentName === "string" && stepRun.data.agentName.trim()) {
+    return stepRun.data.agentName.trim();
+  }
+
+  return getStepAgentNameHint(stepDef);
+}

file update:
diff --git a/paperclip-addon/plugins/workflow-engine/src/dag-engine.ts b/paperclip-addon/plugins/workflow-engine/src/dag-engine.ts
index b59510f0473171038be77277286d3b5c2063e6c5..e7f21ff998cab78b9325a8aeacf4fda9c5e06553
--- a/paperclip-addon/plugins/workflow-engine/src/dag-engine.ts
+++ b/paperclip-addon/plugins/workflow-engine/src/dag-engine.ts
@@ -2,6 +2,7 @@
   id: string;
   title: string;
   dependsOn: string[];
+  sessionMode?: "fresh" | "reuse";
   onFailure?: "retry" | "skip" | "abort_workflow" | "escalate";
   escalateTo?: string;
   maxRetries?: number;
diff --git a/paperclip-addon/plugins/workflow-engine/src/reconciler.ts b/paperclip-addon/plugins/workflow-engine/src/reconciler.ts
index 14ab4e766a835d145a6c2e7b1b564270285cbdc3..d2ab54817bd8185944379d2188bfbdef1722020a
--- a/paperclip-addon/plugins/workflow-engine/src/reconciler.ts
+++ b/paperclip-addon/plugins/workflow-engine/src/reconciler.ts
@@ -8,65 +8,22 @@
   listStepRuns,
   updateStepRun,
   updateWorkflowRun,
-  type WorkflowDefinition,
-  type WorkflowRun,
-  type WorkflowStepRun,
 } from "./workflow-store.js";
 import type { WorkflowStep } from "./dag-engine.js";
-
-type WorkflowRunRecord = PluginEntityRecord & { data: WorkflowRun };
-type WorkflowStepRunRecord = PluginEntityRecord & { data: WorkflowStepRun };
-type WorkflowDefinitionRecord = PluginEntityRecord & { data: WorkflowDefinition };
-type StepAgentMetadata = WorkflowStep & {
-  agent?: string;
-  agentName?: string;
-  assigneeAgentName?: string;
-};
+import {
+  TERMINAL_STEP_STATUSES,
+  findStepDefinition,
+  getStepAgentName,
+  toWorkflowDefinitionRecord,
+  toWorkflowRunRecord,
+  toWorkflowStepRunRecord,
+} from "./workflow-utils.js";
 
 const STALE_THRESHOLD_MS = 300_000;
-const TERMINAL_STEP_STATUSES = new Set<string>([
-  STEP_STATUSES.done,
-  STEP_STATUSES.failed,
-  STEP_STATUSES.skipped,
-  STEP_STATUSES.escalated,
-]);
 
 function isStale(record: PluginEntityRecord, thresholdMs: number): boolean {
   const updatedAt = new Date(record.updatedAt).getTime();
   return Date.now() - updatedAt > thresholdMs;
-}
-
-function toWorkflowRunRecord(record: PluginEntityRecord): WorkflowRunRecord {
-  return record as WorkflowRunRecord;
-}
-
-function toWorkflowStepRunRecord(record: PluginEntityRecord): WorkflowStepRunRecord {
-  return record as WorkflowStepRunRecord;
-}
-
-function toWorkflowDefinitionRecord(record: PluginEntityRecord): WorkflowDefinitionRecord {
-  return record as WorkflowDefinitionRecord;
-}
-
-function findStepDefinition(definition: WorkflowDefinitionRecord, stepId: string): WorkflowStep | null {
-  return definition.data.steps.find((step: WorkflowStep) => step.id === stepId) ?? null;
-}
-
-function getStepAgentName(stepRun: WorkflowStepRunRecord, stepDef: WorkflowStep): string | null {
-  if (typeof stepRun.data.agentName === "string" && stepRun.data.agentName.trim()) {
-    return stepRun.data.agentName.trim();
-  }
-
-  const stepMeta = stepDef as StepAgentMetadata;
-  const candidates = [stepMeta.agentName, stepMeta.agent, stepMeta.assigneeAgentName];
-
-  for (const candidate of candidates) {
-    if (typeof candidate === "string" && candidate.trim()) {
-      return candidate.trim();
-    }
-  }
-
-  return null;
 }
 
 async function invokeAgentByName(
diff --git a/paperclip-addon/plugins/workflow-engine/src/worker.ts b/paperclip-addon/plugins/workflow-engine/src/worker.ts
index c5ac3fbb4ca7f767eb29331bd3630b635449b335..303e35958890fa1f66db3f2b0ff1fa1f134102a8
--- a/paperclip-addon/plugins/workflow-engine/src/worker.ts
+++ b/paperclip-addon/plugins/workflow-engine/src/worker.ts
@@ -15,6 +15,7 @@
 } from "./dag-engine.js";
 import {
   checkIdempotency,
+  createWorkflowRun,
   createStepRun,
   findStepRunByIssueId,
   getWorkflowDefinition,
@@ -25,51 +26,38 @@
   markIdempotency,
   updateStepRun,
   updateWorkflowRun,
-  type WorkflowDefinition,
-  type WorkflowRun,
   type WorkflowStepRun,
 } from "./workflow-store.js";
+import {
+  TERMINAL_STEP_STATUSES,
+  findStepDefinition,
+  getStepAgentName,
+  getStepAgentNameHint,
+  toWorkflowDefinitionRecord,
+  toWorkflowRunRecord,
+  toWorkflowStepRunRecord,
+  type WorkflowDefinitionRecord,
+  type WorkflowRunRecord,
+  type WorkflowStepRunRecord,
+} from "./workflow-utils.js";
 
-type WorkflowStepRecord = PluginEntityRecord & { data: WorkflowStepRun };
-type WorkflowRunRecord = PluginEntityRecord & { data: WorkflowRun };
-type WorkflowDefinitionRecord = PluginEntityRecord & { data: WorkflowDefinition };
 type WorkflowStepMetadata = WorkflowStep & {
   description?: string;
-  agent?: string;
-  agentName?: string;
-  assigneeAgentName?: string;
   assigneeAgentId?: string;
+  sessionMode?: "fresh" | "reuse";
 };
 type AgentRecord = Awaited<ReturnType<PluginContext["agents"]["list"]>>[number];
+type IssueUpdatePatch = Parameters<PluginContext["issues"]["update"]>[1];
 type ReconcilerModule = {
   reconcileStuckSteps?: (ctx: PluginContext) => Promise<void>;
 };
 
-const TERMINAL_STEP_STATUSES = new Set<string>([
-  STEP_STATUSES.done,
-  STEP_STATUSES.failed,
-  STEP_STATUSES.skipped,
-  STEP_STATUSES.escalated,
-]);
-
 function summarizeError(error: unknown): string {
   if (error instanceof Error) {
     return error.message;
   }
 
   return String(error);
-}
-
-function toStepRunRecord(record: PluginEntityRecord): WorkflowStepRecord {
-  return record as WorkflowStepRecord;
-}
-
-function toWorkflowRunRecord(record: PluginEntityRecord): WorkflowRunRecord {
-  return record as WorkflowRunRecord;
-}
-
-function toWorkflowDefinitionRecord(record: PluginEntityRecord): WorkflowDefinitionRecord {
-  return record as WorkflowDefinitionRecord;
 }
 
 function buildIdempotencyKey(event: PluginEvent): string {
@@ -78,10 +66,6 @@
 
 function getStepMetadata(stepDef: WorkflowStep): WorkflowStepMetadata {
   return stepDef as WorkflowStepMetadata;
-}
-
-function findStepDefinition(definition: WorkflowDefinitionRecord, stepId: string): WorkflowStep | null {
-  return definition.data.steps.find((step: WorkflowStep) => step.id === stepId) ?? null;
 }
 
 function getStepDescription(stepDef: WorkflowStep): string | undefined {
@@ -89,19 +73,6 @@
   return typeof description === "string" && description.trim() ? description.trim() : undefined;
 }
 
-function getStepAgentNameHint(stepDef: WorkflowStep): string | null {
-  const meta = getStepMetadata(stepDef);
-  const candidates = [meta.agentName, meta.agent, meta.assigneeAgentName];
-
-  for (const candidate of candidates) {
-    if (typeof candidate === "string" && candidate.trim()) {
-      return candidate.trim();
-    }
-  }
-
-  return null;
-}
-
 function getStepAgentIdHint(stepDef: WorkflowStep): string | null {
   const assigneeAgentId = getStepMetadata(stepDef).assigneeAgentId;
   if (typeof assigneeAgentId !== "string" || !assigneeAgentId.trim()) {
@@ -147,68 +118,256 @@
 
 async function invokeAgentForStep(
   ctx: PluginContext,
+  stepRunRecord: WorkflowStepRunRecord,
   stepDef: WorkflowStep,
-  agentName: string,
   workflowName: string,
   companyId: string,
-): Promise<void> {
+): Promise<WorkflowStepRunRecord> {
+  const agentName = getStepAgentName(stepRunRecord, stepDef);
+  if (!agentName) {
+    ctx.logger.warn("Agent name is missing for workflow step", {
+      companyId,
+      runId: stepRunRecord.data.runId,
+      stepId: stepDef.id,
+      workflowName,
+    });
+    return stepRunRecord;
+  }
+
   const agents = await ctx.agents.list({ companyId });
-  const agent = agents.find((candidate: AgentRecord) => candidate.name === agentName);
+  const agent = agents.find((candidate: AgentRecord) => candidate.name === agentName) ?? null;
 
   if (!agent) {
     ctx.logger.warn("Agent not found for step", {
       agentName,
       companyId,
+      runId: stepRunRecord.data.runId,
+      stepId: stepDef.id,
+      workflowName,
+    });
+    return stepRunRecord;
+  }
+
+  const prompt = `workflow:${workflowName}/step:${stepDef.id} — "${stepDef.title}" is ready. Please proceed with the assigned task.`;
+  const reason = `workflow:${workflowName}/step:${stepDef.id}`;
+  const sessionMode = getStepMetadata(stepDef).sessionMode === "reuse" ? "reuse" : "fresh";
+
+  if (sessionMode === "reuse") {
+    let sessionId = typeof stepRunRecord.data.sessionId === "string" && stepRunRecord.data.sessionId.trim()
+      ? stepRunRecord.data.sessionId.trim()
+      : "";
+
+    if (!sessionId) {
+      const session = await ctx.agents.sessions.create(agent.id, companyId, {
+        reason,
+      });
+      sessionId = session.sessionId;
+      stepRunRecord = toWorkflowStepRunRecord(await updateStepRun(ctx, stepRunRecord.id, {
+        sessionId,
+      }));
+    }
+
+    await ctx.agents.sessions.sendMessage(sessionId, companyId, {
+      prompt,
+      reason,
+    });
+
+    ctx.logger.info("Sent workflow step prompt via agent session", {
+      agentId: agent.id,
+      agentName,
+      companyId,
+      runId: stepRunRecord.data.runId,
+      sessionId,
       stepId: stepDef.id,
       workflowName,
     });
-    return;
+    return stepRunRecord;
   }
 
   await ctx.agents.invoke(agent.id, companyId, {
-    prompt: `workflow:${workflowName}/step:${stepDef.id} — "${stepDef.title}" is ready. Please proceed with the assigned task.`,
-    reason: `workflow:${workflowName}/step:${stepDef.id}`,
+    prompt,
+    reason,
   });
 
   ctx.logger.info("Invoked agent for workflow step", {
     agentId: agent.id,
     agentName,
     companyId,
+    runId: stepRunRecord.data.runId,
     stepId: stepDef.id,
     workflowName,
   });
+
+  return stepRunRecord;
 }
 
 async function activateBacklogStep(
   ctx: PluginContext,
-  stepRunRecord: WorkflowStepRecord,
+  stepRunRecord: WorkflowStepRunRecord,
   stepDef: WorkflowStep,
   workflowName: string,
   companyId: string,
-): Promise<void> {
+  options?: { parentIssueId?: string },
+): Promise<WorkflowStepRunRecord> {
   if (stepRunRecord.data.status !== STEP_STATUSES.backlog) {
-    return;
+    return stepRunRecord;
   }
 
-  const updated = toStepRunRecord(await updateStepRun(ctx, stepRunRecord.id, {
+  const resolvedAgent = await resolveStepAgent(
+    ctx,
+    companyId,
+    stepDef,
+    stepRunRecord.data.agentName,
+  );
+
+  let issueId = typeof stepRunRecord.data.issueId === "string" && stepRunRecord.data.issueId.trim()
+    ? stepRunRecord.data.issueId.trim()
+    : "";
+  const stepDescription = getStepDescription(stepDef) ?? `Workflow step: ${stepDef.id}`;
+
+  if (!issueId) {
+    const issue = await ctx.issues.create({
+      assigneeAgentId: resolvedAgent.agentId ?? undefined,
+      companyId,
+      description: stepDescription,
+      parentId: options?.parentIssueId,
+      title: `[${workflowName}] ${stepDef.title}`,
+    });
+    await ctx.issues.update(issue.id, { status: "todo" } as IssueUpdatePatch, companyId);
+    issueId = issue.id;
+  }
+
+  const nextStartedAt = stepRunRecord.data.startedAt ?? new Date().toISOString();
+  let updatedStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, stepRunRecord.id, {
+    agentName: resolvedAgent.agentName ?? stepRunRecord.data.agentName,
+    issueId,
+    startedAt: nextStartedAt,
     status: STEP_STATUSES.todo,
   }));
 
-  await invokeAgentForStep(
+  updatedStepRun = await invokeAgentForStep(
     ctx,
+    updatedStepRun,
     stepDef,
-    updated.data.agentName,
     workflowName,
     companyId,
   );
+
+  return updatedStepRun;
 }
 
+async function startWorkflow(
+  ctx: PluginContext,
+  workflowId: string,
+  companyId: string,
+  options?: {
+    createParentIssue?: boolean;
+    parentIssueId?: string;
+  },
+): Promise<{
+  activatedStepIds: string[];
+  parentIssueId: string | null;
+  runId: string;
+  workflowId: string;
+}> {
+  const workflowDefinition = await getWorkflowDefinition(ctx, workflowId);
+  if (!workflowDefinition) {
+    throw new Error(`Workflow definition not found: ${workflowId}`);
+  }
+
+  const typedWorkflowDefinition = toWorkflowDefinitionRecord(workflowDefinition);
+  if (typedWorkflowDefinition.data.companyId !== companyId) {
+    throw new Error(`Workflow does not belong to company: ${workflowId}`);
+  }
+
+  let parentIssueId = typeof options?.parentIssueId === "string" && options.parentIssueId.trim()
+    ? options.parentIssueId.trim()
+    : "";
+  if (!parentIssueId && options?.createParentIssue) {
+    const parentIssue = await ctx.issues.create({
+      companyId,
+      description: typedWorkflowDefinition.data.description || `Workflow run: ${typedWorkflowDefinition.data.name}`,
+      title: `[${typedWorkflowDefinition.data.name}] Workflow run`,
+    });
+    parentIssueId = parentIssue.id;
+  }
+
+  const workflowRun = toWorkflowRunRecord(await createWorkflowRun(ctx, {
+    companyId,
+    parentIssueId: parentIssueId || undefined,
+    startedAt: new Date().toISOString(),
+    status: RUN_STATUSES.running,
+    workflowId: typedWorkflowDefinition.id,
+    workflowName: typedWorkflowDefinition.data.name,
+  }));
+
+  const agents = await ctx.agents.list({ companyId });
+  const agentsByName = new Map<string, AgentRecord>();
+  for (const agent of agents) {
+    agentsByName.set(agent.name, agent);
+  }
+
+  const pendingRootSteps: Array<{ stepDef: WorkflowStep; stepRun: WorkflowStepRunRecord }> = [];
+  for (const stepDef of typedWorkflowDefinition.data.steps) {
+    const agentNameHint = getStepAgentNameHint(stepDef);
+    const matchedAgent = agentNameHint ? agentsByName.get(agentNameHint) ?? null : null;
+    const resolvedAgent = matchedAgent
+      ? { agentId: matchedAgent.id, agentName: matchedAgent.name }
+      : await resolveStepAgent(ctx, companyId, stepDef, agentNameHint ?? undefined);
+
+    if (!resolvedAgent.agentName) {
+      throw new Error(`Unable to resolve step assignee for "${stepDef.id}"`);
+    }
+
+    const stepRun = toWorkflowStepRunRecord(await createStepRun(ctx, companyId, {
+      agentName: resolvedAgent.agentName,
+      retryCount: 0,
+      runId: workflowRun.id,
+      status: STEP_STATUSES.backlog,
+      stepId: stepDef.id,
+    }));
+
+    if (stepDef.dependsOn.length === 0 && stepDef.triggerOn !== "escalation") {
+      pendingRootSteps.push({ stepDef, stepRun });
+    }
+  }
+
+  const activatedStepIds: string[] = [];
+  for (const pending of pendingRootSteps) {
+    await activateBacklogStep(
+      ctx,
+      pending.stepRun,
+      pending.stepDef,
+      typedWorkflowDefinition.data.name,
+      companyId,
+      { parentIssueId: parentIssueId || undefined },
+    );
+    activatedStepIds.push(pending.stepDef.id);
+  }
+
+  ctx.logger.info("Started workflow run", {
+    activatedStepIds,
+    companyId,
+    parentIssueId: parentIssueId || null,
+    runId: workflowRun.id,
+    workflowId: typedWorkflowDefinition.id,
+    workflowName: typedWorkflowDefinition.data.name,
+  });
+
+  return {
+    activatedStepIds,
+    parentIssueId: parentIssueId || null,
+    runId: workflowRun.id,
+    workflowId: typedWorkflowDefinition.id,
+  };
+}
+
 async function advanceWorkflow(
   ctx: PluginContext,
   stepRunRecord: PluginEntityRecord,
   companyId: string,
 ): Promise<void> {
-  const completedStepRun = toStepRunRecord(stepRunRecord);
+  const completedStepRun = toWorkflowStepRunRecord(stepRunRecord);
   const workflowRun = await getWorkflowRun(ctx, completedStepRun.data.runId);
 
   if (!workflowRun) {
@@ -237,7 +396,7 @@
   }
 
   const typedWorkflowDefinition = toWorkflowDefinitionRecord(workflowDefinition);
-  const stepRuns = (await listStepRuns(ctx, typedWorkflowRun.id, companyId)).map(toStepRunRecord);
+  const stepRuns = (await listStepRuns(ctx, typedWorkflowRun.id, companyId)).map(toWorkflowStepRunRecord);
   const completed = new Set<string>();
   const failed = new Set<string>();
   const skipped = new Set<string>();
@@ -289,7 +448,7 @@
         continue;
       }
 
-      stepRun = toStepRunRecord(await createStepRun(ctx, companyId, {
+      stepRun = toWorkflowStepRunRecord(await createStepRun(ctx, companyId, {
         agentName: resolvedAgent.agentName,
         retryCount: 0,
         runId: typedWorkflowRun.id,
@@ -305,6 +464,7 @@
       stepDef,
       typedWorkflowRun.data.workflowName,
       companyId,
+      { parentIssueId: typedWorkflowRun.data.parentIssueId },
     );
   }
 
@@ -329,7 +489,7 @@
   ctx: PluginContext,
   workflowRun: WorkflowRunRecord,
   workflowDefinition: WorkflowDefinitionRecord,
-  sourceStepRun: WorkflowStepRecord,
+  sourceStepRun: WorkflowStepRunRecord,
   escalationTargetId: string,
   companyId: string,
 ): Promise<void> {
@@ -338,7 +498,7 @@
     throw new Error(`Escalation target step not found: ${escalationTargetId}`);
   }
 
-  const stepRuns = (await listStepRuns(ctx, workflowRun.id, companyId)).map(toStepRunRecord);
+  const stepRuns = (await listStepRuns(ctx, workflowRun.id, companyId)).map(toWorkflowStepRunRecord);
   let escalationStepRun = stepRuns.find((candidate) => candidate.data.stepId === escalationTargetId) ?? null;
   const resolvedAgent = await resolveStepAgent(
     ctx,
@@ -352,7 +512,7 @@
   }
 
   if (!escalationStepRun) {
-    escalationStepRun = toStepRunRecord(await createStepRun(ctx, companyId, {
+    escalationStepRun = toWorkflowStepRunRecord(await createStepRun(ctx, companyId, {
       agentName: resolvedAgent.agentName,
       retryCount: 0,
       runId: workflowRun.id,
@@ -374,14 +534,14 @@
       title: `${workflowRun.data.workflowName}: ${escalationStep.title}`,
     });
 
-    escalationStepRun = toStepRunRecord(await updateStepRun(ctx, escalationStepRun.id, {
+    escalationStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, escalationStepRun.id, {
       issueId: issue.id,
     }));
   }
 
   const shouldActivate = escalationStepRun.data.status === STEP_STATUSES.backlog;
   if (shouldActivate) {
-    escalationStepRun = toStepRunRecord(await updateStepRun(ctx, escalationStepRun.id, {
+    escalationStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, escalationStepRun.id, {
       status: STEP_STATUSES.todo,
     }));
   }
@@ -389,8 +549,8 @@
   if (shouldActivate) {
     await invokeAgentForStep(
       ctx,
+      escalationStepRun,
       escalationStep,
-      escalationStepRun.data.agentName,
       workflowRun.data.workflowName,
       companyId,
     );
@@ -421,7 +581,7 @@
     return;
   }
 
-  const typedStepRun = toStepRunRecord(stepRunRecord);
+  const typedStepRun = toWorkflowStepRunRecord(stepRunRecord);
   if (TERMINAL_STEP_STATUSES.has(typedStepRun.data.status)) {
     return;
   }
@@ -469,7 +629,7 @@
   if (options.allowRetry) {
     const retryInfo = getRetryInfo(typedWorkflowDefinition.data.steps, typedStepRun.data.stepId);
     if (retryInfo.shouldRetry && typedStepRun.data.retryCount < retryInfo.maxRetries) {
-      const retriedStepRun = toStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
+      const retriedStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
         completedAt: undefined,
         retryCount: typedStepRun.data.retryCount + 1,
         status: STEP_STATUSES.todo,
@@ -477,8 +637,8 @@
 
       await invokeAgentForStep(
         ctx,
+        retriedStepRun,
         stepDef,
-        retriedStepRun.data.agentName,
         typedWorkflowRun.data.workflowName,
         event.companyId,
       );
@@ -496,7 +656,7 @@
   }
 
   if (stepDef.onFailure === "skip") {
-    const skippedStepRun = toStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
+    const skippedStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
       completedAt: new Date().toISOString(),
       status: STEP_STATUSES.skipped,
     }));
@@ -556,7 +716,7 @@
       return;
     }
 
-    const escalatedStepRun = toStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
+    const escalatedStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
       completedAt: new Date().toISOString(),
       status: STEP_STATUSES.escalated,
     }));
@@ -617,6 +777,29 @@
   }
 }
 
+function registerDataHandler(
+  ctx: PluginContext,
+  key: string,
+  handler: (params: Record<string, unknown>) => Promise<unknown>,
+): void {
+  const dataClient = ctx.data as {
+    handle?: (handlerKey: string, handlerFn: (params: Record<string, unknown>) => Promise<unknown>) => void;
+    register?: (handlerKey: string, handlerFn: (params: Record<string, unknown>) => Promise<unknown>) => void;
+  };
+
+  if (typeof dataClient.handle === "function") {
+    dataClient.handle(key, handler);
+    return;
+  }
+
+  if (typeof dataClient.register === "function") {
+    dataClient.register(key, handler);
+    return;
+  }
+
+  throw new Error("Plugin data client does not support handler registration");
+}
+
 const plugin = definePlugin({
   async setup(ctx: PluginContext) {
     ctx.events.on("issue.updated", async (event: PluginEvent) => {
@@ -638,7 +821,7 @@
           return;
         }
 
-        const stepRun = toStepRunRecord(stepRunRecord);
+        const stepRun = toWorkflowStepRunRecord(stepRunRecord);
         const payload = event.payload as { status?: string };
         const issueStatus = typeof payload.status === "string" ? payload.status : undefined;
 
@@ -657,7 +840,7 @@
           return;
         }
 
-        const updatedStepRun = toStepRunRecord(await updateStepRun(ctx, stepRun.id, {
+        const updatedStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, stepRun.id, {
           completedAt: nextStepStatus === STEP_STATUSES.done ? new Date().toISOString() : undefined,
           startedAt: nextStepStatus === STEP_STATUSES.inProgress
             ? stepRun.data.startedAt ?? new Date().toISOString()
@@ -687,11 +870,21 @@
 
     ctx.events.on("issue.created", async (event: PluginEvent) => {
       try {
+        const idempotencyKey = buildIdempotencyKey(event);
+        if (await checkIdempotency(ctx, idempotencyKey, event.companyId)) {
+          return;
+        }
+
         const payload = event.payload as {
           id?: string;
           parentId?: string;
           assigneeAgentId?: string;
         };
+        const issueId = typeof event.entityId === "string" && event.entityId.trim()
+          ? event.entityId.trim()
+          : typeof payload.id === "string" && payload.id.trim()
+            ? payload.id.trim()
+            : "";
 
         const parentId = typeof payload.parentId === "string" && payload.parentId.trim()
           ? payload.parentId.trim()
@@ -700,12 +893,34 @@
           ? payload.assigneeAgentId.trim()
           : "";
 
-        if (parentId || !assigneeAgentId) {
+        if (!issueId || parentId || !assigneeAgentId) {
+          await markIdempotency(ctx, idempotencyKey, event.companyId);
+          return;
+        }
+
+        const agent = await ctx.agents.get(assigneeAgentId, event.companyId);
+        const metadata = agent?.metadata as Record<string, unknown> | undefined;
+        const defaultParentIssueId = typeof metadata?.defaultParentIssueId === "string" && metadata.defaultParentIssueId.trim()
+          ? metadata.defaultParentIssueId.trim()
+          : "";
+
+        if (!defaultParentIssueId) {
+          await markIdempotency(ctx, idempotencyKey, event.companyId);
           return;
         }
 
-        ctx.logger.info("parentId filler: issue created without parent", {
-          issueId: event.entityId ?? payload.id ?? null,
+        await ctx.issues.update(
+          issueId,
+          { parentId: defaultParentIssueId } as IssueUpdatePatch,
+          event.companyId,
+        );
+        await markIdempotency(ctx, idempotencyKey, event.companyId);
+
+        ctx.logger.info("parentId filler: populated issue parentId from agent metadata", {
+          assigneeAgentId,
+          companyId: event.companyId,
+          issueId,
+          parentId: defaultParentIssueId,
         });
       } catch (error) {
         ctx.logger.warn("Failed to handle issue.created event", {
@@ -744,7 +959,23 @@
       await runReconciler(ctx);
     });
 
-    ctx.data.register("workflow-overview", async (params: Record<string, unknown>) => {
+    registerDataHandler(ctx, "start-workflow", async (params: Record<string, unknown>) => {
+      const workflowId = typeof params.workflowId === "string" ? params.workflowId.trim() : "";
+      const companyId = typeof params.companyId === "string" ? params.companyId.trim() : "";
+      const parentIssueId = typeof params.parentIssueId === "string" ? params.parentIssueId.trim() : "";
+      const createParentIssue = params.createParentIssue === true;
+
+      if (!workflowId || !companyId) {
+        throw new Error("start-workflow requires workflowId and companyId");
+      }
+
+      return await startWorkflow(ctx, workflowId, companyId, {
+        createParentIssue,
+        parentIssueId: parentIssueId || undefined,
+      });
+    });
+
+    registerDataHandler(ctx, "workflow-overview", async (params: Record<string, unknown>) => {
       try {
         const companyId = typeof params.companyId === "string" ? params.companyId.trim() : "";
         if (!companyId) {
@@ -782,7 +1013,7 @@
       }
     });
 
-    ctx.data.register("workflow-run-detail", async (params: Record<string, unknown>) => {
+    registerDataHandler(ctx, "workflow-run-detail", async (params: Record<string, unknown>) => {
       try {
         const runId = typeof params.runId === "string" ? params.runId.trim() : "";
         if (!runId) {
@@ -807,7 +1038,7 @@
             ...typedWorkflowRun.data,
           },
           stepRuns: stepRuns.map((record: PluginEntityRecord) => {
-            const stepRun = toStepRunRecord(record);
+            const stepRun = toWorkflowStepRunRecord(record);
             return {
               id: stepRun.id,
               status: stepRun.status,
diff --git a/paperclip-addon/plugins/workflow-engine/src/workflow-store.ts b/paperclip-addon/plugins/workflow-engine/src/workflow-store.ts
index 5871a2082258cebcfe8b69ebafbb0814cf303d50..82eb46836883940e109368e927b7e367c96616b5
--- a/paperclip-addon/plugins/workflow-engine/src/workflow-store.ts
+++ b/paperclip-addon/plugins/workflow-engine/src/workflow-store.ts
@@ -44,7 +44,14 @@
 
 type TypedEntityRecord<T> = Omit<PluginEntityRecord, "data"> & { data: T };
 
-function toTypedRecord<T>(record: PluginEntityRecord): TypedEntityRecord<T> {
+function toTypedRecord<T>(
+  record: PluginEntityRecord,
+  entityType: string,
+): TypedEntityRecord<T> {
+  if (record.entityType !== entityType) {
+    throw new Error(`Expected entity type "${entityType}", got "${record.entityType}"`);
+  }
+
   return record as TypedEntityRecord<T>;
 }
 
@@ -67,13 +74,31 @@
   id: string,
   entityType: string,
 ): Promise<TypedEntityRecord<T> | null> {
-  const record = await ctx.entities.get(id);
+  const listQuery = {
+    entityType,
+    id,
+    limit: 1,
+  } as Record<string, unknown>;
 
-  if (!record || record.entityType !== entityType) {
+  const listed = await ctx.entities.list(
+    listQuery as Parameters<PluginContext["entities"]["list"]>[0],
+  );
+  const listedRecord = listed.find(
+    (record: PluginEntityRecord) => record.id === id,
+  ) ?? null;
+
+  if (listedRecord) {
+    return toTypedRecord<T>(listedRecord, entityType);
+  }
+
+  // Fallback: some host SDK variants ignore `id` on entities.list().
+  // Keep defensive type verification on direct get() until id-filter support is universal.
+  const record = await ctx.entities.get(id);
+  if (!record) {
     return null;
   }
 
-  return toTypedRecord<T>(record);
+  return toTypedRecord<T>(record, entityType);
 }
 
 async function requireEntityByType<T>(
diff --git a/paperclip-addon/plugins/workflow-engine/src/workflow-utils.ts b/paperclip-addon/plugins/workflow-engine/src/workflow-utils.ts
new file mode 100644
index 0000000000000000000000000000000000000000..ec1cf6f6b9aa722b126b58c0a728b4e538ad1494
--- /dev/null
+++ b/paperclip-addon/plugins/workflow-engine/src/workflow-utils.ts
@@ -0,0 +1,62 @@
+import type { PluginEntityRecord } from "@paperclipai/plugin-sdk";
+
+import { STEP_STATUSES } from "./constants.js";
+import type { WorkflowStep } from "./dag-engine.js";
+import type {
+  WorkflowDefinition,
+  WorkflowRun,
+  WorkflowStepRun,
+} from "./workflow-store.js";
+
+export type WorkflowRunRecord = PluginEntityRecord & { data: WorkflowRun };
+export type WorkflowStepRunRecord = PluginEntityRecord & { data: WorkflowStepRun };
+export type WorkflowDefinitionRecord = PluginEntityRecord & { data: WorkflowDefinition };
+type StepAgentMetadata = WorkflowStep & {
+  agent?: string;
+  agentName?: string;
+  assigneeAgentName?: string;
+};
+
+export const TERMINAL_STEP_STATUSES = new Set<string>([
+  STEP_STATUSES.done,
+  STEP_STATUSES.failed,
+  STEP_STATUSES.skipped,
+  STEP_STATUSES.escalated,
+]);
+
+export function toWorkflowRunRecord(record: PluginEntityRecord): WorkflowRunRecord {
+  return record as WorkflowRunRecord;
+}
+
+export function toWorkflowStepRunRecord(record: PluginEntityRecord): WorkflowStepRunRecord {
+  return record as WorkflowStepRunRecord;
+}
+
+export function toWorkflowDefinitionRecord(record: PluginEntityRecord): WorkflowDefinitionRecord {
+  return record as WorkflowDefinitionRecord;
+}
+
+export function findStepDefinition(definition: WorkflowDefinitionRecord, stepId: string): WorkflowStep | null {
+  return definition.data.steps.find((step: WorkflowStep) => step.id === stepId) ?? null;
+}
+
+export function getStepAgentNameHint(stepDef: WorkflowStep): string | null {
+  const stepMeta = stepDef as StepAgentMetadata;
+  const candidates = [stepMeta.agentName, stepMeta.agent, stepMeta.assigneeAgentName];
+
+  for (const candidate of candidates) {
+    if (typeof candidate === "string" && candidate.trim()) {
+      return candidate.trim();
+    }
+  }
+
+  return null;
+}
+
+export function getStepAgentName(stepRun: WorkflowStepRunRecord, stepDef: WorkflowStep): string | null {
+  if (typeof stepRun.data.agentName === "string" && stepRun.data.agentName.trim()) {
+    return stepRun.data.agentName.trim();
+  }
+
+  return getStepAgentNameHint(stepDef);
+}

codex
`workflow-store.ts`의 신규 조회 로직 타입 오류도 정리했습니다. 이제 변경 파일을 다시 훑어 문법/호출 정합성을 확인하고 최종 diff를 정리하겠습니다.
exec
/bin/zsh -lc "sed -n '220,520p' plugins/workflow-engine/src/worker.ts" in /Users/kwak/Projects/paperclip/paperclip-addonexec
/bin/zsh -lc "sed -n '1,220p' plugins/workflow-engine/src/worker.ts" in /Users/kwak/Projects/paperclip/paperclip-addon succeeded in 0ms:
    stepRunRecord.data.agentName,
  );

  let issueId = typeof stepRunRecord.data.issueId === "string" && stepRunRecord.data.issueId.trim()
    ? stepRunRecord.data.issueId.trim()
    : "";
  const stepDescription = getStepDescription(stepDef) ?? `Workflow step: ${stepDef.id}`;

  if (!issueId) {
    const issue = await ctx.issues.create({
      assigneeAgentId: resolvedAgent.agentId ?? undefined,
      companyId,
      description: stepDescription,
      parentId: options?.parentIssueId,
      title: `[${workflowName}] ${stepDef.title}`,
    });
    await ctx.issues.update(issue.id, { status: "todo" } as IssueUpdatePatch, companyId);
    issueId = issue.id;
  }

 succeeded in 0ms:
import {
  definePlugin,
  runWorker,
  type PluginContext,
  type PluginEntityRecord,
  type PluginEvent,
  type PluginJobContext,
} from "@paperclipai/plugin-sdk";
import { JOB_KEYS, RUN_STATUSES, STEP_STATUSES } from "./constants.js";
import {
  getEscalationTarget,
  getNextSteps,
  getRetryInfo,
  type WorkflowStep,
} from "./dag-engine.js";
import {
  checkIdempotency,
  createWorkflowRun,
  createStepRun,
  findStepRunByIssueId,
exec
/bin/zsh -lc "sed -n '520,980p' plugins/workflow-engine/src/worker.ts" in /Users/kwak/Projects/paperclip/paperclip-addon succeeded in 0ms:
      stepId: escalationTargetId,
    }));
  }

  if (!escalationStepRun.data.issueId) {
    const issue = await ctx.issues.create({
      assigneeAgentId: resolvedAgent.agentId ?? undefined,
      companyId,
      description: [
        getStepDescription(escalationStep),
        `Escalated from workflow "${workflowRun.data.workflowName}" step "${sourceStepRun.data.stepId}".`,
        sourceStepRun.data.issueId ? `Origin issue: ${sourceStepRun.data.issueId}.` : undefined,
      ].filter((value): value is string => typeof value === "string" && value.trim().length > 0).join("\n\n"),
      parentId: workflowRun.data.parentIssueId ?? sourceStepRun.data.issueId,
      title: `${workflowRun.data.workflowName}: ${escalationStep.title}`,
    });

    escalationStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, escalationStepRun.id, {
      issueId: issue.id,
    }));
file update:
diff --git a/paperclip-addon/plugins/workflow-engine/src/dag-engine.ts b/paperclip-addon/plugins/workflow-engine/src/dag-engine.ts
index b59510f0473171038be77277286d3b5c2063e6c5..e7f21ff998cab78b9325a8aeacf4fda9c5e06553
--- a/paperclip-addon/plugins/workflow-engine/src/dag-engine.ts
+++ b/paperclip-addon/plugins/workflow-engine/src/dag-engine.ts
@@ -2,6 +2,7 @@
   id: string;
   title: string;
   dependsOn: string[];
+  sessionMode?: "fresh" | "reuse";
   onFailure?: "retry" | "skip" | "abort_workflow" | "escalate";
   escalateTo?: string;
   maxRetries?: number;
diff --git a/paperclip-addon/plugins/workflow-engine/src/reconciler.ts b/paperclip-addon/plugins/workflow-engine/src/reconciler.ts
index 14ab4e766a835d145a6c2e7b1b564270285cbdc3..d2ab54817bd8185944379d2188bfbdef1722020a
--- a/paperclip-addon/plugins/workflow-engine/src/reconciler.ts
+++ b/paperclip-addon/plugins/workflow-engine/src/reconciler.ts
@@ -8,65 +8,22 @@
   listStepRuns,
   updateStepRun,
   updateWorkflowRun,
-  type WorkflowDefinition,
-  type WorkflowRun,
-  type WorkflowStepRun,
 } from "./workflow-store.js";
 import type { WorkflowStep } from "./dag-engine.js";
-
-type WorkflowRunRecord = PluginEntityRecord & { data: WorkflowRun };
-type WorkflowStepRunRecord = PluginEntityRecord & { data: WorkflowStepRun };
-type WorkflowDefinitionRecord = PluginEntityRecord & { data: WorkflowDefinition };
-type StepAgentMetadata = WorkflowStep & {
-  agent?: string;
-  agentName?: string;
-  assigneeAgentName?: string;
-};
+import {
+  TERMINAL_STEP_STATUSES,
+  findStepDefinition,
+  getStepAgentName,
+  toWorkflowDefinitionRecord,
+  toWorkflowRunRecord,
+  toWorkflowStepRunRecord,
+} from "./workflow-utils.js";
 
 const STALE_THRESHOLD_MS = 300_000;
-const TERMINAL_STEP_STATUSES = new Set<string>([
-  STEP_STATUSES.done,
-  STEP_STATUSES.failed,
-  STEP_STATUSES.skipped,
-  STEP_STATUSES.escalated,
-]);
 
 function isStale(record: PluginEntityRecord, thresholdMs: number): boolean {
   const updatedAt = new Date(record.updatedAt).getTime();
   return Date.now() - updatedAt > thresholdMs;
-}
-
-function toWorkflowRunRecord(record: PluginEntityRecord): WorkflowRunRecord {
-  return record as WorkflowRunRecord;
-}
-
-function toWorkflowStepRunRecord(record: PluginEntityRecord): WorkflowStepRunRecord {
-  return record as WorkflowStepRunRecord;
-}
-
-function toWorkflowDefinitionRecord(record: PluginEntityRecord): WorkflowDefinitionRecord {
-  return record as WorkflowDefinitionRecord;
-}
-
-function findStepDefinition(definition: WorkflowDefinitionRecord, stepId: string): WorkflowStep | null {
-  return definition.data.steps.find((step: WorkflowStep) => step.id === stepId) ?? null;
-}
-
-function getStepAgentName(stepRun: WorkflowStepRunRecord, stepDef: WorkflowStep): string | null {
-  if (typeof stepRun.data.agentName === "string" && stepRun.data.agentName.trim()) {
-    return stepRun.data.agentName.trim();
-  }
-
-  const stepMeta = stepDef as StepAgentMetadata;
-  const candidates = [stepMeta.agentName, stepMeta.agent, stepMeta.assigneeAgentName];
-
-  for (const candidate of candidates) {
-    if (typeof candidate === "string" && candidate.trim()) {
-      return candidate.trim();
-    }
-  }
-
-  return null;
 }
 
 async function invokeAgentByName(
diff --git a/paperclip-addon/plugins/workflow-engine/src/worker.ts b/paperclip-addon/plugins/workflow-engine/src/worker.ts
index c5ac3fbb4ca7f767eb29331bd3630b635449b335..303e35958890fa1f66db3f2b0ff1fa1f134102a8
--- a/paperclip-addon/plugins/workflow-engine/src/worker.ts
+++ b/paperclip-addon/plugins/workflow-engine/src/worker.ts
@@ -15,6 +15,7 @@
 } from "./dag-engine.js";
 import {
   checkIdempotency,
+  createWorkflowRun,
   createStepRun,
   findStepRunByIssueId,
   getWorkflowDefinition,
@@ -25,51 +26,38 @@
   markIdempotency,
   updateStepRun,
   updateWorkflowRun,
-  type WorkflowDefinition,
-  type WorkflowRun,
   type WorkflowStepRun,
 } from "./workflow-store.js";
+import {
+  TERMINAL_STEP_STATUSES,
+  findStepDefinition,
+  getStepAgentName,
+  getStepAgentNameHint,
+  toWorkflowDefinitionRecord,
+  toWorkflowRunRecord,
+  toWorkflowStepRunRecord,
+  type WorkflowDefinitionRecord,
+  type WorkflowRunRecord,
+  type WorkflowStepRunRecord,
+} from "./workflow-utils.js";
 
-type WorkflowStepRecord = PluginEntityRecord & { data: WorkflowStepRun };
-type WorkflowRunRecord = PluginEntityRecord & { data: WorkflowRun };
-type WorkflowDefinitionRecord = PluginEntityRecord & { data: WorkflowDefinition };
 type WorkflowStepMetadata = WorkflowStep & {
   description?: string;
-  agent?: string;
-  agentName?: string;
-  assigneeAgentName?: string;
   assigneeAgentId?: string;
+  sessionMode?: "fresh" | "reuse";
 };
 type AgentRecord = Awaited<ReturnType<PluginContext["agents"]["list"]>>[number];
+type IssueUpdatePatch = Parameters<PluginContext["issues"]["update"]>[1];
 type ReconcilerModule = {
   reconcileStuckSteps?: (ctx: PluginContext) => Promise<void>;
 };
 
-const TERMINAL_STEP_STATUSES = new Set<string>([
-  STEP_STATUSES.done,
-  STEP_STATUSES.failed,
-  STEP_STATUSES.skipped,
-  STEP_STATUSES.escalated,
-]);
-
 function summarizeError(error: unknown): string {
   if (error instanceof Error) {
     return error.message;
   }
 
   return String(error);
-}
-
-function toStepRunRecord(record: PluginEntityRecord): WorkflowStepRecord {
-  return record as WorkflowStepRecord;
-}
-
-function toWorkflowRunRecord(record: PluginEntityRecord): WorkflowRunRecord {
-  return record as WorkflowRunRecord;
-}
-
-function toWorkflowDefinitionRecord(record: PluginEntityRecord): WorkflowDefinitionRecord {
-  return record as WorkflowDefinitionRecord;
 }
 
 function buildIdempotencyKey(event: PluginEvent): string {
@@ -78,10 +66,6 @@
 
 function getStepMetadata(stepDef: WorkflowStep): WorkflowStepMetadata {
   return stepDef as WorkflowStepMetadata;
-}
-
-function findStepDefinition(definition: WorkflowDefinitionRecord, stepId: string): WorkflowStep | null {
-  return definition.data.steps.find((step: WorkflowStep) => step.id === stepId) ?? null;
 }
 
 function getStepDescription(stepDef: WorkflowStep): string | undefined {
@@ -89,19 +73,6 @@
   return typeof description === "string" && description.trim() ? description.trim() : undefined;
 }
 
-function getStepAgentNameHint(stepDef: WorkflowStep): string | null {
-  const meta = getStepMetadata(stepDef);
-  const candidates = [meta.agentName, meta.agent, meta.assigneeAgentName];
-
-  for (const candidate of candidates) {
-    if (typeof candidate === "string" && candidate.trim()) {
-      return candidate.trim();
-    }
-  }
-
-  return null;
-}
-
 function getStepAgentIdHint(stepDef: WorkflowStep): string | null {
   const assigneeAgentId = getStepMetadata(stepDef).assigneeAgentId;
   if (typeof assigneeAgentId !== "string" || !assigneeAgentId.trim()) {
@@ -147,68 +118,256 @@
 
 async function invokeAgentForStep(
   ctx: PluginContext,
+  stepRunRecord: WorkflowStepRunRecord,
   stepDef: WorkflowStep,
-  agentName: string,
   workflowName: string,
   companyId: string,
-): Promise<void> {
+): Promise<WorkflowStepRunRecord> {
+  const agentName = getStepAgentName(stepRunRecord, stepDef);
+  if (!agentName) {
+    ctx.logger.warn("Agent name is missing for workflow step", {
+      companyId,
+      runId: stepRunRecord.data.runId,
+      stepId: stepDef.id,
+      workflowName,
+    });
+    return stepRunRecord;
+  }
+
   const agents = await ctx.agents.list({ companyId });
-  const agent = agents.find((candidate: AgentRecord) => candidate.name === agentName);
+  const agent = agents.find((candidate: AgentRecord) => candidate.name === agentName) ?? null;
 
   if (!agent) {
     ctx.logger.warn("Agent not found for step", {
       agentName,
       companyId,
+      runId: stepRunRecord.data.runId,
+      stepId: stepDef.id,
+      workflowName,
+    });
+    return stepRunRecord;
+  }
+
+  const prompt = `workflow:${workflowName}/step:${stepDef.id} — "${stepDef.title}" is ready. Please proceed with the assigned task.`;
+  const reason = `workflow:${workflowName}/step:${stepDef.id}`;
+  const sessionMode = getStepMetadata(stepDef).sessionMode === "reuse" ? "reuse" : "fresh";
+
+  if (sessionMode === "reuse") {
+    let sessionId = typeof stepRunRecord.data.sessionId === "string" && stepRunRecord.data.sessionId.trim()
+      ? stepRunRecord.data.sessionId.trim()
+      : "";
+
+    if (!sessionId) {
+      const session = await ctx.agents.sessions.create(agent.id, companyId, {
+        reason,
+      });
+      sessionId = session.sessionId;
+      stepRunRecord = toWorkflowStepRunRecord(await updateStepRun(ctx, stepRunRecord.id, {
+        sessionId,
+      }));
+    }
+
+    await ctx.agents.sessions.sendMessage(sessionId, companyId, {
+      prompt,
+      reason,
+    });
+
+    ctx.logger.info("Sent workflow step prompt via agent session", {
+      agentId: agent.id,
+      agentName,
+      companyId,
+      runId: stepRunRecord.data.runId,
+      sessionId,
       stepId: stepDef.id,
       workflowName,
     });
-    return;
+    return stepRunRecord;
   }
 
   await ctx.agents.invoke(agent.id, companyId, {
-    prompt: `workflow:${workflowName}/step:${stepDef.id} — "${stepDef.title}" is ready. Please proceed with the assigned task.`,
-    reason: `workflow:${workflowName}/step:${stepDef.id}`,
+    prompt,
+    reason,
   });
 
   ctx.logger.info("Invoked agent for workflow step", {
     agentId: agent.id,
     agentName,
     companyId,
+    runId: stepRunRecord.data.runId,
     stepId: stepDef.id,
     workflowName,
   });
+
+  return stepRunRecord;
 }
 
 async function activateBacklogStep(
   ctx: PluginContext,
-  stepRunRecord: WorkflowStepRecord,
+  stepRunRecord: WorkflowStepRunRecord,
   stepDef: WorkflowStep,
   workflowName: string,
   companyId: string,
-): Promise<void> {
+  options?: { parentIssueId?: string },
+): Promise<WorkflowStepRunRecord> {
   if (stepRunRecord.data.status !== STEP_STATUSES.backlog) {
-    return;
+    return stepRunRecord;
   }
 
-  const updated = toStepRunRecord(await updateStepRun(ctx, stepRunRecord.id, {
+  const resolvedAgent = await resolveStepAgent(
+    ctx,
+    companyId,
+    stepDef,
+    stepRunRecord.data.agentName,
+  );
+
+  let issueId = typeof stepRunRecord.data.issueId === "string" && stepRunRecord.data.issueId.trim()
+    ? stepRunRecord.data.issueId.trim()
+    : "";
+  const stepDescription = getStepDescription(stepDef) ?? `Workflow step: ${stepDef.id}`;
+
+  if (!issueId) {
+    const issue = await ctx.issues.create({
+      assigneeAgentId: resolvedAgent.agentId ?? undefined,
+      companyId,
+      description: stepDescription,
+      parentId: options?.parentIssueId,
+      title: `[${workflowName}] ${stepDef.title}`,
+    });
+    await ctx.issues.update(issue.id, { status: "todo" } as IssueUpdatePatch, companyId);
+    issueId = issue.id;
+  }
+
+  const nextStartedAt = stepRunRecord.data.startedAt ?? new Date().toISOString();
+  let updatedStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, stepRunRecord.id, {
+    agentName: resolvedAgent.agentName ?? stepRunRecord.data.agentName,
+    issueId,
+    startedAt: nextStartedAt,
     status: STEP_STATUSES.todo,
   }));
 
-  await invokeAgentForStep(
+  updatedStepRun = await invokeAgentForStep(
     ctx,
+    updatedStepRun,
     stepDef,
-    updated.data.agentName,
     workflowName,
     companyId,
   );
+
+  return updatedStepRun;
 }
 
+async function startWorkflow(
+  ctx: PluginContext,
+  workflowId: string,
+  companyId: string,
+  options?: {
+    createParentIssue?: boolean;
+    parentIssueId?: string;
+  },
+): Promise<{
+  activatedStepIds: string[];
+  parentIssueId: string | null;
+  runId: string;
+  workflowId: string;
+}> {
+  const workflowDefinition = await getWorkflowDefinition(ctx, workflowId);
+  if (!workflowDefinition) {
+    throw new Error(`Workflow definition not found: ${workflowId}`);
+  }
+
+  const typedWorkflowDefinition = toWorkflowDefinitionRecord(workflowDefinition);
+  if (typedWorkflowDefinition.data.companyId !== companyId) {
+    throw new Error(`Workflow does not belong to company: ${workflowId}`);
+  }
+
+  let parentIssueId = typeof options?.parentIssueId === "string" && options.parentIssueId.trim()
+    ? options.parentIssueId.trim()
+    : "";
+  if (!parentIssueId && options?.createParentIssue) {
+    const parentIssue = await ctx.issues.create({
+      companyId,
+      description: typedWorkflowDefinition.data.description || `Workflow run: ${typedWorkflowDefinition.data.name}`,
+      title: `[${typedWorkflowDefinition.data.name}] Workflow run`,
+    });
+    parentIssueId = parentIssue.id;
+  }
+
+  const workflowRun = toWorkflowRunRecord(await createWorkflowRun(ctx, {
+    companyId,
+    parentIssueId: parentIssueId || undefined,
+    startedAt: new Date().toISOString(),
+    status: RUN_STATUSES.running,
+    workflowId: typedWorkflowDefinition.id,
+    workflowName: typedWorkflowDefinition.data.name,
+  }));
+
+  const agents = await ctx.agents.list({ companyId });
+  const agentsByName = new Map<string, AgentRecord>();
+  for (const agent of agents) {
+    agentsByName.set(agent.name, agent);
+  }
+
+  const pendingRootSteps: Array<{ stepDef: WorkflowStep; stepRun: WorkflowStepRunRecord }> = [];
+  for (const stepDef of typedWorkflowDefinition.data.steps) {
+    const agentNameHint = getStepAgentNameHint(stepDef);
+    const matchedAgent = agentNameHint ? agentsByName.get(agentNameHint) ?? null : null;
+    const resolvedAgent = matchedAgent
+      ? { agentId: matchedAgent.id, agentName: matchedAgent.name }
+      : await resolveStepAgent(ctx, companyId, stepDef, agentNameHint ?? undefined);
+
+    if (!resolvedAgent.agentName) {
+      throw new Error(`Unable to resolve step assignee for "${stepDef.id}"`);
+    }
+
+    const stepRun = toWorkflowStepRunRecord(await createStepRun(ctx, companyId, {
+      agentName: resolvedAgent.agentName,
+      retryCount: 0,
+      runId: workflowRun.id,
+      status: STEP_STATUSES.backlog,
+      stepId: stepDef.id,
+    }));
+
+    if (stepDef.dependsOn.length === 0 && stepDef.triggerOn !== "escalation") {
+      pendingRootSteps.push({ stepDef, stepRun });
+    }
+  }
+
+  const activatedStepIds: string[] = [];
+  for (const pending of pendingRootSteps) {
+    await activateBacklogStep(
+      ctx,
+      pending.stepRun,
+      pending.stepDef,
+      typedWorkflowDefinition.data.name,
+      companyId,
+      { parentIssueId: parentIssueId || undefined },
+    );
+    activatedStepIds.push(pending.stepDef.id);
+  }
+
+  ctx.logger.info("Started workflow run", {
+    activatedStepIds,
+    companyId,
+    parentIssueId: parentIssueId || null,
+    runId: workflowRun.id,
+    workflowId: typedWorkflowDefinition.id,
+    workflowName: typedWorkflowDefinition.data.name,
+  });
+
+  return {
+    activatedStepIds,
+    parentIssueId: parentIssueId || null,
+    runId: workflowRun.id,
+    workflowId: typedWorkflowDefinition.id,
+  };
+}
+
 async function advanceWorkflow(
   ctx: PluginContext,
   stepRunRecord: PluginEntityRecord,
   companyId: string,
 ): Promise<void> {
-  const completedStepRun = toStepRunRecord(stepRunRecord);
+  const completedStepRun = toWorkflowStepRunRecord(stepRunRecord);
   const workflowRun = await getWorkflowRun(ctx, completedStepRun.data.runId);
 
   if (!workflowRun) {
@@ -237,7 +396,7 @@
   }
 
   const typedWorkflowDefinition = toWorkflowDefinitionRecord(workflowDefinition);
-  const stepRuns = (await listStepRuns(ctx, typedWorkflowRun.id, companyId)).map(toStepRunRecord);
+  const stepRuns = (await listStepRuns(ctx, typedWorkflowRun.id, companyId)).map(toWorkflowStepRunRecord);
   const completed = new Set<string>();
   const failed = new Set<string>();
   const skipped = new Set<string>();
@@ -289,7 +448,7 @@
         continue;
       }
 
-      stepRun = toStepRunRecord(await createStepRun(ctx, companyId, {
+      stepRun = toWorkflowStepRunRecord(await createStepRun(ctx, companyId, {
         agentName: resolvedAgent.agentName,
         retryCount: 0,
         runId: typedWorkflowRun.id,
@@ -305,6 +464,7 @@
       stepDef,
       typedWorkflowRun.data.workflowName,
       companyId,
+      { parentIssueId: typedWorkflowRun.data.parentIssueId },
     );
   }
 
@@ -329,7 +489,7 @@
   ctx: PluginContext,
   workflowRun: WorkflowRunRecord,
   workflowDefinition: WorkflowDefinitionRecord,
-  sourceStepRun: WorkflowStepRecord,
+  sourceStepRun: WorkflowStepRunRecord,
   escalationTargetId: string,
   companyId: string,
 ): Promise<void> {
@@ -338,7 +498,7 @@
     throw new Error(`Escalation target step not found: ${escalationTargetId}`);
   }
 
-  const stepRuns = (await listStepRuns(ctx, workflowRun.id, companyId)).map(toStepRunRecord);
+  const stepRuns = (await listStepRuns(ctx, workflowRun.id, companyId)).map(toWorkflowStepRunRecord);
   let escalationStepRun = stepRuns.find((candidate) => candidate.data.stepId === escalationTargetId) ?? null;
   const resolvedAgent = await resolveStepAgent(
     ctx,
@@ -352,7 +512,7 @@
   }
 
   if (!escalationStepRun) {
-    escalationStepRun = toStepRunRecord(await createStepRun(ctx, companyId, {
+    escalationStepRun = toWorkflowStepRunRecord(await createStepRun(ctx, companyId, {
       agentName: resolvedAgent.agentName,
       retryCount: 0,
       runId: workflowRun.id,
@@ -374,14 +534,14 @@
       title: `${workflowRun.data.workflowName}: ${escalationStep.title}`,
     });
 
-    escalationStepRun = toStepRunRecord(await updateStepRun(ctx, escalationStepRun.id, {
+    escalationStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, escalationStepRun.id, {
       issueId: issue.id,
     }));
   }
 
   const shouldActivate = escalationStepRun.data.status === STEP_STATUSES.backlog;
   if (shouldActivate) {
-    escalationStepRun = toStepRunRecord(await updateStepRun(ctx, escalationStepRun.id, {
+    escalationStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, escalationStepRun.id, {
       status: STEP_STATUSES.todo,
     }));
   }
@@ -389,8 +549,8 @@
   if (shouldActivate) {
     await invokeAgentForStep(
       ctx,
+      escalationStepRun,
       escalationStep,
-      escalationStepRun.data.agentName,
       workflowRun.data.workflowName,
       companyId,
     );
@@ -421,7 +581,7 @@
     return;
   }
 
-  const typedStepRun = toStepRunRecord(stepRunRecord);
+  const typedStepRun = toWorkflowStepRunRecord(stepRunRecord);
   if (TERMINAL_STEP_STATUSES.has(typedStepRun.data.status)) {
     return;
   }
@@ -469,7 +629,7 @@
   if (options.allowRetry) {
     const retryInfo = getRetryInfo(typedWorkflowDefinition.data.steps, typedStepRun.data.stepId);
     if (retryInfo.shouldRetry && typedStepRun.data.retryCount < retryInfo.maxRetries) {
-      const retriedStepRun = toStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
+      const retriedStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
         completedAt: undefined,
         retryCount: typedStepRun.data.retryCount + 1,
         status: STEP_STATUSES.todo,
@@ -477,8 +637,8 @@
 
       await invokeAgentForStep(
         ctx,
+        retriedStepRun,
         stepDef,
-        retriedStepRun.data.agentName,
         typedWorkflowRun.data.workflowName,
         event.companyId,
       );
@@ -496,7 +656,7 @@
   }
 
   if (stepDef.onFailure === "skip") {
-    const skippedStepRun = toStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
+    const skippedStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
       completedAt: new Date().toISOString(),
       status: STEP_STATUSES.skipped,
     }));
@@ -556,7 +716,7 @@
       return;
     }
 
-    const escalatedStepRun = toStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
+    const escalatedStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
       completedAt: new Date().toISOString(),
       status: STEP_STATUSES.escalated,
     }));
@@ -617,6 +777,29 @@
   }
 }
 
+function registerDataHandler(
+  ctx: PluginContext,
+  key: string,
+  handler: (params: Record<string, unknown>) => Promise<unknown>,
+): void {
+  const dataClient = ctx.data as {
+    handle?: (handlerKey: string, handlerFn: (params: Record<string, unknown>) => Promise<unknown>) => void;
+    register?: (handlerKey: string, handlerFn: (params: Record<string, unknown>) => Promise<unknown>) => void;
+  };
+
+  if (typeof dataClient.handle === "function") {
+    dataClient.handle(key, handler);
+    return;
+  }
+
+  if (typeof dataClient.register === "function") {
+    dataClient.register(key, handler);
+    return;
+  }
+
+  throw new Error("Plugin data client does not support handler registration");
+}
+
 const plugin = definePlugin({
   async setup(ctx: PluginContext) {
     ctx.events.on("issue.updated", async (event: PluginEvent) => {
@@ -638,7 +821,7 @@
           return;
         }
 
-        const stepRun = toStepRunRecord(stepRunRecord);
+        const stepRun = toWorkflowStepRunRecord(stepRunRecord);
         const payload = event.payload as { status?: string };
         const issueStatus = typeof payload.status === "string" ? payload.status : undefined;
 
@@ -657,7 +840,7 @@
           return;
         }
 
-        const updatedStepRun = toStepRunRecord(await updateStepRun(ctx, stepRun.id, {
+        const updatedStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, stepRun.id, {
           completedAt: nextStepStatus === STEP_STATUSES.done ? new Date().toISOString() : undefined,
           startedAt: nextStepStatus === STEP_STATUSES.inProgress
             ? stepRun.data.startedAt ?? new Date().toISOString()
@@ -687,11 +870,21 @@
 
     ctx.events.on("issue.created", async (event: PluginEvent) => {
       try {
+        const idempotencyKey = buildIdempotencyKey(event);
+        if (await checkIdempotency(ctx, idempotencyKey, event.companyId)) {
+          return;
+        }
+
         const payload = event.payload as {
           id?: string;
           parentId?: string;
           assigneeAgentId?: string;
         };
+        const issueId = typeof event.entityId === "string" && event.entityId.trim()
+          ? event.entityId.trim()
+          : typeof payload.id === "string" && payload.id.trim()
+            ? payload.id.trim()
+            : "";
 
         const parentId = typeof payload.parentId === "string" && payload.parentId.trim()
           ? payload.parentId.trim()
@@ -700,12 +893,34 @@
           ? payload.assigneeAgentId.trim()
           : "";
 
-        if (parentId || !assigneeAgentId) {
+        if (!issueId || parentId || !assigneeAgentId) {
+          await markIdempotency(ctx, idempotencyKey, event.companyId);
+          return;
+        }
+
+        const agent = await ctx.agents.get(assigneeAgentId, event.companyId);
+        const metadata = agent?.metadata as Record<string, unknown> | undefined;
+        const defaultParentIssueId = typeof metadata?.defaultParentIssueId === "string" && metadata.defaultParentIssueId.trim()
+          ? metadata.defaultParentIssueId.trim()
+          : "";
+
+        if (!defaultParentIssueId) {
+          await markIdempotency(ctx, idempotencyKey, event.companyId);
           return;
         }
 
-        ctx.logger.info("parentId filler: issue created without parent", {
-          issueId: event.entityId ?? payload.id ?? null,
+        await ctx.issues.update(
+          issueId,
+          { parentId: defaultParentIssueId } as IssueUpdatePatch,
+          event.companyId,
+        );
+        await markIdempotency(ctx, idempotencyKey, event.companyId);
+
+        ctx.logger.info("parentId filler: populated issue parentId from agent metadata", {
+          assigneeAgentId,
+          companyId: event.companyId,
+          issueId,
+          parentId: defaultParentIssueId,
         });
       } catch (error) {
         ctx.logger.warn("Failed to handle issue.created event", {
@@ -744,7 +959,23 @@
       await runReconciler(ctx);
     });
 
-    ctx.data.register("workflow-overview", async (params: Record<string, unknown>) => {
+    registerDataHandler(ctx, "start-workflow", async (params: Record<string, unknown>) => {
+      const workflowId = typeof params.workflowId === "string" ? params.workflowId.trim() : "";
+      const companyId = typeof params.companyId === "string" ? params.companyId.trim() : "";
+      const parentIssueId = typeof params.parentIssueId === "string" ? params.parentIssueId.trim() : "";
+      const createParentIssue = params.createParentIssue === true;
+
+      if (!workflowId || !companyId) {
+        throw new Error("start-workflow requires workflowId and companyId");
+      }
+
+      return await startWorkflow(ctx, workflowId, companyId, {
+        createParentIssue,
+        parentIssueId: parentIssueId || undefined,
+      });
+    });
+
+    registerDataHandler(ctx, "workflow-overview", async (params: Record<string, unknown>) => {
       try {
         const companyId = typeof params.companyId === "string" ? params.companyId.trim() : "";
         if (!companyId) {
@@ -782,7 +1013,7 @@
       }
     });
 
-    ctx.data.register("workflow-run-detail", async (params: Record<string, unknown>) => {
+    registerDataHandler(ctx, "workflow-run-detail", async (params: Record<string, unknown>) => {
       try {
         const runId = typeof params.runId === "string" ? params.runId.trim() : "";
         if (!runId) {
@@ -807,7 +1038,7 @@
             ...typedWorkflowRun.data,
           },
           stepRuns: stepRuns.map((record: PluginEntityRecord) => {
-            const stepRun = toStepRunRecord(record);
+            const stepRun = toWorkflowStepRunRecord(record);
             return {
               id: stepRun.id,
               status: stepRun.status,
diff --git a/paperclip-addon/plugins/workflow-engine/src/workflow-store.ts b/paperclip-addon/plugins/workflow-engine/src/workflow-store.ts
index 5871a2082258cebcfe8b69ebafbb0814cf303d50..82eb46836883940e109368e927b7e367c96616b5
--- a/paperclip-addon/plugins/workflow-engine/src/workflow-store.ts
+++ b/paperclip-addon/plugins/workflow-engine/src/workflow-store.ts
@@ -44,7 +44,14 @@
 
 type TypedEntityRecord<T> = Omit<PluginEntityRecord, "data"> & { data: T };
 
-function toTypedRecord<T>(record: PluginEntityRecord): TypedEntityRecord<T> {
+function toTypedRecord<T>(
+  record: PluginEntityRecord,
+  entityType: string,
+): TypedEntityRecord<T> {
+  if (record.entityType !== entityType) {
+    throw new Error(`Expected entity type "${entityType}", got "${record.entityType}"`);
+  }
+
   return record as TypedEntityRecord<T>;
 }
 
@@ -67,13 +74,31 @@
   id: string,
   entityType: string,
 ): Promise<TypedEntityRecord<T> | null> {
-  const record = await ctx.entities.get(id);
+  const listQuery = {
+    entityType,
+    id,
+    limit: 1,
+  } as Record<string, unknown>;
 
-  if (!record || record.entityType !== entityType) {
+  const listed = await ctx.entities.list(
+    listQuery as Parameters<PluginContext["entities"]["list"]>[0],
+  );
+  const listedRecord = listed.find(
+    (record: PluginEntityRecord) => record.id === id,
+  ) ?? null;
+
+  if (listedRecord) {
+    return toTypedRecord<T>(listedRecord, entityType);
+  }
+
+  // Fallback: some host SDK variants ignore `id` on entities.list().
+  // Keep defensive type verification on direct get() until id-filter support is universal.
+  const record = await ctx.entities.get(id);
+  if (!record) {
     return null;
   }
 
-  return toTypedRecord<T>(record);
+  return toTypedRecord<T>(record, entityType);
 }
 
 async function requireEntityByType<T>(
diff --git a/paperclip-addon/plugins/workflow-engine/src/workflow-utils.ts b/paperclip-addon/plugins/workflow-engine/src/workflow-utils.ts
new file mode 100644
index 0000000000000000000000000000000000000000..ec1cf6f6b9aa722b126b58c0a728b4e538ad1494
--- /dev/null
+++ b/paperclip-addon/plugins/workflow-engine/src/workflow-utils.ts
@@ -0,0 +1,62 @@
+import type { PluginEntityRecord } from "@paperclipai/plugin-sdk";
+
+import { STEP_STATUSES } from "./constants.js";
+import type { WorkflowStep } from "./dag-engine.js";
+import type {
+  WorkflowDefinition,
+  WorkflowRun,
+  WorkflowStepRun,
+} from "./workflow-store.js";
+
+export type WorkflowRunRecord = PluginEntityRecord & { data: WorkflowRun };
+export type WorkflowStepRunRecord = PluginEntityRecord & { data: WorkflowStepRun };
+export type WorkflowDefinitionRecord = PluginEntityRecord & { data: WorkflowDefinition };
+type StepAgentMetadata = WorkflowStep & {
+  agent?: string;
+  agentName?: string;
+  assigneeAgentName?: string;
+};
+
+export const TERMINAL_STEP_STATUSES = new Set<string>([
+  STEP_STATUSES.done,
+  STEP_STATUSES.failed,
+  STEP_STATUSES.skipped,
+  STEP_STATUSES.escalated,
+]);
+
+export function toWorkflowRunRecord(record: PluginEntityRecord): WorkflowRunRecord {
+  return record as WorkflowRunRecord;
+}
+
+export function toWorkflowStepRunRecord(record: PluginEntityRecord): WorkflowStepRunRecord {
+  return record as WorkflowStepRunRecord;
+}
+
+export function toWorkflowDefinitionRecord(record: PluginEntityRecord): WorkflowDefinitionRecord {
+  return record as WorkflowDefinitionRecord;
+}
+
+export function findStepDefinition(definition: WorkflowDefinitionRecord, stepId: string): WorkflowStep | null {
+  return definition.data.steps.find((step: WorkflowStep) => step.id === stepId) ?? null;
+}
+
+export function getStepAgentNameHint(stepDef: WorkflowStep): string | null {
+  const stepMeta = stepDef as StepAgentMetadata;
+  const candidates = [stepMeta.agentName, stepMeta.agent, stepMeta.assigneeAgentName];
+
+  for (const candidate of candidates) {
+    if (typeof candidate === "string" && candidate.trim()) {
+      return candidate.trim();
+    }
+  }
+
+  return null;
+}
+
+export function getStepAgentName(stepRun: WorkflowStepRunRecord, stepDef: WorkflowStep): string | null {
+  if (typeof stepRun.data.agentName === "string" && stepRun.data.agentName.trim()) {
+    return stepRun.data.agentName.trim();
+  }
+
+  return getStepAgentNameHint(stepDef);
+}

exec
/bin/zsh -lc "sed -n '980,1120p' plugins/workflow-engine/src/worker.ts" in /Users/kwak/Projects/paperclip/paperclip-addon succeeded in 0ms:
        const companyId = typeof params.companyId === "string" ? params.companyId.trim() : "";
        if (!companyId) {
          return { workflows: [], activeRuns: [] };
        }

        const [workflowDefinitions, activeRuns] = await Promise.all([
          listWorkflowDefinitions(ctx, companyId),
          listActiveRuns(ctx, companyId),
        ]);

        return {
          activeRuns: activeRuns.map((record) => {
            const run = toWorkflowRunRecord(record);
            return {
              id: run.id,
              status: run.status,
              ...run.data,
            };
          }),
          workflows: workflowDefinitions.map((record) => {
file update:
diff --git a/paperclip-addon/plugins/workflow-engine/src/dag-engine.ts b/paperclip-addon/plugins/workflow-engine/src/dag-engine.ts
index b59510f0473171038be77277286d3b5c2063e6c5..e7f21ff998cab78b9325a8aeacf4fda9c5e06553
--- a/paperclip-addon/plugins/workflow-engine/src/dag-engine.ts
+++ b/paperclip-addon/plugins/workflow-engine/src/dag-engine.ts
@@ -2,6 +2,7 @@
   id: string;
   title: string;
   dependsOn: string[];
+  sessionMode?: "fresh" | "reuse";
   onFailure?: "retry" | "skip" | "abort_workflow" | "escalate";
   escalateTo?: string;
   maxRetries?: number;
diff --git a/paperclip-addon/plugins/workflow-engine/src/reconciler.ts b/paperclip-addon/plugins/workflow-engine/src/reconciler.ts
index 14ab4e766a835d145a6c2e7b1b564270285cbdc3..d2ab54817bd8185944379d2188bfbdef1722020a
--- a/paperclip-addon/plugins/workflow-engine/src/reconciler.ts
+++ b/paperclip-addon/plugins/workflow-engine/src/reconciler.ts
@@ -8,65 +8,22 @@
   listStepRuns,
   updateStepRun,
   updateWorkflowRun,
-  type WorkflowDefinition,
-  type WorkflowRun,
-  type WorkflowStepRun,
 } from "./workflow-store.js";
 import type { WorkflowStep } from "./dag-engine.js";
-
-type WorkflowRunRecord = PluginEntityRecord & { data: WorkflowRun };
-type WorkflowStepRunRecord = PluginEntityRecord & { data: WorkflowStepRun };
-type WorkflowDefinitionRecord = PluginEntityRecord & { data: WorkflowDefinition };
-type StepAgentMetadata = WorkflowStep & {
-  agent?: string;
-  agentName?: string;
-  assigneeAgentName?: string;
-};
+import {
+  TERMINAL_STEP_STATUSES,
+  findStepDefinition,
+  getStepAgentName,
+  toWorkflowDefinitionRecord,
+  toWorkflowRunRecord,
+  toWorkflowStepRunRecord,
+} from "./workflow-utils.js";
 
 const STALE_THRESHOLD_MS = 300_000;
-const TERMINAL_STEP_STATUSES = new Set<string>([
-  STEP_STATUSES.done,
-  STEP_STATUSES.failed,
-  STEP_STATUSES.skipped,
-  STEP_STATUSES.escalated,
-]);
 
 function isStale(record: PluginEntityRecord, thresholdMs: number): boolean {
   const updatedAt = new Date(record.updatedAt).getTime();
   return Date.now() - updatedAt > thresholdMs;
-}
-
-function toWorkflowRunRecord(record: PluginEntityRecord): WorkflowRunRecord {
-  return record as WorkflowRunRecord;
-}
-
-function toWorkflowStepRunRecord(record: PluginEntityRecord): WorkflowStepRunRecord {
-  return record as WorkflowStepRunRecord;
-}
-
-function toWorkflowDefinitionRecord(record: PluginEntityRecord): WorkflowDefinitionRecord {
-  return record as WorkflowDefinitionRecord;
-}
-
-function findStepDefinition(definition: WorkflowDefinitionRecord, stepId: string): WorkflowStep | null {
-  return definition.data.steps.find((step: WorkflowStep) => step.id === stepId) ?? null;
-}
-
-function getStepAgentName(stepRun: WorkflowStepRunRecord, stepDef: WorkflowStep): string | null {
-  if (typeof stepRun.data.agentName === "string" && stepRun.data.agentName.trim()) {
-    return stepRun.data.agentName.trim();
-  }
-
-  const stepMeta = stepDef as StepAgentMetadata;
-  const candidates = [stepMeta.agentName, stepMeta.agent, stepMeta.assigneeAgentName];
-
-  for (const candidate of candidates) {
-    if (typeof candidate === "string" && candidate.trim()) {
-      return candidate.trim();
-    }
-  }
-
-  return null;
 }
 
 async function invokeAgentByName(
diff --git a/paperclip-addon/plugins/workflow-engine/src/worker.ts b/paperclip-addon/plugins/workflow-engine/src/worker.ts
index c5ac3fbb4ca7f767eb29331bd3630b635449b335..303e35958890fa1f66db3f2b0ff1fa1f134102a8
--- a/paperclip-addon/plugins/workflow-engine/src/worker.ts
+++ b/paperclip-addon/plugins/workflow-engine/src/worker.ts
@@ -15,6 +15,7 @@
 } from "./dag-engine.js";
 import {
   checkIdempotency,
+  createWorkflowRun,
   createStepRun,
   findStepRunByIssueId,
   getWorkflowDefinition,
@@ -25,51 +26,38 @@
   markIdempotency,
   updateStepRun,
   updateWorkflowRun,
-  type WorkflowDefinition,
-  type WorkflowRun,
   type WorkflowStepRun,
 } from "./workflow-store.js";
+import {
+  TERMINAL_STEP_STATUSES,
+  findStepDefinition,
+  getStepAgentName,
+  getStepAgentNameHint,
+  toWorkflowDefinitionRecord,
+  toWorkflowRunRecord,
+  toWorkflowStepRunRecord,
+  type WorkflowDefinitionRecord,
+  type WorkflowRunRecord,
+  type WorkflowStepRunRecord,
+} from "./workflow-utils.js";
 
-type WorkflowStepRecord = PluginEntityRecord & { data: WorkflowStepRun };
-type WorkflowRunRecord = PluginEntityRecord & { data: WorkflowRun };
-type WorkflowDefinitionRecord = PluginEntityRecord & { data: WorkflowDefinition };
 type WorkflowStepMetadata = WorkflowStep & {
   description?: string;
-  agent?: string;
-  agentName?: string;
-  assigneeAgentName?: string;
   assigneeAgentId?: string;
+  sessionMode?: "fresh" | "reuse";
 };
 type AgentRecord = Awaited<ReturnType<PluginContext["agents"]["list"]>>[number];
+type IssueUpdatePatch = Parameters<PluginContext["issues"]["update"]>[1];
 type ReconcilerModule = {
   reconcileStuckSteps?: (ctx: PluginContext) => Promise<void>;
 };
 
-const TERMINAL_STEP_STATUSES = new Set<string>([
-  STEP_STATUSES.done,
-  STEP_STATUSES.failed,
-  STEP_STATUSES.skipped,
-  STEP_STATUSES.escalated,
-]);
-
 function summarizeError(error: unknown): string {
   if (error instanceof Error) {
     return error.message;
   }
 
   return String(error);
-}
-
-function toStepRunRecord(record: PluginEntityRecord): WorkflowStepRecord {
-  return record as WorkflowStepRecord;
-}
-
-function toWorkflowRunRecord(record: PluginEntityRecord): WorkflowRunRecord {
-  return record as WorkflowRunRecord;
-}
-
-function toWorkflowDefinitionRecord(record: PluginEntityRecord): WorkflowDefinitionRecord {
-  return record as WorkflowDefinitionRecord;
 }
 
 function buildIdempotencyKey(event: PluginEvent): string {
@@ -78,10 +66,6 @@
 
 function getStepMetadata(stepDef: WorkflowStep): WorkflowStepMetadata {
   return stepDef as WorkflowStepMetadata;
-}
-
-function findStepDefinition(definition: WorkflowDefinitionRecord, stepId: string): WorkflowStep | null {
-  return definition.data.steps.find((step: WorkflowStep) => step.id === stepId) ?? null;
 }
 
 function getStepDescription(stepDef: WorkflowStep): string | undefined {
@@ -89,19 +73,6 @@
   return typeof description === "string" && description.trim() ? description.trim() : undefined;
 }
 
-function getStepAgentNameHint(stepDef: WorkflowStep): string | null {
-  const meta = getStepMetadata(stepDef);
-  const candidates = [meta.agentName, meta.agent, meta.assigneeAgentName];
-
-  for (const candidate of candidates) {
-    if (typeof candidate === "string" && candidate.trim()) {
-      return candidate.trim();
-    }
-  }
-
-  return null;
-}
-
 function getStepAgentIdHint(stepDef: WorkflowStep): string | null {
   const assigneeAgentId = getStepMetadata(stepDef).assigneeAgentId;
   if (typeof assigneeAgentId !== "string" || !assigneeAgentId.trim()) {
@@ -147,68 +118,256 @@
 
 async function invokeAgentForStep(
   ctx: PluginContext,
+  stepRunRecord: WorkflowStepRunRecord,
   stepDef: WorkflowStep,
-  agentName: string,
   workflowName: string,
   companyId: string,
-): Promise<void> {
+): Promise<WorkflowStepRunRecord> {
+  const agentName = getStepAgentName(stepRunRecord, stepDef);
+  if (!agentName) {
+    ctx.logger.warn("Agent name is missing for workflow step", {
+      companyId,
+      runId: stepRunRecord.data.runId,
+      stepId: stepDef.id,
+      workflowName,
+    });
+    return stepRunRecord;
+  }
+
   const agents = await ctx.agents.list({ companyId });
-  const agent = agents.find((candidate: AgentRecord) => candidate.name === agentName);
+  const agent = agents.find((candidate: AgentRecord) => candidate.name === agentName) ?? null;
 
   if (!agent) {
     ctx.logger.warn("Agent not found for step", {
       agentName,
       companyId,
+      runId: stepRunRecord.data.runId,
+      stepId: stepDef.id,
+      workflowName,
+    });
+    return stepRunRecord;
+  }
+
+  const prompt = `workflow:${workflowName}/step:${stepDef.id} — "${stepDef.title}" is ready. Please proceed with the assigned task.`;
+  const reason = `workflow:${workflowName}/step:${stepDef.id}`;
+  const sessionMode = getStepMetadata(stepDef).sessionMode === "reuse" ? "reuse" : "fresh";
+
+  if (sessionMode === "reuse") {
+    let sessionId = typeof stepRunRecord.data.sessionId === "string" && stepRunRecord.data.sessionId.trim()
+      ? stepRunRecord.data.sessionId.trim()
+      : "";
+
+    if (!sessionId) {
+      const session = await ctx.agents.sessions.create(agent.id, companyId, {
+        reason,
+      });
+      sessionId = session.sessionId;
+      stepRunRecord = toWorkflowStepRunRecord(await updateStepRun(ctx, stepRunRecord.id, {
+        sessionId,
+      }));
+    }
+
+    await ctx.agents.sessions.sendMessage(sessionId, companyId, {
+      prompt,
+      reason,
+    });
+
+    ctx.logger.info("Sent workflow step prompt via agent session", {
+      agentId: agent.id,
+      agentName,
+      companyId,
+      runId: stepRunRecord.data.runId,
+      sessionId,
       stepId: stepDef.id,
       workflowName,
     });
-    return;
+    return stepRunRecord;
   }
 
   await ctx.agents.invoke(agent.id, companyId, {
-    prompt: `workflow:${workflowName}/step:${stepDef.id} — "${stepDef.title}" is ready. Please proceed with the assigned task.`,
-    reason: `workflow:${workflowName}/step:${stepDef.id}`,
+    prompt,
+    reason,
   });
 
   ctx.logger.info("Invoked agent for workflow step", {
     agentId: agent.id,
     agentName,
     companyId,
+    runId: stepRunRecord.data.runId,
     stepId: stepDef.id,
     workflowName,
   });
+
+  return stepRunRecord;
 }
 
 async function activateBacklogStep(
   ctx: PluginContext,
-  stepRunRecord: WorkflowStepRecord,
+  stepRunRecord: WorkflowStepRunRecord,
   stepDef: WorkflowStep,
   workflowName: string,
   companyId: string,
-): Promise<void> {
+  options?: { parentIssueId?: string },
+): Promise<WorkflowStepRunRecord> {
   if (stepRunRecord.data.status !== STEP_STATUSES.backlog) {
-    return;
+    return stepRunRecord;
   }
 
-  const updated = toStepRunRecord(await updateStepRun(ctx, stepRunRecord.id, {
+  const resolvedAgent = await resolveStepAgent(
+    ctx,
+    companyId,
+    stepDef,
+    stepRunRecord.data.agentName,
+  );
+
+  let issueId = typeof stepRunRecord.data.issueId === "string" && stepRunRecord.data.issueId.trim()
+    ? stepRunRecord.data.issueId.trim()
+    : "";
+  const stepDescription = getStepDescription(stepDef) ?? `Workflow step: ${stepDef.id}`;
+
+  if (!issueId) {
+    const issue = await ctx.issues.create({
+      assigneeAgentId: resolvedAgent.agentId ?? undefined,
+      companyId,
+      description: stepDescription,
+      parentId: options?.parentIssueId,
+      title: `[${workflowName}] ${stepDef.title}`,
+    });
+    await ctx.issues.update(issue.id, { status: "todo" } as IssueUpdatePatch, companyId);
+    issueId = issue.id;
+  }
+
+  const nextStartedAt = stepRunRecord.data.startedAt ?? new Date().toISOString();
+  let updatedStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, stepRunRecord.id, {
+    agentName: resolvedAgent.agentName ?? stepRunRecord.data.agentName,
+    issueId,
+    startedAt: nextStartedAt,
     status: STEP_STATUSES.todo,
   }));
 
-  await invokeAgentForStep(
+  updatedStepRun = await invokeAgentForStep(
     ctx,
+    updatedStepRun,
     stepDef,
-    updated.data.agentName,
     workflowName,
     companyId,
   );
+
+  return updatedStepRun;
 }
 
+async function startWorkflow(
+  ctx: PluginContext,
+  workflowId: string,
+  companyId: string,
+  options?: {
+    createParentIssue?: boolean;
+    parentIssueId?: string;
+  },
+): Promise<{
+  activatedStepIds: string[];
+  parentIssueId: string | null;
+  runId: string;
+  workflowId: string;
+}> {
+  const workflowDefinition = await getWorkflowDefinition(ctx, workflowId);
+  if (!workflowDefinition) {
+    throw new Error(`Workflow definition not found: ${workflowId}`);
+  }
+
+  const typedWorkflowDefinition = toWorkflowDefinitionRecord(workflowDefinition);
+  if (typedWorkflowDefinition.data.companyId !== companyId) {
+    throw new Error(`Workflow does not belong to company: ${workflowId}`);
+  }
+
+  let parentIssueId = typeof options?.parentIssueId === "string" && options.parentIssueId.trim()
+    ? options.parentIssueId.trim()
+    : "";
+  if (!parentIssueId && options?.createParentIssue) {
+    const parentIssue = await ctx.issues.create({
+      companyId,
+      description: typedWorkflowDefinition.data.description || `Workflow run: ${typedWorkflowDefinition.data.name}`,
+      title: `[${typedWorkflowDefinition.data.name}] Workflow run`,
+    });
+    parentIssueId = parentIssue.id;
+  }
+
+  const workflowRun = toWorkflowRunRecord(await createWorkflowRun(ctx, {
+    companyId,
+    parentIssueId: parentIssueId || undefined,
+    startedAt: new Date().toISOString(),
+    status: RUN_STATUSES.running,
+    workflowId: typedWorkflowDefinition.id,
+    workflowName: typedWorkflowDefinition.data.name,
+  }));
+
+  const agents = await ctx.agents.list({ companyId });
+  const agentsByName = new Map<string, AgentRecord>();
+  for (const agent of agents) {
+    agentsByName.set(agent.name, agent);
+  }
+
+  const pendingRootSteps: Array<{ stepDef: WorkflowStep; stepRun: WorkflowStepRunRecord }> = [];
+  for (const stepDef of typedWorkflowDefinition.data.steps) {
+    const agentNameHint = getStepAgentNameHint(stepDef);
+    const matchedAgent = agentNameHint ? agentsByName.get(agentNameHint) ?? null : null;
+    const resolvedAgent = matchedAgent
+      ? { agentId: matchedAgent.id, agentName: matchedAgent.name }
+      : await resolveStepAgent(ctx, companyId, stepDef, agentNameHint ?? undefined);
+
+    if (!resolvedAgent.agentName) {
+      throw new Error(`Unable to resolve step assignee for "${stepDef.id}"`);
+    }
+
+    const stepRun = toWorkflowStepRunRecord(await createStepRun(ctx, companyId, {
+      agentName: resolvedAgent.agentName,
+      retryCount: 0,
+      runId: workflowRun.id,
+      status: STEP_STATUSES.backlog,
+      stepId: stepDef.id,
+    }));
+
+    if (stepDef.dependsOn.length === 0 && stepDef.triggerOn !== "escalation") {
+      pendingRootSteps.push({ stepDef, stepRun });
+    }
+  }
+
+  const activatedStepIds: string[] = [];
+  for (const pending of pendingRootSteps) {
+    await activateBacklogStep(
+      ctx,
+      pending.stepRun,
+      pending.stepDef,
+      typedWorkflowDefinition.data.name,
+      companyId,
+      { parentIssueId: parentIssueId || undefined },
+    );
+    activatedStepIds.push(pending.stepDef.id);
+  }
+
+  ctx.logger.info("Started workflow run", {
+    activatedStepIds,
+    companyId,
+    parentIssueId: parentIssueId || null,
+    runId: workflowRun.id,
+    workflowId: typedWorkflowDefinition.id,
+    workflowName: typedWorkflowDefinition.data.name,
+  });
+
+  return {
+    activatedStepIds,
+    parentIssueId: parentIssueId || null,
+    runId: workflowRun.id,
+    workflowId: typedWorkflowDefinition.id,
+  };
+}
+
 async function advanceWorkflow(
   ctx: PluginContext,
   stepRunRecord: PluginEntityRecord,
   companyId: string,
 ): Promise<void> {
-  const completedStepRun = toStepRunRecord(stepRunRecord);
+  const completedStepRun = toWorkflowStepRunRecord(stepRunRecord);
   const workflowRun = await getWorkflowRun(ctx, completedStepRun.data.runId);
 
   if (!workflowRun) {
@@ -237,7 +396,7 @@
   }
 
   const typedWorkflowDefinition = toWorkflowDefinitionRecord(workflowDefinition);
-  const stepRuns = (await listStepRuns(ctx, typedWorkflowRun.id, companyId)).map(toStepRunRecord);
+  const stepRuns = (await listStepRuns(ctx, typedWorkflowRun.id, companyId)).map(toWorkflowStepRunRecord);
   const completed = new Set<string>();
   const failed = new Set<string>();
   const skipped = new Set<string>();
@@ -289,7 +448,7 @@
         continue;
       }
 
-      stepRun = toStepRunRecord(await createStepRun(ctx, companyId, {
+      stepRun = toWorkflowStepRunRecord(await createStepRun(ctx, companyId, {
         agentName: resolvedAgent.agentName,
         retryCount: 0,
         runId: typedWorkflowRun.id,
@@ -305,6 +464,7 @@
       stepDef,
       typedWorkflowRun.data.workflowName,
       companyId,
+      { parentIssueId: typedWorkflowRun.data.parentIssueId },
     );
   }
 
@@ -329,7 +489,7 @@
   ctx: PluginContext,
   workflowRun: WorkflowRunRecord,
   workflowDefinition: WorkflowDefinitionRecord,
-  sourceStepRun: WorkflowStepRecord,
+  sourceStepRun: WorkflowStepRunRecord,
   escalationTargetId: string,
   companyId: string,
 ): Promise<void> {
@@ -338,7 +498,7 @@
     throw new Error(`Escalation target step not found: ${escalationTargetId}`);
   }
 
-  const stepRuns = (await listStepRuns(ctx, workflowRun.id, companyId)).map(toStepRunRecord);
+  const stepRuns = (await listStepRuns(ctx, workflowRun.id, companyId)).map(toWorkflowStepRunRecord);
   let escalationStepRun = stepRuns.find((candidate) => candidate.data.stepId === escalationTargetId) ?? null;
   const resolvedAgent = await resolveStepAgent(
     ctx,
@@ -352,7 +512,7 @@
   }
 
   if (!escalationStepRun) {
-    escalationStepRun = toStepRunRecord(await createStepRun(ctx, companyId, {
+    escalationStepRun = toWorkflowStepRunRecord(await createStepRun(ctx, companyId, {
       agentName: resolvedAgent.agentName,
       retryCount: 0,
       runId: workflowRun.id,
@@ -374,14 +534,14 @@
       title: `${workflowRun.data.workflowName}: ${escalationStep.title}`,
     });
 
-    escalationStepRun = toStepRunRecord(await updateStepRun(ctx, escalationStepRun.id, {
+    escalationStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, escalationStepRun.id, {
       issueId: issue.id,
     }));
   }
 
   const shouldActivate = escalationStepRun.data.status === STEP_STATUSES.backlog;
   if (shouldActivate) {
-    escalationStepRun = toStepRunRecord(await updateStepRun(ctx, escalationStepRun.id, {
+    escalationStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, escalationStepRun.id, {
       status: STEP_STATUSES.todo,
     }));
   }
@@ -389,8 +549,8 @@
   if (shouldActivate) {
     await invokeAgentForStep(
       ctx,
+      escalationStepRun,
       escalationStep,
-      escalationStepRun.data.agentName,
       workflowRun.data.workflowName,
       companyId,
     );
@@ -421,7 +581,7 @@
     return;
   }
 
-  const typedStepRun = toStepRunRecord(stepRunRecord);
+  const typedStepRun = toWorkflowStepRunRecord(stepRunRecord);
   if (TERMINAL_STEP_STATUSES.has(typedStepRun.data.status)) {
     return;
   }
@@ -469,7 +629,7 @@
   if (options.allowRetry) {
     const retryInfo = getRetryInfo(typedWorkflowDefinition.data.steps, typedStepRun.data.stepId);
     if (retryInfo.shouldRetry && typedStepRun.data.retryCount < retryInfo.maxRetries) {
-      const retriedStepRun = toStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
+      const retriedStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
         completedAt: undefined,
         retryCount: typedStepRun.data.retryCount + 1,
         status: STEP_STATUSES.todo,
@@ -477,8 +637,8 @@
 
       await invokeAgentForStep(
         ctx,
+        retriedStepRun,
         stepDef,
-        retriedStepRun.data.agentName,
         typedWorkflowRun.data.workflowName,
         event.companyId,
       );
@@ -496,7 +656,7 @@
   }
 
   if (stepDef.onFailure === "skip") {
-    const skippedStepRun = toStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
+    const skippedStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
       completedAt: new Date().toISOString(),
       status: STEP_STATUSES.skipped,
     }));
@@ -556,7 +716,7 @@
       return;
     }
 
-    const escalatedStepRun = toStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
+    const escalatedStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
       completedAt: new Date().toISOString(),
       status: STEP_STATUSES.escalated,
     }));
@@ -617,6 +777,29 @@
   }
 }
 
+function registerDataHandler(
+  ctx: PluginContext,
+  key: string,
+  handler: (params: Record<string, unknown>) => Promise<unknown>,
+): void {
+  const dataClient = ctx.data as {
+    handle?: (handlerKey: string, handlerFn: (params: Record<string, unknown>) => Promise<unknown>) => void;
+    register?: (handlerKey: string, handlerFn: (params: Record<string, unknown>) => Promise<unknown>) => void;
+  };
+
+  if (typeof dataClient.handle === "function") {
+    dataClient.handle(key, handler);
+    return;
+  }
+
+  if (typeof dataClient.register === "function") {
+    dataClient.register(key, handler);
+    return;
+  }
+
+  throw new Error("Plugin data client does not support handler registration");
+}
+
 const plugin = definePlugin({
   async setup(ctx: PluginContext) {
     ctx.events.on("issue.updated", async (event: PluginEvent) => {
@@ -638,7 +821,7 @@
           return;
         }
 
-        const stepRun = toStepRunRecord(stepRunRecord);
+        const stepRun = toWorkflowStepRunRecord(stepRunRecord);
         const payload = event.payload as { status?: string };
         const issueStatus = typeof payload.status === "string" ? payload.status : undefined;
 
@@ -657,7 +840,7 @@
           return;
         }
 
-        const updatedStepRun = toStepRunRecord(await updateStepRun(ctx, stepRun.id, {
+        const updatedStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, stepRun.id, {
           completedAt: nextStepStatus === STEP_STATUSES.done ? new Date().toISOString() : undefined,
           startedAt: nextStepStatus === STEP_STATUSES.inProgress
             ? stepRun.data.startedAt ?? new Date().toISOString()
@@ -687,11 +870,21 @@
 
     ctx.events.on("issue.created", async (event: PluginEvent) => {
       try {
+        const idempotencyKey = buildIdempotencyKey(event);
+        if (await checkIdempotency(ctx, idempotencyKey, event.companyId)) {
+          return;
+        }
+
         const payload = event.payload as {
           id?: string;
           parentId?: string;
           assigneeAgentId?: string;
         };
+        const issueId = typeof event.entityId === "string" && event.entityId.trim()
+          ? event.entityId.trim()
+          : typeof payload.id === "string" && payload.id.trim()
+            ? payload.id.trim()
+            : "";
 
         const parentId = typeof payload.parentId === "string" && payload.parentId.trim()
           ? payload.parentId.trim()
@@ -700,12 +893,34 @@
           ? payload.assigneeAgentId.trim()
           : "";
 
-        if (parentId || !assigneeAgentId) {
+        if (!issueId || parentId || !assigneeAgentId) {
+          await markIdempotency(ctx, idempotencyKey, event.companyId);
+          return;
+        }
+
+        const agent = await ctx.agents.get(assigneeAgentId, event.companyId);
+        const metadata = agent?.metadata as Record<string, unknown> | undefined;
+        const defaultParentIssueId = typeof metadata?.defaultParentIssueId === "string" && metadata.defaultParentIssueId.trim()
+          ? metadata.defaultParentIssueId.trim()
+          : "";
+
+        if (!defaultParentIssueId) {
+          await markIdempotency(ctx, idempotencyKey, event.companyId);
           return;
         }
 
-        ctx.logger.info("parentId filler: issue created without parent", {
-          issueId: event.entityId ?? payload.id ?? null,
+        await ctx.issues.update(
+          issueId,
+          { parentId: defaultParentIssueId } as IssueUpdatePatch,
+          event.companyId,
+        );
+        await markIdempotency(ctx, idempotencyKey, event.companyId);
+
+        ctx.logger.info("parentId filler: populated issue parentId from agent metadata", {
+          assigneeAgentId,
+          companyId: event.companyId,
+          issueId,
+          parentId: defaultParentIssueId,
         });
       } catch (error) {
         ctx.logger.warn("Failed to handle issue.created event", {
@@ -744,7 +959,23 @@
       await runReconciler(ctx);
     });
 
-    ctx.data.register("workflow-overview", async (params: Record<string, unknown>) => {
+    registerDataHandler(ctx, "start-workflow", async (params: Record<string, unknown>) => {
+      const workflowId = typeof params.workflowId === "string" ? params.workflowId.trim() : "";
+      const companyId = typeof params.companyId === "string" ? params.companyId.trim() : "";
+      const parentIssueId = typeof params.parentIssueId === "string" ? params.parentIssueId.trim() : "";
+      const createParentIssue = params.createParentIssue === true;
+
+      if (!workflowId || !companyId) {
+        throw new Error("start-workflow requires workflowId and companyId");
+      }
+
+      return await startWorkflow(ctx, workflowId, companyId, {
+        createParentIssue,
+        parentIssueId: parentIssueId || undefined,
+      });
+    });
+
+    registerDataHandler(ctx, "workflow-overview", async (params: Record<string, unknown>) => {
       try {
         const companyId = typeof params.companyId === "string" ? params.companyId.trim() : "";
         if (!companyId) {
@@ -782,7 +1013,7 @@
       }
     });
 
-    ctx.data.register("workflow-run-detail", async (params: Record<string, unknown>) => {
+    registerDataHandler(ctx, "workflow-run-detail", async (params: Record<string, unknown>) => {
       try {
         const runId = typeof params.runId === "string" ? params.runId.trim() : "";
         if (!runId) {
@@ -807,7 +1038,7 @@
             ...typedWorkflowRun.data,
           },
           stepRuns: stepRuns.map((record: PluginEntityRecord) => {
-            const stepRun = toStepRunRecord(record);
+            const stepRun = toWorkflowStepRunRecord(record);
             return {
               id: stepRun.id,
               status: stepRun.status,
diff --git a/paperclip-addon/plugins/workflow-engine/src/workflow-store.ts b/paperclip-addon/plugins/workflow-engine/src/workflow-store.ts
index 5871a2082258cebcfe8b69ebafbb0814cf303d50..82eb46836883940e109368e927b7e367c96616b5
--- a/paperclip-addon/plugins/workflow-engine/src/workflow-store.ts
+++ b/paperclip-addon/plugins/workflow-engine/src/workflow-store.ts
@@ -44,7 +44,14 @@
 
 type TypedEntityRecord<T> = Omit<PluginEntityRecord, "data"> & { data: T };
 
-function toTypedRecord<T>(record: PluginEntityRecord): TypedEntityRecord<T> {
+function toTypedRecord<T>(
+  record: PluginEntityRecord,
+  entityType: string,
+): TypedEntityRecord<T> {
+  if (record.entityType !== entityType) {
+    throw new Error(`Expected entity type "${entityType}", got "${record.entityType}"`);
+  }
+
   return record as TypedEntityRecord<T>;
 }
 
@@ -67,13 +74,31 @@
   id: string,
   entityType: string,
 ): Promise<TypedEntityRecord<T> | null> {
-  const record = await ctx.entities.get(id);
+  const listQuery = {
+    entityType,
+    id,
+    limit: 1,
+  } as Record<string, unknown>;
 
-  if (!record || record.entityType !== entityType) {
+  const listed = await ctx.entities.list(
+    listQuery as Parameters<PluginContext["entities"]["list"]>[0],
+  );
+  const listedRecord = listed.find(
+    (record: PluginEntityRecord) => record.id === id,
+  ) ?? null;
+
+  if (listedRecord) {
+    return toTypedRecord<T>(listedRecord, entityType);
+  }
+
+  // Fallback: some host SDK variants ignore `id` on entities.list().
+  // Keep defensive type verification on direct get() until id-filter support is universal.
+  const record = await ctx.entities.get(id);
+  if (!record) {
     return null;
   }
 
-  return toTypedRecord<T>(record);
+  return toTypedRecord<T>(record, entityType);
 }
 
 async function requireEntityByType<T>(
diff --git a/paperclip-addon/plugins/workflow-engine/src/workflow-utils.ts b/paperclip-addon/plugins/workflow-engine/src/workflow-utils.ts
new file mode 100644
index 0000000000000000000000000000000000000000..ec1cf6f6b9aa722b126b58c0a728b4e538ad1494
--- /dev/null
+++ b/paperclip-addon/plugins/workflow-engine/src/workflow-utils.ts
@@ -0,0 +1,62 @@
+import type { PluginEntityRecord } from "@paperclipai/plugin-sdk";
+
+import { STEP_STATUSES } from "./constants.js";
+import type { WorkflowStep } from "./dag-engine.js";
+import type {
+  WorkflowDefinition,
+  WorkflowRun,
+  WorkflowStepRun,
+} from "./workflow-store.js";
+
+export type WorkflowRunRecord = PluginEntityRecord & { data: WorkflowRun };
+export type WorkflowStepRunRecord = PluginEntityRecord & { data: WorkflowStepRun };
+export type WorkflowDefinitionRecord = PluginEntityRecord & { data: WorkflowDefinition };
+type StepAgentMetadata = WorkflowStep & {
+  agent?: string;
+  agentName?: string;
+  assigneeAgentName?: string;
+};
+
+export const TERMINAL_STEP_STATUSES = new Set<string>([
+  STEP_STATUSES.done,
+  STEP_STATUSES.failed,
+  STEP_STATUSES.skipped,
+  STEP_STATUSES.escalated,
+]);
+
+export function toWorkflowRunRecord(record: PluginEntityRecord): WorkflowRunRecord {
+  return record as WorkflowRunRecord;
+}
+
+export function toWorkflowStepRunRecord(record: PluginEntityRecord): WorkflowStepRunRecord {
+  return record as WorkflowStepRunRecord;
+}
+
+export function toWorkflowDefinitionRecord(record: PluginEntityRecord): WorkflowDefinitionRecord {
+  return record as WorkflowDefinitionRecord;
+}
+
+export function findStepDefinition(definition: WorkflowDefinitionRecord, stepId: string): WorkflowStep | null {
+  return definition.data.steps.find((step: WorkflowStep) => step.id === stepId) ?? null;
+}
+
+export function getStepAgentNameHint(stepDef: WorkflowStep): string | null {
+  const stepMeta = stepDef as StepAgentMetadata;
+  const candidates = [stepMeta.agentName, stepMeta.agent, stepMeta.assigneeAgentName];
+
+  for (const candidate of candidates) {
+    if (typeof candidate === "string" && candidate.trim()) {
+      return candidate.trim();
+    }
+  }
+
+  return null;
+}
+
+export function getStepAgentName(stepRun: WorkflowStepRunRecord, stepDef: WorkflowStep): string | null {
+  if (typeof stepRun.data.agentName === "string" && stepRun.data.agentName.trim()) {
+    return stepRun.data.agentName.trim();
+  }
+
+  return getStepAgentNameHint(stepDef);
+}

exec
/bin/zsh -lc "sed -n '1,220p' plugins/workflow-engine/src/reconciler.ts" in /Users/kwak/Projects/paperclip/paperclip-addon succeeded in 0ms:
import type { PluginContext, PluginEntityRecord } from "@paperclipai/plugin-sdk";

import { ENTITY_TYPES, RUN_STATUSES, STEP_STATUSES } from "./constants.js";
import {
  getWorkflowDefinition,
  getWorkflowRun,
  listActiveRuns,
  listStepRuns,
  updateStepRun,
  updateWorkflowRun,
} from "./workflow-store.js";
import type { WorkflowStep } from "./dag-engine.js";
import {
  TERMINAL_STEP_STATUSES,
  findStepDefinition,
  getStepAgentName,
  toWorkflowDefinitionRecord,
  toWorkflowRunRecord,
  toWorkflowStepRunRecord,
} from "./workflow-utils.js";
file update:
diff --git a/paperclip-addon/plugins/workflow-engine/src/dag-engine.ts b/paperclip-addon/plugins/workflow-engine/src/dag-engine.ts
index b59510f0473171038be77277286d3b5c2063e6c5..e7f21ff998cab78b9325a8aeacf4fda9c5e06553
--- a/paperclip-addon/plugins/workflow-engine/src/dag-engine.ts
+++ b/paperclip-addon/plugins/workflow-engine/src/dag-engine.ts
@@ -2,6 +2,7 @@
   id: string;
   title: string;
   dependsOn: string[];
+  sessionMode?: "fresh" | "reuse";
   onFailure?: "retry" | "skip" | "abort_workflow" | "escalate";
   escalateTo?: string;
   maxRetries?: number;
diff --git a/paperclip-addon/plugins/workflow-engine/src/reconciler.ts b/paperclip-addon/plugins/workflow-engine/src/reconciler.ts
index 14ab4e766a835d145a6c2e7b1b564270285cbdc3..d2ab54817bd8185944379d2188bfbdef1722020a
--- a/paperclip-addon/plugins/workflow-engine/src/reconciler.ts
+++ b/paperclip-addon/plugins/workflow-engine/src/reconciler.ts
@@ -8,65 +8,22 @@
   listStepRuns,
   updateStepRun,
   updateWorkflowRun,
-  type WorkflowDefinition,
-  type WorkflowRun,
-  type WorkflowStepRun,
 } from "./workflow-store.js";
 import type { WorkflowStep } from "./dag-engine.js";
-
-type WorkflowRunRecord = PluginEntityRecord & { data: WorkflowRun };
-type WorkflowStepRunRecord = PluginEntityRecord & { data: WorkflowStepRun };
-type WorkflowDefinitionRecord = PluginEntityRecord & { data: WorkflowDefinition };
-type StepAgentMetadata = WorkflowStep & {
-  agent?: string;
-  agentName?: string;
-  assigneeAgentName?: string;
-};
+import {
+  TERMINAL_STEP_STATUSES,
+  findStepDefinition,
+  getStepAgentName,
+  toWorkflowDefinitionRecord,
+  toWorkflowRunRecord,
+  toWorkflowStepRunRecord,
+} from "./workflow-utils.js";
 
 const STALE_THRESHOLD_MS = 300_000;
-const TERMINAL_STEP_STATUSES = new Set<string>([
-  STEP_STATUSES.done,
-  STEP_STATUSES.failed,
-  STEP_STATUSES.skipped,
-  STEP_STATUSES.escalated,
-]);
 
 function isStale(record: PluginEntityRecord, thresholdMs: number): boolean {
   const updatedAt = new Date(record.updatedAt).getTime();
   return Date.now() - updatedAt > thresholdMs;
-}
-
-function toWorkflowRunRecord(record: PluginEntityRecord): WorkflowRunRecord {
-  return record as WorkflowRunRecord;
-}
-
-function toWorkflowStepRunRecord(record: PluginEntityRecord): WorkflowStepRunRecord {
-  return record as WorkflowStepRunRecord;
-}
-
-function toWorkflowDefinitionRecord(record: PluginEntityRecord): WorkflowDefinitionRecord {
-  return record as WorkflowDefinitionRecord;
-}
-
-function findStepDefinition(definition: WorkflowDefinitionRecord, stepId: string): WorkflowStep | null {
-  return definition.data.steps.find((step: WorkflowStep) => step.id === stepId) ?? null;
-}
-
-function getStepAgentName(stepRun: WorkflowStepRunRecord, stepDef: WorkflowStep): string | null {
-  if (typeof stepRun.data.agentName === "string" && stepRun.data.agentName.trim()) {
-    return stepRun.data.agentName.trim();
-  }
-
-  const stepMeta = stepDef as StepAgentMetadata;
-  const candidates = [stepMeta.agentName, stepMeta.agent, stepMeta.assigneeAgentName];
-
-  for (const candidate of candidates) {
-    if (typeof candidate === "string" && candidate.trim()) {
-      return candidate.trim();
-    }
-  }
-
-  return null;
 }
 
 async function invokeAgentByName(
diff --git a/paperclip-addon/plugins/workflow-engine/src/worker.ts b/paperclip-addon/plugins/workflow-engine/src/worker.ts
index c5ac3fbb4ca7f767eb29331bd3630b635449b335..303e35958890fa1f66db3f2b0ff1fa1f134102a8
--- a/paperclip-addon/plugins/workflow-engine/src/worker.ts
+++ b/paperclip-addon/plugins/workflow-engine/src/worker.ts
@@ -15,6 +15,7 @@
 } from "./dag-engine.js";
 import {
   checkIdempotency,
+  createWorkflowRun,
   createStepRun,
   findStepRunByIssueId,
   getWorkflowDefinition,
@@ -25,51 +26,38 @@
   markIdempotency,
   updateStepRun,
   updateWorkflowRun,
-  type WorkflowDefinition,
-  type WorkflowRun,
   type WorkflowStepRun,
 } from "./workflow-store.js";
+import {
+  TERMINAL_STEP_STATUSES,
+  findStepDefinition,
+  getStepAgentName,
+  getStepAgentNameHint,
+  toWorkflowDefinitionRecord,
+  toWorkflowRunRecord,
+  toWorkflowStepRunRecord,
+  type WorkflowDefinitionRecord,
+  type WorkflowRunRecord,
+  type WorkflowStepRunRecord,
+} from "./workflow-utils.js";
 
-type WorkflowStepRecord = PluginEntityRecord & { data: WorkflowStepRun };
-type WorkflowRunRecord = PluginEntityRecord & { data: WorkflowRun };
-type WorkflowDefinitionRecord = PluginEntityRecord & { data: WorkflowDefinition };
 type WorkflowStepMetadata = WorkflowStep & {
   description?: string;
-  agent?: string;
-  agentName?: string;
-  assigneeAgentName?: string;
   assigneeAgentId?: string;
+  sessionMode?: "fresh" | "reuse";
 };
 type AgentRecord = Awaited<ReturnType<PluginContext["agents"]["list"]>>[number];
+type IssueUpdatePatch = Parameters<PluginContext["issues"]["update"]>[1];
 type ReconcilerModule = {
   reconcileStuckSteps?: (ctx: PluginContext) => Promise<void>;
 };
 
-const TERMINAL_STEP_STATUSES = new Set<string>([
-  STEP_STATUSES.done,
-  STEP_STATUSES.failed,
-  STEP_STATUSES.skipped,
-  STEP_STATUSES.escalated,
-]);
-
 function summarizeError(error: unknown): string {
   if (error instanceof Error) {
     return error.message;
   }
 
   return String(error);
-}
-
-function toStepRunRecord(record: PluginEntityRecord): WorkflowStepRecord {
-  return record as WorkflowStepRecord;
-}
-
-function toWorkflowRunRecord(record: PluginEntityRecord): WorkflowRunRecord {
-  return record as WorkflowRunRecord;
-}
-
-function toWorkflowDefinitionRecord(record: PluginEntityRecord): WorkflowDefinitionRecord {
-  return record as WorkflowDefinitionRecord;
 }
 
 function buildIdempotencyKey(event: PluginEvent): string {
@@ -78,10 +66,6 @@
 
 function getStepMetadata(stepDef: WorkflowStep): WorkflowStepMetadata {
   return stepDef as WorkflowStepMetadata;
-}
-
-function findStepDefinition(definition: WorkflowDefinitionRecord, stepId: string): WorkflowStep | null {
-  return definition.data.steps.find((step: WorkflowStep) => step.id === stepId) ?? null;
 }
 
 function getStepDescription(stepDef: WorkflowStep): string | undefined {
@@ -89,19 +73,6 @@
   return typeof description === "string" && description.trim() ? description.trim() : undefined;
 }
 
-function getStepAgentNameHint(stepDef: WorkflowStep): string | null {
-  const meta = getStepMetadata(stepDef);
-  const candidates = [meta.agentName, meta.agent, meta.assigneeAgentName];
-
-  for (const candidate of candidates) {
-    if (typeof candidate === "string" && candidate.trim()) {
-      return candidate.trim();
-    }
-  }
-
-  return null;
-}
-
 function getStepAgentIdHint(stepDef: WorkflowStep): string | null {
   const assigneeAgentId = getStepMetadata(stepDef).assigneeAgentId;
   if (typeof assigneeAgentId !== "string" || !assigneeAgentId.trim()) {
@@ -147,68 +118,256 @@
 
 async function invokeAgentForStep(
   ctx: PluginContext,
+  stepRunRecord: WorkflowStepRunRecord,
   stepDef: WorkflowStep,
-  agentName: string,
   workflowName: string,
   companyId: string,
-): Promise<void> {
+): Promise<WorkflowStepRunRecord> {
+  const agentName = getStepAgentName(stepRunRecord, stepDef);
+  if (!agentName) {
+    ctx.logger.warn("Agent name is missing for workflow step", {
+      companyId,
+      runId: stepRunRecord.data.runId,
+      stepId: stepDef.id,
+      workflowName,
+    });
+    return stepRunRecord;
+  }
+
   const agents = await ctx.agents.list({ companyId });
-  const agent = agents.find((candidate: AgentRecord) => candidate.name === agentName);
+  const agent = agents.find((candidate: AgentRecord) => candidate.name === agentName) ?? null;
 
   if (!agent) {
     ctx.logger.warn("Agent not found for step", {
       agentName,
       companyId,
+      runId: stepRunRecord.data.runId,
+      stepId: stepDef.id,
+      workflowName,
+    });
+    return stepRunRecord;
+  }
+
+  const prompt = `workflow:${workflowName}/step:${stepDef.id} — "${stepDef.title}" is ready. Please proceed with the assigned task.`;
+  const reason = `workflow:${workflowName}/step:${stepDef.id}`;
+  const sessionMode = getStepMetadata(stepDef).sessionMode === "reuse" ? "reuse" : "fresh";
+
+  if (sessionMode === "reuse") {
+    let sessionId = typeof stepRunRecord.data.sessionId === "string" && stepRunRecord.data.sessionId.trim()
+      ? stepRunRecord.data.sessionId.trim()
+      : "";
+
+    if (!sessionId) {
+      const session = await ctx.agents.sessions.create(agent.id, companyId, {
+        reason,
+      });
+      sessionId = session.sessionId;
+      stepRunRecord = toWorkflowStepRunRecord(await updateStepRun(ctx, stepRunRecord.id, {
+        sessionId,
+      }));
+    }
+
+    await ctx.agents.sessions.sendMessage(sessionId, companyId, {
+      prompt,
+      reason,
+    });
+
+    ctx.logger.info("Sent workflow step prompt via agent session", {
+      agentId: agent.id,
+      agentName,
+      companyId,
+      runId: stepRunRecord.data.runId,
+      sessionId,
       stepId: stepDef.id,
       workflowName,
     });
-    return;
+    return stepRunRecord;
   }
 
   await ctx.agents.invoke(agent.id, companyId, {
-    prompt: `workflow:${workflowName}/step:${stepDef.id} — "${stepDef.title}" is ready. Please proceed with the assigned task.`,
-    reason: `workflow:${workflowName}/step:${stepDef.id}`,
+    prompt,
+    reason,
   });
 
   ctx.logger.info("Invoked agent for workflow step", {
     agentId: agent.id,
     agentName,
     companyId,
+    runId: stepRunRecord.data.runId,
     stepId: stepDef.id,
     workflowName,
   });
+
+  return stepRunRecord;
 }
 
 async function activateBacklogStep(
   ctx: PluginContext,
-  stepRunRecord: WorkflowStepRecord,
+  stepRunRecord: WorkflowStepRunRecord,
   stepDef: WorkflowStep,
   workflowName: string,
   companyId: string,
-): Promise<void> {
+  options?: { parentIssueId?: string },
+): Promise<WorkflowStepRunRecord> {
   if (stepRunRecord.data.status !== STEP_STATUSES.backlog) {
-    return;
+    return stepRunRecord;
   }
 
-  const updated = toStepRunRecord(await updateStepRun(ctx, stepRunRecord.id, {
+  const resolvedAgent = await resolveStepAgent(
+    ctx,
+    companyId,
+    stepDef,
+    stepRunRecord.data.agentName,
+  );
+
+  let issueId = typeof stepRunRecord.data.issueId === "string" && stepRunRecord.data.issueId.trim()
+    ? stepRunRecord.data.issueId.trim()
+    : "";
+  const stepDescription = getStepDescription(stepDef) ?? `Workflow step: ${stepDef.id}`;
+
+  if (!issueId) {
+    const issue = await ctx.issues.create({
+      assigneeAgentId: resolvedAgent.agentId ?? undefined,
+      companyId,
+      description: stepDescription,
+      parentId: options?.parentIssueId,
+      title: `[${workflowName}] ${stepDef.title}`,
+    });
+    await ctx.issues.update(issue.id, { status: "todo" } as IssueUpdatePatch, companyId);
+    issueId = issue.id;
+  }
+
+  const nextStartedAt = stepRunRecord.data.startedAt ?? new Date().toISOString();
+  let updatedStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, stepRunRecord.id, {
+    agentName: resolvedAgent.agentName ?? stepRunRecord.data.agentName,
+    issueId,
+    startedAt: nextStartedAt,
     status: STEP_STATUSES.todo,
   }));
 
-  await invokeAgentForStep(
+  updatedStepRun = await invokeAgentForStep(
     ctx,
+    updatedStepRun,
     stepDef,
-    updated.data.agentName,
     workflowName,
     companyId,
   );
+
+  return updatedStepRun;
 }
 
+async function startWorkflow(
+  ctx: PluginContext,
+  workflowId: string,
+  companyId: string,
+  options?: {
+    createParentIssue?: boolean;
+    parentIssueId?: string;
+  },
+): Promise<{
+  activatedStepIds: string[];
+  parentIssueId: string | null;
+  runId: string;
+  workflowId: string;
+}> {
+  const workflowDefinition = await getWorkflowDefinition(ctx, workflowId);
+  if (!workflowDefinition) {
+    throw new Error(`Workflow definition not found: ${workflowId}`);
+  }
+
+  const typedWorkflowDefinition = toWorkflowDefinitionRecord(workflowDefinition);
+  if (typedWorkflowDefinition.data.companyId !== companyId) {
+    throw new Error(`Workflow does not belong to company: ${workflowId}`);
+  }
+
+  let parentIssueId = typeof options?.parentIssueId === "string" && options.parentIssueId.trim()
+    ? options.parentIssueId.trim()
+    : "";
+  if (!parentIssueId && options?.createParentIssue) {
+    const parentIssue = await ctx.issues.create({
+      companyId,
+      description: typedWorkflowDefinition.data.description || `Workflow run: ${typedWorkflowDefinition.data.name}`,
+      title: `[${typedWorkflowDefinition.data.name}] Workflow run`,
+    });
+    parentIssueId = parentIssue.id;
+  }
+
+  const workflowRun = toWorkflowRunRecord(await createWorkflowRun(ctx, {
+    companyId,
+    parentIssueId: parentIssueId || undefined,
+    startedAt: new Date().toISOString(),
+    status: RUN_STATUSES.running,
+    workflowId: typedWorkflowDefinition.id,
+    workflowName: typedWorkflowDefinition.data.name,
+  }));
+
+  const agents = await ctx.agents.list({ companyId });
+  const agentsByName = new Map<string, AgentRecord>();
+  for (const agent of agents) {
+    agentsByName.set(agent.name, agent);
+  }
+
+  const pendingRootSteps: Array<{ stepDef: WorkflowStep; stepRun: WorkflowStepRunRecord }> = [];
+  for (const stepDef of typedWorkflowDefinition.data.steps) {
+    const agentNameHint = getStepAgentNameHint(stepDef);
+    const matchedAgent = agentNameHint ? agentsByName.get(agentNameHint) ?? null : null;
+    const resolvedAgent = matchedAgent
+      ? { agentId: matchedAgent.id, agentName: matchedAgent.name }
+      : await resolveStepAgent(ctx, companyId, stepDef, agentNameHint ?? undefined);
+
+    if (!resolvedAgent.agentName) {
+      throw new Error(`Unable to resolve step assignee for "${stepDef.id}"`);
+    }
+
+    const stepRun = toWorkflowStepRunRecord(await createStepRun(ctx, companyId, {
+      agentName: resolvedAgent.agentName,
+      retryCount: 0,
+      runId: workflowRun.id,
+      status: STEP_STATUSES.backlog,
+      stepId: stepDef.id,
+    }));
+
+    if (stepDef.dependsOn.length === 0 && stepDef.triggerOn !== "escalation") {
+      pendingRootSteps.push({ stepDef, stepRun });
+    }
+  }
+
+  const activatedStepIds: string[] = [];
+  for (const pending of pendingRootSteps) {
+    await activateBacklogStep(
+      ctx,
+      pending.stepRun,
+      pending.stepDef,
+      typedWorkflowDefinition.data.name,
+      companyId,
+      { parentIssueId: parentIssueId || undefined },
+    );
+    activatedStepIds.push(pending.stepDef.id);
+  }
+
+  ctx.logger.info("Started workflow run", {
+    activatedStepIds,
+    companyId,
+    parentIssueId: parentIssueId || null,
+    runId: workflowRun.id,
+    workflowId: typedWorkflowDefinition.id,
+    workflowName: typedWorkflowDefinition.data.name,
+  });
+
+  return {
+    activatedStepIds,
+    parentIssueId: parentIssueId || null,
+    runId: workflowRun.id,
+    workflowId: typedWorkflowDefinition.id,
+  };
+}
+
 async function advanceWorkflow(
   ctx: PluginContext,
   stepRunRecord: PluginEntityRecord,
   companyId: string,
 ): Promise<void> {
-  const completedStepRun = toStepRunRecord(stepRunRecord);
+  const completedStepRun = toWorkflowStepRunRecord(stepRunRecord);
   const workflowRun = await getWorkflowRun(ctx, completedStepRun.data.runId);
 
   if (!workflowRun) {
@@ -237,7 +396,7 @@
   }
 
   const typedWorkflowDefinition = toWorkflowDefinitionRecord(workflowDefinition);
-  const stepRuns = (await listStepRuns(ctx, typedWorkflowRun.id, companyId)).map(toStepRunRecord);
+  const stepRuns = (await listStepRuns(ctx, typedWorkflowRun.id, companyId)).map(toWorkflowStepRunRecord);
   const completed = new Set<string>();
   const failed = new Set<string>();
   const skipped = new Set<string>();
@@ -289,7 +448,7 @@
         continue;
       }
 
-      stepRun = toStepRunRecord(await createStepRun(ctx, companyId, {
+      stepRun = toWorkflowStepRunRecord(await createStepRun(ctx, companyId, {
         agentName: resolvedAgent.agentName,
         retryCount: 0,
         runId: typedWorkflowRun.id,
@@ -305,6 +464,7 @@
       stepDef,
       typedWorkflowRun.data.workflowName,
       companyId,
+      { parentIssueId: typedWorkflowRun.data.parentIssueId },
     );
   }
 
@@ -329,7 +489,7 @@
   ctx: PluginContext,
   workflowRun: WorkflowRunRecord,
   workflowDefinition: WorkflowDefinitionRecord,
-  sourceStepRun: WorkflowStepRecord,
+  sourceStepRun: WorkflowStepRunRecord,
   escalationTargetId: string,
   companyId: string,
 ): Promise<void> {
@@ -338,7 +498,7 @@
     throw new Error(`Escalation target step not found: ${escalationTargetId}`);
   }
 
-  const stepRuns = (await listStepRuns(ctx, workflowRun.id, companyId)).map(toStepRunRecord);
+  const stepRuns = (await listStepRuns(ctx, workflowRun.id, companyId)).map(toWorkflowStepRunRecord);
   let escalationStepRun = stepRuns.find((candidate) => candidate.data.stepId === escalationTargetId) ?? null;
   const resolvedAgent = await resolveStepAgent(
     ctx,
@@ -352,7 +512,7 @@
   }
 
   if (!escalationStepRun) {
-    escalationStepRun = toStepRunRecord(await createStepRun(ctx, companyId, {
+    escalationStepRun = toWorkflowStepRunRecord(await createStepRun(ctx, companyId, {
       agentName: resolvedAgent.agentName,
       retryCount: 0,
       runId: workflowRun.id,
@@ -374,14 +534,14 @@
       title: `${workflowRun.data.workflowName}: ${escalationStep.title}`,
     });
 
-    escalationStepRun = toStepRunRecord(await updateStepRun(ctx, escalationStepRun.id, {
+    escalationStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, escalationStepRun.id, {
       issueId: issue.id,
     }));
   }
 
   const shouldActivate = escalationStepRun.data.status === STEP_STATUSES.backlog;
   if (shouldActivate) {
-    escalationStepRun = toStepRunRecord(await updateStepRun(ctx, escalationStepRun.id, {
+    escalationStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, escalationStepRun.id, {
       status: STEP_STATUSES.todo,
     }));
   }
@@ -389,8 +549,8 @@
   if (shouldActivate) {
     await invokeAgentForStep(
       ctx,
+      escalationStepRun,
       escalationStep,
-      escalationStepRun.data.agentName,
       workflowRun.data.workflowName,
       companyId,
     );
@@ -421,7 +581,7 @@
     return;
   }
 
-  const typedStepRun = toStepRunRecord(stepRunRecord);
+  const typedStepRun = toWorkflowStepRunRecord(stepRunRecord);
   if (TERMINAL_STEP_STATUSES.has(typedStepRun.data.status)) {
     return;
   }
@@ -469,7 +629,7 @@
   if (options.allowRetry) {
     const retryInfo = getRetryInfo(typedWorkflowDefinition.data.steps, typedStepRun.data.stepId);
     if (retryInfo.shouldRetry && typedStepRun.data.retryCount < retryInfo.maxRetries) {
-      const retriedStepRun = toStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
+      const retriedStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
         completedAt: undefined,
         retryCount: typedStepRun.data.retryCount + 1,
         status: STEP_STATUSES.todo,
@@ -477,8 +637,8 @@
 
       await invokeAgentForStep(
         ctx,
+        retriedStepRun,
         stepDef,
-        retriedStepRun.data.agentName,
         typedWorkflowRun.data.workflowName,
         event.companyId,
       );
@@ -496,7 +656,7 @@
   }
 
   if (stepDef.onFailure === "skip") {
-    const skippedStepRun = toStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
+    const skippedStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
       completedAt: new Date().toISOString(),
       status: STEP_STATUSES.skipped,
     }));
@@ -556,7 +716,7 @@
       return;
     }
 
-    const escalatedStepRun = toStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
+    const escalatedStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
       completedAt: new Date().toISOString(),
       status: STEP_STATUSES.escalated,
     }));
@@ -617,6 +777,29 @@
   }
 }
 
+function registerDataHandler(
+  ctx: PluginContext,
+  key: string,
+  handler: (params: Record<string, unknown>) => Promise<unknown>,
+): void {
+  const dataClient = ctx.data as {
+    handle?: (handlerKey: string, handlerFn: (params: Record<string, unknown>) => Promise<unknown>) => void;
+    register?: (handlerKey: string, handlerFn: (params: Record<string, unknown>) => Promise<unknown>) => void;
+  };
+
+  if (typeof dataClient.handle === "function") {
+    dataClient.handle(key, handler);
+    return;
+  }
+
+  if (typeof dataClient.register === "function") {
+    dataClient.register(key, handler);
+    return;
+  }
+
+  throw new Error("Plugin data client does not support handler registration");
+}
+
 const plugin = definePlugin({
   async setup(ctx: PluginContext) {
     ctx.events.on("issue.updated", async (event: PluginEvent) => {
@@ -638,7 +821,7 @@
           return;
         }
 
-        const stepRun = toStepRunRecord(stepRunRecord);
+        const stepRun = toWorkflowStepRunRecord(stepRunRecord);
         const payload = event.payload as { status?: string };
         const issueStatus = typeof payload.status === "string" ? payload.status : undefined;
 
@@ -657,7 +840,7 @@
           return;
         }
 
-        const updatedStepRun = toStepRunRecord(await updateStepRun(ctx, stepRun.id, {
+        const updatedStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, stepRun.id, {
           completedAt: nextStepStatus === STEP_STATUSES.done ? new Date().toISOString() : undefined,
           startedAt: nextStepStatus === STEP_STATUSES.inProgress
             ? stepRun.data.startedAt ?? new Date().toISOString()
@@ -687,11 +870,21 @@
 
     ctx.events.on("issue.created", async (event: PluginEvent) => {
       try {
+        const idempotencyKey = buildIdempotencyKey(event);
+        if (await checkIdempotency(ctx, idempotencyKey, event.companyId)) {
+          return;
+        }
+
         const payload = event.payload as {
           id?: string;
           parentId?: string;
           assigneeAgentId?: string;
         };
+        const issueId = typeof event.entityId === "string" && event.entityId.trim()
+          ? event.entityId.trim()
+          : typeof payload.id === "string" && payload.id.trim()
+            ? payload.id.trim()
+            : "";
 
         const parentId = typeof payload.parentId === "string" && payload.parentId.trim()
           ? payload.parentId.trim()
@@ -700,12 +893,34 @@
           ? payload.assigneeAgentId.trim()
           : "";
 
-        if (parentId || !assigneeAgentId) {
+        if (!issueId || parentId || !assigneeAgentId) {
+          await markIdempotency(ctx, idempotencyKey, event.companyId);
+          return;
+        }
+
+        const agent = await ctx.agents.get(assigneeAgentId, event.companyId);
+        const metadata = agent?.metadata as Record<string, unknown> | undefined;
+        const defaultParentIssueId = typeof metadata?.defaultParentIssueId === "string" && metadata.defaultParentIssueId.trim()
+          ? metadata.defaultParentIssueId.trim()
+          : "";
+
+        if (!defaultParentIssueId) {
+          await markIdempotency(ctx, idempotencyKey, event.companyId);
           return;
         }
 
-        ctx.logger.info("parentId filler: issue created without parent", {
-          issueId: event.entityId ?? payload.id ?? null,
+        await ctx.issues.update(
+          issueId,
+          { parentId: defaultParentIssueId } as IssueUpdatePatch,
+          event.companyId,
+        );
+        await markIdempotency(ctx, idempotencyKey, event.companyId);
+
+        ctx.logger.info("parentId filler: populated issue parentId from agent metadata", {
+          assigneeAgentId,
+          companyId: event.companyId,
+          issueId,
+          parentId: defaultParentIssueId,
         });
       } catch (error) {
         ctx.logger.warn("Failed to handle issue.created event", {
@@ -744,7 +959,23 @@
       await runReconciler(ctx);
     });
 
-    ctx.data.register("workflow-overview", async (params: Record<string, unknown>) => {
+    registerDataHandler(ctx, "start-workflow", async (params: Record<string, unknown>) => {
+      const workflowId = typeof params.workflowId === "string" ? params.workflowId.trim() : "";
+      const companyId = typeof params.companyId === "string" ? params.companyId.trim() : "";
+      const parentIssueId = typeof params.parentIssueId === "string" ? params.parentIssueId.trim() : "";
+      const createParentIssue = params.createParentIssue === true;
+
+      if (!workflowId || !companyId) {
+        throw new Error("start-workflow requires workflowId and companyId");
+      }
+
+      return await startWorkflow(ctx, workflowId, companyId, {
+        createParentIssue,
+        parentIssueId: parentIssueId || undefined,
+      });
+    });
+
+    registerDataHandler(ctx, "workflow-overview", async (params: Record<string, unknown>) => {
       try {
         const companyId = typeof params.companyId === "string" ? params.companyId.trim() : "";
         if (!companyId) {
@@ -782,7 +1013,7 @@
       }
     });
 
-    ctx.data.register("workflow-run-detail", async (params: Record<string, unknown>) => {
+    registerDataHandler(ctx, "workflow-run-detail", async (params: Record<string, unknown>) => {
       try {
         const runId = typeof params.runId === "string" ? params.runId.trim() : "";
         if (!runId) {
@@ -807,7 +1038,7 @@
             ...typedWorkflowRun.data,
           },
           stepRuns: stepRuns.map((record: PluginEntityRecord) => {
-            const stepRun = toStepRunRecord(record);
+            const stepRun = toWorkflowStepRunRecord(record);
             return {
               id: stepRun.id,
               status: stepRun.status,
diff --git a/paperclip-addon/plugins/workflow-engine/src/workflow-store.ts b/paperclip-addon/plugins/workflow-engine/src/workflow-store.ts
index 5871a2082258cebcfe8b69ebafbb0814cf303d50..82eb46836883940e109368e927b7e367c96616b5
--- a/paperclip-addon/plugins/workflow-engine/src/workflow-store.ts
+++ b/paperclip-addon/plugins/workflow-engine/src/workflow-store.ts
@@ -44,7 +44,14 @@
 
 type TypedEntityRecord<T> = Omit<PluginEntityRecord, "data"> & { data: T };
 
-function toTypedRecord<T>(record: PluginEntityRecord): TypedEntityRecord<T> {
+function toTypedRecord<T>(
+  record: PluginEntityRecord,
+  entityType: string,
+): TypedEntityRecord<T> {
+  if (record.entityType !== entityType) {
+    throw new Error(`Expected entity type "${entityType}", got "${record.entityType}"`);
+  }
+
   return record as TypedEntityRecord<T>;
 }
 
@@ -67,13 +74,31 @@
   id: string,
   entityType: string,
 ): Promise<TypedEntityRecord<T> | null> {
-  const record = await ctx.entities.get(id);
+  const listQuery = {
+    entityType,
+    id,
+    limit: 1,
+  } as Record<string, unknown>;
 
-  if (!record || record.entityType !== entityType) {
+  const listed = await ctx.entities.list(
+    listQuery as Parameters<PluginContext["entities"]["list"]>[0],
+  );
+  const listedRecord = listed.find(
+    (record: PluginEntityRecord) => record.id === id,
+  ) ?? null;
+
+  if (listedRecord) {
+    return toTypedRecord<T>(listedRecord, entityType);
+  }
+
+  // Fallback: some host SDK variants ignore `id` on entities.list().
+  // Keep defensive type verification on direct get() until id-filter support is universal.
+  const record = await ctx.entities.get(id);
+  if (!record) {
     return null;
   }
 
-  return toTypedRecord<T>(record);
+  return toTypedRecord<T>(record, entityType);
 }
 
 async function requireEntityByType<T>(
diff --git a/paperclip-addon/plugins/workflow-engine/src/workflow-utils.ts b/paperclip-addon/plugins/workflow-engine/src/workflow-utils.ts
new file mode 100644
index 0000000000000000000000000000000000000000..ec1cf6f6b9aa722b126b58c0a728b4e538ad1494
--- /dev/null
+++ b/paperclip-addon/plugins/workflow-engine/src/workflow-utils.ts
@@ -0,0 +1,62 @@
+import type { PluginEntityRecord } from "@paperclipai/plugin-sdk";
+
+import { STEP_STATUSES } from "./constants.js";
+import type { WorkflowStep } from "./dag-engine.js";
+import type {
+  WorkflowDefinition,
+  WorkflowRun,
+  WorkflowStepRun,
+} from "./workflow-store.js";
+
+export type WorkflowRunRecord = PluginEntityRecord & { data: WorkflowRun };
+export type WorkflowStepRunRecord = PluginEntityRecord & { data: WorkflowStepRun };
+export type WorkflowDefinitionRecord = PluginEntityRecord & { data: WorkflowDefinition };
+type StepAgentMetadata = WorkflowStep & {
+  agent?: string;
+  agentName?: string;
+  assigneeAgentName?: string;
+};
+
+export const TERMINAL_STEP_STATUSES = new Set<string>([
+  STEP_STATUSES.done,
+  STEP_STATUSES.failed,
+  STEP_STATUSES.skipped,
+  STEP_STATUSES.escalated,
+]);
+
+export function toWorkflowRunRecord(record: PluginEntityRecord): WorkflowRunRecord {
+  return record as WorkflowRunRecord;
+}
+
+export function toWorkflowStepRunRecord(record: PluginEntityRecord): WorkflowStepRunRecord {
+  return record as WorkflowStepRunRecord;
+}
+
+export function toWorkflowDefinitionRecord(record: PluginEntityRecord): WorkflowDefinitionRecord {
+  return record as WorkflowDefinitionRecord;
+}
+
+export function findStepDefinition(definition: WorkflowDefinitionRecord, stepId: string): WorkflowStep | null {
+  return definition.data.steps.find((step: WorkflowStep) => step.id === stepId) ?? null;
+}
+
+export function getStepAgentNameHint(stepDef: WorkflowStep): string | null {
+  const stepMeta = stepDef as StepAgentMetadata;
+  const candidates = [stepMeta.agentName, stepMeta.agent, stepMeta.assigneeAgentName];
+
+  for (const candidate of candidates) {
+    if (typeof candidate === "string" && candidate.trim()) {
+      return candidate.trim();
+    }
+  }
+
+  return null;
+}
+
+export function getStepAgentName(stepRun: WorkflowStepRunRecord, stepDef: WorkflowStep): string | null {
+  if (typeof stepRun.data.agentName === "string" && stepRun.data.agentName.trim()) {
+    return stepRun.data.agentName.trim();
+  }
+
+  return getStepAgentNameHint(stepDef);
+}

file update
M /Users/kwak/Projects/paperclip/paperclip-addon/plugins/workflow-engine/src/reconciler.ts
@@ -11,3 +11,2 @@
 } from "./workflow-store.js";
-import type { WorkflowStep } from "./dag-engine.js";
 import {
apply_patch(auto_approved=true) exited 0 in 122ms:
Success. Updated the following files:
M /Users/kwak/Projects/paperclip/paperclip-addon/plugins/workflow-engine/src/reconciler.ts
file update:
diff --git a/paperclip-addon/plugins/workflow-engine/src/dag-engine.ts b/paperclip-addon/plugins/workflow-engine/src/dag-engine.ts
index b59510f0473171038be77277286d3b5c2063e6c5..e7f21ff998cab78b9325a8aeacf4fda9c5e06553
--- a/paperclip-addon/plugins/workflow-engine/src/dag-engine.ts
+++ b/paperclip-addon/plugins/workflow-engine/src/dag-engine.ts
@@ -2,6 +2,7 @@
   id: string;
   title: string;
   dependsOn: string[];
+  sessionMode?: "fresh" | "reuse";
   onFailure?: "retry" | "skip" | "abort_workflow" | "escalate";
   escalateTo?: string;
   maxRetries?: number;
diff --git a/paperclip-addon/plugins/workflow-engine/src/reconciler.ts b/paperclip-addon/plugins/workflow-engine/src/reconciler.ts
index 14ab4e766a835d145a6c2e7b1b564270285cbdc3..55b649037e8a19a88bb39b9633a6a0cc84fa8d59
--- a/paperclip-addon/plugins/workflow-engine/src/reconciler.ts
+++ b/paperclip-addon/plugins/workflow-engine/src/reconciler.ts
@@ -8,65 +8,21 @@
   listStepRuns,
   updateStepRun,
   updateWorkflowRun,
-  type WorkflowDefinition,
-  type WorkflowRun,
-  type WorkflowStepRun,
 } from "./workflow-store.js";
-import type { WorkflowStep } from "./dag-engine.js";
-
-type WorkflowRunRecord = PluginEntityRecord & { data: WorkflowRun };
-type WorkflowStepRunRecord = PluginEntityRecord & { data: WorkflowStepRun };
-type WorkflowDefinitionRecord = PluginEntityRecord & { data: WorkflowDefinition };
-type StepAgentMetadata = WorkflowStep & {
-  agent?: string;
-  agentName?: string;
-  assigneeAgentName?: string;
-};
+import {
+  TERMINAL_STEP_STATUSES,
+  findStepDefinition,
+  getStepAgentName,
+  toWorkflowDefinitionRecord,
+  toWorkflowRunRecord,
+  toWorkflowStepRunRecord,
+} from "./workflow-utils.js";
 
 const STALE_THRESHOLD_MS = 300_000;
-const TERMINAL_STEP_STATUSES = new Set<string>([
-  STEP_STATUSES.done,
-  STEP_STATUSES.failed,
-  STEP_STATUSES.skipped,
-  STEP_STATUSES.escalated,
-]);
 
 function isStale(record: PluginEntityRecord, thresholdMs: number): boolean {
   const updatedAt = new Date(record.updatedAt).getTime();
   return Date.now() - updatedAt > thresholdMs;
-}
-
-function toWorkflowRunRecord(record: PluginEntityRecord): WorkflowRunRecord {
-  return record as WorkflowRunRecord;
-}
-
-function toWorkflowStepRunRecord(record: PluginEntityRecord): WorkflowStepRunRecord {
-  return record as WorkflowStepRunRecord;
-}
-
-function toWorkflowDefinitionRecord(record: PluginEntityRecord): WorkflowDefinitionRecord {
-  return record as WorkflowDefinitionRecord;
-}
-
-function findStepDefinition(definition: WorkflowDefinitionRecord, stepId: string): WorkflowStep | null {
-  return definition.data.steps.find((step: WorkflowStep) => step.id === stepId) ?? null;
-}
-
-function getStepAgentName(stepRun: WorkflowStepRunRecord, stepDef: WorkflowStep): string | null {
-  if (typeof stepRun.data.agentName === "string" && stepRun.data.agentName.trim()) {
-    return stepRun.data.agentName.trim();
-  }
-
-  const stepMeta = stepDef as StepAgentMetadata;
-  const candidates = [stepMeta.agentName, stepMeta.agent, stepMeta.assigneeAgentName];
-
-  for (const candidate of candidates) {
-    if (typeof candidate === "string" && candidate.trim()) {
-      return candidate.trim();
-    }
-  }
-
-  return null;
 }
 
 async function invokeAgentByName(
diff --git a/paperclip-addon/plugins/workflow-engine/src/worker.ts b/paperclip-addon/plugins/workflow-engine/src/worker.ts
index c5ac3fbb4ca7f767eb29331bd3630b635449b335..303e35958890fa1f66db3f2b0ff1fa1f134102a8
--- a/paperclip-addon/plugins/workflow-engine/src/worker.ts
+++ b/paperclip-addon/plugins/workflow-engine/src/worker.ts
@@ -15,6 +15,7 @@
 } from "./dag-engine.js";
 import {
   checkIdempotency,
+  createWorkflowRun,
   createStepRun,
   findStepRunByIssueId,
   getWorkflowDefinition,
@@ -25,51 +26,38 @@
   markIdempotency,
   updateStepRun,
   updateWorkflowRun,
-  type WorkflowDefinition,
-  type WorkflowRun,
   type WorkflowStepRun,
 } from "./workflow-store.js";
+import {
+  TERMINAL_STEP_STATUSES,
+  findStepDefinition,
+  getStepAgentName,
+  getStepAgentNameHint,
+  toWorkflowDefinitionRecord,
+  toWorkflowRunRecord,
+  toWorkflowStepRunRecord,
+  type WorkflowDefinitionRecord,
+  type WorkflowRunRecord,
+  type WorkflowStepRunRecord,
+} from "./workflow-utils.js";
 
-type WorkflowStepRecord = PluginEntityRecord & { data: WorkflowStepRun };
-type WorkflowRunRecord = PluginEntityRecord & { data: WorkflowRun };
-type WorkflowDefinitionRecord = PluginEntityRecord & { data: WorkflowDefinition };
 type WorkflowStepMetadata = WorkflowStep & {
   description?: string;
-  agent?: string;
-  agentName?: string;
-  assigneeAgentName?: string;
   assigneeAgentId?: string;
+  sessionMode?: "fresh" | "reuse";
 };
 type AgentRecord = Awaited<ReturnType<PluginContext["agents"]["list"]>>[number];
+type IssueUpdatePatch = Parameters<PluginContext["issues"]["update"]>[1];
 type ReconcilerModule = {
   reconcileStuckSteps?: (ctx: PluginContext) => Promise<void>;
 };
 
-const TERMINAL_STEP_STATUSES = new Set<string>([
-  STEP_STATUSES.done,
-  STEP_STATUSES.failed,
-  STEP_STATUSES.skipped,
-  STEP_STATUSES.escalated,
-]);
-
 function summarizeError(error: unknown): string {
   if (error instanceof Error) {
     return error.message;
   }
 
   return String(error);
-}
-
-function toStepRunRecord(record: PluginEntityRecord): WorkflowStepRecord {
-  return record as WorkflowStepRecord;
-}
-
-function toWorkflowRunRecord(record: PluginEntityRecord): WorkflowRunRecord {
-  return record as WorkflowRunRecord;
-}
-
-function toWorkflowDefinitionRecord(record: PluginEntityRecord): WorkflowDefinitionRecord {
-  return record as WorkflowDefinitionRecord;
 }
 
 function buildIdempotencyKey(event: PluginEvent): string {
@@ -78,10 +66,6 @@
 
 function getStepMetadata(stepDef: WorkflowStep): WorkflowStepMetadata {
   return stepDef as WorkflowStepMetadata;
-}
-
-function findStepDefinition(definition: WorkflowDefinitionRecord, stepId: string): WorkflowStep | null {
-  return definition.data.steps.find((step: WorkflowStep) => step.id === stepId) ?? null;
 }
 
 function getStepDescription(stepDef: WorkflowStep): string | undefined {
@@ -89,19 +73,6 @@
   return typeof description === "string" && description.trim() ? description.trim() : undefined;
 }
 
-function getStepAgentNameHint(stepDef: WorkflowStep): string | null {
-  const meta = getStepMetadata(stepDef);
-  const candidates = [meta.agentName, meta.agent, meta.assigneeAgentName];
-
-  for (const candidate of candidates) {
-    if (typeof candidate === "string" && candidate.trim()) {
-      return candidate.trim();
-    }
-  }
-
-  return null;
-}
-
 function getStepAgentIdHint(stepDef: WorkflowStep): string | null {
   const assigneeAgentId = getStepMetadata(stepDef).assigneeAgentId;
   if (typeof assigneeAgentId !== "string" || !assigneeAgentId.trim()) {
@@ -147,68 +118,256 @@
 
 async function invokeAgentForStep(
   ctx: PluginContext,
+  stepRunRecord: WorkflowStepRunRecord,
   stepDef: WorkflowStep,
-  agentName: string,
   workflowName: string,
   companyId: string,
-): Promise<void> {
+): Promise<WorkflowStepRunRecord> {
+  const agentName = getStepAgentName(stepRunRecord, stepDef);
+  if (!agentName) {
+    ctx.logger.warn("Agent name is missing for workflow step", {
+      companyId,
+      runId: stepRunRecord.data.runId,
+      stepId: stepDef.id,
+      workflowName,
+    });
+    return stepRunRecord;
+  }
+
   const agents = await ctx.agents.list({ companyId });
-  const agent = agents.find((candidate: AgentRecord) => candidate.name === agentName);
+  const agent = agents.find((candidate: AgentRecord) => candidate.name === agentName) ?? null;
 
   if (!agent) {
     ctx.logger.warn("Agent not found for step", {
       agentName,
       companyId,
+      runId: stepRunRecord.data.runId,
+      stepId: stepDef.id,
+      workflowName,
+    });
+    return stepRunRecord;
+  }
+
+  const prompt = `workflow:${workflowName}/step:${stepDef.id} — "${stepDef.title}" is ready. Please proceed with the assigned task.`;
+  const reason = `workflow:${workflowName}/step:${stepDef.id}`;
+  const sessionMode = getStepMetadata(stepDef).sessionMode === "reuse" ? "reuse" : "fresh";
+
+  if (sessionMode === "reuse") {
+    let sessionId = typeof stepRunRecord.data.sessionId === "string" && stepRunRecord.data.sessionId.trim()
+      ? stepRunRecord.data.sessionId.trim()
+      : "";
+
+    if (!sessionId) {
+      const session = await ctx.agents.sessions.create(agent.id, companyId, {
+        reason,
+      });
+      sessionId = session.sessionId;
+      stepRunRecord = toWorkflowStepRunRecord(await updateStepRun(ctx, stepRunRecord.id, {
+        sessionId,
+      }));
+    }
+
+    await ctx.agents.sessions.sendMessage(sessionId, companyId, {
+      prompt,
+      reason,
+    });
+
+    ctx.logger.info("Sent workflow step prompt via agent session", {
+      agentId: agent.id,
+      agentName,
+      companyId,
+      runId: stepRunRecord.data.runId,
+      sessionId,
       stepId: stepDef.id,
       workflowName,
     });
-    return;
+    return stepRunRecord;
   }
 
   await ctx.agents.invoke(agent.id, companyId, {
-    prompt: `workflow:${workflowName}/step:${stepDef.id} — "${stepDef.title}" is ready. Please proceed with the assigned task.`,
-    reason: `workflow:${workflowName}/step:${stepDef.id}`,
+    prompt,
+    reason,
   });
 
   ctx.logger.info("Invoked agent for workflow step", {
     agentId: agent.id,
     agentName,
     companyId,
+    runId: stepRunRecord.data.runId,
     stepId: stepDef.id,
     workflowName,
   });
+
+  return stepRunRecord;
 }
 
 async function activateBacklogStep(
   ctx: PluginContext,
-  stepRunRecord: WorkflowStepRecord,
+  stepRunRecord: WorkflowStepRunRecord,
   stepDef: WorkflowStep,
   workflowName: string,
   companyId: string,
-): Promise<void> {
+  options?: { parentIssueId?: string },
+): Promise<WorkflowStepRunRecord> {
   if (stepRunRecord.data.status !== STEP_STATUSES.backlog) {
-    return;
+    return stepRunRecord;
   }
 
-  const updated = toStepRunRecord(await updateStepRun(ctx, stepRunRecord.id, {
+  const resolvedAgent = await resolveStepAgent(
+    ctx,
+    companyId,
+    stepDef,
+    stepRunRecord.data.agentName,
+  );
+
+  let issueId = typeof stepRunRecord.data.issueId === "string" && stepRunRecord.data.issueId.trim()
+    ? stepRunRecord.data.issueId.trim()
+    : "";
+  const stepDescription = getStepDescription(stepDef) ?? `Workflow step: ${stepDef.id}`;
+
+  if (!issueId) {
+    const issue = await ctx.issues.create({
+      assigneeAgentId: resolvedAgent.agentId ?? undefined,
+      companyId,
+      description: stepDescription,
+      parentId: options?.parentIssueId,
+      title: `[${workflowName}] ${stepDef.title}`,
+    });
+    await ctx.issues.update(issue.id, { status: "todo" } as IssueUpdatePatch, companyId);
+    issueId = issue.id;
+  }
+
+  const nextStartedAt = stepRunRecord.data.startedAt ?? new Date().toISOString();
+  let updatedStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, stepRunRecord.id, {
+    agentName: resolvedAgent.agentName ?? stepRunRecord.data.agentName,
+    issueId,
+    startedAt: nextStartedAt,
     status: STEP_STATUSES.todo,
   }));
 
-  await invokeAgentForStep(
+  updatedStepRun = await invokeAgentForStep(
     ctx,
+    updatedStepRun,
     stepDef,
-    updated.data.agentName,
     workflowName,
     companyId,
   );
+
+  return updatedStepRun;
 }
 
+async function startWorkflow(
+  ctx: PluginContext,
+  workflowId: string,
+  companyId: string,
+  options?: {
+    createParentIssue?: boolean;
+    parentIssueId?: string;
+  },
+): Promise<{
+  activatedStepIds: string[];
+  parentIssueId: string | null;
+  runId: string;
+  workflowId: string;
+}> {
+  const workflowDefinition = await getWorkflowDefinition(ctx, workflowId);
+  if (!workflowDefinition) {
+    throw new Error(`Workflow definition not found: ${workflowId}`);
+  }
+
+  const typedWorkflowDefinition = toWorkflowDefinitionRecord(workflowDefinition);
+  if (typedWorkflowDefinition.data.companyId !== companyId) {
+    throw new Error(`Workflow does not belong to company: ${workflowId}`);
+  }
+
+  let parentIssueId = typeof options?.parentIssueId === "string" && options.parentIssueId.trim()
+    ? options.parentIssueId.trim()
+    : "";
+  if (!parentIssueId && options?.createParentIssue) {
+    const parentIssue = await ctx.issues.create({
+      companyId,
+      description: typedWorkflowDefinition.data.description || `Workflow run: ${typedWorkflowDefinition.data.name}`,
+      title: `[${typedWorkflowDefinition.data.name}] Workflow run`,
+    });
+    parentIssueId = parentIssue.id;
+  }
+
+  const workflowRun = toWorkflowRunRecord(await createWorkflowRun(ctx, {
+    companyId,
+    parentIssueId: parentIssueId || undefined,
+    startedAt: new Date().toISOString(),
+    status: RUN_STATUSES.running,
+    workflowId: typedWorkflowDefinition.id,
+    workflowName: typedWorkflowDefinition.data.name,
+  }));
+
+  const agents = await ctx.agents.list({ companyId });
+  const agentsByName = new Map<string, AgentRecord>();
+  for (const agent of agents) {
+    agentsByName.set(agent.name, agent);
+  }
+
+  const pendingRootSteps: Array<{ stepDef: WorkflowStep; stepRun: WorkflowStepRunRecord }> = [];
+  for (const stepDef of typedWorkflowDefinition.data.steps) {
+    const agentNameHint = getStepAgentNameHint(stepDef);
+    const matchedAgent = agentNameHint ? agentsByName.get(agentNameHint) ?? null : null;
+    const resolvedAgent = matchedAgent
+      ? { agentId: matchedAgent.id, agentName: matchedAgent.name }
+      : await resolveStepAgent(ctx, companyId, stepDef, agentNameHint ?? undefined);
+
+    if (!resolvedAgent.agentName) {
+      throw new Error(`Unable to resolve step assignee for "${stepDef.id}"`);
+    }
+
+    const stepRun = toWorkflowStepRunRecord(await createStepRun(ctx, companyId, {
+      agentName: resolvedAgent.agentName,
+      retryCount: 0,
+      runId: workflowRun.id,
+      status: STEP_STATUSES.backlog,
+      stepId: stepDef.id,
+    }));
+
+    if (stepDef.dependsOn.length === 0 && stepDef.triggerOn !== "escalation") {
+      pendingRootSteps.push({ stepDef, stepRun });
+    }
+  }
+
+  const activatedStepIds: string[] = [];
+  for (const pending of pendingRootSteps) {
+    await activateBacklogStep(
+      ctx,
+      pending.stepRun,
+      pending.stepDef,
+      typedWorkflowDefinition.data.name,
+      companyId,
+      { parentIssueId: parentIssueId || undefined },
+    );
+    activatedStepIds.push(pending.stepDef.id);
+  }
+
+  ctx.logger.info("Started workflow run", {
+    activatedStepIds,
+    companyId,
+    parentIssueId: parentIssueId || null,
+    runId: workflowRun.id,
+    workflowId: typedWorkflowDefinition.id,
+    workflowName: typedWorkflowDefinition.data.name,
+  });
+
+  return {
+    activatedStepIds,
+    parentIssueId: parentIssueId || null,
+    runId: workflowRun.id,
+    workflowId: typedWorkflowDefinition.id,
+  };
+}
+
 async function advanceWorkflow(
   ctx: PluginContext,
   stepRunRecord: PluginEntityRecord,
   companyId: string,
 ): Promise<void> {
-  const completedStepRun = toStepRunRecord(stepRunRecord);
+  const completedStepRun = toWorkflowStepRunRecord(stepRunRecord);
   const workflowRun = await getWorkflowRun(ctx, completedStepRun.data.runId);
 
   if (!workflowRun) {
@@ -237,7 +396,7 @@
   }
 
   const typedWorkflowDefinition = toWorkflowDefinitionRecord(workflowDefinition);
-  const stepRuns = (await listStepRuns(ctx, typedWorkflowRun.id, companyId)).map(toStepRunRecord);
+  const stepRuns = (await listStepRuns(ctx, typedWorkflowRun.id, companyId)).map(toWorkflowStepRunRecord);
   const completed = new Set<string>();
   const failed = new Set<string>();
   const skipped = new Set<string>();
@@ -289,7 +448,7 @@
         continue;
       }
 
-      stepRun = toStepRunRecord(await createStepRun(ctx, companyId, {
+      stepRun = toWorkflowStepRunRecord(await createStepRun(ctx, companyId, {
         agentName: resolvedAgent.agentName,
         retryCount: 0,
         runId: typedWorkflowRun.id,
@@ -305,6 +464,7 @@
       stepDef,
       typedWorkflowRun.data.workflowName,
       companyId,
+      { parentIssueId: typedWorkflowRun.data.parentIssueId },
     );
   }
 
@@ -329,7 +489,7 @@
   ctx: PluginContext,
   workflowRun: WorkflowRunRecord,
   workflowDefinition: WorkflowDefinitionRecord,
-  sourceStepRun: WorkflowStepRecord,
+  sourceStepRun: WorkflowStepRunRecord,
   escalationTargetId: string,
   companyId: string,
 ): Promise<void> {
@@ -338,7 +498,7 @@
     throw new Error(`Escalation target step not found: ${escalationTargetId}`);
   }
 
-  const stepRuns = (await listStepRuns(ctx, workflowRun.id, companyId)).map(toStepRunRecord);
+  const stepRuns = (await listStepRuns(ctx, workflowRun.id, companyId)).map(toWorkflowStepRunRecord);
   let escalationStepRun = stepRuns.find((candidate) => candidate.data.stepId === escalationTargetId) ?? null;
   const resolvedAgent = await resolveStepAgent(
     ctx,
@@ -352,7 +512,7 @@
   }
 
   if (!escalationStepRun) {
-    escalationStepRun = toStepRunRecord(await createStepRun(ctx, companyId, {
+    escalationStepRun = toWorkflowStepRunRecord(await createStepRun(ctx, companyId, {
       agentName: resolvedAgent.agentName,
       retryCount: 0,
       runId: workflowRun.id,
@@ -374,14 +534,14 @@
       title: `${workflowRun.data.workflowName}: ${escalationStep.title}`,
     });
 
-    escalationStepRun = toStepRunRecord(await updateStepRun(ctx, escalationStepRun.id, {
+    escalationStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, escalationStepRun.id, {
       issueId: issue.id,
     }));
   }
 
   const shouldActivate = escalationStepRun.data.status === STEP_STATUSES.backlog;
   if (shouldActivate) {
-    escalationStepRun = toStepRunRecord(await updateStepRun(ctx, escalationStepRun.id, {
+    escalationStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, escalationStepRun.id, {
       status: STEP_STATUSES.todo,
     }));
   }
@@ -389,8 +549,8 @@
   if (shouldActivate) {
     await invokeAgentForStep(
       ctx,
+      escalationStepRun,
       escalationStep,
-      escalationStepRun.data.agentName,
       workflowRun.data.workflowName,
       companyId,
     );
@@ -421,7 +581,7 @@
     return;
   }
 
-  const typedStepRun = toStepRunRecord(stepRunRecord);
+  const typedStepRun = toWorkflowStepRunRecord(stepRunRecord);
   if (TERMINAL_STEP_STATUSES.has(typedStepRun.data.status)) {
     return;
   }
@@ -469,7 +629,7 @@
   if (options.allowRetry) {
     const retryInfo = getRetryInfo(typedWorkflowDefinition.data.steps, typedStepRun.data.stepId);
     if (retryInfo.shouldRetry && typedStepRun.data.retryCount < retryInfo.maxRetries) {
-      const retriedStepRun = toStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
+      const retriedStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
         completedAt: undefined,
         retryCount: typedStepRun.data.retryCount + 1,
         status: STEP_STATUSES.todo,
@@ -477,8 +637,8 @@
 
       await invokeAgentForStep(
         ctx,
+        retriedStepRun,
         stepDef,
-        retriedStepRun.data.agentName,
         typedWorkflowRun.data.workflowName,
         event.companyId,
       );
@@ -496,7 +656,7 @@
   }
 
   if (stepDef.onFailure === "skip") {
-    const skippedStepRun = toStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
+    const skippedStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
       completedAt: new Date().toISOString(),
       status: STEP_STATUSES.skipped,
     }));
@@ -556,7 +716,7 @@
       return;
     }
 
-    const escalatedStepRun = toStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
+    const escalatedStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
       completedAt: new Date().toISOString(),
       status: STEP_STATUSES.escalated,
     }));
@@ -617,6 +777,29 @@
   }
 }
 
+function registerDataHandler(
+  ctx: PluginContext,
+  key: string,
+  handler: (params: Record<string, unknown>) => Promise<unknown>,
+): void {
+  const dataClient = ctx.data as {
+    handle?: (handlerKey: string, handlerFn: (params: Record<string, unknown>) => Promise<unknown>) => void;
+    register?: (handlerKey: string, handlerFn: (params: Record<string, unknown>) => Promise<unknown>) => void;
+  };
+
+  if (typeof dataClient.handle === "function") {
+    dataClient.handle(key, handler);
+    return;
+  }
+
+  if (typeof dataClient.register === "function") {
+    dataClient.register(key, handler);
+    return;
+  }
+
+  throw new Error("Plugin data client does not support handler registration");
+}
+
 const plugin = definePlugin({
   async setup(ctx: PluginContext) {
     ctx.events.on("issue.updated", async (event: PluginEvent) => {
@@ -638,7 +821,7 @@
           return;
         }
 
-        const stepRun = toStepRunRecord(stepRunRecord);
+        const stepRun = toWorkflowStepRunRecord(stepRunRecord);
         const payload = event.payload as { status?: string };
         const issueStatus = typeof payload.status === "string" ? payload.status : undefined;
 
@@ -657,7 +840,7 @@
           return;
         }
 
-        const updatedStepRun = toStepRunRecord(await updateStepRun(ctx, stepRun.id, {
+        const updatedStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, stepRun.id, {
           completedAt: nextStepStatus === STEP_STATUSES.done ? new Date().toISOString() : undefined,
           startedAt: nextStepStatus === STEP_STATUSES.inProgress
             ? stepRun.data.startedAt ?? new Date().toISOString()
@@ -687,11 +870,21 @@
 
     ctx.events.on("issue.created", async (event: PluginEvent) => {
       try {
+        const idempotencyKey = buildIdempotencyKey(event);
+        if (await checkIdempotency(ctx, idempotencyKey, event.companyId)) {
+          return;
+        }
+
         const payload = event.payload as {
           id?: string;
           parentId?: string;
           assigneeAgentId?: string;
         };
+        const issueId = typeof event.entityId === "string" && event.entityId.trim()
+          ? event.entityId.trim()
+          : typeof payload.id === "string" && payload.id.trim()
+            ? payload.id.trim()
+            : "";
 
         const parentId = typeof payload.parentId === "string" && payload.parentId.trim()
           ? payload.parentId.trim()
@@ -700,12 +893,34 @@
           ? payload.assigneeAgentId.trim()
           : "";
 
-        if (parentId || !assigneeAgentId) {
+        if (!issueId || parentId || !assigneeAgentId) {
+          await markIdempotency(ctx, idempotencyKey, event.companyId);
+          return;
+        }
+
+        const agent = await ctx.agents.get(assigneeAgentId, event.companyId);
+        const metadata = agent?.metadata as Record<string, unknown> | undefined;
+        const defaultParentIssueId = typeof metadata?.defaultParentIssueId === "string" && metadata.defaultParentIssueId.trim()
+          ? metadata.defaultParentIssueId.trim()
+          : "";
+
+        if (!defaultParentIssueId) {
+          await markIdempotency(ctx, idempotencyKey, event.companyId);
           return;
         }
 
-        ctx.logger.info("parentId filler: issue created without parent", {
-          issueId: event.entityId ?? payload.id ?? null,
+        await ctx.issues.update(
+          issueId,
+          { parentId: defaultParentIssueId } as IssueUpdatePatch,
+          event.companyId,
+        );
+        await markIdempotency(ctx, idempotencyKey, event.companyId);
+
+        ctx.logger.info("parentId filler: populated issue parentId from agent metadata", {
+          assigneeAgentId,
+          companyId: event.companyId,
+          issueId,
+          parentId: defaultParentIssueId,
         });
       } catch (error) {
         ctx.logger.warn("Failed to handle issue.created event", {
@@ -744,7 +959,23 @@
       await runReconciler(ctx);
     });
 
-    ctx.data.register("workflow-overview", async (params: Record<string, unknown>) => {
+    registerDataHandler(ctx, "start-workflow", async (params: Record<string, unknown>) => {
+      const workflowId = typeof params.workflowId === "string" ? params.workflowId.trim() : "";
+      const companyId = typeof params.companyId === "string" ? params.companyId.trim() : "";
+      const parentIssueId = typeof params.parentIssueId === "string" ? params.parentIssueId.trim() : "";
+      const createParentIssue = params.createParentIssue === true;
+
+      if (!workflowId || !companyId) {
+        throw new Error("start-workflow requires workflowId and companyId");
+      }
+
+      return await startWorkflow(ctx, workflowId, companyId, {
+        createParentIssue,
+        parentIssueId: parentIssueId || undefined,
+      });
+    });
+
+    registerDataHandler(ctx, "workflow-overview", async (params: Record<string, unknown>) => {
       try {
         const companyId = typeof params.companyId === "string" ? params.companyId.trim() : "";
         if (!companyId) {
@@ -782,7 +1013,7 @@
       }
     });
 
-    ctx.data.register("workflow-run-detail", async (params: Record<string, unknown>) => {
+    registerDataHandler(ctx, "workflow-run-detail", async (params: Record<string, unknown>) => {
       try {
         const runId = typeof params.runId === "string" ? params.runId.trim() : "";
         if (!runId) {
@@ -807,7 +1038,7 @@
             ...typedWorkflowRun.data,
           },
           stepRuns: stepRuns.map((record: PluginEntityRecord) => {
-            const stepRun = toStepRunRecord(record);
+            const stepRun = toWorkflowStepRunRecord(record);
             return {
               id: stepRun.id,
               status: stepRun.status,
diff --git a/paperclip-addon/plugins/workflow-engine/src/workflow-store.ts b/paperclip-addon/plugins/workflow-engine/src/workflow-store.ts
index 5871a2082258cebcfe8b69ebafbb0814cf303d50..82eb46836883940e109368e927b7e367c96616b5
--- a/paperclip-addon/plugins/workflow-engine/src/workflow-store.ts
+++ b/paperclip-addon/plugins/workflow-engine/src/workflow-store.ts
@@ -44,7 +44,14 @@
 
 type TypedEntityRecord<T> = Omit<PluginEntityRecord, "data"> & { data: T };
 
-function toTypedRecord<T>(record: PluginEntityRecord): TypedEntityRecord<T> {
+function toTypedRecord<T>(
+  record: PluginEntityRecord,
+  entityType: string,
+): TypedEntityRecord<T> {
+  if (record.entityType !== entityType) {
+    throw new Error(`Expected entity type "${entityType}", got "${record.entityType}"`);
+  }
+
   return record as TypedEntityRecord<T>;
 }
 
@@ -67,13 +74,31 @@
   id: string,
   entityType: string,
 ): Promise<TypedEntityRecord<T> | null> {
-  const record = await ctx.entities.get(id);
+  const listQuery = {
+    entityType,
+    id,
+    limit: 1,
+  } as Record<string, unknown>;
 
-  if (!record || record.entityType !== entityType) {
+  const listed = await ctx.entities.list(
+    listQuery as Parameters<PluginContext["entities"]["list"]>[0],
+  );
+  const listedRecord = listed.find(
+    (record: PluginEntityRecord) => record.id === id,
+  ) ?? null;
+
+  if (listedRecord) {
+    return toTypedRecord<T>(listedRecord, entityType);
+  }
+
+  // Fallback: some host SDK variants ignore `id` on entities.list().
+  // Keep defensive type verification on direct get() until id-filter support is universal.
+  const record = await ctx.entities.get(id);
+  if (!record) {
     return null;
   }
 
-  return toTypedRecord<T>(record);
+  return toTypedRecord<T>(record, entityType);
 }
 
 async function requireEntityByType<T>(
diff --git a/paperclip-addon/plugins/workflow-engine/src/workflow-utils.ts b/paperclip-addon/plugins/workflow-engine/src/workflow-utils.ts
new file mode 100644
index 0000000000000000000000000000000000000000..ec1cf6f6b9aa722b126b58c0a728b4e538ad1494
--- /dev/null
+++ b/paperclip-addon/plugins/workflow-engine/src/workflow-utils.ts
@@ -0,0 +1,62 @@
+import type { PluginEntityRecord } from "@paperclipai/plugin-sdk";
+
+import { STEP_STATUSES } from "./constants.js";
+import type { WorkflowStep } from "./dag-engine.js";
+import type {
+  WorkflowDefinition,
+  WorkflowRun,
+  WorkflowStepRun,
+} from "./workflow-store.js";
+
+export type WorkflowRunRecord = PluginEntityRecord & { data: WorkflowRun };
+export type WorkflowStepRunRecord = PluginEntityRecord & { data: WorkflowStepRun };
+export type WorkflowDefinitionRecord = PluginEntityRecord & { data: WorkflowDefinition };
+type StepAgentMetadata = WorkflowStep & {
+  agent?: string;
+  agentName?: string;
+  assigneeAgentName?: string;
+};
+
+export const TERMINAL_STEP_STATUSES = new Set<string>([
+  STEP_STATUSES.done,
+  STEP_STATUSES.failed,
+  STEP_STATUSES.skipped,
+  STEP_STATUSES.escalated,
+]);
+
+export function toWorkflowRunRecord(record: PluginEntityRecord): WorkflowRunRecord {
+  return record as WorkflowRunRecord;
+}
+
+export function toWorkflowStepRunRecord(record: PluginEntityRecord): WorkflowStepRunRecord {
+  return record as WorkflowStepRunRecord;
+}
+
+export function toWorkflowDefinitionRecord(record: PluginEntityRecord): WorkflowDefinitionRecord {
+  return record as WorkflowDefinitionRecord;
+}
+
+export function findStepDefinition(definition: WorkflowDefinitionRecord, stepId: string): WorkflowStep | null {
+  return definition.data.steps.find((step: WorkflowStep) => step.id === stepId) ?? null;
+}
+
+export function getStepAgentNameHint(stepDef: WorkflowStep): string | null {
+  const stepMeta = stepDef as StepAgentMetadata;
+  const candidates = [stepMeta.agentName, stepMeta.agent, stepMeta.assigneeAgentName];
+
+  for (const candidate of candidates) {
+    if (typeof candidate === "string" && candidate.trim()) {
+      return candidate.trim();
+    }
+  }
+
+  return null;
+}
+
+export function getStepAgentName(stepRun: WorkflowStepRunRecord, stepDef: WorkflowStep): string | null {
+  if (typeof stepRun.data.agentName === "string" && stepRun.data.agentName.trim()) {
+    return stepRun.data.agentName.trim();
+  }
+
+  return getStepAgentNameHint(stepDef);
+}

file update:
diff --git a/paperclip-addon/plugins/workflow-engine/src/dag-engine.ts b/paperclip-addon/plugins/workflow-engine/src/dag-engine.ts
index b59510f0473171038be77277286d3b5c2063e6c5..e7f21ff998cab78b9325a8aeacf4fda9c5e06553
--- a/paperclip-addon/plugins/workflow-engine/src/dag-engine.ts
+++ b/paperclip-addon/plugins/workflow-engine/src/dag-engine.ts
@@ -2,6 +2,7 @@
   id: string;
   title: string;
   dependsOn: string[];
+  sessionMode?: "fresh" | "reuse";
   onFailure?: "retry" | "skip" | "abort_workflow" | "escalate";
   escalateTo?: string;
   maxRetries?: number;
diff --git a/paperclip-addon/plugins/workflow-engine/src/reconciler.ts b/paperclip-addon/plugins/workflow-engine/src/reconciler.ts
index 14ab4e766a835d145a6c2e7b1b564270285cbdc3..55b649037e8a19a88bb39b9633a6a0cc84fa8d59
--- a/paperclip-addon/plugins/workflow-engine/src/reconciler.ts
+++ b/paperclip-addon/plugins/workflow-engine/src/reconciler.ts
@@ -8,65 +8,21 @@
   listStepRuns,
   updateStepRun,
   updateWorkflowRun,
-  type WorkflowDefinition,
-  type WorkflowRun,
-  type WorkflowStepRun,
 } from "./workflow-store.js";
-import type { WorkflowStep } from "./dag-engine.js";
-
-type WorkflowRunRecord = PluginEntityRecord & { data: WorkflowRun };
-type WorkflowStepRunRecord = PluginEntityRecord & { data: WorkflowStepRun };
-type WorkflowDefinitionRecord = PluginEntityRecord & { data: WorkflowDefinition };
-type StepAgentMetadata = WorkflowStep & {
-  agent?: string;
-  agentName?: string;
-  assigneeAgentName?: string;
-};
+import {
+  TERMINAL_STEP_STATUSES,
+  findStepDefinition,
+  getStepAgentName,
+  toWorkflowDefinitionRecord,
+  toWorkflowRunRecord,
+  toWorkflowStepRunRecord,
+} from "./workflow-utils.js";
 
 const STALE_THRESHOLD_MS = 300_000;
-const TERMINAL_STEP_STATUSES = new Set<string>([
-  STEP_STATUSES.done,
-  STEP_STATUSES.failed,
-  STEP_STATUSES.skipped,
-  STEP_STATUSES.escalated,
-]);
 
 function isStale(record: PluginEntityRecord, thresholdMs: number): boolean {
   const updatedAt = new Date(record.updatedAt).getTime();
   return Date.now() - updatedAt > thresholdMs;
-}
-
-function toWorkflowRunRecord(record: PluginEntityRecord): WorkflowRunRecord {
-  return record as WorkflowRunRecord;
-}
-
-function toWorkflowStepRunRecord(record: PluginEntityRecord): WorkflowStepRunRecord {
-  return record as WorkflowStepRunRecord;
-}
-
-function toWorkflowDefinitionRecord(record: PluginEntityRecord): WorkflowDefinitionRecord {
-  return record as WorkflowDefinitionRecord;
-}
-
-function findStepDefinition(definition: WorkflowDefinitionRecord, stepId: string): WorkflowStep | null {
-  return definition.data.steps.find((step: WorkflowStep) => step.id === stepId) ?? null;
-}
-
-function getStepAgentName(stepRun: WorkflowStepRunRecord, stepDef: WorkflowStep): string | null {
-  if (typeof stepRun.data.agentName === "string" && stepRun.data.agentName.trim()) {
-    return stepRun.data.agentName.trim();
-  }
-
-  const stepMeta = stepDef as StepAgentMetadata;
-  const candidates = [stepMeta.agentName, stepMeta.agent, stepMeta.assigneeAgentName];
-
-  for (const candidate of candidates) {
-    if (typeof candidate === "string" && candidate.trim()) {
-      return candidate.trim();
-    }
-  }
-
-  return null;
 }
 
 async function invokeAgentByName(
diff --git a/paperclip-addon/plugins/workflow-engine/src/worker.ts b/paperclip-addon/plugins/workflow-engine/src/worker.ts
index c5ac3fbb4ca7f767eb29331bd3630b635449b335..303e35958890fa1f66db3f2b0ff1fa1f134102a8
--- a/paperclip-addon/plugins/workflow-engine/src/worker.ts
+++ b/paperclip-addon/plugins/workflow-engine/src/worker.ts
@@ -15,6 +15,7 @@
 } from "./dag-engine.js";
 import {
   checkIdempotency,
+  createWorkflowRun,
   createStepRun,
   findStepRunByIssueId,
   getWorkflowDefinition,
@@ -25,51 +26,38 @@
   markIdempotency,
   updateStepRun,
   updateWorkflowRun,
-  type WorkflowDefinition,
-  type WorkflowRun,
   type WorkflowStepRun,
 } from "./workflow-store.js";
+import {
+  TERMINAL_STEP_STATUSES,
+  findStepDefinition,
+  getStepAgentName,
+  getStepAgentNameHint,
+  toWorkflowDefinitionRecord,
+  toWorkflowRunRecord,
+  toWorkflowStepRunRecord,
+  type WorkflowDefinitionRecord,
+  type WorkflowRunRecord,
+  type WorkflowStepRunRecord,
+} from "./workflow-utils.js";
 
-type WorkflowStepRecord = PluginEntityRecord & { data: WorkflowStepRun };
-type WorkflowRunRecord = PluginEntityRecord & { data: WorkflowRun };
-type WorkflowDefinitionRecord = PluginEntityRecord & { data: WorkflowDefinition };
 type WorkflowStepMetadata = WorkflowStep & {
   description?: string;
-  agent?: string;
-  agentName?: string;
-  assigneeAgentName?: string;
   assigneeAgentId?: string;
+  sessionMode?: "fresh" | "reuse";
 };
 type AgentRecord = Awaited<ReturnType<PluginContext["agents"]["list"]>>[number];
+type IssueUpdatePatch = Parameters<PluginContext["issues"]["update"]>[1];
 type ReconcilerModule = {
   reconcileStuckSteps?: (ctx: PluginContext) => Promise<void>;
 };
 
-const TERMINAL_STEP_STATUSES = new Set<string>([
-  STEP_STATUSES.done,
-  STEP_STATUSES.failed,
-  STEP_STATUSES.skipped,
-  STEP_STATUSES.escalated,
-]);
-
 function summarizeError(error: unknown): string {
   if (error instanceof Error) {
     return error.message;
   }
 
   return String(error);
-}
-
-function toStepRunRecord(record: PluginEntityRecord): WorkflowStepRecord {
-  return record as WorkflowStepRecord;
-}
-
-function toWorkflowRunRecord(record: PluginEntityRecord): WorkflowRunRecord {
-  return record as WorkflowRunRecord;
-}
-
-function toWorkflowDefinitionRecord(record: PluginEntityRecord): WorkflowDefinitionRecord {
-  return record as WorkflowDefinitionRecord;
 }
 
 function buildIdempotencyKey(event: PluginEvent): string {
@@ -78,10 +66,6 @@
 
 function getStepMetadata(stepDef: WorkflowStep): WorkflowStepMetadata {
   return stepDef as WorkflowStepMetadata;
-}
-
-function findStepDefinition(definition: WorkflowDefinitionRecord, stepId: string): WorkflowStep | null {
-  return definition.data.steps.find((step: WorkflowStep) => step.id === stepId) ?? null;
 }
 
 function getStepDescription(stepDef: WorkflowStep): string | undefined {
@@ -89,19 +73,6 @@
   return typeof description === "string" && description.trim() ? description.trim() : undefined;
 }
 
-function getStepAgentNameHint(stepDef: WorkflowStep): string | null {
-  const meta = getStepMetadata(stepDef);
-  const candidates = [meta.agentName, meta.agent, meta.assigneeAgentName];
-
-  for (const candidate of candidates) {
-    if (typeof candidate === "string" && candidate.trim()) {
-      return candidate.trim();
-    }
-  }
-
-  return null;
-}
-
 function getStepAgentIdHint(stepDef: WorkflowStep): string | null {
   const assigneeAgentId = getStepMetadata(stepDef).assigneeAgentId;
   if (typeof assigneeAgentId !== "string" || !assigneeAgentId.trim()) {
@@ -147,68 +118,256 @@
 
 async function invokeAgentForStep(
   ctx: PluginContext,
+  stepRunRecord: WorkflowStepRunRecord,
   stepDef: WorkflowStep,
-  agentName: string,
   workflowName: string,
   companyId: string,
-): Promise<void> {
+): Promise<WorkflowStepRunRecord> {
+  const agentName = getStepAgentName(stepRunRecord, stepDef);
+  if (!agentName) {
+    ctx.logger.warn("Agent name is missing for workflow step", {
+      companyId,
+      runId: stepRunRecord.data.runId,
+      stepId: stepDef.id,
+      workflowName,
+    });
+    return stepRunRecord;
+  }
+
   const agents = await ctx.agents.list({ companyId });
-  const agent = agents.find((candidate: AgentRecord) => candidate.name === agentName);
+  const agent = agents.find((candidate: AgentRecord) => candidate.name === agentName) ?? null;
 
   if (!agent) {
     ctx.logger.warn("Agent not found for step", {
       agentName,
       companyId,
+      runId: stepRunRecord.data.runId,
+      stepId: stepDef.id,
+      workflowName,
+    });
+    return stepRunRecord;
+  }
+
+  const prompt = `workflow:${workflowName}/step:${stepDef.id} — "${stepDef.title}" is ready. Please proceed with the assigned task.`;
+  const reason = `workflow:${workflowName}/step:${stepDef.id}`;
+  const sessionMode = getStepMetadata(stepDef).sessionMode === "reuse" ? "reuse" : "fresh";
+
+  if (sessionMode === "reuse") {
+    let sessionId = typeof stepRunRecord.data.sessionId === "string" && stepRunRecord.data.sessionId.trim()
+      ? stepRunRecord.data.sessionId.trim()
+      : "";
+
+    if (!sessionId) {
+      const session = await ctx.agents.sessions.create(agent.id, companyId, {
+        reason,
+      });
+      sessionId = session.sessionId;
+      stepRunRecord = toWorkflowStepRunRecord(await updateStepRun(ctx, stepRunRecord.id, {
+        sessionId,
+      }));
+    }
+
+    await ctx.agents.sessions.sendMessage(sessionId, companyId, {
+      prompt,
+      reason,
+    });
+
+    ctx.logger.info("Sent workflow step prompt via agent session", {
+      agentId: agent.id,
+      agentName,
+      companyId,
+      runId: stepRunRecord.data.runId,
+      sessionId,
       stepId: stepDef.id,
       workflowName,
     });
-    return;
+    return stepRunRecord;
   }
 
   await ctx.agents.invoke(agent.id, companyId, {
-    prompt: `workflow:${workflowName}/step:${stepDef.id} — "${stepDef.title}" is ready. Please proceed with the assigned task.`,
-    reason: `workflow:${workflowName}/step:${stepDef.id}`,
+    prompt,
+    reason,
   });
 
   ctx.logger.info("Invoked agent for workflow step", {
     agentId: agent.id,
     agentName,
     companyId,
+    runId: stepRunRecord.data.runId,
     stepId: stepDef.id,
     workflowName,
   });
+
+  return stepRunRecord;
 }
 
 async function activateBacklogStep(
   ctx: PluginContext,
-  stepRunRecord: WorkflowStepRecord,
+  stepRunRecord: WorkflowStepRunRecord,
   stepDef: WorkflowStep,
   workflowName: string,
   companyId: string,
-): Promise<void> {
+  options?: { parentIssueId?: string },
+): Promise<WorkflowStepRunRecord> {
   if (stepRunRecord.data.status !== STEP_STATUSES.backlog) {
-    return;
+    return stepRunRecord;
   }
 
-  const updated = toStepRunRecord(await updateStepRun(ctx, stepRunRecord.id, {
+  const resolvedAgent = await resolveStepAgent(
+    ctx,
+    companyId,
+    stepDef,
+    stepRunRecord.data.agentName,
+  );
+
+  let issueId = typeof stepRunRecord.data.issueId === "string" && stepRunRecord.data.issueId.trim()
+    ? stepRunRecord.data.issueId.trim()
+    : "";
+  const stepDescription = getStepDescription(stepDef) ?? `Workflow step: ${stepDef.id}`;
+
+  if (!issueId) {
+    const issue = await ctx.issues.create({
+      assigneeAgentId: resolvedAgent.agentId ?? undefined,
+      companyId,
+      description: stepDescription,
+      parentId: options?.parentIssueId,
+      title: `[${workflowName}] ${stepDef.title}`,
+    });
+    await ctx.issues.update(issue.id, { status: "todo" } as IssueUpdatePatch, companyId);
+    issueId = issue.id;
+  }
+
+  const nextStartedAt = stepRunRecord.data.startedAt ?? new Date().toISOString();
+  let updatedStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, stepRunRecord.id, {
+    agentName: resolvedAgent.agentName ?? stepRunRecord.data.agentName,
+    issueId,
+    startedAt: nextStartedAt,
     status: STEP_STATUSES.todo,
   }));
 
-  await invokeAgentForStep(
+  updatedStepRun = await invokeAgentForStep(
     ctx,
+    updatedStepRun,
     stepDef,
-    updated.data.agentName,
     workflowName,
     companyId,
   );
+
+  return updatedStepRun;
 }
 
+async function startWorkflow(
+  ctx: PluginContext,
+  workflowId: string,
+  companyId: string,
+  options?: {
+    createParentIssue?: boolean;
+    parentIssueId?: string;
+  },
+): Promise<{
+  activatedStepIds: string[];
+  parentIssueId: string | null;
+  runId: string;
+  workflowId: string;
+}> {
+  const workflowDefinition = await getWorkflowDefinition(ctx, workflowId);
+  if (!workflowDefinition) {
+    throw new Error(`Workflow definition not found: ${workflowId}`);
+  }
+
+  const typedWorkflowDefinition = toWorkflowDefinitionRecord(workflowDefinition);
+  if (typedWorkflowDefinition.data.companyId !== companyId) {
+    throw new Error(`Workflow does not belong to company: ${workflowId}`);
+  }
+
+  let parentIssueId = typeof options?.parentIssueId === "string" && options.parentIssueId.trim()
+    ? options.parentIssueId.trim()
+    : "";
+  if (!parentIssueId && options?.createParentIssue) {
+    const parentIssue = await ctx.issues.create({
+      companyId,
+      description: typedWorkflowDefinition.data.description || `Workflow run: ${typedWorkflowDefinition.data.name}`,
+      title: `[${typedWorkflowDefinition.data.name}] Workflow run`,
+    });
+    parentIssueId = parentIssue.id;
+  }
+
+  const workflowRun = toWorkflowRunRecord(await createWorkflowRun(ctx, {
+    companyId,
+    parentIssueId: parentIssueId || undefined,
+    startedAt: new Date().toISOString(),
+    status: RUN_STATUSES.running,
+    workflowId: typedWorkflowDefinition.id,
+    workflowName: typedWorkflowDefinition.data.name,
+  }));
+
+  const agents = await ctx.agents.list({ companyId });
+  const agentsByName = new Map<string, AgentRecord>();
+  for (const agent of agents) {
+    agentsByName.set(agent.name, agent);
+  }
+
+  const pendingRootSteps: Array<{ stepDef: WorkflowStep; stepRun: WorkflowStepRunRecord }> = [];
+  for (const stepDef of typedWorkflowDefinition.data.steps) {
+    const agentNameHint = getStepAgentNameHint(stepDef);
+    const matchedAgent = agentNameHint ? agentsByName.get(agentNameHint) ?? null : null;
+    const resolvedAgent = matchedAgent
+      ? { agentId: matchedAgent.id, agentName: matchedAgent.name }
+      : await resolveStepAgent(ctx, companyId, stepDef, agentNameHint ?? undefined);
+
+    if (!resolvedAgent.agentName) {
+      throw new Error(`Unable to resolve step assignee for "${stepDef.id}"`);
+    }
+
+    const stepRun = toWorkflowStepRunRecord(await createStepRun(ctx, companyId, {
+      agentName: resolvedAgent.agentName,
+      retryCount: 0,
+      runId: workflowRun.id,
+      status: STEP_STATUSES.backlog,
+      stepId: stepDef.id,
+    }));
+
+    if (stepDef.dependsOn.length === 0 && stepDef.triggerOn !== "escalation") {
+      pendingRootSteps.push({ stepDef, stepRun });
+    }
+  }
+
+  const activatedStepIds: string[] = [];
+  for (const pending of pendingRootSteps) {
+    await activateBacklogStep(
+      ctx,
+      pending.stepRun,
+      pending.stepDef,
+      typedWorkflowDefinition.data.name,
+      companyId,
+      { parentIssueId: parentIssueId || undefined },
+    );
+    activatedStepIds.push(pending.stepDef.id);
+  }
+
+  ctx.logger.info("Started workflow run", {
+    activatedStepIds,
+    companyId,
+    parentIssueId: parentIssueId || null,
+    runId: workflowRun.id,
+    workflowId: typedWorkflowDefinition.id,
+    workflowName: typedWorkflowDefinition.data.name,
+  });
+
+  return {
+    activatedStepIds,
+    parentIssueId: parentIssueId || null,
+    runId: workflowRun.id,
+    workflowId: typedWorkflowDefinition.id,
+  };
+}
+
 async function advanceWorkflow(
   ctx: PluginContext,
   stepRunRecord: PluginEntityRecord,
   companyId: string,
 ): Promise<void> {
-  const completedStepRun = toStepRunRecord(stepRunRecord);
+  const completedStepRun = toWorkflowStepRunRecord(stepRunRecord);
   const workflowRun = await getWorkflowRun(ctx, completedStepRun.data.runId);
 
   if (!workflowRun) {
@@ -237,7 +396,7 @@
   }
 
   const typedWorkflowDefinition = toWorkflowDefinitionRecord(workflowDefinition);
-  const stepRuns = (await listStepRuns(ctx, typedWorkflowRun.id, companyId)).map(toStepRunRecord);
+  const stepRuns = (await listStepRuns(ctx, typedWorkflowRun.id, companyId)).map(toWorkflowStepRunRecord);
   const completed = new Set<string>();
   const failed = new Set<string>();
   const skipped = new Set<string>();
@@ -289,7 +448,7 @@
         continue;
       }
 
-      stepRun = toStepRunRecord(await createStepRun(ctx, companyId, {
+      stepRun = toWorkflowStepRunRecord(await createStepRun(ctx, companyId, {
         agentName: resolvedAgent.agentName,
         retryCount: 0,
         runId: typedWorkflowRun.id,
@@ -305,6 +464,7 @@
       stepDef,
       typedWorkflowRun.data.workflowName,
       companyId,
+      { parentIssueId: typedWorkflowRun.data.parentIssueId },
     );
   }
 
@@ -329,7 +489,7 @@
   ctx: PluginContext,
   workflowRun: WorkflowRunRecord,
   workflowDefinition: WorkflowDefinitionRecord,
-  sourceStepRun: WorkflowStepRecord,
+  sourceStepRun: WorkflowStepRunRecord,
   escalationTargetId: string,
   companyId: string,
 ): Promise<void> {
@@ -338,7 +498,7 @@
     throw new Error(`Escalation target step not found: ${escalationTargetId}`);
   }
 
-  const stepRuns = (await listStepRuns(ctx, workflowRun.id, companyId)).map(toStepRunRecord);
+  const stepRuns = (await listStepRuns(ctx, workflowRun.id, companyId)).map(toWorkflowStepRunRecord);
   let escalationStepRun = stepRuns.find((candidate) => candidate.data.stepId === escalationTargetId) ?? null;
   const resolvedAgent = await resolveStepAgent(
     ctx,
@@ -352,7 +512,7 @@
   }
 
   if (!escalationStepRun) {
-    escalationStepRun = toStepRunRecord(await createStepRun(ctx, companyId, {
+    escalationStepRun = toWorkflowStepRunRecord(await createStepRun(ctx, companyId, {
       agentName: resolvedAgent.agentName,
       retryCount: 0,
       runId: workflowRun.id,
@@ -374,14 +534,14 @@
       title: `${workflowRun.data.workflowName}: ${escalationStep.title}`,
     });
 
-    escalationStepRun = toStepRunRecord(await updateStepRun(ctx, escalationStepRun.id, {
+    escalationStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, escalationStepRun.id, {
       issueId: issue.id,
     }));
   }
 
   const shouldActivate = escalationStepRun.data.status === STEP_STATUSES.backlog;
   if (shouldActivate) {
-    escalationStepRun = toStepRunRecord(await updateStepRun(ctx, escalationStepRun.id, {
+    escalationStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, escalationStepRun.id, {
       status: STEP_STATUSES.todo,
     }));
   }
@@ -389,8 +549,8 @@
   if (shouldActivate) {
     await invokeAgentForStep(
       ctx,
+      escalationStepRun,
       escalationStep,
-      escalationStepRun.data.agentName,
       workflowRun.data.workflowName,
       companyId,
     );
@@ -421,7 +581,7 @@
     return;
   }
 
-  const typedStepRun = toStepRunRecord(stepRunRecord);
+  const typedStepRun = toWorkflowStepRunRecord(stepRunRecord);
   if (TERMINAL_STEP_STATUSES.has(typedStepRun.data.status)) {
     return;
   }
@@ -469,7 +629,7 @@
   if (options.allowRetry) {
     const retryInfo = getRetryInfo(typedWorkflowDefinition.data.steps, typedStepRun.data.stepId);
     if (retryInfo.shouldRetry && typedStepRun.data.retryCount < retryInfo.maxRetries) {
-      const retriedStepRun = toStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
+      const retriedStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
         completedAt: undefined,
         retryCount: typedStepRun.data.retryCount + 1,
         status: STEP_STATUSES.todo,
@@ -477,8 +637,8 @@
 
       await invokeAgentForStep(
         ctx,
+        retriedStepRun,
         stepDef,
-        retriedStepRun.data.agentName,
         typedWorkflowRun.data.workflowName,
         event.companyId,
       );
@@ -496,7 +656,7 @@
   }
 
   if (stepDef.onFailure === "skip") {
-    const skippedStepRun = toStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
+    const skippedStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
       completedAt: new Date().toISOString(),
       status: STEP_STATUSES.skipped,
     }));
@@ -556,7 +716,7 @@
       return;
     }
 
-    const escalatedStepRun = toStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
+    const escalatedStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
       completedAt: new Date().toISOString(),
       status: STEP_STATUSES.escalated,
     }));
@@ -617,6 +777,29 @@
   }
 }
 
+function registerDataHandler(
+  ctx: PluginContext,
+  key: string,
+  handler: (params: Record<string, unknown>) => Promise<unknown>,
+): void {
+  const dataClient = ctx.data as {
+    handle?: (handlerKey: string, handlerFn: (params: Record<string, unknown>) => Promise<unknown>) => void;
+    register?: (handlerKey: string, handlerFn: (params: Record<string, unknown>) => Promise<unknown>) => void;
+  };
+
+  if (typeof dataClient.handle === "function") {
+    dataClient.handle(key, handler);
+    return;
+  }
+
+  if (typeof dataClient.register === "function") {
+    dataClient.register(key, handler);
+    return;
+  }
+
+  throw new Error("Plugin data client does not support handler registration");
+}
+
 const plugin = definePlugin({
   async setup(ctx: PluginContext) {
     ctx.events.on("issue.updated", async (event: PluginEvent) => {
@@ -638,7 +821,7 @@
           return;
         }
 
-        const stepRun = toStepRunRecord(stepRunRecord);
+        const stepRun = toWorkflowStepRunRecord(stepRunRecord);
         const payload = event.payload as { status?: string };
         const issueStatus = typeof payload.status === "string" ? payload.status : undefined;
 
@@ -657,7 +840,7 @@
           return;
         }
 
-        const updatedStepRun = toStepRunRecord(await updateStepRun(ctx, stepRun.id, {
+        const updatedStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, stepRun.id, {
           completedAt: nextStepStatus === STEP_STATUSES.done ? new Date().toISOString() : undefined,
           startedAt: nextStepStatus === STEP_STATUSES.inProgress
             ? stepRun.data.startedAt ?? new Date().toISOString()
@@ -687,11 +870,21 @@
 
     ctx.events.on("issue.created", async (event: PluginEvent) => {
       try {
+        const idempotencyKey = buildIdempotencyKey(event);
+        if (await checkIdempotency(ctx, idempotencyKey, event.companyId)) {
+          return;
+        }
+
         const payload = event.payload as {
           id?: string;
           parentId?: string;
           assigneeAgentId?: string;
         };
+        const issueId = typeof event.entityId === "string" && event.entityId.trim()
+          ? event.entityId.trim()
+          : typeof payload.id === "string" && payload.id.trim()
+            ? payload.id.trim()
+            : "";
 
         const parentId = typeof payload.parentId === "string" && payload.parentId.trim()
           ? payload.parentId.trim()
@@ -700,12 +893,34 @@
           ? payload.assigneeAgentId.trim()
           : "";
 
-        if (parentId || !assigneeAgentId) {
+        if (!issueId || parentId || !assigneeAgentId) {
+          await markIdempotency(ctx, idempotencyKey, event.companyId);
+          return;
+        }
+
+        const agent = await ctx.agents.get(assigneeAgentId, event.companyId);
+        const metadata = agent?.metadata as Record<string, unknown> | undefined;
+        const defaultParentIssueId = typeof metadata?.defaultParentIssueId === "string" && metadata.defaultParentIssueId.trim()
+          ? metadata.defaultParentIssueId.trim()
+          : "";
+
+        if (!defaultParentIssueId) {
+          await markIdempotency(ctx, idempotencyKey, event.companyId);
           return;
         }
 
-        ctx.logger.info("parentId filler: issue created without parent", {
-          issueId: event.entityId ?? payload.id ?? null,
+        await ctx.issues.update(
+          issueId,
+          { parentId: defaultParentIssueId } as IssueUpdatePatch,
+          event.companyId,
+        );
+        await markIdempotency(ctx, idempotencyKey, event.companyId);
+
+        ctx.logger.info("parentId filler: populated issue parentId from agent metadata", {
+          assigneeAgentId,
+          companyId: event.companyId,
+          issueId,
+          parentId: defaultParentIssueId,
         });
       } catch (error) {
         ctx.logger.warn("Failed to handle issue.created event", {
@@ -744,7 +959,23 @@
       await runReconciler(ctx);
     });
 
-    ctx.data.register("workflow-overview", async (params: Record<string, unknown>) => {
+    registerDataHandler(ctx, "start-workflow", async (params: Record<string, unknown>) => {
+      const workflowId = typeof params.workflowId === "string" ? params.workflowId.trim() : "";
+      const companyId = typeof params.companyId === "string" ? params.companyId.trim() : "";
+      const parentIssueId = typeof params.parentIssueId === "string" ? params.parentIssueId.trim() : "";
+      const createParentIssue = params.createParentIssue === true;
+
+      if (!workflowId || !companyId) {
+        throw new Error("start-workflow requires workflowId and companyId");
+      }
+
+      return await startWorkflow(ctx, workflowId, companyId, {
+        createParentIssue,
+        parentIssueId: parentIssueId || undefined,
+      });
+    });
+
+    registerDataHandler(ctx, "workflow-overview", async (params: Record<string, unknown>) => {
       try {
         const companyId = typeof params.companyId === "string" ? params.companyId.trim() : "";
         if (!companyId) {
@@ -782,7 +1013,7 @@
       }
     });
 
-    ctx.data.register("workflow-run-detail", async (params: Record<string, unknown>) => {
+    registerDataHandler(ctx, "workflow-run-detail", async (params: Record<string, unknown>) => {
       try {
         const runId = typeof params.runId === "string" ? params.runId.trim() : "";
         if (!runId) {
@@ -807,7 +1038,7 @@
             ...typedWorkflowRun.data,
           },
           stepRuns: stepRuns.map((record: PluginEntityRecord) => {
-            const stepRun = toStepRunRecord(record);
+            const stepRun = toWorkflowStepRunRecord(record);
             return {
               id: stepRun.id,
               status: stepRun.status,
diff --git a/paperclip-addon/plugins/workflow-engine/src/workflow-store.ts b/paperclip-addon/plugins/workflow-engine/src/workflow-store.ts
index 5871a2082258cebcfe8b69ebafbb0814cf303d50..82eb46836883940e109368e927b7e367c96616b5
--- a/paperclip-addon/plugins/workflow-engine/src/workflow-store.ts
+++ b/paperclip-addon/plugins/workflow-engine/src/workflow-store.ts
@@ -44,7 +44,14 @@
 
 type TypedEntityRecord<T> = Omit<PluginEntityRecord, "data"> & { data: T };
 
-function toTypedRecord<T>(record: PluginEntityRecord): TypedEntityRecord<T> {
+function toTypedRecord<T>(
+  record: PluginEntityRecord,
+  entityType: string,
+): TypedEntityRecord<T> {
+  if (record.entityType !== entityType) {
+    throw new Error(`Expected entity type "${entityType}", got "${record.entityType}"`);
+  }
+
   return record as TypedEntityRecord<T>;
 }
 
@@ -67,13 +74,31 @@
   id: string,
   entityType: string,
 ): Promise<TypedEntityRecord<T> | null> {
-  const record = await ctx.entities.get(id);
+  const listQuery = {
+    entityType,
+    id,
+    limit: 1,
+  } as Record<string, unknown>;
 
-  if (!record || record.entityType !== entityType) {
+  const listed = await ctx.entities.list(
+    listQuery as Parameters<PluginContext["entities"]["list"]>[0],
+  );
+  const listedRecord = listed.find(
+    (record: PluginEntityRecord) => record.id === id,
+  ) ?? null;
+
+  if (listedRecord) {
+    return toTypedRecord<T>(listedRecord, entityType);
+  }
+
+  // Fallback: some host SDK variants ignore `id` on entities.list().
+  // Keep defensive type verification on direct get() until id-filter support is universal.
+  const record = await ctx.entities.get(id);
+  if (!record) {
     return null;
   }
 
-  return toTypedRecord<T>(record);
+  return toTypedRecord<T>(record, entityType);
 }
 
 async function requireEntityByType<T>(
diff --git a/paperclip-addon/plugins/workflow-engine/src/workflow-utils.ts b/paperclip-addon/plugins/workflow-engine/src/workflow-utils.ts
new file mode 100644
index 0000000000000000000000000000000000000000..ec1cf6f6b9aa722b126b58c0a728b4e538ad1494
--- /dev/null
+++ b/paperclip-addon/plugins/workflow-engine/src/workflow-utils.ts
@@ -0,0 +1,62 @@
+import type { PluginEntityRecord } from "@paperclipai/plugin-sdk";
+
+import { STEP_STATUSES } from "./constants.js";
+import type { WorkflowStep } from "./dag-engine.js";
+import type {
+  WorkflowDefinition,
+  WorkflowRun,
+  WorkflowStepRun,
+} from "./workflow-store.js";
+
+export type WorkflowRunRecord = PluginEntityRecord & { data: WorkflowRun };
+export type WorkflowStepRunRecord = PluginEntityRecord & { data: WorkflowStepRun };
+export type WorkflowDefinitionRecord = PluginEntityRecord & { data: WorkflowDefinition };
+type StepAgentMetadata = WorkflowStep & {
+  agent?: string;
+  agentName?: string;
+  assigneeAgentName?: string;
+};
+
+export const TERMINAL_STEP_STATUSES = new Set<string>([
+  STEP_STATUSES.done,
+  STEP_STATUSES.failed,
+  STEP_STATUSES.skipped,
+  STEP_STATUSES.escalated,
+]);
+
+export function toWorkflowRunRecord(record: PluginEntityRecord): WorkflowRunRecord {
+  return record as WorkflowRunRecord;
+}
+
+export function toWorkflowStepRunRecord(record: PluginEntityRecord): WorkflowStepRunRecord {
+  return record as WorkflowStepRunRecord;
+}
+
+export function toWorkflowDefinitionRecord(record: PluginEntityRecord): WorkflowDefinitionRecord {
+  return record as WorkflowDefinitionRecord;
+}
+
+export function findStepDefinition(definition: WorkflowDefinitionRecord, stepId: string): WorkflowStep | null {
+  return definition.data.steps.find((step: WorkflowStep) => step.id === stepId) ?? null;
+}
+
+export function getStepAgentNameHint(stepDef: WorkflowStep): string | null {
+  const stepMeta = stepDef as StepAgentMetadata;
+  const candidates = [stepMeta.agentName, stepMeta.agent, stepMeta.assigneeAgentName];
+
+  for (const candidate of candidates) {
+    if (typeof candidate === "string" && candidate.trim()) {
+      return candidate.trim();
+    }
+  }
+
+  return null;
+}
+
+export function getStepAgentName(stepRun: WorkflowStepRunRecord, stepDef: WorkflowStep): string | null {
+  if (typeof stepRun.data.agentName === "string" && stepRun.data.agentName.trim()) {
+    return stepRun.data.agentName.trim();
+  }
+
+  return getStepAgentNameHint(stepDef);
+}

exec
/bin/zsh -lc "sed -n '1,130p' plugins/workflow-engine/src/workflow-store.ts" in /Users/kwak/Projects/paperclip/paperclip-addon succeeded in 0ms:
import type { PluginContext, PluginEntityRecord } from "@paperclipai/plugin-sdk";

import { ENTITY_TYPES } from "./constants.js";
import type { WorkflowStep } from "./dag-engine.js";

export interface WorkflowDefinition {
  name: string;
  description: string;
  companyId: string;
  status: "active" | "paused" | "archived";
  steps: WorkflowStep[];
  timeoutMinutes?: number;
  maxConcurrentRuns?: number;
}

export interface WorkflowRun {
  workflowId: string;
  workflowName: string;
  companyId: string;
  status: "running" | "completed" | "failed" | "aborted" | "timed-out";
file update:
diff --git a/paperclip-addon/plugins/workflow-engine/src/dag-engine.ts b/paperclip-addon/plugins/workflow-engine/src/dag-engine.ts
index b59510f0473171038be77277286d3b5c2063e6c5..e7f21ff998cab78b9325a8aeacf4fda9c5e06553
--- a/paperclip-addon/plugins/workflow-engine/src/dag-engine.ts
+++ b/paperclip-addon/plugins/workflow-engine/src/dag-engine.ts
@@ -2,6 +2,7 @@
   id: string;
   title: string;
   dependsOn: string[];
+  sessionMode?: "fresh" | "reuse";
   onFailure?: "retry" | "skip" | "abort_workflow" | "escalate";
   escalateTo?: string;
   maxRetries?: number;
diff --git a/paperclip-addon/plugins/workflow-engine/src/reconciler.ts b/paperclip-addon/plugins/workflow-engine/src/reconciler.ts
index 14ab4e766a835d145a6c2e7b1b564270285cbdc3..55b649037e8a19a88bb39b9633a6a0cc84fa8d59
--- a/paperclip-addon/plugins/workflow-engine/src/reconciler.ts
+++ b/paperclip-addon/plugins/workflow-engine/src/reconciler.ts
@@ -8,65 +8,21 @@
   listStepRuns,
   updateStepRun,
   updateWorkflowRun,
-  type WorkflowDefinition,
-  type WorkflowRun,
-  type WorkflowStepRun,
 } from "./workflow-store.js";
-import type { WorkflowStep } from "./dag-engine.js";
-
-type WorkflowRunRecord = PluginEntityRecord & { data: WorkflowRun };
-type WorkflowStepRunRecord = PluginEntityRecord & { data: WorkflowStepRun };
-type WorkflowDefinitionRecord = PluginEntityRecord & { data: WorkflowDefinition };
-type StepAgentMetadata = WorkflowStep & {
-  agent?: string;
-  agentName?: string;
-  assigneeAgentName?: string;
-};
+import {
+  TERMINAL_STEP_STATUSES,
+  findStepDefinition,
+  getStepAgentName,
+  toWorkflowDefinitionRecord,
+  toWorkflowRunRecord,
+  toWorkflowStepRunRecord,
+} from "./workflow-utils.js";
 
 const STALE_THRESHOLD_MS = 300_000;
-const TERMINAL_STEP_STATUSES = new Set<string>([
-  STEP_STATUSES.done,
-  STEP_STATUSES.failed,
-  STEP_STATUSES.skipped,
-  STEP_STATUSES.escalated,
-]);
 
 function isStale(record: PluginEntityRecord, thresholdMs: number): boolean {
   const updatedAt = new Date(record.updatedAt).getTime();
   return Date.now() - updatedAt > thresholdMs;
-}
-
-function toWorkflowRunRecord(record: PluginEntityRecord): WorkflowRunRecord {
-  return record as WorkflowRunRecord;
-}
-
-function toWorkflowStepRunRecord(record: PluginEntityRecord): WorkflowStepRunRecord {
-  return record as WorkflowStepRunRecord;
-}
-
-function toWorkflowDefinitionRecord(record: PluginEntityRecord): WorkflowDefinitionRecord {
-  return record as WorkflowDefinitionRecord;
-}
-
-function findStepDefinition(definition: WorkflowDefinitionRecord, stepId: string): WorkflowStep | null {
-  return definition.data.steps.find((step: WorkflowStep) => step.id === stepId) ?? null;
-}
-
-function getStepAgentName(stepRun: WorkflowStepRunRecord, stepDef: WorkflowStep): string | null {
-  if (typeof stepRun.data.agentName === "string" && stepRun.data.agentName.trim()) {
-    return stepRun.data.agentName.trim();
-  }
-
-  const stepMeta = stepDef as StepAgentMetadata;
-  const candidates = [stepMeta.agentName, stepMeta.agent, stepMeta.assigneeAgentName];
-
-  for (const candidate of candidates) {
-    if (typeof candidate === "string" && candidate.trim()) {
-      return candidate.trim();
-    }
-  }
-
-  return null;
 }
 
 async function invokeAgentByName(
diff --git a/paperclip-addon/plugins/workflow-engine/src/worker.ts b/paperclip-addon/plugins/workflow-engine/src/worker.ts
index c5ac3fbb4ca7f767eb29331bd3630b635449b335..303e35958890fa1f66db3f2b0ff1fa1f134102a8
--- a/paperclip-addon/plugins/workflow-engine/src/worker.ts
+++ b/paperclip-addon/plugins/workflow-engine/src/worker.ts
@@ -15,6 +15,7 @@
 } from "./dag-engine.js";
 import {
   checkIdempotency,
+  createWorkflowRun,
   createStepRun,
   findStepRunByIssueId,
   getWorkflowDefinition,
@@ -25,51 +26,38 @@
   markIdempotency,
   updateStepRun,
   updateWorkflowRun,
-  type WorkflowDefinition,
-  type WorkflowRun,
   type WorkflowStepRun,
 } from "./workflow-store.js";
+import {
+  TERMINAL_STEP_STATUSES,
+  findStepDefinition,
+  getStepAgentName,
+  getStepAgentNameHint,
+  toWorkflowDefinitionRecord,
+  toWorkflowRunRecord,
+  toWorkflowStepRunRecord,
+  type WorkflowDefinitionRecord,
+  type WorkflowRunRecord,
+  type WorkflowStepRunRecord,
+} from "./workflow-utils.js";
 
-type WorkflowStepRecord = PluginEntityRecord & { data: WorkflowStepRun };
-type WorkflowRunRecord = PluginEntityRecord & { data: WorkflowRun };
-type WorkflowDefinitionRecord = PluginEntityRecord & { data: WorkflowDefinition };
 type WorkflowStepMetadata = WorkflowStep & {
   description?: string;
-  agent?: string;
-  agentName?: string;
-  assigneeAgentName?: string;
   assigneeAgentId?: string;
+  sessionMode?: "fresh" | "reuse";
 };
 type AgentRecord = Awaited<ReturnType<PluginContext["agents"]["list"]>>[number];
+type IssueUpdatePatch = Parameters<PluginContext["issues"]["update"]>[1];
 type ReconcilerModule = {
   reconcileStuckSteps?: (ctx: PluginContext) => Promise<void>;
 };
 
-const TERMINAL_STEP_STATUSES = new Set<string>([
-  STEP_STATUSES.done,
-  STEP_STATUSES.failed,
-  STEP_STATUSES.skipped,
-  STEP_STATUSES.escalated,
-]);
-
 function summarizeError(error: unknown): string {
   if (error instanceof Error) {
     return error.message;
   }
 
   return String(error);
-}
-
-function toStepRunRecord(record: PluginEntityRecord): WorkflowStepRecord {
-  return record as WorkflowStepRecord;
-}
-
-function toWorkflowRunRecord(record: PluginEntityRecord): WorkflowRunRecord {
-  return record as WorkflowRunRecord;
-}
-
-function toWorkflowDefinitionRecord(record: PluginEntityRecord): WorkflowDefinitionRecord {
-  return record as WorkflowDefinitionRecord;
 }
 
 function buildIdempotencyKey(event: PluginEvent): string {
@@ -78,10 +66,6 @@
 
 function getStepMetadata(stepDef: WorkflowStep): WorkflowStepMetadata {
   return stepDef as WorkflowStepMetadata;
-}
-
-function findStepDefinition(definition: WorkflowDefinitionRecord, stepId: string): WorkflowStep | null {
-  return definition.data.steps.find((step: WorkflowStep) => step.id === stepId) ?? null;
 }
 
 function getStepDescription(stepDef: WorkflowStep): string | undefined {
@@ -89,19 +73,6 @@
   return typeof description === "string" && description.trim() ? description.trim() : undefined;
 }
 
-function getStepAgentNameHint(stepDef: WorkflowStep): string | null {
-  const meta = getStepMetadata(stepDef);
-  const candidates = [meta.agentName, meta.agent, meta.assigneeAgentName];
-
-  for (const candidate of candidates) {
-    if (typeof candidate === "string" && candidate.trim()) {
-      return candidate.trim();
-    }
-  }
-
-  return null;
-}
-
 function getStepAgentIdHint(stepDef: WorkflowStep): string | null {
   const assigneeAgentId = getStepMetadata(stepDef).assigneeAgentId;
   if (typeof assigneeAgentId !== "string" || !assigneeAgentId.trim()) {
@@ -147,68 +118,256 @@
 
 async function invokeAgentForStep(
   ctx: PluginContext,
+  stepRunRecord: WorkflowStepRunRecord,
   stepDef: WorkflowStep,
-  agentName: string,
   workflowName: string,
   companyId: string,
-): Promise<void> {
+): Promise<WorkflowStepRunRecord> {
+  const agentName = getStepAgentName(stepRunRecord, stepDef);
+  if (!agentName) {
+    ctx.logger.warn("Agent name is missing for workflow step", {
+      companyId,
+      runId: stepRunRecord.data.runId,
+      stepId: stepDef.id,
+      workflowName,
+    });
+    return stepRunRecord;
+  }
+
   const agents = await ctx.agents.list({ companyId });
-  const agent = agents.find((candidate: AgentRecord) => candidate.name === agentName);
+  const agent = agents.find((candidate: AgentRecord) => candidate.name === agentName) ?? null;
 
   if (!agent) {
     ctx.logger.warn("Agent not found for step", {
       agentName,
       companyId,
+      runId: stepRunRecord.data.runId,
+      stepId: stepDef.id,
+      workflowName,
+    });
+    return stepRunRecord;
+  }
+
+  const prompt = `workflow:${workflowName}/step:${stepDef.id} — "${stepDef.title}" is ready. Please proceed with the assigned task.`;
+  const reason = `workflow:${workflowName}/step:${stepDef.id}`;
+  const sessionMode = getStepMetadata(stepDef).sessionMode === "reuse" ? "reuse" : "fresh";
+
+  if (sessionMode === "reuse") {
+    let sessionId = typeof stepRunRecord.data.sessionId === "string" && stepRunRecord.data.sessionId.trim()
+      ? stepRunRecord.data.sessionId.trim()
+      : "";
+
+    if (!sessionId) {
+      const session = await ctx.agents.sessions.create(agent.id, companyId, {
+        reason,
+      });
+      sessionId = session.sessionId;
+      stepRunRecord = toWorkflowStepRunRecord(await updateStepRun(ctx, stepRunRecord.id, {
+        sessionId,
+      }));
+    }
+
+    await ctx.agents.sessions.sendMessage(sessionId, companyId, {
+      prompt,
+      reason,
+    });
+
+    ctx.logger.info("Sent workflow step prompt via agent session", {
+      agentId: agent.id,
+      agentName,
+      companyId,
+      runId: stepRunRecord.data.runId,
+      sessionId,
       stepId: stepDef.id,
       workflowName,
     });
-    return;
+    return stepRunRecord;
   }
 
   await ctx.agents.invoke(agent.id, companyId, {
-    prompt: `workflow:${workflowName}/step:${stepDef.id} — "${stepDef.title}" is ready. Please proceed with the assigned task.`,
-    reason: `workflow:${workflowName}/step:${stepDef.id}`,
+    prompt,
+    reason,
   });
 
   ctx.logger.info("Invoked agent for workflow step", {
     agentId: agent.id,
     agentName,
     companyId,
+    runId: stepRunRecord.data.runId,
     stepId: stepDef.id,
     workflowName,
   });
+
+  return stepRunRecord;
 }
 
 async function activateBacklogStep(
   ctx: PluginContext,
-  stepRunRecord: WorkflowStepRecord,
+  stepRunRecord: WorkflowStepRunRecord,
   stepDef: WorkflowStep,
   workflowName: string,
   companyId: string,
-): Promise<void> {
+  options?: { parentIssueId?: string },
+): Promise<WorkflowStepRunRecord> {
   if (stepRunRecord.data.status !== STEP_STATUSES.backlog) {
-    return;
+    return stepRunRecord;
   }
 
-  const updated = toStepRunRecord(await updateStepRun(ctx, stepRunRecord.id, {
+  const resolvedAgent = await resolveStepAgent(
+    ctx,
+    companyId,
+    stepDef,
+    stepRunRecord.data.agentName,
+  );
+
+  let issueId = typeof stepRunRecord.data.issueId === "string" && stepRunRecord.data.issueId.trim()
+    ? stepRunRecord.data.issueId.trim()
+    : "";
+  const stepDescription = getStepDescription(stepDef) ?? `Workflow step: ${stepDef.id}`;
+
+  if (!issueId) {
+    const issue = await ctx.issues.create({
+      assigneeAgentId: resolvedAgent.agentId ?? undefined,
+      companyId,
+      description: stepDescription,
+      parentId: options?.parentIssueId,
+      title: `[${workflowName}] ${stepDef.title}`,
+    });
+    await ctx.issues.update(issue.id, { status: "todo" } as IssueUpdatePatch, companyId);
+    issueId = issue.id;
+  }
+
+  const nextStartedAt = stepRunRecord.data.startedAt ?? new Date().toISOString();
+  let updatedStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, stepRunRecord.id, {
+    agentName: resolvedAgent.agentName ?? stepRunRecord.data.agentName,
+    issueId,
+    startedAt: nextStartedAt,
     status: STEP_STATUSES.todo,
   }));
 
-  await invokeAgentForStep(
+  updatedStepRun = await invokeAgentForStep(
     ctx,
+    updatedStepRun,
     stepDef,
-    updated.data.agentName,
     workflowName,
     companyId,
   );
+
+  return updatedStepRun;
 }
 
+async function startWorkflow(
+  ctx: PluginContext,
+  workflowId: string,
+  companyId: string,
+  options?: {
+    createParentIssue?: boolean;
+    parentIssueId?: string;
+  },
+): Promise<{
+  activatedStepIds: string[];
+  parentIssueId: string | null;
+  runId: string;
+  workflowId: string;
+}> {
+  const workflowDefinition = await getWorkflowDefinition(ctx, workflowId);
+  if (!workflowDefinition) {
+    throw new Error(`Workflow definition not found: ${workflowId}`);
+  }
+
+  const typedWorkflowDefinition = toWorkflowDefinitionRecord(workflowDefinition);
+  if (typedWorkflowDefinition.data.companyId !== companyId) {
+    throw new Error(`Workflow does not belong to company: ${workflowId}`);
+  }
+
+  let parentIssueId = typeof options?.parentIssueId === "string" && options.parentIssueId.trim()
+    ? options.parentIssueId.trim()
+    : "";
+  if (!parentIssueId && options?.createParentIssue) {
+    const parentIssue = await ctx.issues.create({
+      companyId,
+      description: typedWorkflowDefinition.data.description || `Workflow run: ${typedWorkflowDefinition.data.name}`,
+      title: `[${typedWorkflowDefinition.data.name}] Workflow run`,
+    });
+    parentIssueId = parentIssue.id;
+  }
+
+  const workflowRun = toWorkflowRunRecord(await createWorkflowRun(ctx, {
+    companyId,
+    parentIssueId: parentIssueId || undefined,
+    startedAt: new Date().toISOString(),
+    status: RUN_STATUSES.running,
+    workflowId: typedWorkflowDefinition.id,
+    workflowName: typedWorkflowDefinition.data.name,
+  }));
+
+  const agents = await ctx.agents.list({ companyId });
+  const agentsByName = new Map<string, AgentRecord>();
+  for (const agent of agents) {
+    agentsByName.set(agent.name, agent);
+  }
+
+  const pendingRootSteps: Array<{ stepDef: WorkflowStep; stepRun: WorkflowStepRunRecord }> = [];
+  for (const stepDef of typedWorkflowDefinition.data.steps) {
+    const agentNameHint = getStepAgentNameHint(stepDef);
+    const matchedAgent = agentNameHint ? agentsByName.get(agentNameHint) ?? null : null;
+    const resolvedAgent = matchedAgent
+      ? { agentId: matchedAgent.id, agentName: matchedAgent.name }
+      : await resolveStepAgent(ctx, companyId, stepDef, agentNameHint ?? undefined);
+
+    if (!resolvedAgent.agentName) {
+      throw new Error(`Unable to resolve step assignee for "${stepDef.id}"`);
+    }
+
+    const stepRun = toWorkflowStepRunRecord(await createStepRun(ctx, companyId, {
+      agentName: resolvedAgent.agentName,
+      retryCount: 0,
+      runId: workflowRun.id,
+      status: STEP_STATUSES.backlog,
+      stepId: stepDef.id,
+    }));
+
+    if (stepDef.dependsOn.length === 0 && stepDef.triggerOn !== "escalation") {
+      pendingRootSteps.push({ stepDef, stepRun });
+    }
+  }
+
+  const activatedStepIds: string[] = [];
+  for (const pending of pendingRootSteps) {
+    await activateBacklogStep(
+      ctx,
+      pending.stepRun,
+      pending.stepDef,
+      typedWorkflowDefinition.data.name,
+      companyId,
+      { parentIssueId: parentIssueId || undefined },
+    );
+    activatedStepIds.push(pending.stepDef.id);
+  }
+
+  ctx.logger.info("Started workflow run", {
+    activatedStepIds,
+    companyId,
+    parentIssueId: parentIssueId || null,
+    runId: workflowRun.id,
+    workflowId: typedWorkflowDefinition.id,
+    workflowName: typedWorkflowDefinition.data.name,
+  });
+
+  return {
+    activatedStepIds,
+    parentIssueId: parentIssueId || null,
+    runId: workflowRun.id,
+    workflowId: typedWorkflowDefinition.id,
+  };
+}
+
 async function advanceWorkflow(
   ctx: PluginContext,
   stepRunRecord: PluginEntityRecord,
   companyId: string,
 ): Promise<void> {
-  const completedStepRun = toStepRunRecord(stepRunRecord);
+  const completedStepRun = toWorkflowStepRunRecord(stepRunRecord);
   const workflowRun = await getWorkflowRun(ctx, completedStepRun.data.runId);
 
   if (!workflowRun) {
@@ -237,7 +396,7 @@
   }
 
   const typedWorkflowDefinition = toWorkflowDefinitionRecord(workflowDefinition);
-  const stepRuns = (await listStepRuns(ctx, typedWorkflowRun.id, companyId)).map(toStepRunRecord);
+  const stepRuns = (await listStepRuns(ctx, typedWorkflowRun.id, companyId)).map(toWorkflowStepRunRecord);
   const completed = new Set<string>();
   const failed = new Set<string>();
   const skipped = new Set<string>();
@@ -289,7 +448,7 @@
         continue;
       }
 
-      stepRun = toStepRunRecord(await createStepRun(ctx, companyId, {
+      stepRun = toWorkflowStepRunRecord(await createStepRun(ctx, companyId, {
         agentName: resolvedAgent.agentName,
         retryCount: 0,
         runId: typedWorkflowRun.id,
@@ -305,6 +464,7 @@
       stepDef,
       typedWorkflowRun.data.workflowName,
       companyId,
+      { parentIssueId: typedWorkflowRun.data.parentIssueId },
     );
   }
 
@@ -329,7 +489,7 @@
   ctx: PluginContext,
   workflowRun: WorkflowRunRecord,
   workflowDefinition: WorkflowDefinitionRecord,
-  sourceStepRun: WorkflowStepRecord,
+  sourceStepRun: WorkflowStepRunRecord,
   escalationTargetId: string,
   companyId: string,
 ): Promise<void> {
@@ -338,7 +498,7 @@
     throw new Error(`Escalation target step not found: ${escalationTargetId}`);
   }
 
-  const stepRuns = (await listStepRuns(ctx, workflowRun.id, companyId)).map(toStepRunRecord);
+  const stepRuns = (await listStepRuns(ctx, workflowRun.id, companyId)).map(toWorkflowStepRunRecord);
   let escalationStepRun = stepRuns.find((candidate) => candidate.data.stepId === escalationTargetId) ?? null;
   const resolvedAgent = await resolveStepAgent(
     ctx,
@@ -352,7 +512,7 @@
   }
 
   if (!escalationStepRun) {
-    escalationStepRun = toStepRunRecord(await createStepRun(ctx, companyId, {
+    escalationStepRun = toWorkflowStepRunRecord(await createStepRun(ctx, companyId, {
       agentName: resolvedAgent.agentName,
       retryCount: 0,
       runId: workflowRun.id,
@@ -374,14 +534,14 @@
       title: `${workflowRun.data.workflowName}: ${escalationStep.title}`,
     });
 
-    escalationStepRun = toStepRunRecord(await updateStepRun(ctx, escalationStepRun.id, {
+    escalationStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, escalationStepRun.id, {
       issueId: issue.id,
     }));
   }
 
   const shouldActivate = escalationStepRun.data.status === STEP_STATUSES.backlog;
   if (shouldActivate) {
-    escalationStepRun = toStepRunRecord(await updateStepRun(ctx, escalationStepRun.id, {
+    escalationStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, escalationStepRun.id, {
       status: STEP_STATUSES.todo,
     }));
   }
@@ -389,8 +549,8 @@
   if (shouldActivate) {
     await invokeAgentForStep(
       ctx,
+      escalationStepRun,
       escalationStep,
-      escalationStepRun.data.agentName,
       workflowRun.data.workflowName,
       companyId,
     );
@@ -421,7 +581,7 @@
     return;
   }
 
-  const typedStepRun = toStepRunRecord(stepRunRecord);
+  const typedStepRun = toWorkflowStepRunRecord(stepRunRecord);
   if (TERMINAL_STEP_STATUSES.has(typedStepRun.data.status)) {
     return;
   }
@@ -469,7 +629,7 @@
   if (options.allowRetry) {
     const retryInfo = getRetryInfo(typedWorkflowDefinition.data.steps, typedStepRun.data.stepId);
     if (retryInfo.shouldRetry && typedStepRun.data.retryCount < retryInfo.maxRetries) {
-      const retriedStepRun = toStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
+      const retriedStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
         completedAt: undefined,
         retryCount: typedStepRun.data.retryCount + 1,
         status: STEP_STATUSES.todo,
@@ -477,8 +637,8 @@
 
       await invokeAgentForStep(
         ctx,
+        retriedStepRun,
         stepDef,
-        retriedStepRun.data.agentName,
         typedWorkflowRun.data.workflowName,
         event.companyId,
       );
@@ -496,7 +656,7 @@
   }
 
   if (stepDef.onFailure === "skip") {
-    const skippedStepRun = toStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
+    const skippedStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
       completedAt: new Date().toISOString(),
       status: STEP_STATUSES.skipped,
     }));
@@ -556,7 +716,7 @@
       return;
     }
 
-    const escalatedStepRun = toStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
+    const escalatedStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
       completedAt: new Date().toISOString(),
       status: STEP_STATUSES.escalated,
     }));
@@ -617,6 +777,29 @@
   }
 }
 
+function registerDataHandler(
+  ctx: PluginContext,
+  key: string,
+  handler: (params: Record<string, unknown>) => Promise<unknown>,
+): void {
+  const dataClient = ctx.data as {
+    handle?: (handlerKey: string, handlerFn: (params: Record<string, unknown>) => Promise<unknown>) => void;
+    register?: (handlerKey: string, handlerFn: (params: Record<string, unknown>) => Promise<unknown>) => void;
+  };
+
+  if (typeof dataClient.handle === "function") {
+    dataClient.handle(key, handler);
+    return;
+  }
+
+  if (typeof dataClient.register === "function") {
+    dataClient.register(key, handler);
+    return;
+  }
+
+  throw new Error("Plugin data client does not support handler registration");
+}
+
 const plugin = definePlugin({
   async setup(ctx: PluginContext) {
     ctx.events.on("issue.updated", async (event: PluginEvent) => {
@@ -638,7 +821,7 @@
           return;
         }
 
-        const stepRun = toStepRunRecord(stepRunRecord);
+        const stepRun = toWorkflowStepRunRecord(stepRunRecord);
         const payload = event.payload as { status?: string };
         const issueStatus = typeof payload.status === "string" ? payload.status : undefined;
 
@@ -657,7 +840,7 @@
           return;
         }
 
-        const updatedStepRun = toStepRunRecord(await updateStepRun(ctx, stepRun.id, {
+        const updatedStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, stepRun.id, {
           completedAt: nextStepStatus === STEP_STATUSES.done ? new Date().toISOString() : undefined,
           startedAt: nextStepStatus === STEP_STATUSES.inProgress
             ? stepRun.data.startedAt ?? new Date().toISOString()
@@ -687,11 +870,21 @@
 
     ctx.events.on("issue.created", async (event: PluginEvent) => {
       try {
+        const idempotencyKey = buildIdempotencyKey(event);
+        if (await checkIdempotency(ctx, idempotencyKey, event.companyId)) {
+          return;
+        }
+
         const payload = event.payload as {
           id?: string;
           parentId?: string;
           assigneeAgentId?: string;
         };
+        const issueId = typeof event.entityId === "string" && event.entityId.trim()
+          ? event.entityId.trim()
+          : typeof payload.id === "string" && payload.id.trim()
+            ? payload.id.trim()
+            : "";
 
         const parentId = typeof payload.parentId === "string" && payload.parentId.trim()
           ? payload.parentId.trim()
@@ -700,12 +893,34 @@
           ? payload.assigneeAgentId.trim()
           : "";
 
-        if (parentId || !assigneeAgentId) {
+        if (!issueId || parentId || !assigneeAgentId) {
+          await markIdempotency(ctx, idempotencyKey, event.companyId);
+          return;
+        }
+
+        const agent = await ctx.agents.get(assigneeAgentId, event.companyId);
+        const metadata = agent?.metadata as Record<string, unknown> | undefined;
+        const defaultParentIssueId = typeof metadata?.defaultParentIssueId === "string" && metadata.defaultParentIssueId.trim()
+          ? metadata.defaultParentIssueId.trim()
+          : "";
+
+        if (!defaultParentIssueId) {
+          await markIdempotency(ctx, idempotencyKey, event.companyId);
           return;
         }
 
-        ctx.logger.info("parentId filler: issue created without parent", {
-          issueId: event.entityId ?? payload.id ?? null,
+        await ctx.issues.update(
+          issueId,
+          { parentId: defaultParentIssueId } as IssueUpdatePatch,
+          event.companyId,
+        );
+        await markIdempotency(ctx, idempotencyKey, event.companyId);
+
+        ctx.logger.info("parentId filler: populated issue parentId from agent metadata", {
+          assigneeAgentId,
+          companyId: event.companyId,
+          issueId,
+          parentId: defaultParentIssueId,
         });
       } catch (error) {
         ctx.logger.warn("Failed to handle issue.created event", {
@@ -744,7 +959,23 @@
       await runReconciler(ctx);
     });
 
-    ctx.data.register("workflow-overview", async (params: Record<string, unknown>) => {
+    registerDataHandler(ctx, "start-workflow", async (params: Record<string, unknown>) => {
+      const workflowId = typeof params.workflowId === "string" ? params.workflowId.trim() : "";
+      const companyId = typeof params.companyId === "string" ? params.companyId.trim() : "";
+      const parentIssueId = typeof params.parentIssueId === "string" ? params.parentIssueId.trim() : "";
+      const createParentIssue = params.createParentIssue === true;
+
+      if (!workflowId || !companyId) {
+        throw new Error("start-workflow requires workflowId and companyId");
+      }
+
+      return await startWorkflow(ctx, workflowId, companyId, {
+        createParentIssue,
+        parentIssueId: parentIssueId || undefined,
+      });
+    });
+
+    registerDataHandler(ctx, "workflow-overview", async (params: Record<string, unknown>) => {
       try {
         const companyId = typeof params.companyId === "string" ? params.companyId.trim() : "";
         if (!companyId) {
@@ -782,7 +1013,7 @@
       }
     });
 
-    ctx.data.register("workflow-run-detail", async (params: Record<string, unknown>) => {
+    registerDataHandler(ctx, "workflow-run-detail", async (params: Record<string, unknown>) => {
       try {
         const runId = typeof params.runId === "string" ? params.runId.trim() : "";
         if (!runId) {
@@ -807,7 +1038,7 @@
             ...typedWorkflowRun.data,
           },
           stepRuns: stepRuns.map((record: PluginEntityRecord) => {
-            const stepRun = toStepRunRecord(record);
+            const stepRun = toWorkflowStepRunRecord(record);
             return {
               id: stepRun.id,
               status: stepRun.status,
diff --git a/paperclip-addon/plugins/workflow-engine/src/workflow-store.ts b/paperclip-addon/plugins/workflow-engine/src/workflow-store.ts
index 5871a2082258cebcfe8b69ebafbb0814cf303d50..82eb46836883940e109368e927b7e367c96616b5
--- a/paperclip-addon/plugins/workflow-engine/src/workflow-store.ts
+++ b/paperclip-addon/plugins/workflow-engine/src/workflow-store.ts
@@ -44,7 +44,14 @@
 
 type TypedEntityRecord<T> = Omit<PluginEntityRecord, "data"> & { data: T };
 
-function toTypedRecord<T>(record: PluginEntityRecord): TypedEntityRecord<T> {
+function toTypedRecord<T>(
+  record: PluginEntityRecord,
+  entityType: string,
+): TypedEntityRecord<T> {
+  if (record.entityType !== entityType) {
+    throw new Error(`Expected entity type "${entityType}", got "${record.entityType}"`);
+  }
+
   return record as TypedEntityRecord<T>;
 }
 
@@ -67,13 +74,31 @@
   id: string,
   entityType: string,
 ): Promise<TypedEntityRecord<T> | null> {
-  const record = await ctx.entities.get(id);
+  const listQuery = {
+    entityType,
+    id,
+    limit: 1,
+  } as Record<string, unknown>;
 
-  if (!record || record.entityType !== entityType) {
+  const listed = await ctx.entities.list(
+    listQuery as Parameters<PluginContext["entities"]["list"]>[0],
+  );
+  const listedRecord = listed.find(
+    (record: PluginEntityRecord) => record.id === id,
+  ) ?? null;
+
+  if (listedRecord) {
+    return toTypedRecord<T>(listedRecord, entityType);
+  }
+
+  // Fallback: some host SDK variants ignore `id` on entities.list().
+  // Keep defensive type verification on direct get() until id-filter support is universal.
+  const record = await ctx.entities.get(id);
+  if (!record) {
     return null;
   }
 
-  return toTypedRecord<T>(record);
+  return toTypedRecord<T>(record, entityType);
 }
 
 async function requireEntityByType<T>(
diff --git a/paperclip-addon/plugins/workflow-engine/src/workflow-utils.ts b/paperclip-addon/plugins/workflow-engine/src/workflow-utils.ts
new file mode 100644
index 0000000000000000000000000000000000000000..ec1cf6f6b9aa722b126b58c0a728b4e538ad1494
--- /dev/null
+++ b/paperclip-addon/plugins/workflow-engine/src/workflow-utils.ts
@@ -0,0 +1,62 @@
+import type { PluginEntityRecord } from "@paperclipai/plugin-sdk";
+
+import { STEP_STATUSES } from "./constants.js";
+import type { WorkflowStep } from "./dag-engine.js";
+import type {
+  WorkflowDefinition,
+  WorkflowRun,
+  WorkflowStepRun,
+} from "./workflow-store.js";
+
+export type WorkflowRunRecord = PluginEntityRecord & { data: WorkflowRun };
+export type WorkflowStepRunRecord = PluginEntityRecord & { data: WorkflowStepRun };
+export type WorkflowDefinitionRecord = PluginEntityRecord & { data: WorkflowDefinition };
+type StepAgentMetadata = WorkflowStep & {
+  agent?: string;
+  agentName?: string;
+  assigneeAgentName?: string;
+};
+
+export const TERMINAL_STEP_STATUSES = new Set<string>([
+  STEP_STATUSES.done,
+  STEP_STATUSES.failed,
+  STEP_STATUSES.skipped,
+  STEP_STATUSES.escalated,
+]);
+
+export function toWorkflowRunRecord(record: PluginEntityRecord): WorkflowRunRecord {
+  return record as WorkflowRunRecord;
+}
+
+export function toWorkflowStepRunRecord(record: PluginEntityRecord): WorkflowStepRunRecord {
+  return record as WorkflowStepRunRecord;
+}
+
+export function toWorkflowDefinitionRecord(record: PluginEntityRecord): WorkflowDefinitionRecord {
+  return record as WorkflowDefinitionRecord;
+}
+
+export function findStepDefinition(definition: WorkflowDefinitionRecord, stepId: string): WorkflowStep | null {
+  return definition.data.steps.find((step: WorkflowStep) => step.id === stepId) ?? null;
+}
+
+export function getStepAgentNameHint(stepDef: WorkflowStep): string | null {
+  const stepMeta = stepDef as StepAgentMetadata;
+  const candidates = [stepMeta.agentName, stepMeta.agent, stepMeta.assigneeAgentName];
+
+  for (const candidate of candidates) {
+    if (typeof candidate === "string" && candidate.trim()) {
+      return candidate.trim();
+    }
+  }
+
+  return null;
+}
+
+export function getStepAgentName(stepRun: WorkflowStepRunRecord, stepDef: WorkflowStep): string | null {
+  if (typeof stepRun.data.agentName === "string" && stepRun.data.agentName.trim()) {
+    return stepRun.data.agentName.trim();
+  }
+
+  return getStepAgentNameHint(stepDef);
+}

exec
/bin/zsh -lc 'cat plugins/workflow-engine/src/workflow-utils.ts' in /Users/kwak/Projects/paperclip/paperclip-addon succeeded in 0ms:
import type { PluginEntityRecord } from "@paperclipai/plugin-sdk";

import { STEP_STATUSES } from "./constants.js";
import type { WorkflowStep } from "./dag-engine.js";
import type {
  WorkflowDefinition,
  WorkflowRun,
  WorkflowStepRun,
} from "./workflow-store.js";

export type WorkflowRunRecord = PluginEntityRecord & { data: WorkflowRun };
export type WorkflowStepRunRecord = PluginEntityRecord & { data: WorkflowStepRun };
export type WorkflowDefinitionRecord = PluginEntityRecord & { data: WorkflowDefinition };
type StepAgentMetadata = WorkflowStep & {
  agent?: string;
  agentName?: string;
  assigneeAgentName?: string;
};

export const TERMINAL_STEP_STATUSES = new Set<string>([
file update:
diff --git a/paperclip-addon/plugins/workflow-engine/src/dag-engine.ts b/paperclip-addon/plugins/workflow-engine/src/dag-engine.ts
index b59510f0473171038be77277286d3b5c2063e6c5..e7f21ff998cab78b9325a8aeacf4fda9c5e06553
--- a/paperclip-addon/plugins/workflow-engine/src/dag-engine.ts
+++ b/paperclip-addon/plugins/workflow-engine/src/dag-engine.ts
@@ -2,6 +2,7 @@
   id: string;
   title: string;
   dependsOn: string[];
+  sessionMode?: "fresh" | "reuse";
   onFailure?: "retry" | "skip" | "abort_workflow" | "escalate";
   escalateTo?: string;
   maxRetries?: number;
diff --git a/paperclip-addon/plugins/workflow-engine/src/reconciler.ts b/paperclip-addon/plugins/workflow-engine/src/reconciler.ts
index 14ab4e766a835d145a6c2e7b1b564270285cbdc3..55b649037e8a19a88bb39b9633a6a0cc84fa8d59
--- a/paperclip-addon/plugins/workflow-engine/src/reconciler.ts
+++ b/paperclip-addon/plugins/workflow-engine/src/reconciler.ts
@@ -8,65 +8,21 @@
   listStepRuns,
   updateStepRun,
   updateWorkflowRun,
-  type WorkflowDefinition,
-  type WorkflowRun,
-  type WorkflowStepRun,
 } from "./workflow-store.js";
-import type { WorkflowStep } from "./dag-engine.js";
-
-type WorkflowRunRecord = PluginEntityRecord & { data: WorkflowRun };
-type WorkflowStepRunRecord = PluginEntityRecord & { data: WorkflowStepRun };
-type WorkflowDefinitionRecord = PluginEntityRecord & { data: WorkflowDefinition };
-type StepAgentMetadata = WorkflowStep & {
-  agent?: string;
-  agentName?: string;
-  assigneeAgentName?: string;
-};
+import {
+  TERMINAL_STEP_STATUSES,
+  findStepDefinition,
+  getStepAgentName,
+  toWorkflowDefinitionRecord,
+  toWorkflowRunRecord,
+  toWorkflowStepRunRecord,
+} from "./workflow-utils.js";
 
 const STALE_THRESHOLD_MS = 300_000;
-const TERMINAL_STEP_STATUSES = new Set<string>([
-  STEP_STATUSES.done,
-  STEP_STATUSES.failed,
-  STEP_STATUSES.skipped,
-  STEP_STATUSES.escalated,
-]);
 
 function isStale(record: PluginEntityRecord, thresholdMs: number): boolean {
   const updatedAt = new Date(record.updatedAt).getTime();
   return Date.now() - updatedAt > thresholdMs;
-}
-
-function toWorkflowRunRecord(record: PluginEntityRecord): WorkflowRunRecord {
-  return record as WorkflowRunRecord;
-}
-
-function toWorkflowStepRunRecord(record: PluginEntityRecord): WorkflowStepRunRecord {
-  return record as WorkflowStepRunRecord;
-}
-
-function toWorkflowDefinitionRecord(record: PluginEntityRecord): WorkflowDefinitionRecord {
-  return record as WorkflowDefinitionRecord;
-}
-
-function findStepDefinition(definition: WorkflowDefinitionRecord, stepId: string): WorkflowStep | null {
-  return definition.data.steps.find((step: WorkflowStep) => step.id === stepId) ?? null;
-}
-
-function getStepAgentName(stepRun: WorkflowStepRunRecord, stepDef: WorkflowStep): string | null {
-  if (typeof stepRun.data.agentName === "string" && stepRun.data.agentName.trim()) {
-    return stepRun.data.agentName.trim();
-  }
-
-  const stepMeta = stepDef as StepAgentMetadata;
-  const candidates = [stepMeta.agentName, stepMeta.agent, stepMeta.assigneeAgentName];
-
-  for (const candidate of candidates) {
-    if (typeof candidate === "string" && candidate.trim()) {
-      return candidate.trim();
-    }
-  }
-
-  return null;
 }
 
 async function invokeAgentByName(
diff --git a/paperclip-addon/plugins/workflow-engine/src/worker.ts b/paperclip-addon/plugins/workflow-engine/src/worker.ts
index c5ac3fbb4ca7f767eb29331bd3630b635449b335..303e35958890fa1f66db3f2b0ff1fa1f134102a8
--- a/paperclip-addon/plugins/workflow-engine/src/worker.ts
+++ b/paperclip-addon/plugins/workflow-engine/src/worker.ts
@@ -15,6 +15,7 @@
 } from "./dag-engine.js";
 import {
   checkIdempotency,
+  createWorkflowRun,
   createStepRun,
   findStepRunByIssueId,
   getWorkflowDefinition,
@@ -25,51 +26,38 @@
   markIdempotency,
   updateStepRun,
   updateWorkflowRun,
-  type WorkflowDefinition,
-  type WorkflowRun,
   type WorkflowStepRun,
 } from "./workflow-store.js";
+import {
+  TERMINAL_STEP_STATUSES,
+  findStepDefinition,
+  getStepAgentName,
+  getStepAgentNameHint,
+  toWorkflowDefinitionRecord,
+  toWorkflowRunRecord,
+  toWorkflowStepRunRecord,
+  type WorkflowDefinitionRecord,
+  type WorkflowRunRecord,
+  type WorkflowStepRunRecord,
+} from "./workflow-utils.js";
 
-type WorkflowStepRecord = PluginEntityRecord & { data: WorkflowStepRun };
-type WorkflowRunRecord = PluginEntityRecord & { data: WorkflowRun };
-type WorkflowDefinitionRecord = PluginEntityRecord & { data: WorkflowDefinition };
 type WorkflowStepMetadata = WorkflowStep & {
   description?: string;
-  agent?: string;
-  agentName?: string;
-  assigneeAgentName?: string;
   assigneeAgentId?: string;
+  sessionMode?: "fresh" | "reuse";
 };
 type AgentRecord = Awaited<ReturnType<PluginContext["agents"]["list"]>>[number];
+type IssueUpdatePatch = Parameters<PluginContext["issues"]["update"]>[1];
 type ReconcilerModule = {
   reconcileStuckSteps?: (ctx: PluginContext) => Promise<void>;
 };
 
-const TERMINAL_STEP_STATUSES = new Set<string>([
-  STEP_STATUSES.done,
-  STEP_STATUSES.failed,
-  STEP_STATUSES.skipped,
-  STEP_STATUSES.escalated,
-]);
-
 function summarizeError(error: unknown): string {
   if (error instanceof Error) {
     return error.message;
   }
 
   return String(error);
-}
-
-function toStepRunRecord(record: PluginEntityRecord): WorkflowStepRecord {
-  return record as WorkflowStepRecord;
-}
-
-function toWorkflowRunRecord(record: PluginEntityRecord): WorkflowRunRecord {
-  return record as WorkflowRunRecord;
-}
-
-function toWorkflowDefinitionRecord(record: PluginEntityRecord): WorkflowDefinitionRecord {
-  return record as WorkflowDefinitionRecord;
 }
 
 function buildIdempotencyKey(event: PluginEvent): string {
@@ -78,10 +66,6 @@
 
 function getStepMetadata(stepDef: WorkflowStep): WorkflowStepMetadata {
   return stepDef as WorkflowStepMetadata;
-}
-
-function findStepDefinition(definition: WorkflowDefinitionRecord, stepId: string): WorkflowStep | null {
-  return definition.data.steps.find((step: WorkflowStep) => step.id === stepId) ?? null;
 }
 
 function getStepDescription(stepDef: WorkflowStep): string | undefined {
@@ -89,19 +73,6 @@
   return typeof description === "string" && description.trim() ? description.trim() : undefined;
 }
 
-function getStepAgentNameHint(stepDef: WorkflowStep): string | null {
-  const meta = getStepMetadata(stepDef);
-  const candidates = [meta.agentName, meta.agent, meta.assigneeAgentName];
-
-  for (const candidate of candidates) {
-    if (typeof candidate === "string" && candidate.trim()) {
-      return candidate.trim();
-    }
-  }
-
-  return null;
-}
-
 function getStepAgentIdHint(stepDef: WorkflowStep): string | null {
   const assigneeAgentId = getStepMetadata(stepDef).assigneeAgentId;
   if (typeof assigneeAgentId !== "string" || !assigneeAgentId.trim()) {
@@ -147,68 +118,256 @@
 
 async function invokeAgentForStep(
   ctx: PluginContext,
+  stepRunRecord: WorkflowStepRunRecord,
   stepDef: WorkflowStep,
-  agentName: string,
   workflowName: string,
   companyId: string,
-): Promise<void> {
+): Promise<WorkflowStepRunRecord> {
+  const agentName = getStepAgentName(stepRunRecord, stepDef);
+  if (!agentName) {
+    ctx.logger.warn("Agent name is missing for workflow step", {
+      companyId,
+      runId: stepRunRecord.data.runId,
+      stepId: stepDef.id,
+      workflowName,
+    });
+    return stepRunRecord;
+  }
+
   const agents = await ctx.agents.list({ companyId });
-  const agent = agents.find((candidate: AgentRecord) => candidate.name === agentName);
+  const agent = agents.find((candidate: AgentRecord) => candidate.name === agentName) ?? null;
 
   if (!agent) {
     ctx.logger.warn("Agent not found for step", {
       agentName,
       companyId,
+      runId: stepRunRecord.data.runId,
+      stepId: stepDef.id,
+      workflowName,
+    });
+    return stepRunRecord;
+  }
+
+  const prompt = `workflow:${workflowName}/step:${stepDef.id} — "${stepDef.title}" is ready. Please proceed with the assigned task.`;
+  const reason = `workflow:${workflowName}/step:${stepDef.id}`;
+  const sessionMode = getStepMetadata(stepDef).sessionMode === "reuse" ? "reuse" : "fresh";
+
+  if (sessionMode === "reuse") {
+    let sessionId = typeof stepRunRecord.data.sessionId === "string" && stepRunRecord.data.sessionId.trim()
+      ? stepRunRecord.data.sessionId.trim()
+      : "";
+
+    if (!sessionId) {
+      const session = await ctx.agents.sessions.create(agent.id, companyId, {
+        reason,
+      });
+      sessionId = session.sessionId;
+      stepRunRecord = toWorkflowStepRunRecord(await updateStepRun(ctx, stepRunRecord.id, {
+        sessionId,
+      }));
+    }
+
+    await ctx.agents.sessions.sendMessage(sessionId, companyId, {
+      prompt,
+      reason,
+    });
+
+    ctx.logger.info("Sent workflow step prompt via agent session", {
+      agentId: agent.id,
+      agentName,
+      companyId,
+      runId: stepRunRecord.data.runId,
+      sessionId,
       stepId: stepDef.id,
       workflowName,
     });
-    return;
+    return stepRunRecord;
   }
 
   await ctx.agents.invoke(agent.id, companyId, {
-    prompt: `workflow:${workflowName}/step:${stepDef.id} — "${stepDef.title}" is ready. Please proceed with the assigned task.`,
-    reason: `workflow:${workflowName}/step:${stepDef.id}`,
+    prompt,
+    reason,
   });
 
   ctx.logger.info("Invoked agent for workflow step", {
     agentId: agent.id,
     agentName,
     companyId,
+    runId: stepRunRecord.data.runId,
     stepId: stepDef.id,
     workflowName,
   });
+
+  return stepRunRecord;
 }
 
 async function activateBacklogStep(
   ctx: PluginContext,
-  stepRunRecord: WorkflowStepRecord,
+  stepRunRecord: WorkflowStepRunRecord,
   stepDef: WorkflowStep,
   workflowName: string,
   companyId: string,
-): Promise<void> {
+  options?: { parentIssueId?: string },
+): Promise<WorkflowStepRunRecord> {
   if (stepRunRecord.data.status !== STEP_STATUSES.backlog) {
-    return;
+    return stepRunRecord;
   }
 
-  const updated = toStepRunRecord(await updateStepRun(ctx, stepRunRecord.id, {
+  const resolvedAgent = await resolveStepAgent(
+    ctx,
+    companyId,
+    stepDef,
+    stepRunRecord.data.agentName,
+  );
+
+  let issueId = typeof stepRunRecord.data.issueId === "string" && stepRunRecord.data.issueId.trim()
+    ? stepRunRecord.data.issueId.trim()
+    : "";
+  const stepDescription = getStepDescription(stepDef) ?? `Workflow step: ${stepDef.id}`;
+
+  if (!issueId) {
+    const issue = await ctx.issues.create({
+      assigneeAgentId: resolvedAgent.agentId ?? undefined,
+      companyId,
+      description: stepDescription,
+      parentId: options?.parentIssueId,
+      title: `[${workflowName}] ${stepDef.title}`,
+    });
+    await ctx.issues.update(issue.id, { status: "todo" } as IssueUpdatePatch, companyId);
+    issueId = issue.id;
+  }
+
+  const nextStartedAt = stepRunRecord.data.startedAt ?? new Date().toISOString();
+  let updatedStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, stepRunRecord.id, {
+    agentName: resolvedAgent.agentName ?? stepRunRecord.data.agentName,
+    issueId,
+    startedAt: nextStartedAt,
     status: STEP_STATUSES.todo,
   }));
 
-  await invokeAgentForStep(
+  updatedStepRun = await invokeAgentForStep(
     ctx,
+    updatedStepRun,
     stepDef,
-    updated.data.agentName,
     workflowName,
     companyId,
   );
+
+  return updatedStepRun;
 }
 
+async function startWorkflow(
+  ctx: PluginContext,
+  workflowId: string,
+  companyId: string,
+  options?: {
+    createParentIssue?: boolean;
+    parentIssueId?: string;
+  },
+): Promise<{
+  activatedStepIds: string[];
+  parentIssueId: string | null;
+  runId: string;
+  workflowId: string;
+}> {
+  const workflowDefinition = await getWorkflowDefinition(ctx, workflowId);
+  if (!workflowDefinition) {
+    throw new Error(`Workflow definition not found: ${workflowId}`);
+  }
+
+  const typedWorkflowDefinition = toWorkflowDefinitionRecord(workflowDefinition);
+  if (typedWorkflowDefinition.data.companyId !== companyId) {
+    throw new Error(`Workflow does not belong to company: ${workflowId}`);
+  }
+
+  let parentIssueId = typeof options?.parentIssueId === "string" && options.parentIssueId.trim()
+    ? options.parentIssueId.trim()
+    : "";
+  if (!parentIssueId && options?.createParentIssue) {
+    const parentIssue = await ctx.issues.create({
+      companyId,
+      description: typedWorkflowDefinition.data.description || `Workflow run: ${typedWorkflowDefinition.data.name}`,
+      title: `[${typedWorkflowDefinition.data.name}] Workflow run`,
+    });
+    parentIssueId = parentIssue.id;
+  }
+
+  const workflowRun = toWorkflowRunRecord(await createWorkflowRun(ctx, {
+    companyId,
+    parentIssueId: parentIssueId || undefined,
+    startedAt: new Date().toISOString(),
+    status: RUN_STATUSES.running,
+    workflowId: typedWorkflowDefinition.id,
+    workflowName: typedWorkflowDefinition.data.name,
+  }));
+
+  const agents = await ctx.agents.list({ companyId });
+  const agentsByName = new Map<string, AgentRecord>();
+  for (const agent of agents) {
+    agentsByName.set(agent.name, agent);
+  }
+
+  const pendingRootSteps: Array<{ stepDef: WorkflowStep; stepRun: WorkflowStepRunRecord }> = [];
+  for (const stepDef of typedWorkflowDefinition.data.steps) {
+    const agentNameHint = getStepAgentNameHint(stepDef);
+    const matchedAgent = agentNameHint ? agentsByName.get(agentNameHint) ?? null : null;
+    const resolvedAgent = matchedAgent
+      ? { agentId: matchedAgent.id, agentName: matchedAgent.name }
+      : await resolveStepAgent(ctx, companyId, stepDef, agentNameHint ?? undefined);
+
+    if (!resolvedAgent.agentName) {
+      throw new Error(`Unable to resolve step assignee for "${stepDef.id}"`);
+    }
+
+    const stepRun = toWorkflowStepRunRecord(await createStepRun(ctx, companyId, {
+      agentName: resolvedAgent.agentName,
+      retryCount: 0,
+      runId: workflowRun.id,
+      status: STEP_STATUSES.backlog,
+      stepId: stepDef.id,
+    }));
+
+    if (stepDef.dependsOn.length === 0 && stepDef.triggerOn !== "escalation") {
+      pendingRootSteps.push({ stepDef, stepRun });
+    }
+  }
+
+  const activatedStepIds: string[] = [];
+  for (const pending of pendingRootSteps) {
+    await activateBacklogStep(
+      ctx,
+      pending.stepRun,
+      pending.stepDef,
+      typedWorkflowDefinition.data.name,
+      companyId,
+      { parentIssueId: parentIssueId || undefined },
+    );
+    activatedStepIds.push(pending.stepDef.id);
+  }
+
+  ctx.logger.info("Started workflow run", {
+    activatedStepIds,
+    companyId,
+    parentIssueId: parentIssueId || null,
+    runId: workflowRun.id,
+    workflowId: typedWorkflowDefinition.id,
+    workflowName: typedWorkflowDefinition.data.name,
+  });
+
+  return {
+    activatedStepIds,
+    parentIssueId: parentIssueId || null,
+    runId: workflowRun.id,
+    workflowId: typedWorkflowDefinition.id,
+  };
+}
+
 async function advanceWorkflow(
   ctx: PluginContext,
   stepRunRecord: PluginEntityRecord,
   companyId: string,
 ): Promise<void> {
-  const completedStepRun = toStepRunRecord(stepRunRecord);
+  const completedStepRun = toWorkflowStepRunRecord(stepRunRecord);
   const workflowRun = await getWorkflowRun(ctx, completedStepRun.data.runId);
 
   if (!workflowRun) {
@@ -237,7 +396,7 @@
   }
 
   const typedWorkflowDefinition = toWorkflowDefinitionRecord(workflowDefinition);
-  const stepRuns = (await listStepRuns(ctx, typedWorkflowRun.id, companyId)).map(toStepRunRecord);
+  const stepRuns = (await listStepRuns(ctx, typedWorkflowRun.id, companyId)).map(toWorkflowStepRunRecord);
   const completed = new Set<string>();
   const failed = new Set<string>();
   const skipped = new Set<string>();
@@ -289,7 +448,7 @@
         continue;
       }
 
-      stepRun = toStepRunRecord(await createStepRun(ctx, companyId, {
+      stepRun = toWorkflowStepRunRecord(await createStepRun(ctx, companyId, {
         agentName: resolvedAgent.agentName,
         retryCount: 0,
         runId: typedWorkflowRun.id,
@@ -305,6 +464,7 @@
       stepDef,
       typedWorkflowRun.data.workflowName,
       companyId,
+      { parentIssueId: typedWorkflowRun.data.parentIssueId },
     );
   }
 
@@ -329,7 +489,7 @@
   ctx: PluginContext,
   workflowRun: WorkflowRunRecord,
   workflowDefinition: WorkflowDefinitionRecord,
-  sourceStepRun: WorkflowStepRecord,
+  sourceStepRun: WorkflowStepRunRecord,
   escalationTargetId: string,
   companyId: string,
 ): Promise<void> {
@@ -338,7 +498,7 @@
     throw new Error(`Escalation target step not found: ${escalationTargetId}`);
   }
 
-  const stepRuns = (await listStepRuns(ctx, workflowRun.id, companyId)).map(toStepRunRecord);
+  const stepRuns = (await listStepRuns(ctx, workflowRun.id, companyId)).map(toWorkflowStepRunRecord);
   let escalationStepRun = stepRuns.find((candidate) => candidate.data.stepId === escalationTargetId) ?? null;
   const resolvedAgent = await resolveStepAgent(
     ctx,
@@ -352,7 +512,7 @@
   }
 
   if (!escalationStepRun) {
-    escalationStepRun = toStepRunRecord(await createStepRun(ctx, companyId, {
+    escalationStepRun = toWorkflowStepRunRecord(await createStepRun(ctx, companyId, {
       agentName: resolvedAgent.agentName,
       retryCount: 0,
       runId: workflowRun.id,
@@ -374,14 +534,14 @@
       title: `${workflowRun.data.workflowName}: ${escalationStep.title}`,
     });
 
-    escalationStepRun = toStepRunRecord(await updateStepRun(ctx, escalationStepRun.id, {
+    escalationStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, escalationStepRun.id, {
       issueId: issue.id,
     }));
   }
 
   const shouldActivate = escalationStepRun.data.status === STEP_STATUSES.backlog;
   if (shouldActivate) {
-    escalationStepRun = toStepRunRecord(await updateStepRun(ctx, escalationStepRun.id, {
+    escalationStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, escalationStepRun.id, {
       status: STEP_STATUSES.todo,
     }));
   }
@@ -389,8 +549,8 @@
   if (shouldActivate) {
     await invokeAgentForStep(
       ctx,
+      escalationStepRun,
       escalationStep,
-      escalationStepRun.data.agentName,
       workflowRun.data.workflowName,
       companyId,
     );
@@ -421,7 +581,7 @@
     return;
   }
 
-  const typedStepRun = toStepRunRecord(stepRunRecord);
+  const typedStepRun = toWorkflowStepRunRecord(stepRunRecord);
   if (TERMINAL_STEP_STATUSES.has(typedStepRun.data.status)) {
     return;
   }
@@ -469,7 +629,7 @@
   if (options.allowRetry) {
     const retryInfo = getRetryInfo(typedWorkflowDefinition.data.steps, typedStepRun.data.stepId);
     if (retryInfo.shouldRetry && typedStepRun.data.retryCount < retryInfo.maxRetries) {
-      const retriedStepRun = toStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
+      const retriedStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
         completedAt: undefined,
         retryCount: typedStepRun.data.retryCount + 1,
         status: STEP_STATUSES.todo,
@@ -477,8 +637,8 @@
 
       await invokeAgentForStep(
         ctx,
+        retriedStepRun,
         stepDef,
-        retriedStepRun.data.agentName,
         typedWorkflowRun.data.workflowName,
         event.companyId,
       );
@@ -496,7 +656,7 @@
   }
 
   if (stepDef.onFailure === "skip") {
-    const skippedStepRun = toStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
+    const skippedStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
       completedAt: new Date().toISOString(),
       status: STEP_STATUSES.skipped,
     }));
@@ -556,7 +716,7 @@
       return;
     }
 
-    const escalatedStepRun = toStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
+    const escalatedStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
       completedAt: new Date().toISOString(),
       status: STEP_STATUSES.escalated,
     }));
@@ -617,6 +777,29 @@
   }
 }
 
+function registerDataHandler(
+  ctx: PluginContext,
+  key: string,
+  handler: (params: Record<string, unknown>) => Promise<unknown>,
+): void {
+  const dataClient = ctx.data as {
+    handle?: (handlerKey: string, handlerFn: (params: Record<string, unknown>) => Promise<unknown>) => void;
+    register?: (handlerKey: string, handlerFn: (params: Record<string, unknown>) => Promise<unknown>) => void;
+  };
+
+  if (typeof dataClient.handle === "function") {
+    dataClient.handle(key, handler);
+    return;
+  }
+
+  if (typeof dataClient.register === "function") {
+    dataClient.register(key, handler);
+    return;
+  }
+
+  throw new Error("Plugin data client does not support handler registration");
+}
+
 const plugin = definePlugin({
   async setup(ctx: PluginContext) {
     ctx.events.on("issue.updated", async (event: PluginEvent) => {
@@ -638,7 +821,7 @@
           return;
         }
 
-        const stepRun = toStepRunRecord(stepRunRecord);
+        const stepRun = toWorkflowStepRunRecord(stepRunRecord);
         const payload = event.payload as { status?: string };
         const issueStatus = typeof payload.status === "string" ? payload.status : undefined;
 
@@ -657,7 +840,7 @@
           return;
         }
 
-        const updatedStepRun = toStepRunRecord(await updateStepRun(ctx, stepRun.id, {
+        const updatedStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, stepRun.id, {
           completedAt: nextStepStatus === STEP_STATUSES.done ? new Date().toISOString() : undefined,
           startedAt: nextStepStatus === STEP_STATUSES.inProgress
             ? stepRun.data.startedAt ?? new Date().toISOString()
@@ -687,11 +870,21 @@
 
     ctx.events.on("issue.created", async (event: PluginEvent) => {
       try {
+        const idempotencyKey = buildIdempotencyKey(event);
+        if (await checkIdempotency(ctx, idempotencyKey, event.companyId)) {
+          return;
+        }
+
         const payload = event.payload as {
           id?: string;
           parentId?: string;
           assigneeAgentId?: string;
         };
+        const issueId = typeof event.entityId === "string" && event.entityId.trim()
+          ? event.entityId.trim()
+          : typeof payload.id === "string" && payload.id.trim()
+            ? payload.id.trim()
+            : "";
 
         const parentId = typeof payload.parentId === "string" && payload.parentId.trim()
           ? payload.parentId.trim()
@@ -700,12 +893,34 @@
           ? payload.assigneeAgentId.trim()
           : "";
 
-        if (parentId || !assigneeAgentId) {
+        if (!issueId || parentId || !assigneeAgentId) {
+          await markIdempotency(ctx, idempotencyKey, event.companyId);
+          return;
+        }
+
+        const agent = await ctx.agents.get(assigneeAgentId, event.companyId);
+        const metadata = agent?.metadata as Record<string, unknown> | undefined;
+        const defaultParentIssueId = typeof metadata?.defaultParentIssueId === "string" && metadata.defaultParentIssueId.trim()
+          ? metadata.defaultParentIssueId.trim()
+          : "";
+
+        if (!defaultParentIssueId) {
+          await markIdempotency(ctx, idempotencyKey, event.companyId);
           return;
         }
 
-        ctx.logger.info("parentId filler: issue created without parent", {
-          issueId: event.entityId ?? payload.id ?? null,
+        await ctx.issues.update(
+          issueId,
+          { parentId: defaultParentIssueId } as IssueUpdatePatch,
+          event.companyId,
+        );
+        await markIdempotency(ctx, idempotencyKey, event.companyId);
+
+        ctx.logger.info("parentId filler: populated issue parentId from agent metadata", {
+          assigneeAgentId,
+          companyId: event.companyId,
+          issueId,
+          parentId: defaultParentIssueId,
         });
       } catch (error) {
         ctx.logger.warn("Failed to handle issue.created event", {
@@ -744,7 +959,23 @@
       await runReconciler(ctx);
     });
 
-    ctx.data.register("workflow-overview", async (params: Record<string, unknown>) => {
+    registerDataHandler(ctx, "start-workflow", async (params: Record<string, unknown>) => {
+      const workflowId = typeof params.workflowId === "string" ? params.workflowId.trim() : "";
+      const companyId = typeof params.companyId === "string" ? params.companyId.trim() : "";
+      const parentIssueId = typeof params.parentIssueId === "string" ? params.parentIssueId.trim() : "";
+      const createParentIssue = params.createParentIssue === true;
+
+      if (!workflowId || !companyId) {
+        throw new Error("start-workflow requires workflowId and companyId");
+      }
+
+      return await startWorkflow(ctx, workflowId, companyId, {
+        createParentIssue,
+        parentIssueId: parentIssueId || undefined,
+      });
+    });
+
+    registerDataHandler(ctx, "workflow-overview", async (params: Record<string, unknown>) => {
       try {
         const companyId = typeof params.companyId === "string" ? params.companyId.trim() : "";
         if (!companyId) {
@@ -782,7 +1013,7 @@
       }
     });
 
-    ctx.data.register("workflow-run-detail", async (params: Record<string, unknown>) => {
+    registerDataHandler(ctx, "workflow-run-detail", async (params: Record<string, unknown>) => {
       try {
         const runId = typeof params.runId === "string" ? params.runId.trim() : "";
         if (!runId) {
@@ -807,7 +1038,7 @@
             ...typedWorkflowRun.data,
           },
           stepRuns: stepRuns.map((record: PluginEntityRecord) => {
-            const stepRun = toStepRunRecord(record);
+            const stepRun = toWorkflowStepRunRecord(record);
             return {
               id: stepRun.id,
               status: stepRun.status,
diff --git a/paperclip-addon/plugins/workflow-engine/src/workflow-store.ts b/paperclip-addon/plugins/workflow-engine/src/workflow-store.ts
index 5871a2082258cebcfe8b69ebafbb0814cf303d50..82eb46836883940e109368e927b7e367c96616b5
--- a/paperclip-addon/plugins/workflow-engine/src/workflow-store.ts
+++ b/paperclip-addon/plugins/workflow-engine/src/workflow-store.ts
@@ -44,7 +44,14 @@
 
 type TypedEntityRecord<T> = Omit<PluginEntityRecord, "data"> & { data: T };
 
-function toTypedRecord<T>(record: PluginEntityRecord): TypedEntityRecord<T> {
+function toTypedRecord<T>(
+  record: PluginEntityRecord,
+  entityType: string,
+): TypedEntityRecord<T> {
+  if (record.entityType !== entityType) {
+    throw new Error(`Expected entity type "${entityType}", got "${record.entityType}"`);
+  }
+
   return record as TypedEntityRecord<T>;
 }
 
@@ -67,13 +74,31 @@
   id: string,
   entityType: string,
 ): Promise<TypedEntityRecord<T> | null> {
-  const record = await ctx.entities.get(id);
+  const listQuery = {
+    entityType,
+    id,
+    limit: 1,
+  } as Record<string, unknown>;
 
-  if (!record || record.entityType !== entityType) {
+  const listed = await ctx.entities.list(
+    listQuery as Parameters<PluginContext["entities"]["list"]>[0],
+  );
+  const listedRecord = listed.find(
+    (record: PluginEntityRecord) => record.id === id,
+  ) ?? null;
+
+  if (listedRecord) {
+    return toTypedRecord<T>(listedRecord, entityType);
+  }
+
+  // Fallback: some host SDK variants ignore `id` on entities.list().
+  // Keep defensive type verification on direct get() until id-filter support is universal.
+  const record = await ctx.entities.get(id);
+  if (!record) {
     return null;
   }
 
-  return toTypedRecord<T>(record);
+  return toTypedRecord<T>(record, entityType);
 }
 
 async function requireEntityByType<T>(
diff --git a/paperclip-addon/plugins/workflow-engine/src/workflow-utils.ts b/paperclip-addon/plugins/workflow-engine/src/workflow-utils.ts
new file mode 100644
index 0000000000000000000000000000000000000000..ec1cf6f6b9aa722b126b58c0a728b4e538ad1494
--- /dev/null
+++ b/paperclip-addon/plugins/workflow-engine/src/workflow-utils.ts
@@ -0,0 +1,62 @@
+import type { PluginEntityRecord } from "@paperclipai/plugin-sdk";
+
+import { STEP_STATUSES } from "./constants.js";
+import type { WorkflowStep } from "./dag-engine.js";
+import type {
+  WorkflowDefinition,
+  WorkflowRun,
+  WorkflowStepRun,
+} from "./workflow-store.js";
+
+export type WorkflowRunRecord = PluginEntityRecord & { data: WorkflowRun };
+export type WorkflowStepRunRecord = PluginEntityRecord & { data: WorkflowStepRun };
+export type WorkflowDefinitionRecord = PluginEntityRecord & { data: WorkflowDefinition };
+type StepAgentMetadata = WorkflowStep & {
+  agent?: string;
+  agentName?: string;
+  assigneeAgentName?: string;
+};
+
+export const TERMINAL_STEP_STATUSES = new Set<string>([
+  STEP_STATUSES.done,
+  STEP_STATUSES.failed,
+  STEP_STATUSES.skipped,
+  STEP_STATUSES.escalated,
+]);
+
+export function toWorkflowRunRecord(record: PluginEntityRecord): WorkflowRunRecord {
+  return record as WorkflowRunRecord;
+}
+
+export function toWorkflowStepRunRecord(record: PluginEntityRecord): WorkflowStepRunRecord {
+  return record as WorkflowStepRunRecord;
+}
+
+export function toWorkflowDefinitionRecord(record: PluginEntityRecord): WorkflowDefinitionRecord {
+  return record as WorkflowDefinitionRecord;
+}
+
+export function findStepDefinition(definition: WorkflowDefinitionRecord, stepId: string): WorkflowStep | null {
+  return definition.data.steps.find((step: WorkflowStep) => step.id === stepId) ?? null;
+}
+
+export function getStepAgentNameHint(stepDef: WorkflowStep): string | null {
+  const stepMeta = stepDef as StepAgentMetadata;
+  const candidates = [stepMeta.agentName, stepMeta.agent, stepMeta.assigneeAgentName];
+
+  for (const candidate of candidates) {
+    if (typeof candidate === "string" && candidate.trim()) {
+      return candidate.trim();
+    }
+  }
+
+  return null;
+}
+
+export function getStepAgentName(stepRun: WorkflowStepRunRecord, stepDef: WorkflowStep): string | null {
+  if (typeof stepRun.data.agentName === "string" && stepRun.data.agentName.trim()) {
+    return stepRun.data.agentName.trim();
+  }
+
+  return getStepAgentNameHint(stepDef);
+}

codex
변경사항 반영은 완료됐고, 지금 최종 diff를 점검해 요구사항 누락이 없는지 확인 중입니다. 이어서 수정 파일 목록과 핵심 포인트를 정리해서 전달하겠습니다.
exec
/bin/zsh -lc 'git -C /Users/kwak/Projects/paperclip/paperclip-addon diff -- plugins/workflow-engine/src/worker.ts plugins/workflow-engine/src/workflow-store.ts plugins/workflow-engine/src/reconciler.ts plugins/workflow-engine/src/dag-engine.ts plugins/workflow-engine/src/workflow-utils.ts' in /Users/kwak/Projects/paperclip/paperclip-addon succeeded in 0ms:

exec
/bin/zsh -lc 'git -C /Users/kwak/Projects/paperclip/paperclip-addon status --short plugins/workflow-engine/src' in /Users/kwak/Projects/paperclip/paperclip-addon succeeded in 0ms:
?? plugins/workflow-engine/src/
file update:
diff --git a/paperclip-addon/plugins/workflow-engine/src/dag-engine.ts b/paperclip-addon/plugins/workflow-engine/src/dag-engine.ts
index b59510f0473171038be77277286d3b5c2063e6c5..e7f21ff998cab78b9325a8aeacf4fda9c5e06553
--- a/paperclip-addon/plugins/workflow-engine/src/dag-engine.ts
+++ b/paperclip-addon/plugins/workflow-engine/src/dag-engine.ts
@@ -2,6 +2,7 @@
   id: string;
   title: string;
   dependsOn: string[];
+  sessionMode?: "fresh" | "reuse";
   onFailure?: "retry" | "skip" | "abort_workflow" | "escalate";
   escalateTo?: string;
   maxRetries?: number;
diff --git a/paperclip-addon/plugins/workflow-engine/src/reconciler.ts b/paperclip-addon/plugins/workflow-engine/src/reconciler.ts
index 14ab4e766a835d145a6c2e7b1b564270285cbdc3..55b649037e8a19a88bb39b9633a6a0cc84fa8d59
--- a/paperclip-addon/plugins/workflow-engine/src/reconciler.ts
+++ b/paperclip-addon/plugins/workflow-engine/src/reconciler.ts
@@ -8,65 +8,21 @@
   listStepRuns,
   updateStepRun,
   updateWorkflowRun,
-  type WorkflowDefinition,
-  type WorkflowRun,
-  type WorkflowStepRun,
 } from "./workflow-store.js";
-import type { WorkflowStep } from "./dag-engine.js";
-
-type WorkflowRunRecord = PluginEntityRecord & { data: WorkflowRun };
-type WorkflowStepRunRecord = PluginEntityRecord & { data: WorkflowStepRun };
-type WorkflowDefinitionRecord = PluginEntityRecord & { data: WorkflowDefinition };
-type StepAgentMetadata = WorkflowStep & {
-  agent?: string;
-  agentName?: string;
-  assigneeAgentName?: string;
-};
+import {
+  TERMINAL_STEP_STATUSES,
+  findStepDefinition,
+  getStepAgentName,
+  toWorkflowDefinitionRecord,
+  toWorkflowRunRecord,
+  toWorkflowStepRunRecord,
+} from "./workflow-utils.js";
 
 const STALE_THRESHOLD_MS = 300_000;
-const TERMINAL_STEP_STATUSES = new Set<string>([
-  STEP_STATUSES.done,
-  STEP_STATUSES.failed,
-  STEP_STATUSES.skipped,
-  STEP_STATUSES.escalated,
-]);
 
 function isStale(record: PluginEntityRecord, thresholdMs: number): boolean {
   const updatedAt = new Date(record.updatedAt).getTime();
   return Date.now() - updatedAt > thresholdMs;
-}
-
-function toWorkflowRunRecord(record: PluginEntityRecord): WorkflowRunRecord {
-  return record as WorkflowRunRecord;
-}
-
-function toWorkflowStepRunRecord(record: PluginEntityRecord): WorkflowStepRunRecord {
-  return record as WorkflowStepRunRecord;
-}
-
-function toWorkflowDefinitionRecord(record: PluginEntityRecord): WorkflowDefinitionRecord {
-  return record as WorkflowDefinitionRecord;
-}
-
-function findStepDefinition(definition: WorkflowDefinitionRecord, stepId: string): WorkflowStep | null {
-  return definition.data.steps.find((step: WorkflowStep) => step.id === stepId) ?? null;
-}
-
-function getStepAgentName(stepRun: WorkflowStepRunRecord, stepDef: WorkflowStep): string | null {
-  if (typeof stepRun.data.agentName === "string" && stepRun.data.agentName.trim()) {
-    return stepRun.data.agentName.trim();
-  }
-
-  const stepMeta = stepDef as StepAgentMetadata;
-  const candidates = [stepMeta.agentName, stepMeta.agent, stepMeta.assigneeAgentName];
-
-  for (const candidate of candidates) {
-    if (typeof candidate === "string" && candidate.trim()) {
-      return candidate.trim();
-    }
-  }
-
-  return null;
 }
 
 async function invokeAgentByName(
diff --git a/paperclip-addon/plugins/workflow-engine/src/worker.ts b/paperclip-addon/plugins/workflow-engine/src/worker.ts
index c5ac3fbb4ca7f767eb29331bd3630b635449b335..303e35958890fa1f66db3f2b0ff1fa1f134102a8
--- a/paperclip-addon/plugins/workflow-engine/src/worker.ts
+++ b/paperclip-addon/plugins/workflow-engine/src/worker.ts
@@ -15,6 +15,7 @@
 } from "./dag-engine.js";
 import {
   checkIdempotency,
+  createWorkflowRun,
   createStepRun,
   findStepRunByIssueId,
   getWorkflowDefinition,
@@ -25,51 +26,38 @@
   markIdempotency,
   updateStepRun,
   updateWorkflowRun,
-  type WorkflowDefinition,
-  type WorkflowRun,
   type WorkflowStepRun,
 } from "./workflow-store.js";
+import {
+  TERMINAL_STEP_STATUSES,
+  findStepDefinition,
+  getStepAgentName,
+  getStepAgentNameHint,
+  toWorkflowDefinitionRecord,
+  toWorkflowRunRecord,
+  toWorkflowStepRunRecord,
+  type WorkflowDefinitionRecord,
+  type WorkflowRunRecord,
+  type WorkflowStepRunRecord,
+} from "./workflow-utils.js";
 
-type WorkflowStepRecord = PluginEntityRecord & { data: WorkflowStepRun };
-type WorkflowRunRecord = PluginEntityRecord & { data: WorkflowRun };
-type WorkflowDefinitionRecord = PluginEntityRecord & { data: WorkflowDefinition };
 type WorkflowStepMetadata = WorkflowStep & {
   description?: string;
-  agent?: string;
-  agentName?: string;
-  assigneeAgentName?: string;
   assigneeAgentId?: string;
+  sessionMode?: "fresh" | "reuse";
 };
 type AgentRecord = Awaited<ReturnType<PluginContext["agents"]["list"]>>[number];
+type IssueUpdatePatch = Parameters<PluginContext["issues"]["update"]>[1];
 type ReconcilerModule = {
   reconcileStuckSteps?: (ctx: PluginContext) => Promise<void>;
 };
 
-const TERMINAL_STEP_STATUSES = new Set<string>([
-  STEP_STATUSES.done,
-  STEP_STATUSES.failed,
-  STEP_STATUSES.skipped,
-  STEP_STATUSES.escalated,
-]);
-
 function summarizeError(error: unknown): string {
   if (error instanceof Error) {
     return error.message;
   }
 
   return String(error);
-}
-
-function toStepRunRecord(record: PluginEntityRecord): WorkflowStepRecord {
-  return record as WorkflowStepRecord;
-}
-
-function toWorkflowRunRecord(record: PluginEntityRecord): WorkflowRunRecord {
-  return record as WorkflowRunRecord;
-}
-
-function toWorkflowDefinitionRecord(record: PluginEntityRecord): WorkflowDefinitionRecord {
-  return record as WorkflowDefinitionRecord;
 }
 
 function buildIdempotencyKey(event: PluginEvent): string {
@@ -78,10 +66,6 @@
 
 function getStepMetadata(stepDef: WorkflowStep): WorkflowStepMetadata {
   return stepDef as WorkflowStepMetadata;
-}
-
-function findStepDefinition(definition: WorkflowDefinitionRecord, stepId: string): WorkflowStep | null {
-  return definition.data.steps.find((step: WorkflowStep) => step.id === stepId) ?? null;
 }
 
 function getStepDescription(stepDef: WorkflowStep): string | undefined {
@@ -89,19 +73,6 @@
   return typeof description === "string" && description.trim() ? description.trim() : undefined;
 }
 
-function getStepAgentNameHint(stepDef: WorkflowStep): string | null {
-  const meta = getStepMetadata(stepDef);
-  const candidates = [meta.agentName, meta.agent, meta.assigneeAgentName];
-
-  for (const candidate of candidates) {
-    if (typeof candidate === "string" && candidate.trim()) {
-      return candidate.trim();
-    }
-  }
-
-  return null;
-}
-
 function getStepAgentIdHint(stepDef: WorkflowStep): string | null {
   const assigneeAgentId = getStepMetadata(stepDef).assigneeAgentId;
   if (typeof assigneeAgentId !== "string" || !assigneeAgentId.trim()) {
@@ -147,68 +118,256 @@
 
 async function invokeAgentForStep(
   ctx: PluginContext,
+  stepRunRecord: WorkflowStepRunRecord,
   stepDef: WorkflowStep,
-  agentName: string,
   workflowName: string,
   companyId: string,
-): Promise<void> {
+): Promise<WorkflowStepRunRecord> {
+  const agentName = getStepAgentName(stepRunRecord, stepDef);
+  if (!agentName) {
+    ctx.logger.warn("Agent name is missing for workflow step", {
+      companyId,
+      runId: stepRunRecord.data.runId,
+      stepId: stepDef.id,
+      workflowName,
+    });
+    return stepRunRecord;
+  }
+
   const agents = await ctx.agents.list({ companyId });
-  const agent = agents.find((candidate: AgentRecord) => candidate.name === agentName);
+  const agent = agents.find((candidate: AgentRecord) => candidate.name === agentName) ?? null;
 
   if (!agent) {
     ctx.logger.warn("Agent not found for step", {
       agentName,
       companyId,
+      runId: stepRunRecord.data.runId,
+      stepId: stepDef.id,
+      workflowName,
+    });
+    return stepRunRecord;
+  }
+
+  const prompt = `workflow:${workflowName}/step:${stepDef.id} — "${stepDef.title}" is ready. Please proceed with the assigned task.`;
+  const reason = `workflow:${workflowName}/step:${stepDef.id}`;
+  const sessionMode = getStepMetadata(stepDef).sessionMode === "reuse" ? "reuse" : "fresh";
+
+  if (sessionMode === "reuse") {
+    let sessionId = typeof stepRunRecord.data.sessionId === "string" && stepRunRecord.data.sessionId.trim()
+      ? stepRunRecord.data.sessionId.trim()
+      : "";
+
+    if (!sessionId) {
+      const session = await ctx.agents.sessions.create(agent.id, companyId, {
+        reason,
+      });
+      sessionId = session.sessionId;
+      stepRunRecord = toWorkflowStepRunRecord(await updateStepRun(ctx, stepRunRecord.id, {
+        sessionId,
+      }));
+    }
+
+    await ctx.agents.sessions.sendMessage(sessionId, companyId, {
+      prompt,
+      reason,
+    });
+
+    ctx.logger.info("Sent workflow step prompt via agent session", {
+      agentId: agent.id,
+      agentName,
+      companyId,
+      runId: stepRunRecord.data.runId,
+      sessionId,
       stepId: stepDef.id,
       workflowName,
     });
-    return;
+    return stepRunRecord;
   }
 
   await ctx.agents.invoke(agent.id, companyId, {
-    prompt: `workflow:${workflowName}/step:${stepDef.id} — "${stepDef.title}" is ready. Please proceed with the assigned task.`,
-    reason: `workflow:${workflowName}/step:${stepDef.id}`,
+    prompt,
+    reason,
   });
 
   ctx.logger.info("Invoked agent for workflow step", {
     agentId: agent.id,
     agentName,
     companyId,
+    runId: stepRunRecord.data.runId,
     stepId: stepDef.id,
     workflowName,
   });
+
+  return stepRunRecord;
 }
 
 async function activateBacklogStep(
   ctx: PluginContext,
-  stepRunRecord: WorkflowStepRecord,
+  stepRunRecord: WorkflowStepRunRecord,
   stepDef: WorkflowStep,
   workflowName: string,
   companyId: string,
-): Promise<void> {
+  options?: { parentIssueId?: string },
+): Promise<WorkflowStepRunRecord> {
   if (stepRunRecord.data.status !== STEP_STATUSES.backlog) {
-    return;
+    return stepRunRecord;
   }
 
-  const updated = toStepRunRecord(await updateStepRun(ctx, stepRunRecord.id, {
+  const resolvedAgent = await resolveStepAgent(
+    ctx,
+    companyId,
+    stepDef,
+    stepRunRecord.data.agentName,
+  );
+
+  let issueId = typeof stepRunRecord.data.issueId === "string" && stepRunRecord.data.issueId.trim()
+    ? stepRunRecord.data.issueId.trim()
+    : "";
+  const stepDescription = getStepDescription(stepDef) ?? `Workflow step: ${stepDef.id}`;
+
+  if (!issueId) {
+    const issue = await ctx.issues.create({
+      assigneeAgentId: resolvedAgent.agentId ?? undefined,
+      companyId,
+      description: stepDescription,
+      parentId: options?.parentIssueId,
+      title: `[${workflowName}] ${stepDef.title}`,
+    });
+    await ctx.issues.update(issue.id, { status: "todo" } as IssueUpdatePatch, companyId);
+    issueId = issue.id;
+  }
+
+  const nextStartedAt = stepRunRecord.data.startedAt ?? new Date().toISOString();
+  let updatedStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, stepRunRecord.id, {
+    agentName: resolvedAgent.agentName ?? stepRunRecord.data.agentName,
+    issueId,
+    startedAt: nextStartedAt,
     status: STEP_STATUSES.todo,
   }));
 
-  await invokeAgentForStep(
+  updatedStepRun = await invokeAgentForStep(
     ctx,
+    updatedStepRun,
     stepDef,
-    updated.data.agentName,
     workflowName,
     companyId,
   );
+
+  return updatedStepRun;
 }
 
+async function startWorkflow(
+  ctx: PluginContext,
+  workflowId: string,
+  companyId: string,
+  options?: {
+    createParentIssue?: boolean;
+    parentIssueId?: string;
+  },
+): Promise<{
+  activatedStepIds: string[];
+  parentIssueId: string | null;
+  runId: string;
+  workflowId: string;
+}> {
+  const workflowDefinition = await getWorkflowDefinition(ctx, workflowId);
+  if (!workflowDefinition) {
+    throw new Error(`Workflow definition not found: ${workflowId}`);
+  }
+
+  const typedWorkflowDefinition = toWorkflowDefinitionRecord(workflowDefinition);
+  if (typedWorkflowDefinition.data.companyId !== companyId) {
+    throw new Error(`Workflow does not belong to company: ${workflowId}`);
+  }
+
+  let parentIssueId = typeof options?.parentIssueId === "string" && options.parentIssueId.trim()
+    ? options.parentIssueId.trim()
+    : "";
+  if (!parentIssueId && options?.createParentIssue) {
+    const parentIssue = await ctx.issues.create({
+      companyId,
+      description: typedWorkflowDefinition.data.description || `Workflow run: ${typedWorkflowDefinition.data.name}`,
+      title: `[${typedWorkflowDefinition.data.name}] Workflow run`,
+    });
+    parentIssueId = parentIssue.id;
+  }
+
+  const workflowRun = toWorkflowRunRecord(await createWorkflowRun(ctx, {
+    companyId,
+    parentIssueId: parentIssueId || undefined,
+    startedAt: new Date().toISOString(),
+    status: RUN_STATUSES.running,
+    workflowId: typedWorkflowDefinition.id,
+    workflowName: typedWorkflowDefinition.data.name,
+  }));
+
+  const agents = await ctx.agents.list({ companyId });
+  const agentsByName = new Map<string, AgentRecord>();
+  for (const agent of agents) {
+    agentsByName.set(agent.name, agent);
+  }
+
+  const pendingRootSteps: Array<{ stepDef: WorkflowStep; stepRun: WorkflowStepRunRecord }> = [];
+  for (const stepDef of typedWorkflowDefinition.data.steps) {
+    const agentNameHint = getStepAgentNameHint(stepDef);
+    const matchedAgent = agentNameHint ? agentsByName.get(agentNameHint) ?? null : null;
+    const resolvedAgent = matchedAgent
+      ? { agentId: matchedAgent.id, agentName: matchedAgent.name }
+      : await resolveStepAgent(ctx, companyId, stepDef, agentNameHint ?? undefined);
+
+    if (!resolvedAgent.agentName) {
+      throw new Error(`Unable to resolve step assignee for "${stepDef.id}"`);
+    }
+
+    const stepRun = toWorkflowStepRunRecord(await createStepRun(ctx, companyId, {
+      agentName: resolvedAgent.agentName,
+      retryCount: 0,
+      runId: workflowRun.id,
+      status: STEP_STATUSES.backlog,
+      stepId: stepDef.id,
+    }));
+
+    if (stepDef.dependsOn.length === 0 && stepDef.triggerOn !== "escalation") {
+      pendingRootSteps.push({ stepDef, stepRun });
+    }
+  }
+
+  const activatedStepIds: string[] = [];
+  for (const pending of pendingRootSteps) {
+    await activateBacklogStep(
+      ctx,
+      pending.stepRun,
+      pending.stepDef,
+      typedWorkflowDefinition.data.name,
+      companyId,
+      { parentIssueId: parentIssueId || undefined },
+    );
+    activatedStepIds.push(pending.stepDef.id);
+  }
+
+  ctx.logger.info("Started workflow run", {
+    activatedStepIds,
+    companyId,
+    parentIssueId: parentIssueId || null,
+    runId: workflowRun.id,
+    workflowId: typedWorkflowDefinition.id,
+    workflowName: typedWorkflowDefinition.data.name,
+  });
+
+  return {
+    activatedStepIds,
+    parentIssueId: parentIssueId || null,
+    runId: workflowRun.id,
+    workflowId: typedWorkflowDefinition.id,
+  };
+}
+
 async function advanceWorkflow(
   ctx: PluginContext,
   stepRunRecord: PluginEntityRecord,
   companyId: string,
 ): Promise<void> {
-  const completedStepRun = toStepRunRecord(stepRunRecord);
+  const completedStepRun = toWorkflowStepRunRecord(stepRunRecord);
   const workflowRun = await getWorkflowRun(ctx, completedStepRun.data.runId);
 
   if (!workflowRun) {
@@ -237,7 +396,7 @@
   }
 
   const typedWorkflowDefinition = toWorkflowDefinitionRecord(workflowDefinition);
-  const stepRuns = (await listStepRuns(ctx, typedWorkflowRun.id, companyId)).map(toStepRunRecord);
+  const stepRuns = (await listStepRuns(ctx, typedWorkflowRun.id, companyId)).map(toWorkflowStepRunRecord);
   const completed = new Set<string>();
   const failed = new Set<string>();
   const skipped = new Set<string>();
@@ -289,7 +448,7 @@
         continue;
       }
 
-      stepRun = toStepRunRecord(await createStepRun(ctx, companyId, {
+      stepRun = toWorkflowStepRunRecord(await createStepRun(ctx, companyId, {
         agentName: resolvedAgent.agentName,
         retryCount: 0,
         runId: typedWorkflowRun.id,
@@ -305,6 +464,7 @@
       stepDef,
       typedWorkflowRun.data.workflowName,
       companyId,
+      { parentIssueId: typedWorkflowRun.data.parentIssueId },
     );
   }
 
@@ -329,7 +489,7 @@
   ctx: PluginContext,
   workflowRun: WorkflowRunRecord,
   workflowDefinition: WorkflowDefinitionRecord,
-  sourceStepRun: WorkflowStepRecord,
+  sourceStepRun: WorkflowStepRunRecord,
   escalationTargetId: string,
   companyId: string,
 ): Promise<void> {
@@ -338,7 +498,7 @@
     throw new Error(`Escalation target step not found: ${escalationTargetId}`);
   }
 
-  const stepRuns = (await listStepRuns(ctx, workflowRun.id, companyId)).map(toStepRunRecord);
+  const stepRuns = (await listStepRuns(ctx, workflowRun.id, companyId)).map(toWorkflowStepRunRecord);
   let escalationStepRun = stepRuns.find((candidate) => candidate.data.stepId === escalationTargetId) ?? null;
   const resolvedAgent = await resolveStepAgent(
     ctx,
@@ -352,7 +512,7 @@
   }
 
   if (!escalationStepRun) {
-    escalationStepRun = toStepRunRecord(await createStepRun(ctx, companyId, {
+    escalationStepRun = toWorkflowStepRunRecord(await createStepRun(ctx, companyId, {
       agentName: resolvedAgent.agentName,
       retryCount: 0,
       runId: workflowRun.id,
@@ -374,14 +534,14 @@
       title: `${workflowRun.data.workflowName}: ${escalationStep.title}`,
     });
 
-    escalationStepRun = toStepRunRecord(await updateStepRun(ctx, escalationStepRun.id, {
+    escalationStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, escalationStepRun.id, {
       issueId: issue.id,
     }));
   }
 
   const shouldActivate = escalationStepRun.data.status === STEP_STATUSES.backlog;
   if (shouldActivate) {
-    escalationStepRun = toStepRunRecord(await updateStepRun(ctx, escalationStepRun.id, {
+    escalationStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, escalationStepRun.id, {
       status: STEP_STATUSES.todo,
     }));
   }
@@ -389,8 +549,8 @@
   if (shouldActivate) {
     await invokeAgentForStep(
       ctx,
+      escalationStepRun,
       escalationStep,
-      escalationStepRun.data.agentName,
       workflowRun.data.workflowName,
       companyId,
     );
@@ -421,7 +581,7 @@
     return;
   }
 
-  const typedStepRun = toStepRunRecord(stepRunRecord);
+  const typedStepRun = toWorkflowStepRunRecord(stepRunRecord);
   if (TERMINAL_STEP_STATUSES.has(typedStepRun.data.status)) {
     return;
   }
@@ -469,7 +629,7 @@
   if (options.allowRetry) {
     const retryInfo = getRetryInfo(typedWorkflowDefinition.data.steps, typedStepRun.data.stepId);
     if (retryInfo.shouldRetry && typedStepRun.data.retryCount < retryInfo.maxRetries) {
-      const retriedStepRun = toStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
+      const retriedStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
         completedAt: undefined,
         retryCount: typedStepRun.data.retryCount + 1,
         status: STEP_STATUSES.todo,
@@ -477,8 +637,8 @@
 
       await invokeAgentForStep(
         ctx,
+        retriedStepRun,
         stepDef,
-        retriedStepRun.data.agentName,
         typedWorkflowRun.data.workflowName,
         event.companyId,
       );
@@ -496,7 +656,7 @@
   }
 
   if (stepDef.onFailure === "skip") {
-    const skippedStepRun = toStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
+    const skippedStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
       completedAt: new Date().toISOString(),
       status: STEP_STATUSES.skipped,
     }));
@@ -556,7 +716,7 @@
       return;
     }
 
-    const escalatedStepRun = toStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
+    const escalatedStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
       completedAt: new Date().toISOString(),
       status: STEP_STATUSES.escalated,
     }));
@@ -617,6 +777,29 @@
   }
 }
 
+function registerDataHandler(
+  ctx: PluginContext,
+  key: string,
+  handler: (params: Record<string, unknown>) => Promise<unknown>,
+): void {
+  const dataClient = ctx.data as {
+    handle?: (handlerKey: string, handlerFn: (params: Record<string, unknown>) => Promise<unknown>) => void;
+    register?: (handlerKey: string, handlerFn: (params: Record<string, unknown>) => Promise<unknown>) => void;
+  };
+
+  if (typeof dataClient.handle === "function") {
+    dataClient.handle(key, handler);
+    return;
+  }
+
+  if (typeof dataClient.register === "function") {
+    dataClient.register(key, handler);
+    return;
+  }
+
+  throw new Error("Plugin data client does not support handler registration");
+}
+
 const plugin = definePlugin({
   async setup(ctx: PluginContext) {
     ctx.events.on("issue.updated", async (event: PluginEvent) => {
@@ -638,7 +821,7 @@
           return;
         }
 
-        const stepRun = toStepRunRecord(stepRunRecord);
+        const stepRun = toWorkflowStepRunRecord(stepRunRecord);
         const payload = event.payload as { status?: string };
         const issueStatus = typeof payload.status === "string" ? payload.status : undefined;
 
@@ -657,7 +840,7 @@
           return;
         }
 
-        const updatedStepRun = toStepRunRecord(await updateStepRun(ctx, stepRun.id, {
+        const updatedStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, stepRun.id, {
           completedAt: nextStepStatus === STEP_STATUSES.done ? new Date().toISOString() : undefined,
           startedAt: nextStepStatus === STEP_STATUSES.inProgress
             ? stepRun.data.startedAt ?? new Date().toISOString()
@@ -687,11 +870,21 @@
 
     ctx.events.on("issue.created", async (event: PluginEvent) => {
       try {
+        const idempotencyKey = buildIdempotencyKey(event);
+        if (await checkIdempotency(ctx, idempotencyKey, event.companyId)) {
+          return;
+        }
+
         const payload = event.payload as {
           id?: string;
           parentId?: string;
           assigneeAgentId?: string;
         };
+        const issueId = typeof event.entityId === "string" && event.entityId.trim()
+          ? event.entityId.trim()
+          : typeof payload.id === "string" && payload.id.trim()
+            ? payload.id.trim()
+            : "";
 
         const parentId = typeof payload.parentId === "string" && payload.parentId.trim()
           ? payload.parentId.trim()
@@ -700,12 +893,34 @@
           ? payload.assigneeAgentId.trim()
           : "";
 
-        if (parentId || !assigneeAgentId) {
+        if (!issueId || parentId || !assigneeAgentId) {
+          await markIdempotency(ctx, idempotencyKey, event.companyId);
+          return;
+        }
+
+        const agent = await ctx.agents.get(assigneeAgentId, event.companyId);
+        const metadata = agent?.metadata as Record<string, unknown> | undefined;
+        const defaultParentIssueId = typeof metadata?.defaultParentIssueId === "string" && metadata.defaultParentIssueId.trim()
+          ? metadata.defaultParentIssueId.trim()
+          : "";
+
+        if (!defaultParentIssueId) {
+          await markIdempotency(ctx, idempotencyKey, event.companyId);
           return;
         }
 
-        ctx.logger.info("parentId filler: issue created without parent", {
-          issueId: event.entityId ?? payload.id ?? null,
+        await ctx.issues.update(
+          issueId,
+          { parentId: defaultParentIssueId } as IssueUpdatePatch,
+          event.companyId,
+        );
+        await markIdempotency(ctx, idempotencyKey, event.companyId);
+
+        ctx.logger.info("parentId filler: populated issue parentId from agent metadata", {
+          assigneeAgentId,
+          companyId: event.companyId,
+          issueId,
+          parentId: defaultParentIssueId,
         });
       } catch (error) {
         ctx.logger.warn("Failed to handle issue.created event", {
@@ -744,7 +959,23 @@
       await runReconciler(ctx);
     });
 
-    ctx.data.register("workflow-overview", async (params: Record<string, unknown>) => {
+    registerDataHandler(ctx, "start-workflow", async (params: Record<string, unknown>) => {
+      const workflowId = typeof params.workflowId === "string" ? params.workflowId.trim() : "";
+      const companyId = typeof params.companyId === "string" ? params.companyId.trim() : "";
+      const parentIssueId = typeof params.parentIssueId === "string" ? params.parentIssueId.trim() : "";
+      const createParentIssue = params.createParentIssue === true;
+
+      if (!workflowId || !companyId) {
+        throw new Error("start-workflow requires workflowId and companyId");
+      }
+
+      return await startWorkflow(ctx, workflowId, companyId, {
+        createParentIssue,
+        parentIssueId: parentIssueId || undefined,
+      });
+    });
+
+    registerDataHandler(ctx, "workflow-overview", async (params: Record<string, unknown>) => {
       try {
         const companyId = typeof params.companyId === "string" ? params.companyId.trim() : "";
         if (!companyId) {
@@ -782,7 +1013,7 @@
       }
     });
 
-    ctx.data.register("workflow-run-detail", async (params: Record<string, unknown>) => {
+    registerDataHandler(ctx, "workflow-run-detail", async (params: Record<string, unknown>) => {
       try {
         const runId = typeof params.runId === "string" ? params.runId.trim() : "";
         if (!runId) {
@@ -807,7 +1038,7 @@
             ...typedWorkflowRun.data,
           },
           stepRuns: stepRuns.map((record: PluginEntityRecord) => {
-            const stepRun = toStepRunRecord(record);
+            const stepRun = toWorkflowStepRunRecord(record);
             return {
               id: stepRun.id,
               status: stepRun.status,
diff --git a/paperclip-addon/plugins/workflow-engine/src/workflow-store.ts b/paperclip-addon/plugins/workflow-engine/src/workflow-store.ts
index 5871a2082258cebcfe8b69ebafbb0814cf303d50..82eb46836883940e109368e927b7e367c96616b5
--- a/paperclip-addon/plugins/workflow-engine/src/workflow-store.ts
+++ b/paperclip-addon/plugins/workflow-engine/src/workflow-store.ts
@@ -44,7 +44,14 @@
 
 type TypedEntityRecord<T> = Omit<PluginEntityRecord, "data"> & { data: T };
 
-function toTypedRecord<T>(record: PluginEntityRecord): TypedEntityRecord<T> {
+function toTypedRecord<T>(
+  record: PluginEntityRecord,
+  entityType: string,
+): TypedEntityRecord<T> {
+  if (record.entityType !== entityType) {
+    throw new Error(`Expected entity type "${entityType}", got "${record.entityType}"`);
+  }
+
   return record as TypedEntityRecord<T>;
 }
 
@@ -67,13 +74,31 @@
   id: string,
   entityType: string,
 ): Promise<TypedEntityRecord<T> | null> {
-  const record = await ctx.entities.get(id);
+  const listQuery = {
+    entityType,
+    id,
+    limit: 1,
+  } as Record<string, unknown>;
 
-  if (!record || record.entityType !== entityType) {
+  const listed = await ctx.entities.list(
+    listQuery as Parameters<PluginContext["entities"]["list"]>[0],
+  );
+  const listedRecord = listed.find(
+    (record: PluginEntityRecord) => record.id === id,
+  ) ?? null;
+
+  if (listedRecord) {
+    return toTypedRecord<T>(listedRecord, entityType);
+  }
+
+  // Fallback: some host SDK variants ignore `id` on entities.list().
+  // Keep defensive type verification on direct get() until id-filter support is universal.
+  const record = await ctx.entities.get(id);
+  if (!record) {
     return null;
   }
 
-  return toTypedRecord<T>(record);
+  return toTypedRecord<T>(record, entityType);
 }
 
 async function requireEntityByType<T>(
diff --git a/paperclip-addon/plugins/workflow-engine/src/workflow-utils.ts b/paperclip-addon/plugins/workflow-engine/src/workflow-utils.ts
new file mode 100644
index 0000000000000000000000000000000000000000..ec1cf6f6b9aa722b126b58c0a728b4e538ad1494
--- /dev/null
+++ b/paperclip-addon/plugins/workflow-engine/src/workflow-utils.ts
@@ -0,0 +1,62 @@
+import type { PluginEntityRecord } from "@paperclipai/plugin-sdk";
+
+import { STEP_STATUSES } from "./constants.js";
+import type { WorkflowStep } from "./dag-engine.js";
+import type {
+  WorkflowDefinition,
+  WorkflowRun,
+  WorkflowStepRun,
+} from "./workflow-store.js";
+
+export type WorkflowRunRecord = PluginEntityRecord & { data: WorkflowRun };
+export type WorkflowStepRunRecord = PluginEntityRecord & { data: WorkflowStepRun };
+export type WorkflowDefinitionRecord = PluginEntityRecord & { data: WorkflowDefinition };
+type StepAgentMetadata = WorkflowStep & {
+  agent?: string;
+  agentName?: string;
+  assigneeAgentName?: string;
+};
+
+export const TERMINAL_STEP_STATUSES = new Set<string>([
+  STEP_STATUSES.done,
+  STEP_STATUSES.failed,
+  STEP_STATUSES.skipped,
+  STEP_STATUSES.escalated,
+]);
+
+export function toWorkflowRunRecord(record: PluginEntityRecord): WorkflowRunRecord {
+  return record as WorkflowRunRecord;
+}
+
+export function toWorkflowStepRunRecord(record: PluginEntityRecord): WorkflowStepRunRecord {
+  return record as WorkflowStepRunRecord;
+}
+
+export function toWorkflowDefinitionRecord(record: PluginEntityRecord): WorkflowDefinitionRecord {
+  return record as WorkflowDefinitionRecord;
+}
+
+export function findStepDefinition(definition: WorkflowDefinitionRecord, stepId: string): WorkflowStep | null {
+  return definition.data.steps.find((step: WorkflowStep) => step.id === stepId) ?? null;
+}
+
+export function getStepAgentNameHint(stepDef: WorkflowStep): string | null {
+  const stepMeta = stepDef as StepAgentMetadata;
+  const candidates = [stepMeta.agentName, stepMeta.agent, stepMeta.assigneeAgentName];
+
+  for (const candidate of candidates) {
+    if (typeof candidate === "string" && candidate.trim()) {
+      return candidate.trim();
+    }
+  }
+
+  return null;
+}
+
+export function getStepAgentName(stepRun: WorkflowStepRunRecord, stepDef: WorkflowStep): string | null {
+  if (typeof stepRun.data.agentName === "string" && stepRun.data.agentName.trim()) {
+    return stepRun.data.agentName.trim();
+  }
+
+  return getStepAgentNameHint(stepDef);
+}

codex
현재 워크트리에서 `plugins/workflow-engine/src`가 통째로 `untracked`로 보이고 있어, 수정 diff가 일반 방식으로 나오지 않는 상태입니다. 실제 파일 내용은 반영되어 있으니, 경로/추적 상태를 확인해 최종 보고에 경로 기준으로 명확히 정리하겠습니다.
exec
/bin/zsh -lc 'git -C /Users/kwak/Projects/paperclip/paperclip-addon rev-parse --show-toplevel' in /Users/kwak/Projects/paperclip/paperclip-addon succeeded in 0ms:
/Users/kwak/Projects/paperclip
exec
/bin/zsh -lc "git -C /Users/kwak/Projects/paperclip/paperclip-addon status --short | sed -n '1,120p'" in /Users/kwak/Projects/paperclip/paperclip-addon succeeded in 0ms:
 D ../AGENTS.md
 D ../CONTRIBUTING.md
 D ../Dockerfile
 D ../Dockerfile.onboard-smoke
 D ../LICENSE
 D ../README.md
 D ../cli/CHANGELOG.md
 D ../cli/esbuild.config.mjs
 D ../cli/package.json
 D ../cli/src/__tests__/agent-jwt-env.test.ts
 D ../cli/src/__tests__/allowed-hostname.test.ts
 D ../cli/src/__tests__/auth-command-registration.test.ts
 D ../cli/src/__tests__/board-auth.test.ts
 D ../cli/src/__tests__/common.test.ts
 D ../cli/src/__tests__/company-delete.test.ts
 D ../cli/src/__tests__/company-import-export-e2e.test.ts
 D ../cli/src/__tests__/company-import-url.test.ts
 D ../cli/src/__tests__/company.test.ts
 D ../cli/src/__tests__/context.test.ts
 D ../cli/src/__tests__/data-dir.test.ts
exec
/bin/zsh -lc 'ls -la /Users/kwak/Projects/paperclip/paperclip-addon/plugins/workflow-engine/src' in /Users/kwak/Projects/paperclip/paperclip-addon succeeded in 0ms:
total 160
drwxr-xr-x@ 11 kwak  staff    352 Mar 24 13:08 .
drwxr-xr-x@  7 kwak  staff    224 Mar 24 13:11 ..
-rw-r--r--@  1 kwak  staff   1109 Mar 24 12:01 constants.ts
-rw-r--r--@  1 kwak  staff   4643 Mar 24 13:07 dag-engine.ts
-rw-r--r--@  1 kwak  staff    102 Mar 24 12:01 index.ts
-rw-r--r--@  1 kwak  staff   1767 Mar 24 12:07 manifest.ts
-rw-r--r--@  1 kwak  staff   7186 Mar 24 13:12 reconciler.ts
drwxr-xr-x@  3 kwak  staff     96 Mar 24 12:20 ui
-rw-r--r--@  1 kwak  staff  33304 Mar 24 13:11 worker.ts
-rw-r--r--@  1 kwak  staff   8275 Mar 24 13:12 workflow-store.ts
-rw-r--r--@  1 kwak  staff   2064 Mar 24 13:06 workflow-utils.ts
file update:
diff --git a/paperclip-addon/plugins/workflow-engine/src/dag-engine.ts b/paperclip-addon/plugins/workflow-engine/src/dag-engine.ts
index b59510f0473171038be77277286d3b5c2063e6c5..e7f21ff998cab78b9325a8aeacf4fda9c5e06553
--- a/paperclip-addon/plugins/workflow-engine/src/dag-engine.ts
+++ b/paperclip-addon/plugins/workflow-engine/src/dag-engine.ts
@@ -2,6 +2,7 @@
   id: string;
   title: string;
   dependsOn: string[];
+  sessionMode?: "fresh" | "reuse";
   onFailure?: "retry" | "skip" | "abort_workflow" | "escalate";
   escalateTo?: string;
   maxRetries?: number;
diff --git a/paperclip-addon/plugins/workflow-engine/src/reconciler.ts b/paperclip-addon/plugins/workflow-engine/src/reconciler.ts
index 14ab4e766a835d145a6c2e7b1b564270285cbdc3..55b649037e8a19a88bb39b9633a6a0cc84fa8d59
--- a/paperclip-addon/plugins/workflow-engine/src/reconciler.ts
+++ b/paperclip-addon/plugins/workflow-engine/src/reconciler.ts
@@ -8,65 +8,21 @@
   listStepRuns,
   updateStepRun,
   updateWorkflowRun,
-  type WorkflowDefinition,
-  type WorkflowRun,
-  type WorkflowStepRun,
 } from "./workflow-store.js";
-import type { WorkflowStep } from "./dag-engine.js";
-
-type WorkflowRunRecord = PluginEntityRecord & { data: WorkflowRun };
-type WorkflowStepRunRecord = PluginEntityRecord & { data: WorkflowStepRun };
-type WorkflowDefinitionRecord = PluginEntityRecord & { data: WorkflowDefinition };
-type StepAgentMetadata = WorkflowStep & {
-  agent?: string;
-  agentName?: string;
-  assigneeAgentName?: string;
-};
+import {
+  TERMINAL_STEP_STATUSES,
+  findStepDefinition,
+  getStepAgentName,
+  toWorkflowDefinitionRecord,
+  toWorkflowRunRecord,
+  toWorkflowStepRunRecord,
+} from "./workflow-utils.js";
 
 const STALE_THRESHOLD_MS = 300_000;
-const TERMINAL_STEP_STATUSES = new Set<string>([
-  STEP_STATUSES.done,
-  STEP_STATUSES.failed,
-  STEP_STATUSES.skipped,
-  STEP_STATUSES.escalated,
-]);
 
 function isStale(record: PluginEntityRecord, thresholdMs: number): boolean {
   const updatedAt = new Date(record.updatedAt).getTime();
   return Date.now() - updatedAt > thresholdMs;
-}
-
-function toWorkflowRunRecord(record: PluginEntityRecord): WorkflowRunRecord {
-  return record as WorkflowRunRecord;
-}
-
-function toWorkflowStepRunRecord(record: PluginEntityRecord): WorkflowStepRunRecord {
-  return record as WorkflowStepRunRecord;
-}
-
-function toWorkflowDefinitionRecord(record: PluginEntityRecord): WorkflowDefinitionRecord {
-  return record as WorkflowDefinitionRecord;
-}
-
-function findStepDefinition(definition: WorkflowDefinitionRecord, stepId: string): WorkflowStep | null {
-  return definition.data.steps.find((step: WorkflowStep) => step.id === stepId) ?? null;
-}
-
-function getStepAgentName(stepRun: WorkflowStepRunRecord, stepDef: WorkflowStep): string | null {
-  if (typeof stepRun.data.agentName === "string" && stepRun.data.agentName.trim()) {
-    return stepRun.data.agentName.trim();
-  }
-
-  const stepMeta = stepDef as StepAgentMetadata;
-  const candidates = [stepMeta.agentName, stepMeta.agent, stepMeta.assigneeAgentName];
-
-  for (const candidate of candidates) {
-    if (typeof candidate === "string" && candidate.trim()) {
-      return candidate.trim();
-    }
-  }
-
-  return null;
 }
 
 async function invokeAgentByName(
diff --git a/paperclip-addon/plugins/workflow-engine/src/worker.ts b/paperclip-addon/plugins/workflow-engine/src/worker.ts
index c5ac3fbb4ca7f767eb29331bd3630b635449b335..303e35958890fa1f66db3f2b0ff1fa1f134102a8
--- a/paperclip-addon/plugins/workflow-engine/src/worker.ts
+++ b/paperclip-addon/plugins/workflow-engine/src/worker.ts
@@ -15,6 +15,7 @@
 } from "./dag-engine.js";
 import {
   checkIdempotency,
+  createWorkflowRun,
   createStepRun,
   findStepRunByIssueId,
   getWorkflowDefinition,
@@ -25,51 +26,38 @@
   markIdempotency,
   updateStepRun,
   updateWorkflowRun,
-  type WorkflowDefinition,
-  type WorkflowRun,
   type WorkflowStepRun,
 } from "./workflow-store.js";
+import {
+  TERMINAL_STEP_STATUSES,
+  findStepDefinition,
+  getStepAgentName,
+  getStepAgentNameHint,
+  toWorkflowDefinitionRecord,
+  toWorkflowRunRecord,
+  toWorkflowStepRunRecord,
+  type WorkflowDefinitionRecord,
+  type WorkflowRunRecord,
+  type WorkflowStepRunRecord,
+} from "./workflow-utils.js";
 
-type WorkflowStepRecord = PluginEntityRecord & { data: WorkflowStepRun };
-type WorkflowRunRecord = PluginEntityRecord & { data: WorkflowRun };
-type WorkflowDefinitionRecord = PluginEntityRecord & { data: WorkflowDefinition };
 type WorkflowStepMetadata = WorkflowStep & {
   description?: string;
-  agent?: string;
-  agentName?: string;
-  assigneeAgentName?: string;
   assigneeAgentId?: string;
+  sessionMode?: "fresh" | "reuse";
 };
 type AgentRecord = Awaited<ReturnType<PluginContext["agents"]["list"]>>[number];
+type IssueUpdatePatch = Parameters<PluginContext["issues"]["update"]>[1];
 type ReconcilerModule = {
   reconcileStuckSteps?: (ctx: PluginContext) => Promise<void>;
 };
 
-const TERMINAL_STEP_STATUSES = new Set<string>([
-  STEP_STATUSES.done,
-  STEP_STATUSES.failed,
-  STEP_STATUSES.skipped,
-  STEP_STATUSES.escalated,
-]);
-
 function summarizeError(error: unknown): string {
   if (error instanceof Error) {
     return error.message;
   }
 
   return String(error);
-}
-
-function toStepRunRecord(record: PluginEntityRecord): WorkflowStepRecord {
-  return record as WorkflowStepRecord;
-}
-
-function toWorkflowRunRecord(record: PluginEntityRecord): WorkflowRunRecord {
-  return record as WorkflowRunRecord;
-}
-
-function toWorkflowDefinitionRecord(record: PluginEntityRecord): WorkflowDefinitionRecord {
-  return record as WorkflowDefinitionRecord;
 }
 
 function buildIdempotencyKey(event: PluginEvent): string {
@@ -78,10 +66,6 @@
 
 function getStepMetadata(stepDef: WorkflowStep): WorkflowStepMetadata {
   return stepDef as WorkflowStepMetadata;
-}
-
-function findStepDefinition(definition: WorkflowDefinitionRecord, stepId: string): WorkflowStep | null {
-  return definition.data.steps.find((step: WorkflowStep) => step.id === stepId) ?? null;
 }
 
 function getStepDescription(stepDef: WorkflowStep): string | undefined {
@@ -89,19 +73,6 @@
   return typeof description === "string" && description.trim() ? description.trim() : undefined;
 }
 
-function getStepAgentNameHint(stepDef: WorkflowStep): string | null {
-  const meta = getStepMetadata(stepDef);
-  const candidates = [meta.agentName, meta.agent, meta.assigneeAgentName];
-
-  for (const candidate of candidates) {
-    if (typeof candidate === "string" && candidate.trim()) {
-      return candidate.trim();
-    }
-  }
-
-  return null;
-}
-
 function getStepAgentIdHint(stepDef: WorkflowStep): string | null {
   const assigneeAgentId = getStepMetadata(stepDef).assigneeAgentId;
   if (typeof assigneeAgentId !== "string" || !assigneeAgentId.trim()) {
@@ -147,68 +118,256 @@
 
 async function invokeAgentForStep(
   ctx: PluginContext,
+  stepRunRecord: WorkflowStepRunRecord,
   stepDef: WorkflowStep,
-  agentName: string,
   workflowName: string,
   companyId: string,
-): Promise<void> {
+): Promise<WorkflowStepRunRecord> {
+  const agentName = getStepAgentName(stepRunRecord, stepDef);
+  if (!agentName) {
+    ctx.logger.warn("Agent name is missing for workflow step", {
+      companyId,
+      runId: stepRunRecord.data.runId,
+      stepId: stepDef.id,
+      workflowName,
+    });
+    return stepRunRecord;
+  }
+
   const agents = await ctx.agents.list({ companyId });
-  const agent = agents.find((candidate: AgentRecord) => candidate.name === agentName);
+  const agent = agents.find((candidate: AgentRecord) => candidate.name === agentName) ?? null;
 
   if (!agent) {
     ctx.logger.warn("Agent not found for step", {
       agentName,
       companyId,
+      runId: stepRunRecord.data.runId,
+      stepId: stepDef.id,
+      workflowName,
+    });
+    return stepRunRecord;
+  }
+
+  const prompt = `workflow:${workflowName}/step:${stepDef.id} — "${stepDef.title}" is ready. Please proceed with the assigned task.`;
+  const reason = `workflow:${workflowName}/step:${stepDef.id}`;
+  const sessionMode = getStepMetadata(stepDef).sessionMode === "reuse" ? "reuse" : "fresh";
+
+  if (sessionMode === "reuse") {
+    let sessionId = typeof stepRunRecord.data.sessionId === "string" && stepRunRecord.data.sessionId.trim()
+      ? stepRunRecord.data.sessionId.trim()
+      : "";
+
+    if (!sessionId) {
+      const session = await ctx.agents.sessions.create(agent.id, companyId, {
+        reason,
+      });
+      sessionId = session.sessionId;
+      stepRunRecord = toWorkflowStepRunRecord(await updateStepRun(ctx, stepRunRecord.id, {
+        sessionId,
+      }));
+    }
+
+    await ctx.agents.sessions.sendMessage(sessionId, companyId, {
+      prompt,
+      reason,
+    });
+
+    ctx.logger.info("Sent workflow step prompt via agent session", {
+      agentId: agent.id,
+      agentName,
+      companyId,
+      runId: stepRunRecord.data.runId,
+      sessionId,
       stepId: stepDef.id,
       workflowName,
     });
-    return;
+    return stepRunRecord;
   }
 
   await ctx.agents.invoke(agent.id, companyId, {
-    prompt: `workflow:${workflowName}/step:${stepDef.id} — "${stepDef.title}" is ready. Please proceed with the assigned task.`,
-    reason: `workflow:${workflowName}/step:${stepDef.id}`,
+    prompt,
+    reason,
   });
 
   ctx.logger.info("Invoked agent for workflow step", {
     agentId: agent.id,
     agentName,
     companyId,
+    runId: stepRunRecord.data.runId,
     stepId: stepDef.id,
     workflowName,
   });
+
+  return stepRunRecord;
 }
 
 async function activateBacklogStep(
   ctx: PluginContext,
-  stepRunRecord: WorkflowStepRecord,
+  stepRunRecord: WorkflowStepRunRecord,
   stepDef: WorkflowStep,
   workflowName: string,
   companyId: string,
-): Promise<void> {
+  options?: { parentIssueId?: string },
+): Promise<WorkflowStepRunRecord> {
   if (stepRunRecord.data.status !== STEP_STATUSES.backlog) {
-    return;
+    return stepRunRecord;
   }
 
-  const updated = toStepRunRecord(await updateStepRun(ctx, stepRunRecord.id, {
+  const resolvedAgent = await resolveStepAgent(
+    ctx,
+    companyId,
+    stepDef,
+    stepRunRecord.data.agentName,
+  );
+
+  let issueId = typeof stepRunRecord.data.issueId === "string" && stepRunRecord.data.issueId.trim()
+    ? stepRunRecord.data.issueId.trim()
+    : "";
+  const stepDescription = getStepDescription(stepDef) ?? `Workflow step: ${stepDef.id}`;
+
+  if (!issueId) {
+    const issue = await ctx.issues.create({
+      assigneeAgentId: resolvedAgent.agentId ?? undefined,
+      companyId,
+      description: stepDescription,
+      parentId: options?.parentIssueId,
+      title: `[${workflowName}] ${stepDef.title}`,
+    });
+    await ctx.issues.update(issue.id, { status: "todo" } as IssueUpdatePatch, companyId);
+    issueId = issue.id;
+  }
+
+  const nextStartedAt = stepRunRecord.data.startedAt ?? new Date().toISOString();
+  let updatedStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, stepRunRecord.id, {
+    agentName: resolvedAgent.agentName ?? stepRunRecord.data.agentName,
+    issueId,
+    startedAt: nextStartedAt,
     status: STEP_STATUSES.todo,
   }));
 
-  await invokeAgentForStep(
+  updatedStepRun = await invokeAgentForStep(
     ctx,
+    updatedStepRun,
     stepDef,
-    updated.data.agentName,
     workflowName,
     companyId,
   );
+
+  return updatedStepRun;
 }
 
+async function startWorkflow(
+  ctx: PluginContext,
+  workflowId: string,
+  companyId: string,
+  options?: {
+    createParentIssue?: boolean;
+    parentIssueId?: string;
+  },
+): Promise<{
+  activatedStepIds: string[];
+  parentIssueId: string | null;
+  runId: string;
+  workflowId: string;
+}> {
+  const workflowDefinition = await getWorkflowDefinition(ctx, workflowId);
+  if (!workflowDefinition) {
+    throw new Error(`Workflow definition not found: ${workflowId}`);
+  }
+
+  const typedWorkflowDefinition = toWorkflowDefinitionRecord(workflowDefinition);
+  if (typedWorkflowDefinition.data.companyId !== companyId) {
+    throw new Error(`Workflow does not belong to company: ${workflowId}`);
+  }
+
+  let parentIssueId = typeof options?.parentIssueId === "string" && options.parentIssueId.trim()
+    ? options.parentIssueId.trim()
+    : "";
+  if (!parentIssueId && options?.createParentIssue) {
+    const parentIssue = await ctx.issues.create({
+      companyId,
+      description: typedWorkflowDefinition.data.description || `Workflow run: ${typedWorkflowDefinition.data.name}`,
+      title: `[${typedWorkflowDefinition.data.name}] Workflow run`,
+    });
+    parentIssueId = parentIssue.id;
+  }
+
+  const workflowRun = toWorkflowRunRecord(await createWorkflowRun(ctx, {
+    companyId,
+    parentIssueId: parentIssueId || undefined,
+    startedAt: new Date().toISOString(),
+    status: RUN_STATUSES.running,
+    workflowId: typedWorkflowDefinition.id,
+    workflowName: typedWorkflowDefinition.data.name,
+  }));
+
+  const agents = await ctx.agents.list({ companyId });
+  const agentsByName = new Map<string, AgentRecord>();
+  for (const agent of agents) {
+    agentsByName.set(agent.name, agent);
+  }
+
+  const pendingRootSteps: Array<{ stepDef: WorkflowStep; stepRun: WorkflowStepRunRecord }> = [];
+  for (const stepDef of typedWorkflowDefinition.data.steps) {
+    const agentNameHint = getStepAgentNameHint(stepDef);
+    const matchedAgent = agentNameHint ? agentsByName.get(agentNameHint) ?? null : null;
+    const resolvedAgent = matchedAgent
+      ? { agentId: matchedAgent.id, agentName: matchedAgent.name }
+      : await resolveStepAgent(ctx, companyId, stepDef, agentNameHint ?? undefined);
+
+    if (!resolvedAgent.agentName) {
+      throw new Error(`Unable to resolve step assignee for "${stepDef.id}"`);
+    }
+
+    const stepRun = toWorkflowStepRunRecord(await createStepRun(ctx, companyId, {
+      agentName: resolvedAgent.agentName,
+      retryCount: 0,
+      runId: workflowRun.id,
+      status: STEP_STATUSES.backlog,
+      stepId: stepDef.id,
+    }));
+
+    if (stepDef.dependsOn.length === 0 && stepDef.triggerOn !== "escalation") {
+      pendingRootSteps.push({ stepDef, stepRun });
+    }
+  }
+
+  const activatedStepIds: string[] = [];
+  for (const pending of pendingRootSteps) {
+    await activateBacklogStep(
+      ctx,
+      pending.stepRun,
+      pending.stepDef,
+      typedWorkflowDefinition.data.name,
+      companyId,
+      { parentIssueId: parentIssueId || undefined },
+    );
+    activatedStepIds.push(pending.stepDef.id);
+  }
+
+  ctx.logger.info("Started workflow run", {
+    activatedStepIds,
+    companyId,
+    parentIssueId: parentIssueId || null,
+    runId: workflowRun.id,
+    workflowId: typedWorkflowDefinition.id,
+    workflowName: typedWorkflowDefinition.data.name,
+  });
+
+  return {
+    activatedStepIds,
+    parentIssueId: parentIssueId || null,
+    runId: workflowRun.id,
+    workflowId: typedWorkflowDefinition.id,
+  };
+}
+
 async function advanceWorkflow(
   ctx: PluginContext,
   stepRunRecord: PluginEntityRecord,
   companyId: string,
 ): Promise<void> {
-  const completedStepRun = toStepRunRecord(stepRunRecord);
+  const completedStepRun = toWorkflowStepRunRecord(stepRunRecord);
   const workflowRun = await getWorkflowRun(ctx, completedStepRun.data.runId);
 
   if (!workflowRun) {
@@ -237,7 +396,7 @@
   }
 
   const typedWorkflowDefinition = toWorkflowDefinitionRecord(workflowDefinition);
-  const stepRuns = (await listStepRuns(ctx, typedWorkflowRun.id, companyId)).map(toStepRunRecord);
+  const stepRuns = (await listStepRuns(ctx, typedWorkflowRun.id, companyId)).map(toWorkflowStepRunRecord);
   const completed = new Set<string>();
   const failed = new Set<string>();
   const skipped = new Set<string>();
@@ -289,7 +448,7 @@
         continue;
       }
 
-      stepRun = toStepRunRecord(await createStepRun(ctx, companyId, {
+      stepRun = toWorkflowStepRunRecord(await createStepRun(ctx, companyId, {
         agentName: resolvedAgent.agentName,
         retryCount: 0,
         runId: typedWorkflowRun.id,
@@ -305,6 +464,7 @@
       stepDef,
       typedWorkflowRun.data.workflowName,
       companyId,
+      { parentIssueId: typedWorkflowRun.data.parentIssueId },
     );
   }
 
@@ -329,7 +489,7 @@
   ctx: PluginContext,
   workflowRun: WorkflowRunRecord,
   workflowDefinition: WorkflowDefinitionRecord,
-  sourceStepRun: WorkflowStepRecord,
+  sourceStepRun: WorkflowStepRunRecord,
   escalationTargetId: string,
   companyId: string,
 ): Promise<void> {
@@ -338,7 +498,7 @@
     throw new Error(`Escalation target step not found: ${escalationTargetId}`);
   }
 
-  const stepRuns = (await listStepRuns(ctx, workflowRun.id, companyId)).map(toStepRunRecord);
+  const stepRuns = (await listStepRuns(ctx, workflowRun.id, companyId)).map(toWorkflowStepRunRecord);
   let escalationStepRun = stepRuns.find((candidate) => candidate.data.stepId === escalationTargetId) ?? null;
   const resolvedAgent = await resolveStepAgent(
     ctx,
@@ -352,7 +512,7 @@
   }
 
   if (!escalationStepRun) {
-    escalationStepRun = toStepRunRecord(await createStepRun(ctx, companyId, {
+    escalationStepRun = toWorkflowStepRunRecord(await createStepRun(ctx, companyId, {
       agentName: resolvedAgent.agentName,
       retryCount: 0,
       runId: workflowRun.id,
@@ -374,14 +534,14 @@
       title: `${workflowRun.data.workflowName}: ${escalationStep.title}`,
     });
 
-    escalationStepRun = toStepRunRecord(await updateStepRun(ctx, escalationStepRun.id, {
+    escalationStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, escalationStepRun.id, {
       issueId: issue.id,
     }));
   }
 
   const shouldActivate = escalationStepRun.data.status === STEP_STATUSES.backlog;
   if (shouldActivate) {
-    escalationStepRun = toStepRunRecord(await updateStepRun(ctx, escalationStepRun.id, {
+    escalationStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, escalationStepRun.id, {
       status: STEP_STATUSES.todo,
     }));
   }
@@ -389,8 +549,8 @@
   if (shouldActivate) {
     await invokeAgentForStep(
       ctx,
+      escalationStepRun,
       escalationStep,
-      escalationStepRun.data.agentName,
       workflowRun.data.workflowName,
       companyId,
     );
@@ -421,7 +581,7 @@
     return;
   }
 
-  const typedStepRun = toStepRunRecord(stepRunRecord);
+  const typedStepRun = toWorkflowStepRunRecord(stepRunRecord);
   if (TERMINAL_STEP_STATUSES.has(typedStepRun.data.status)) {
     return;
   }
@@ -469,7 +629,7 @@
   if (options.allowRetry) {
     const retryInfo = getRetryInfo(typedWorkflowDefinition.data.steps, typedStepRun.data.stepId);
     if (retryInfo.shouldRetry && typedStepRun.data.retryCount < retryInfo.maxRetries) {
-      const retriedStepRun = toStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
+      const retriedStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
         completedAt: undefined,
         retryCount: typedStepRun.data.retryCount + 1,
         status: STEP_STATUSES.todo,
@@ -477,8 +637,8 @@
 
       await invokeAgentForStep(
         ctx,
+        retriedStepRun,
         stepDef,
-        retriedStepRun.data.agentName,
         typedWorkflowRun.data.workflowName,
         event.companyId,
       );
@@ -496,7 +656,7 @@
   }
 
   if (stepDef.onFailure === "skip") {
-    const skippedStepRun = toStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
+    const skippedStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
       completedAt: new Date().toISOString(),
       status: STEP_STATUSES.skipped,
     }));
@@ -556,7 +716,7 @@
       return;
     }
 
-    const escalatedStepRun = toStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
+    const escalatedStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
       completedAt: new Date().toISOString(),
       status: STEP_STATUSES.escalated,
     }));
@@ -617,6 +777,29 @@
   }
 }
 
+function registerDataHandler(
+  ctx: PluginContext,
+  key: string,
+  handler: (params: Record<string, unknown>) => Promise<unknown>,
+): void {
+  const dataClient = ctx.data as {
+    handle?: (handlerKey: string, handlerFn: (params: Record<string, unknown>) => Promise<unknown>) => void;
+    register?: (handlerKey: string, handlerFn: (params: Record<string, unknown>) => Promise<unknown>) => void;
+  };
+
+  if (typeof dataClient.handle === "function") {
+    dataClient.handle(key, handler);
+    return;
+  }
+
+  if (typeof dataClient.register === "function") {
+    dataClient.register(key, handler);
+    return;
+  }
+
+  throw new Error("Plugin data client does not support handler registration");
+}
+
 const plugin = definePlugin({
   async setup(ctx: PluginContext) {
     ctx.events.on("issue.updated", async (event: PluginEvent) => {
@@ -638,7 +821,7 @@
           return;
         }
 
-        const stepRun = toStepRunRecord(stepRunRecord);
+        const stepRun = toWorkflowStepRunRecord(stepRunRecord);
         const payload = event.payload as { status?: string };
         const issueStatus = typeof payload.status === "string" ? payload.status : undefined;
 
@@ -657,7 +840,7 @@
           return;
         }
 
-        const updatedStepRun = toStepRunRecord(await updateStepRun(ctx, stepRun.id, {
+        const updatedStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, stepRun.id, {
           completedAt: nextStepStatus === STEP_STATUSES.done ? new Date().toISOString() : undefined,
           startedAt: nextStepStatus === STEP_STATUSES.inProgress
             ? stepRun.data.startedAt ?? new Date().toISOString()
@@ -687,11 +870,21 @@
 
     ctx.events.on("issue.created", async (event: PluginEvent) => {
       try {
+        const idempotencyKey = buildIdempotencyKey(event);
+        if (await checkIdempotency(ctx, idempotencyKey, event.companyId)) {
+          return;
+        }
+
         const payload = event.payload as {
           id?: string;
           parentId?: string;
           assigneeAgentId?: string;
         };
+        const issueId = typeof event.entityId === "string" && event.entityId.trim()
+          ? event.entityId.trim()
+          : typeof payload.id === "string" && payload.id.trim()
+            ? payload.id.trim()
+            : "";
 
         const parentId = typeof payload.parentId === "string" && payload.parentId.trim()
           ? payload.parentId.trim()
@@ -700,12 +893,34 @@
           ? payload.assigneeAgentId.trim()
           : "";
 
-        if (parentId || !assigneeAgentId) {
+        if (!issueId || parentId || !assigneeAgentId) {
+          await markIdempotency(ctx, idempotencyKey, event.companyId);
+          return;
+        }
+
+        const agent = await ctx.agents.get(assigneeAgentId, event.companyId);
+        const metadata = agent?.metadata as Record<string, unknown> | undefined;
+        const defaultParentIssueId = typeof metadata?.defaultParentIssueId === "string" && metadata.defaultParentIssueId.trim()
+          ? metadata.defaultParentIssueId.trim()
+          : "";
+
+        if (!defaultParentIssueId) {
+          await markIdempotency(ctx, idempotencyKey, event.companyId);
           return;
         }
 
-        ctx.logger.info("parentId filler: issue created without parent", {
-          issueId: event.entityId ?? payload.id ?? null,
+        await ctx.issues.update(
+          issueId,
+          { parentId: defaultParentIssueId } as IssueUpdatePatch,
+          event.companyId,
+        );
+        await markIdempotency(ctx, idempotencyKey, event.companyId);
+
+        ctx.logger.info("parentId filler: populated issue parentId from agent metadata", {
+          assigneeAgentId,
+          companyId: event.companyId,
+          issueId,
+          parentId: defaultParentIssueId,
         });
       } catch (error) {
         ctx.logger.warn("Failed to handle issue.created event", {
@@ -744,7 +959,23 @@
       await runReconciler(ctx);
     });
 
-    ctx.data.register("workflow-overview", async (params: Record<string, unknown>) => {
+    registerDataHandler(ctx, "start-workflow", async (params: Record<string, unknown>) => {
+      const workflowId = typeof params.workflowId === "string" ? params.workflowId.trim() : "";
+      const companyId = typeof params.companyId === "string" ? params.companyId.trim() : "";
+      const parentIssueId = typeof params.parentIssueId === "string" ? params.parentIssueId.trim() : "";
+      const createParentIssue = params.createParentIssue === true;
+
+      if (!workflowId || !companyId) {
+        throw new Error("start-workflow requires workflowId and companyId");
+      }
+
+      return await startWorkflow(ctx, workflowId, companyId, {
+        createParentIssue,
+        parentIssueId: parentIssueId || undefined,
+      });
+    });
+
+    registerDataHandler(ctx, "workflow-overview", async (params: Record<string, unknown>) => {
       try {
         const companyId = typeof params.companyId === "string" ? params.companyId.trim() : "";
         if (!companyId) {
@@ -782,7 +1013,7 @@
       }
     });
 
-    ctx.data.register("workflow-run-detail", async (params: Record<string, unknown>) => {
+    registerDataHandler(ctx, "workflow-run-detail", async (params: Record<string, unknown>) => {
       try {
         const runId = typeof params.runId === "string" ? params.runId.trim() : "";
         if (!runId) {
@@ -807,7 +1038,7 @@
             ...typedWorkflowRun.data,
           },
           stepRuns: stepRuns.map((record: PluginEntityRecord) => {
-            const stepRun = toStepRunRecord(record);
+            const stepRun = toWorkflowStepRunRecord(record);
             return {
               id: stepRun.id,
               status: stepRun.status,
diff --git a/paperclip-addon/plugins/workflow-engine/src/workflow-store.ts b/paperclip-addon/plugins/workflow-engine/src/workflow-store.ts
index 5871a2082258cebcfe8b69ebafbb0814cf303d50..82eb46836883940e109368e927b7e367c96616b5
--- a/paperclip-addon/plugins/workflow-engine/src/workflow-store.ts
+++ b/paperclip-addon/plugins/workflow-engine/src/workflow-store.ts
@@ -44,7 +44,14 @@
 
 type TypedEntityRecord<T> = Omit<PluginEntityRecord, "data"> & { data: T };
 
-function toTypedRecord<T>(record: PluginEntityRecord): TypedEntityRecord<T> {
+function toTypedRecord<T>(
+  record: PluginEntityRecord,
+  entityType: string,
+): TypedEntityRecord<T> {
+  if (record.entityType !== entityType) {
+    throw new Error(`Expected entity type "${entityType}", got "${record.entityType}"`);
+  }
+
   return record as TypedEntityRecord<T>;
 }
 
@@ -67,13 +74,31 @@
   id: string,
   entityType: string,
 ): Promise<TypedEntityRecord<T> | null> {
-  const record = await ctx.entities.get(id);
+  const listQuery = {
+    entityType,
+    id,
+    limit: 1,
+  } as Record<string, unknown>;
 
-  if (!record || record.entityType !== entityType) {
+  const listed = await ctx.entities.list(
+    listQuery as Parameters<PluginContext["entities"]["list"]>[0],
+  );
+  const listedRecord = listed.find(
+    (record: PluginEntityRecord) => record.id === id,
+  ) ?? null;
+
+  if (listedRecord) {
+    return toTypedRecord<T>(listedRecord, entityType);
+  }
+
+  // Fallback: some host SDK variants ignore `id` on entities.list().
+  // Keep defensive type verification on direct get() until id-filter support is universal.
+  const record = await ctx.entities.get(id);
+  if (!record) {
     return null;
   }
 
-  return toTypedRecord<T>(record);
+  return toTypedRecord<T>(record, entityType);
 }
 
 async function requireEntityByType<T>(
diff --git a/paperclip-addon/plugins/workflow-engine/src/workflow-utils.ts b/paperclip-addon/plugins/workflow-engine/src/workflow-utils.ts
new file mode 100644
index 0000000000000000000000000000000000000000..ec1cf6f6b9aa722b126b58c0a728b4e538ad1494
--- /dev/null
+++ b/paperclip-addon/plugins/workflow-engine/src/workflow-utils.ts
@@ -0,0 +1,62 @@
+import type { PluginEntityRecord } from "@paperclipai/plugin-sdk";
+
+import { STEP_STATUSES } from "./constants.js";
+import type { WorkflowStep } from "./dag-engine.js";
+import type {
+  WorkflowDefinition,
+  WorkflowRun,
+  WorkflowStepRun,
+} from "./workflow-store.js";
+
+export type WorkflowRunRecord = PluginEntityRecord & { data: WorkflowRun };
+export type WorkflowStepRunRecord = PluginEntityRecord & { data: WorkflowStepRun };
+export type WorkflowDefinitionRecord = PluginEntityRecord & { data: WorkflowDefinition };
+type StepAgentMetadata = WorkflowStep & {
+  agent?: string;
+  agentName?: string;
+  assigneeAgentName?: string;
+};
+
+export const TERMINAL_STEP_STATUSES = new Set<string>([
+  STEP_STATUSES.done,
+  STEP_STATUSES.failed,
+  STEP_STATUSES.skipped,
+  STEP_STATUSES.escalated,
+]);
+
+export function toWorkflowRunRecord(record: PluginEntityRecord): WorkflowRunRecord {
+  return record as WorkflowRunRecord;
+}
+
+export function toWorkflowStepRunRecord(record: PluginEntityRecord): WorkflowStepRunRecord {
+  return record as WorkflowStepRunRecord;
+}
+
+export function toWorkflowDefinitionRecord(record: PluginEntityRecord): WorkflowDefinitionRecord {
+  return record as WorkflowDefinitionRecord;
+}
+
+export function findStepDefinition(definition: WorkflowDefinitionRecord, stepId: string): WorkflowStep | null {
+  return definition.data.steps.find((step: WorkflowStep) => step.id === stepId) ?? null;
+}
+
+export function getStepAgentNameHint(stepDef: WorkflowStep): string | null {
+  const stepMeta = stepDef as StepAgentMetadata;
+  const candidates = [stepMeta.agentName, stepMeta.agent, stepMeta.assigneeAgentName];
+
+  for (const candidate of candidates) {
+    if (typeof candidate === "string" && candidate.trim()) {
+      return candidate.trim();
+    }
+  }
+
+  return null;
+}
+
+export function getStepAgentName(stepRun: WorkflowStepRunRecord, stepDef: WorkflowStep): string | null {
+  if (typeof stepRun.data.agentName === "string" && stepRun.data.agentName.trim()) {
+    return stepRun.data.agentName.trim();
+  }
+
+  return getStepAgentNameHint(stepDef);
+}

exec
/bin/zsh -lc "git -C /Users/kwak/Projects/paperclip status --short -- paperclip-addon/plugins/workflow-engine/src | sed -n '1,120p'" in /Users/kwak/Projects/paperclip/paperclip-addon succeeded in 0ms:
?? paperclip-addon/plugins/workflow-engine/src/
file update:
diff --git a/paperclip-addon/plugins/workflow-engine/src/dag-engine.ts b/paperclip-addon/plugins/workflow-engine/src/dag-engine.ts
index b59510f0473171038be77277286d3b5c2063e6c5..e7f21ff998cab78b9325a8aeacf4fda9c5e06553
--- a/paperclip-addon/plugins/workflow-engine/src/dag-engine.ts
+++ b/paperclip-addon/plugins/workflow-engine/src/dag-engine.ts
@@ -2,6 +2,7 @@
   id: string;
   title: string;
   dependsOn: string[];
+  sessionMode?: "fresh" | "reuse";
   onFailure?: "retry" | "skip" | "abort_workflow" | "escalate";
   escalateTo?: string;
   maxRetries?: number;
diff --git a/paperclip-addon/plugins/workflow-engine/src/reconciler.ts b/paperclip-addon/plugins/workflow-engine/src/reconciler.ts
index 14ab4e766a835d145a6c2e7b1b564270285cbdc3..55b649037e8a19a88bb39b9633a6a0cc84fa8d59
--- a/paperclip-addon/plugins/workflow-engine/src/reconciler.ts
+++ b/paperclip-addon/plugins/workflow-engine/src/reconciler.ts
@@ -8,65 +8,21 @@
   listStepRuns,
   updateStepRun,
   updateWorkflowRun,
-  type WorkflowDefinition,
-  type WorkflowRun,
-  type WorkflowStepRun,
 } from "./workflow-store.js";
-import type { WorkflowStep } from "./dag-engine.js";
-
-type WorkflowRunRecord = PluginEntityRecord & { data: WorkflowRun };
-type WorkflowStepRunRecord = PluginEntityRecord & { data: WorkflowStepRun };
-type WorkflowDefinitionRecord = PluginEntityRecord & { data: WorkflowDefinition };
-type StepAgentMetadata = WorkflowStep & {
-  agent?: string;
-  agentName?: string;
-  assigneeAgentName?: string;
-};
+import {
+  TERMINAL_STEP_STATUSES,
+  findStepDefinition,
+  getStepAgentName,
+  toWorkflowDefinitionRecord,
+  toWorkflowRunRecord,
+  toWorkflowStepRunRecord,
+} from "./workflow-utils.js";
 
 const STALE_THRESHOLD_MS = 300_000;
-const TERMINAL_STEP_STATUSES = new Set<string>([
-  STEP_STATUSES.done,
-  STEP_STATUSES.failed,
-  STEP_STATUSES.skipped,
-  STEP_STATUSES.escalated,
-]);
 
 function isStale(record: PluginEntityRecord, thresholdMs: number): boolean {
   const updatedAt = new Date(record.updatedAt).getTime();
   return Date.now() - updatedAt > thresholdMs;
-}
-
-function toWorkflowRunRecord(record: PluginEntityRecord): WorkflowRunRecord {
-  return record as WorkflowRunRecord;
-}
-
-function toWorkflowStepRunRecord(record: PluginEntityRecord): WorkflowStepRunRecord {
-  return record as WorkflowStepRunRecord;
-}
-
-function toWorkflowDefinitionRecord(record: PluginEntityRecord): WorkflowDefinitionRecord {
-  return record as WorkflowDefinitionRecord;
-}
-
-function findStepDefinition(definition: WorkflowDefinitionRecord, stepId: string): WorkflowStep | null {
-  return definition.data.steps.find((step: WorkflowStep) => step.id === stepId) ?? null;
-}
-
-function getStepAgentName(stepRun: WorkflowStepRunRecord, stepDef: WorkflowStep): string | null {
-  if (typeof stepRun.data.agentName === "string" && stepRun.data.agentName.trim()) {
-    return stepRun.data.agentName.trim();
-  }
-
-  const stepMeta = stepDef as StepAgentMetadata;
-  const candidates = [stepMeta.agentName, stepMeta.agent, stepMeta.assigneeAgentName];
-
-  for (const candidate of candidates) {
-    if (typeof candidate === "string" && candidate.trim()) {
-      return candidate.trim();
-    }
-  }
-
-  return null;
 }
 
 async function invokeAgentByName(
diff --git a/paperclip-addon/plugins/workflow-engine/src/worker.ts b/paperclip-addon/plugins/workflow-engine/src/worker.ts
index c5ac3fbb4ca7f767eb29331bd3630b635449b335..303e35958890fa1f66db3f2b0ff1fa1f134102a8
--- a/paperclip-addon/plugins/workflow-engine/src/worker.ts
+++ b/paperclip-addon/plugins/workflow-engine/src/worker.ts
@@ -15,6 +15,7 @@
 } from "./dag-engine.js";
 import {
   checkIdempotency,
+  createWorkflowRun,
   createStepRun,
   findStepRunByIssueId,
   getWorkflowDefinition,
@@ -25,51 +26,38 @@
   markIdempotency,
   updateStepRun,
   updateWorkflowRun,
-  type WorkflowDefinition,
-  type WorkflowRun,
   type WorkflowStepRun,
 } from "./workflow-store.js";
+import {
+  TERMINAL_STEP_STATUSES,
+  findStepDefinition,
+  getStepAgentName,
+  getStepAgentNameHint,
+  toWorkflowDefinitionRecord,
+  toWorkflowRunRecord,
+  toWorkflowStepRunRecord,
+  type WorkflowDefinitionRecord,
+  type WorkflowRunRecord,
+  type WorkflowStepRunRecord,
+} from "./workflow-utils.js";
 
-type WorkflowStepRecord = PluginEntityRecord & { data: WorkflowStepRun };
-type WorkflowRunRecord = PluginEntityRecord & { data: WorkflowRun };
-type WorkflowDefinitionRecord = PluginEntityRecord & { data: WorkflowDefinition };
 type WorkflowStepMetadata = WorkflowStep & {
   description?: string;
-  agent?: string;
-  agentName?: string;
-  assigneeAgentName?: string;
   assigneeAgentId?: string;
+  sessionMode?: "fresh" | "reuse";
 };
 type AgentRecord = Awaited<ReturnType<PluginContext["agents"]["list"]>>[number];
+type IssueUpdatePatch = Parameters<PluginContext["issues"]["update"]>[1];
 type ReconcilerModule = {
   reconcileStuckSteps?: (ctx: PluginContext) => Promise<void>;
 };
 
-const TERMINAL_STEP_STATUSES = new Set<string>([
-  STEP_STATUSES.done,
-  STEP_STATUSES.failed,
-  STEP_STATUSES.skipped,
-  STEP_STATUSES.escalated,
-]);
-
 function summarizeError(error: unknown): string {
   if (error instanceof Error) {
     return error.message;
   }
 
   return String(error);
-}
-
-function toStepRunRecord(record: PluginEntityRecord): WorkflowStepRecord {
-  return record as WorkflowStepRecord;
-}
-
-function toWorkflowRunRecord(record: PluginEntityRecord): WorkflowRunRecord {
-  return record as WorkflowRunRecord;
-}
-
-function toWorkflowDefinitionRecord(record: PluginEntityRecord): WorkflowDefinitionRecord {
-  return record as WorkflowDefinitionRecord;
 }
 
 function buildIdempotencyKey(event: PluginEvent): string {
@@ -78,10 +66,6 @@
 
 function getStepMetadata(stepDef: WorkflowStep): WorkflowStepMetadata {
   return stepDef as WorkflowStepMetadata;
-}
-
-function findStepDefinition(definition: WorkflowDefinitionRecord, stepId: string): WorkflowStep | null {
-  return definition.data.steps.find((step: WorkflowStep) => step.id === stepId) ?? null;
 }
 
 function getStepDescription(stepDef: WorkflowStep): string | undefined {
@@ -89,19 +73,6 @@
   return typeof description === "string" && description.trim() ? description.trim() : undefined;
 }
 
-function getStepAgentNameHint(stepDef: WorkflowStep): string | null {
-  const meta = getStepMetadata(stepDef);
-  const candidates = [meta.agentName, meta.agent, meta.assigneeAgentName];
-
-  for (const candidate of candidates) {
-    if (typeof candidate === "string" && candidate.trim()) {
-      return candidate.trim();
-    }
-  }
-
-  return null;
-}
-
 function getStepAgentIdHint(stepDef: WorkflowStep): string | null {
   const assigneeAgentId = getStepMetadata(stepDef).assigneeAgentId;
   if (typeof assigneeAgentId !== "string" || !assigneeAgentId.trim()) {
@@ -147,68 +118,256 @@
 
 async function invokeAgentForStep(
   ctx: PluginContext,
+  stepRunRecord: WorkflowStepRunRecord,
   stepDef: WorkflowStep,
-  agentName: string,
   workflowName: string,
   companyId: string,
-): Promise<void> {
+): Promise<WorkflowStepRunRecord> {
+  const agentName = getStepAgentName(stepRunRecord, stepDef);
+  if (!agentName) {
+    ctx.logger.warn("Agent name is missing for workflow step", {
+      companyId,
+      runId: stepRunRecord.data.runId,
+      stepId: stepDef.id,
+      workflowName,
+    });
+    return stepRunRecord;
+  }
+
   const agents = await ctx.agents.list({ companyId });
-  const agent = agents.find((candidate: AgentRecord) => candidate.name === agentName);
+  const agent = agents.find((candidate: AgentRecord) => candidate.name === agentName) ?? null;
 
   if (!agent) {
     ctx.logger.warn("Agent not found for step", {
       agentName,
       companyId,
+      runId: stepRunRecord.data.runId,
+      stepId: stepDef.id,
+      workflowName,
+    });
+    return stepRunRecord;
+  }
+
+  const prompt = `workflow:${workflowName}/step:${stepDef.id} — "${stepDef.title}" is ready. Please proceed with the assigned task.`;
+  const reason = `workflow:${workflowName}/step:${stepDef.id}`;
+  const sessionMode = getStepMetadata(stepDef).sessionMode === "reuse" ? "reuse" : "fresh";
+
+  if (sessionMode === "reuse") {
+    let sessionId = typeof stepRunRecord.data.sessionId === "string" && stepRunRecord.data.sessionId.trim()
+      ? stepRunRecord.data.sessionId.trim()
+      : "";
+
+    if (!sessionId) {
+      const session = await ctx.agents.sessions.create(agent.id, companyId, {
+        reason,
+      });
+      sessionId = session.sessionId;
+      stepRunRecord = toWorkflowStepRunRecord(await updateStepRun(ctx, stepRunRecord.id, {
+        sessionId,
+      }));
+    }
+
+    await ctx.agents.sessions.sendMessage(sessionId, companyId, {
+      prompt,
+      reason,
+    });
+
+    ctx.logger.info("Sent workflow step prompt via agent session", {
+      agentId: agent.id,
+      agentName,
+      companyId,
+      runId: stepRunRecord.data.runId,
+      sessionId,
       stepId: stepDef.id,
       workflowName,
     });
-    return;
+    return stepRunRecord;
   }
 
   await ctx.agents.invoke(agent.id, companyId, {
-    prompt: `workflow:${workflowName}/step:${stepDef.id} — "${stepDef.title}" is ready. Please proceed with the assigned task.`,
-    reason: `workflow:${workflowName}/step:${stepDef.id}`,
+    prompt,
+    reason,
   });
 
   ctx.logger.info("Invoked agent for workflow step", {
     agentId: agent.id,
     agentName,
     companyId,
+    runId: stepRunRecord.data.runId,
     stepId: stepDef.id,
     workflowName,
   });
+
+  return stepRunRecord;
 }
 
 async function activateBacklogStep(
   ctx: PluginContext,
-  stepRunRecord: WorkflowStepRecord,
+  stepRunRecord: WorkflowStepRunRecord,
   stepDef: WorkflowStep,
   workflowName: string,
   companyId: string,
-): Promise<void> {
+  options?: { parentIssueId?: string },
+): Promise<WorkflowStepRunRecord> {
   if (stepRunRecord.data.status !== STEP_STATUSES.backlog) {
-    return;
+    return stepRunRecord;
   }
 
-  const updated = toStepRunRecord(await updateStepRun(ctx, stepRunRecord.id, {
+  const resolvedAgent = await resolveStepAgent(
+    ctx,
+    companyId,
+    stepDef,
+    stepRunRecord.data.agentName,
+  );
+
+  let issueId = typeof stepRunRecord.data.issueId === "string" && stepRunRecord.data.issueId.trim()
+    ? stepRunRecord.data.issueId.trim()
+    : "";
+  const stepDescription = getStepDescription(stepDef) ?? `Workflow step: ${stepDef.id}`;
+
+  if (!issueId) {
+    const issue = await ctx.issues.create({
+      assigneeAgentId: resolvedAgent.agentId ?? undefined,
+      companyId,
+      description: stepDescription,
+      parentId: options?.parentIssueId,
+      title: `[${workflowName}] ${stepDef.title}`,
+    });
+    await ctx.issues.update(issue.id, { status: "todo" } as IssueUpdatePatch, companyId);
+    issueId = issue.id;
+  }
+
+  const nextStartedAt = stepRunRecord.data.startedAt ?? new Date().toISOString();
+  let updatedStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, stepRunRecord.id, {
+    agentName: resolvedAgent.agentName ?? stepRunRecord.data.agentName,
+    issueId,
+    startedAt: nextStartedAt,
     status: STEP_STATUSES.todo,
   }));
 
-  await invokeAgentForStep(
+  updatedStepRun = await invokeAgentForStep(
     ctx,
+    updatedStepRun,
     stepDef,
-    updated.data.agentName,
     workflowName,
     companyId,
   );
+
+  return updatedStepRun;
 }
 
+async function startWorkflow(
+  ctx: PluginContext,
+  workflowId: string,
+  companyId: string,
+  options?: {
+    createParentIssue?: boolean;
+    parentIssueId?: string;
+  },
+): Promise<{
+  activatedStepIds: string[];
+  parentIssueId: string | null;
+  runId: string;
+  workflowId: string;
+}> {
+  const workflowDefinition = await getWorkflowDefinition(ctx, workflowId);
+  if (!workflowDefinition) {
+    throw new Error(`Workflow definition not found: ${workflowId}`);
+  }
+
+  const typedWorkflowDefinition = toWorkflowDefinitionRecord(workflowDefinition);
+  if (typedWorkflowDefinition.data.companyId !== companyId) {
+    throw new Error(`Workflow does not belong to company: ${workflowId}`);
+  }
+
+  let parentIssueId = typeof options?.parentIssueId === "string" && options.parentIssueId.trim()
+    ? options.parentIssueId.trim()
+    : "";
+  if (!parentIssueId && options?.createParentIssue) {
+    const parentIssue = await ctx.issues.create({
+      companyId,
+      description: typedWorkflowDefinition.data.description || `Workflow run: ${typedWorkflowDefinition.data.name}`,
+      title: `[${typedWorkflowDefinition.data.name}] Workflow run`,
+    });
+    parentIssueId = parentIssue.id;
+  }
+
+  const workflowRun = toWorkflowRunRecord(await createWorkflowRun(ctx, {
+    companyId,
+    parentIssueId: parentIssueId || undefined,
+    startedAt: new Date().toISOString(),
+    status: RUN_STATUSES.running,
+    workflowId: typedWorkflowDefinition.id,
+    workflowName: typedWorkflowDefinition.data.name,
+  }));
+
+  const agents = await ctx.agents.list({ companyId });
+  const agentsByName = new Map<string, AgentRecord>();
+  for (const agent of agents) {
+    agentsByName.set(agent.name, agent);
+  }
+
+  const pendingRootSteps: Array<{ stepDef: WorkflowStep; stepRun: WorkflowStepRunRecord }> = [];
+  for (const stepDef of typedWorkflowDefinition.data.steps) {
+    const agentNameHint = getStepAgentNameHint(stepDef);
+    const matchedAgent = agentNameHint ? agentsByName.get(agentNameHint) ?? null : null;
+    const resolvedAgent = matchedAgent
+      ? { agentId: matchedAgent.id, agentName: matchedAgent.name }
+      : await resolveStepAgent(ctx, companyId, stepDef, agentNameHint ?? undefined);
+
+    if (!resolvedAgent.agentName) {
+      throw new Error(`Unable to resolve step assignee for "${stepDef.id}"`);
+    }
+
+    const stepRun = toWorkflowStepRunRecord(await createStepRun(ctx, companyId, {
+      agentName: resolvedAgent.agentName,
+      retryCount: 0,
+      runId: workflowRun.id,
+      status: STEP_STATUSES.backlog,
+      stepId: stepDef.id,
+    }));
+
+    if (stepDef.dependsOn.length === 0 && stepDef.triggerOn !== "escalation") {
+      pendingRootSteps.push({ stepDef, stepRun });
+    }
+  }
+
+  const activatedStepIds: string[] = [];
+  for (const pending of pendingRootSteps) {
+    await activateBacklogStep(
+      ctx,
+      pending.stepRun,
+      pending.stepDef,
+      typedWorkflowDefinition.data.name,
+      companyId,
+      { parentIssueId: parentIssueId || undefined },
+    );
+    activatedStepIds.push(pending.stepDef.id);
+  }
+
+  ctx.logger.info("Started workflow run", {
+    activatedStepIds,
+    companyId,
+    parentIssueId: parentIssueId || null,
+    runId: workflowRun.id,
+    workflowId: typedWorkflowDefinition.id,
+    workflowName: typedWorkflowDefinition.data.name,
+  });
+
+  return {
+    activatedStepIds,
+    parentIssueId: parentIssueId || null,
+    runId: workflowRun.id,
+    workflowId: typedWorkflowDefinition.id,
+  };
+}
+
 async function advanceWorkflow(
   ctx: PluginContext,
   stepRunRecord: PluginEntityRecord,
   companyId: string,
 ): Promise<void> {
-  const completedStepRun = toStepRunRecord(stepRunRecord);
+  const completedStepRun = toWorkflowStepRunRecord(stepRunRecord);
   const workflowRun = await getWorkflowRun(ctx, completedStepRun.data.runId);
 
   if (!workflowRun) {
@@ -237,7 +396,7 @@
   }
 
   const typedWorkflowDefinition = toWorkflowDefinitionRecord(workflowDefinition);
-  const stepRuns = (await listStepRuns(ctx, typedWorkflowRun.id, companyId)).map(toStepRunRecord);
+  const stepRuns = (await listStepRuns(ctx, typedWorkflowRun.id, companyId)).map(toWorkflowStepRunRecord);
   const completed = new Set<string>();
   const failed = new Set<string>();
   const skipped = new Set<string>();
@@ -289,7 +448,7 @@
         continue;
       }
 
-      stepRun = toStepRunRecord(await createStepRun(ctx, companyId, {
+      stepRun = toWorkflowStepRunRecord(await createStepRun(ctx, companyId, {
         agentName: resolvedAgent.agentName,
         retryCount: 0,
         runId: typedWorkflowRun.id,
@@ -305,6 +464,7 @@
       stepDef,
       typedWorkflowRun.data.workflowName,
       companyId,
+      { parentIssueId: typedWorkflowRun.data.parentIssueId },
     );
   }
 
@@ -329,7 +489,7 @@
   ctx: PluginContext,
   workflowRun: WorkflowRunRecord,
   workflowDefinition: WorkflowDefinitionRecord,
-  sourceStepRun: WorkflowStepRecord,
+  sourceStepRun: WorkflowStepRunRecord,
   escalationTargetId: string,
   companyId: string,
 ): Promise<void> {
@@ -338,7 +498,7 @@
     throw new Error(`Escalation target step not found: ${escalationTargetId}`);
   }
 
-  const stepRuns = (await listStepRuns(ctx, workflowRun.id, companyId)).map(toStepRunRecord);
+  const stepRuns = (await listStepRuns(ctx, workflowRun.id, companyId)).map(toWorkflowStepRunRecord);
   let escalationStepRun = stepRuns.find((candidate) => candidate.data.stepId === escalationTargetId) ?? null;
   const resolvedAgent = await resolveStepAgent(
     ctx,
@@ -352,7 +512,7 @@
   }
 
   if (!escalationStepRun) {
-    escalationStepRun = toStepRunRecord(await createStepRun(ctx, companyId, {
+    escalationStepRun = toWorkflowStepRunRecord(await createStepRun(ctx, companyId, {
       agentName: resolvedAgent.agentName,
       retryCount: 0,
       runId: workflowRun.id,
@@ -374,14 +534,14 @@
       title: `${workflowRun.data.workflowName}: ${escalationStep.title}`,
     });
 
-    escalationStepRun = toStepRunRecord(await updateStepRun(ctx, escalationStepRun.id, {
+    escalationStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, escalationStepRun.id, {
       issueId: issue.id,
     }));
   }
 
   const shouldActivate = escalationStepRun.data.status === STEP_STATUSES.backlog;
   if (shouldActivate) {
-    escalationStepRun = toStepRunRecord(await updateStepRun(ctx, escalationStepRun.id, {
+    escalationStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, escalationStepRun.id, {
       status: STEP_STATUSES.todo,
     }));
   }
@@ -389,8 +549,8 @@
   if (shouldActivate) {
     await invokeAgentForStep(
       ctx,
+      escalationStepRun,
       escalationStep,
-      escalationStepRun.data.agentName,
       workflowRun.data.workflowName,
       companyId,
     );
@@ -421,7 +581,7 @@
     return;
   }
 
-  const typedStepRun = toStepRunRecord(stepRunRecord);
+  const typedStepRun = toWorkflowStepRunRecord(stepRunRecord);
   if (TERMINAL_STEP_STATUSES.has(typedStepRun.data.status)) {
     return;
   }
@@ -469,7 +629,7 @@
   if (options.allowRetry) {
     const retryInfo = getRetryInfo(typedWorkflowDefinition.data.steps, typedStepRun.data.stepId);
     if (retryInfo.shouldRetry && typedStepRun.data.retryCount < retryInfo.maxRetries) {
-      const retriedStepRun = toStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
+      const retriedStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
         completedAt: undefined,
         retryCount: typedStepRun.data.retryCount + 1,
         status: STEP_STATUSES.todo,
@@ -477,8 +637,8 @@
 
       await invokeAgentForStep(
         ctx,
+        retriedStepRun,
         stepDef,
-        retriedStepRun.data.agentName,
         typedWorkflowRun.data.workflowName,
         event.companyId,
       );
@@ -496,7 +656,7 @@
   }
 
   if (stepDef.onFailure === "skip") {
-    const skippedStepRun = toStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
+    const skippedStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
       completedAt: new Date().toISOString(),
       status: STEP_STATUSES.skipped,
     }));
@@ -556,7 +716,7 @@
       return;
     }
 
-    const escalatedStepRun = toStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
+    const escalatedStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
       completedAt: new Date().toISOString(),
       status: STEP_STATUSES.escalated,
     }));
@@ -617,6 +777,29 @@
   }
 }
 
+function registerDataHandler(
+  ctx: PluginContext,
+  key: string,
+  handler: (params: Record<string, unknown>) => Promise<unknown>,
+): void {
+  const dataClient = ctx.data as {
+    handle?: (handlerKey: string, handlerFn: (params: Record<string, unknown>) => Promise<unknown>) => void;
+    register?: (handlerKey: string, handlerFn: (params: Record<string, unknown>) => Promise<unknown>) => void;
+  };
+
+  if (typeof dataClient.handle === "function") {
+    dataClient.handle(key, handler);
+    return;
+  }
+
+  if (typeof dataClient.register === "function") {
+    dataClient.register(key, handler);
+    return;
+  }
+
+  throw new Error("Plugin data client does not support handler registration");
+}
+
 const plugin = definePlugin({
   async setup(ctx: PluginContext) {
     ctx.events.on("issue.updated", async (event: PluginEvent) => {
@@ -638,7 +821,7 @@
           return;
         }
 
-        const stepRun = toStepRunRecord(stepRunRecord);
+        const stepRun = toWorkflowStepRunRecord(stepRunRecord);
         const payload = event.payload as { status?: string };
         const issueStatus = typeof payload.status === "string" ? payload.status : undefined;
 
@@ -657,7 +840,7 @@
           return;
         }
 
-        const updatedStepRun = toStepRunRecord(await updateStepRun(ctx, stepRun.id, {
+        const updatedStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, stepRun.id, {
           completedAt: nextStepStatus === STEP_STATUSES.done ? new Date().toISOString() : undefined,
           startedAt: nextStepStatus === STEP_STATUSES.inProgress
             ? stepRun.data.startedAt ?? new Date().toISOString()
@@ -687,11 +870,21 @@
 
     ctx.events.on("issue.created", async (event: PluginEvent) => {
       try {
+        const idempotencyKey = buildIdempotencyKey(event);
+        if (await checkIdempotency(ctx, idempotencyKey, event.companyId)) {
+          return;
+        }
+
         const payload = event.payload as {
           id?: string;
           parentId?: string;
           assigneeAgentId?: string;
         };
+        const issueId = typeof event.entityId === "string" && event.entityId.trim()
+          ? event.entityId.trim()
+          : typeof payload.id === "string" && payload.id.trim()
+            ? payload.id.trim()
+            : "";
 
         const parentId = typeof payload.parentId === "string" && payload.parentId.trim()
           ? payload.parentId.trim()
@@ -700,12 +893,34 @@
           ? payload.assigneeAgentId.trim()
           : "";
 
-        if (parentId || !assigneeAgentId) {
+        if (!issueId || parentId || !assigneeAgentId) {
+          await markIdempotency(ctx, idempotencyKey, event.companyId);
+          return;
+        }
+
+        const agent = await ctx.agents.get(assigneeAgentId, event.companyId);
+        const metadata = agent?.metadata as Record<string, unknown> | undefined;
+        const defaultParentIssueId = typeof metadata?.defaultParentIssueId === "string" && metadata.defaultParentIssueId.trim()
+          ? metadata.defaultParentIssueId.trim()
+          : "";
+
+        if (!defaultParentIssueId) {
+          await markIdempotency(ctx, idempotencyKey, event.companyId);
           return;
         }
 
-        ctx.logger.info("parentId filler: issue created without parent", {
-          issueId: event.entityId ?? payload.id ?? null,
+        await ctx.issues.update(
+          issueId,
+          { parentId: defaultParentIssueId } as IssueUpdatePatch,
+          event.companyId,
+        );
+        await markIdempotency(ctx, idempotencyKey, event.companyId);
+
+        ctx.logger.info("parentId filler: populated issue parentId from agent metadata", {
+          assigneeAgentId,
+          companyId: event.companyId,
+          issueId,
+          parentId: defaultParentIssueId,
         });
       } catch (error) {
         ctx.logger.warn("Failed to handle issue.created event", {
@@ -744,7 +959,23 @@
       await runReconciler(ctx);
     });
 
-    ctx.data.register("workflow-overview", async (params: Record<string, unknown>) => {
+    registerDataHandler(ctx, "start-workflow", async (params: Record<string, unknown>) => {
+      const workflowId = typeof params.workflowId === "string" ? params.workflowId.trim() : "";
+      const companyId = typeof params.companyId === "string" ? params.companyId.trim() : "";
+      const parentIssueId = typeof params.parentIssueId === "string" ? params.parentIssueId.trim() : "";
+      const createParentIssue = params.createParentIssue === true;
+
+      if (!workflowId || !companyId) {
+        throw new Error("start-workflow requires workflowId and companyId");
+      }
+
+      return await startWorkflow(ctx, workflowId, companyId, {
+        createParentIssue,
+        parentIssueId: parentIssueId || undefined,
+      });
+    });
+
+    registerDataHandler(ctx, "workflow-overview", async (params: Record<string, unknown>) => {
       try {
         const companyId = typeof params.companyId === "string" ? params.companyId.trim() : "";
         if (!companyId) {
@@ -782,7 +1013,7 @@
       }
     });
 
-    ctx.data.register("workflow-run-detail", async (params: Record<string, unknown>) => {
+    registerDataHandler(ctx, "workflow-run-detail", async (params: Record<string, unknown>) => {
       try {
         const runId = typeof params.runId === "string" ? params.runId.trim() : "";
         if (!runId) {
@@ -807,7 +1038,7 @@
             ...typedWorkflowRun.data,
           },
           stepRuns: stepRuns.map((record: PluginEntityRecord) => {
-            const stepRun = toStepRunRecord(record);
+            const stepRun = toWorkflowStepRunRecord(record);
             return {
               id: stepRun.id,
               status: stepRun.status,
diff --git a/paperclip-addon/plugins/workflow-engine/src/workflow-store.ts b/paperclip-addon/plugins/workflow-engine/src/workflow-store.ts
index 5871a2082258cebcfe8b69ebafbb0814cf303d50..82eb46836883940e109368e927b7e367c96616b5
--- a/paperclip-addon/plugins/workflow-engine/src/workflow-store.ts
+++ b/paperclip-addon/plugins/workflow-engine/src/workflow-store.ts
@@ -44,7 +44,14 @@
 
 type TypedEntityRecord<T> = Omit<PluginEntityRecord, "data"> & { data: T };
 
-function toTypedRecord<T>(record: PluginEntityRecord): TypedEntityRecord<T> {
+function toTypedRecord<T>(
+  record: PluginEntityRecord,
+  entityType: string,
+): TypedEntityRecord<T> {
+  if (record.entityType !== entityType) {
+    throw new Error(`Expected entity type "${entityType}", got "${record.entityType}"`);
+  }
+
   return record as TypedEntityRecord<T>;
 }
 
@@ -67,13 +74,31 @@
   id: string,
   entityType: string,
 ): Promise<TypedEntityRecord<T> | null> {
-  const record = await ctx.entities.get(id);
+  const listQuery = {
+    entityType,
+    id,
+    limit: 1,
+  } as Record<string, unknown>;
 
-  if (!record || record.entityType !== entityType) {
+  const listed = await ctx.entities.list(
+    listQuery as Parameters<PluginContext["entities"]["list"]>[0],
+  );
+  const listedRecord = listed.find(
+    (record: PluginEntityRecord) => record.id === id,
+  ) ?? null;
+
+  if (listedRecord) {
+    return toTypedRecord<T>(listedRecord, entityType);
+  }
+
+  // Fallback: some host SDK variants ignore `id` on entities.list().
+  // Keep defensive type verification on direct get() until id-filter support is universal.
+  const record = await ctx.entities.get(id);
+  if (!record) {
     return null;
   }
 
-  return toTypedRecord<T>(record);
+  return toTypedRecord<T>(record, entityType);
 }
 
 async function requireEntityByType<T>(
diff --git a/paperclip-addon/plugins/workflow-engine/src/workflow-utils.ts b/paperclip-addon/plugins/workflow-engine/src/workflow-utils.ts
new file mode 100644
index 0000000000000000000000000000000000000000..ec1cf6f6b9aa722b126b58c0a728b4e538ad1494
--- /dev/null
+++ b/paperclip-addon/plugins/workflow-engine/src/workflow-utils.ts
@@ -0,0 +1,62 @@
+import type { PluginEntityRecord } from "@paperclipai/plugin-sdk";
+
+import { STEP_STATUSES } from "./constants.js";
+import type { WorkflowStep } from "./dag-engine.js";
+import type {
+  WorkflowDefinition,
+  WorkflowRun,
+  WorkflowStepRun,
+} from "./workflow-store.js";
+
+export type WorkflowRunRecord = PluginEntityRecord & { data: WorkflowRun };
+export type WorkflowStepRunRecord = PluginEntityRecord & { data: WorkflowStepRun };
+export type WorkflowDefinitionRecord = PluginEntityRecord & { data: WorkflowDefinition };
+type StepAgentMetadata = WorkflowStep & {
+  agent?: string;
+  agentName?: string;
+  assigneeAgentName?: string;
+};
+
+export const TERMINAL_STEP_STATUSES = new Set<string>([
+  STEP_STATUSES.done,
+  STEP_STATUSES.failed,
+  STEP_STATUSES.skipped,
+  STEP_STATUSES.escalated,
+]);
+
+export function toWorkflowRunRecord(record: PluginEntityRecord): WorkflowRunRecord {
+  return record as WorkflowRunRecord;
+}
+
+export function toWorkflowStepRunRecord(record: PluginEntityRecord): WorkflowStepRunRecord {
+  return record as WorkflowStepRunRecord;
+}
+
+export function toWorkflowDefinitionRecord(record: PluginEntityRecord): WorkflowDefinitionRecord {
+  return record as WorkflowDefinitionRecord;
+}
+
+export function findStepDefinition(definition: WorkflowDefinitionRecord, stepId: string): WorkflowStep | null {
+  return definition.data.steps.find((step: WorkflowStep) => step.id === stepId) ?? null;
+}
+
+export function getStepAgentNameHint(stepDef: WorkflowStep): string | null {
+  const stepMeta = stepDef as StepAgentMetadata;
+  const candidates = [stepMeta.agentName, stepMeta.agent, stepMeta.assigneeAgentName];
+
+  for (const candidate of candidates) {
+    if (typeof candidate === "string" && candidate.trim()) {
+      return candidate.trim();
+    }
+  }
+
+  return null;
+}
+
+export function getStepAgentName(stepRun: WorkflowStepRunRecord, stepDef: WorkflowStep): string | null {
+  if (typeof stepRun.data.agentName === "string" && stepRun.data.agentName.trim()) {
+    return stepRun.data.agentName.trim();
+  }
+
+  return getStepAgentNameHint(stepDef);
+}

file update
M /Users/kwak/Projects/paperclip/paperclip-addon/plugins/workflow-engine/src/workflow-store.ts
@@ -90,2 +90,6 @@
   if (listedRecord) {
+    if (listedRecord.entityType !== entityType) {
+      return null;
+    }
+
     return toTypedRecord<T>(listedRecord, entityType);
@@ -100,2 +104,6 @@
 
+  if (record.entityType !== entityType) {
+    return null;
+  }
+
   return toTypedRecord<T>(record, entityType);
apply_patch(auto_approved=true) exited 0 in 120ms:
Success. Updated the following files:
M /Users/kwak/Projects/paperclip/paperclip-addon/plugins/workflow-engine/src/workflow-store.ts
file update:
diff --git a/paperclip-addon/plugins/workflow-engine/src/dag-engine.ts b/paperclip-addon/plugins/workflow-engine/src/dag-engine.ts
index b59510f0473171038be77277286d3b5c2063e6c5..e7f21ff998cab78b9325a8aeacf4fda9c5e06553
--- a/paperclip-addon/plugins/workflow-engine/src/dag-engine.ts
+++ b/paperclip-addon/plugins/workflow-engine/src/dag-engine.ts
@@ -2,6 +2,7 @@
   id: string;
   title: string;
   dependsOn: string[];
+  sessionMode?: "fresh" | "reuse";
   onFailure?: "retry" | "skip" | "abort_workflow" | "escalate";
   escalateTo?: string;
   maxRetries?: number;
diff --git a/paperclip-addon/plugins/workflow-engine/src/reconciler.ts b/paperclip-addon/plugins/workflow-engine/src/reconciler.ts
index 14ab4e766a835d145a6c2e7b1b564270285cbdc3..55b649037e8a19a88bb39b9633a6a0cc84fa8d59
--- a/paperclip-addon/plugins/workflow-engine/src/reconciler.ts
+++ b/paperclip-addon/plugins/workflow-engine/src/reconciler.ts
@@ -8,65 +8,21 @@
   listStepRuns,
   updateStepRun,
   updateWorkflowRun,
-  type WorkflowDefinition,
-  type WorkflowRun,
-  type WorkflowStepRun,
 } from "./workflow-store.js";
-import type { WorkflowStep } from "./dag-engine.js";
-
-type WorkflowRunRecord = PluginEntityRecord & { data: WorkflowRun };
-type WorkflowStepRunRecord = PluginEntityRecord & { data: WorkflowStepRun };
-type WorkflowDefinitionRecord = PluginEntityRecord & { data: WorkflowDefinition };
-type StepAgentMetadata = WorkflowStep & {
-  agent?: string;
-  agentName?: string;
-  assigneeAgentName?: string;
-};
+import {
+  TERMINAL_STEP_STATUSES,
+  findStepDefinition,
+  getStepAgentName,
+  toWorkflowDefinitionRecord,
+  toWorkflowRunRecord,
+  toWorkflowStepRunRecord,
+} from "./workflow-utils.js";
 
 const STALE_THRESHOLD_MS = 300_000;
-const TERMINAL_STEP_STATUSES = new Set<string>([
-  STEP_STATUSES.done,
-  STEP_STATUSES.failed,
-  STEP_STATUSES.skipped,
-  STEP_STATUSES.escalated,
-]);
 
 function isStale(record: PluginEntityRecord, thresholdMs: number): boolean {
   const updatedAt = new Date(record.updatedAt).getTime();
   return Date.now() - updatedAt > thresholdMs;
-}
-
-function toWorkflowRunRecord(record: PluginEntityRecord): WorkflowRunRecord {
-  return record as WorkflowRunRecord;
-}
-
-function toWorkflowStepRunRecord(record: PluginEntityRecord): WorkflowStepRunRecord {
-  return record as WorkflowStepRunRecord;
-}
-
-function toWorkflowDefinitionRecord(record: PluginEntityRecord): WorkflowDefinitionRecord {
-  return record as WorkflowDefinitionRecord;
-}
-
-function findStepDefinition(definition: WorkflowDefinitionRecord, stepId: string): WorkflowStep | null {
-  return definition.data.steps.find((step: WorkflowStep) => step.id === stepId) ?? null;
-}
-
-function getStepAgentName(stepRun: WorkflowStepRunRecord, stepDef: WorkflowStep): string | null {
-  if (typeof stepRun.data.agentName === "string" && stepRun.data.agentName.trim()) {
-    return stepRun.data.agentName.trim();
-  }
-
-  const stepMeta = stepDef as StepAgentMetadata;
-  const candidates = [stepMeta.agentName, stepMeta.agent, stepMeta.assigneeAgentName];
-
-  for (const candidate of candidates) {
-    if (typeof candidate === "string" && candidate.trim()) {
-      return candidate.trim();
-    }
-  }
-
-  return null;
 }
 
 async function invokeAgentByName(
diff --git a/paperclip-addon/plugins/workflow-engine/src/worker.ts b/paperclip-addon/plugins/workflow-engine/src/worker.ts
index c5ac3fbb4ca7f767eb29331bd3630b635449b335..303e35958890fa1f66db3f2b0ff1fa1f134102a8
--- a/paperclip-addon/plugins/workflow-engine/src/worker.ts
+++ b/paperclip-addon/plugins/workflow-engine/src/worker.ts
@@ -15,6 +15,7 @@
 } from "./dag-engine.js";
 import {
   checkIdempotency,
+  createWorkflowRun,
   createStepRun,
   findStepRunByIssueId,
   getWorkflowDefinition,
@@ -25,51 +26,38 @@
   markIdempotency,
   updateStepRun,
   updateWorkflowRun,
-  type WorkflowDefinition,
-  type WorkflowRun,
   type WorkflowStepRun,
 } from "./workflow-store.js";
+import {
+  TERMINAL_STEP_STATUSES,
+  findStepDefinition,
+  getStepAgentName,
+  getStepAgentNameHint,
+  toWorkflowDefinitionRecord,
+  toWorkflowRunRecord,
+  toWorkflowStepRunRecord,
+  type WorkflowDefinitionRecord,
+  type WorkflowRunRecord,
+  type WorkflowStepRunRecord,
+} from "./workflow-utils.js";
 
-type WorkflowStepRecord = PluginEntityRecord & { data: WorkflowStepRun };
-type WorkflowRunRecord = PluginEntityRecord & { data: WorkflowRun };
-type WorkflowDefinitionRecord = PluginEntityRecord & { data: WorkflowDefinition };
 type WorkflowStepMetadata = WorkflowStep & {
   description?: string;
-  agent?: string;
-  agentName?: string;
-  assigneeAgentName?: string;
   assigneeAgentId?: string;
+  sessionMode?: "fresh" | "reuse";
 };
 type AgentRecord = Awaited<ReturnType<PluginContext["agents"]["list"]>>[number];
+type IssueUpdatePatch = Parameters<PluginContext["issues"]["update"]>[1];
 type ReconcilerModule = {
   reconcileStuckSteps?: (ctx: PluginContext) => Promise<void>;
 };
 
-const TERMINAL_STEP_STATUSES = new Set<string>([
-  STEP_STATUSES.done,
-  STEP_STATUSES.failed,
-  STEP_STATUSES.skipped,
-  STEP_STATUSES.escalated,
-]);
-
 function summarizeError(error: unknown): string {
   if (error instanceof Error) {
     return error.message;
   }
 
   return String(error);
-}
-
-function toStepRunRecord(record: PluginEntityRecord): WorkflowStepRecord {
-  return record as WorkflowStepRecord;
-}
-
-function toWorkflowRunRecord(record: PluginEntityRecord): WorkflowRunRecord {
-  return record as WorkflowRunRecord;
-}
-
-function toWorkflowDefinitionRecord(record: PluginEntityRecord): WorkflowDefinitionRecord {
-  return record as WorkflowDefinitionRecord;
 }
 
 function buildIdempotencyKey(event: PluginEvent): string {
@@ -78,10 +66,6 @@
 
 function getStepMetadata(stepDef: WorkflowStep): WorkflowStepMetadata {
   return stepDef as WorkflowStepMetadata;
-}
-
-function findStepDefinition(definition: WorkflowDefinitionRecord, stepId: string): WorkflowStep | null {
-  return definition.data.steps.find((step: WorkflowStep) => step.id === stepId) ?? null;
 }
 
 function getStepDescription(stepDef: WorkflowStep): string | undefined {
@@ -89,19 +73,6 @@
   return typeof description === "string" && description.trim() ? description.trim() : undefined;
 }
 
-function getStepAgentNameHint(stepDef: WorkflowStep): string | null {
-  const meta = getStepMetadata(stepDef);
-  const candidates = [meta.agentName, meta.agent, meta.assigneeAgentName];
-
-  for (const candidate of candidates) {
-    if (typeof candidate === "string" && candidate.trim()) {
-      return candidate.trim();
-    }
-  }
-
-  return null;
-}
-
 function getStepAgentIdHint(stepDef: WorkflowStep): string | null {
   const assigneeAgentId = getStepMetadata(stepDef).assigneeAgentId;
   if (typeof assigneeAgentId !== "string" || !assigneeAgentId.trim()) {
@@ -147,68 +118,256 @@
 
 async function invokeAgentForStep(
   ctx: PluginContext,
+  stepRunRecord: WorkflowStepRunRecord,
   stepDef: WorkflowStep,
-  agentName: string,
   workflowName: string,
   companyId: string,
-): Promise<void> {
+): Promise<WorkflowStepRunRecord> {
+  const agentName = getStepAgentName(stepRunRecord, stepDef);
+  if (!agentName) {
+    ctx.logger.warn("Agent name is missing for workflow step", {
+      companyId,
+      runId: stepRunRecord.data.runId,
+      stepId: stepDef.id,
+      workflowName,
+    });
+    return stepRunRecord;
+  }
+
   const agents = await ctx.agents.list({ companyId });
-  const agent = agents.find((candidate: AgentRecord) => candidate.name === agentName);
+  const agent = agents.find((candidate: AgentRecord) => candidate.name === agentName) ?? null;
 
   if (!agent) {
     ctx.logger.warn("Agent not found for step", {
       agentName,
       companyId,
+      runId: stepRunRecord.data.runId,
+      stepId: stepDef.id,
+      workflowName,
+    });
+    return stepRunRecord;
+  }
+
+  const prompt = `workflow:${workflowName}/step:${stepDef.id} — "${stepDef.title}" is ready. Please proceed with the assigned task.`;
+  const reason = `workflow:${workflowName}/step:${stepDef.id}`;
+  const sessionMode = getStepMetadata(stepDef).sessionMode === "reuse" ? "reuse" : "fresh";
+
+  if (sessionMode === "reuse") {
+    let sessionId = typeof stepRunRecord.data.sessionId === "string" && stepRunRecord.data.sessionId.trim()
+      ? stepRunRecord.data.sessionId.trim()
+      : "";
+
+    if (!sessionId) {
+      const session = await ctx.agents.sessions.create(agent.id, companyId, {
+        reason,
+      });
+      sessionId = session.sessionId;
+      stepRunRecord = toWorkflowStepRunRecord(await updateStepRun(ctx, stepRunRecord.id, {
+        sessionId,
+      }));
+    }
+
+    await ctx.agents.sessions.sendMessage(sessionId, companyId, {
+      prompt,
+      reason,
+    });
+
+    ctx.logger.info("Sent workflow step prompt via agent session", {
+      agentId: agent.id,
+      agentName,
+      companyId,
+      runId: stepRunRecord.data.runId,
+      sessionId,
       stepId: stepDef.id,
       workflowName,
     });
-    return;
+    return stepRunRecord;
   }
 
   await ctx.agents.invoke(agent.id, companyId, {
-    prompt: `workflow:${workflowName}/step:${stepDef.id} — "${stepDef.title}" is ready. Please proceed with the assigned task.`,
-    reason: `workflow:${workflowName}/step:${stepDef.id}`,
+    prompt,
+    reason,
   });
 
   ctx.logger.info("Invoked agent for workflow step", {
     agentId: agent.id,
     agentName,
     companyId,
+    runId: stepRunRecord.data.runId,
     stepId: stepDef.id,
     workflowName,
   });
+
+  return stepRunRecord;
 }
 
 async function activateBacklogStep(
   ctx: PluginContext,
-  stepRunRecord: WorkflowStepRecord,
+  stepRunRecord: WorkflowStepRunRecord,
   stepDef: WorkflowStep,
   workflowName: string,
   companyId: string,
-): Promise<void> {
+  options?: { parentIssueId?: string },
+): Promise<WorkflowStepRunRecord> {
   if (stepRunRecord.data.status !== STEP_STATUSES.backlog) {
-    return;
+    return stepRunRecord;
   }
 
-  const updated = toStepRunRecord(await updateStepRun(ctx, stepRunRecord.id, {
+  const resolvedAgent = await resolveStepAgent(
+    ctx,
+    companyId,
+    stepDef,
+    stepRunRecord.data.agentName,
+  );
+
+  let issueId = typeof stepRunRecord.data.issueId === "string" && stepRunRecord.data.issueId.trim()
+    ? stepRunRecord.data.issueId.trim()
+    : "";
+  const stepDescription = getStepDescription(stepDef) ?? `Workflow step: ${stepDef.id}`;
+
+  if (!issueId) {
+    const issue = await ctx.issues.create({
+      assigneeAgentId: resolvedAgent.agentId ?? undefined,
+      companyId,
+      description: stepDescription,
+      parentId: options?.parentIssueId,
+      title: `[${workflowName}] ${stepDef.title}`,
+    });
+    await ctx.issues.update(issue.id, { status: "todo" } as IssueUpdatePatch, companyId);
+    issueId = issue.id;
+  }
+
+  const nextStartedAt = stepRunRecord.data.startedAt ?? new Date().toISOString();
+  let updatedStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, stepRunRecord.id, {
+    agentName: resolvedAgent.agentName ?? stepRunRecord.data.agentName,
+    issueId,
+    startedAt: nextStartedAt,
     status: STEP_STATUSES.todo,
   }));
 
-  await invokeAgentForStep(
+  updatedStepRun = await invokeAgentForStep(
     ctx,
+    updatedStepRun,
     stepDef,
-    updated.data.agentName,
     workflowName,
     companyId,
   );
+
+  return updatedStepRun;
 }
 
+async function startWorkflow(
+  ctx: PluginContext,
+  workflowId: string,
+  companyId: string,
+  options?: {
+    createParentIssue?: boolean;
+    parentIssueId?: string;
+  },
+): Promise<{
+  activatedStepIds: string[];
+  parentIssueId: string | null;
+  runId: string;
+  workflowId: string;
+}> {
+  const workflowDefinition = await getWorkflowDefinition(ctx, workflowId);
+  if (!workflowDefinition) {
+    throw new Error(`Workflow definition not found: ${workflowId}`);
+  }
+
+  const typedWorkflowDefinition = toWorkflowDefinitionRecord(workflowDefinition);
+  if (typedWorkflowDefinition.data.companyId !== companyId) {
+    throw new Error(`Workflow does not belong to company: ${workflowId}`);
+  }
+
+  let parentIssueId = typeof options?.parentIssueId === "string" && options.parentIssueId.trim()
+    ? options.parentIssueId.trim()
+    : "";
+  if (!parentIssueId && options?.createParentIssue) {
+    const parentIssue = await ctx.issues.create({
+      companyId,
+      description: typedWorkflowDefinition.data.description || `Workflow run: ${typedWorkflowDefinition.data.name}`,
+      title: `[${typedWorkflowDefinition.data.name}] Workflow run`,
+    });
+    parentIssueId = parentIssue.id;
+  }
+
+  const workflowRun = toWorkflowRunRecord(await createWorkflowRun(ctx, {
+    companyId,
+    parentIssueId: parentIssueId || undefined,
+    startedAt: new Date().toISOString(),
+    status: RUN_STATUSES.running,
+    workflowId: typedWorkflowDefinition.id,
+    workflowName: typedWorkflowDefinition.data.name,
+  }));
+
+  const agents = await ctx.agents.list({ companyId });
+  const agentsByName = new Map<string, AgentRecord>();
+  for (const agent of agents) {
+    agentsByName.set(agent.name, agent);
+  }
+
+  const pendingRootSteps: Array<{ stepDef: WorkflowStep; stepRun: WorkflowStepRunRecord }> = [];
+  for (const stepDef of typedWorkflowDefinition.data.steps) {
+    const agentNameHint = getStepAgentNameHint(stepDef);
+    const matchedAgent = agentNameHint ? agentsByName.get(agentNameHint) ?? null : null;
+    const resolvedAgent = matchedAgent
+      ? { agentId: matchedAgent.id, agentName: matchedAgent.name }
+      : await resolveStepAgent(ctx, companyId, stepDef, agentNameHint ?? undefined);
+
+    if (!resolvedAgent.agentName) {
+      throw new Error(`Unable to resolve step assignee for "${stepDef.id}"`);
+    }
+
+    const stepRun = toWorkflowStepRunRecord(await createStepRun(ctx, companyId, {
+      agentName: resolvedAgent.agentName,
+      retryCount: 0,
+      runId: workflowRun.id,
+      status: STEP_STATUSES.backlog,
+      stepId: stepDef.id,
+    }));
+
+    if (stepDef.dependsOn.length === 0 && stepDef.triggerOn !== "escalation") {
+      pendingRootSteps.push({ stepDef, stepRun });
+    }
+  }
+
+  const activatedStepIds: string[] = [];
+  for (const pending of pendingRootSteps) {
+    await activateBacklogStep(
+      ctx,
+      pending.stepRun,
+      pending.stepDef,
+      typedWorkflowDefinition.data.name,
+      companyId,
+      { parentIssueId: parentIssueId || undefined },
+    );
+    activatedStepIds.push(pending.stepDef.id);
+  }
+
+  ctx.logger.info("Started workflow run", {
+    activatedStepIds,
+    companyId,
+    parentIssueId: parentIssueId || null,
+    runId: workflowRun.id,
+    workflowId: typedWorkflowDefinition.id,
+    workflowName: typedWorkflowDefinition.data.name,
+  });
+
+  return {
+    activatedStepIds,
+    parentIssueId: parentIssueId || null,
+    runId: workflowRun.id,
+    workflowId: typedWorkflowDefinition.id,
+  };
+}
+
 async function advanceWorkflow(
   ctx: PluginContext,
   stepRunRecord: PluginEntityRecord,
   companyId: string,
 ): Promise<void> {
-  const completedStepRun = toStepRunRecord(stepRunRecord);
+  const completedStepRun = toWorkflowStepRunRecord(stepRunRecord);
   const workflowRun = await getWorkflowRun(ctx, completedStepRun.data.runId);
 
   if (!workflowRun) {
@@ -237,7 +396,7 @@
   }
 
   const typedWorkflowDefinition = toWorkflowDefinitionRecord(workflowDefinition);
-  const stepRuns = (await listStepRuns(ctx, typedWorkflowRun.id, companyId)).map(toStepRunRecord);
+  const stepRuns = (await listStepRuns(ctx, typedWorkflowRun.id, companyId)).map(toWorkflowStepRunRecord);
   const completed = new Set<string>();
   const failed = new Set<string>();
   const skipped = new Set<string>();
@@ -289,7 +448,7 @@
         continue;
       }
 
-      stepRun = toStepRunRecord(await createStepRun(ctx, companyId, {
+      stepRun = toWorkflowStepRunRecord(await createStepRun(ctx, companyId, {
         agentName: resolvedAgent.agentName,
         retryCount: 0,
         runId: typedWorkflowRun.id,
@@ -305,6 +464,7 @@
       stepDef,
       typedWorkflowRun.data.workflowName,
       companyId,
+      { parentIssueId: typedWorkflowRun.data.parentIssueId },
     );
   }
 
@@ -329,7 +489,7 @@
   ctx: PluginContext,
   workflowRun: WorkflowRunRecord,
   workflowDefinition: WorkflowDefinitionRecord,
-  sourceStepRun: WorkflowStepRecord,
+  sourceStepRun: WorkflowStepRunRecord,
   escalationTargetId: string,
   companyId: string,
 ): Promise<void> {
@@ -338,7 +498,7 @@
     throw new Error(`Escalation target step not found: ${escalationTargetId}`);
   }
 
-  const stepRuns = (await listStepRuns(ctx, workflowRun.id, companyId)).map(toStepRunRecord);
+  const stepRuns = (await listStepRuns(ctx, workflowRun.id, companyId)).map(toWorkflowStepRunRecord);
   let escalationStepRun = stepRuns.find((candidate) => candidate.data.stepId === escalationTargetId) ?? null;
   const resolvedAgent = await resolveStepAgent(
     ctx,
@@ -352,7 +512,7 @@
   }
 
   if (!escalationStepRun) {
-    escalationStepRun = toStepRunRecord(await createStepRun(ctx, companyId, {
+    escalationStepRun = toWorkflowStepRunRecord(await createStepRun(ctx, companyId, {
       agentName: resolvedAgent.agentName,
       retryCount: 0,
       runId: workflowRun.id,
@@ -374,14 +534,14 @@
       title: `${workflowRun.data.workflowName}: ${escalationStep.title}`,
     });
 
-    escalationStepRun = toStepRunRecord(await updateStepRun(ctx, escalationStepRun.id, {
+    escalationStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, escalationStepRun.id, {
       issueId: issue.id,
     }));
   }
 
   const shouldActivate = escalationStepRun.data.status === STEP_STATUSES.backlog;
   if (shouldActivate) {
-    escalationStepRun = toStepRunRecord(await updateStepRun(ctx, escalationStepRun.id, {
+    escalationStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, escalationStepRun.id, {
       status: STEP_STATUSES.todo,
     }));
   }
@@ -389,8 +549,8 @@
   if (shouldActivate) {
     await invokeAgentForStep(
       ctx,
+      escalationStepRun,
       escalationStep,
-      escalationStepRun.data.agentName,
       workflowRun.data.workflowName,
       companyId,
     );
@@ -421,7 +581,7 @@
     return;
   }
 
-  const typedStepRun = toStepRunRecord(stepRunRecord);
+  const typedStepRun = toWorkflowStepRunRecord(stepRunRecord);
   if (TERMINAL_STEP_STATUSES.has(typedStepRun.data.status)) {
     return;
   }
@@ -469,7 +629,7 @@
   if (options.allowRetry) {
     const retryInfo = getRetryInfo(typedWorkflowDefinition.data.steps, typedStepRun.data.stepId);
     if (retryInfo.shouldRetry && typedStepRun.data.retryCount < retryInfo.maxRetries) {
-      const retriedStepRun = toStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
+      const retriedStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
         completedAt: undefined,
         retryCount: typedStepRun.data.retryCount + 1,
         status: STEP_STATUSES.todo,
@@ -477,8 +637,8 @@
 
       await invokeAgentForStep(
         ctx,
+        retriedStepRun,
         stepDef,
-        retriedStepRun.data.agentName,
         typedWorkflowRun.data.workflowName,
         event.companyId,
       );
@@ -496,7 +656,7 @@
   }
 
   if (stepDef.onFailure === "skip") {
-    const skippedStepRun = toStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
+    const skippedStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
       completedAt: new Date().toISOString(),
       status: STEP_STATUSES.skipped,
     }));
@@ -556,7 +716,7 @@
       return;
     }
 
-    const escalatedStepRun = toStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
+    const escalatedStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
       completedAt: new Date().toISOString(),
       status: STEP_STATUSES.escalated,
     }));
@@ -617,6 +777,29 @@
   }
 }
 
+function registerDataHandler(
+  ctx: PluginContext,
+  key: string,
+  handler: (params: Record<string, unknown>) => Promise<unknown>,
+): void {
+  const dataClient = ctx.data as {
+    handle?: (handlerKey: string, handlerFn: (params: Record<string, unknown>) => Promise<unknown>) => void;
+    register?: (handlerKey: string, handlerFn: (params: Record<string, unknown>) => Promise<unknown>) => void;
+  };
+
+  if (typeof dataClient.handle === "function") {
+    dataClient.handle(key, handler);
+    return;
+  }
+
+  if (typeof dataClient.register === "function") {
+    dataClient.register(key, handler);
+    return;
+  }
+
+  throw new Error("Plugin data client does not support handler registration");
+}
+
 const plugin = definePlugin({
   async setup(ctx: PluginContext) {
     ctx.events.on("issue.updated", async (event: PluginEvent) => {
@@ -638,7 +821,7 @@
           return;
         }
 
-        const stepRun = toStepRunRecord(stepRunRecord);
+        const stepRun = toWorkflowStepRunRecord(stepRunRecord);
         const payload = event.payload as { status?: string };
         const issueStatus = typeof payload.status === "string" ? payload.status : undefined;
 
@@ -657,7 +840,7 @@
           return;
         }
 
-        const updatedStepRun = toStepRunRecord(await updateStepRun(ctx, stepRun.id, {
+        const updatedStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, stepRun.id, {
           completedAt: nextStepStatus === STEP_STATUSES.done ? new Date().toISOString() : undefined,
           startedAt: nextStepStatus === STEP_STATUSES.inProgress
             ? stepRun.data.startedAt ?? new Date().toISOString()
@@ -687,11 +870,21 @@
 
     ctx.events.on("issue.created", async (event: PluginEvent) => {
       try {
+        const idempotencyKey = buildIdempotencyKey(event);
+        if (await checkIdempotency(ctx, idempotencyKey, event.companyId)) {
+          return;
+        }
+
         const payload = event.payload as {
           id?: string;
           parentId?: string;
           assigneeAgentId?: string;
         };
+        const issueId = typeof event.entityId === "string" && event.entityId.trim()
+          ? event.entityId.trim()
+          : typeof payload.id === "string" && payload.id.trim()
+            ? payload.id.trim()
+            : "";
 
         const parentId = typeof payload.parentId === "string" && payload.parentId.trim()
           ? payload.parentId.trim()
@@ -700,12 +893,34 @@
           ? payload.assigneeAgentId.trim()
           : "";
 
-        if (parentId || !assigneeAgentId) {
+        if (!issueId || parentId || !assigneeAgentId) {
+          await markIdempotency(ctx, idempotencyKey, event.companyId);
+          return;
+        }
+
+        const agent = await ctx.agents.get(assigneeAgentId, event.companyId);
+        const metadata = agent?.metadata as Record<string, unknown> | undefined;
+        const defaultParentIssueId = typeof metadata?.defaultParentIssueId === "string" && metadata.defaultParentIssueId.trim()
+          ? metadata.defaultParentIssueId.trim()
+          : "";
+
+        if (!defaultParentIssueId) {
+          await markIdempotency(ctx, idempotencyKey, event.companyId);
           return;
         }
 
-        ctx.logger.info("parentId filler: issue created without parent", {
-          issueId: event.entityId ?? payload.id ?? null,
+        await ctx.issues.update(
+          issueId,
+          { parentId: defaultParentIssueId } as IssueUpdatePatch,
+          event.companyId,
+        );
+        await markIdempotency(ctx, idempotencyKey, event.companyId);
+
+        ctx.logger.info("parentId filler: populated issue parentId from agent metadata", {
+          assigneeAgentId,
+          companyId: event.companyId,
+          issueId,
+          parentId: defaultParentIssueId,
         });
       } catch (error) {
         ctx.logger.warn("Failed to handle issue.created event", {
@@ -744,7 +959,23 @@
       await runReconciler(ctx);
     });
 
-    ctx.data.register("workflow-overview", async (params: Record<string, unknown>) => {
+    registerDataHandler(ctx, "start-workflow", async (params: Record<string, unknown>) => {
+      const workflowId = typeof params.workflowId === "string" ? params.workflowId.trim() : "";
+      const companyId = typeof params.companyId === "string" ? params.companyId.trim() : "";
+      const parentIssueId = typeof params.parentIssueId === "string" ? params.parentIssueId.trim() : "";
+      const createParentIssue = params.createParentIssue === true;
+
+      if (!workflowId || !companyId) {
+        throw new Error("start-workflow requires workflowId and companyId");
+      }
+
+      return await startWorkflow(ctx, workflowId, companyId, {
+        createParentIssue,
+        parentIssueId: parentIssueId || undefined,
+      });
+    });
+
+    registerDataHandler(ctx, "workflow-overview", async (params: Record<string, unknown>) => {
       try {
         const companyId = typeof params.companyId === "string" ? params.companyId.trim() : "";
         if (!companyId) {
@@ -782,7 +1013,7 @@
       }
     });
 
-    ctx.data.register("workflow-run-detail", async (params: Record<string, unknown>) => {
+    registerDataHandler(ctx, "workflow-run-detail", async (params: Record<string, unknown>) => {
       try {
         const runId = typeof params.runId === "string" ? params.runId.trim() : "";
         if (!runId) {
@@ -807,7 +1038,7 @@
             ...typedWorkflowRun.data,
           },
           stepRuns: stepRuns.map((record: PluginEntityRecord) => {
-            const stepRun = toStepRunRecord(record);
+            const stepRun = toWorkflowStepRunRecord(record);
             return {
               id: stepRun.id,
               status: stepRun.status,
diff --git a/paperclip-addon/plugins/workflow-engine/src/workflow-store.ts b/paperclip-addon/plugins/workflow-engine/src/workflow-store.ts
index 5871a2082258cebcfe8b69ebafbb0814cf303d50..27791ef142b641d85b6ef7e35a6746218a64d941
--- a/paperclip-addon/plugins/workflow-engine/src/workflow-store.ts
+++ b/paperclip-addon/plugins/workflow-engine/src/workflow-store.ts
@@ -44,7 +44,14 @@
 
 type TypedEntityRecord<T> = Omit<PluginEntityRecord, "data"> & { data: T };
 
-function toTypedRecord<T>(record: PluginEntityRecord): TypedEntityRecord<T> {
+function toTypedRecord<T>(
+  record: PluginEntityRecord,
+  entityType: string,
+): TypedEntityRecord<T> {
+  if (record.entityType !== entityType) {
+    throw new Error(`Expected entity type "${entityType}", got "${record.entityType}"`);
+  }
+
   return record as TypedEntityRecord<T>;
 }
 
@@ -67,13 +74,39 @@
   id: string,
   entityType: string,
 ): Promise<TypedEntityRecord<T> | null> {
+  const listQuery = {
+    entityType,
+    id,
+    limit: 1,
+  } as Record<string, unknown>;
+
+  const listed = await ctx.entities.list(
+    listQuery as Parameters<PluginContext["entities"]["list"]>[0],
+  );
+  const listedRecord = listed.find(
+    (record: PluginEntityRecord) => record.id === id,
+  ) ?? null;
+
+  if (listedRecord) {
+    if (listedRecord.entityType !== entityType) {
+      return null;
+    }
+
+    return toTypedRecord<T>(listedRecord, entityType);
+  }
+
+  // Fallback: some host SDK variants ignore `id` on entities.list().
+  // Keep defensive type verification on direct get() until id-filter support is universal.
   const record = await ctx.entities.get(id);
+  if (!record) {
+    return null;
+  }
 
-  if (!record || record.entityType !== entityType) {
+  if (record.entityType !== entityType) {
     return null;
   }
 
-  return toTypedRecord<T>(record);
+  return toTypedRecord<T>(record, entityType);
 }
 
 async function requireEntityByType<T>(
diff --git a/paperclip-addon/plugins/workflow-engine/src/workflow-utils.ts b/paperclip-addon/plugins/workflow-engine/src/workflow-utils.ts
new file mode 100644
index 0000000000000000000000000000000000000000..ec1cf6f6b9aa722b126b58c0a728b4e538ad1494
--- /dev/null
+++ b/paperclip-addon/plugins/workflow-engine/src/workflow-utils.ts
@@ -0,0 +1,62 @@
+import type { PluginEntityRecord } from "@paperclipai/plugin-sdk";
+
+import { STEP_STATUSES } from "./constants.js";
+import type { WorkflowStep } from "./dag-engine.js";
+import type {
+  WorkflowDefinition,
+  WorkflowRun,
+  WorkflowStepRun,
+} from "./workflow-store.js";
+
+export type WorkflowRunRecord = PluginEntityRecord & { data: WorkflowRun };
+export type WorkflowStepRunRecord = PluginEntityRecord & { data: WorkflowStepRun };
+export type WorkflowDefinitionRecord = PluginEntityRecord & { data: WorkflowDefinition };
+type StepAgentMetadata = WorkflowStep & {
+  agent?: string;
+  agentName?: string;
+  assigneeAgentName?: string;
+};
+
+export const TERMINAL_STEP_STATUSES = new Set<string>([
+  STEP_STATUSES.done,
+  STEP_STATUSES.failed,
+  STEP_STATUSES.skipped,
+  STEP_STATUSES.escalated,
+]);
+
+export function toWorkflowRunRecord(record: PluginEntityRecord): WorkflowRunRecord {
+  return record as WorkflowRunRecord;
+}
+
+export function toWorkflowStepRunRecord(record: PluginEntityRecord): WorkflowStepRunRecord {
+  return record as WorkflowStepRunRecord;
+}
+
+export function toWorkflowDefinitionRecord(record: PluginEntityRecord): WorkflowDefinitionRecord {
+  return record as WorkflowDefinitionRecord;
+}
+
+export function findStepDefinition(definition: WorkflowDefinitionRecord, stepId: string): WorkflowStep | null {
+  return definition.data.steps.find((step: WorkflowStep) => step.id === stepId) ?? null;
+}
+
+export function getStepAgentNameHint(stepDef: WorkflowStep): string | null {
+  const stepMeta = stepDef as StepAgentMetadata;
+  const candidates = [stepMeta.agentName, stepMeta.agent, stepMeta.assigneeAgentName];
+
+  for (const candidate of candidates) {
+    if (typeof candidate === "string" && candidate.trim()) {
+      return candidate.trim();
+    }
+  }
+
+  return null;
+}
+
+export function getStepAgentName(stepRun: WorkflowStepRunRecord, stepDef: WorkflowStep): string | null {
+  if (typeof stepRun.data.agentName === "string" && stepRun.data.agentName.trim()) {
+    return stepRun.data.agentName.trim();
+  }
+
+  return getStepAgentNameHint(stepDef);
+}

file update:
diff --git a/paperclip-addon/plugins/workflow-engine/src/dag-engine.ts b/paperclip-addon/plugins/workflow-engine/src/dag-engine.ts
index b59510f0473171038be77277286d3b5c2063e6c5..e7f21ff998cab78b9325a8aeacf4fda9c5e06553
--- a/paperclip-addon/plugins/workflow-engine/src/dag-engine.ts
+++ b/paperclip-addon/plugins/workflow-engine/src/dag-engine.ts
@@ -2,6 +2,7 @@
   id: string;
   title: string;
   dependsOn: string[];
+  sessionMode?: "fresh" | "reuse";
   onFailure?: "retry" | "skip" | "abort_workflow" | "escalate";
   escalateTo?: string;
   maxRetries?: number;
diff --git a/paperclip-addon/plugins/workflow-engine/src/reconciler.ts b/paperclip-addon/plugins/workflow-engine/src/reconciler.ts
index 14ab4e766a835d145a6c2e7b1b564270285cbdc3..55b649037e8a19a88bb39b9633a6a0cc84fa8d59
--- a/paperclip-addon/plugins/workflow-engine/src/reconciler.ts
+++ b/paperclip-addon/plugins/workflow-engine/src/reconciler.ts
@@ -8,65 +8,21 @@
   listStepRuns,
   updateStepRun,
   updateWorkflowRun,
-  type WorkflowDefinition,
-  type WorkflowRun,
-  type WorkflowStepRun,
 } from "./workflow-store.js";
-import type { WorkflowStep } from "./dag-engine.js";
-
-type WorkflowRunRecord = PluginEntityRecord & { data: WorkflowRun };
-type WorkflowStepRunRecord = PluginEntityRecord & { data: WorkflowStepRun };
-type WorkflowDefinitionRecord = PluginEntityRecord & { data: WorkflowDefinition };
-type StepAgentMetadata = WorkflowStep & {
-  agent?: string;
-  agentName?: string;
-  assigneeAgentName?: string;
-};
+import {
+  TERMINAL_STEP_STATUSES,
+  findStepDefinition,
+  getStepAgentName,
+  toWorkflowDefinitionRecord,
+  toWorkflowRunRecord,
+  toWorkflowStepRunRecord,
+} from "./workflow-utils.js";
 
 const STALE_THRESHOLD_MS = 300_000;
-const TERMINAL_STEP_STATUSES = new Set<string>([
-  STEP_STATUSES.done,
-  STEP_STATUSES.failed,
-  STEP_STATUSES.skipped,
-  STEP_STATUSES.escalated,
-]);
 
 function isStale(record: PluginEntityRecord, thresholdMs: number): boolean {
   const updatedAt = new Date(record.updatedAt).getTime();
   return Date.now() - updatedAt > thresholdMs;
-}
-
-function toWorkflowRunRecord(record: PluginEntityRecord): WorkflowRunRecord {
-  return record as WorkflowRunRecord;
-}
-
-function toWorkflowStepRunRecord(record: PluginEntityRecord): WorkflowStepRunRecord {
-  return record as WorkflowStepRunRecord;
-}
-
-function toWorkflowDefinitionRecord(record: PluginEntityRecord): WorkflowDefinitionRecord {
-  return record as WorkflowDefinitionRecord;
-}
-
-function findStepDefinition(definition: WorkflowDefinitionRecord, stepId: string): WorkflowStep | null {
-  return definition.data.steps.find((step: WorkflowStep) => step.id === stepId) ?? null;
-}
-
-function getStepAgentName(stepRun: WorkflowStepRunRecord, stepDef: WorkflowStep): string | null {
-  if (typeof stepRun.data.agentName === "string" && stepRun.data.agentName.trim()) {
-    return stepRun.data.agentName.trim();
-  }
-
-  const stepMeta = stepDef as StepAgentMetadata;
-  const candidates = [stepMeta.agentName, stepMeta.agent, stepMeta.assigneeAgentName];
-
-  for (const candidate of candidates) {
-    if (typeof candidate === "string" && candidate.trim()) {
-      return candidate.trim();
-    }
-  }
-
-  return null;
 }
 
 async function invokeAgentByName(
diff --git a/paperclip-addon/plugins/workflow-engine/src/worker.ts b/paperclip-addon/plugins/workflow-engine/src/worker.ts
index c5ac3fbb4ca7f767eb29331bd3630b635449b335..303e35958890fa1f66db3f2b0ff1fa1f134102a8
--- a/paperclip-addon/plugins/workflow-engine/src/worker.ts
+++ b/paperclip-addon/plugins/workflow-engine/src/worker.ts
@@ -15,6 +15,7 @@
 } from "./dag-engine.js";
 import {
   checkIdempotency,
+  createWorkflowRun,
   createStepRun,
   findStepRunByIssueId,
   getWorkflowDefinition,
@@ -25,51 +26,38 @@
   markIdempotency,
   updateStepRun,
   updateWorkflowRun,
-  type WorkflowDefinition,
-  type WorkflowRun,
   type WorkflowStepRun,
 } from "./workflow-store.js";
+import {
+  TERMINAL_STEP_STATUSES,
+  findStepDefinition,
+  getStepAgentName,
+  getStepAgentNameHint,
+  toWorkflowDefinitionRecord,
+  toWorkflowRunRecord,
+  toWorkflowStepRunRecord,
+  type WorkflowDefinitionRecord,
+  type WorkflowRunRecord,
+  type WorkflowStepRunRecord,
+} from "./workflow-utils.js";
 
-type WorkflowStepRecord = PluginEntityRecord & { data: WorkflowStepRun };
-type WorkflowRunRecord = PluginEntityRecord & { data: WorkflowRun };
-type WorkflowDefinitionRecord = PluginEntityRecord & { data: WorkflowDefinition };
 type WorkflowStepMetadata = WorkflowStep & {
   description?: string;
-  agent?: string;
-  agentName?: string;
-  assigneeAgentName?: string;
   assigneeAgentId?: string;
+  sessionMode?: "fresh" | "reuse";
 };
 type AgentRecord = Awaited<ReturnType<PluginContext["agents"]["list"]>>[number];
+type IssueUpdatePatch = Parameters<PluginContext["issues"]["update"]>[1];
 type ReconcilerModule = {
   reconcileStuckSteps?: (ctx: PluginContext) => Promise<void>;
 };
 
-const TERMINAL_STEP_STATUSES = new Set<string>([
-  STEP_STATUSES.done,
-  STEP_STATUSES.failed,
-  STEP_STATUSES.skipped,
-  STEP_STATUSES.escalated,
-]);
-
 function summarizeError(error: unknown): string {
   if (error instanceof Error) {
     return error.message;
   }
 
   return String(error);
-}
-
-function toStepRunRecord(record: PluginEntityRecord): WorkflowStepRecord {
-  return record as WorkflowStepRecord;
-}
-
-function toWorkflowRunRecord(record: PluginEntityRecord): WorkflowRunRecord {
-  return record as WorkflowRunRecord;
-}
-
-function toWorkflowDefinitionRecord(record: PluginEntityRecord): WorkflowDefinitionRecord {
-  return record as WorkflowDefinitionRecord;
 }
 
 function buildIdempotencyKey(event: PluginEvent): string {
@@ -78,10 +66,6 @@
 
 function getStepMetadata(stepDef: WorkflowStep): WorkflowStepMetadata {
   return stepDef as WorkflowStepMetadata;
-}
-
-function findStepDefinition(definition: WorkflowDefinitionRecord, stepId: string): WorkflowStep | null {
-  return definition.data.steps.find((step: WorkflowStep) => step.id === stepId) ?? null;
 }
 
 function getStepDescription(stepDef: WorkflowStep): string | undefined {
@@ -89,19 +73,6 @@
   return typeof description === "string" && description.trim() ? description.trim() : undefined;
 }
 
-function getStepAgentNameHint(stepDef: WorkflowStep): string | null {
-  const meta = getStepMetadata(stepDef);
-  const candidates = [meta.agentName, meta.agent, meta.assigneeAgentName];
-
-  for (const candidate of candidates) {
-    if (typeof candidate === "string" && candidate.trim()) {
-      return candidate.trim();
-    }
-  }
-
-  return null;
-}
-
 function getStepAgentIdHint(stepDef: WorkflowStep): string | null {
   const assigneeAgentId = getStepMetadata(stepDef).assigneeAgentId;
   if (typeof assigneeAgentId !== "string" || !assigneeAgentId.trim()) {
@@ -147,68 +118,256 @@
 
 async function invokeAgentForStep(
   ctx: PluginContext,
+  stepRunRecord: WorkflowStepRunRecord,
   stepDef: WorkflowStep,
-  agentName: string,
   workflowName: string,
   companyId: string,
-): Promise<void> {
+): Promise<WorkflowStepRunRecord> {
+  const agentName = getStepAgentName(stepRunRecord, stepDef);
+  if (!agentName) {
+    ctx.logger.warn("Agent name is missing for workflow step", {
+      companyId,
+      runId: stepRunRecord.data.runId,
+      stepId: stepDef.id,
+      workflowName,
+    });
+    return stepRunRecord;
+  }
+
   const agents = await ctx.agents.list({ companyId });
-  const agent = agents.find((candidate: AgentRecord) => candidate.name === agentName);
+  const agent = agents.find((candidate: AgentRecord) => candidate.name === agentName) ?? null;
 
   if (!agent) {
     ctx.logger.warn("Agent not found for step", {
       agentName,
       companyId,
+      runId: stepRunRecord.data.runId,
+      stepId: stepDef.id,
+      workflowName,
+    });
+    return stepRunRecord;
+  }
+
+  const prompt = `workflow:${workflowName}/step:${stepDef.id} — "${stepDef.title}" is ready. Please proceed with the assigned task.`;
+  const reason = `workflow:${workflowName}/step:${stepDef.id}`;
+  const sessionMode = getStepMetadata(stepDef).sessionMode === "reuse" ? "reuse" : "fresh";
+
+  if (sessionMode === "reuse") {
+    let sessionId = typeof stepRunRecord.data.sessionId === "string" && stepRunRecord.data.sessionId.trim()
+      ? stepRunRecord.data.sessionId.trim()
+      : "";
+
+    if (!sessionId) {
+      const session = await ctx.agents.sessions.create(agent.id, companyId, {
+        reason,
+      });
+      sessionId = session.sessionId;
+      stepRunRecord = toWorkflowStepRunRecord(await updateStepRun(ctx, stepRunRecord.id, {
+        sessionId,
+      }));
+    }
+
+    await ctx.agents.sessions.sendMessage(sessionId, companyId, {
+      prompt,
+      reason,
+    });
+
+    ctx.logger.info("Sent workflow step prompt via agent session", {
+      agentId: agent.id,
+      agentName,
+      companyId,
+      runId: stepRunRecord.data.runId,
+      sessionId,
       stepId: stepDef.id,
       workflowName,
     });
-    return;
+    return stepRunRecord;
   }
 
   await ctx.agents.invoke(agent.id, companyId, {
-    prompt: `workflow:${workflowName}/step:${stepDef.id} — "${stepDef.title}" is ready. Please proceed with the assigned task.`,
-    reason: `workflow:${workflowName}/step:${stepDef.id}`,
+    prompt,
+    reason,
   });
 
   ctx.logger.info("Invoked agent for workflow step", {
     agentId: agent.id,
     agentName,
     companyId,
+    runId: stepRunRecord.data.runId,
     stepId: stepDef.id,
     workflowName,
   });
+
+  return stepRunRecord;
 }
 
 async function activateBacklogStep(
   ctx: PluginContext,
-  stepRunRecord: WorkflowStepRecord,
+  stepRunRecord: WorkflowStepRunRecord,
   stepDef: WorkflowStep,
   workflowName: string,
   companyId: string,
-): Promise<void> {
+  options?: { parentIssueId?: string },
+): Promise<WorkflowStepRunRecord> {
   if (stepRunRecord.data.status !== STEP_STATUSES.backlog) {
-    return;
+    return stepRunRecord;
   }
 
-  const updated = toStepRunRecord(await updateStepRun(ctx, stepRunRecord.id, {
+  const resolvedAgent = await resolveStepAgent(
+    ctx,
+    companyId,
+    stepDef,
+    stepRunRecord.data.agentName,
+  );
+
+  let issueId = typeof stepRunRecord.data.issueId === "string" && stepRunRecord.data.issueId.trim()
+    ? stepRunRecord.data.issueId.trim()
+    : "";
+  const stepDescription = getStepDescription(stepDef) ?? `Workflow step: ${stepDef.id}`;
+
+  if (!issueId) {
+    const issue = await ctx.issues.create({
+      assigneeAgentId: resolvedAgent.agentId ?? undefined,
+      companyId,
+      description: stepDescription,
+      parentId: options?.parentIssueId,
+      title: `[${workflowName}] ${stepDef.title}`,
+    });
+    await ctx.issues.update(issue.id, { status: "todo" } as IssueUpdatePatch, companyId);
+    issueId = issue.id;
+  }
+
+  const nextStartedAt = stepRunRecord.data.startedAt ?? new Date().toISOString();
+  let updatedStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, stepRunRecord.id, {
+    agentName: resolvedAgent.agentName ?? stepRunRecord.data.agentName,
+    issueId,
+    startedAt: nextStartedAt,
     status: STEP_STATUSES.todo,
   }));
 
-  await invokeAgentForStep(
+  updatedStepRun = await invokeAgentForStep(
     ctx,
+    updatedStepRun,
     stepDef,
-    updated.data.agentName,
     workflowName,
     companyId,
   );
+
+  return updatedStepRun;
 }
 
+async function startWorkflow(
+  ctx: PluginContext,
+  workflowId: string,
+  companyId: string,
+  options?: {
+    createParentIssue?: boolean;
+    parentIssueId?: string;
+  },
+): Promise<{
+  activatedStepIds: string[];
+  parentIssueId: string | null;
+  runId: string;
+  workflowId: string;
+}> {
+  const workflowDefinition = await getWorkflowDefinition(ctx, workflowId);
+  if (!workflowDefinition) {
+    throw new Error(`Workflow definition not found: ${workflowId}`);
+  }
+
+  const typedWorkflowDefinition = toWorkflowDefinitionRecord(workflowDefinition);
+  if (typedWorkflowDefinition.data.companyId !== companyId) {
+    throw new Error(`Workflow does not belong to company: ${workflowId}`);
+  }
+
+  let parentIssueId = typeof options?.parentIssueId === "string" && options.parentIssueId.trim()
+    ? options.parentIssueId.trim()
+    : "";
+  if (!parentIssueId && options?.createParentIssue) {
+    const parentIssue = await ctx.issues.create({
+      companyId,
+      description: typedWorkflowDefinition.data.description || `Workflow run: ${typedWorkflowDefinition.data.name}`,
+      title: `[${typedWorkflowDefinition.data.name}] Workflow run`,
+    });
+    parentIssueId = parentIssue.id;
+  }
+
+  const workflowRun = toWorkflowRunRecord(await createWorkflowRun(ctx, {
+    companyId,
+    parentIssueId: parentIssueId || undefined,
+    startedAt: new Date().toISOString(),
+    status: RUN_STATUSES.running,
+    workflowId: typedWorkflowDefinition.id,
+    workflowName: typedWorkflowDefinition.data.name,
+  }));
+
+  const agents = await ctx.agents.list({ companyId });
+  const agentsByName = new Map<string, AgentRecord>();
+  for (const agent of agents) {
+    agentsByName.set(agent.name, agent);
+  }
+
+  const pendingRootSteps: Array<{ stepDef: WorkflowStep; stepRun: WorkflowStepRunRecord }> = [];
+  for (const stepDef of typedWorkflowDefinition.data.steps) {
+    const agentNameHint = getStepAgentNameHint(stepDef);
+    const matchedAgent = agentNameHint ? agentsByName.get(agentNameHint) ?? null : null;
+    const resolvedAgent = matchedAgent
+      ? { agentId: matchedAgent.id, agentName: matchedAgent.name }
+      : await resolveStepAgent(ctx, companyId, stepDef, agentNameHint ?? undefined);
+
+    if (!resolvedAgent.agentName) {
+      throw new Error(`Unable to resolve step assignee for "${stepDef.id}"`);
+    }
+
+    const stepRun = toWorkflowStepRunRecord(await createStepRun(ctx, companyId, {
+      agentName: resolvedAgent.agentName,
+      retryCount: 0,
+      runId: workflowRun.id,
+      status: STEP_STATUSES.backlog,
+      stepId: stepDef.id,
+    }));
+
+    if (stepDef.dependsOn.length === 0 && stepDef.triggerOn !== "escalation") {
+      pendingRootSteps.push({ stepDef, stepRun });
+    }
+  }
+
+  const activatedStepIds: string[] = [];
+  for (const pending of pendingRootSteps) {
+    await activateBacklogStep(
+      ctx,
+      pending.stepRun,
+      pending.stepDef,
+      typedWorkflowDefinition.data.name,
+      companyId,
+      { parentIssueId: parentIssueId || undefined },
+    );
+    activatedStepIds.push(pending.stepDef.id);
+  }
+
+  ctx.logger.info("Started workflow run", {
+    activatedStepIds,
+    companyId,
+    parentIssueId: parentIssueId || null,
+    runId: workflowRun.id,
+    workflowId: typedWorkflowDefinition.id,
+    workflowName: typedWorkflowDefinition.data.name,
+  });
+
+  return {
+    activatedStepIds,
+    parentIssueId: parentIssueId || null,
+    runId: workflowRun.id,
+    workflowId: typedWorkflowDefinition.id,
+  };
+}
+
 async function advanceWorkflow(
   ctx: PluginContext,
   stepRunRecord: PluginEntityRecord,
   companyId: string,
 ): Promise<void> {
-  const completedStepRun = toStepRunRecord(stepRunRecord);
+  const completedStepRun = toWorkflowStepRunRecord(stepRunRecord);
   const workflowRun = await getWorkflowRun(ctx, completedStepRun.data.runId);
 
   if (!workflowRun) {
@@ -237,7 +396,7 @@
   }
 
   const typedWorkflowDefinition = toWorkflowDefinitionRecord(workflowDefinition);
-  const stepRuns = (await listStepRuns(ctx, typedWorkflowRun.id, companyId)).map(toStepRunRecord);
+  const stepRuns = (await listStepRuns(ctx, typedWorkflowRun.id, companyId)).map(toWorkflowStepRunRecord);
   const completed = new Set<string>();
   const failed = new Set<string>();
   const skipped = new Set<string>();
@@ -289,7 +448,7 @@
         continue;
       }
 
-      stepRun = toStepRunRecord(await createStepRun(ctx, companyId, {
+      stepRun = toWorkflowStepRunRecord(await createStepRun(ctx, companyId, {
         agentName: resolvedAgent.agentName,
         retryCount: 0,
         runId: typedWorkflowRun.id,
@@ -305,6 +464,7 @@
       stepDef,
       typedWorkflowRun.data.workflowName,
       companyId,
+      { parentIssueId: typedWorkflowRun.data.parentIssueId },
     );
   }
 
@@ -329,7 +489,7 @@
   ctx: PluginContext,
   workflowRun: WorkflowRunRecord,
   workflowDefinition: WorkflowDefinitionRecord,
-  sourceStepRun: WorkflowStepRecord,
+  sourceStepRun: WorkflowStepRunRecord,
   escalationTargetId: string,
   companyId: string,
 ): Promise<void> {
@@ -338,7 +498,7 @@
     throw new Error(`Escalation target step not found: ${escalationTargetId}`);
   }
 
-  const stepRuns = (await listStepRuns(ctx, workflowRun.id, companyId)).map(toStepRunRecord);
+  const stepRuns = (await listStepRuns(ctx, workflowRun.id, companyId)).map(toWorkflowStepRunRecord);
   let escalationStepRun = stepRuns.find((candidate) => candidate.data.stepId === escalationTargetId) ?? null;
   const resolvedAgent = await resolveStepAgent(
     ctx,
@@ -352,7 +512,7 @@
   }
 
   if (!escalationStepRun) {
-    escalationStepRun = toStepRunRecord(await createStepRun(ctx, companyId, {
+    escalationStepRun = toWorkflowStepRunRecord(await createStepRun(ctx, companyId, {
       agentName: resolvedAgent.agentName,
       retryCount: 0,
       runId: workflowRun.id,
@@ -374,14 +534,14 @@
       title: `${workflowRun.data.workflowName}: ${escalationStep.title}`,
     });
 
-    escalationStepRun = toStepRunRecord(await updateStepRun(ctx, escalationStepRun.id, {
+    escalationStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, escalationStepRun.id, {
       issueId: issue.id,
     }));
   }
 
   const shouldActivate = escalationStepRun.data.status === STEP_STATUSES.backlog;
   if (shouldActivate) {
-    escalationStepRun = toStepRunRecord(await updateStepRun(ctx, escalationStepRun.id, {
+    escalationStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, escalationStepRun.id, {
       status: STEP_STATUSES.todo,
     }));
   }
@@ -389,8 +549,8 @@
   if (shouldActivate) {
     await invokeAgentForStep(
       ctx,
+      escalationStepRun,
       escalationStep,
-      escalationStepRun.data.agentName,
       workflowRun.data.workflowName,
       companyId,
     );
@@ -421,7 +581,7 @@
     return;
   }
 
-  const typedStepRun = toStepRunRecord(stepRunRecord);
+  const typedStepRun = toWorkflowStepRunRecord(stepRunRecord);
   if (TERMINAL_STEP_STATUSES.has(typedStepRun.data.status)) {
     return;
   }
@@ -469,7 +629,7 @@
   if (options.allowRetry) {
     const retryInfo = getRetryInfo(typedWorkflowDefinition.data.steps, typedStepRun.data.stepId);
     if (retryInfo.shouldRetry && typedStepRun.data.retryCount < retryInfo.maxRetries) {
-      const retriedStepRun = toStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
+      const retriedStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
         completedAt: undefined,
         retryCount: typedStepRun.data.retryCount + 1,
         status: STEP_STATUSES.todo,
@@ -477,8 +637,8 @@
 
       await invokeAgentForStep(
         ctx,
+        retriedStepRun,
         stepDef,
-        retriedStepRun.data.agentName,
         typedWorkflowRun.data.workflowName,
         event.companyId,
       );
@@ -496,7 +656,7 @@
   }
 
   if (stepDef.onFailure === "skip") {
-    const skippedStepRun = toStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
+    const skippedStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
       completedAt: new Date().toISOString(),
       status: STEP_STATUSES.skipped,
     }));
@@ -556,7 +716,7 @@
       return;
     }
 
-    const escalatedStepRun = toStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
+    const escalatedStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
       completedAt: new Date().toISOString(),
       status: STEP_STATUSES.escalated,
     }));
@@ -617,6 +777,29 @@
   }
 }
 
+function registerDataHandler(
+  ctx: PluginContext,
+  key: string,
+  handler: (params: Record<string, unknown>) => Promise<unknown>,
+): void {
+  const dataClient = ctx.data as {
+    handle?: (handlerKey: string, handlerFn: (params: Record<string, unknown>) => Promise<unknown>) => void;
+    register?: (handlerKey: string, handlerFn: (params: Record<string, unknown>) => Promise<unknown>) => void;
+  };
+
+  if (typeof dataClient.handle === "function") {
+    dataClient.handle(key, handler);
+    return;
+  }
+
+  if (typeof dataClient.register === "function") {
+    dataClient.register(key, handler);
+    return;
+  }
+
+  throw new Error("Plugin data client does not support handler registration");
+}
+
 const plugin = definePlugin({
   async setup(ctx: PluginContext) {
     ctx.events.on("issue.updated", async (event: PluginEvent) => {
@@ -638,7 +821,7 @@
           return;
         }
 
-        const stepRun = toStepRunRecord(stepRunRecord);
+        const stepRun = toWorkflowStepRunRecord(stepRunRecord);
         const payload = event.payload as { status?: string };
         const issueStatus = typeof payload.status === "string" ? payload.status : undefined;
 
@@ -657,7 +840,7 @@
           return;
         }
 
-        const updatedStepRun = toStepRunRecord(await updateStepRun(ctx, stepRun.id, {
+        const updatedStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, stepRun.id, {
           completedAt: nextStepStatus === STEP_STATUSES.done ? new Date().toISOString() : undefined,
           startedAt: nextStepStatus === STEP_STATUSES.inProgress
             ? stepRun.data.startedAt ?? new Date().toISOString()
@@ -687,11 +870,21 @@
 
     ctx.events.on("issue.created", async (event: PluginEvent) => {
       try {
+        const idempotencyKey = buildIdempotencyKey(event);
+        if (await checkIdempotency(ctx, idempotencyKey, event.companyId)) {
+          return;
+        }
+
         const payload = event.payload as {
           id?: string;
           parentId?: string;
           assigneeAgentId?: string;
         };
+        const issueId = typeof event.entityId === "string" && event.entityId.trim()
+          ? event.entityId.trim()
+          : typeof payload.id === "string" && payload.id.trim()
+            ? payload.id.trim()
+            : "";
 
         const parentId = typeof payload.parentId === "string" && payload.parentId.trim()
           ? payload.parentId.trim()
@@ -700,12 +893,34 @@
           ? payload.assigneeAgentId.trim()
           : "";
 
-        if (parentId || !assigneeAgentId) {
+        if (!issueId || parentId || !assigneeAgentId) {
+          await markIdempotency(ctx, idempotencyKey, event.companyId);
+          return;
+        }
+
+        const agent = await ctx.agents.get(assigneeAgentId, event.companyId);
+        const metadata = agent?.metadata as Record<string, unknown> | undefined;
+        const defaultParentIssueId = typeof metadata?.defaultParentIssueId === "string" && metadata.defaultParentIssueId.trim()
+          ? metadata.defaultParentIssueId.trim()
+          : "";
+
+        if (!defaultParentIssueId) {
+          await markIdempotency(ctx, idempotencyKey, event.companyId);
           return;
         }
 
-        ctx.logger.info("parentId filler: issue created without parent", {
-          issueId: event.entityId ?? payload.id ?? null,
+        await ctx.issues.update(
+          issueId,
+          { parentId: defaultParentIssueId } as IssueUpdatePatch,
+          event.companyId,
+        );
+        await markIdempotency(ctx, idempotencyKey, event.companyId);
+
+        ctx.logger.info("parentId filler: populated issue parentId from agent metadata", {
+          assigneeAgentId,
+          companyId: event.companyId,
+          issueId,
+          parentId: defaultParentIssueId,
         });
       } catch (error) {
         ctx.logger.warn("Failed to handle issue.created event", {
@@ -744,7 +959,23 @@
       await runReconciler(ctx);
     });
 
-    ctx.data.register("workflow-overview", async (params: Record<string, unknown>) => {
+    registerDataHandler(ctx, "start-workflow", async (params: Record<string, unknown>) => {
+      const workflowId = typeof params.workflowId === "string" ? params.workflowId.trim() : "";
+      const companyId = typeof params.companyId === "string" ? params.companyId.trim() : "";
+      const parentIssueId = typeof params.parentIssueId === "string" ? params.parentIssueId.trim() : "";
+      const createParentIssue = params.createParentIssue === true;
+
+      if (!workflowId || !companyId) {
+        throw new Error("start-workflow requires workflowId and companyId");
+      }
+
+      return await startWorkflow(ctx, workflowId, companyId, {
+        createParentIssue,
+        parentIssueId: parentIssueId || undefined,
+      });
+    });
+
+    registerDataHandler(ctx, "workflow-overview", async (params: Record<string, unknown>) => {
       try {
         const companyId = typeof params.companyId === "string" ? params.companyId.trim() : "";
         if (!companyId) {
@@ -782,7 +1013,7 @@
       }
     });
 
-    ctx.data.register("workflow-run-detail", async (params: Record<string, unknown>) => {
+    registerDataHandler(ctx, "workflow-run-detail", async (params: Record<string, unknown>) => {
       try {
         const runId = typeof params.runId === "string" ? params.runId.trim() : "";
         if (!runId) {
@@ -807,7 +1038,7 @@
             ...typedWorkflowRun.data,
           },
           stepRuns: stepRuns.map((record: PluginEntityRecord) => {
-            const stepRun = toStepRunRecord(record);
+            const stepRun = toWorkflowStepRunRecord(record);
             return {
               id: stepRun.id,
               status: stepRun.status,
diff --git a/paperclip-addon/plugins/workflow-engine/src/workflow-store.ts b/paperclip-addon/plugins/workflow-engine/src/workflow-store.ts
index 5871a2082258cebcfe8b69ebafbb0814cf303d50..27791ef142b641d85b6ef7e35a6746218a64d941
--- a/paperclip-addon/plugins/workflow-engine/src/workflow-store.ts
+++ b/paperclip-addon/plugins/workflow-engine/src/workflow-store.ts
@@ -44,7 +44,14 @@
 
 type TypedEntityRecord<T> = Omit<PluginEntityRecord, "data"> & { data: T };
 
-function toTypedRecord<T>(record: PluginEntityRecord): TypedEntityRecord<T> {
+function toTypedRecord<T>(
+  record: PluginEntityRecord,
+  entityType: string,
+): TypedEntityRecord<T> {
+  if (record.entityType !== entityType) {
+    throw new Error(`Expected entity type "${entityType}", got "${record.entityType}"`);
+  }
+
   return record as TypedEntityRecord<T>;
 }
 
@@ -67,13 +74,39 @@
   id: string,
   entityType: string,
 ): Promise<TypedEntityRecord<T> | null> {
+  const listQuery = {
+    entityType,
+    id,
+    limit: 1,
+  } as Record<string, unknown>;
+
+  const listed = await ctx.entities.list(
+    listQuery as Parameters<PluginContext["entities"]["list"]>[0],
+  );
+  const listedRecord = listed.find(
+    (record: PluginEntityRecord) => record.id === id,
+  ) ?? null;
+
+  if (listedRecord) {
+    if (listedRecord.entityType !== entityType) {
+      return null;
+    }
+
+    return toTypedRecord<T>(listedRecord, entityType);
+  }
+
+  // Fallback: some host SDK variants ignore `id` on entities.list().
+  // Keep defensive type verification on direct get() until id-filter support is universal.
   const record = await ctx.entities.get(id);
+  if (!record) {
+    return null;
+  }
 
-  if (!record || record.entityType !== entityType) {
+  if (record.entityType !== entityType) {
     return null;
   }
 
-  return toTypedRecord<T>(record);
+  return toTypedRecord<T>(record, entityType);
 }
 
 async function requireEntityByType<T>(
diff --git a/paperclip-addon/plugins/workflow-engine/src/workflow-utils.ts b/paperclip-addon/plugins/workflow-engine/src/workflow-utils.ts
new file mode 100644
index 0000000000000000000000000000000000000000..ec1cf6f6b9aa722b126b58c0a728b4e538ad1494
--- /dev/null
+++ b/paperclip-addon/plugins/workflow-engine/src/workflow-utils.ts
@@ -0,0 +1,62 @@
+import type { PluginEntityRecord } from "@paperclipai/plugin-sdk";
+
+import { STEP_STATUSES } from "./constants.js";
+import type { WorkflowStep } from "./dag-engine.js";
+import type {
+  WorkflowDefinition,
+  WorkflowRun,
+  WorkflowStepRun,
+} from "./workflow-store.js";
+
+export type WorkflowRunRecord = PluginEntityRecord & { data: WorkflowRun };
+export type WorkflowStepRunRecord = PluginEntityRecord & { data: WorkflowStepRun };
+export type WorkflowDefinitionRecord = PluginEntityRecord & { data: WorkflowDefinition };
+type StepAgentMetadata = WorkflowStep & {
+  agent?: string;
+  agentName?: string;
+  assigneeAgentName?: string;
+};
+
+export const TERMINAL_STEP_STATUSES = new Set<string>([
+  STEP_STATUSES.done,
+  STEP_STATUSES.failed,
+  STEP_STATUSES.skipped,
+  STEP_STATUSES.escalated,
+]);
+
+export function toWorkflowRunRecord(record: PluginEntityRecord): WorkflowRunRecord {
+  return record as WorkflowRunRecord;
+}
+
+export function toWorkflowStepRunRecord(record: PluginEntityRecord): WorkflowStepRunRecord {
+  return record as WorkflowStepRunRecord;
+}
+
+export function toWorkflowDefinitionRecord(record: PluginEntityRecord): WorkflowDefinitionRecord {
+  return record as WorkflowDefinitionRecord;
+}
+
+export function findStepDefinition(definition: WorkflowDefinitionRecord, stepId: string): WorkflowStep | null {
+  return definition.data.steps.find((step: WorkflowStep) => step.id === stepId) ?? null;
+}
+
+export function getStepAgentNameHint(stepDef: WorkflowStep): string | null {
+  const stepMeta = stepDef as StepAgentMetadata;
+  const candidates = [stepMeta.agentName, stepMeta.agent, stepMeta.assigneeAgentName];
+
+  for (const candidate of candidates) {
+    if (typeof candidate === "string" && candidate.trim()) {
+      return candidate.trim();
+    }
+  }
+
+  return null;
+}
+
+export function getStepAgentName(stepRun: WorkflowStepRunRecord, stepDef: WorkflowStep): string | null {
+  if (typeof stepRun.data.agentName === "string" && stepRun.data.agentName.trim()) {
+    return stepRun.data.agentName.trim();
+  }
+
+  return getStepAgentNameHint(stepDef);
+}

exec
/bin/zsh -lc "cd plugins/workflow-engine && npm run typecheck -- --pretty false 2>&1 | rg \"workflow-store\\.ts|worker\\.ts|workflow-utils\\.ts|reconciler\\.ts\"" in /Users/kwak/Projects/paperclip/paperclip-addon succeeded in 333ms:
src/reconciler.ts(1,56): error TS2307: Cannot find module '@paperclipai/plugin-sdk' or its corresponding type declarations.
src/worker.ts(8,8): error TS2307: Cannot find module '@paperclipai/plugin-sdk' or its corresponding type declarations.
src/workflow-store.ts(1,56): error TS2307: Cannot find module '@paperclipai/plugin-sdk' or its corresponding type declarations.
src/workflow-utils.ts(1,41): error TS2307: Cannot find module '@paperclipai/plugin-sdk' or its corresponding type declarations.
file update:
diff --git a/paperclip-addon/plugins/workflow-engine/src/dag-engine.ts b/paperclip-addon/plugins/workflow-engine/src/dag-engine.ts
index b59510f0473171038be77277286d3b5c2063e6c5..e7f21ff998cab78b9325a8aeacf4fda9c5e06553
--- a/paperclip-addon/plugins/workflow-engine/src/dag-engine.ts
+++ b/paperclip-addon/plugins/workflow-engine/src/dag-engine.ts
@@ -2,6 +2,7 @@
   id: string;
   title: string;
   dependsOn: string[];
+  sessionMode?: "fresh" | "reuse";
   onFailure?: "retry" | "skip" | "abort_workflow" | "escalate";
   escalateTo?: string;
   maxRetries?: number;
diff --git a/paperclip-addon/plugins/workflow-engine/src/reconciler.ts b/paperclip-addon/plugins/workflow-engine/src/reconciler.ts
index 14ab4e766a835d145a6c2e7b1b564270285cbdc3..55b649037e8a19a88bb39b9633a6a0cc84fa8d59
--- a/paperclip-addon/plugins/workflow-engine/src/reconciler.ts
+++ b/paperclip-addon/plugins/workflow-engine/src/reconciler.ts
@@ -8,65 +8,21 @@
   listStepRuns,
   updateStepRun,
   updateWorkflowRun,
-  type WorkflowDefinition,
-  type WorkflowRun,
-  type WorkflowStepRun,
 } from "./workflow-store.js";
-import type { WorkflowStep } from "./dag-engine.js";
-
-type WorkflowRunRecord = PluginEntityRecord & { data: WorkflowRun };
-type WorkflowStepRunRecord = PluginEntityRecord & { data: WorkflowStepRun };
-type WorkflowDefinitionRecord = PluginEntityRecord & { data: WorkflowDefinition };
-type StepAgentMetadata = WorkflowStep & {
-  agent?: string;
-  agentName?: string;
-  assigneeAgentName?: string;
-};
+import {
+  TERMINAL_STEP_STATUSES,
+  findStepDefinition,
+  getStepAgentName,
+  toWorkflowDefinitionRecord,
+  toWorkflowRunRecord,
+  toWorkflowStepRunRecord,
+} from "./workflow-utils.js";
 
 const STALE_THRESHOLD_MS = 300_000;
-const TERMINAL_STEP_STATUSES = new Set<string>([
-  STEP_STATUSES.done,
-  STEP_STATUSES.failed,
-  STEP_STATUSES.skipped,
-  STEP_STATUSES.escalated,
-]);
 
 function isStale(record: PluginEntityRecord, thresholdMs: number): boolean {
   const updatedAt = new Date(record.updatedAt).getTime();
   return Date.now() - updatedAt > thresholdMs;
-}
-
-function toWorkflowRunRecord(record: PluginEntityRecord): WorkflowRunRecord {
-  return record as WorkflowRunRecord;
-}
-
-function toWorkflowStepRunRecord(record: PluginEntityRecord): WorkflowStepRunRecord {
-  return record as WorkflowStepRunRecord;
-}
-
-function toWorkflowDefinitionRecord(record: PluginEntityRecord): WorkflowDefinitionRecord {
-  return record as WorkflowDefinitionRecord;
-}
-
-function findStepDefinition(definition: WorkflowDefinitionRecord, stepId: string): WorkflowStep | null {
-  return definition.data.steps.find((step: WorkflowStep) => step.id === stepId) ?? null;
-}
-
-function getStepAgentName(stepRun: WorkflowStepRunRecord, stepDef: WorkflowStep): string | null {
-  if (typeof stepRun.data.agentName === "string" && stepRun.data.agentName.trim()) {
-    return stepRun.data.agentName.trim();
-  }
-
-  const stepMeta = stepDef as StepAgentMetadata;
-  const candidates = [stepMeta.agentName, stepMeta.agent, stepMeta.assigneeAgentName];
-
-  for (const candidate of candidates) {
-    if (typeof candidate === "string" && candidate.trim()) {
-      return candidate.trim();
-    }
-  }
-
-  return null;
 }
 
 async function invokeAgentByName(
diff --git a/paperclip-addon/plugins/workflow-engine/src/worker.ts b/paperclip-addon/plugins/workflow-engine/src/worker.ts
index c5ac3fbb4ca7f767eb29331bd3630b635449b335..303e35958890fa1f66db3f2b0ff1fa1f134102a8
--- a/paperclip-addon/plugins/workflow-engine/src/worker.ts
+++ b/paperclip-addon/plugins/workflow-engine/src/worker.ts
@@ -15,6 +15,7 @@
 } from "./dag-engine.js";
 import {
   checkIdempotency,
+  createWorkflowRun,
   createStepRun,
   findStepRunByIssueId,
   getWorkflowDefinition,
@@ -25,51 +26,38 @@
   markIdempotency,
   updateStepRun,
   updateWorkflowRun,
-  type WorkflowDefinition,
-  type WorkflowRun,
   type WorkflowStepRun,
 } from "./workflow-store.js";
+import {
+  TERMINAL_STEP_STATUSES,
+  findStepDefinition,
+  getStepAgentName,
+  getStepAgentNameHint,
+  toWorkflowDefinitionRecord,
+  toWorkflowRunRecord,
+  toWorkflowStepRunRecord,
+  type WorkflowDefinitionRecord,
+  type WorkflowRunRecord,
+  type WorkflowStepRunRecord,
+} from "./workflow-utils.js";
 
-type WorkflowStepRecord = PluginEntityRecord & { data: WorkflowStepRun };
-type WorkflowRunRecord = PluginEntityRecord & { data: WorkflowRun };
-type WorkflowDefinitionRecord = PluginEntityRecord & { data: WorkflowDefinition };
 type WorkflowStepMetadata = WorkflowStep & {
   description?: string;
-  agent?: string;
-  agentName?: string;
-  assigneeAgentName?: string;
   assigneeAgentId?: string;
+  sessionMode?: "fresh" | "reuse";
 };
 type AgentRecord = Awaited<ReturnType<PluginContext["agents"]["list"]>>[number];
+type IssueUpdatePatch = Parameters<PluginContext["issues"]["update"]>[1];
 type ReconcilerModule = {
   reconcileStuckSteps?: (ctx: PluginContext) => Promise<void>;
 };
 
-const TERMINAL_STEP_STATUSES = new Set<string>([
-  STEP_STATUSES.done,
-  STEP_STATUSES.failed,
-  STEP_STATUSES.skipped,
-  STEP_STATUSES.escalated,
-]);
-
 function summarizeError(error: unknown): string {
   if (error instanceof Error) {
     return error.message;
   }
 
   return String(error);
-}
-
-function toStepRunRecord(record: PluginEntityRecord): WorkflowStepRecord {
-  return record as WorkflowStepRecord;
-}
-
-function toWorkflowRunRecord(record: PluginEntityRecord): WorkflowRunRecord {
-  return record as WorkflowRunRecord;
-}
-
-function toWorkflowDefinitionRecord(record: PluginEntityRecord): WorkflowDefinitionRecord {
-  return record as WorkflowDefinitionRecord;
 }
 
 function buildIdempotencyKey(event: PluginEvent): string {
@@ -78,10 +66,6 @@
 
 function getStepMetadata(stepDef: WorkflowStep): WorkflowStepMetadata {
   return stepDef as WorkflowStepMetadata;
-}
-
-function findStepDefinition(definition: WorkflowDefinitionRecord, stepId: string): WorkflowStep | null {
-  return definition.data.steps.find((step: WorkflowStep) => step.id === stepId) ?? null;
 }
 
 function getStepDescription(stepDef: WorkflowStep): string | undefined {
@@ -89,19 +73,6 @@
   return typeof description === "string" && description.trim() ? description.trim() : undefined;
 }
 
-function getStepAgentNameHint(stepDef: WorkflowStep): string | null {
-  const meta = getStepMetadata(stepDef);
-  const candidates = [meta.agentName, meta.agent, meta.assigneeAgentName];
-
-  for (const candidate of candidates) {
-    if (typeof candidate === "string" && candidate.trim()) {
-      return candidate.trim();
-    }
-  }
-
-  return null;
-}
-
 function getStepAgentIdHint(stepDef: WorkflowStep): string | null {
   const assigneeAgentId = getStepMetadata(stepDef).assigneeAgentId;
   if (typeof assigneeAgentId !== "string" || !assigneeAgentId.trim()) {
@@ -147,68 +118,256 @@
 
 async function invokeAgentForStep(
   ctx: PluginContext,
+  stepRunRecord: WorkflowStepRunRecord,
   stepDef: WorkflowStep,
-  agentName: string,
   workflowName: string,
   companyId: string,
-): Promise<void> {
+): Promise<WorkflowStepRunRecord> {
+  const agentName = getStepAgentName(stepRunRecord, stepDef);
+  if (!agentName) {
+    ctx.logger.warn("Agent name is missing for workflow step", {
+      companyId,
+      runId: stepRunRecord.data.runId,
+      stepId: stepDef.id,
+      workflowName,
+    });
+    return stepRunRecord;
+  }
+
   const agents = await ctx.agents.list({ companyId });
-  const agent = agents.find((candidate: AgentRecord) => candidate.name === agentName);
+  const agent = agents.find((candidate: AgentRecord) => candidate.name === agentName) ?? null;
 
   if (!agent) {
     ctx.logger.warn("Agent not found for step", {
       agentName,
       companyId,
+      runId: stepRunRecord.data.runId,
+      stepId: stepDef.id,
+      workflowName,
+    });
+    return stepRunRecord;
+  }
+
+  const prompt = `workflow:${workflowName}/step:${stepDef.id} — "${stepDef.title}" is ready. Please proceed with the assigned task.`;
+  const reason = `workflow:${workflowName}/step:${stepDef.id}`;
+  const sessionMode = getStepMetadata(stepDef).sessionMode === "reuse" ? "reuse" : "fresh";
+
+  if (sessionMode === "reuse") {
+    let sessionId = typeof stepRunRecord.data.sessionId === "string" && stepRunRecord.data.sessionId.trim()
+      ? stepRunRecord.data.sessionId.trim()
+      : "";
+
+    if (!sessionId) {
+      const session = await ctx.agents.sessions.create(agent.id, companyId, {
+        reason,
+      });
+      sessionId = session.sessionId;
+      stepRunRecord = toWorkflowStepRunRecord(await updateStepRun(ctx, stepRunRecord.id, {
+        sessionId,
+      }));
+    }
+
+    await ctx.agents.sessions.sendMessage(sessionId, companyId, {
+      prompt,
+      reason,
+    });
+
+    ctx.logger.info("Sent workflow step prompt via agent session", {
+      agentId: agent.id,
+      agentName,
+      companyId,
+      runId: stepRunRecord.data.runId,
+      sessionId,
       stepId: stepDef.id,
       workflowName,
     });
-    return;
+    return stepRunRecord;
   }
 
   await ctx.agents.invoke(agent.id, companyId, {
-    prompt: `workflow:${workflowName}/step:${stepDef.id} — "${stepDef.title}" is ready. Please proceed with the assigned task.`,
-    reason: `workflow:${workflowName}/step:${stepDef.id}`,
+    prompt,
+    reason,
   });
 
   ctx.logger.info("Invoked agent for workflow step", {
     agentId: agent.id,
     agentName,
     companyId,
+    runId: stepRunRecord.data.runId,
     stepId: stepDef.id,
     workflowName,
   });
+
+  return stepRunRecord;
 }
 
 async function activateBacklogStep(
   ctx: PluginContext,
-  stepRunRecord: WorkflowStepRecord,
+  stepRunRecord: WorkflowStepRunRecord,
   stepDef: WorkflowStep,
   workflowName: string,
   companyId: string,
-): Promise<void> {
+  options?: { parentIssueId?: string },
+): Promise<WorkflowStepRunRecord> {
   if (stepRunRecord.data.status !== STEP_STATUSES.backlog) {
-    return;
+    return stepRunRecord;
   }
 
-  const updated = toStepRunRecord(await updateStepRun(ctx, stepRunRecord.id, {
+  const resolvedAgent = await resolveStepAgent(
+    ctx,
+    companyId,
+    stepDef,
+    stepRunRecord.data.agentName,
+  );
+
+  let issueId = typeof stepRunRecord.data.issueId === "string" && stepRunRecord.data.issueId.trim()
+    ? stepRunRecord.data.issueId.trim()
+    : "";
+  const stepDescription = getStepDescription(stepDef) ?? `Workflow step: ${stepDef.id}`;
+
+  if (!issueId) {
+    const issue = await ctx.issues.create({
+      assigneeAgentId: resolvedAgent.agentId ?? undefined,
+      companyId,
+      description: stepDescription,
+      parentId: options?.parentIssueId,
+      title: `[${workflowName}] ${stepDef.title}`,
+    });
+    await ctx.issues.update(issue.id, { status: "todo" } as IssueUpdatePatch, companyId);
+    issueId = issue.id;
+  }
+
+  const nextStartedAt = stepRunRecord.data.startedAt ?? new Date().toISOString();
+  let updatedStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, stepRunRecord.id, {
+    agentName: resolvedAgent.agentName ?? stepRunRecord.data.agentName,
+    issueId,
+    startedAt: nextStartedAt,
     status: STEP_STATUSES.todo,
   }));
 
-  await invokeAgentForStep(
+  updatedStepRun = await invokeAgentForStep(
     ctx,
+    updatedStepRun,
     stepDef,
-    updated.data.agentName,
     workflowName,
     companyId,
   );
+
+  return updatedStepRun;
 }
 
+async function startWorkflow(
+  ctx: PluginContext,
+  workflowId: string,
+  companyId: string,
+  options?: {
+    createParentIssue?: boolean;
+    parentIssueId?: string;
+  },
+): Promise<{
+  activatedStepIds: string[];
+  parentIssueId: string | null;
+  runId: string;
+  workflowId: string;
+}> {
+  const workflowDefinition = await getWorkflowDefinition(ctx, workflowId);
+  if (!workflowDefinition) {
+    throw new Error(`Workflow definition not found: ${workflowId}`);
+  }
+
+  const typedWorkflowDefinition = toWorkflowDefinitionRecord(workflowDefinition);
+  if (typedWorkflowDefinition.data.companyId !== companyId) {
+    throw new Error(`Workflow does not belong to company: ${workflowId}`);
+  }
+
+  let parentIssueId = typeof options?.parentIssueId === "string" && options.parentIssueId.trim()
+    ? options.parentIssueId.trim()
+    : "";
+  if (!parentIssueId && options?.createParentIssue) {
+    const parentIssue = await ctx.issues.create({
+      companyId,
+      description: typedWorkflowDefinition.data.description || `Workflow run: ${typedWorkflowDefinition.data.name}`,
+      title: `[${typedWorkflowDefinition.data.name}] Workflow run`,
+    });
+    parentIssueId = parentIssue.id;
+  }
+
+  const workflowRun = toWorkflowRunRecord(await createWorkflowRun(ctx, {
+    companyId,
+    parentIssueId: parentIssueId || undefined,
+    startedAt: new Date().toISOString(),
+    status: RUN_STATUSES.running,
+    workflowId: typedWorkflowDefinition.id,
+    workflowName: typedWorkflowDefinition.data.name,
+  }));
+
+  const agents = await ctx.agents.list({ companyId });
+  const agentsByName = new Map<string, AgentRecord>();
+  for (const agent of agents) {
+    agentsByName.set(agent.name, agent);
+  }
+
+  const pendingRootSteps: Array<{ stepDef: WorkflowStep; stepRun: WorkflowStepRunRecord }> = [];
+  for (const stepDef of typedWorkflowDefinition.data.steps) {
+    const agentNameHint = getStepAgentNameHint(stepDef);
+    const matchedAgent = agentNameHint ? agentsByName.get(agentNameHint) ?? null : null;
+    const resolvedAgent = matchedAgent
+      ? { agentId: matchedAgent.id, agentName: matchedAgent.name }
+      : await resolveStepAgent(ctx, companyId, stepDef, agentNameHint ?? undefined);
+
+    if (!resolvedAgent.agentName) {
+      throw new Error(`Unable to resolve step assignee for "${stepDef.id}"`);
+    }
+
+    const stepRun = toWorkflowStepRunRecord(await createStepRun(ctx, companyId, {
+      agentName: resolvedAgent.agentName,
+      retryCount: 0,
+      runId: workflowRun.id,
+      status: STEP_STATUSES.backlog,
+      stepId: stepDef.id,
+    }));
+
+    if (stepDef.dependsOn.length === 0 && stepDef.triggerOn !== "escalation") {
+      pendingRootSteps.push({ stepDef, stepRun });
+    }
+  }
+
+  const activatedStepIds: string[] = [];
+  for (const pending of pendingRootSteps) {
+    await activateBacklogStep(
+      ctx,
+      pending.stepRun,
+      pending.stepDef,
+      typedWorkflowDefinition.data.name,
+      companyId,
+      { parentIssueId: parentIssueId || undefined },
+    );
+    activatedStepIds.push(pending.stepDef.id);
+  }
+
+  ctx.logger.info("Started workflow run", {
+    activatedStepIds,
+    companyId,
+    parentIssueId: parentIssueId || null,
+    runId: workflowRun.id,
+    workflowId: typedWorkflowDefinition.id,
+    workflowName: typedWorkflowDefinition.data.name,
+  });
+
+  return {
+    activatedStepIds,
+    parentIssueId: parentIssueId || null,
+    runId: workflowRun.id,
+    workflowId: typedWorkflowDefinition.id,
+  };
+}
+
 async function advanceWorkflow(
   ctx: PluginContext,
   stepRunRecord: PluginEntityRecord,
   companyId: string,
 ): Promise<void> {
-  const completedStepRun = toStepRunRecord(stepRunRecord);
+  const completedStepRun = toWorkflowStepRunRecord(stepRunRecord);
   const workflowRun = await getWorkflowRun(ctx, completedStepRun.data.runId);
 
   if (!workflowRun) {
@@ -237,7 +396,7 @@
   }
 
   const typedWorkflowDefinition = toWorkflowDefinitionRecord(workflowDefinition);
-  const stepRuns = (await listStepRuns(ctx, typedWorkflowRun.id, companyId)).map(toStepRunRecord);
+  const stepRuns = (await listStepRuns(ctx, typedWorkflowRun.id, companyId)).map(toWorkflowStepRunRecord);
   const completed = new Set<string>();
   const failed = new Set<string>();
   const skipped = new Set<string>();
@@ -289,7 +448,7 @@
         continue;
       }
 
-      stepRun = toStepRunRecord(await createStepRun(ctx, companyId, {
+      stepRun = toWorkflowStepRunRecord(await createStepRun(ctx, companyId, {
         agentName: resolvedAgent.agentName,
         retryCount: 0,
         runId: typedWorkflowRun.id,
@@ -305,6 +464,7 @@
       stepDef,
       typedWorkflowRun.data.workflowName,
       companyId,
+      { parentIssueId: typedWorkflowRun.data.parentIssueId },
     );
   }
 
@@ -329,7 +489,7 @@
   ctx: PluginContext,
   workflowRun: WorkflowRunRecord,
   workflowDefinition: WorkflowDefinitionRecord,
-  sourceStepRun: WorkflowStepRecord,
+  sourceStepRun: WorkflowStepRunRecord,
   escalationTargetId: string,
   companyId: string,
 ): Promise<void> {
@@ -338,7 +498,7 @@
     throw new Error(`Escalation target step not found: ${escalationTargetId}`);
   }
 
-  const stepRuns = (await listStepRuns(ctx, workflowRun.id, companyId)).map(toStepRunRecord);
+  const stepRuns = (await listStepRuns(ctx, workflowRun.id, companyId)).map(toWorkflowStepRunRecord);
   let escalationStepRun = stepRuns.find((candidate) => candidate.data.stepId === escalationTargetId) ?? null;
   const resolvedAgent = await resolveStepAgent(
     ctx,
@@ -352,7 +512,7 @@
   }
 
   if (!escalationStepRun) {
-    escalationStepRun = toStepRunRecord(await createStepRun(ctx, companyId, {
+    escalationStepRun = toWorkflowStepRunRecord(await createStepRun(ctx, companyId, {
       agentName: resolvedAgent.agentName,
       retryCount: 0,
       runId: workflowRun.id,
@@ -374,14 +534,14 @@
       title: `${workflowRun.data.workflowName}: ${escalationStep.title}`,
     });
 
-    escalationStepRun = toStepRunRecord(await updateStepRun(ctx, escalationStepRun.id, {
+    escalationStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, escalationStepRun.id, {
       issueId: issue.id,
     }));
   }
 
   const shouldActivate = escalationStepRun.data.status === STEP_STATUSES.backlog;
   if (shouldActivate) {
-    escalationStepRun = toStepRunRecord(await updateStepRun(ctx, escalationStepRun.id, {
+    escalationStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, escalationStepRun.id, {
       status: STEP_STATUSES.todo,
     }));
   }
@@ -389,8 +549,8 @@
   if (shouldActivate) {
     await invokeAgentForStep(
       ctx,
+      escalationStepRun,
       escalationStep,
-      escalationStepRun.data.agentName,
       workflowRun.data.workflowName,
       companyId,
     );
@@ -421,7 +581,7 @@
     return;
   }
 
-  const typedStepRun = toStepRunRecord(stepRunRecord);
+  const typedStepRun = toWorkflowStepRunRecord(stepRunRecord);
   if (TERMINAL_STEP_STATUSES.has(typedStepRun.data.status)) {
     return;
   }
@@ -469,7 +629,7 @@
   if (options.allowRetry) {
     const retryInfo = getRetryInfo(typedWorkflowDefinition.data.steps, typedStepRun.data.stepId);
     if (retryInfo.shouldRetry && typedStepRun.data.retryCount < retryInfo.maxRetries) {
-      const retriedStepRun = toStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
+      const retriedStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
         completedAt: undefined,
         retryCount: typedStepRun.data.retryCount + 1,
         status: STEP_STATUSES.todo,
@@ -477,8 +637,8 @@
 
       await invokeAgentForStep(
         ctx,
+        retriedStepRun,
         stepDef,
-        retriedStepRun.data.agentName,
         typedWorkflowRun.data.workflowName,
         event.companyId,
       );
@@ -496,7 +656,7 @@
   }
 
   if (stepDef.onFailure === "skip") {
-    const skippedStepRun = toStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
+    const skippedStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
       completedAt: new Date().toISOString(),
       status: STEP_STATUSES.skipped,
     }));
@@ -556,7 +716,7 @@
       return;
     }
 
-    const escalatedStepRun = toStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
+    const escalatedStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
       completedAt: new Date().toISOString(),
       status: STEP_STATUSES.escalated,
     }));
@@ -617,6 +777,29 @@
   }
 }
 
+function registerDataHandler(
+  ctx: PluginContext,
+  key: string,
+  handler: (params: Record<string, unknown>) => Promise<unknown>,
+): void {
+  const dataClient = ctx.data as {
+    handle?: (handlerKey: string, handlerFn: (params: Record<string, unknown>) => Promise<unknown>) => void;
+    register?: (handlerKey: string, handlerFn: (params: Record<string, unknown>) => Promise<unknown>) => void;
+  };
+
+  if (typeof dataClient.handle === "function") {
+    dataClient.handle(key, handler);
+    return;
+  }
+
+  if (typeof dataClient.register === "function") {
+    dataClient.register(key, handler);
+    return;
+  }
+
+  throw new Error("Plugin data client does not support handler registration");
+}
+
 const plugin = definePlugin({
   async setup(ctx: PluginContext) {
     ctx.events.on("issue.updated", async (event: PluginEvent) => {
@@ -638,7 +821,7 @@
           return;
         }
 
-        const stepRun = toStepRunRecord(stepRunRecord);
+        const stepRun = toWorkflowStepRunRecord(stepRunRecord);
         const payload = event.payload as { status?: string };
         const issueStatus = typeof payload.status === "string" ? payload.status : undefined;
 
@@ -657,7 +840,7 @@
           return;
         }
 
-        const updatedStepRun = toStepRunRecord(await updateStepRun(ctx, stepRun.id, {
+        const updatedStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, stepRun.id, {
           completedAt: nextStepStatus === STEP_STATUSES.done ? new Date().toISOString() : undefined,
           startedAt: nextStepStatus === STEP_STATUSES.inProgress
             ? stepRun.data.startedAt ?? new Date().toISOString()
@@ -687,11 +870,21 @@
 
     ctx.events.on("issue.created", async (event: PluginEvent) => {
       try {
+        const idempotencyKey = buildIdempotencyKey(event);
+        if (await checkIdempotency(ctx, idempotencyKey, event.companyId)) {
+          return;
+        }
+
         const payload = event.payload as {
           id?: string;
           parentId?: string;
           assigneeAgentId?: string;
         };
+        const issueId = typeof event.entityId === "string" && event.entityId.trim()
+          ? event.entityId.trim()
+          : typeof payload.id === "string" && payload.id.trim()
+            ? payload.id.trim()
+            : "";
 
         const parentId = typeof payload.parentId === "string" && payload.parentId.trim()
           ? payload.parentId.trim()
@@ -700,12 +893,34 @@
           ? payload.assigneeAgentId.trim()
           : "";
 
-        if (parentId || !assigneeAgentId) {
+        if (!issueId || parentId || !assigneeAgentId) {
+          await markIdempotency(ctx, idempotencyKey, event.companyId);
+          return;
+        }
+
+        const agent = await ctx.agents.get(assigneeAgentId, event.companyId);
+        const metadata = agent?.metadata as Record<string, unknown> | undefined;
+        const defaultParentIssueId = typeof metadata?.defaultParentIssueId === "string" && metadata.defaultParentIssueId.trim()
+          ? metadata.defaultParentIssueId.trim()
+          : "";
+
+        if (!defaultParentIssueId) {
+          await markIdempotency(ctx, idempotencyKey, event.companyId);
           return;
         }
 
-        ctx.logger.info("parentId filler: issue created without parent", {
-          issueId: event.entityId ?? payload.id ?? null,
+        await ctx.issues.update(
+          issueId,
+          { parentId: defaultParentIssueId } as IssueUpdatePatch,
+          event.companyId,
+        );
+        await markIdempotency(ctx, idempotencyKey, event.companyId);
+
+        ctx.logger.info("parentId filler: populated issue parentId from agent metadata", {
+          assigneeAgentId,
+          companyId: event.companyId,
+          issueId,
+          parentId: defaultParentIssueId,
         });
       } catch (error) {
         ctx.logger.warn("Failed to handle issue.created event", {
@@ -744,7 +959,23 @@
       await runReconciler(ctx);
     });
 
-    ctx.data.register("workflow-overview", async (params: Record<string, unknown>) => {
+    registerDataHandler(ctx, "start-workflow", async (params: Record<string, unknown>) => {
+      const workflowId = typeof params.workflowId === "string" ? params.workflowId.trim() : "";
+      const companyId = typeof params.companyId === "string" ? params.companyId.trim() : "";
+      const parentIssueId = typeof params.parentIssueId === "string" ? params.parentIssueId.trim() : "";
+      const createParentIssue = params.createParentIssue === true;
+
+      if (!workflowId || !companyId) {
+        throw new Error("start-workflow requires workflowId and companyId");
+      }
+
+      return await startWorkflow(ctx, workflowId, companyId, {
+        createParentIssue,
+        parentIssueId: parentIssueId || undefined,
+      });
+    });
+
+    registerDataHandler(ctx, "workflow-overview", async (params: Record<string, unknown>) => {
       try {
         const companyId = typeof params.companyId === "string" ? params.companyId.trim() : "";
         if (!companyId) {
@@ -782,7 +1013,7 @@
       }
     });
 
-    ctx.data.register("workflow-run-detail", async (params: Record<string, unknown>) => {
+    registerDataHandler(ctx, "workflow-run-detail", async (params: Record<string, unknown>) => {
       try {
         const runId = typeof params.runId === "string" ? params.runId.trim() : "";
         if (!runId) {
@@ -807,7 +1038,7 @@
             ...typedWorkflowRun.data,
           },
           stepRuns: stepRuns.map((record: PluginEntityRecord) => {
-            const stepRun = toStepRunRecord(record);
+            const stepRun = toWorkflowStepRunRecord(record);
             return {
               id: stepRun.id,
               status: stepRun.status,
diff --git a/paperclip-addon/plugins/workflow-engine/src/workflow-store.ts b/paperclip-addon/plugins/workflow-engine/src/workflow-store.ts
index 5871a2082258cebcfe8b69ebafbb0814cf303d50..27791ef142b641d85b6ef7e35a6746218a64d941
--- a/paperclip-addon/plugins/workflow-engine/src/workflow-store.ts
+++ b/paperclip-addon/plugins/workflow-engine/src/workflow-store.ts
@@ -44,7 +44,14 @@
 
 type TypedEntityRecord<T> = Omit<PluginEntityRecord, "data"> & { data: T };
 
-function toTypedRecord<T>(record: PluginEntityRecord): TypedEntityRecord<T> {
+function toTypedRecord<T>(
+  record: PluginEntityRecord,
+  entityType: string,
+): TypedEntityRecord<T> {
+  if (record.entityType !== entityType) {
+    throw new Error(`Expected entity type "${entityType}", got "${record.entityType}"`);
+  }
+
   return record as TypedEntityRecord<T>;
 }
 
@@ -67,13 +74,39 @@
   id: string,
   entityType: string,
 ): Promise<TypedEntityRecord<T> | null> {
+  const listQuery = {
+    entityType,
+    id,
+    limit: 1,
+  } as Record<string, unknown>;
+
+  const listed = await ctx.entities.list(
+    listQuery as Parameters<PluginContext["entities"]["list"]>[0],
+  );
+  const listedRecord = listed.find(
+    (record: PluginEntityRecord) => record.id === id,
+  ) ?? null;
+
+  if (listedRecord) {
+    if (listedRecord.entityType !== entityType) {
+      return null;
+    }
+
+    return toTypedRecord<T>(listedRecord, entityType);
+  }
+
+  // Fallback: some host SDK variants ignore `id` on entities.list().
+  // Keep defensive type verification on direct get() until id-filter support is universal.
   const record = await ctx.entities.get(id);
+  if (!record) {
+    return null;
+  }
 
-  if (!record || record.entityType !== entityType) {
+  if (record.entityType !== entityType) {
     return null;
   }
 
-  return toTypedRecord<T>(record);
+  return toTypedRecord<T>(record, entityType);
 }
 
 async function requireEntityByType<T>(
diff --git a/paperclip-addon/plugins/workflow-engine/src/workflow-utils.ts b/paperclip-addon/plugins/workflow-engine/src/workflow-utils.ts
new file mode 100644
index 0000000000000000000000000000000000000000..ec1cf6f6b9aa722b126b58c0a728b4e538ad1494
--- /dev/null
+++ b/paperclip-addon/plugins/workflow-engine/src/workflow-utils.ts
@@ -0,0 +1,62 @@
+import type { PluginEntityRecord } from "@paperclipai/plugin-sdk";
+
+import { STEP_STATUSES } from "./constants.js";
+import type { WorkflowStep } from "./dag-engine.js";
+import type {
+  WorkflowDefinition,
+  WorkflowRun,
+  WorkflowStepRun,
+} from "./workflow-store.js";
+
+export type WorkflowRunRecord = PluginEntityRecord & { data: WorkflowRun };
+export type WorkflowStepRunRecord = PluginEntityRecord & { data: WorkflowStepRun };
+export type WorkflowDefinitionRecord = PluginEntityRecord & { data: WorkflowDefinition };
+type StepAgentMetadata = WorkflowStep & {
+  agent?: string;
+  agentName?: string;
+  assigneeAgentName?: string;
+};
+
+export const TERMINAL_STEP_STATUSES = new Set<string>([
+  STEP_STATUSES.done,
+  STEP_STATUSES.failed,
+  STEP_STATUSES.skipped,
+  STEP_STATUSES.escalated,
+]);
+
+export function toWorkflowRunRecord(record: PluginEntityRecord): WorkflowRunRecord {
+  return record as WorkflowRunRecord;
+}
+
+export function toWorkflowStepRunRecord(record: PluginEntityRecord): WorkflowStepRunRecord {
+  return record as WorkflowStepRunRecord;
+}
+
+export function toWorkflowDefinitionRecord(record: PluginEntityRecord): WorkflowDefinitionRecord {
+  return record as WorkflowDefinitionRecord;
+}
+
+export function findStepDefinition(definition: WorkflowDefinitionRecord, stepId: string): WorkflowStep | null {
+  return definition.data.steps.find((step: WorkflowStep) => step.id === stepId) ?? null;
+}
+
+export function getStepAgentNameHint(stepDef: WorkflowStep): string | null {
+  const stepMeta = stepDef as StepAgentMetadata;
+  const candidates = [stepMeta.agentName, stepMeta.agent, stepMeta.assigneeAgentName];
+
+  for (const candidate of candidates) {
+    if (typeof candidate === "string" && candidate.trim()) {
+      return candidate.trim();
+    }
+  }
+
+  return null;
+}
+
+export function getStepAgentName(stepRun: WorkflowStepRunRecord, stepDef: WorkflowStep): string | null {
+  if (typeof stepRun.data.agentName === "string" && stepRun.data.agentName.trim()) {
+    return stepRun.data.agentName.trim();
+  }
+
+  return getStepAgentNameHint(stepDef);
+}

codex
요청하신 코드리뷰 반영사항 7건을 모두 적용했습니다.

1. `activateBacklogStep()` 이슈 생성 추가  
- [worker.ts](/Users/kwak/Projects/paperclip/paperclip-addon/plugins/workflow-engine/src/worker.ts)  
- backlog step 활성화 시 `ctx.issues.create(...)`로 이슈 생성 후 `issueId`, `status: todo`, `startedAt`을 `stepRun`에 저장하도록 변경했습니다.  
- 생성 직후 `ctx.issues.update(..., { status: "todo" })`도 넣어 상태를 명시적으로 맞췄습니다.

2. `startWorkflow()` 함수 추가 + data handler 등록  
- [worker.ts](/Users/kwak/Projects/paperclip/paperclip-addon/plugins/workflow-engine/src/worker.ts)  
- `startWorkflow(ctx, workflowId, companyId, options)` 추가:  
  - `WorkflowRun` 생성 (`running`)  
  - 모든 step에 대해 `WorkflowStepRun` 생성 (`backlog`)  
  - `ctx.agents.list` 기반 name→agent resolve  
  - `dependsOn` 없는 일반 step 즉시 `activateBacklogStep()` 호출  
  - 옵션으로 parent issue 생성(`createParentIssue`) 및 연결  
- data handler `start-workflow` 등록 완료.  
- `ctx.data.handle`/`ctx.data.register` 모두 대응하도록 `registerDataHandler()` 유틸을 추가했습니다.

3. `ctx.entities.get(id)` 안전 조회 패턴 개선  
- [workflow-store.ts](/Users/kwak/Projects/paperclip/paperclip-addon/plugins/workflow-engine/src/workflow-store.ts)  
- `getEntityByType()`에서 우선 `ctx.entities.list({ entityType, id, limit: 1 })` 패턴 사용, 미지원 SDK 대비 `get(id)` fallback + 한계 주석 추가했습니다.

4. 중복 헬퍼 shared utils 추출  
- 신규 [workflow-utils.ts](/Users/kwak/Projects/paperclip/paperclip-addon/plugins/workflow-engine/src/workflow-utils.ts)  
- `toWorkflowRunRecord`, `toWorkflowStepRunRecord`, `toWorkflowDefinitionRecord`, `findStepDefinition`, `getStepAgentName`, `getStepAgentNameHint`, `TERMINAL_STEP_STATUSES` 추출.  
- [worker.ts](/Users/kwak/Projects/paperclip/paperclip-addon/plugins/workflow-engine/src/worker.ts), [reconciler.ts](/Users/kwak/Projects/paperclip/paperclip-addon/plugins/workflow-engine/src/reconciler.ts)에서 import로 교체.

5. parentId filler 구현  
- [worker.ts](/Users/kwak/Projects/paperclip/paperclip-addon/plugins/workflow-engine/src/worker.ts)  
- `issue.created` 핸들러에서:  
  - parentId 없고 assigneeAgentId 있으면  
  - `ctx.agents.get(agentId, companyId)`의 `metadata.defaultParentIssueId` 확인  
  - `ctx.issues.update(issueId, { parentId }, companyId)` 수행  
- idempotency key 체크/마킹도 추가했습니다.

6. Agent Sessions 연동  
- [worker.ts](/Users/kwak/Projects/paperclip/paperclip-addon/plugins/workflow-engine/src/worker.ts), [dag-engine.ts](/Users/kwak/Projects/paperclip/paperclip-addon/plugins/workflow-engine/src/dag-engine.ts)  
- `WorkflowStep.sessionMode?: "fresh" | "reuse"` 추가.  
- `invokeAgentForStep()`에서:  
  - `fresh`(기본): `ctx.agents.invoke`  
  - `reuse`: 기존 `stepRun.sessionId` 있으면 `ctx.agents.sessions.sendMessage`, 없으면 `sessions.create` 후 `sessionId` 저장하고 `sendMessage`

7. 타입 캐스팅 정리 (`toTypedRecord` guard)  
- [workflow-store.ts](/Users/kwak/Projects/paperclip/paperclip-addon/plugins/workflow-engine/src/workflow-store.ts)  
- `toTypedRecord<T>(record, entityType)`로 변경하고 runtime guard 추가했습니다.

검증 결과  
- `npm run typecheck`, `npm run build` 실행 시 현재 환경에 `@paperclipai/plugin-sdk`, `react` 타입 의존성이 없어 실패했습니다.  
- 다만 이번 변경으로 인한 추가 TS 에러(로컬 변경 코드 관련)는 제거되어, 남은 오류는 의존성 미설치 오류만 확인되었습니다.

다음 단계  
1. 워크스페이스 의존성 설치/연결 후 `plugins/workflow-engine`에서 `npm run typecheck && npm run build` 재검증.
file update:
diff --git a/paperclip-addon/plugins/workflow-engine/src/dag-engine.ts b/paperclip-addon/plugins/workflow-engine/src/dag-engine.ts
index b59510f0473171038be77277286d3b5c2063e6c5..e7f21ff998cab78b9325a8aeacf4fda9c5e06553
--- a/paperclip-addon/plugins/workflow-engine/src/dag-engine.ts
+++ b/paperclip-addon/plugins/workflow-engine/src/dag-engine.ts
@@ -2,6 +2,7 @@
   id: string;
   title: string;
   dependsOn: string[];
+  sessionMode?: "fresh" | "reuse";
   onFailure?: "retry" | "skip" | "abort_workflow" | "escalate";
   escalateTo?: string;
   maxRetries?: number;
diff --git a/paperclip-addon/plugins/workflow-engine/src/reconciler.ts b/paperclip-addon/plugins/workflow-engine/src/reconciler.ts
index 14ab4e766a835d145a6c2e7b1b564270285cbdc3..55b649037e8a19a88bb39b9633a6a0cc84fa8d59
--- a/paperclip-addon/plugins/workflow-engine/src/reconciler.ts
+++ b/paperclip-addon/plugins/workflow-engine/src/reconciler.ts
@@ -8,65 +8,21 @@
   listStepRuns,
   updateStepRun,
   updateWorkflowRun,
-  type WorkflowDefinition,
-  type WorkflowRun,
-  type WorkflowStepRun,
 } from "./workflow-store.js";
-import type { WorkflowStep } from "./dag-engine.js";
-
-type WorkflowRunRecord = PluginEntityRecord & { data: WorkflowRun };
-type WorkflowStepRunRecord = PluginEntityRecord & { data: WorkflowStepRun };
-type WorkflowDefinitionRecord = PluginEntityRecord & { data: WorkflowDefinition };
-type StepAgentMetadata = WorkflowStep & {
-  agent?: string;
-  agentName?: string;
-  assigneeAgentName?: string;
-};
+import {
+  TERMINAL_STEP_STATUSES,
+  findStepDefinition,
+  getStepAgentName,
+  toWorkflowDefinitionRecord,
+  toWorkflowRunRecord,
+  toWorkflowStepRunRecord,
+} from "./workflow-utils.js";
 
 const STALE_THRESHOLD_MS = 300_000;
-const TERMINAL_STEP_STATUSES = new Set<string>([
-  STEP_STATUSES.done,
-  STEP_STATUSES.failed,
-  STEP_STATUSES.skipped,
-  STEP_STATUSES.escalated,
-]);
 
 function isStale(record: PluginEntityRecord, thresholdMs: number): boolean {
   const updatedAt = new Date(record.updatedAt).getTime();
   return Date.now() - updatedAt > thresholdMs;
-}
-
-function toWorkflowRunRecord(record: PluginEntityRecord): WorkflowRunRecord {
-  return record as WorkflowRunRecord;
-}
-
-function toWorkflowStepRunRecord(record: PluginEntityRecord): WorkflowStepRunRecord {
-  return record as WorkflowStepRunRecord;
-}
-
-function toWorkflowDefinitionRecord(record: PluginEntityRecord): WorkflowDefinitionRecord {
-  return record as WorkflowDefinitionRecord;
-}
-
-function findStepDefinition(definition: WorkflowDefinitionRecord, stepId: string): WorkflowStep | null {
-  return definition.data.steps.find((step: WorkflowStep) => step.id === stepId) ?? null;
-}
-
-function getStepAgentName(stepRun: WorkflowStepRunRecord, stepDef: WorkflowStep): string | null {
-  if (typeof stepRun.data.agentName === "string" && stepRun.data.agentName.trim()) {
-    return stepRun.data.agentName.trim();
-  }
-
-  const stepMeta = stepDef as StepAgentMetadata;
-  const candidates = [stepMeta.agentName, stepMeta.agent, stepMeta.assigneeAgentName];
-
-  for (const candidate of candidates) {
-    if (typeof candidate === "string" && candidate.trim()) {
-      return candidate.trim();
-    }
-  }
-
-  return null;
 }
 
 async function invokeAgentByName(
diff --git a/paperclip-addon/plugins/workflow-engine/src/worker.ts b/paperclip-addon/plugins/workflow-engine/src/worker.ts
index c5ac3fbb4ca7f767eb29331bd3630b635449b335..303e35958890fa1f66db3f2b0ff1fa1f134102a8
--- a/paperclip-addon/plugins/workflow-engine/src/worker.ts
+++ b/paperclip-addon/plugins/workflow-engine/src/worker.ts
@@ -15,6 +15,7 @@
 } from "./dag-engine.js";
 import {
   checkIdempotency,
+  createWorkflowRun,
   createStepRun,
   findStepRunByIssueId,
   getWorkflowDefinition,
@@ -25,51 +26,38 @@
   markIdempotency,
   updateStepRun,
   updateWorkflowRun,
-  type WorkflowDefinition,
-  type WorkflowRun,
   type WorkflowStepRun,
 } from "./workflow-store.js";
+import {
+  TERMINAL_STEP_STATUSES,
+  findStepDefinition,
+  getStepAgentName,
+  getStepAgentNameHint,
+  toWorkflowDefinitionRecord,
+  toWorkflowRunRecord,
+  toWorkflowStepRunRecord,
+  type WorkflowDefinitionRecord,
+  type WorkflowRunRecord,
+  type WorkflowStepRunRecord,
+} from "./workflow-utils.js";
 
-type WorkflowStepRecord = PluginEntityRecord & { data: WorkflowStepRun };
-type WorkflowRunRecord = PluginEntityRecord & { data: WorkflowRun };
-type WorkflowDefinitionRecord = PluginEntityRecord & { data: WorkflowDefinition };
 type WorkflowStepMetadata = WorkflowStep & {
   description?: string;
-  agent?: string;
-  agentName?: string;
-  assigneeAgentName?: string;
   assigneeAgentId?: string;
+  sessionMode?: "fresh" | "reuse";
 };
 type AgentRecord = Awaited<ReturnType<PluginContext["agents"]["list"]>>[number];
+type IssueUpdatePatch = Parameters<PluginContext["issues"]["update"]>[1];
 type ReconcilerModule = {
   reconcileStuckSteps?: (ctx: PluginContext) => Promise<void>;
 };
 
-const TERMINAL_STEP_STATUSES = new Set<string>([
-  STEP_STATUSES.done,
-  STEP_STATUSES.failed,
-  STEP_STATUSES.skipped,
-  STEP_STATUSES.escalated,
-]);
-
 function summarizeError(error: unknown): string {
   if (error instanceof Error) {
     return error.message;
   }
 
   return String(error);
-}
-
-function toStepRunRecord(record: PluginEntityRecord): WorkflowStepRecord {
-  return record as WorkflowStepRecord;
-}
-
-function toWorkflowRunRecord(record: PluginEntityRecord): WorkflowRunRecord {
-  return record as WorkflowRunRecord;
-}
-
-function toWorkflowDefinitionRecord(record: PluginEntityRecord): WorkflowDefinitionRecord {
-  return record as WorkflowDefinitionRecord;
 }
 
 function buildIdempotencyKey(event: PluginEvent): string {
@@ -78,10 +66,6 @@
 
 function getStepMetadata(stepDef: WorkflowStep): WorkflowStepMetadata {
   return stepDef as WorkflowStepMetadata;
-}
-
-function findStepDefinition(definition: WorkflowDefinitionRecord, stepId: string): WorkflowStep | null {
-  return definition.data.steps.find((step: WorkflowStep) => step.id === stepId) ?? null;
 }
 
 function getStepDescription(stepDef: WorkflowStep): string | undefined {
@@ -89,19 +73,6 @@
   return typeof description === "string" && description.trim() ? description.trim() : undefined;
 }
 
-function getStepAgentNameHint(stepDef: WorkflowStep): string | null {
-  const meta = getStepMetadata(stepDef);
-  const candidates = [meta.agentName, meta.agent, meta.assigneeAgentName];
-
-  for (const candidate of candidates) {
-    if (typeof candidate === "string" && candidate.trim()) {
-      return candidate.trim();
-    }
-  }
-
-  return null;
-}
-
 function getStepAgentIdHint(stepDef: WorkflowStep): string | null {
   const assigneeAgentId = getStepMetadata(stepDef).assigneeAgentId;
   if (typeof assigneeAgentId !== "string" || !assigneeAgentId.trim()) {
@@ -147,68 +118,256 @@
 
 async function invokeAgentForStep(
   ctx: PluginContext,
+  stepRunRecord: WorkflowStepRunRecord,
   stepDef: WorkflowStep,
-  agentName: string,
   workflowName: string,
   companyId: string,
-): Promise<void> {
+): Promise<WorkflowStepRunRecord> {
+  const agentName = getStepAgentName(stepRunRecord, stepDef);
+  if (!agentName) {
+    ctx.logger.warn("Agent name is missing for workflow step", {
+      companyId,
+      runId: stepRunRecord.data.runId,
+      stepId: stepDef.id,
+      workflowName,
+    });
+    return stepRunRecord;
+  }
+
   const agents = await ctx.agents.list({ companyId });
-  const agent = agents.find((candidate: AgentRecord) => candidate.name === agentName);
+  const agent = agents.find((candidate: AgentRecord) => candidate.name === agentName) ?? null;
 
   if (!agent) {
     ctx.logger.warn("Agent not found for step", {
       agentName,
       companyId,
+      runId: stepRunRecord.data.runId,
+      stepId: stepDef.id,
+      workflowName,
+    });
+    return stepRunRecord;
+  }
+
+  const prompt = `workflow:${workflowName}/step:${stepDef.id} — "${stepDef.title}" is ready. Please proceed with the assigned task.`;
+  const reason = `workflow:${workflowName}/step:${stepDef.id}`;
+  const sessionMode = getStepMetadata(stepDef).sessionMode === "reuse" ? "reuse" : "fresh";
+
+  if (sessionMode === "reuse") {
+    let sessionId = typeof stepRunRecord.data.sessionId === "string" && stepRunRecord.data.sessionId.trim()
+      ? stepRunRecord.data.sessionId.trim()
+      : "";
+
+    if (!sessionId) {
+      const session = await ctx.agents.sessions.create(agent.id, companyId, {
+        reason,
+      });
+      sessionId = session.sessionId;
+      stepRunRecord = toWorkflowStepRunRecord(await updateStepRun(ctx, stepRunRecord.id, {
+        sessionId,
+      }));
+    }
+
+    await ctx.agents.sessions.sendMessage(sessionId, companyId, {
+      prompt,
+      reason,
+    });
+
+    ctx.logger.info("Sent workflow step prompt via agent session", {
+      agentId: agent.id,
+      agentName,
+      companyId,
+      runId: stepRunRecord.data.runId,
+      sessionId,
       stepId: stepDef.id,
       workflowName,
     });
-    return;
+    return stepRunRecord;
   }
 
   await ctx.agents.invoke(agent.id, companyId, {
-    prompt: `workflow:${workflowName}/step:${stepDef.id} — "${stepDef.title}" is ready. Please proceed with the assigned task.`,
-    reason: `workflow:${workflowName}/step:${stepDef.id}`,
+    prompt,
+    reason,
   });
 
   ctx.logger.info("Invoked agent for workflow step", {
     agentId: agent.id,
     agentName,
     companyId,
+    runId: stepRunRecord.data.runId,
     stepId: stepDef.id,
     workflowName,
   });
+
+  return stepRunRecord;
 }
 
 async function activateBacklogStep(
   ctx: PluginContext,
-  stepRunRecord: WorkflowStepRecord,
+  stepRunRecord: WorkflowStepRunRecord,
   stepDef: WorkflowStep,
   workflowName: string,
   companyId: string,
-): Promise<void> {
+  options?: { parentIssueId?: string },
+): Promise<WorkflowStepRunRecord> {
   if (stepRunRecord.data.status !== STEP_STATUSES.backlog) {
-    return;
+    return stepRunRecord;
   }
 
-  const updated = toStepRunRecord(await updateStepRun(ctx, stepRunRecord.id, {
+  const resolvedAgent = await resolveStepAgent(
+    ctx,
+    companyId,
+    stepDef,
+    stepRunRecord.data.agentName,
+  );
+
+  let issueId = typeof stepRunRecord.data.issueId === "string" && stepRunRecord.data.issueId.trim()
+    ? stepRunRecord.data.issueId.trim()
+    : "";
+  const stepDescription = getStepDescription(stepDef) ?? `Workflow step: ${stepDef.id}`;
+
+  if (!issueId) {
+    const issue = await ctx.issues.create({
+      assigneeAgentId: resolvedAgent.agentId ?? undefined,
+      companyId,
+      description: stepDescription,
+      parentId: options?.parentIssueId,
+      title: `[${workflowName}] ${stepDef.title}`,
+    });
+    await ctx.issues.update(issue.id, { status: "todo" } as IssueUpdatePatch, companyId);
+    issueId = issue.id;
+  }
+
+  const nextStartedAt = stepRunRecord.data.startedAt ?? new Date().toISOString();
+  let updatedStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, stepRunRecord.id, {
+    agentName: resolvedAgent.agentName ?? stepRunRecord.data.agentName,
+    issueId,
+    startedAt: nextStartedAt,
     status: STEP_STATUSES.todo,
   }));
 
-  await invokeAgentForStep(
+  updatedStepRun = await invokeAgentForStep(
     ctx,
+    updatedStepRun,
     stepDef,
-    updated.data.agentName,
     workflowName,
     companyId,
   );
+
+  return updatedStepRun;
 }
 
+async function startWorkflow(
+  ctx: PluginContext,
+  workflowId: string,
+  companyId: string,
+  options?: {
+    createParentIssue?: boolean;
+    parentIssueId?: string;
+  },
+): Promise<{
+  activatedStepIds: string[];
+  parentIssueId: string | null;
+  runId: string;
+  workflowId: string;
+}> {
+  const workflowDefinition = await getWorkflowDefinition(ctx, workflowId);
+  if (!workflowDefinition) {
+    throw new Error(`Workflow definition not found: ${workflowId}`);
+  }
+
+  const typedWorkflowDefinition = toWorkflowDefinitionRecord(workflowDefinition);
+  if (typedWorkflowDefinition.data.companyId !== companyId) {
+    throw new Error(`Workflow does not belong to company: ${workflowId}`);
+  }
+
+  let parentIssueId = typeof options?.parentIssueId === "string" && options.parentIssueId.trim()
+    ? options.parentIssueId.trim()
+    : "";
+  if (!parentIssueId && options?.createParentIssue) {
+    const parentIssue = await ctx.issues.create({
+      companyId,
+      description: typedWorkflowDefinition.data.description || `Workflow run: ${typedWorkflowDefinition.data.name}`,
+      title: `[${typedWorkflowDefinition.data.name}] Workflow run`,
+    });
+    parentIssueId = parentIssue.id;
+  }
+
+  const workflowRun = toWorkflowRunRecord(await createWorkflowRun(ctx, {
+    companyId,
+    parentIssueId: parentIssueId || undefined,
+    startedAt: new Date().toISOString(),
+    status: RUN_STATUSES.running,
+    workflowId: typedWorkflowDefinition.id,
+    workflowName: typedWorkflowDefinition.data.name,
+  }));
+
+  const agents = await ctx.agents.list({ companyId });
+  const agentsByName = new Map<string, AgentRecord>();
+  for (const agent of agents) {
+    agentsByName.set(agent.name, agent);
+  }
+
+  const pendingRootSteps: Array<{ stepDef: WorkflowStep; stepRun: WorkflowStepRunRecord }> = [];
+  for (const stepDef of typedWorkflowDefinition.data.steps) {
+    const agentNameHint = getStepAgentNameHint(stepDef);
+    const matchedAgent = agentNameHint ? agentsByName.get(agentNameHint) ?? null : null;
+    const resolvedAgent = matchedAgent
+      ? { agentId: matchedAgent.id, agentName: matchedAgent.name }
+      : await resolveStepAgent(ctx, companyId, stepDef, agentNameHint ?? undefined);
+
+    if (!resolvedAgent.agentName) {
+      throw new Error(`Unable to resolve step assignee for "${stepDef.id}"`);
+    }
+
+    const stepRun = toWorkflowStepRunRecord(await createStepRun(ctx, companyId, {
+      agentName: resolvedAgent.agentName,
+      retryCount: 0,
+      runId: workflowRun.id,
+      status: STEP_STATUSES.backlog,
+      stepId: stepDef.id,
+    }));
+
+    if (stepDef.dependsOn.length === 0 && stepDef.triggerOn !== "escalation") {
+      pendingRootSteps.push({ stepDef, stepRun });
+    }
+  }
+
+  const activatedStepIds: string[] = [];
+  for (const pending of pendingRootSteps) {
+    await activateBacklogStep(
+      ctx,
+      pending.stepRun,
+      pending.stepDef,
+      typedWorkflowDefinition.data.name,
+      companyId,
+      { parentIssueId: parentIssueId || undefined },
+    );
+    activatedStepIds.push(pending.stepDef.id);
+  }
+
+  ctx.logger.info("Started workflow run", {
+    activatedStepIds,
+    companyId,
+    parentIssueId: parentIssueId || null,
+    runId: workflowRun.id,
+    workflowId: typedWorkflowDefinition.id,
+    workflowName: typedWorkflowDefinition.data.name,
+  });
+
+  return {
+    activatedStepIds,
+    parentIssueId: parentIssueId || null,
+    runId: workflowRun.id,
+    workflowId: typedWorkflowDefinition.id,
+  };
+}
+
 async function advanceWorkflow(
   ctx: PluginContext,
   stepRunRecord: PluginEntityRecord,
   companyId: string,
 ): Promise<void> {
-  const completedStepRun = toStepRunRecord(stepRunRecord);
+  const completedStepRun = toWorkflowStepRunRecord(stepRunRecord);
   const workflowRun = await getWorkflowRun(ctx, completedStepRun.data.runId);
 
   if (!workflowRun) {
@@ -237,7 +396,7 @@
   }
 
   const typedWorkflowDefinition = toWorkflowDefinitionRecord(workflowDefinition);
-  const stepRuns = (await listStepRuns(ctx, typedWorkflowRun.id, companyId)).map(toStepRunRecord);
+  const stepRuns = (await listStepRuns(ctx, typedWorkflowRun.id, companyId)).map(toWorkflowStepRunRecord);
   const completed = new Set<string>();
   const failed = new Set<string>();
   const skipped = new Set<string>();
@@ -289,7 +448,7 @@
         continue;
       }
 
-      stepRun = toStepRunRecord(await createStepRun(ctx, companyId, {
+      stepRun = toWorkflowStepRunRecord(await createStepRun(ctx, companyId, {
         agentName: resolvedAgent.agentName,
         retryCount: 0,
         runId: typedWorkflowRun.id,
@@ -305,6 +464,7 @@
       stepDef,
       typedWorkflowRun.data.workflowName,
       companyId,
+      { parentIssueId: typedWorkflowRun.data.parentIssueId },
     );
   }
 
@@ -329,7 +489,7 @@
   ctx: PluginContext,
   workflowRun: WorkflowRunRecord,
   workflowDefinition: WorkflowDefinitionRecord,
-  sourceStepRun: WorkflowStepRecord,
+  sourceStepRun: WorkflowStepRunRecord,
   escalationTargetId: string,
   companyId: string,
 ): Promise<void> {
@@ -338,7 +498,7 @@
     throw new Error(`Escalation target step not found: ${escalationTargetId}`);
   }
 
-  const stepRuns = (await listStepRuns(ctx, workflowRun.id, companyId)).map(toStepRunRecord);
+  const stepRuns = (await listStepRuns(ctx, workflowRun.id, companyId)).map(toWorkflowStepRunRecord);
   let escalationStepRun = stepRuns.find((candidate) => candidate.data.stepId === escalationTargetId) ?? null;
   const resolvedAgent = await resolveStepAgent(
     ctx,
@@ -352,7 +512,7 @@
   }
 
   if (!escalationStepRun) {
-    escalationStepRun = toStepRunRecord(await createStepRun(ctx, companyId, {
+    escalationStepRun = toWorkflowStepRunRecord(await createStepRun(ctx, companyId, {
       agentName: resolvedAgent.agentName,
       retryCount: 0,
       runId: workflowRun.id,
@@ -374,14 +534,14 @@
       title: `${workflowRun.data.workflowName}: ${escalationStep.title}`,
     });
 
-    escalationStepRun = toStepRunRecord(await updateStepRun(ctx, escalationStepRun.id, {
+    escalationStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, escalationStepRun.id, {
       issueId: issue.id,
     }));
   }
 
   const shouldActivate = escalationStepRun.data.status === STEP_STATUSES.backlog;
   if (shouldActivate) {
-    escalationStepRun = toStepRunRecord(await updateStepRun(ctx, escalationStepRun.id, {
+    escalationStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, escalationStepRun.id, {
       status: STEP_STATUSES.todo,
     }));
   }
@@ -389,8 +549,8 @@
   if (shouldActivate) {
     await invokeAgentForStep(
       ctx,
+      escalationStepRun,
       escalationStep,
-      escalationStepRun.data.agentName,
       workflowRun.data.workflowName,
       companyId,
     );
@@ -421,7 +581,7 @@
     return;
   }
 
-  const typedStepRun = toStepRunRecord(stepRunRecord);
+  const typedStepRun = toWorkflowStepRunRecord(stepRunRecord);
   if (TERMINAL_STEP_STATUSES.has(typedStepRun.data.status)) {
     return;
   }
@@ -469,7 +629,7 @@
   if (options.allowRetry) {
     const retryInfo = getRetryInfo(typedWorkflowDefinition.data.steps, typedStepRun.data.stepId);
     if (retryInfo.shouldRetry && typedStepRun.data.retryCount < retryInfo.maxRetries) {
-      const retriedStepRun = toStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
+      const retriedStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
         completedAt: undefined,
         retryCount: typedStepRun.data.retryCount + 1,
         status: STEP_STATUSES.todo,
@@ -477,8 +637,8 @@
 
       await invokeAgentForStep(
         ctx,
+        retriedStepRun,
         stepDef,
-        retriedStepRun.data.agentName,
         typedWorkflowRun.data.workflowName,
         event.companyId,
       );
@@ -496,7 +656,7 @@
   }
 
   if (stepDef.onFailure === "skip") {
-    const skippedStepRun = toStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
+    const skippedStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
       completedAt: new Date().toISOString(),
       status: STEP_STATUSES.skipped,
     }));
@@ -556,7 +716,7 @@
       return;
     }
 
-    const escalatedStepRun = toStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
+    const escalatedStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, typedStepRun.id, {
       completedAt: new Date().toISOString(),
       status: STEP_STATUSES.escalated,
     }));
@@ -617,6 +777,29 @@
   }
 }
 
+function registerDataHandler(
+  ctx: PluginContext,
+  key: string,
+  handler: (params: Record<string, unknown>) => Promise<unknown>,
+): void {
+  const dataClient = ctx.data as {
+    handle?: (handlerKey: string, handlerFn: (params: Record<string, unknown>) => Promise<unknown>) => void;
+    register?: (handlerKey: string, handlerFn: (params: Record<string, unknown>) => Promise<unknown>) => void;
+  };
+
+  if (typeof dataClient.handle === "function") {
+    dataClient.handle(key, handler);
+    return;
+  }
+
+  if (typeof dataClient.register === "function") {
+    dataClient.register(key, handler);
+    return;
+  }
+
+  throw new Error("Plugin data client does not support handler registration");
+}
+
 const plugin = definePlugin({
   async setup(ctx: PluginContext) {
     ctx.events.on("issue.updated", async (event: PluginEvent) => {
@@ -638,7 +821,7 @@
           return;
         }
 
-        const stepRun = toStepRunRecord(stepRunRecord);
+        const stepRun = toWorkflowStepRunRecord(stepRunRecord);
         const payload = event.payload as { status?: string };
         const issueStatus = typeof payload.status === "string" ? payload.status : undefined;
 
@@ -657,7 +840,7 @@
           return;
         }
 
-        const updatedStepRun = toStepRunRecord(await updateStepRun(ctx, stepRun.id, {
+        const updatedStepRun = toWorkflowStepRunRecord(await updateStepRun(ctx, stepRun.id, {
           completedAt: nextStepStatus === STEP_STATUSES.done ? new Date().toISOString() : undefined,
           startedAt: nextStepStatus === STEP_STATUSES.inProgress
             ? stepRun.data.startedAt ?? new Date().toISOString()
@@ -687,11 +870,21 @@
 
     ctx.events.on("issue.created", async (event: PluginEvent) => {
       try {
+        const idempotencyKey = buildIdempotencyKey(event);
+        if (await checkIdempotency(ctx, idempotencyKey, event.companyId)) {
+          return;
+        }
+
         const payload = event.payload as {
           id?: string;
           parentId?: string;
           assigneeAgentId?: string;
         };
+        const issueId = typeof event.entityId === "string" && event.entityId.trim()
+          ? event.entityId.trim()
+          : typeof payload.id === "string" && payload.id.trim()
+            ? payload.id.trim()
+            : "";
 
         const parentId = typeof payload.parentId === "string" && payload.parentId.trim()
           ? payload.parentId.trim()
@@ -700,12 +893,34 @@
           ? payload.assigneeAgentId.trim()
           : "";
 
-        if (parentId || !assigneeAgentId) {
+        if (!issueId || parentId || !assigneeAgentId) {
+          await markIdempotency(ctx, idempotencyKey, event.companyId);
+          return;
+        }
+
+        const agent = await ctx.agents.get(assigneeAgentId, event.companyId);
+        const metadata = agent?.metadata as Record<string, unknown> | undefined;
+        const defaultParentIssueId = typeof metadata?.defaultParentIssueId === "string" && metadata.defaultParentIssueId.trim()
+          ? metadata.defaultParentIssueId.trim()
+          : "";
+
+        if (!defaultParentIssueId) {
+          await markIdempotency(ctx, idempotencyKey, event.companyId);
           return;
         }
 
-        ctx.logger.info("parentId filler: issue created without parent", {
-          issueId: event.entityId ?? payload.id ?? null,
+        await ctx.issues.update(
+          issueId,
+          { parentId: defaultParentIssueId } as IssueUpdatePatch,
+          event.companyId,
+        );
+        await markIdempotency(ctx, idempotencyKey, event.companyId);
+
+        ctx.logger.info("parentId filler: populated issue parentId from agent metadata", {
+          assigneeAgentId,
+          companyId: event.companyId,
+          issueId,
+          parentId: defaultParentIssueId,
         });
       } catch (error) {
         ctx.logger.warn("Failed to handle issue.created event", {
@@ -744,7 +959,23 @@
       await runReconciler(ctx);
     });
 
-    ctx.data.register("workflow-overview", async (params: Record<string, unknown>) => {
+    registerDataHandler(ctx, "start-workflow", async (params: Record<string, unknown>) => {
+      const workflowId = typeof params.workflowId === "string" ? params.workflowId.trim() : "";
+      const companyId = typeof params.companyId === "string" ? params.companyId.trim() : "";
+      const parentIssueId = typeof params.parentIssueId === "string" ? params.parentIssueId.trim() : "";
+      const createParentIssue = params.createParentIssue === true;
+
+      if (!workflowId || !companyId) {
+        throw new Error("start-workflow requires workflowId and companyId");
+      }
+
+      return await startWorkflow(ctx, workflowId, companyId, {
+        createParentIssue,
+        parentIssueId: parentIssueId || undefined,
+      });
+    });
+
+    registerDataHandler(ctx, "workflow-overview", async (params: Record<string, unknown>) => {
       try {
         const companyId = typeof params.companyId === "string" ? params.companyId.trim() : "";
         if (!companyId) {
@@ -782,7 +1013,7 @@
       }
     });
 
-    ctx.data.register("workflow-run-detail", async (params: Record<string, unknown>) => {
+    registerDataHandler(ctx, "workflow-run-detail", async (params: Record<string, unknown>) => {
       try {
         const runId = typeof params.runId === "string" ? params.runId.trim() : "";
         if (!runId) {
@@ -807,7 +1038,7 @@
             ...typedWorkflowRun.data,
           },
           stepRuns: stepRuns.map((record: PluginEntityRecord) => {
-            const stepRun = toStepRunRecord(record);
+            const stepRun = toWorkflowStepRunRecord(record);
             return {
               id: stepRun.id,
               status: stepRun.status,
diff --git a/paperclip-addon/plugins/workflow-engine/src/workflow-store.ts b/paperclip-addon/plugins/workflow-engine/src/workflow-store.ts
index 5871a2082258cebcfe8b69ebafbb0814cf303d50..27791ef142b641d85b6ef7e35a6746218a64d941
--- a/paperclip-addon/plugins/workflow-engine/src/workflow-store.ts
+++ b/paperclip-addon/plugins/workflow-engine/src/workflow-store.ts
@@ -44,7 +44,14 @@
 
 type TypedEntityRecord<T> = Omit<PluginEntityRecord, "data"> & { data: T };
 
-function toTypedRecord<T>(record: PluginEntityRecord): TypedEntityRecord<T> {
+function toTypedRecord<T>(
+  record: PluginEntityRecord,
+  entityType: string,
+): TypedEntityRecord<T> {
+  if (record.entityType !== entityType) {
+    throw new Error(`Expected entity type "${entityType}", got "${record.entityType}"`);
+  }
+
   return record as TypedEntityRecord<T>;
 }
 
@@ -67,13 +74,39 @@
   id: string,
   entityType: string,
 ): Promise<TypedEntityRecord<T> | null> {
+  const listQuery = {
+    entityType,
+    id,
+    limit: 1,
+  } as Record<string, unknown>;
+
+  const listed = await ctx.entities.list(
+    listQuery as Parameters<PluginContext["entities"]["list"]>[0],
+  );
+  const listedRecord = listed.find(
+    (record: PluginEntityRecord) => record.id === id,
+  ) ?? null;
+
+  if (listedRecord) {
+    if (listedRecord.entityType !== entityType) {
+      return null;
+    }
+
+    return toTypedRecord<T>(listedRecord, entityType);
+  }
+
+  // Fallback: some host SDK variants ignore `id` on entities.list().
+  // Keep defensive type verification on direct get() until id-filter support is universal.
   const record = await ctx.entities.get(id);
+  if (!record) {
+    return null;
+  }
 
-  if (!record || record.entityType !== entityType) {
+  if (record.entityType !== entityType) {
     return null;
   }
 
-  return toTypedRecord<T>(record);
+  return toTypedRecord<T>(record, entityType);
 }
 
 async function requireEntityByType<T>(
diff --git a/paperclip-addon/plugins/workflow-engine/src/workflow-utils.ts b/paperclip-addon/plugins/workflow-engine/src/workflow-utils.ts
new file mode 100644
index 0000000000000000000000000000000000000000..ec1cf6f6b9aa722b126b58c0a728b4e538ad1494
--- /dev/null
+++ b/paperclip-addon/plugins/workflow-engine/src/workflow-utils.ts
@@ -0,0 +1,62 @@
+import type { PluginEntityRecord } from "@paperclipai/plugin-sdk";
+
+import { STEP_STATUSES } from "./constants.js";
+import type { WorkflowStep } from "./dag-engine.js";
+import type {
+  WorkflowDefinition,
+  WorkflowRun,
+  WorkflowStepRun,
+} from "./workflow-store.js";
+
+export type WorkflowRunRecord = PluginEntityRecord & { data: WorkflowRun };
+export type WorkflowStepRunRecord = PluginEntityRecord & { data: WorkflowStepRun };
+export type WorkflowDefinitionRecord = PluginEntityRecord & { data: WorkflowDefinition };
+type StepAgentMetadata = WorkflowStep & {
+  agent?: string;
+  agentName?: string;
+  assigneeAgentName?: string;
+};
+
+export const TERMINAL_STEP_STATUSES = new Set<string>([
+  STEP_STATUSES.done,
+  STEP_STATUSES.failed,
+  STEP_STATUSES.skipped,
+  STEP_STATUSES.escalated,
+]);
+
+export function toWorkflowRunRecord(record: PluginEntityRecord): WorkflowRunRecord {
+  return record as WorkflowRunRecord;
+}
+
+export function toWorkflowStepRunRecord(record: PluginEntityRecord): WorkflowStepRunRecord {
+  return record as WorkflowStepRunRecord;
+}
+
+export function toWorkflowDefinitionRecord(record: PluginEntityRecord): WorkflowDefinitionRecord {
+  return record as WorkflowDefinitionRecord;
+}
+
+export function findStepDefinition(definition: WorkflowDefinitionRecord, stepId: string): WorkflowStep | null {
+  return definition.data.steps.find((step: WorkflowStep) => step.id === stepId) ?? null;
+}
+
+export function getStepAgentNameHint(stepDef: WorkflowStep): string | null {
+  const stepMeta = stepDef as StepAgentMetadata;
+  const candidates = [stepMeta.agentName, stepMeta.agent, stepMeta.assigneeAgentName];
+
+  for (const candidate of candidates) {
+    if (typeof candidate === "string" && candidate.trim()) {
+      return candidate.trim();
+    }
+  }
+
+  return null;
+}
+
+export function getStepAgentName(stepRun: WorkflowStepRunRecord, stepDef: WorkflowStep): string | null {
+  if (typeof stepRun.data.agentName === "string" && stepRun.data.agentName.trim()) {
+    return stepRun.data.agentName.trim();
+  }
+
+  return getStepAgentNameHint(stepDef);
+}

tokens used
218,070
요청하신 코드리뷰 반영사항 7건을 모두 적용했습니다.

1. `activateBacklogStep()` 이슈 생성 추가  
- [worker.ts](/Users/kwak/Projects/paperclip/paperclip-addon/plugins/workflow-engine/src/worker.ts)  
- backlog step 활성화 시 `ctx.issues.create(...)`로 이슈 생성 후 `issueId`, `status: todo`, `startedAt`을 `stepRun`에 저장하도록 변경했습니다.  
- 생성 직후 `ctx.issues.update(..., { status: "todo" })`도 넣어 상태를 명시적으로 맞췄습니다.

2. `startWorkflow()` 함수 추가 + data handler 등록  
- [worker.ts](/Users/kwak/Projects/paperclip/paperclip-addon/plugins/workflow-engine/src/worker.ts)  
- `startWorkflow(ctx, workflowId, companyId, options)` 추가:  
  - `WorkflowRun` 생성 (`running`)  
  - 모든 step에 대해 `WorkflowStepRun` 생성 (`backlog`)  
  - `ctx.agents.list` 기반 name→agent resolve  
  - `dependsOn` 없는 일반 step 즉시 `activateBacklogStep()` 호출  
  - 옵션으로 parent issue 생성(`createParentIssue`) 및 연결  
- data handler `start-workflow` 등록 완료.  
- `ctx.data.handle`/`ctx.data.register` 모두 대응하도록 `registerDataHandler()` 유틸을 추가했습니다.

3. `ctx.entities.get(id)` 안전 조회 패턴 개선  
- [workflow-store.ts](/Users/kwak/Projects/paperclip/paperclip-addon/plugins/workflow-engine/src/workflow-store.ts)  
- `getEntityByType()`에서 우선 `ctx.entities.list({ entityType, id, limit: 1 })` 패턴 사용, 미지원 SDK 대비 `get(id)` fallback + 한계 주석 추가했습니다.

4. 중복 헬퍼 shared utils 추출  
- 신규 [workflow-utils.ts](/Users/kwak/Projects/paperclip/paperclip-addon/plugins/workflow-engine/src/workflow-utils.ts)  
- `toWorkflowRunRecord`, `toWorkflowStepRunRecord`, `toWorkflowDefinitionRecord`, `findStepDefinition`, `getStepAgentName`, `getStepAgentNameHint`, `TERMINAL_STEP_STATUSES` 추출.  
- [worker.ts](/Users/kwak/Projects/paperclip/paperclip-addon/plugins/workflow-engine/src/worker.ts), [reconciler.ts](/Users/kwak/Projects/paperclip/paperclip-addon/plugins/workflow-engine/src/reconciler.ts)에서 import로 교체.

5. parentId filler 구현  
- [worker.ts](/Users/kwak/Projects/paperclip/paperclip-addon/plugins/workflow-engine/src/worker.ts)  
- `issue.created` 핸들러에서:  
  - parentId 없고 assigneeAgentId 있으면  
  - `ctx.agents.get(agentId, companyId)`의 `metadata.defaultParentIssueId` 확인  
  - `ctx.issues.update(issueId, { parentId }, companyId)` 수행  
- idempotency key 체크/마킹도 추가했습니다.

6. Agent Sessions 연동  
- [worker.ts](/Users/kwak/Projects/paperclip/paperclip-addon/plugins/workflow-engine/src/worker.ts), [dag-engine.ts](/Users/kwak/Projects/paperclip/paperclip-addon/plugins/workflow-engine/src/dag-engine.ts)  
- `WorkflowStep.sessionMode?: "fresh" | "reuse"` 추가.  
- `invokeAgentForStep()`에서:  
  - `fresh`(기본): `ctx.agents.invoke`  
  - `reuse`: 기존 `stepRun.sessionId` 있으면 `ctx.agents.sessions.sendMessage`, 없으면 `sessions.create` 후 `sessionId` 저장하고 `sendMessage`

7. 타입 캐스팅 정리 (`toTypedRecord` guard)  
- [workflow-store.ts](/Users/kwak/Projects/paperclip/paperclip-addon/plugins/workflow-engine/src/workflow-store.ts)  
- `toTypedRecord<T>(record, entityType)`로 변경하고 runtime guard 추가했습니다.

검증 결과  
- `npm run typecheck`, `npm run build` 실행 시 현재 환경에 `@paperclipai/plugin-sdk`, `react` 타입 의존성이 없어 실패했습니다.  
- 다만 이번 변경으로 인한 추가 TS 에러(로컬 변경 코드 관련)는 제거되어, 남은 오류는 의존성 미설치 오류만 확인되었습니다.

다음 단계  
1. 워크스페이스 의존성 설치/연결 후 `plugins/workflow-engine`에서 `npm run typecheck && npm run build` 재검증.
